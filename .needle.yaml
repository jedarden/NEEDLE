# NEEDLE workspace configuration
# Workspace: /home/coding/NEEDLE

agent:
  default: claude
  timeout: 3600

strands:
  explore:
    enabled: true
    workspaces:
      - /tmp/needle-remote-e2e

prompt:
  context_files: []
  instructions: |
    You are working on the NEEDLE project.
    Follow the conventions in CLAUDE.md and the plan in plan/plan.md.
