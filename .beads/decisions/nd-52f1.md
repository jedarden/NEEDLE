# ADR: Use Bash

**Decision ID:** nd-52f1
**Date:** 2026-08-07

## Context

Analysis of options during task execution

## Alternatives Considered

1. Considered alternatives

## Decision

Use Bash

## Rationale

The help clearly says: `bf label add [OPTIONS] --label <LABEL>... <ID>` with `<ID>` at the end. But when I test it, I need to put the ID first. Let me test `bf` directly instead of `br`:

## Outcome

Decision implemented (success)
