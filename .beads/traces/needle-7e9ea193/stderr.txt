shell-init: error retrieving current directory: getcwd: cannot access parent directories: No such file or directory
chdir: error retrieving current directory: getcwd: cannot access parent directories: No such file or directory
OpenAI Codex v0.148.0
--------
workdir: /home/coding/NEEDLE
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: xhigh
reasoning summaries: none
session id: 01a0238c-e013-7003-91d4-367ff032a344
--------
user
[needle:codex-gpt-5.6-sol-xhigh-sol-claudeprint:needle-7e9ea193:explore]
## Task

Fix Success classification to depend on verification, not exit code

## Description

Child 1 of needle-3386daef. Fix the root defect: Success must mean verification passed, not just that the agent exited 0.

CURRENT BUG: classify(0, false) == Outcome::Success. Exit code 0 means 'the agent process terminated normally', but NEEDLE incorrectly reads it as 'the work is correct'.

FIX REQUIRED:
- Modify classify() in outcome/mod.rs to depend on verification result, not exit code alone
- An agent that exits 0 but whose verification FAILS must produce Outcome::Failure, not Success
- Failure outcome must release the bead and increment failure count
- This ensures verification is the gate, not the exit signal

ACCEPTANCE CRITERIA:
- classify() returns Success only when verification passes, regardless of exit code
- classify() returns Failure when verification fails, even if exit code is 0
- All existing tests pass (or are updated to match new semantics)
- A test verifies: exit_code=0, verification=false => Failure

This is foundational work - Child 2 (structural enforcement) builds on this fixed classification.



## Workspace

/home/coding/NEEDLE

## Context Files

(no context files found)

## Instructions

(no workspace instructions)

Complete the task described above. When finished:
- **If you changed files, commit and push them.** `git commit`, then `git push`. Real
  changes are source, tests, config, or documentation the task actually asked for.
- **If the task needed no file changes, do NOT manufacture a commit.** Verification-only
  work, work that turned out to be already done, and work you found blocked are all
  legitimate outcomes with nothing to commit. Record the finding on the bead instead:

`bead update needle-7e9ea193 --notes "what you checked and what you found"`

  Never create `notes/needle-7e9ea193.md`, a summary, a report, or a status file to satisfy a
  commit requirement. Status belongs on the bead, not in git history. A commit whose only
  changes are under `notes/` or `.beads/` does not count as shipped work and will be
  rejected.
- Close the bead:

`bead close needle-7e9ea193 --reason "..."`

If you cannot complete the task:
- Do NOT close the bead
- The bead will be automatically released for retry

Bead ID: needle-7e9ea193
2026-08-21T09:00:30.952943Z ERROR codex_rmcp_client::oauth::refresh_transaction: error=failed to refresh OAuth tokens for server ibkr: OAuth refresh token was rejected: Server returned error response: invalid_grant: Refresh token is invalid or expired
hook: SessionStart
hook: SessionStart Completed
codex
I’ll trace the current classification and its downstream bead/failure handling, make the smallest semantic and test changes, then run the repository’s required fast definition-of-done before committing, pushing, and closing the bead.
exec
/bin/bash -lc "git status --short && find .. -name AGENTS.md -print && rg -n \"fn classify|classify\\(|Outcome::Success|verification\" src tests --glob '*.rs' | head -n 240 && git remote -v && git branch --show-current && git log -5 --oneline" in /home/coding/NEEDLE
 exited 1 in 2800ms:
 M .beads/bypasses.jsonl
 M .beads/checkpoint/current.json
 M .beads/checkpoint/forensic.jsonl
 M .beads/checkpoint/previous.json
 M .beads/events.jsonl
 M .beads/heartbeats.jsonl
 M .beads/traces/needle-068b926f/metadata.json
 M .beads/traces/needle-068b926f/stdout.txt
 M .beads/traces/needle-12433d46/metadata.json
 M .beads/traces/needle-12433d46/stderr.txt
 M .beads/traces/needle-12433d46/stdout.txt
 M .beads/traces/needle-1ef0ec6e/metadata.json
 M .beads/traces/needle-1ef0ec6e/stdout.txt
 M .beads/traces/needle-208840e4/metadata.json
 M .beads/traces/needle-208840e4/stdout.txt
 M .beads/traces/needle-24e42694/metadata.json
 M .beads/traces/needle-24e42694/stderr.txt
 M .beads/traces/needle-24e42694/stdout.txt
 M .beads/traces/needle-3a6e1ecd/metadata.json
 M .beads/traces/needle-3a6e1ecd/stdout.txt
 M .beads/traces/needle-44a0f603/metadata.json
 M .beads/traces/needle-44a0f603/stdout.txt
 M .beads/traces/needle-4b602eed/metadata.json
 M .beads/traces/needle-4b602eed/stderr.txt
 M .beads/traces/needle-4b602eed/stdout.txt
 M .beads/traces/needle-4c48325a/metadata.json
 M .beads/traces/needle-4c48325a/stderr.txt
 M .beads/traces/needle-4c48325a/stdout.txt
 M .beads/traces/needle-51084068/metadata.json
 M .beads/traces/needle-51084068/stdout.txt
 M .beads/traces/needle-597972f3/metadata.json
 M .beads/traces/needle-597972f3/stdout.txt
 M .beads/traces/needle-5c826b55/metadata.json
 M .beads/traces/needle-5c826b55/stdout.txt
 M .beads/traces/needle-615b57e9/metadata.json
 M .beads/traces/needle-615b57e9/stdout.txt
 M .beads/traces/needle-677fcad2/metadata.json
 M .beads/traces/needle-677fcad2/stdout.txt
 M .beads/traces/needle-67f3bb56/metadata.json
 M .beads/traces/needle-67f3bb56/stdout.txt
 M .beads/traces/needle-6b4aabcf/metadata.json
 M .beads/traces/needle-6b4aabcf/stdout.txt
 M .beads/traces/needle-6f242187/metadata.json
 M .beads/traces/needle-6f242187/stdout.txt
 M .beads/traces/needle-6f50ee12/metadata.json
 M .beads/traces/needle-6f50ee12/stdout.txt
 M .beads/traces/needle-791e962b/metadata.json
 M .beads/traces/needle-791e962b/stdout.txt
 M .beads/traces/needle-8cb71c7c/metadata.json
 M .beads/traces/needle-8cb71c7c/stderr.txt
 M .beads/traces/needle-8cb71c7c/stdout.txt
 M .beads/traces/needle-99c51a9d/metadata.json
 M .beads/traces/needle-99c51a9d/stdout.txt
 M .beads/traces/needle-b3617592/metadata.json
 M .beads/traces/needle-b3617592/stdout.txt
 M .beads/traces/needle-b3dc9203/metadata.json
 M .beads/traces/needle-b3dc9203/stdout.txt
 M .beads/traces/needle-b93a96bd/metadata.json
 M .beads/traces/needle-b93a96bd/stdout.txt
 M .beads/traces/needle-c0464426/metadata.json
 M .beads/traces/needle-c0464426/stdout.txt
 M .beads/traces/needle-c3e2cb9a/metadata.json
 M .beads/traces/needle-c3e2cb9a/stdout.txt
 M .beads/traces/needle-c8c008be/metadata.json
 M .beads/traces/needle-c8c008be/stdout.txt
 M .beads/traces/needle-ceea8c53/metadata.json
 M .beads/traces/needle-ceea8c53/stdout.txt
 M .beads/traces/needle-cfe0ad63/metadata.json
 M .beads/traces/needle-cfe0ad63/stdout.txt
 M .beads/traces/needle-d1b2ee0d/metadata.json
 M .beads/traces/needle-d1b2ee0d/stdout.txt
 M .beads/traces/needle-de6bb8ad/metadata.json
 M .beads/traces/needle-de6bb8ad/stdout.txt
 M .beads/traces/needle-e9df85fc/metadata.json
 M .beads/traces/needle-e9df85fc/stdout.txt
 M .beads/traces/needle-ea26063c/metadata.json
 M .beads/traces/needle-ea26063c/stdout.txt
 M .beads/traces/needle-ea87796e/metadata.json
 M .beads/traces/needle-ea87796e/stdout.txt
 M .beads/traces/needle-ebede907/metadata.json
 M .beads/traces/needle-ebede907/stdout.txt
 M .beads/traces/needle-f78eebbb/metadata.json
 M .beads/traces/needle-f78eebbb/stdout.txt
 M .needle-predispatch-sha
 M .needle.yaml
 M install.sh
 M src/outcome/mod.rs
 M tests/installer/test_checksum_verification.sh
 M tests/integration_tests.rs
 M tests/property_tests.rs
?? .beads/checkpoint/objects/gen-0ac480b50a206d03b43ce170b8123a09.jsonl
?? .beads/checkpoint/objects/gen-127565bff08a14d985fa3d9a97946a71.jsonl
?? .beads/checkpoint/objects/gen-13d3b95635a386827601d2e9d5e27794.jsonl
?? .beads/checkpoint/objects/gen-141ad94764b7118b62fa9d64714595c4.jsonl
?? .beads/checkpoint/objects/gen-16ccbbe3e13f0e5d9bc880321f9966e9.jsonl
?? .beads/checkpoint/objects/gen-21d8dce830b84bd86167dd434026cec7.jsonl
?? .beads/checkpoint/objects/gen-2ba35efc49d309b7c77bc5c9a129d1c3.jsonl
?? .beads/checkpoint/objects/gen-2bc2f49d4707fc7d7299d5d4c3b4cbf2.jsonl
?? .beads/checkpoint/objects/gen-5a4a8a38eef8912ddb0c5687f69d8e31.jsonl
?? .beads/checkpoint/objects/gen-65a404a0f0f034cf16f3f077fa229090.jsonl
?? .beads/checkpoint/objects/gen-686af9927f77ee7f412e7556ab1414b2.jsonl
?? .beads/checkpoint/objects/gen-7494b0f63252ef9cb4ff3466999d093e.jsonl
?? .beads/checkpoint/objects/gen-81001dc65cba6d1255d5867973893588.jsonl
?? .beads/checkpoint/objects/gen-84404631582427855a1be476fabceb16.jsonl
?? .beads/checkpoint/objects/gen-8518d601f31914e3fb5476ff42931655.jsonl
?? .beads/checkpoint/objects/gen-8bff3da9de5c1086e18adfc2d9dad0cf.jsonl
?? .beads/checkpoint/objects/gen-8cdb48e31ab3db586d8b631c421fb293.jsonl
?? .beads/checkpoint/objects/gen-9035f55bcfb453024d776318ff08c68d.jsonl
?? .beads/checkpoint/objects/gen-9163a2a453aad9d00f6688f275c6bee2.jsonl
?? .beads/checkpoint/objects/gen-98696b1584316647bbbc8285deab1a94.jsonl
?? .beads/checkpoint/objects/gen-9d193168794fc4acc125a815e16cc6b7.jsonl
?? .beads/checkpoint/objects/gen-a0c1b968a009fa232d5b0ea2ff6c1699.jsonl
?? .beads/checkpoint/objects/gen-acd3c8e1e60b81aa65bb4d9aa012ceea.jsonl
?? .beads/checkpoint/objects/gen-ba734699cd99f5a60c0864dd204fc098.jsonl
?? .beads/checkpoint/objects/gen-cdca3863621bf43c66d9ae53785632bb.jsonl
?? .beads/checkpoint/objects/gen-cfacaa57e377c31c673fe86e50deb0f6.jsonl
?? .beads/checkpoint/objects/gen-d5b7861651300ac71a4ec9da012240db.jsonl
?? .beads/checkpoint/objects/gen-dd94f8a662af4d52a852a4b492a4791e.jsonl
?? .beads/checkpoint/objects/gen-e3170f74b3daaa008d24a95a5eed1a7c.jsonl
?? .beads/checkpoint/objects/gen-e920c560183f0438ab863dba937899f0.jsonl
?? .beads/checkpoint/objects/gen-ea62841db85c4e1b952acf803c5820cf.jsonl
?? .beads/checkpoint/objects/gen-ed4623fa89bd1020b0ebbe79b3066821.jsonl
?? .beads/checkpoint/objects/gen-ee988d49cb20ec21f5d6ddb651e20ee5.jsonl
?? .beads/checkpoint/objects/gen-f17d6fc376e80e044210b356d5661446.jsonl
?? .beads/checkpoint/objects/gen-f49f5b1f8a0a7e500b60049ad63ab671.jsonl
?? .beads/checkpoint/objects/gen-f8030ed02cc5bfb5734e010ccaba32bc.jsonl
?? .beads/traces/needle-018e29a7/
?? .beads/traces/needle-068b926f/trace.jsonl
?? .beads/traces/needle-092b40ea/
?? .beads/traces/needle-12433d46/trace.jsonl
?? .beads/traces/needle-161e49b7/
?? .beads/traces/needle-1ef0ec6e/trace.jsonl
?? .beads/traces/needle-208840e4/trace.jsonl
?? .beads/traces/needle-22f56650/
?? .beads/traces/needle-24e42694/trace.jsonl
?? .beads/traces/needle-25774c75/
?? .beads/traces/needle-3386daef/trace.jsonl
?? .beads/traces/needle-3a6e1ecd/trace.jsonl
?? .beads/traces/needle-3bd9d945/
?? .beads/traces/needle-44a0f603/trace.jsonl
?? .beads/traces/needle-48adb04c/
?? .beads/traces/needle-4b602eed/trace.jsonl
?? .beads/traces/needle-4c48325a/trace.jsonl
?? .beads/traces/needle-4ddfbf70/
?? .beads/traces/needle-51084068/trace.jsonl
?? .beads/traces/needle-58d9752d/
?? .beads/traces/needle-597972f3/trace.jsonl
?? .beads/traces/needle-5c826b55/trace.jsonl
?? .beads/traces/needle-615b57e9/trace.jsonl
?? .beads/traces/needle-677fcad2/trace.jsonl
?? .beads/traces/needle-67f3bb56/trace.jsonl
?? .beads/traces/needle-6f242187/trace.jsonl
?? .beads/traces/needle-6f50ee12/trace.jsonl
?? .beads/traces/needle-791e962b/trace.jsonl
?? .beads/traces/needle-7e9ea193/
?? .beads/traces/needle-8cb71c7c/trace.jsonl
?? .beads/traces/needle-99c51a9d/trace.jsonl
?? .beads/traces/needle-9c8640b7/
?? .beads/traces/needle-b3617592/trace.jsonl
?? .beads/traces/needle-b3dc9203/trace.jsonl
?? .beads/traces/needle-b93a96bd/trace.jsonl
?? .beads/traces/needle-c0464426/trace.jsonl
?? .beads/traces/needle-c3e2cb9a/trace.jsonl
?? .beads/traces/needle-c8c008be/trace.jsonl
?? .beads/traces/needle-ceea8c53/trace.jsonl
?? .beads/traces/needle-cfe0ad63/trace.jsonl
?? .beads/traces/needle-d1b2ee0d/trace.jsonl
?? .beads/traces/needle-de6bb8ad/trace.jsonl
?? .beads/traces/needle-e9df85fc/trace.jsonl
?? .beads/traces/needle-ea26063c/trace.jsonl
?? .beads/traces/needle-ea87796e/trace.jsonl
?? .beads/traces/needle-ebede907/trace.jsonl
?? .beads/traces/needle-f78eebbb/trace.jsonl
?? verification-failing-tests-list-2026-08-20.md
../.local/share/Trash/files/claudepr-1a29fae3-index.nV4KoC/AGENTS.md
../.local/share/Trash/files/claude-print-v020-gate.VQ7TNM/AGENTS.md
find: ‘../.local/share/docker/volumes/b006a21209eae85011ee9064418b63d8d33acf62db1dd123396acc05b43412fa/_data’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/100/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/104/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/61/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29899/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33131/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33865/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/101/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/101/fs/home/nonroot’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32918/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/110/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/47/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32896/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/114/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29915/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33138/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32916/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32908/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/46/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/97/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/108/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/108/fs/var/cache/apt/archives/partial’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/18/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33860/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33860/fs/var/cache/apt/archives/partial’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/45/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/54/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32129/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/39/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33147/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32979/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33121/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33863/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/111/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32147/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32909/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/113/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32160/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32917/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32131/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32154/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/51/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32919/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/9/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33128/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/50/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/102/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/17/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/34/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33864/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/115/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/15/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33137/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32920/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/105/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33123/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32894/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33129/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/14/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32139/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/19/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/7/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32980/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32907/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29914/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29903/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/13/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32124/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32910/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33869/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32145/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/37/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/37/fs/var/cache/apt/archives/partial’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29901/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33144/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33871/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33867/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33122/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29898/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/31880/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32987/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/57/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/41/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/11/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29902/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32985/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32187/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33/fs/var/cache/apt/archives/partial’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/58/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/48/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29904/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29904/fs/var/cache/apt/archives/partial’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33873/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32986/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33120/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/55/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33862/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33136/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29910/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/49/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/106/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/56/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/36/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29911/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/44/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/96/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33139/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32899/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33874/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33868/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32895/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32900/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29900/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33130/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29908/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/40/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33143/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32893/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32893/fs/var/lib/apt/lists/partial’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32893/fs/var/cache/apt/archives/partial’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32911/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/112/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33861/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/43/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/31879/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29906/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/98/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33145/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29905/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33870/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/42/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32897/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/109/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/60/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33872/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/38/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/62/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/31882/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29913/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/16/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/53/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/12/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/10/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33146/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32981/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/31881/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32120/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33866/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/99/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32138/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29912/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29907/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32898/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33140/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29909/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/103/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/59/work/work’: Permission denied
../declarative-config/.claude/worktrees/agent-a54c92149120be9c7/k8s/apexalgo-iad/AGENTS.md
../declarative-config/.claude/worktrees/agent-a1ec9db3e206474d0/k8s/apexalgo-iad/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/nixos/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/ardenone-manager/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/iad-ci/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/apexalgo-iad/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/ord-devimprint/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/iad-kalshi/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/rs-manager/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/ardenone-cluster/devpod/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/ardenone-cluster/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/iad-options/AGENTS.md
../declarative-config/AGENTS.md
../declarative-config/nixos/AGENTS.md
../declarative-config/k8s/AGENTS.md
../declarative-config/k8s/ardenone-manager/AGENTS.md
../declarative-config/k8s/iad-ci/AGENTS.md
../declarative-config/k8s/apexalgo-iad/AGENTS.md
../declarative-config/k8s/ord-devimprint/AGENTS.md
../declarative-config/k8s/iad-kalshi/AGENTS.md
../declarative-config/k8s/rs-manager/AGENTS.md
../declarative-config/k8s/ardenone-cluster/devpod/AGENTS.md
../declarative-config/k8s/ardenone-cluster/AGENTS.md
../declarative-config/k8s/iad-options/AGENTS.md
../perplexity-jedarden/slides/node_modules/.pnpm/cytoscape@3.34.0/node_modules/cytoscape/AGENTS.md
../.cargo/git/checkouts/needle-c229d4d3b22a5d4a/c63dc9e/AGENTS.md
../.cargo/git/checkouts/beads_rust-18649610c6bb6d7c/29afe25/AGENTS.md
../.cargo/git/checkouts/beads_rust-18649610c6bb6d7c/29afe25/docs/agent/AGENTS.md
../.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zerocopy-0.8.48/AGENTS.md
../.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zerocopy-0.8.54/AGENTS.md
../.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zerocopy-0.8.52/AGENTS.md
../.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zerocopy-0.8.56/AGENTS.md
../.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zerocopy-0.8.47/AGENTS.md
../.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zerocopy-0.8.42/AGENTS.md
../AGENTS.md
../scratch/claude-print-home-verify.7oClmR/AGENTS.md
../scratch/SEAM-phase6a-auth-mount.Mr0I1n/AGENTS.md
../scratch/esphome-source/AGENTS.md
../scratch/herdr-source-diagnosis/AGENTS.md
../scratch/herdr-source-diagnosis/vendor/libghostty-vt/AGENTS.md
../scratch/herdr-source-diagnosis/vendor/libghostty-vt/src/inspector/AGENTS.md
../scratch/herdr-source-diagnosis/vendor/libghostty-vt/src/terminal/apc/glyph/AGENTS.md
../scratch/herdr-source-diagnosis/vendor/libghostty-vt/src/terminal/c/AGENTS.md
../scratch/herdr-source-diagnosis/vendor/libghostty-vt/src/terminal/compress/AGENTS.md
../scratch/herdr-source-diagnosis/vendor/libghostty-vt/src/benchmark/AGENTS.md
../scratch/NEEDLE-telemetry/AGENTS.md
../scratch/claude-print-as4.AhNnDZ/AGENTS.md
../scratch/forgejo-cutover-clone.8Mq9O1/repo/AGENTS.md
../scratch/forgejo-cutover-clone.8Mq9O1/repo/nixos/AGENTS.md
../scratch/forgejo-cutover-clone.8Mq9O1/repo/k8s/AGENTS.md
../scratch/forgejo-cutover-clone.8Mq9O1/repo/k8s/ardenone-manager/AGENTS.md
../scratch/forgejo-cutover-clone.8Mq9O1/repo/k8s/iad-ci/AGENTS.md
../scratch/forgejo-cutover-clone.8Mq9O1/repo/k8s/apexalgo-iad/AGENTS.md
../scratch/forgejo-cutover-clone.8Mq9O1/repo/k8s/ord-devimprint/AGENTS.md
../scratch/forgejo-cutover-clone.8Mq9O1/repo/k8s/iad-kalshi/AGENTS.md
../scratch/forgejo-cutover-clone.8Mq9O1/repo/k8s/rs-manager/AGENTS.md
../scratch/forgejo-cutover-clone.8Mq9O1/repo/k8s/ardenone-cluster/devpod/AGENTS.md
../scratch/forgejo-cutover-clone.8Mq9O1/repo/k8s/ardenone-cluster/AGENTS.md
../scratch/forgejo-cutover-clone.8Mq9O1/repo/k8s/iad-options/AGENTS.md
../scratch/claude-print-config.vbt8ln/AGENTS.md
../scratch/claude-print-91ed83ea-head-lsaR84/AGENTS.md
../scratch/acb-ci-verify.GH80av/AGENTS.md
../scratch/acb-ci-verify.GH80av/nixos/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/ardenone-manager/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/iad-ci/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/apexalgo-iad/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/ord-devimprint/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/iad-kalshi/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/rs-manager/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/ardenone-cluster/devpod/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/ardenone-cluster/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/iad-options/AGENTS.md
../scratch/dc-fleet-ready.M3ds8f/AGENTS.md
../scratch/dc-fleet-ready.M3ds8f/nixos/AGENTS.md
../scratch/dc-fleet-ready.M3ds8f/k8s/AGENTS.md
../scratch/dc-fleet-ready.M3ds8f/k8s/ardenone-manager/AGENTS.md
../scratch/dc-fleet-ready.M3ds8f/k8s/iad-ci/AGENTS.md
../scratch/dc-fleet-ready.M3ds8f/k8s/apexalgo-iad/AGENTS.md
../scratch/dc-fleet-ready.M3ds8f/k8s/ord-devimprint/AGENTS.md
../scratch/dc-fleet-ready.M3ds8f/k8s/iad-kalshi/AGENTS.md
../scratch/dc-fleet-ready.M3ds8f/k8s/rs-manager/AGENTS.md
../scratch/dc-fleet-ready.M3ds8f/k8s/ardenone-cluster/devpod/AGENTS.md
../scratch/dc-fleet-ready.M3ds8f/k8s/ardenone-cluster/AGENTS.md
../scratch/dc-fleet-ready.M3ds8f/k8s/iad-options/AGENTS.md
../scratch/NEEDLE-completion-audit-20260815/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/ultimate_mcp_client/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/slb/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/rust_scriptbots/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/ultimate_mcp_server/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/ultrasearch/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/savant-elite/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/toon_rust/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/rust_stream_deck/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/surface-dial-rust/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/storage_ballast_helper/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/remote_compilation_helper/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/ultimate_bug_scanner/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/xf/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/rust_proxy/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/repo_updater/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/vibe_cockpit/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/rich_rust/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/rano/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/source_to_prompt_tui/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/q_z/repos/sqlmodel_rust/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/phage_explorer/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/post_compact_reminder/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/pi_agent_rust/tests/ext_conformance/artifacts/npm/mitsupi/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/pi_agent_rust/tests/ext_conformance/artifacts/npm/vaayne-agent-kit/mcps/mcp-fs/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/pi_agent_rust/tests/ext_conformance/artifacts/npm/vaayne-agent-kit/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/pi_agent_rust/tests/ext_conformance/artifacts/npm/vaayne-agent-kit/mcphub/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/pi_agent_rust/tests/ext_conformance/artifacts/npm/vaayne-agent-kit/skills/react-best-practices/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/pi_agent_rust/tests/ext_conformance/artifacts/npm/zenobius-pi-dcp/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/pi_agent_rust/tests/ext_conformance/artifacts/npm/aliou-pi-linkup/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/pi_agent_rust/tests/ext_conformance/artifacts/npm/aliou-pi-synthetic/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/pi_agent_rust/tests/ext_conformance/artifacts/npm/pi-shadow-git/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/pi_agent_rust/tests/ext_conformance/artifacts/templates-davila7/cli-tool/components/skills/database/supabase-postgres-best-practices/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/pi_agent_rust/tests/ext_conformance/artifacts/templates-davila7/cli-tool/components/skills/development/postgres-best-practices/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/pi_agent_rust/tests/ext_conformance/artifacts/templates-davila7/cli-tool/components/skills/development/react-best-practices/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/pi_agent_rust/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/pi_agent_rust/legacy_pi_mono_code/pi-mono/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/process_triage/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/jazz_chord_progression_editor_html/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/opentui_rust/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/ntm/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/giil/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/mcp_agent_mail/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/model_guided_research/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/jeffrey_emanuel_personal_site/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/markdown_web_browser/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/jeffreysprompts.com/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/power_failure_resumer/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/mcp_agent_mail_website/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/meta_skill/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/g_p/clones/mcp_agent_mail_rust/AGENTS.md
../scratch/dicklesworthstone-plan-corpus/a_f/candidates/cass_memory_system/competing_proposal_plans/claude_version/AGENTS.md
../go/src/github.com/Dicklesworthstone/coding_agent_account_manager/AGENTS.md
../go/pkg/mod/github.com/pb33f/libopenapi@v0.38.7/AGENTS.md
../go/pkg/mod/github.com/pelletier/go-toml/v2@v2.3.1/AGENTS.md
../go/pkg/mod/github.com/clipperhouse/displaywidth@v0.11.0/AGENTS.md
../go/pkg/mod/github.com/clipperhouse/uax29/v2@v2.5.0/AGENTS.md
../go/pkg/mod/github.com/clipperhouse/uax29/v2@v2.7.0/AGENTS.md
../go/pkg/mod/github.com/clipperhouse/displaywidth@v0.9.0/AGENTS.md
../go/pkg/mod/github.com/alecthomas/chroma/v2@v2.24.1/AGENTS.md
../SEAM/AGENTS.md
../NEEDLE/AGENTS.md
../vibe-coding-discovery/AGENTS.md
../HOOP/AGENTS.md
../jedarden.com/node_modules/png-to-ico/AGENTS.md
../.codex/AGENTS.md
../.codex/.tmp/plugins/plugins/build-web-apps/skills/supabase-best-practices/AGENTS.md
../.codex/.tmp/plugins/plugins/build-web-apps/skills/react-best-practices/AGENTS.md
../.codex/.tmp/plugins/plugins/zoom/AGENTS.md
../src/declarative-config/AGENTS.md
../src/declarative-config/nixos/AGENTS.md
../src/declarative-config/k8s/AGENTS.md
../src/declarative-config/k8s/ardenone-manager/AGENTS.md
../src/declarative-config/k8s/iad-ci/AGENTS.md
../src/declarative-config/k8s/apexalgo-iad/AGENTS.md
../src/declarative-config/k8s/ord-devimprint/AGENTS.md
../src/declarative-config/k8s/iad-kalshi/AGENTS.md
../src/declarative-config/k8s/rs-manager/AGENTS.md
../src/declarative-config/k8s/ardenone-cluster/devpod/AGENTS.md
../src/declarative-config/k8s/ardenone-cluster/AGENTS.md
../src/declarative-config/k8s/iad-options/AGENTS.md
../bead-rs/AGENTS.md
../apexalgo-growth-lab/AGENTS.md
../claude-print/.claude/worktrees/agent-a161678e24fbdc24d/AGENTS.md
../claude-print/.claude/worktrees/agent-a1de5ee8b82b9638c/AGENTS.md
../claude-print/AGENTS.md
../commitgraph/AGENTS.md
../ardenone-cluster/declarative-config/k8s/apexalgo-iad/AGENTS.md
../ardenone-cluster/project/mission-control/notes/claude-code-hooks-multi-agent-observability/AGENTS.md
../ardenone-cluster/cluster-configuration/apexalgo-iad/AGENTS.md
../dashboard-site/AGENTS.md

codex
The shared checkout is heavily dirty, including the exact outcome and test files in scope. I’m separating pre-existing edits from this bead before touching anything so I don’t overwrite another worker’s work.
exec
/bin/bash -lc "git branch --show-current && git remote -v && git log -5 --oneline && git diff -- src/outcome/mod.rs tests/integration_tests.rs tests/property_tests.rs && sed -n '1,260p' src/outcome/mod.rs && rg -n -C 4 \"classify\\(\" src tests --glob '*.rs' && rg -n -C 6 \"Outcome::Failure|failure_count|release\" src --glob '*.rs'" in /home/coding/NEEDLE
 succeeded in 0ms:
main
origin	https://git.ardenone.com/jedarden/NEEDLE.git (fetch)
origin	https://git.ardenone.com/jedarden/NEEDLE.git (push)
af97791 feat(needle-ebede907): add stack trace capture script for failed tests
b24d3cf test(needle-ea26063c): add comprehensive tests for bead backend template rendering
49eb6e9 test(needle-f78eebbb): add regression test for plan.md OTLP schema
e1346e5 fix(needle-b3617592): deduplicate starvation alert beads by workspace
9cd3437 docs(needle-d1b2ee0d): add unified definition-of-done to agent verification guidance
diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 996c217..8099cb9 100644
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -27,8 +27,8 @@ use crate::validation::{verify_shipped_work, GateConfig, GateReport, ValidationG
 /// Classify an agent exit code into an `Outcome`, with shutdown signal support.
 ///
 /// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted, verified)
+pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
+    Outcome::classify(exit_code, was_interrupted)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
diff --git a/tests/integration_tests.rs b/tests/integration_tests.rs
index fbef0a8..c1d8aec 100644
--- a/tests/integration_tests.rs
+++ b/tests/integration_tests.rs
@@ -7016,6 +7016,7 @@ async fn truncate_commit_sha_handles_short_shas() {
     );
 }
 
+// ═════════════════════════════════════════════════════════════════════════════
 // CI verification test for needle-318c33ba
 // This test is intentionally broken to verify that test failures surface correctly
 #[tokio::test]
diff --git a/tests/property_tests.rs b/tests/property_tests.rs
index 6c22dbc..37076cd 100644
--- a/tests/property_tests.rs
+++ b/tests/property_tests.rs
@@ -152,14 +152,14 @@ proptest! {
     /// classify() never panics for any i32 exit code.
     #[test]
     fn exhaustive_outcomes_no_panic(exit_code: i32) {
-        let _ = Outcome::classify(exit_code, false, false);
-        let _ = Outcome::classify(exit_code, true, false);
+        let _ = Outcome::classify(exit_code, false);
+        let _ = Outcome::classify(exit_code, true);
     }
 
     /// classify() returns a valid Outcome for every exit code.
     #[test]
     fn exhaustive_outcomes_valid_variant(exit_code: i32) {
-        let outcome = Outcome::classify(exit_code, false, true);
+        let outcome = Outcome::classify(exit_code, false);
 
         // Verify the outcome is a known variant by exhaustively matching.
         match outcome {
@@ -193,7 +193,7 @@ proptest! {
     /// was_interrupted=true always produces Interrupted, regardless of exit code.
     #[test]
     fn exhaustive_outcomes_interrupted_overrides(exit_code: i32) {
-        let outcome = Outcome::classify(exit_code, true, false);
+        let outcome = Outcome::classify(exit_code, true);
         prop_assert_eq!(outcome, Outcome::Interrupted,
             "was_interrupted=true must always produce Interrupted for exit_code={}",
             exit_code);
@@ -203,8 +203,8 @@ proptest! {
     /// same result.
     #[test]
     fn exhaustive_outcomes_idempotent(exit_code: i32, was_interrupted: bool) {
-        let a = Outcome::classify(exit_code, was_interrupted, true);
-        let b = Outcome::classify(exit_code, was_interrupted, true);
+        let a = Outcome::classify(exit_code, was_interrupted);
+        let b = Outcome::classify(exit_code, was_interrupted);
         prop_assert_eq!(a, b, "classify must be deterministic");
     }
 }
//! Outcome routing: map agent exit codes to explicit handlers.
//!
//! Every possible exit code has a named handler. The type system enforces
//! exhaustiveness — if an outcome can happen, it must have a handler.
//!
//! Depends on: `types`, `config`, `bead_store`, `telemetry`, `validation`.

use std::fmt;
use std::sync::atomic::{AtomicBool, Ordering};
use std::sync::Arc;

use anyhow::Result;
use chrono::Utc;

use crate::bead_store::{BeadStore, SyncRecoveryError};
use crate::config::Config;
use crate::telemetry::{EventKind, Telemetry};
#[cfg(test)]
use crate::types::BeadStatus;
use crate::types::{AgentOutcome, Bead, BeadAction, HandlerResult, Outcome};
use crate::validation::{verify_shipped_work, GateConfig, GateReport, ValidationGate};

// ──────────────────────────────────────────────────────────────────────────────
// classify (convenience re-export)
// ──────────────────────────────────────────────────────────────────────────────

/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
///
/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
    Outcome::classify(exit_code, was_interrupted)
}

// ──────────────────────────────────────────────────────────────────────────────
// OutcomeHandler
// ──────────────────────────────────────────────────────────────────────────────

/// Routes agent outcomes to their explicit handlers.
pub struct OutcomeHandler {
    config: Config,
    telemetry: Telemetry,
}

impl OutcomeHandler {
    pub fn new(config: Config, telemetry: Telemetry) -> Self {
        OutcomeHandler { config, telemetry }
    }

    /// Run a bead store operation with a 30s timeout.
    ///
    /// Returns `Ok(Some(T))` on success, `Ok(None)` on timeout, and `Err(E)` on
    /// other errors. Callers should treat timeout and error as non-fatal — log
    /// and continue rather than blocking the worker in HANDLING state.
    async fn timeout_op<T, F, Fut>(
        &self,
        op: F,
        operation_name: &str,
    ) -> Result<Option<T>, anyhow::Error>
    where
        F: FnOnce() -> Fut,
        Fut: std::future::Future<Output = Result<T, anyhow::Error>>,
    {
        match tokio::time::timeout(std::time::Duration::from_secs(30), op()).await {
            Ok(Ok(result)) => Ok(Some(result)),
            Ok(Err(e)) => Err(e),
            Err(_) => {
                tracing::error!(
                    operation = operation_name,
                    "bead store operation timed out after 30s"
                );
                Ok(None)
            }
        }
    }

    /// Release a bead with sync conflict recovery and graceful failure handling.
    ///
    /// Flow:
    /// 1. Run `br sync --flush-only` to push local writes to JSONL first.
    /// 2. Attempt `br update` to release the bead (set status=open, assignee="").
    /// 3. If SYNC_CONFLICT recovery fails, emit `BeadReleaseFailed` and return Ok.
    ///
    /// This method never returns an error — release failures are non-fatal.
    /// The worker can continue processing other beads even if release fails.
    async fn release_bead(&self, store: &dyn BeadStore, bead: &Bead) -> Result<Vec<EventKind>> {
        let mut events = Vec::new();

        // Step 1: Flush local writes to JSONL before attempting release.
        // Emit heartbeat before br call to track where hang occurs.
        let _ = self.telemetry.emit_try_lock(EventKind::HeartbeatEmitted {
            bead_id: Some(bead.id.clone()),
            state: "HANDLING_FLUSH".to_string(),
        });

        match self.timeout_op(|| store.flush(), "flush").await {
            Ok(Some(())) => {
                tracing::debug!(
                    bead_id = %bead.id,
                    "flushed local changes to JSONL before release"
                );
                // Emit heartbeat after successful flush.
                let _ = self.telemetry.emit_try_lock(EventKind::HeartbeatEmitted {
                    bead_id: Some(bead.id.clone()),
                    state: "HANDLING_FLUSH_DONE".to_string(),
                });
            }
            Ok(None) => {
                tracing::warn!(
                    bead_id = %bead.id,
                    "flush timed out before release, attempting release anyway"
                );
                events.push(EventKind::WorkerHandlingTimeout {
                    bead_id: bead.id.clone(),
                    outcome: "release".to_string(),
                    operation: "flush".to_string(),
                    error: "timeout after 30s".to_string(),
                });
            }
            Err(e) => {
                tracing::warn!(
                    bead_id = %bead.id,
                    error = %e,
                    "flush failed before release, attempting release anyway"
                );
                events.push(EventKind::WorkerHandlingTimeout {
                    bead_id: bead.id.clone(),
                    outcome: "release".to_string(),
                    operation: "flush".to_string(),
                    error: e.to_string(),
                });
            }
        }

        // Step 2: Attempt release with timeout protection.
        // Emit heartbeat before br release call to track where hang occurs.
        let _ = self.telemetry.emit_try_lock(EventKind::HeartbeatEmitted {
            bead_id: Some(bead.id.clone()),
            state: "HANDLING_RELEASE".to_string(),
        });

        let release_result =
            tokio::time::timeout(std::time::Duration::from_secs(30), store.release(&bead.id)).await;

        match release_result {
            Ok(Ok(())) => {
                tracing::debug!(
                    bead_id = %bead.id,
                    "bead released successfully"
                );
                // Emit heartbeat after successful release.
                let _ = self.telemetry.emit_try_lock(EventKind::HeartbeatEmitted {
                    bead_id: Some(bead.id.clone()),
                    state: "HANDLING_RELEASE_DONE".to_string(),
                });
                events.push(EventKind::BeadReleased {
                    bead_id: bead.id.clone(),
                    reason: "release_success".to_string(),
                });
            }
            Ok(Err(e)) => {
                // Release failed (br error, not timeout).
                // Check if this is a SyncRecoveryError (sync recovery was attempted but failed).
                if e.downcast_ref::<SyncRecoveryError>().is_some() {
                    tracing::error!(
                        bead_id = %bead.id,
                        error = %e,
                        "br release failed after SYNC_CONFLICT recovery — \
                         worker will continue (bead may remain assigned)"
                    );
                } else {
                    tracing::error!(
                        bead_id = %bead.id,
                        error = %e,
                        "br release failed — worker will continue (bead may remain assigned)"
                    );
                }
                events.push(EventKind::BeadReleaseFailed {
                    bead_id: bead.id.clone(),
                    reason: e.to_string(),
                });
            }
            Err(_) => {
                // Timeout after 30s.
                tracing::error!(
                    bead_id = %bead.id,
                    "br release timed out after 30s — worker will continue (bead may remain assigned)"
                );
                events.push(EventKind::BeadReleaseFailed {
                    bead_id: bead.id.clone(),
                    reason: "timeout after 30s".to_string(),
                });
            }
        }

        Ok(events)
    }

    /// Run verification gates for a bead, returning whether all passed.
    ///
    /// This is extracted as a helper so it can be called BEFORE outcome classification.
    /// Verification must determine Success, not just exit code 0.
    async fn run_verification_gates(&self, bead: &Bead) -> Result<(bool, Option<GateReport>)> {
        // Try pluggable gates first, fall back to legacy verification commands.
        let gate_opt = if !self.config.gates.is_empty() {
            // New pluggable gate system. Fill in each command gate's stderr
            // cap from `validation.stderr_cap_bytes` unless the gate already
            // set its own override — see GitHub issue jedarden/NEEDLE#9.
            let default_stderr_cap = self.config.validation.stderr_cap_bytes;
            let gate_configs: Vec<(String, GateConfig)> = self
                .config
                .gates
                .iter()
                .enumerate()
                .map(|(i, config)| {
                    let mut config = config.clone();
                    let GateConfig::Command {
                        stderr_cap_bytes, ..
                    } = &mut config;
                    if stderr_cap_bytes.is_none() {
                        *stderr_cap_bytes = Some(default_stderr_cap);
                    }
                    (format!("gate_{}", i), config)
                })
                .collect();
            ValidationGate::new(gate_configs, bead.workspace.clone())
        } else if !self.config.verification.is_empty() {
            // Legacy verification command format.
            ValidationGate::from_commands_with_stderr_cap(
                self.config.verification.clone(),
                bead.workspace.clone(),
                self.config.validation.stderr_cap_bytes,
            )
        } else {
            // No gates configured — treat as verified (backward compatible).
            return Ok((true, None));
        };

        // Run the gate and return the result.
        let gate = gate_opt.ok_or_else(|| {
            anyhow::anyhow!("validation gate creation failed - all gates failed to initialize")
        })?;
        let report = gate.run(bead).await?;
        let all_passed = report.all_passed;
        Ok((all_passed, Some(report)))
    }

    /// Handle a process output for the given bead.
    ///
    /// CRITICAL: For exit code 0, verification gates run BEFORE classification.
    /// This ensures Success means verification passed, not just that the agent exited 0.
    /// An agent that exits 0 but fails verification produces Failure, not Success.
    #[tracing::instrument(
        name = "bead.outcome",
        skip(self, store, bead, output),
        fields(
            needle.bead.id = %bead.id,
            needle.outcome = tracing::field::Empty,
            needle.outcome.action = tracing::field::Empty,
        )
    )]
src/dispatch/mod.rs-1838-                let _ = capture.write_trace_jsonl(lines);
src/dispatch/mod.rs-1839-            }
src/dispatch/mod.rs-1840-
src/dispatch/mod.rs-1841-            // Create and write metadata.
src/dispatch/mod.rs:1842:            let outcome = Outcome::classify(exit_code, false);
src/dispatch/mod.rs-1843-            let metadata = TraceMetadata {
src/dispatch/mod.rs-1844-                bead_id: bead_id.clone(),
src/dispatch/mod.rs-1845-                agent: adapter.name.clone(),
src/dispatch/mod.rs-1846-                provider: adapter.provider.clone(),
--
src/dispatch/mod.rs-3458-            .await
src/dispatch/mod.rs-3459-            .unwrap();
src/dispatch/mod.rs-3460-
src/dispatch/mod.rs-3461-        assert_eq!(result.exit_code, 0);
src/dispatch/mod.rs:3462:        assert_eq!(Outcome::classify(result.exit_code, false), Outcome::Success);
src/dispatch/mod.rs-3463-    }
src/dispatch/mod.rs-3464-
src/dispatch/mod.rs-3465-    #[tokio::test]
src/dispatch/mod.rs-3466-    async fn e2e_exit_code_1_is_failure() {
--
src/dispatch/mod.rs-3481-            .await
src/dispatch/mod.rs-3482-            .unwrap();
src/dispatch/mod.rs-3483-
src/dispatch/mod.rs-3484-        assert_eq!(result.exit_code, 1);
src/dispatch/mod.rs:3485:        assert_eq!(Outcome::classify(result.exit_code, false), Outcome::Failure);
src/dispatch/mod.rs-3486-    }
src/dispatch/mod.rs-3487-
src/dispatch/mod.rs-3488-    #[tokio::test]
src/dispatch/mod.rs-3489-    async fn e2e_exit_code_2_is_failure() {
--
src/dispatch/mod.rs-3504-            .await
src/dispatch/mod.rs-3505-            .unwrap();
src/dispatch/mod.rs-3506-
src/dispatch/mod.rs-3507-        assert_eq!(result.exit_code, 2);
src/dispatch/mod.rs:3508:        assert_eq!(Outcome::classify(result.exit_code, false), Outcome::Failure);
src/dispatch/mod.rs-3509-    }
src/dispatch/mod.rs-3510-
src/dispatch/mod.rs-3511-    #[tokio::test]
src/dispatch/mod.rs-3512-    async fn e2e_exit_code_137_is_crash() {
--
src/dispatch/mod.rs-3528-            .unwrap();
src/dispatch/mod.rs-3529-
src/dispatch/mod.rs-3530-        assert_eq!(result.exit_code, 137);
src/dispatch/mod.rs-3531-        assert_eq!(
src/dispatch/mod.rs:3532:            Outcome::classify(result.exit_code, false),
src/dispatch/mod.rs-3533-            Outcome::Crash(137)
src/dispatch/mod.rs-3534-        );
src/dispatch/mod.rs-3535-    }
src/dispatch/mod.rs-3536-
--
tests/property_tests.rs-148-// function must never panic, and the result must be one of the documented
tests/property_tests.rs-149-// variants.
tests/property_tests.rs-150-
tests/property_tests.rs-151-proptest! {
tests/property_tests.rs:152:    /// classify() never panics for any i32 exit code.
tests/property_tests.rs-153-    #[test]
tests/property_tests.rs-154-    fn exhaustive_outcomes_no_panic(exit_code: i32) {
tests/property_tests.rs:155:        let _ = Outcome::classify(exit_code, false);
tests/property_tests.rs:156:        let _ = Outcome::classify(exit_code, true);
tests/property_tests.rs-157-    }
tests/property_tests.rs-158-
tests/property_tests.rs:159:    /// classify() returns a valid Outcome for every exit code.
tests/property_tests.rs-160-    #[test]
tests/property_tests.rs-161-    fn exhaustive_outcomes_valid_variant(exit_code: i32) {
tests/property_tests.rs:162:        let outcome = Outcome::classify(exit_code, false);
tests/property_tests.rs-163-
tests/property_tests.rs-164-        // Verify the outcome is a known variant by exhaustively matching.
tests/property_tests.rs-165-        match outcome {
tests/property_tests.rs-166-            Outcome::Success => prop_assert_eq!(exit_code, 0),
--
tests/property_tests.rs-192-
tests/property_tests.rs-193-    /// was_interrupted=true always produces Interrupted, regardless of exit code.
tests/property_tests.rs-194-    #[test]
tests/property_tests.rs-195-    fn exhaustive_outcomes_interrupted_overrides(exit_code: i32) {
tests/property_tests.rs:196:        let outcome = Outcome::classify(exit_code, true);
tests/property_tests.rs-197-        prop_assert_eq!(outcome, Outcome::Interrupted,
tests/property_tests.rs-198-            "was_interrupted=true must always produce Interrupted for exit_code={}",
tests/property_tests.rs-199-            exit_code);
tests/property_tests.rs-200-    }
--
tests/property_tests.rs-202-    /// classify is idempotent: calling it twice with the same input produces the
tests/property_tests.rs-203-    /// same result.
tests/property_tests.rs-204-    #[test]
tests/property_tests.rs-205-    fn exhaustive_outcomes_idempotent(exit_code: i32, was_interrupted: bool) {
tests/property_tests.rs:206:        let a = Outcome::classify(exit_code, was_interrupted);
tests/property_tests.rs:207:        let b = Outcome::classify(exit_code, was_interrupted);
tests/property_tests.rs-208-        prop_assert_eq!(a, b, "classify must be deterministic");
tests/property_tests.rs-209-    }
tests/property_tests.rs-210-}
tests/property_tests.rs-211-
--
src/outcome/mod.rs-25-// ──────────────────────────────────────────────────────────────────────────────
src/outcome/mod.rs-26-
src/outcome/mod.rs-27-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
src/outcome/mod.rs-28-///
src/outcome/mod.rs:29:/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
src/outcome/mod.rs:30:pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
src/outcome/mod.rs:31:    Outcome::classify(exit_code, was_interrupted)
src/outcome/mod.rs-32-}
src/outcome/mod.rs-33-
src/outcome/mod.rs-34-// ──────────────────────────────────────────────────────────────────────────────
src/outcome/mod.rs-35-// OutcomeHandler
--
src/outcome/mod.rs-273-            // Non-zero exit or interrupted — verification irrelevant.
src/outcome/mod.rs-274-            (true, None)
src/outcome/mod.rs-275-        };
src/outcome/mod.rs-276-
src/outcome/mod.rs:277:        let outcome = classify(output.exit_code, was_interrupted, verified);
src/outcome/mod.rs-278-
src/outcome/mod.rs-279-        // Set outcome as span attribute
src/outcome/mod.rs-280-        tracing::Span::current().record("needle.outcome", outcome.as_str());
src/outcome/mod.rs-281-
--
src/outcome/mod.rs-367-            );
src/outcome/mod.rs-368-            // Return a default HandlerResult to allow the worker to recover.
src/outcome/mod.rs-369-            // Verification never ran, so conservatively treat as unverified.
src/outcome/mod.rs-370-            return Ok(HandlerResult {
src/outcome/mod.rs:371:                outcome: classify(output.exit_code, was_interrupted, false),
src/outcome/mod.rs-372-                bead_action: BeadAction::None,
src/outcome/mod.rs-373-                telemetry_events: vec![],
src/outcome/mod.rs-374-            });
src/outcome/mod.rs-375-        }
--
src/outcome/mod.rs-409-                );
src/outcome/mod.rs-410-                // Emit a timeout event for observability.
src/outcome/mod.rs-411-                let _ = telemetry.emit(EventKind::WorkerHandlingTimeout {
src/outcome/mod.rs-412-                    bead_id: bead_id.clone(),
src/outcome/mod.rs:413:                    outcome: classify(output.exit_code, was_interrupted, false)
src/outcome/mod.rs-414-                        .as_str()
src/outcome/mod.rs-415-                        .to_string(),
src/outcome/mod.rs-416-                    operation: "handle".to_string(),
src/outcome/mod.rs-417-                    error: format!("timeout after {}s", timeout_secs),
--
src/outcome/mod.rs-419-                // Return a default HandlerResult to allow the worker to recover.
src/outcome/mod.rs-420-                // The worker's 60-second timeout wrapper will handle best-effort release.
src/outcome/mod.rs-421-                // Verification never ran, so conservatively treat as unverified.
src/outcome/mod.rs-422-                Ok(HandlerResult {
src/outcome/mod.rs:423:                    outcome: classify(output.exit_code, was_interrupted, false),
src/outcome/mod.rs-424-                    bead_action: BeadAction::Released,
src/outcome/mod.rs-425-                    telemetry_events: vec![EventKind::BeadReleased {
src/outcome/mod.rs-426-                        bead_id,
src/outcome/mod.rs-427-                        reason: "handler_timeout".to_string(),
--
src/outcome/mod.rs-1550-    // ── classify tests ──
src/outcome/mod.rs-1551-
src/outcome/mod.rs-1552-    #[test]
src/outcome/mod.rs-1553-    fn classify_was_interrupted_always_returns_interrupted() {
src/outcome/mod.rs:1554:        assert_eq!(classify(0, true, true), Outcome::Interrupted);
src/outcome/mod.rs:1555:        assert_eq!(classify(1, true, false), Outcome::Interrupted);
src/outcome/mod.rs:1556:        assert_eq!(classify(127, true, true), Outcome::Interrupted);
src/outcome/mod.rs-1557-    }
src/outcome/mod.rs-1558-
src/outcome/mod.rs-1559-    #[test]
src/outcome/mod.rs-1560-    fn classify_not_interrupted_uses_exit_code_and_verification() {
src/outcome/mod.rs-1561-        // Exit code 0 with verification passes → Success
src/outcome/mod.rs:1562:        assert_eq!(classify(0, false, true), Outcome::Success);
src/outcome/mod.rs-1563-        // Exit code 0 with verification fails → Failure
src/outcome/mod.rs:1564:        assert_eq!(classify(0, false, false), Outcome::Failure);
src/outcome/mod.rs-1565-        // Non-zero exit codes always fail regardless of verification
src/outcome/mod.rs:1566:        assert_eq!(classify(1, false, true), Outcome::Failure);
src/outcome/mod.rs:1567:        assert_eq!(classify(1, false, false), Outcome::Failure);
src/outcome/mod.rs:1568:        assert_eq!(classify(124, false, true), Outcome::Timeout);
src/outcome/mod.rs:1569:        assert_eq!(classify(127, false, true), Outcome::AgentNotFound);
src/outcome/mod.rs:1570:        assert_eq!(classify(129, false, true), Outcome::Crash(129));
src/outcome/mod.rs-1571-    }
src/outcome/mod.rs-1572-
src/outcome/mod.rs-1573-    #[test]
src/outcome/mod.rs-1574-    fn classify_no_wildcard_arms() {
src/outcome/mod.rs-1575-        // Verify key exit codes map correctly per spec.
src/outcome/mod.rs-1576-        // Exit code 0 ONLY succeeds when verification passes
src/outcome/mod.rs:1577:        assert_eq!(classify(0, false, true), Outcome::Success);
src/outcome/mod.rs:1578:        assert_eq!(classify(0, false, false), Outcome::Failure);
src/outcome/mod.rs:1579:        assert_eq!(classify(1, false, true), Outcome::Failure);
src/outcome/mod.rs:1580:        assert_eq!(classify(2, false, true), Outcome::Failure);
src/outcome/mod.rs:1581:        assert_eq!(classify(99, false, true), Outcome::Failure);
src/outcome/mod.rs:1582:        assert_eq!(classify(100, false, true), Outcome::Failure);
src/outcome/mod.rs:1583:        assert_eq!(classify(124, false, true), Outcome::Timeout);
src/outcome/mod.rs:1584:        assert_eq!(classify(125, false, true), Outcome::Failure);
src/outcome/mod.rs:1585:        assert_eq!(classify(128, false, true), Outcome::Failure); // not >128 per spec
src/outcome/mod.rs:1586:        assert_eq!(classify(129, false, true), Outcome::Crash(129));
src/outcome/mod.rs:1587:        assert_eq!(classify(137, false, true), Outcome::Crash(137));
src/outcome/mod.rs:1588:        assert_eq!(classify(-9, false, true), Outcome::Crash(-9));
src/outcome/mod.rs-1589-    }
src/outcome/mod.rs-1590-
src/outcome/mod.rs-1591-    #[test]
src/outcome/mod.rs-1592-    fn classify_verification_gate() {
src/outcome/mod.rs-1593-        // The core fix: exit code 0 does NOT guarantee Success.
src/outcome/mod.rs-1594-        // Verification is the gate.
src/outcome/mod.rs-1595-        assert_eq!(
src/outcome/mod.rs:1596:            classify(0, false, false),
src/outcome/mod.rs-1597-            Outcome::Failure,
src/outcome/mod.rs-1598-            "exit_code=0, verified=false must return Failure"
src/outcome/mod.rs-1599-        );
src/outcome/mod.rs-1600-        assert_eq!(
src/outcome/mod.rs:1601:            classify(0, false, true),
src/outcome/mod.rs-1602-            Outcome::Success,
src/outcome/mod.rs-1603-            "exit_code=0, verified=true must return Success"
src/outcome/mod.rs-1604-        );
src/outcome/mod.rs-1605-    }
--
tests/integration_tests.rs-1306-    ];
tests/integration_tests.rs-1307-
tests/integration_tests.rs-1308-    for code in test_codes {
tests/integration_tests.rs-1309-        // Should not panic.
tests/integration_tests.rs:1310:        let outcome = Outcome::classify(code, false);
tests/integration_tests.rs-1311-        // Verify specific mappings.
tests/integration_tests.rs-1312-        match code {
tests/integration_tests.rs-1313-            0 => assert_eq!(outcome, Outcome::Success),
tests/integration_tests.rs-1314-            1 => assert_eq!(outcome, Outcome::Failure),
--
tests/integration_tests.rs-1322-
tests/integration_tests.rs-1323-    // Verify interrupted flag always wins.
tests/integration_tests.rs-1324-    for code in [-1, 0, 1, 127, 137] {
tests/integration_tests.rs-1325-        assert_eq!(
tests/integration_tests.rs:1326:            Outcome::classify(code, true),
tests/integration_tests.rs-1327-            Outcome::Interrupted,
tests/integration_tests.rs-1328-            "was_interrupted=true must always return Interrupted for code {code}"
tests/integration_tests.rs-1329-        );
tests/integration_tests.rs-1330-    }
--
src/mitosis/timeout_eligibility.rs-163-        };
src/mitosis/timeout_eligibility.rs-164-    }
src/mitosis/timeout_eligibility.rs-165-
src/mitosis/timeout_eligibility.rs-166-    // Step 1: Classify the outcome to determine if this is a timeout at all
src/mitosis/timeout_eligibility.rs:167:    let outcome_classification = Outcome::classify(outcome.exit_code, false);
src/mitosis/timeout_eligibility.rs-168-
src/mitosis/timeout_eligibility.rs-169-    // Step 2: Reject non-timeout outcomes immediately
src/mitosis/timeout_eligibility.rs-170-    match outcome_classification {
src/mitosis/timeout_eligibility.rs-171-        Outcome::Timeout => {
--
src/types/mod.rs-210-// ──────────────────────────────────────────────────────────────────────────────
src/types/mod.rs-211-
src/types/mod.rs-212-/// The classified outcome of an agent process.
src/types/mod.rs-213-///
src/types/mod.rs:214:/// Every exit code maps to exactly one variant via `Outcome::classify()`.
src/types/mod.rs-215-#[derive(Debug, Clone, PartialEq, Eq)]
src/types/mod.rs-216-pub enum Outcome {
src/types/mod.rs-217-    /// Exit 0 — agent completed work successfully.
src/types/mod.rs-218-    Success,
--
src/types/mod.rs-242-    /// - exit 127 → `AgentNotFound`
src/types/mod.rs-243-    /// - exit >128 → `Crash(exit_code)`
src/types/mod.rs-244-    /// - exit <0 → `Crash(exit_code)`
src/types/mod.rs-245-    /// - all other → `Failure`
src/types/mod.rs:246:    pub fn classify(exit_code: i32, was_interrupted: bool) -> Self {
src/types/mod.rs-247-        // Interrupted flag takes precedence (graceful shutdown path).
src/types/mod.rs-248-        if was_interrupted {
src/types/mod.rs-249-            return Outcome::Interrupted;
src/types/mod.rs-250-        }
--
src/types/mod.rs-1087-
src/types/mod.rs-1088-    #[test]
src/types/mod.rs-1089-    fn outcome_classify_key_codes() {
src/types/mod.rs-1090-        // Core mappings per spec
src/types/mod.rs:1091:        assert_eq!(Outcome::classify(0, false), Outcome::Success);
src/types/mod.rs:1092:        assert_eq!(Outcome::classify(1, false), Outcome::Failure);
src/types/mod.rs:1093:        assert_eq!(Outcome::classify(124, false), Outcome::Timeout);
src/types/mod.rs:1094:        assert_eq!(Outcome::classify(127, false), Outcome::AgentNotFound);
src/types/mod.rs-1095-    }
src/types/mod.rs-1096-
src/types/mod.rs-1097-    #[test]
src/types/mod.rs-1098-    fn outcome_classify_ranges() {
src/types/mod.rs-1099-        // 2..=123 map to Failure (except 124 which is Timeout)
src/types/mod.rs:1100:        assert_eq!(Outcome::classify(2, false), Outcome::Failure);
src/types/mod.rs:1101:        assert_eq!(Outcome::classify(50, false), Outcome::Failure);
src/types/mod.rs:1102:        assert_eq!(Outcome::classify(99, false), Outcome::Failure);
src/types/mod.rs:1103:        assert_eq!(Outcome::classify(100, false), Outcome::Failure); // NOT timeout per spec
src/types/mod.rs:1104:        assert_eq!(Outcome::classify(123, false), Outcome::Failure);
src/types/mod.rs-1105-        // 125-126 -> Failure (not AgentNotFound per spec)
src/types/mod.rs:1106:        assert_eq!(Outcome::classify(125, false), Outcome::Failure);
src/types/mod.rs:1107:        assert_eq!(Outcome::classify(126, false), Outcome::Failure);
src/types/mod.rs-1108-        // >128 -> Crash (signal exits)
src/types/mod.rs:1109:        assert_eq!(Outcome::classify(128, false), Outcome::Failure); // 128 is NOT >128 per spec
src/types/mod.rs:1110:        assert_eq!(Outcome::classify(129, false), Outcome::Crash(129));
src/types/mod.rs:1111:        assert_eq!(Outcome::classify(130, false), Outcome::Crash(130)); // SIGINT -> Crash
src/types/mod.rs:1112:        assert_eq!(Outcome::classify(137, false), Outcome::Crash(137)); // SIGKILL
src/types/mod.rs:1113:        assert_eq!(Outcome::classify(143, false), Outcome::Crash(143)); // SIGTERM -> Crash
src/types/mod.rs:1114:        assert_eq!(Outcome::classify(255, false), Outcome::Crash(255));
src/types/mod.rs-1115-        // negative -> Crash
src/types/mod.rs:1116:        assert_eq!(Outcome::classify(-1, false), Outcome::Crash(-1));
src/types/mod.rs:1117:        assert_eq!(Outcome::classify(-9, false), Outcome::Crash(-9));
src/types/mod.rs-1118-        // Large positive values >255 -> Crash per spec (exit > 128)
src/types/mod.rs:1119:        assert_eq!(Outcome::classify(256, false), Outcome::Crash(256));
src/types/mod.rs:1120:        assert_eq!(Outcome::classify(1000, false), Outcome::Crash(1000));
src/types/mod.rs-1121-    }
src/types/mod.rs-1122-
src/types/mod.rs-1123-    #[test]
src/types/mod.rs-1124-    fn outcome_classify_interrupted_flag() {
src/types/mod.rs-1125-        // was_interrupted=true always returns Interrupted, regardless of exit code
src/types/mod.rs:1126:        assert_eq!(Outcome::classify(0, true), Outcome::Interrupted);
src/types/mod.rs:1127:        assert_eq!(Outcome::classify(1, true), Outcome::Interrupted);
src/types/mod.rs:1128:        assert_eq!(Outcome::classify(127, true), Outcome::Interrupted);
src/types/mod.rs:1129:        assert_eq!(Outcome::classify(-1, true), Outcome::Interrupted);
src/types/mod.rs:1130:        assert_eq!(Outcome::classify(137, true), Outcome::Interrupted);
src/types/mod.rs-1131-    }
src/types/mod.rs-1132-
src/types/mod.rs-1133-    #[test]
src/types/mod.rs-1134-    fn outcome_as_str() {
--
src/types/mod.rs-1352-
src/types/mod.rs-1353-    #[test]
src/types/mod.rs-1354-    fn outcome_classify_boundary_values() {
src/types/mod.rs-1355-        // Exact boundaries between ranges
src/types/mod.rs:1356:        assert_eq!(Outcome::classify(0, false), Outcome::Success);
src/types/mod.rs:1357:        assert_eq!(Outcome::classify(1, false), Outcome::Failure);
src/types/mod.rs:1358:        assert_eq!(Outcome::classify(2, false), Outcome::Failure);
src/types/mod.rs:1359:        assert_eq!(Outcome::classify(123, false), Outcome::Failure);
src/types/mod.rs:1360:        assert_eq!(Outcome::classify(124, false), Outcome::Timeout);
src/types/mod.rs:1361:        assert_eq!(Outcome::classify(125, false), Outcome::Failure);
src/types/mod.rs:1362:        assert_eq!(Outcome::classify(126, false), Outcome::Failure);
src/types/mod.rs:1363:        assert_eq!(Outcome::classify(127, false), Outcome::AgentNotFound);
src/types/mod.rs:1364:        assert_eq!(Outcome::classify(128, false), Outcome::Failure);
src/types/mod.rs:1365:        assert_eq!(Outcome::classify(129, false), Outcome::Crash(129)); // First crash code
src/types/mod.rs-1366-
src/types/mod.rs-1367-        // i32 extremes
src/types/mod.rs:1368:        assert_eq!(Outcome::classify(i32::MAX, false), Outcome::Crash(i32::MAX));
src/types/mod.rs:1369:        assert_eq!(Outcome::classify(i32::MIN, false), Outcome::Crash(i32::MIN));
src/types/mod.rs-1370-    }
src/types/mod.rs-1371-
src/types/mod.rs-1372-    #[test]
src/types/mod.rs-1373-    fn outcome_classify_common_signals() {
src/types/mod.rs-1374-        // SIGHUP (128 + 1 = 129)
src/types/mod.rs:1375:        assert_eq!(Outcome::classify(129, false), Outcome::Crash(129));
src/types/mod.rs-1376-        // SIGINT (128 + 2 = 130)
src/types/mod.rs:1377:        assert_eq!(Outcome::classify(130, false), Outcome::Crash(130));
src/types/mod.rs-1378-        // SIGKILL (128 + 9 = 137)
src/types/mod.rs:1379:        assert_eq!(Outcome::classify(137, false), Outcome::Crash(137));
src/types/mod.rs-1380-        // SIGTERM (128 + 15 = 143)
src/types/mod.rs:1381:        assert_eq!(Outcome::classify(143, false), Outcome::Crash(143));
src/types/mod.rs-1382-        // SIGSEGV (128 + 11 = 139)
src/types/mod.rs:1383:        assert_eq!(Outcome::classify(139, false), Outcome::Crash(139));
src/types/mod.rs-1384-    }
src/types/mod.rs-1385-
src/types/mod.rs-1386-    #[test]
src/types/mod.rs-1387-    fn bead_action_display() {
src/peer/mod.rs:1://! Peer monitoring — stale claim release and crash cleanup.
src/peer/mod.rs-2-//!
src/peer/mod.rs-3-//! Detects crashed or stuck workers by reading heartbeat files and comparing
src/peer/mod.rs-4-//! them against the configured TTL. For crashed workers (stale heartbeat +
src/peer/mod.rs:5://! dead PID), releases the claimed bead, removes the heartbeat file, and
src/peer/mod.rs-6-//! deregisters from the worker registry.
src/peer/mod.rs-7-//!
src/peer/mod.rs-8-//! This module is designed to be called by the Mend strand (Strand 2). No
src/peer/mod.rs-9-//! other strand should perform peer monitoring.
src/peer/mod.rs-10-//!
src/peer/mod.rs-11-//! Depends on: `bead_store`, `health`, `registry`, `telemetry`, `types`.
--
src/peer/mod.rs-29-#[derive(Debug)]
src/peer/mod.rs-30-pub struct PeerCheckResult {
src/peer/mod.rs-31-    /// Number of crashed workers detected (dead PID).
src/peer/mod.rs-32-    pub crashed_count: u32,
src/peer/mod.rs-33-    /// Number of stuck workers detected (alive PID, stale heartbeat).
src/peer/mod.rs-34-    pub stuck_count: u32,
src/peer/mod.rs:35:    /// Number of beads released from crashed workers.
src/peer/mod.rs:36:    pub beads_released: u32,
src/peer/mod.rs-37-}
src/peer/mod.rs-38-
src/peer/mod.rs-39-impl PeerCheckResult {
src/peer/mod.rs-40-    /// Whether any cleanup work was performed.
src/peer/mod.rs-41-    pub fn did_work(&self) -> bool {
src/peer/mod.rs:42:        self.beads_released > 0
src/peer/mod.rs-43-    }
src/peer/mod.rs-44-}
src/peer/mod.rs-45-
src/peer/mod.rs-46-// ──────────────────────────────────────────────────────────────────────────────
src/peer/mod.rs-47-// PeerMonitor
src/peer/mod.rs-48-// ──────────────────────────────────────────────────────────────────────────────
--
src/peer/mod.rs-89-
src/peer/mod.rs-90-    /// Run one peer monitoring cycle.
src/peer/mod.rs-91-    ///
src/peer/mod.rs-92-    /// Algorithm:
src/peer/mod.rs-93-    /// 1. Read all heartbeat files
src/peer/mod.rs-94-    /// 2. For each stale peer (excluding ourselves):
src/peer/mod.rs:95:    ///    a. If PID is dead → CRASHED: release bead, remove heartbeat, deregister
src/peer/mod.rs:96:    ///    b. If PID is alive → STUCK: emit warning only, do NOT release
src/peer/mod.rs-97-    /// 3. Return summary of actions taken
src/peer/mod.rs-98-    pub async fn check_peers(&self) -> Result<PeerCheckResult> {
src/peer/mod.rs-99-        let heartbeats = HealthMonitor::read_all_heartbeats(&self.heartbeat_dir)?;
src/peer/mod.rs-100-
src/peer/mod.rs-101-        let mut result = PeerCheckResult {
src/peer/mod.rs-102-            crashed_count: 0,
src/peer/mod.rs-103-            stuck_count: 0,
src/peer/mod.rs:104:            beads_released: 0,
src/peer/mod.rs-105-        };
src/peer/mod.rs-106-
src/peer/mod.rs-107-        for hb in &heartbeats {
src/peer/mod.rs-108-            // Skip our own heartbeat (match by qualified identity).
src/peer/mod.rs-109-            if hb.qualified_id == self.own_worker_id {
src/peer/mod.rs-110-                continue;
--
src/peer/mod.rs-126-                heartbeat_file: hb.heartbeat_file.clone().unwrap_or_else(|| {
src/peer/mod.rs-127-                    self.heartbeat_dir.join(format!("{}.json", hb.qualified_id))
src/peer/mod.rs-128-                }),
src/peer/mod.rs-129-            };
src/peer/mod.rs-130-
src/peer/mod.rs-131-            if pid_alive {
src/peer/mod.rs:132:                // STUCK: alive PID, stale heartbeat. Warn only — do NOT release.
src/peer/mod.rs-133-                self.handle_stuck_peer(&stale_peer)?;
src/peer/mod.rs-134-                result.stuck_count += 1;
src/peer/mod.rs-135-            } else {
src/peer/mod.rs-136-                // CRASHED: dead PID. Clean up.
src/peer/mod.rs:137:                let released = self.handle_crashed_peer(&stale_peer).await?;
src/peer/mod.rs-138-                result.crashed_count += 1;
src/peer/mod.rs:139:                if released {
src/peer/mod.rs:140:                    result.beads_released += 1;
src/peer/mod.rs-141-                }
src/peer/mod.rs-142-            }
src/peer/mod.rs-143-        }
src/peer/mod.rs-144-
src/peer/mod.rs-145-        if result.crashed_count > 0 || result.stuck_count > 0 {
src/peer/mod.rs-146-            tracing::info!(
src/peer/mod.rs-147-                crashed = result.crashed_count,
src/peer/mod.rs-148-                stuck = result.stuck_count,
src/peer/mod.rs:149:                beads_released = result.beads_released,
src/peer/mod.rs-150-                "peer monitoring cycle complete"
src/peer/mod.rs-151-            );
src/peer/mod.rs-152-        }
src/peer/mod.rs-153-
src/peer/mod.rs-154-        Ok(result)
src/peer/mod.rs-155-    }
src/peer/mod.rs-156-
src/peer/mod.rs-157-    /// Handle a stuck peer: alive PID but stale heartbeat.
src/peer/mod.rs-158-    ///
src/peer/mod.rs:159:    /// Safety: never release the bead, never kill the process. Alert only.
src/peer/mod.rs-160-    fn handle_stuck_peer(&self, peer: &StalePeer) -> Result<()> {
src/peer/mod.rs-161-        let age_secs = Utc::now()
src/peer/mod.rs-162-            .signed_duration_since(peer.last_heartbeat)
src/peer/mod.rs-163-            .num_seconds()
src/peer/mod.rs-164-            .max(0) as u64;
src/peer/mod.rs-165-
--
src/peer/mod.rs-186-    ///
src/peer/mod.rs-187-    /// 1. Release the claimed bead (if any)
src/peer/mod.rs-188-    /// 2. Remove the heartbeat file
src/peer/mod.rs-189-    /// 3. Deregister from the worker registry
src/peer/mod.rs-190-    /// 4. Emit peer.crashed telemetry
src/peer/mod.rs-191-    ///
src/peer/mod.rs:192:    /// Returns `true` if a bead was released.
src/peer/mod.rs-193-    async fn handle_crashed_peer(&self, peer: &StalePeer) -> Result<bool> {
src/peer/mod.rs-194-        tracing::info!(
src/peer/mod.rs-195-            worker = %peer.worker_id,
src/peer/mod.rs-196-            pid = peer.pid,
src/peer/mod.rs-197-            bead = ?peer.current_bead,
src/peer/mod.rs-198-            "peer crashed — cleaning up"
src/peer/mod.rs-199-        );
src/peer/mod.rs-200-
src/peer/mod.rs:201:        let mut bead_released = false;
src/peer/mod.rs-202-
src/peer/mod.rs-203-        // 1. Release the claimed bead (if any).
src/peer/mod.rs-204-        if let Some(ref bead_id) = peer.current_bead {
src/peer/mod.rs:205:            match self.store.release(bead_id).await {
src/peer/mod.rs-206-                Ok(()) => {
src/peer/mod.rs-207-                    tracing::info!(
src/peer/mod.rs-208-                        bead = %bead_id,
src/peer/mod.rs-209-                        worker = %peer.worker_id,
src/peer/mod.rs:210:                        "released orphaned bead from crashed worker"
src/peer/mod.rs-211-                    );
src/peer/mod.rs:212:                    bead_released = true;
src/peer/mod.rs-213-                }
src/peer/mod.rs-214-                Err(e) => {
src/peer/mod.rs-215-                    // Release failure is non-fatal — the bead may have already
src/peer/mod.rs:216:                    // been released or closed by another path.
src/peer/mod.rs-217-                    tracing::warn!(
src/peer/mod.rs-218-                        bead = %bead_id,
src/peer/mod.rs-219-                        worker = %peer.worker_id,
src/peer/mod.rs-220-                        error = %e,
src/peer/mod.rs:221:                        "failed to release bead from crashed worker (may already be released)"
src/peer/mod.rs-222-                    );
src/peer/mod.rs-223-                }
src/peer/mod.rs-224-            }
src/peer/mod.rs-225-
src/peer/mod.rs-226-            // Emit peer.crashed telemetry.
src/peer/mod.rs-227-            self.telemetry.emit(EventKind::StuckReleased {
--
src/peer/mod.rs-244-                worker = %peer.worker_id,
src/peer/mod.rs-245-                error = %e,
src/peer/mod.rs-246-                "failed to deregister crashed worker from registry"
src/peer/mod.rs-247-            );
src/peer/mod.rs-248-        }
src/peer/mod.rs-249-
src/peer/mod.rs:250:        Ok(bead_released)
src/peer/mod.rs-251-    }
src/peer/mod.rs-252-}
src/peer/mod.rs-253-
src/peer/mod.rs-254-// ──────────────────────────────────────────────────────────────────────────────
src/peer/mod.rs-255-// Helpers
src/peer/mod.rs-256-// ──────────────────────────────────────────────────────────────────────────────
--
src/peer/mod.rs-288-    use std::path::PathBuf;
src/peer/mod.rs-289-    use std::sync::atomic::{AtomicU32, Ordering};
src/peer/mod.rs-290-    use std::sync::Arc;
src/peer/mod.rs-291-
src/peer/mod.rs-292-    // ── Mock bead store ─────────────────────────────────────────────────────
src/peer/mod.rs-293-
src/peer/mod.rs:294:    /// Track which beads were released.
src/peer/mod.rs-295-    struct MockBeadStore {
src/peer/mod.rs:296:        release_count: Arc<AtomicU32>,
src/peer/mod.rs-297-    }
src/peer/mod.rs-298-
src/peer/mod.rs-299-    impl MockBeadStore {
src/peer/mod.rs-300-        fn new() -> (Self, Arc<AtomicU32>) {
src/peer/mod.rs-301-            let count = Arc::new(AtomicU32::new(0));
src/peer/mod.rs-302-            (
src/peer/mod.rs-303-                MockBeadStore {
src/peer/mod.rs:304:                    release_count: count.clone(),
src/peer/mod.rs-305-                },
src/peer/mod.rs-306-                count,
src/peer/mod.rs-307-            )
src/peer/mod.rs-308-        }
src/peer/mod.rs-309-    }
src/peer/mod.rs-310-
--
src/peer/mod.rs-324-        }
src/peer/mod.rs-325-
src/peer/mod.rs-326-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/peer/mod.rs-327-            anyhow::bail!("not implemented in mock")
src/peer/mod.rs-328-        }
src/peer/mod.rs-329-
src/peer/mod.rs:330:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/peer/mod.rs:331:            self.release_count.fetch_add(1, Ordering::Relaxed);
src/peer/mod.rs-332-            Ok(())
src/peer/mod.rs-333-        }
src/peer/mod.rs-334-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/peer/mod.rs-335-            Ok(())
src/peer/mod.rs-336-        }
src/peer/mod.rs-337-        async fn flush(&self) -> Result<()> {
--
src/peer/mod.rs-437-        // Write a fresh (non-stale) heartbeat for another worker.
src/peer/mod.rs-438-        write_heartbeat(
src/peer/mod.rs-439-            hb_dir,
src/peer/mod.rs-440-            &make_heartbeat("other-worker", std::process::id(), Some("nd-abc"), false),
src/peer/mod.rs-441-        );
src/peer/mod.rs-442-
src/peer/mod.rs:443:        let (store, release_count) = MockBeadStore::new();
src/peer/mod.rs-444-        let registry = Registry::new(reg_dir.path());
src/peer/mod.rs-445-        let telemetry = Telemetry::new("test-monitor".to_string());
src/peer/mod.rs-446-
src/peer/mod.rs-447-        let monitor = PeerMonitor::new(
src/peer/mod.rs-448-            hb_dir.to_path_buf(),
src/peer/mod.rs-449-            Duration::from_secs(300),
--
src/peer/mod.rs-453-            telemetry,
src/peer/mod.rs-454-        );
src/peer/mod.rs-455-
src/peer/mod.rs-456-        let result = monitor.check_peers().await.unwrap();
src/peer/mod.rs-457-        assert_eq!(result.crashed_count, 0);
src/peer/mod.rs-458-        assert_eq!(result.stuck_count, 0);
src/peer/mod.rs:459:        assert_eq!(result.beads_released, 0);
src/peer/mod.rs-460-        assert!(!result.did_work());
src/peer/mod.rs:461:        assert_eq!(release_count.load(Ordering::Relaxed), 0);
src/peer/mod.rs-462-    }
src/peer/mod.rs-463-
src/peer/mod.rs-464-    #[tokio::test]
src/peer/mod.rs:465:    async fn crashed_peer_bead_released() {
src/peer/mod.rs-466-        let dir = tempfile::tempdir().unwrap();
src/peer/mod.rs-467-        let hb_dir = dir.path();
src/peer/mod.rs-468-        let reg_dir = tempfile::tempdir().unwrap();
src/peer/mod.rs-469-
src/peer/mod.rs-470-        // Write a stale heartbeat with a dead PID (99999999).
src/peer/mod.rs-471-        write_heartbeat(
src/peer/mod.rs-472-            hb_dir,
src/peer/mod.rs-473-            &make_heartbeat("dead-worker", 99_999_999, Some("nd-orphan"), true),
src/peer/mod.rs-474-        );
src/peer/mod.rs-475-
src/peer/mod.rs:476:        let (store, release_count) = MockBeadStore::new();
src/peer/mod.rs-477-        let registry = Registry::new(reg_dir.path());
src/peer/mod.rs-478-        let telemetry = Telemetry::new("test-monitor".to_string());
src/peer/mod.rs-479-
src/peer/mod.rs-480-        let monitor = PeerMonitor::new(
src/peer/mod.rs-481-            hb_dir.to_path_buf(),
src/peer/mod.rs-482-            Duration::from_secs(300),
--
src/peer/mod.rs-486-            telemetry,
src/peer/mod.rs-487-        );
src/peer/mod.rs-488-
src/peer/mod.rs-489-        let result = monitor.check_peers().await.unwrap();
src/peer/mod.rs-490-        assert_eq!(result.crashed_count, 1);
src/peer/mod.rs-491-        assert_eq!(result.stuck_count, 0);
src/peer/mod.rs:492:        assert_eq!(result.beads_released, 1);
src/peer/mod.rs-493-        assert!(result.did_work());
src/peer/mod.rs:494:        assert_eq!(release_count.load(Ordering::Relaxed), 1);
src/peer/mod.rs-495-
src/peer/mod.rs-496-        // Heartbeat file should be removed.
src/peer/mod.rs-497-        let hb_path = hb_dir.join("claude-dead-worker.json");
src/peer/mod.rs-498-        assert!(!hb_path.exists(), "heartbeat file should be removed");
src/peer/mod.rs-499-    }
src/peer/mod.rs-500-
src/peer/mod.rs-501-    #[tokio::test]
src/peer/mod.rs:502:    async fn stuck_peer_not_released() {
src/peer/mod.rs-503-        let dir = tempfile::tempdir().unwrap();
src/peer/mod.rs-504-        let hb_dir = dir.path();
src/peer/mod.rs-505-        let reg_dir = tempfile::tempdir().unwrap();
src/peer/mod.rs-506-
src/peer/mod.rs-507-        // Write a stale heartbeat with our own PID (alive).
src/peer/mod.rs-508-        // Use a different worker_id so it's not skipped as "self".
src/peer/mod.rs-509-        write_heartbeat(
src/peer/mod.rs-510-            hb_dir,
src/peer/mod.rs-511-            &make_heartbeat("stuck-worker", std::process::id(), Some("nd-busy"), true),
src/peer/mod.rs-512-        );
src/peer/mod.rs-513-
src/peer/mod.rs:514:        let (store, release_count) = MockBeadStore::new();
src/peer/mod.rs-515-        let registry = Registry::new(reg_dir.path());
src/peer/mod.rs-516-        let telemetry = Telemetry::new("test-monitor".to_string());
src/peer/mod.rs-517-
src/peer/mod.rs-518-        let monitor = PeerMonitor::new(
src/peer/mod.rs-519-            hb_dir.to_path_buf(),
src/peer/mod.rs-520-            Duration::from_secs(300),
--
src/peer/mod.rs-524-            telemetry,
src/peer/mod.rs-525-        );
src/peer/mod.rs-526-
src/peer/mod.rs-527-        let result = monitor.check_peers().await.unwrap();
src/peer/mod.rs-528-        assert_eq!(result.crashed_count, 0);
src/peer/mod.rs-529-        assert_eq!(result.stuck_count, 1);
src/peer/mod.rs:530:        assert_eq!(result.beads_released, 0);
src/peer/mod.rs-531-        assert!(!result.did_work());
src/peer/mod.rs:532:        assert_eq!(release_count.load(Ordering::Relaxed), 0);
src/peer/mod.rs-533-
src/peer/mod.rs-534-        // Heartbeat file should NOT be removed for stuck workers.
src/peer/mod.rs-535-        let hb_path = hb_dir.join("claude-stuck-worker.json");
src/peer/mod.rs-536-        assert!(
src/peer/mod.rs-537-            hb_path.exists(),
src/peer/mod.rs-538-            "heartbeat file should remain for stuck worker"
--
src/peer/mod.rs-548-        // Write a stale heartbeat for ourselves (should be skipped).
src/peer/mod.rs-549-        write_heartbeat(
src/peer/mod.rs-550-            hb_dir,
src/peer/mod.rs-551-            &make_heartbeat("my-worker", std::process::id(), Some("nd-mine"), true),
src/peer/mod.rs-552-        );
src/peer/mod.rs-553-
src/peer/mod.rs:554:        let (store, release_count) = MockBeadStore::new();
src/peer/mod.rs-555-        let registry = Registry::new(reg_dir.path());
src/peer/mod.rs-556-        let telemetry = Telemetry::new("test-monitor".to_string());
src/peer/mod.rs-557-
src/peer/mod.rs-558-        let monitor = PeerMonitor::new(
src/peer/mod.rs-559-            hb_dir.to_path_buf(),
src/peer/mod.rs-560-            Duration::from_secs(300),
--
src/peer/mod.rs-564-            telemetry,
src/peer/mod.rs-565-        );
src/peer/mod.rs-566-
src/peer/mod.rs-567-        let result = monitor.check_peers().await.unwrap();
src/peer/mod.rs-568-        assert_eq!(result.crashed_count, 0);
src/peer/mod.rs-569-        assert_eq!(result.stuck_count, 0);
src/peer/mod.rs:570:        assert_eq!(result.beads_released, 0);
src/peer/mod.rs:571:        assert_eq!(release_count.load(Ordering::Relaxed), 0);
src/peer/mod.rs-572-    }
src/peer/mod.rs-573-
src/peer/mod.rs-574-    #[tokio::test]
src/peer/mod.rs-575-    async fn crashed_peer_no_bead_still_cleaned() {
src/peer/mod.rs-576-        let dir = tempfile::tempdir().unwrap();
src/peer/mod.rs-577-        let hb_dir = dir.path();
--
src/peer/mod.rs-592-                provider: None,
src/peer/mod.rs-593-                started_at: Utc::now(),
src/peer/mod.rs-594-                beads_processed: 0,
src/peer/mod.rs-595-            })
src/peer/mod.rs-596-            .unwrap();
src/peer/mod.rs-597-
src/peer/mod.rs:598:        let (store, release_count) = MockBeadStore::new();
src/peer/mod.rs-599-        let telemetry = Telemetry::new("test-monitor".to_string());
src/peer/mod.rs-600-
src/peer/mod.rs-601-        let monitor = PeerMonitor::new(
src/peer/mod.rs-602-            hb_dir.to_path_buf(),
src/peer/mod.rs-603-            Duration::from_secs(300),
src/peer/mod.rs-604-            "my-worker".to_string(),
--
src/peer/mod.rs-606-            &registry,
src/peer/mod.rs-607-            telemetry,
src/peer/mod.rs-608-        );
src/peer/mod.rs-609-
src/peer/mod.rs-610-        let result = monitor.check_peers().await.unwrap();
src/peer/mod.rs-611-        assert_eq!(result.crashed_count, 1);
src/peer/mod.rs:612:        assert_eq!(result.beads_released, 0); // no bead to release
src/peer/mod.rs:613:        assert_eq!(release_count.load(Ordering::Relaxed), 0);
src/peer/mod.rs-614-
src/peer/mod.rs-615-        // Heartbeat file should be removed.
src/peer/mod.rs-616-        assert!(!hb_dir.join("claude-dead-idle.json").exists());
src/peer/mod.rs-617-
src/peer/mod.rs-618-        // Worker should be deregistered.
src/peer/mod.rs-619-        let workers = registry.list().unwrap();
--
src/peer/mod.rs-644-        // Our own heartbeat (should be skipped even if stale).
src/peer/mod.rs-645-        write_heartbeat(
src/peer/mod.rs-646-            hb_dir,
src/peer/mod.rs-647-            &make_heartbeat("my-worker", std::process::id(), Some("nd-4"), true),
src/peer/mod.rs-648-        );
src/peer/mod.rs-649-
src/peer/mod.rs:650:        let (store, release_count) = MockBeadStore::new();
src/peer/mod.rs-651-        let registry = Registry::new(reg_dir.path());
src/peer/mod.rs-652-        let telemetry = Telemetry::new("test-monitor".to_string());
src/peer/mod.rs-653-
src/peer/mod.rs-654-        let monitor = PeerMonitor::new(
src/peer/mod.rs-655-            hb_dir.to_path_buf(),
src/peer/mod.rs-656-            Duration::from_secs(300),
--
src/peer/mod.rs-660-            telemetry,
src/peer/mod.rs-661-        );
src/peer/mod.rs-662-
src/peer/mod.rs-663-        let result = monitor.check_peers().await.unwrap();
src/peer/mod.rs-664-        assert_eq!(result.crashed_count, 1);
src/peer/mod.rs-665-        assert_eq!(result.stuck_count, 1);
src/peer/mod.rs:666:        assert_eq!(result.beads_released, 1);
src/peer/mod.rs-667-        assert!(result.did_work());
src/peer/mod.rs:668:        assert_eq!(release_count.load(Ordering::Relaxed), 1);
src/peer/mod.rs-669-
src/peer/mod.rs-670-        // Only the crashed peer's heartbeat should be removed.
src/peer/mod.rs-671-        assert!(hb_dir.join("claude-healthy.json").exists());
src/peer/mod.rs-672-        assert!(hb_dir.join("claude-stuck.json").exists());
src/peer/mod.rs-673-        assert!(!hb_dir.join("claude-crashed.json").exists());
src/peer/mod.rs-674-        assert!(hb_dir.join("claude-my-worker.json").exists());
--
src/peer/mod.rs-693-            telemetry,
src/peer/mod.rs-694-        );
src/peer/mod.rs-695-
src/peer/mod.rs-696-        let result = monitor.check_peers().await.unwrap();
src/peer/mod.rs-697-        assert_eq!(result.crashed_count, 0);
src/peer/mod.rs-698-        assert_eq!(result.stuck_count, 0);
src/peer/mod.rs:699:        assert_eq!(result.beads_released, 0);
src/peer/mod.rs-700-    }
src/peer/mod.rs-701-
src/peer/mod.rs-702-    #[tokio::test]
src/peer/mod.rs-703-    async fn nonexistent_heartbeat_dir_returns_zero() {
src/peer/mod.rs-704-        let reg_dir = tempfile::tempdir().unwrap();
src/peer/mod.rs-705-        let (store, _) = MockBeadStore::new();
--
src/peer/mod.rs-715-            telemetry,
src/peer/mod.rs-716-        );
src/peer/mod.rs-717-
src/peer/mod.rs-718-        let result = monitor.check_peers().await.unwrap();
src/peer/mod.rs-719-        assert_eq!(result.crashed_count, 0);
src/peer/mod.rs-720-        assert_eq!(result.stuck_count, 0);
src/peer/mod.rs:721:        assert_eq!(result.beads_released, 0);
src/peer/mod.rs-722-    }
src/peer/mod.rs-723-
src/peer/mod.rs-724-    #[test]
src/peer/mod.rs-725-    fn remove_heartbeat_file_nonexistent_is_ok() {
src/peer/mod.rs-726-        let result = remove_heartbeat_file(Path::new("/nonexistent/file.json"));
src/peer/mod.rs-727-        assert!(result.is_ok());
--
src/peer/mod.rs-740-
src/peer/mod.rs-741-    #[test]
src/peer/mod.rs-742-    fn peer_check_result_did_work() {
src/peer/mod.rs-743-        let no_work = PeerCheckResult {
src/peer/mod.rs-744-            crashed_count: 0,
src/peer/mod.rs-745-            stuck_count: 1, // stuck doesn't count as "work"
src/peer/mod.rs:746:            beads_released: 0,
src/peer/mod.rs-747-        };
src/peer/mod.rs-748-        assert!(!no_work.did_work());
src/peer/mod.rs-749-
src/peer/mod.rs-750-        let did_work = PeerCheckResult {
src/peer/mod.rs-751-            crashed_count: 1,
src/peer/mod.rs-752-            stuck_count: 0,
src/peer/mod.rs:753:            beads_released: 1,
src/peer/mod.rs-754-        };
src/peer/mod.rs-755-        assert!(did_work.did_work());
src/peer/mod.rs-756-    }
src/peer/mod.rs-757-}
--
src/dispatch/mod.rs-1863-        } else {
src/dispatch/mod.rs-1864-            None
src/dispatch/mod.rs-1865-        };
src/dispatch/mod.rs-1866-
src/dispatch/mod.rs-1867-        // Release tsnet identity if it was provisioned
src/dispatch/mod.rs-1868-        if let (Some(ref identity), Some(ref registry)) = (&tsnet_identity, &self.tsnet_registry) {
src/dispatch/mod.rs:1869:            registry.release_identity(&identity.hostname).await;
src/dispatch/mod.rs-1870-        }
src/dispatch/mod.rs-1871-
src/dispatch/mod.rs-1872-        Ok(ExecutionResult {
src/dispatch/mod.rs-1873-            exit_code,
src/dispatch/mod.rs-1874-            stdout,
src/dispatch/mod.rs-1875-            stderr,
--
src/dispatch/mod.rs-3479-                Path::new("/tmp"),
src/dispatch/mod.rs-3480-            )
src/dispatch/mod.rs-3481-            .await
src/dispatch/mod.rs-3482-            .unwrap();
src/dispatch/mod.rs-3483-
src/dispatch/mod.rs-3484-        assert_eq!(result.exit_code, 1);
src/dispatch/mod.rs:3485:        assert_eq!(Outcome::classify(result.exit_code, false), Outcome::Failure);
src/dispatch/mod.rs-3486-    }
src/dispatch/mod.rs-3487-
src/dispatch/mod.rs-3488-    #[tokio::test]
src/dispatch/mod.rs-3489-    async fn e2e_exit_code_2_is_failure() {
src/dispatch/mod.rs-3490-        use crate::types::Outcome;
src/dispatch/mod.rs-3491-
--
src/dispatch/mod.rs-3502-                Path::new("/tmp"),
src/dispatch/mod.rs-3503-            )
src/dispatch/mod.rs-3504-            .await
src/dispatch/mod.rs-3505-            .unwrap();
src/dispatch/mod.rs-3506-
src/dispatch/mod.rs-3507-        assert_eq!(result.exit_code, 2);
src/dispatch/mod.rs:3508:        assert_eq!(Outcome::classify(result.exit_code, false), Outcome::Failure);
src/dispatch/mod.rs-3509-    }
src/dispatch/mod.rs-3510-
src/dispatch/mod.rs-3511-    #[tokio::test]
src/dispatch/mod.rs-3512-    async fn e2e_exit_code_137_is_crash() {
src/dispatch/mod.rs-3513-        use crate::types::Outcome;
src/dispatch/mod.rs-3514-
--
src/canary/mod.rs-17-//! - `.beads/` — Bead database with test beads
src/canary/mod.rs-18-//! - `expected/` — Expected outcome files for each test bead
src/canary/mod.rs-19-//!
src/canary/mod.rs-20-//! ## Test Scenarios
src/canary/mod.rs-21-//!
src/canary/mod.rs-22-//! - Happy path: Agent completes successfully, bead closed
src/canary/mod.rs:23://! - Failure path: Agent fails, bead released with failure label
src/canary/mod.rs-24-//! - Timeout: Agent times out, bead deferred
src/canary/mod.rs-25-//! - State machine integrity: Worker state transitions are valid
src/canary/mod.rs-26-
src/canary/mod.rs-27-use std::path::{Path, PathBuf};
src/canary/mod.rs-28-use std::process::Command;
src/canary/mod.rs-29-use std::time::{Duration, Instant};
--
src/canary/mod.rs-446-            "ready",
src/canary/mod.rs-447-            "deferred",
src/canary/mod.rs-448-        ];
src/canary/mod.rs-449-
src/canary/mod.rs-450-        let status_to_check = match expected {
src/canary/mod.rs-451-            ExpectedOutcome::Success { final_status, .. } => Some(final_status.as_str()),
src/canary/mod.rs:452:            ExpectedOutcome::Failure { final_status, .. } => Some(final_status.as_str()),
src/canary/mod.rs-453-            ExpectedOutcome::Timeout { final_status } => Some(final_status.as_str()),
src/canary/mod.rs-454-            ExpectedOutcome::StateMachine { .. } => None, // State machine tests don't check final_status
src/canary/mod.rs-455-        };
src/canary/mod.rs-456-
src/canary/mod.rs-457-        if let Some(status) = status_to_check {
src/canary/mod.rs-458-            if !valid_statuses.contains(&status) {
--
src/canary/mod.rs-694-                labels,
src/canary/mod.rs-695-            } => {
src/canary/mod.rs-696-                actual.final_status == *final_status
src/canary/mod.rs-697-                    && labels.iter().all(|l| actual.labels.contains(l))
src/canary/mod.rs-698-                    && actual.exit_code == Some(0)
src/canary/mod.rs-699-            }
src/canary/mod.rs:700:            ExpectedOutcome::Failure {
src/canary/mod.rs-701-                final_status,
src/canary/mod.rs-702-                labels,
src/canary/mod.rs-703-            } => {
src/canary/mod.rs-704-                actual.final_status == *final_status
src/canary/mod.rs-705-                    && labels.iter().all(|l| actual.labels.contains(l))
src/canary/mod.rs-706-                    && actual.exit_code.is_some_and(|c| c != 0)
--
src/canary/mod.rs-740-                }
src/canary/mod.rs-741-                if actual.exit_code != Some(0) {
src/canary/mod.rs-742-                    reasons.push(format!("expected exit 0, got {:?}", actual.exit_code));
src/canary/mod.rs-743-                }
src/canary/mod.rs-744-                reasons.join("; ")
src/canary/mod.rs-745-            }
src/canary/mod.rs:746:            ExpectedOutcome::Failure {
src/canary/mod.rs-747-                final_status,
src/canary/mod.rs-748-                labels,
src/canary/mod.rs-749-            } => {
src/canary/mod.rs-750-                let mut reasons = Vec::new();
src/canary/mod.rs-751-                if actual.final_status != *final_status {
src/canary/mod.rs-752-                    reasons.push(format!(
--
src/canary/mod.rs-889-            "rolled back to previous stable"
src/canary/mod.rs-890-        );
src/canary/mod.rs-891-
src/canary/mod.rs-892-        Ok(())
src/canary/mod.rs-893-    }
src/canary/mod.rs-894-
src/canary/mod.rs:895:    /// Check the current release channel status.
src/canary/mod.rs-896-    pub fn status(&self) -> Result<ChannelStatus> {
src/canary/mod.rs-897-        let testing = self.testing_binary();
src/canary/mod.rs-898-        let stable = self.stable_binary();
src/canary/mod.rs-899-        let prev = self.prev_binary();
src/canary/mod.rs-900-        let symlink = self.symlink_path();
src/canary/mod.rs-901-
--
src/canary/mod.rs-923-}
src/canary/mod.rs-924-
src/canary/mod.rs-925-// ──────────────────────────────────────────────────────────────────────────────
src/canary/mod.rs-926-// ChannelStatus
src/canary/mod.rs-927-// ──────────────────────────────────────────────────────────────────────────────
src/canary/mod.rs-928-
src/canary/mod.rs:929:/// Status of the release channels.
src/canary/mod.rs-930-#[derive(Debug, Clone)]
src/canary/mod.rs-931-pub struct ChannelStatus {
src/canary/mod.rs-932-    /// Whether :testing binary exists.
src/canary/mod.rs-933-    pub testing_exists: bool,
src/canary/mod.rs-934-    /// Whether :stable binary exists.
src/canary/mod.rs-935-    pub stable_exists: bool,
--
src/canary/mod.rs-1117-        let runner = CanaryRunner::new(
src/canary/mod.rs-1118-            PathBuf::from("/tmp/.needle"),
src/canary/mod.rs-1119-            PathBuf::from("/tmp/canary"),
src/canary/mod.rs-1120-            300,
src/canary/mod.rs-1121-        );
src/canary/mod.rs-1122-
src/canary/mod.rs:1123:        let expected = ExpectedOutcome::Failure {
src/canary/mod.rs-1124-            final_status: "open".to_string(),
src/canary/mod.rs-1125-            labels: vec!["failure-count:1".to_string()],
src/canary/mod.rs-1126-        };
src/canary/mod.rs-1127-
src/canary/mod.rs-1128-        let actual = ActualOutcome {
src/canary/mod.rs-1129-            exit_code: Some(1),
--
src/canary/mod.rs-1211-        let runner = CanaryRunner::new(
src/canary/mod.rs-1212-            PathBuf::from("/tmp/.needle"),
src/canary/mod.rs-1213-            PathBuf::from("/tmp/canary"),
src/canary/mod.rs-1214-            300,
src/canary/mod.rs-1215-        );
src/canary/mod.rs-1216-
src/canary/mod.rs:1217:        let expected = ExpectedOutcome::Failure {
src/canary/mod.rs-1218-            final_status: "open".to_string(),
src/canary/mod.rs-1219-            labels: vec!["failure-count:1".to_string()],
src/canary/mod.rs-1220-        };
src/canary/mod.rs-1221-
src/canary/mod.rs-1222-        let actual = ActualOutcome {
src/canary/mod.rs-1223-            exit_code: Some(0),
--
src/canary/mod.rs-1400-
src/canary/mod.rs-1401-        let runner =
src/canary/mod.rs-1402-            CanaryRunner::new(PathBuf::from("/tmp/.needle"), tmp.path().to_path_buf(), 300);
src/canary/mod.rs-1403-
src/canary/mod.rs-1404-        let outcome = runner.load_expected_outcome("test-fail").unwrap();
src/canary/mod.rs-1405-        match outcome {
src/canary/mod.rs:1406:            ExpectedOutcome::Failure {
src/canary/mod.rs-1407-                final_status,
src/canary/mod.rs-1408-                labels,
src/canary/mod.rs-1409-            } => {
src/canary/mod.rs-1410-                assert_eq!(final_status, "open");
src/canary/mod.rs-1411-                assert_eq!(labels, vec!["failure-count:1".to_string()]);
src/canary/mod.rs-1412-            }
--
src/canary/mod.rs-1650-    fn expected_outcome_serde_roundtrip() {
src/canary/mod.rs-1651-        let outcomes = vec![
src/canary/mod.rs-1652-            ExpectedOutcome::Success {
src/canary/mod.rs-1653-                final_status: "done".to_string(),
src/canary/mod.rs-1654-                labels: vec!["verified".to_string()],
src/canary/mod.rs-1655-            },
src/canary/mod.rs:1656:            ExpectedOutcome::Failure {
src/canary/mod.rs-1657-                final_status: "open".to_string(),
src/canary/mod.rs-1658-                labels: vec!["failure-count:1".to_string()],
src/canary/mod.rs-1659-            },
src/canary/mod.rs-1660-            ExpectedOutcome::Timeout {
src/canary/mod.rs-1661-                final_status: "open".to_string(),
src/canary/mod.rs-1662-            },
--
src/canary/mod.rs-1747-        let runner = CanaryRunner::new(
src/canary/mod.rs-1748-            PathBuf::from("/tmp/.needle"),
src/canary/mod.rs-1749-            PathBuf::from("/tmp/canary"),
src/canary/mod.rs-1750-            300,
src/canary/mod.rs-1751-        );
src/canary/mod.rs-1752-
src/canary/mod.rs:1753:        let expected = ExpectedOutcome::Failure {
src/canary/mod.rs-1754-            final_status: "open".to_string(),
src/canary/mod.rs-1755-            labels: vec![],
src/canary/mod.rs-1756-        };
src/canary/mod.rs-1757-
src/canary/mod.rs-1758-        // Exit code is None (e.g., killed by signal) — is_some_and(|c| c != 0) is false.
src/canary/mod.rs-1759-        let actual = ActualOutcome {
--
src/rate_limit/mod.rs-331-            )
src/rate_limit/mod.rs-332-        })?;
src/rate_limit/mod.rs-333-
src/rate_limit/mod.rs-334-        // Release lock.
src/rate_limit/mod.rs-335-        FileExt::unlock(&file).with_context(|| {
src/rate_limit/mod.rs-336-            format!(
src/rate_limit/mod.rs:337:                "failed to release lock on rate limit bucket: {}",
src/rate_limit/mod.rs-338-                bucket_path.display()
src/rate_limit/mod.rs-339-            )
src/rate_limit/mod.rs-340-        })?;
src/rate_limit/mod.rs-341-
src/rate_limit/mod.rs-342-        Ok(allowed)
src/rate_limit/mod.rs-343-    }
--
src/test_runner.rs-695-        assert_eq!(runner.timeout_secs(), 60);
src/test_runner.rs-696-    }
src/test_runner.rs-697-
src/test_runner.rs-698-    #[test]
src/test_runner.rs-699-    fn test_runner_with_extra_arg() {
src/test_runner.rs-700-        let runner = TestRunner::new(Path::new("/tmp"))
src/test_runner.rs:701:            .with_extra_arg("--release")
src/test_runner.rs-702-            .with_extra_arg("--lib");
src/test_runner.rs-703-        assert_eq!(runner.extra_args().len(), 2);
src/test_runner.rs:704:        assert_eq!(runner.extra_args()[0], "--release");
src/test_runner.rs-705-        assert_eq!(runner.extra_args()[1], "--lib");
src/test_runner.rs-706-    }
src/test_runner.rs-707-
src/test_runner.rs-708-    // ──────────────────────────────────────────────────────────────────────────────
src/test_runner.rs-709-    // File Persistence Tests
src/test_runner.rs-710-    // ──────────────────────────────────────────────────────────────────────────────
--
src/cli/mod.rs-291-    Canary {
src/cli/mod.rs-292-        /// Show channel status instead of running tests.
src/cli/mod.rs-293-        #[arg(long)]
src/cli/mod.rs-294-        status: bool,
src/cli/mod.rs-295-    },
src/cli/mod.rs-296-
src/cli/mod.rs:297:    /// Check for and install updates from GitHub releases.
src/cli/mod.rs-298-    ///
src/cli/mod.rs-299-    /// Do not cp/mv onto ~/.local/bin/needle or ~/.needle/bin/needle-stable while any worker is running.
src/cli/mod.rs-300-    /// This causes session disruption or permanent hot-reload stall. Use `needle upgrade` instead.
src/cli/mod.rs-301-    Upgrade {
src/cli/mod.rs-302-        /// Check only — show available update without installing.
src/cli/mod.rs-303-        #[arg(long)]
--
src/cli/mod.rs-5225-        let check = crate::upgrade::check_for_update_with_telemetry(Some(&tel))?;
src/cli/mod.rs-5226-        if check.update_available {
src/cli/mod.rs-5227-            println!(
src/cli/mod.rs-5228-                "Update available: {} → {}",
src/cli/mod.rs-5229-                check.current_version, check.latest_version
src/cli/mod.rs-5230-            );
src/cli/mod.rs:5231:            if let Some(notes) = &check.release_notes {
src/cli/mod.rs-5232-                println!("\nRelease notes:\n{notes}");
src/cli/mod.rs-5233-            }
src/cli/mod.rs-5234-        } else {
src/cli/mod.rs-5235-            println!("Already up to date (version {})", check.current_version);
src/cli/mod.rs-5236-        }
src/cli/mod.rs-5237-        return Ok(());
--
src/integration_t/supervise_auto_scale_gate.rs-100-    async fn claim_auto(&self, _actor: &str) -> Result<needle::types::ClaimResult> {
src/integration_t/supervise_auto_scale_gate.rs-101-        Ok(needle::types::ClaimResult::NotClaimable {
src/integration_t/supervise_auto_scale_gate.rs-102-            reason: "mock store does not support claims".to_string(),
src/integration_t/supervise_auto_scale_gate.rs-103-        })
src/integration_t/supervise_auto_scale_gate.rs-104-    }
src/integration_t/supervise_auto_scale_gate.rs-105-
src/integration_t/supervise_auto_scale_gate.rs:106:    async fn release(&self, _id: &BeadId) -> Result<()> {
src/integration_t/supervise_auto_scale_gate.rs-107-        Ok(())
src/integration_t/supervise_auto_scale_gate.rs-108-    }
src/integration_t/supervise_auto_scale_gate.rs-109-
src/integration_t/supervise_auto_scale_gate.rs-110-    async fn block(&self, _id: &BeadId) -> Result<()> {
src/integration_t/supervise_auto_scale_gate.rs-111-        Ok(())
src/integration_t/supervise_auto_scale_gate.rs-112-    }
--
src/sanitize/mod.rs-772-        );
src/sanitize/mod.rs-773-    }
src/sanitize/mod.rs-774-
src/sanitize/mod.rs-775-    #[test]
src/sanitize/mod.rs-776-    fn sanitizer_performance_100kb_median() {
src/sanitize/mod.rs-777-        // Phase 4 success criterion: sanitization of 100KB trace must
src/sanitize/mod.rs:778:        // complete in <10ms median on release builds (<500ms on debug).
src/sanitize/mod.rs-779-        // This test measures median latency over multiple samples for
src/sanitize/mod.rs-780-        // more stable results than a single measurement.
src/sanitize/mod.rs-781-
src/sanitize/mod.rs-782-        let s = make_sanitizer();
src/sanitize/mod.rs-783-
src/sanitize/mod.rs-784-        // Generate representative 100KB trace content (JSONL format).
--
src/integration_t/mod.rs-424-    async fn claim_auto(&self, _actor: &str) -> Result<needle::types::ClaimResult> {
src/integration_t/mod.rs-425-        Ok(needle::types::ClaimResult::NotClaimable {
src/integration_t/mod.rs-426-            reason: "no beads available".to_string(),
src/integration_t/mod.rs-427-        })
src/integration_t/mod.rs-428-    }
src/integration_t/mod.rs-429-
src/integration_t/mod.rs:430:    async fn release(&self, _id: &BeadId) -> Result<()> {
src/integration_t/mod.rs:431:        // For load simulation, we just remove released beads to prevent re-selection
src/integration_t/mod.rs-432-        Ok(())
src/integration_t/mod.rs-433-    }
src/integration_t/mod.rs-434-
src/integration_t/mod.rs-435-    async fn block(&self, _id: &BeadId) -> Result<()> {
src/integration_t/mod.rs-436-        Ok(())
src/integration_t/mod.rs-437-    }
--
src/cargo_test.rs-56-    /// Test target (e.g., "--lib", "--bins", "--test <name>").
src/cargo_test.rs-57-    pub target: Option<String>,
src/cargo_test.rs-58-    /// Test filter expression (e.g., "test_name").
src/cargo_test.rs-59-    pub filter: Option<String>,
src/cargo_test.rs-60-    /// List of specific tests to run (e.g., ["test1", "test2"]).
src/cargo_test.rs-61-    pub test_names: Vec<String>,
src/cargo_test.rs:62:    /// Additional cargo flags (e.g., "--release", "--features foo").
src/cargo_test.rs-63-    pub cargo_flags: Vec<String>,
src/cargo_test.rs-64-    /// Additional test flags (e.g., "--exact", "--ignored").
src/cargo_test.rs-65-    pub test_flags: Vec<String>,
src/cargo_test.rs-66-}
src/cargo_test.rs-67-
src/cargo_test.rs-68-impl TestArgs {
--
src/cargo_test.rs-86-    /// Add a test name to run.
src/cargo_test.rs-87-    pub fn add_test_name(mut self, name: impl Into<String>) -> Self {
src/cargo_test.rs-88-        self.test_names.push(name.into());
src/cargo_test.rs-89-        self
src/cargo_test.rs-90-    }
src/cargo_test.rs-91-
src/cargo_test.rs:92:    /// Add a cargo flag (e.g., "--release").
src/cargo_test.rs-93-    pub fn add_cargo_flag(mut self, flag: impl Into<String>) -> Self {
src/cargo_test.rs-94-        self.cargo_flags.push(flag.into());
src/cargo_test.rs-95-        self
src/cargo_test.rs-96-    }
src/cargo_test.rs-97-
src/cargo_test.rs-98-    /// Add a test flag (e.g., "--exact").
--
src/cargo_test.rs-1056-        let args = TestArgs::new().add_test_name("test_one");
src/cargo_test.rs-1057-        assert_eq!(args.test_names, vec!["test_one"]);
src/cargo_test.rs-1058-    }
src/cargo_test.rs-1059-
src/cargo_test.rs-1060-    #[test]
src/cargo_test.rs-1061-    fn test_args_add_cargo_flag() {
src/cargo_test.rs:1062:        let args = TestArgs::new().add_cargo_flag("--release");
src/cargo_test.rs:1063:        assert_eq!(args.cargo_flags, vec!["--release"]);
src/cargo_test.rs-1064-    }
src/cargo_test.rs-1065-
src/cargo_test.rs-1066-    #[test]
src/cargo_test.rs-1067-    fn test_args_add_test_flag() {
src/cargo_test.rs-1068-        let args = TestArgs::new().add_test_flag("--exact");
src/cargo_test.rs-1069-        assert_eq!(args.test_flags, vec!["--exact"]);
--
src/cargo_test.rs-1083-        assert_eq!(built, vec!["test", "--lib"]);
src/cargo_test.rs-1084-    }
src/cargo_test.rs-1085-
src/cargo_test.rs-1086-    #[test]
src/cargo_test.rs-1087-    fn test_args_build_with_cargo_flags() {
src/cargo_test.rs-1088-        let args = TestArgs::new()
src/cargo_test.rs:1089:            .add_cargo_flag("--release")
src/cargo_test.rs-1090-            .add_cargo_flag("--features")
src/cargo_test.rs-1091-            .add_cargo_flag("foo");
src/cargo_test.rs-1092-        let built = args.build_args();
src/cargo_test.rs:1093:        assert_eq!(built, vec!["--release", "--features", "foo", "test"]);
src/cargo_test.rs-1094-    }
src/cargo_test.rs-1095-
src/cargo_test.rs-1096-    #[test]
src/cargo_test.rs-1097-    fn test_args_build_with_test_flags() {
src/cargo_test.rs-1098-        let args = TestArgs::new().add_test_flag("--exact");
src/cargo_test.rs-1099-        let built = args.build_args();
src/cargo_test.rs-1100-        assert_eq!(built, vec!["test", "--exact"]);
src/cargo_test.rs-1101-    }
src/cargo_test.rs-1102-
src/cargo_test.rs-1103-    #[test]
src/cargo_test.rs-1104-    fn test_args_build_full() {
src/cargo_test.rs-1105-        let args = TestArgs::new()
src/cargo_test.rs:1106:            .add_cargo_flag("--release")
src/cargo_test.rs-1107-            .with_target("--lib")
src/cargo_test.rs-1108-            .add_test_flag("--exact")
src/cargo_test.rs-1109-            .add_test_name("test_one");
src/cargo_test.rs-1110-        let built = args.build_args();
src/cargo_test.rs-1111-        assert_eq!(
src/cargo_test.rs-1112-            built,
src/cargo_test.rs:1113:            vec!["--release", "test", "--lib", "--exact", "--", "test_one"]
src/cargo_test.rs-1114-        );
src/cargo_test.rs-1115-    }
src/cargo_test.rs-1116-
src/cargo_test.rs-1117-    #[test]
src/cargo_test.rs-1118-    fn test_outcome_success_with_zero_exit() {
src/cargo_test.rs-1119-        let outcome = TestOutcome {
--
src/tsnet.rs-208-            "tsnet auth key generation failed: no Tailscale API key source configured for hostname {}",
src/tsnet.rs-209-            hostname
src/tsnet.rs-210-        );
src/tsnet.rs-211-    }
src/tsnet.rs-212-
src/tsnet.rs-213-    /// Mark an identity as used (remove from registry).
src/tsnet.rs:214:    pub async fn release_identity(&self, hostname: &str) {
src/tsnet.rs-215-        let mut identities = self.identities.write().await;
src/tsnet.rs-216-        if let Some(identity) = identities.remove(hostname) {
src/tsnet.rs-217-            tracing::debug!(
src/tsnet.rs-218-                worker_id = %identity.worker_id,
src/tsnet.rs-219-                bead_id = %identity.bead_id.as_ref(),
src/tsnet.rs-220-                hostname = %hostname,
src/tsnet.rs:221:                "released tsnet identity"
src/tsnet.rs-222-            );
src/tsnet.rs-223-        }
src/tsnet.rs-224-    }
src/tsnet.rs-225-
src/tsnet.rs-226-    /// Clean up expired identities.
src/tsnet.rs-227-    pub async fn cleanup_expired(&self) -> Result<()> {
--
src/tsnet.rs-360-        assert!(err
src/tsnet.rs-361-            .to_string()
src/tsnet.rs-362-            .contains("no Tailscale API key source configured"));
src/tsnet.rs-363-    }
src/tsnet.rs-364-
src/tsnet.rs-365-    #[tokio::test]
src/tsnet.rs:366:    async fn test_identity_registry_release() {
src/tsnet.rs-367-        let config = TsnetConfig {
src/tsnet.rs-368-            enabled: true,
src/tsnet.rs-369-            ..Default::default()
src/tsnet.rs-370-        };
src/tsnet.rs-371-        let registry = IdentityRegistry::new(config);
src/tsnet.rs-372-
--
src/tsnet.rs-378-        assert!(result.is_err());
src/tsnet.rs-379-
src/tsnet.rs-380-        // No identities should be registered since provisioning failed
src/tsnet.rs-381-        assert_eq!(registry.active_count().await, 0);
src/tsnet.rs-382-
src/tsnet.rs-383-        // Release on a non-existent identity should be safe (no-op)
src/tsnet.rs:384:        registry.release_identity("needle-worker-1-bf-test").await;
src/tsnet.rs-385-        assert_eq!(registry.active_count().await, 0);
src/tsnet.rs-386-    }
src/tsnet.rs-387-
src/tsnet.rs-388-    #[test]
src/tsnet.rs-389-    fn test_inject_identity_env() {
src/tsnet.rs-390-        let config = TsnetConfig {
--
src/claim/mod.rs-462-    }
src/claim/mod.rs-463-
src/claim/mod.rs-464-    /// Verify that a bead is still assigned to the expected worker at dispatch time.
src/claim/mod.rs-465-    ///
src/claim/mod.rs-466-    /// This prevents double-dispatch by checking the live bead store immediately
src/claim/mod.rs-467-    /// before agent execution. If another worker has reassigned the bead or the
src/claim/mod.rs:468:    /// bead has been released, the dispatch should be aborted.
src/claim/mod.rs-469-    ///
src/claim/mod.rs-470-    /// Returns:
src/claim/mod.rs-471-    /// - `Ok(true)`: bead is still in_progress and assigned to expected actor
src/claim/mod.rs-472-    /// - `Ok(false)`: bead is not assigned to expected actor (dispatch should abort)
src/claim/mod.rs-473-    /// - `Err(e)`: store error
src/claim/mod.rs-474-    pub async fn verify_claim_at_dispatch(
--
src/claim/mod.rs-603-    let hash = hasher.finish();
src/claim/mod.rs-604-    lock_dir.join(format!("needle-claim-{:016x}.lock", hash))
src/claim/mod.rs-605-}
src/claim/mod.rs-606-
src/claim/mod.rs-607-/// Acquire an exclusive flock with a 10-second timeout.
src/claim/mod.rs-608-///
src/claim/mod.rs:609:/// Returns the locked file on success. The lock is released when the
src/claim/mod.rs:610:/// file is dropped (flock auto-releases on close).
src/claim/mod.rs-611-pub async fn acquire_flock(lock_path: &Path) -> Result<std::fs::File> {
src/claim/mod.rs-612-    if let Some(parent) = lock_path.parent() {
src/claim/mod.rs-613-        std::fs::create_dir_all(parent)?;
src/claim/mod.rs-614-    }
src/claim/mod.rs-615-
src/claim/mod.rs-616-    let file = std::fs::OpenOptions::new()
--
src/claim/mod.rs-757-            // Return NotClaimable for tests unless overridden
src/claim/mod.rs-758-            Ok(ClaimResult::NotClaimable {
src/claim/mod.rs-759-                reason: "no beads available".to_string(),
src/claim/mod.rs-760-            })
src/claim/mod.rs-761-        }
src/claim/mod.rs-762-
src/claim/mod.rs:763:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/claim/mod.rs-764-            Ok(())
src/claim/mod.rs-765-        }
src/claim/mod.rs-766-
src/claim/mod.rs-767-        async fn block(&self, id: &BeadId) -> Result<()> {
src/claim/mod.rs-768-            self.blocked_beads.lock().unwrap().push(id.clone());
src/claim/mod.rs-769-            Ok(())
--
src/claim/mod.rs-1094-        let path1 = workspace_lock_path(&dir, Path::new("/workspace/a"));
src/claim/mod.rs-1095-        let path2 = workspace_lock_path(&dir, Path::new("/workspace/b"));
src/claim/mod.rs-1096-        assert_ne!(path1, path2);
src/claim/mod.rs-1097-    }
src/claim/mod.rs-1098-
src/claim/mod.rs-1099-    #[tokio::test]
src/claim/mod.rs:1100:    async fn flock_acquire_and_release() {
src/claim/mod.rs-1101-        let dir = std::env::temp_dir().join("needle-test-flock");
src/claim/mod.rs-1102-        std::fs::create_dir_all(&dir).unwrap();
src/claim/mod.rs-1103-        let lock_path = dir.join("test.lock");
src/claim/mod.rs-1104-
src/claim/mod.rs-1105-        // Acquire lock
src/claim/mod.rs-1106-        let file = acquire_flock(&lock_path).await.unwrap();
src/claim/mod.rs-1107-        assert!(lock_path.exists());
src/claim/mod.rs-1108-
src/claim/mod.rs:1109:        // Drop releases the lock
src/claim/mod.rs-1110-        drop(file);
src/claim/mod.rs-1111-
src/claim/mod.rs-1112-        // Can re-acquire immediately
src/claim/mod.rs-1113-        let file2 = acquire_flock(&lock_path).await.unwrap();
src/claim/mod.rs-1114-        drop(file2);
src/claim/mod.rs-1115-
--
src/claim/mod.rs-1388-            async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/claim/mod.rs-1389-                Ok(ClaimResult::NotClaimable {
src/claim/mod.rs-1390-                    reason: "no beads available".to_string(),
src/claim/mod.rs-1391-                })
src/claim/mod.rs-1392-            }
src/claim/mod.rs-1393-
src/claim/mod.rs:1394:            async fn release(&self, _id: &BeadId) -> Result<()> {
src/claim/mod.rs-1395-                Ok(())
src/claim/mod.rs-1396-            }
src/claim/mod.rs-1397-
src/claim/mod.rs-1398-            async fn block(&self, _id: &BeadId) -> Result<()> {
src/claim/mod.rs-1399-                Ok(())
src/claim/mod.rs-1400-            }
--
src/strand/resolve.rs-462-        async fn show(&self, _id: &BeadId) -> Result<crate::types::Bead> {
src/strand/resolve.rs-463-            anyhow::bail!("not found")
src/strand/resolve.rs-464-        }
src/strand/resolve.rs-465-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<ClaimResult> {
src/strand/resolve.rs-466-            anyhow::bail!("not implemented")
src/strand/resolve.rs-467-        }
src/strand/resolve.rs:468:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/resolve.rs-469-            Ok(())
src/strand/resolve.rs-470-        }
src/strand/resolve.rs-471-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/resolve.rs-472-            Ok(())
src/strand/resolve.rs-473-        }
src/strand/resolve.rs-474-        async fn flush(&self) -> Result<()> {
--
src/bead_store/backend.rs-16-const REQUIRED_OPERATIONS: &[&str] = &[
src/bead_store/backend.rs-17-    "ready",
src/bead_store/backend.rs-18-    "list_all",
src/bead_store/backend.rs-19-    "show",
src/bead_store/backend.rs-20-    "claim",
src/bead_store/backend.rs-21-    "claim_auto",
src/bead_store/backend.rs:22:    "release",
src/bead_store/backend.rs-23-    "block",
src/bead_store/backend.rs-24-    "clear_assignee",
src/bead_store/backend.rs-25-    "flush",
src/bead_store/backend.rs-26-    "reopen",
src/bead_store/backend.rs-27-    "labels",
src/bead_store/backend.rs-28-    "label_add",
--
src/bead_store/backend.rs-315-}
src/bead_store/backend.rs-316-
src/bead_store/backend.rs-317-fn allowed_placeholders(operation: &str) -> &'static [&'static str] {
src/bead_store/backend.rs-318-    match operation {
src/bead_store/backend.rs-319-        "ready" => &["limit", "assignee"],
src/bead_store/backend.rs-320-        "list_all" => &["limit"],
src/bead_store/backend.rs:321:        "show" | "release" | "block" | "clear_assignee" | "reopen" | "labels" => &["id"],
src/bead_store/backend.rs-322-        "claim" => &["id", "actor"],
src/bead_store/backend.rs-323-        "claim_auto" => &["actor", "model", "harness", "harness_version"],
src/bead_store/backend.rs-324-        "label_add" | "label_remove" => &["id", "label"],
src/bead_store/backend.rs-325-        "create" => &[
src/bead_store/backend.rs-326-            "title",
src/bead_store/backend.rs-327-            "body",
--
src/bead_store/backend.rs-428-        ("show", operation(&[], None, Some(ParseShape::JsonObject))),
src/bead_store/backend.rs-429-        ("claim", operation(&[], None, Some(ParseShape::JsonObject))),
src/bead_store/backend.rs-430-        (
src/bead_store/backend.rs-431-            "claim_auto",
src/bead_store/backend.rs-432-            operation(&[], None, Some(ParseShape::JsonObject)),
src/bead_store/backend.rs-433-        ),
src/bead_store/backend.rs:434:        ("release", operation(&[], None, None)),
src/bead_store/backend.rs-435-        ("block", operation(&[], None, None)),
src/bead_store/backend.rs-436-        ("clear_assignee", operation(&[], None, None)),
src/bead_store/backend.rs-437-        ("flush", operation(&[], None, None)),
src/bead_store/backend.rs-438-        ("reopen", operation(&[], None, None)),
src/bead_store/backend.rs-439-        ("labels", operation(&[], None, None)),
src/bead_store/backend.rs-440-        ("label_add", operation(&[], None, None)),
--
src/bead_store/backend.rs-535-            &["claim", "--assignee", "{actor}", "--json"],
src/bead_store/backend.rs-536-            Some("atomic_subcommand"),
src/bead_store/backend.rs-537-            Some(ParseShape::JsonObject),
src/bead_store/backend.rs-538-        ),
src/bead_store/backend.rs-539-    );
src/bead_store/backend.rs-540-    operations.insert(
src/bead_store/backend.rs:541:        "release".into(),
src/bead_store/backend.rs:542:        operation(&["release", "{id}"], None, None),
src/bead_store/backend.rs-543-    );
src/bead_store/backend.rs-544-    operations.insert(
src/bead_store/backend.rs-545-        "block".into(),
src/bead_store/backend.rs-546-        operation(&["update", "{id}", "--status", "blocked"], None, None),
src/bead_store/backend.rs-547-    );
src/bead_store/backend.rs-548-    operations.insert(
--
src/bead_store/backend.rs-835-                "--json",
src/bead_store/backend.rs-836-            ],
src/bead_store/backend.rs-837-            Some("atomic_subcommand"),
src/bead_store/backend.rs-838-            Some(ParseShape::JsonObject),
src/bead_store/backend.rs-839-        ),
src/bead_store/backend.rs-840-    );
src/bead_store/backend.rs:841:    operations.insert("release".into(), operation(&[], None, None));
src/bead_store/backend.rs-842-    operations.insert("clear_assignee".into(), operation(&[], None, None));
src/bead_store/backend.rs-843-    operations.insert(
src/bead_store/backend.rs-844-        "dep_add".into(),
src/bead_store/backend.rs-845-        operation(
src/bead_store/backend.rs-846-            &["dep", "add", "{blocker}", "--blocks", "{blocked}"],
src/bead_store/backend.rs-847-            None,
--
src/util.rs-560-        std::fs::create_dir_all(dir.join("state")).expect("failed to create test home state dir");
src/util.rs-561-        dir
src/util.rs-562-    }
src/util.rs-563-
src/util.rs-564-    /// Acquire the environment lock and capture `HOME`/`PATH` for restoration.
src/util.rs-565-    ///
src/util.rs:566:    /// Hold the returned tuple for the whole test body; dropping it releases
src/util.rs-567-    /// the lock and restores the environment. The lock is intentionally
src/util.rs-568-    /// poison-tolerant: a panicking test must not wedge every later test.
src/util.rs-569-    pub(crate) fn isolate_env() -> (MutexGuard<'static, ()>, EnvGuard) {
src/util.rs-570-        let lock = ENV_LOCK
src/util.rs-571-            .lock()
src/util.rs-572-            .unwrap_or_else(|poisoned| poisoned.into_inner());
--
src/util.rs-1272-
src/util.rs-1273-        // Test with various output formats
src/util.rs-1274-        let test_outputs = [
src/util.rs-1275-            "backend-name 1.0.0",
src/util.rs-1276-            "my-backend 2.3.4-beta",
src/util.rs-1277-            "tool 0.1.0+build.sha1",
src/util.rs:1278:            "cli 3.0.0 (release)",
src/util.rs-1279-        ];
src/util.rs-1280-
src/util.rs-1281-        for (i, output) in test_outputs.iter().enumerate() {
src/util.rs-1282-            let script = format!(
src/util.rs-1283-                r#"#!/bin/sh
src/util.rs-1284-echo "{}"
--
src/worker/mod.rs-887-                    format!("signal received ({name})")
src/worker/mod.rs-888-                } else {
src/worker/mod.rs-889-                    "signal received".to_string()
src/worker/mod.rs-890-                };
src/worker/mod.rs-891-
src/worker/mod.rs-892-                match self.state {
src/worker/mod.rs:893:                    // If we're in the middle of processing a bead, release it
src/worker/mod.rs-894-                    // before stopping. This ensures the bead is returned to
src/worker/mod.rs-895-                    // the open state and can be claimed by another worker.
src/worker/mod.rs-896-                    WorkerState::Building
src/worker/mod.rs-897-                    | WorkerState::Dispatching
src/worker/mod.rs-898-                    | WorkerState::Executing
src/worker/mod.rs-899-                    | WorkerState::Handling => {
src/worker/mod.rs-900-                        // Release any claimed bead before stopping.
src/worker/mod.rs:901:                        self.release_current_bead(&reason).await;
src/worker/mod.rs-902-                        return self.stop(&reason).await;
src/worker/mod.rs-903-                    }
src/worker/mod.rs-904-                    // For states where we don't hold a bead, stop immediately.
src/worker/mod.rs-905-                    WorkerState::Selecting
src/worker/mod.rs-906-                    | WorkerState::Claiming
src/worker/mod.rs-907-                    | WorkerState::Retrying
src/worker/mod.rs-908-                    | WorkerState::Logging => {
src/worker/mod.rs-909-                        // Release any claimed bead before stopping (should be none in these states).
src/worker/mod.rs:910:                        self.release_current_bead(&reason).await;
src/worker/mod.rs-911-                        return self.stop(&reason).await;
src/worker/mod.rs-912-                    }
src/worker/mod.rs-913-                    WorkerState::Stopped | WorkerState::Exhausted | WorkerState::Errored => {
src/worker/mod.rs-914-                        return self.stop(&reason).await;
src/worker/mod.rs-915-                    }
src/worker/mod.rs-916-                    WorkerState::Booting => {
--
src/worker/mod.rs-936-                        operation: "watchdog".to_string(),
src/worker/mod.rs-937-                        error: format!(
src/worker/mod.rs-938-                            "HANDLING state exceeded {}s timeout",
src/worker/mod.rs-939-                            HANDLING_WATCHDOG_TIMEOUT_SECS
src/worker/mod.rs-940-                        ),
src/worker/mod.rs-941-                    });
src/worker/mod.rs:942:                // Attempt best-effort release if we have a bead.
src/worker/mod.rs-943-                if let Some(ref bead) = self.current_bead {
src/worker/mod.rs-944-                    let bead_id = bead.id.clone();
src/worker/mod.rs:945:                    tracing::warn!(bead_id = %bead_id, "best-effort bead release due to watchdog timeout");
src/worker/mod.rs-946-                    let _ =
src/worker/mod.rs:947:                        tokio::time::timeout(Duration::from_secs(30), self.store.release(&bead_id))
src/worker/mod.rs-948-                            .await;
src/worker/mod.rs-949-                }
src/worker/mod.rs-950-                // Clear the watchdog trigger and force transition to LOGGING.
src/worker/mod.rs-951-                self.watchdog_triggered.store(false, Ordering::Release);
src/worker/mod.rs-952-                self.handling_state_entered_at = None;
src/worker/mod.rs-953-                // Force transition to LOGGING to recover.
--
src/worker/mod.rs-1412-
src/worker/mod.rs-1413-        match candidate.bead {
src/worker/mod.rs-1414-            Some((bead, strand_name)) => {
src/worker/mod.rs-1415-                tracing::debug!(bead_id = %bead.id, strand = %strand_name, "candidate found");
src/worker/mod.rs-1416-
src/worker/mod.rs-1417-                // If the bead is from a remote workspace (found by Explore),
src/worker/mod.rs:1418:                // swap the active store so claim/show/release operate on the
src/worker/mod.rs-1419-                // correct workspace. Only switch if the workspace has a real
src/worker/mod.rs-1420-                // .beads/ directory — avoids false triggers from mock/stub beads.
src/worker/mod.rs-1421-                let bead_ws = bead.workspace.clone();
src/worker/mod.rs-1422-                if !is_workspace_unset(&bead_ws)
src/worker/mod.rs-1423-                    && bead_ws != self.config.workspace.default
src/worker/mod.rs-1424-                    && bead_ws.join(".beads").is_dir()
--
src/worker/mod.rs-1994-                });
src/worker/mod.rs-1995-            }
src/worker/mod.rs-1996-        });
src/worker/mod.rs-1997-
src/worker/mod.rs-1998-        // Check if we should use SPLIT mode instead of normal PLUCK.
src/worker/mod.rs-1999-        // Auto-split triggers when a bead has >= split_after_failures consecutive failures.
src/worker/mod.rs:2000:        let (template_name, failure_count) = self.check_split_mode(&bead).await;
src/worker/mod.rs-2001-
src/worker/mod.rs-2002-        // If SPLIT mode would be used, check if the bead references NEEDLE-internal
src/worker/mod.rs-2003-        // configuration. These tasks have no legitimate resolution path from inside
src/worker/mod.rs-2004-        // a target repo and should not be split into child beads there.
src/worker/mod.rs-2005-        if template_name == "split" && detects_needle_internal_config(&bead) {
src/worker/mod.rs-2006-            heartbeat_handle.abort();
src/worker/mod.rs-2007-
src/worker/mod.rs-2008-            tracing::info!(
src/worker/mod.rs-2009-                bead_id = %bead_id,
src/worker/mod.rs-2010-                title = %bead.title,
src/worker/mod.rs:2011:                failure_count,
src/worker/mod.rs-2012-                "SPLIT mode skipped: bead references NEEDLE-internal configuration"
src/worker/mod.rs-2013-            );
src/worker/mod.rs-2014-
src/worker/mod.rs-2015-            // Set Error status on the bead.prompt_build span
src/worker/mod.rs-2016-            tracing::Span::current().record("otel.status_code", 2u64);
src/worker/mod.rs-2017-            tracing::Span::current().record(
--
src/worker/mod.rs-2025-                reason: "references NEEDLE-internal configuration".to_string(),
src/worker/mod.rs-2026-            });
src/worker/mod.rs-2027-
src/worker/mod.rs-2028-            // Release the bead with timeout protection.
src/worker/mod.rs-2029-            let _ = tokio::time::timeout(
src/worker/mod.rs-2030-                std::time::Duration::from_secs(30),
src/worker/mod.rs:2031:                self.store.release(&bead_id),
src/worker/mod.rs-2032-            )
src/worker/mod.rs-2033-            .await;
src/worker/mod.rs-2034-
src/worker/mod.rs:2035:            // Emit bead.released event.
src/worker/mod.rs-2036-            let _ = self.telemetry.emit(EventKind::BeadReleased {
src/worker/mod.rs-2037-                bead_id: bead_id.clone(),
src/worker/mod.rs-2038-                reason: "split_out_of_scope".to_string(),
src/worker/mod.rs-2039-            });
src/worker/mod.rs-2040-
src/worker/mod.rs-2041-            // Clear current bead and transition to RETRYING.
--
src/worker/mod.rs-2045-        }
src/worker/mod.rs-2046-
src/worker/mod.rs-2047-        let mut prompt = match tokio::time::timeout(
src/worker/mod.rs-2048-            timeout_dur,
src/worker/mod.rs-2049-            tokio::task::spawn_blocking(move || {
src/worker/mod.rs-2050-                if template_name == "split" {
src/worker/mod.rs:2051:                    prompt_builder.build_split(&bead, &build_ws, &worker_name, failure_count)
src/worker/mod.rs-2052-                } else {
src/worker/mod.rs-2053-                    prompt_builder.build_pluck(&bead, &build_ws, &worker_name)
src/worker/mod.rs-2054-                }
src/worker/mod.rs-2055-            }),
src/worker/mod.rs-2056-        )
src/worker/mod.rs-2057-        .await
--
src/worker/mod.rs-2060-            Ok(Err(e)) => {
src/worker/mod.rs-2061-                heartbeat_handle.abort();
src/worker/mod.rs-2062-                bail!("prompt building task failed: {}", e);
src/worker/mod.rs-2063-            }
src/worker/mod.rs-2064-            Err(_) => {
src/worker/mod.rs-2065-                heartbeat_handle.abort();
src/worker/mod.rs:2066:                // Timeout: release the bead and transition to RETRYING.
src/worker/mod.rs-2067-                tracing::error!(
src/worker/mod.rs-2068-                    bead_id = %bead_id,
src/worker/mod.rs-2069-                    timeout_secs = timeout_secs,
src/worker/mod.rs-2070-                    configured_timeout = self.config.worker.building_timeout,
src/worker/mod.rs-2071-                    "BUILDING state timed out"
src/worker/mod.rs-2072-                );
--
src/worker/mod.rs-2092-                    serde_json::json!({}),
src/worker/mod.rs-2093-                );
src/worker/mod.rs-2094-
src/worker/mod.rs-2095-                // Release the bead with timeout protection.
src/worker/mod.rs-2096-                let _ = tokio::time::timeout(
src/worker/mod.rs-2097-                    std::time::Duration::from_secs(30),
src/worker/mod.rs:2098:                    self.store.release(&bead_id),
src/worker/mod.rs-2099-                )
src/worker/mod.rs-2100-                .await;
src/worker/mod.rs-2101-
src/worker/mod.rs:2102:                // Emit bead.released event.
src/worker/mod.rs-2103-                let _ = self.telemetry.emit(EventKind::BeadReleased {
src/worker/mod.rs-2104-                    bead_id: bead_id.clone(),
src/worker/mod.rs-2105-                    reason: "build_timeout".to_string(),
src/worker/mod.rs-2106-                });
src/worker/mod.rs-2107-                crate::hoop_hooks::emit_needle_event(
src/worker/mod.rs-2108-                    &self.current_workspace,
src/worker/mod.rs-2109-                    &self.worker_name,
src/worker/mod.rs-2110-                    Some(bead_id.as_ref()),
src/worker/mod.rs-2111-                    self.current_strand.as_deref(),
src/worker/mod.rs:2112:                    "release",
src/worker/mod.rs-2113-                    serde_json::json!({}),
src/worker/mod.rs-2114-                );
src/worker/mod.rs-2115-
src/worker/mod.rs-2116-                // Clear current bead and transition to RETRYING.
src/worker/mod.rs-2117-                self.current_bead = None;
src/worker/mod.rs-2118-                self.set_state(WorkerState::Retrying)?;
--
src/worker/mod.rs-2313-                // before dispatch. This prevents double-dispatch where two workers
src/worker/mod.rs-2314-                // dispatch the same bead concurrently.
src/worker/mod.rs-2315-                //
src/worker/mod.rs-2316-                // This check queries the LIVE bead store (not a stale snapshot) to
src/worker/mod.rs-2317-                // ensure the bead is still in_progress and assigned to this worker.
src/worker/mod.rs-2318-                // If another worker has reassigned the bead or the bead has been
src/worker/mod.rs:2319:                // released, we abort the dispatch.
src/worker/mod.rs-2320-                let is_valid = self
src/worker/mod.rs-2321-                    .claimer
src/worker/mod.rs-2322-                    .verify_claim_at_dispatch(&bead.id, &qualified_actor)
src/worker/mod.rs-2323-                    .await
src/worker/mod.rs-2324-                    .with_context(|| {
src/worker/mod.rs-2325-                        format!(
--
src/worker/mod.rs-2335-                        bead_id = %bead.id,
src/worker/mod.rs-2336-                        worker = %self.worker_name,
src/worker/mod.rs-2337-                        "dispatch-time claim verification failed: bead not assigned to this worker, aborting dispatch and releasing back to ready state"
src/worker/mod.rs-2338-                    );
src/worker/mod.rs-2339-
src/worker/mod.rs-2340-                    // Release the bead back to open status
src/worker/mod.rs:2341:                    if let Err(e) = self.store.release(&bead.id).await {
src/worker/mod.rs-2342-                        tracing::error!(
src/worker/mod.rs-2343-                            bead_id = %bead.id,
src/worker/mod.rs-2344-                            error = %e,
src/worker/mod.rs:2345:                            "failed to release bead after dispatch-time verification failure"
src/worker/mod.rs-2346-                        );
src/worker/mod.rs-2347-                    }
src/worker/mod.rs-2348-
src/worker/mod.rs-2349-                    // Emit telemetry for the failed verification
src/worker/mod.rs-2350-                    let _ = self.telemetry.emit(EventKind::ClaimVerifyFailed {
src/worker/mod.rs-2351-                        bead_id: bead.id.clone(),
--
src/worker/mod.rs-2640-                        action = %result.bead_action,
src/worker/mod.rs-2641-                        "handler completed successfully, stopping heartbeat task"
src/worker/mod.rs-2642-                    );
src/worker/mod.rs-2643-                    Ok(result)
src/worker/mod.rs-2644-                }
src/worker/mod.rs-2645-                Ok(Err(e)) => {
src/worker/mod.rs:2646:                    // Handler returned an error - attempt best-effort release and recover.
src/worker/mod.rs-2647-                    tracing::error!(
src/worker/mod.rs-2648-                        bead_id = %bead.id,
src/worker/mod.rs-2649-                        error = %e,
src/worker/mod.rs:2650:                        "outcome handler failed, attempting best-effort release and transitioning to LOGGING"
src/worker/mod.rs-2651-                    );
src/worker/mod.rs-2652-                    // Set cancellation flag to stop heartbeat and abort any in-flight br calls.
src/worker/mod.rs-2653-                    cancelled.store(true, Ordering::Release);
src/worker/mod.rs-2654-                    // Abort the heartbeat task to prevent it from continuing in the background.
src/worker/mod.rs-2655-                    heartbeat_task.abort();
src/worker/mod.rs-2656-                    // Use emit_try_lock() to avoid blocking on telemetry mutex if writer is stuck.
src/worker/mod.rs-2657-                    let _ = telemetry_clone.emit_try_lock(EventKind::WorkerHandlingTimeout {
src/worker/mod.rs-2658-                        bead_id: bead_id_clone.clone(),
src/worker/mod.rs-2659-                        outcome: "unknown".to_string(),
src/worker/mod.rs-2660-                        operation: "handle".to_string(),
src/worker/mod.rs-2661-                        error: e.to_string(),
src/worker/mod.rs-2662-                    });
src/worker/mod.rs:2663:                    // Attempt best-effort release with timeout.
src/worker/mod.rs-2664-                    let _ = tokio::time::timeout(
src/worker/mod.rs-2665-                        std::time::Duration::from_secs(30),
src/worker/mod.rs:2666:                        store_clone.release(&bead_id_clone),
src/worker/mod.rs-2667-                    )
src/worker/mod.rs-2668-                    .await;
src/worker/mod.rs-2669-                    // Explicitly transition to LOGGING to recover.
src/worker/mod.rs-2670-                    self.set_state(WorkerState::Logging)?;
src/worker/mod.rs-2671-                    Err(anyhow::anyhow!("handler failed: {}", e))
src/worker/mod.rs-2672-                }
src/worker/mod.rs-2673-                Err(_) => {
src/worker/mod.rs:2674:                    // Timeout after 60 seconds - attempt best-effort release and transition to LOGGING.
src/worker/mod.rs-2675-                    tracing::error!(
src/worker/mod.rs-2676-                        bead_id = %bead.id,
src/worker/mod.rs:2677:                        "outcome handler timed out after 60s, attempting best-effort release and transitioning to LOGGING"
src/worker/mod.rs-2678-                    );
src/worker/mod.rs-2679-                    // Set cancellation flag to stop heartbeat and abort any in-flight br calls.
src/worker/mod.rs-2680-                    cancelled.store(true, Ordering::Release);
src/worker/mod.rs-2681-                    // Abort the heartbeat task to prevent it from continuing in the background.
src/worker/mod.rs-2682-                    heartbeat_task.abort();
src/worker/mod.rs-2683-                    // Use emit_try_lock() to avoid blocking on telemetry mutex if writer is stuck.
src/worker/mod.rs-2684-                    let _ = telemetry_clone.emit_try_lock(EventKind::WorkerHandlingTimeout {
src/worker/mod.rs-2685-                        bead_id: bead_id_clone.clone(),
src/worker/mod.rs-2686-                        outcome: "unknown".to_string(),
src/worker/mod.rs-2687-                        operation: "handle".to_string(),
src/worker/mod.rs-2688-                        error: "timeout after 60s".to_string(),
src/worker/mod.rs-2689-                    });
src/worker/mod.rs:2690:                    // Attempt best-effort release with timeout.
src/worker/mod.rs-2691-                    let _ = tokio::time::timeout(
src/worker/mod.rs-2692-                        std::time::Duration::from_secs(30),
src/worker/mod.rs:2693:                        store_clone.release(&bead_id_clone),
src/worker/mod.rs-2694-                    )
src/worker/mod.rs-2695-                    .await;
src/worker/mod.rs-2696-                    // Explicitly transition to LOGGING to recover.
src/worker/mod.rs-2697-                    self.set_state(WorkerState::Logging)?;
src/worker/mod.rs-2698-                    Err(anyhow::anyhow!("handler timed out after 60s"))
src/worker/mod.rs-2699-                }
--
src/worker/mod.rs-2739-                let _ = telemetry_clone.emit_try_lock(EventKind::WorkerHandlingTimeout {
src/worker/mod.rs-2740-                    bead_id: bead_id_clone.clone(),
src/worker/mod.rs-2741-                    outcome: "unknown".to_string(),
src/worker/mod.rs-2742-                    operation: "handling_state".to_string(),
src/worker/mod.rs-2743-                    error: "critical timeout after 90s".to_string(),
src/worker/mod.rs-2744-                });
src/worker/mod.rs:2745:                // Attempt best-effort release with timeout.
src/worker/mod.rs-2746-                let _ = tokio::time::timeout(
src/worker/mod.rs-2747-                    std::time::Duration::from_secs(30),
src/worker/mod.rs:2748:                    store_clone.release(&bead_id_clone),
src/worker/mod.rs-2749-                )
src/worker/mod.rs-2750-                .await;
src/worker/mod.rs-2751-                // Force transition to LOGGING to recover.
src/worker/mod.rs-2752-                self.set_state(WorkerState::Logging)?;
src/worker/mod.rs-2753-                return Ok(());
src/worker/mod.rs-2754-            }
--
src/worker/mod.rs-2792-                .exec_started_at
src/worker/mod.rs-2793-                .take()
src/worker/mod.rs-2794-                .map(|start| start.elapsed().as_millis() as u64);
src/worker/mod.rs-2795-            let outcome_str = handler_result.outcome.as_str();
src/worker/mod.rs-2796-            let event_name = match &handler_result.outcome {
src/worker/mod.rs-2797-                crate::types::Outcome::Success => "complete",
src/worker/mod.rs:2798:                crate::types::Outcome::Failure | crate::types::Outcome::AgentNotFound => "fail",
src/worker/mod.rs-2799-                crate::types::Outcome::Timeout => "timeout",
src/worker/mod.rs-2800-                crate::types::Outcome::Crash(_) => "crash",
src/worker/mod.rs:2801:                crate::types::Outcome::Interrupted => "release",
src/worker/mod.rs-2802-            };
src/worker/mod.rs-2803-            let mut extra = serde_json::json!({
src/worker/mod.rs-2804-                "outcome": outcome_str,
src/worker/mod.rs-2805-                "exit_code": output.exit_code,
src/worker/mod.rs-2806-            });
src/worker/mod.rs-2807-            if let Some(ms) = duration_ms {
--
src/worker/mod.rs-2824-        let _ = self.telemetry.emit_try_lock(EventKind::HeartbeatEmitted {
src/worker/mod.rs-2825-            bead_id: Some(bead.id.clone()),
src/worker/mod.rs-2826-            state: "HANDLING_POST_HANDLER".to_string(),
src/worker/mod.rs-2827-        });
src/worker/mod.rs-2828-
src/worker/mod.rs-2829-        // Evaluate for mitosis after failure — the bead has already been
src/worker/mod.rs:2830:        // released and failure count incremented by the outcome handler.
src/worker/mod.rs:2831:        if handler_result.outcome == Outcome::Failure {
src/worker/mod.rs-2832-            let workspace = if is_workspace_unset(&bead.workspace) {
src/worker/mod.rs-2833-                self.config.workspace.default.clone()
src/worker/mod.rs-2834-            } else {
src/worker/mod.rs-2835-                bead.workspace.clone()
src/worker/mod.rs-2836-            };
src/worker/mod.rs-2837-
--
src/worker/mod.rs-2889-                    tracing::Span::current().record("otel.status_code", 2u64);
src/worker/mod.rs-2890-                    tracing::Span::current()
src/worker/mod.rs-2891-                        .record("otel.status_description", format!("error: {e}"));
src/worker/mod.rs-2892-                    tracing::warn!(
src/worker/mod.rs-2893-                        bead_id = %bead.id,
src/worker/mod.rs-2894-                        error = %e,
src/worker/mod.rs:2895:                        "mitosis evaluation failed (bead already released)"
src/worker/mod.rs-2896-                    );
src/worker/mod.rs-2897-                }
src/worker/mod.rs-2898-                Err(_) => {
src/worker/mod.rs-2899-                    // Timeout after 120s - log warning and continue.
src/worker/mod.rs-2900-                    tracing::Span::current().record("needle.mitosis.result", "timeout");
src/worker/mod.rs-2901-                    tracing::Span::current().record("otel.status_code", 2u64);
--
src/worker/mod.rs-2910-            }
src/worker/mod.rs-2911-            .instrument(mitosis_span)
src/worker/mod.rs-2912-            .await;
src/worker/mod.rs-2913-        }
src/worker/mod.rs-2914-
src/worker/mod.rs-2915-        // Evaluate for timeout-triggered mitosis after timeout — the bead has already been
src/worker/mod.rs:2916:        // released and marked as deferred by the outcome handler.
src/worker/mod.rs-2917-        if handler_result.outcome == Outcome::Timeout {
src/worker/mod.rs-2918-            let workspace = if is_workspace_unset(&bead.workspace) {
src/worker/mod.rs-2919-                self.config.workspace.default.clone()
src/worker/mod.rs-2920-            } else {
src/worker/mod.rs-2921-                bead.workspace.clone()
src/worker/mod.rs-2922-            };
--
src/worker/mod.rs-2992-                    tracing::Span::current().record("otel.status_code", 2u64);
src/worker/mod.rs-2993-                    tracing::Span::current()
src/worker/mod.rs-2994-                        .record("otel.status_description", format!("error: {e}"));
src/worker/mod.rs-2995-                    tracing::warn!(
src/worker/mod.rs-2996-                        bead_id = %bead.id,
src/worker/mod.rs-2997-                        error = %e,
src/worker/mod.rs:2998:                        "timeout mitosis evaluation failed (bead already released)"
src/worker/mod.rs-2999-                    );
src/worker/mod.rs-3000-                }
src/worker/mod.rs-3001-                Err(_) => {
src/worker/mod.rs-3002-                    // Timeout after 120s - log warning and continue.
src/worker/mod.rs-3003-                    tracing::Span::current().record("needle.mitosis.result", "timeout");
src/worker/mod.rs-3004-                    tracing::Span::current().record("otel.status_code", 2u64);
--
src/worker/mod.rs-3799-                self.state = WorkerState::Selecting;
src/worker/mod.rs-3800-                Ok(WorkerState::Selecting)
src/worker/mod.rs-3801-            }
src/worker/mod.rs-3802-            IdleAction::Exit => {
src/worker/mod.rs-3803-                tracing::info!("all strands exhausted and idle_action=exit, stopping");
src/worker/mod.rs-3804-                // Release any claimed bead before exiting (should be none in exhausted state, but be safe).
src/worker/mod.rs:3805:                self.release_current_bead("exhausted").await;
src/worker/mod.rs-3806-                self.stop("exhausted").await
src/worker/mod.rs-3807-            }
src/worker/mod.rs-3808-        }
src/worker/mod.rs-3809-    }
src/worker/mod.rs-3810-
src/worker/mod.rs-3811-    /// Release the currently claimed bead (if any).
src/worker/mod.rs-3812-    ///
src/worker/mod.rs-3813-    /// This helper method is called during graceful shutdown to ensure that
src/worker/mod.rs-3814-    /// in-progress beads are returned to the open state and can be claimed by
src/worker/mod.rs-3815-    /// another worker.
src/worker/mod.rs:3816:    async fn release_current_bead(&mut self, shutdown_reason: &str) {
src/worker/mod.rs-3817-        if let Some(ref bead) = self.current_bead {
src/worker/mod.rs-3818-            let bead_id = bead.id.clone();
src/worker/mod.rs-3819-            tracing::info!(bead_id = %bead_id, reason = %shutdown_reason, "releasing bead on shutdown");
src/worker/mod.rs:3820:            let _ = self.store.release(&bead_id).await;
src/worker/mod.rs:3821:            // Emit bead.released event for observability
src/worker/mod.rs-3822-            let _ = self.telemetry.emit(EventKind::BeadReleased {
src/worker/mod.rs-3823-                bead_id: bead_id.clone(),
src/worker/mod.rs-3824-                reason: shutdown_reason.to_string(),
src/worker/mod.rs-3825-            });
src/worker/mod.rs-3826-        }
src/worker/mod.rs-3827-    }
--
src/worker/mod.rs-4235-            .adapter(adapter_name)
src/worker/mod.rs-4236-            .and_then(|a| a.provider.clone())
src/worker/mod.rs-4237-    }
src/worker/mod.rs-4238-
src/worker/mod.rs-4239-    /// Check if a bead should use SPLIT mode based on consecutive failures.
src/worker/mod.rs-4240-    ///
src/worker/mod.rs:4241:    /// Returns (template_name, failure_count). Uses SPLIT template when
src/worker/mod.rs-4242-    /// the bead's consecutive failure count meets or exceeds the configured
src/worker/mod.rs-4243-    /// threshold (split_after_failures), otherwise uses normal PLUCK template.
src/worker/mod.rs-4244-    async fn check_split_mode(&self, bead: &Bead) -> (&'static str, u32) {
src/worker/mod.rs-4245-        // Read labels with timeout protection (5s is generous for a local br call).
src/worker/mod.rs-4246-        let labels =
src/worker/mod.rs-4247-            match tokio::time::timeout(Duration::from_secs(5), self.store.labels(&bead.id)).await {
--
src/worker/mod.rs-4262-                    return ("pluck", 0);
src/worker/mod.rs-4263-                }
src/worker/mod.rs-4264-            };
src/worker/mod.rs-4265-
src/worker/mod.rs-4266-        // Extract current failure count from labels (format: "failure-count:N").
src/worker/mod.rs-4267-        // Multiple labels may exist from previous increments; we take the max.
src/worker/mod.rs:4268:        let failure_count = labels
src/worker/mod.rs-4269-            .iter()
src/worker/mod.rs-4270-            .filter_map(|l| l.strip_prefix("failure-count:"))
src/worker/mod.rs-4271-            .filter_map(|n| n.parse::<u32>().ok())
src/worker/mod.rs-4272-            .max()
src/worker/mod.rs-4273-            .unwrap_or(0);
src/worker/mod.rs-4274-
src/worker/mod.rs-4275-        // Check if we should use SPLIT mode.
src/worker/mod.rs-4276-        let threshold = self.config.strands.pluck.split_after_failures;
src/worker/mod.rs:4277:        if failure_count >= threshold {
src/worker/mod.rs-4278-            tracing::info!(
src/worker/mod.rs-4279-                bead_id = %bead.id,
src/worker/mod.rs:4280:                failure_count,
src/worker/mod.rs-4281-                threshold,
src/worker/mod.rs-4282-                "auto-split triggered: using SPLIT template"
src/worker/mod.rs-4283-            );
src/worker/mod.rs:4284:            ("split", failure_count)
src/worker/mod.rs-4285-        } else {
src/worker/mod.rs:4286:            ("pluck", failure_count)
src/worker/mod.rs-4287-        }
src/worker/mod.rs-4288-    }
src/worker/mod.rs-4289-
src/worker/mod.rs-4290-    /// Return the current worker state (for testing/inspection).
src/worker/mod.rs-4291-    pub fn state(&self) -> &WorkerState {
src/worker/mod.rs-4292-        &self.state
--
src/worker/mod.rs-4414-                bead.assignee = Some(actor.to_string());
src/worker/mod.rs-4415-                Ok(ClaimResult::Claimed(bead.clone()))
src/worker/mod.rs-4416-            } else {
src/worker/mod.rs-4417-                anyhow::bail!("bead not found: {id}")
src/worker/mod.rs-4418-            }
src/worker/mod.rs-4419-        }
src/worker/mod.rs:4420:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/worker/mod.rs-4421-            Ok(())
src/worker/mod.rs-4422-        }
src/worker/mod.rs-4423-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/worker/mod.rs-4424-            Ok(())
src/worker/mod.rs-4425-        }
src/worker/mod.rs-4426-        async fn flush(&self) -> Result<()> {
--
src/worker/mod.rs-4754-        }
src/worker/mod.rs-4755-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<ClaimResult> {
src/worker/mod.rs-4756-            Ok(ClaimResult::RaceLost {
src/worker/mod.rs-4757-                claimed_by: "other-worker".to_string(),
src/worker/mod.rs-4758-            })
src/worker/mod.rs-4759-        }
src/worker/mod.rs:4760:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/worker/mod.rs-4761-            Ok(())
src/worker/mod.rs-4762-        }
src/worker/mod.rs-4763-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/worker/mod.rs-4764-            Ok(())
src/worker/mod.rs-4765-        }
src/worker/mod.rs-4766-        async fn flush(&self) -> Result<()> {
--
src/worker/mod.rs-4842-        }
src/worker/mod.rs-4843-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<ClaimResult> {
src/worker/mod.rs-4844-            Ok(ClaimResult::NotClaimable {
src/worker/mod.rs-4845-                reason: "already closed".to_string(),
src/worker/mod.rs-4846-            })
src/worker/mod.rs-4847-        }
src/worker/mod.rs:4848:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/worker/mod.rs-4849-            Ok(())
src/worker/mod.rs-4850-        }
src/worker/mod.rs-4851-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/worker/mod.rs-4852-            Ok(())
src/worker/mod.rs-4853-        }
src/worker/mod.rs-4854-        async fn flush(&self) -> Result<()> {
--
src/worker/mod.rs-4936-            Ok(ClaimResult::Suspect {
src/worker/mod.rs-4937-                bead_id: id.clone(),
src/worker/mod.rs-4938-                consecutive_errors: 3,
src/worker/mod.rs-4939-                last_error: "database disk image is malformed".to_string(),
src/worker/mod.rs-4940-            })
src/worker/mod.rs-4941-        }
src/worker/mod.rs:4942:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/worker/mod.rs-4943-            Ok(())
src/worker/mod.rs-4944-        }
src/worker/mod.rs-4945-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/worker/mod.rs-4946-            Ok(())
src/worker/mod.rs-4947-        }
src/worker/mod.rs-4948-        async fn flush(&self) -> Result<()> {
--
src/prompt/mod.rs-64-- Close the bead:
src/prompt/mod.rs-65-
src/prompt/mod.rs-66-`{bead_cli} close {bead_id} --reason \"...\"`
src/prompt/mod.rs-67-
src/prompt/mod.rs-68-If you cannot complete the task:
src/prompt/mod.rs-69-- Do NOT close the bead
src/prompt/mod.rs:70:- The bead will be automatically released for retry
src/prompt/mod.rs-71-
src/prompt/mod.rs-72-Bead ID: {bead_id}";
src/prompt/mod.rs-73-
src/prompt/mod.rs-74-/// Built-in mitosis analysis template.
src/prompt/mod.rs-75-///
src/prompt/mod.rs-76-/// The agent receives the bead context and must output JSON describing whether
--
src/prompt/mod.rs-304-
src/prompt/mod.rs-305-### Your Task
src/prompt/mod.rs-306-
src/prompt/mod.rs-307-Analyze the operation outcome and choose ONE of these decisions:
src/prompt/mod.rs-308-
src/prompt/mod.rs-309-1. **Complete** - Task succeeded, bead should be closed
src/prompt/mod.rs:310:2. **Retry** - Temporary failure, release for immediate retry
src/prompt/mod.rs-311-3. **Blocked** - External dependency, cannot proceed without human input
src/prompt/mod.rs-312-4. **Split** - Task decomposes into multiple independent subtasks
src/prompt/mod.rs-313-
src/prompt/mod.rs-314-### Decision Criteria
src/prompt/mod.rs-315-
src/prompt/mod.rs-316-**Choose Complete if:**
--
src/prompt/mod.rs-418-///
src/prompt/mod.rs-419-/// This template instructs the agent to decompose a problematic bead into
src/prompt/mod.rs-420-/// smaller, focused child beads and convert the parent into an umbrella.
src/prompt/mod.rs-421-const DEFAULT_SPLIT_TEMPLATE: &str = "\
src/prompt/mod.rs-422-## Auto-Split: Decompose This Bead
src/prompt/mod.rs-423-
src/prompt/mod.rs:424:This bead has failed {failure_count} times in a row and needs to be broken down \
src/prompt/mod.rs-425-into smaller, manageable pieces.
src/prompt/mod.rs-426-
src/prompt/mod.rs-427-### Bead to Split
src/prompt/mod.rs-428-
src/prompt/mod.rs-429-**Title:** {bead_title}
src/prompt/mod.rs-430-**Bead ID:** {bead_id}
--
src/prompt/mod.rs-517-
src/prompt/mod.rs-518-/// Returns the extra (strand-specific) variables allowed for a given template name.
src/prompt/mod.rs-519-/// Returns `None` for unknown template names.
src/prompt/mod.rs-520-fn extra_vars_for_template(name: &str) -> Option<&'static [&'static str]> {
src/prompt/mod.rs-521-    match name {
src/prompt/mod.rs-522-        "pluck" => Some(&[]),
src/prompt/mod.rs:523:        "split" => Some(&["{failure_count}"]),
src/prompt/mod.rs-524-        "mitosis" => Some(&["{existing_children}"]),
src/prompt/mod.rs-525-        "mitosis-timeout" => Some(&[
src/prompt/mod.rs-526-            "{existing_children}",
src/prompt/mod.rs-527-            "{elapsed_duration}",
src/prompt/mod.rs-528-            "{timeout_duration}",
src/prompt/mod.rs-529-            "{elapsed_percent}",
--
src/prompt/mod.rs-892-    ) -> Result<BuiltPrompt> {
src/prompt/mod.rs-893-        self.build(bead, workspace, worker_id, "pluck")
src/prompt/mod.rs-894-    }
src/prompt/mod.rs-895-
src/prompt/mod.rs-896-    /// Build a split (auto-split) prompt.
src/prompt/mod.rs-897-    ///
src/prompt/mod.rs:898:    /// `failure_count` — the current consecutive failure count for the bead.
src/prompt/mod.rs-899-    pub fn build_split(
src/prompt/mod.rs-900-        &self,
src/prompt/mod.rs-901-        bead: &Bead,
src/prompt/mod.rs-902-        workspace: &Path,
src/prompt/mod.rs-903-        worker_id: &str,
src/prompt/mod.rs:904:        failure_count: u32,
src/prompt/mod.rs-905-    ) -> Result<BuiltPrompt> {
src/prompt/mod.rs-906-        self.build_with_vars(
src/prompt/mod.rs-907-            bead,
src/prompt/mod.rs-908-            workspace,
src/prompt/mod.rs-909-            worker_id,
src/prompt/mod.rs-910-            "split",
src/prompt/mod.rs:911:            &[("{failure_count}", &failure_count.to_string())],
src/prompt/mod.rs-912-        )
src/prompt/mod.rs-913-    }
src/prompt/mod.rs-914-
src/prompt/mod.rs-915-    /// Build a weave (gap analysis) prompt.
src/prompt/mod.rs-916-    ///
src/prompt/mod.rs-917-    /// `doc_files` — formatted listing of documentation files and contents.
--
src/bead_store/strategies.rs-51-    /// condition atomically, this strategy degrades to best-effort claiming
src/bead_store/strategies.rs-52-    /// and duplicate claims are possible.
src/bead_store/strategies.rs-53-    ///
src/bead_store/strategies.rs-54-    /// **Used by:** `br` (beads_rust), `bead` (bead-rs)
src/bead_store/strategies.rs-55-    CompareAndSet,
src/bead_store/strategies.rs-56-
src/bead_store/strategies.rs:57:    /// Single atomic batch operation that both claims and releases beads.
src/bead_store/strategies.rs-58-    ///
src/bead_store/strategies.rs-59-    /// **Race semantics:** No race — the backend guarantees atomicity. If the
src/bead_store/strategies.rs-60-    /// batch fails (e.g., a bead was already claimed by another actor), the
src/bead_store/strategies.rs-61-    /// entire batch is rejected and no partial state change occurs.
src/bead_store/strategies.rs-62-    ///
src/bead_store/strategies.rs-63-    /// **Used by:** `bf` (bead-forge)
--
src/upgrade/mod.rs-1-//! Self-update functionality for needle.
src/upgrade/mod.rs-2-//!
src/upgrade/mod.rs:3://! Provides the `needle upgrade` command that checks GitHub releases for
src/upgrade/mod.rs-4-//! newer versions and downloads/replaces the binary. Also provides hot-reload
src/upgrade/mod.rs-5-//! support: detecting a new `:stable` binary and re-exec'ing into it.
src/upgrade/mod.rs-6-
src/upgrade/mod.rs-7-use std::env;
src/upgrade/mod.rs-8-use std::fs;
src/upgrade/mod.rs-9-use std::io::{self, Cursor, Read};
--
src/upgrade/mod.rs-16-use crate::bead_store::spawn_with_etxtbsy_retry_child;
src/upgrade/mod.rs-17-use crate::telemetry::{EventKind, Telemetry};
src/upgrade/mod.rs-18-
src/upgrade/mod.rs-19-/// Current version of the needle binary.
src/upgrade/mod.rs-20-const CURRENT_VERSION: &str = env!("CARGO_PKG_VERSION");
src/upgrade/mod.rs-21-
src/upgrade/mod.rs:22:/// GitHub repository for releases.
src/upgrade/mod.rs-23-const GITHUB_REPO: &str = "jedarden/NEEDLE";
src/upgrade/mod.rs-24-
src/upgrade/mod.rs:25:/// GitHub API URL for latest release.
src/upgrade/mod.rs:26:fn latest_release_url() -> String {
src/upgrade/mod.rs:27:    format!("https://api.github.com/repos/{GITHUB_REPO}/releases/latest")
src/upgrade/mod.rs-28-}
src/upgrade/mod.rs-29-
src/upgrade/mod.rs-30-/// Download URL for a specific asset.
src/upgrade/mod.rs-31-fn asset_download_url(asset_name: &str) -> String {
src/upgrade/mod.rs-32-    format!(
src/upgrade/mod.rs:33:        "https://github.com/{}/releases/latest/download/{}",
src/upgrade/mod.rs-34-        GITHUB_REPO, asset_name
src/upgrade/mod.rs-35-    )
src/upgrade/mod.rs-36-}
src/upgrade/mod.rs-37-
src/upgrade/mod.rs:38:/// GitHub release asset information (fields required for JSON deserialization).
src/upgrade/mod.rs-39-#[derive(Debug, Deserialize)]
src/upgrade/mod.rs-40-#[allow(dead_code)]
src/upgrade/mod.rs-41-struct ReleaseAsset {
src/upgrade/mod.rs-42-    name: String,
src/upgrade/mod.rs-43-    browser_download_url: String,
src/upgrade/mod.rs-44-}
src/upgrade/mod.rs-45-
src/upgrade/mod.rs:46:/// GitHub release information (fields required for JSON deserialization).
src/upgrade/mod.rs-47-#[derive(Debug, Deserialize)]
src/upgrade/mod.rs-48-#[allow(dead_code)]
src/upgrade/mod.rs-49-struct ReleaseInfo {
src/upgrade/mod.rs-50-    tag_name: String,
src/upgrade/mod.rs-51-    name: Option<String>,
src/upgrade/mod.rs-52-    body: Option<String>,
--
src/upgrade/mod.rs-60-    pub current_version: String,
src/upgrade/mod.rs-61-    /// Latest available version.
src/upgrade/mod.rs-62-    pub latest_version: String,
src/upgrade/mod.rs-63-    /// Whether an update is available.
src/upgrade/mod.rs-64-    pub update_available: bool,
src/upgrade/mod.rs-65-    /// Release notes (if any).
src/upgrade/mod.rs:66:    pub release_notes: Option<String>,
src/upgrade/mod.rs-67-}
src/upgrade/mod.rs-68-
src/upgrade/mod.rs-69-/// Result of downloading to the testing channel.
src/upgrade/mod.rs-70-#[derive(Debug, Clone, PartialEq, Eq)]
src/upgrade/mod.rs-71-pub enum DownloadToTestingResult {
src/upgrade/mod.rs-72-    /// Download completed successfully.
--
src/upgrade/mod.rs-84-        testing_path: PathBuf,
src/upgrade/mod.rs-85-    },
src/upgrade/mod.rs-86-    /// No update available (already at latest version).
src/upgrade/mod.rs-87-    NoUpdateAvailable,
src/upgrade/mod.rs-88-}
src/upgrade/mod.rs-89-
src/upgrade/mod.rs:90:/// Check GitHub for the latest release.
src/upgrade/mod.rs-91-///
src/upgrade/mod.rs:92:/// Returns information about the latest release compared to the current version.
src/upgrade/mod.rs-93-pub fn check_for_update() -> Result<UpdateCheck> {
src/upgrade/mod.rs-94-    check_for_update_with_telemetry(None)
src/upgrade/mod.rs-95-}
src/upgrade/mod.rs-96-
src/upgrade/mod.rs:97:/// Check GitHub for the latest release with telemetry.
src/upgrade/mod.rs-98-///
src/upgrade/mod.rs:99:/// Returns information about the latest release compared to the current version.
src/upgrade/mod.rs-100-/// Emits telemetry events for the check attempt and outcome.
src/upgrade/mod.rs-101-pub fn check_for_update_with_telemetry(telemetry: Option<&Telemetry>) -> Result<UpdateCheck> {
src/upgrade/mod.rs-102-    let source = "manual";
src/upgrade/mod.rs-103-
src/upgrade/mod.rs-104-    // Emit upgrade check started event
src/upgrade/mod.rs-105-    if let Some(telemetry) = telemetry {
--
src/upgrade/mod.rs-116-            if let Some(telemetry) = telemetry {
src/upgrade/mod.rs-117-                let _ = telemetry.emit(EventKind::UpgradeCheckCompleted {
src/upgrade/mod.rs-118-                    source: source.to_string(),
src/upgrade/mod.rs-119-                    current_version: check.current_version.clone(),
src/upgrade/mod.rs-120-                    latest_version: check.latest_version.clone(),
src/upgrade/mod.rs-121-                    update_available: check.update_available,
src/upgrade/mod.rs:122:                    has_release_notes: check.release_notes.is_some(),
src/upgrade/mod.rs-123-                });
src/upgrade/mod.rs-124-            }
src/upgrade/mod.rs-125-        }
src/upgrade/mod.rs-126-        Err(error) => {
src/upgrade/mod.rs-127-            if let Some(telemetry) = telemetry {
src/upgrade/mod.rs-128-                let error_type = classify_upgrade_error(error);
--
src/upgrade/mod.rs-138-    result
src/upgrade/mod.rs-139-}
src/upgrade/mod.rs-140-
src/upgrade/mod.rs-141-/// Internal implementation of update check without telemetry.
src/upgrade/mod.rs-142-fn check_for_update_internal() -> Result<UpdateCheck> {
src/upgrade/mod.rs-143-    let response = ureq::agent()
src/upgrade/mod.rs:144:        .get(&latest_release_url())
src/upgrade/mod.rs-145-        .set("User-Agent", &format!("needle/{CURRENT_VERSION}"))
src/upgrade/mod.rs-146-        .call()
src/upgrade/mod.rs:147:        .context("failed to fetch latest release from GitHub")?;
src/upgrade/mod.rs-148-
src/upgrade/mod.rs-149-    if response.status() >= 400 {
src/upgrade/mod.rs-150-        bail!(
src/upgrade/mod.rs-151-            "GitHub API returned status {} when checking for updates",
src/upgrade/mod.rs-152-            response.status()
src/upgrade/mod.rs-153-        );
src/upgrade/mod.rs-154-    }
src/upgrade/mod.rs-155-
src/upgrade/mod.rs:156:    let release: ReleaseInfo = serde_json::from_reader(response.into_reader())
src/upgrade/mod.rs:157:        .context("failed to parse GitHub release response")?;
src/upgrade/mod.rs-158-
src/upgrade/mod.rs-159-    // Parse version from tag (strip 'v' prefix if present).
src/upgrade/mod.rs:160:    let latest_version = release
src/upgrade/mod.rs-161-        .tag_name
src/upgrade/mod.rs-162-        .strip_prefix('v')
src/upgrade/mod.rs:163:        .unwrap_or(&release.tag_name)
src/upgrade/mod.rs-164-        .to_string();
src/upgrade/mod.rs-165-
src/upgrade/mod.rs-166-    let update_available = is_newer_version(&latest_version, CURRENT_VERSION);
src/upgrade/mod.rs-167-
src/upgrade/mod.rs-168-    Ok(UpdateCheck {
src/upgrade/mod.rs-169-        current_version: CURRENT_VERSION.to_string(),
src/upgrade/mod.rs-170-        latest_version,
src/upgrade/mod.rs-171-        update_available,
src/upgrade/mod.rs:172:        release_notes: release.body,
src/upgrade/mod.rs-173-    })
src/upgrade/mod.rs-174-}
src/upgrade/mod.rs-175-
src/upgrade/mod.rs-176-/// Classify an upgrade error into a category for telemetry.
src/upgrade/mod.rs-177-fn classify_upgrade_error(error: &anyhow::Error) -> String {
src/upgrade/mod.rs-178-    let error_msg = error.to_string().to_lowercase();
--
src/upgrade/mod.rs-191-        "api".to_string()
src/upgrade/mod.rs-192-    } else {
src/upgrade/mod.rs-193-        "unknown".to_string()
src/upgrade/mod.rs-194-    }
src/upgrade/mod.rs-195-}
src/upgrade/mod.rs-196-
src/upgrade/mod.rs:197:/// Download a newer GitHub release to the testing channel.
src/upgrade/mod.rs-198-///
src/upgrade/mod.rs:199:/// This function downloads the latest release to `needle_home/bin/needle-testing`
src/upgrade/mod.rs-200-/// instead of overwriting the running binary. It skips download if an unpromoted
src/upgrade/mod.rs-201-/// testing binary already exists to avoid clobbering an in-flight self-modification
src/upgrade/mod.rs-202-/// candidate.
src/upgrade/mod.rs-203-///
src/upgrade/mod.rs-204-/// # Returns
src/upgrade/mod.rs-205-///
--
src/upgrade/mod.rs-208-/// - `Ok(NoUpdateAvailable)` if already at latest version
src/upgrade/mod.rs-209-/// - `Err` if download failed
src/upgrade/mod.rs-210-pub fn download_to_testing_channel() -> Result<DownloadToTestingResult> {
src/upgrade/mod.rs-211-    download_to_testing_channel_with_telemetry(None)
src/upgrade/mod.rs-212-}
src/upgrade/mod.rs-213-
src/upgrade/mod.rs:214:/// Download a newer GitHub release to the testing channel with telemetry.
src/upgrade/mod.rs-215-pub fn download_to_testing_channel_with_telemetry(
src/upgrade/mod.rs-216-    telemetry: Option<&Telemetry>,
src/upgrade/mod.rs-217-) -> Result<DownloadToTestingResult> {
src/upgrade/mod.rs-218-    let source = "download_to_testing";
src/upgrade/mod.rs-219-
src/upgrade/mod.rs-220-    // Emit upgrade check started event
--
src/upgrade/mod.rs-252-
src/upgrade/mod.rs-253-                let _ = telemetry.emit(EventKind::UpgradeCheckCompleted {
src/upgrade/mod.rs-254-                    source: source.to_string(),
src/upgrade/mod.rs-255-                    current_version,
src/upgrade/mod.rs-256-                    latest_version,
src/upgrade/mod.rs-257-                    update_available,
src/upgrade/mod.rs:258:                    has_release_notes: has_notes,
src/upgrade/mod.rs-259-                });
src/upgrade/mod.rs-260-            }
src/upgrade/mod.rs-261-        }
src/upgrade/mod.rs-262-        Err(error) => {
src/upgrade/mod.rs-263-            if let Some(telemetry) = telemetry {
src/upgrade/mod.rs-264-                let error_type = classify_upgrade_error(error);
--
src/upgrade/mod.rs-316-    let asset_name = get_asset_name()?;
src/upgrade/mod.rs-317-    let download_url = asset_download_url(&asset_name);
src/upgrade/mod.rs-318-
src/upgrade/mod.rs-319-    tracing::info!(
src/upgrade/mod.rs-320-        version = %check.latest_version,
src/upgrade/mod.rs-321-        url = %download_url,
src/upgrade/mod.rs:322:        "Downloading new release to testing channel"
src/upgrade/mod.rs-323-    );
src/upgrade/mod.rs-324-
src/upgrade/mod.rs-325-    let response = ureq::agent()
src/upgrade/mod.rs-326-        .get(&download_url)
src/upgrade/mod.rs-327-        .set("User-Agent", &format!("needle/{}", check.current_version))
src/upgrade/mod.rs-328-        .call()
--
src/upgrade/mod.rs-389-        }
src/upgrade/mod.rs-390-    }
src/upgrade/mod.rs-391-
src/upgrade/mod.rs-392-    latest_parts.len() > current_parts.len()
src/upgrade/mod.rs-393-}
src/upgrade/mod.rs-394-
src/upgrade/mod.rs:395:/// Get the platform-specific suffix for release asset names (e.g. "x86_64-unknown-linux-gnu").
src/upgrade/mod.rs-396-fn get_platform_suffix() -> Result<&'static str> {
src/upgrade/mod.rs-397-    let os = env::consts::OS;
src/upgrade/mod.rs-398-    let arch = env::consts::ARCH;
src/upgrade/mod.rs-399-
src/upgrade/mod.rs-400-    let suffix = match (os, arch) {
src/upgrade/mod.rs-401-        ("linux", "x86_64") => "x86_64-unknown-linux-gnu",
--
src/upgrade/mod.rs-433-            .set("User-Agent", &format!("needle/{CURRENT_VERSION}"))
src/upgrade/mod.rs-434-            .call()
src/upgrade/mod.rs-435-        {
src/upgrade/mod.rs-436-            Ok(r) if r.status() < 400 => r,
src/upgrade/mod.rs-437-            Ok(r) => {
src/upgrade/mod.rs-438-                println!(
src/upgrade/mod.rs:439:                    "  Warning: {transform} not in release (HTTP {}), skipping.",
src/upgrade/mod.rs-440-                    r.status()
src/upgrade/mod.rs-441-                );
src/upgrade/mod.rs-442-                continue;
src/upgrade/mod.rs-443-            }
src/upgrade/mod.rs-444-            Err(e) => {
src/upgrade/mod.rs-445-                println!("  Warning: failed to download {transform}: {e}. Skipping.");
--
src/upgrade/mod.rs-636-    let stable_path = bin_dir.join("needle-stable");
src/upgrade/mod.rs-637-    let install_dir = bin_dir.as_path();
src/upgrade/mod.rs-638-    if let Ok(suffix) = get_platform_suffix() {
src/upgrade/mod.rs-639-        install_transform_binaries(install_dir, suffix);
src/upgrade/mod.rs-640-    }
src/upgrade/mod.rs-641-
src/upgrade/mod.rs:642:    if let Some(notes) = &check.release_notes {
src/upgrade/mod.rs-643-        println!("\nRelease notes:\n{}", notes);
src/upgrade/mod.rs-644-    }
src/upgrade/mod.rs-645-
src/upgrade/mod.rs-646-    Ok(stable_path)
src/upgrade/mod.rs-647-}
src/upgrade/mod.rs-648-
--
src/upgrade/mod.rs-924-    #[test]
src/upgrade/mod.rs-925-    fn update_check_current_version() {
src/upgrade/mod.rs-926-        assert!(!CURRENT_VERSION.is_empty());
src/upgrade/mod.rs-927-    }
src/upgrade/mod.rs-928-
src/upgrade/mod.rs-929-    #[test]
src/upgrade/mod.rs:930:    fn latest_release_url_format() {
src/upgrade/mod.rs:931:        let url = latest_release_url();
src/upgrade/mod.rs-932-        assert!(url.contains("api.github.com"));
src/upgrade/mod.rs-933-        assert!(url.contains(GITHUB_REPO));
src/upgrade/mod.rs-934-    }
src/upgrade/mod.rs-935-
src/upgrade/mod.rs-936-    #[test]
src/upgrade/mod.rs-937-    fn asset_download_url_format() {
--
src/types/mod.rs-253-        // Each range is explicitly enumerated to ensure compile errors
src/types/mod.rs-254-        // if a new Outcome variant is added without updating this match.
src/types/mod.rs-255-        match exit_code {
src/types/mod.rs-256-            // Success
src/types/mod.rs-257-            0 => Outcome::Success,
src/types/mod.rs-258-            // Explicit failure code
src/types/mod.rs:259:            1 => Outcome::Failure,
src/types/mod.rs-260-            // Timeout (GNU timeout exit code)
src/types/mod.rs-261-            124 => Outcome::Timeout,
src/types/mod.rs-262-            // Agent not found (shell exit code for missing command)
src/types/mod.rs-263-            127 => Outcome::AgentNotFound,
src/types/mod.rs-264-            // Failure range: 2-123
src/types/mod.rs:265:            2..=123 => Outcome::Failure,
src/types/mod.rs-266-            // Failure: 125-128 (not >128 per spec, so 128 is not Crash)
src/types/mod.rs:267:            125..=128 => Outcome::Failure,
src/types/mod.rs-268-            // Signal exits: >128 (128 + signal number)
src/types/mod.rs-269-            // These are all crashes per the spec.
src/types/mod.rs-270-            129..=i32::MAX => Outcome::Crash(exit_code),
src/types/mod.rs-271-            // Negative exit codes (abnormal termination)
src/types/mod.rs-272-            i32::MIN..=-1 => Outcome::Crash(exit_code),
src/types/mod.rs-273-        }
src/types/mod.rs-274-    }
src/types/mod.rs-275-
src/types/mod.rs-276-    /// Return a string representation for telemetry/logging.
src/types/mod.rs-277-    pub fn as_str(&self) -> &'static str {
src/types/mod.rs-278-        match self {
src/types/mod.rs-279-            Outcome::Success => "success",
src/types/mod.rs:280:            Outcome::Failure => "failure",
src/types/mod.rs-281-            Outcome::Timeout => "timeout",
src/types/mod.rs-282-            Outcome::AgentNotFound => "agent_not_found",
src/types/mod.rs-283-            Outcome::Interrupted => "interrupted",
src/types/mod.rs-284-            Outcome::Crash(_) => "crash",
src/types/mod.rs-285-        }
src/types/mod.rs-286-    }
--
src/types/mod.rs-296-/// - **Timeout mode**: Activated when a bead times out. The bead is decomposed
src/types/mod.rs-297-///   into smaller child beads via `ChildBeadProposal`, each representing a
src/types/mod.rs-298-///   phase of the original work. This is a recovery strategy for work that
src/types/mod.rs-299-///   exceeded its time budget but may still be completable in smaller pieces.
src/types/mod.rs-300-/// - **OrdinaryFailure mode**: Activated for any non-timeout failure (compilation
src/types/mod.rs-301-///   error, test failure, agent crash, etc.). The bead is NOT decomposed.
src/types/mod.rs:302:///   It is either retried, released, or escalated based on the failure type
src/types/mod.rs-303-///   and consecutive failure count.
src/types/mod.rs-304-///
src/types/mod.rs-305-/// # When to use each mode
src/types/mod.rs-306-///
src/types/mod.rs-307-/// **Use `Timeout` mode when:**
src/types/mod.rs-308-/// - An agent process exits with code 124 (GNU timeout) or times out via another wrapper
--
src/types/mod.rs-336-        /// This can be used to name the first child bead (e.g., "Phase 1: Routing Rules").
src/types/mod.rs-337-        current_phase_hint: Option<String>,
src/types/mod.rs-338-    },
src/types/mod.rs-339-    /// Ordinary failure mode — no decomposition.
src/types/mod.rs-340-    ///
src/types/mod.rs-341-    /// Used for all non-timeout failures where splitting the bead into child
src/types/mod.rs:342:    /// beads would not help. The bead should be retried, released, or escalated
src/types/mod.rs-343-    /// via the normal failure-handling path.
src/types/mod.rs-344-    OrdinaryFailure,
src/types/mod.rs-345-}
src/types/mod.rs-346-
src/types/mod.rs-347-impl DecompositionMode {
src/types/mod.rs-348-    /// Returns true if this is timeout decomposition mode.
--
src/types/mod.rs-369-        matches!(self, DecompositionMode::Timeout { .. })
src/types/mod.rs-370-    }
src/types/mod.rs-371-
src/types/mod.rs-372-    /// Returns true if this is ordinary failure mode.
src/types/mod.rs-373-    ///
src/types/mod.rs-374-    /// This is the primary query method for routing to the normal failure
src/types/mod.rs:375:    /// handling path (retry, release, or escalate without decomposition).
src/types/mod.rs-376-    ///
src/types/mod.rs-377-    /// # Examples
src/types/mod.rs-378-    ///
src/types/mod.rs-379-    /// ```
src/types/mod.rs-380-    /// use needle::types::DecompositionMode;
src/types/mod.rs-381-    ///
--
src/types/mod.rs-1086-    }
src/types/mod.rs-1087-
src/types/mod.rs-1088-    #[test]
src/types/mod.rs-1089-    fn outcome_classify_key_codes() {
src/types/mod.rs-1090-        // Core mappings per spec
src/types/mod.rs-1091-        assert_eq!(Outcome::classify(0, false), Outcome::Success);
src/types/mod.rs:1092:        assert_eq!(Outcome::classify(1, false), Outcome::Failure);
src/types/mod.rs-1093-        assert_eq!(Outcome::classify(124, false), Outcome::Timeout);
src/types/mod.rs-1094-        assert_eq!(Outcome::classify(127, false), Outcome::AgentNotFound);
src/types/mod.rs-1095-    }
src/types/mod.rs-1096-
src/types/mod.rs-1097-    #[test]
src/types/mod.rs-1098-    fn outcome_classify_ranges() {
src/types/mod.rs-1099-        // 2..=123 map to Failure (except 124 which is Timeout)
src/types/mod.rs:1100:        assert_eq!(Outcome::classify(2, false), Outcome::Failure);
src/types/mod.rs:1101:        assert_eq!(Outcome::classify(50, false), Outcome::Failure);
src/types/mod.rs:1102:        assert_eq!(Outcome::classify(99, false), Outcome::Failure);
src/types/mod.rs:1103:        assert_eq!(Outcome::classify(100, false), Outcome::Failure); // NOT timeout per spec
src/types/mod.rs:1104:        assert_eq!(Outcome::classify(123, false), Outcome::Failure);
src/types/mod.rs-1105-        // 125-126 -> Failure (not AgentNotFound per spec)
src/types/mod.rs:1106:        assert_eq!(Outcome::classify(125, false), Outcome::Failure);
src/types/mod.rs:1107:        assert_eq!(Outcome::classify(126, false), Outcome::Failure);
src/types/mod.rs-1108-        // >128 -> Crash (signal exits)
src/types/mod.rs:1109:        assert_eq!(Outcome::classify(128, false), Outcome::Failure); // 128 is NOT >128 per spec
src/types/mod.rs-1110-        assert_eq!(Outcome::classify(129, false), Outcome::Crash(129));
src/types/mod.rs-1111-        assert_eq!(Outcome::classify(130, false), Outcome::Crash(130)); // SIGINT -> Crash
src/types/mod.rs-1112-        assert_eq!(Outcome::classify(137, false), Outcome::Crash(137)); // SIGKILL
src/types/mod.rs-1113-        assert_eq!(Outcome::classify(143, false), Outcome::Crash(143)); // SIGTERM -> Crash
src/types/mod.rs-1114-        assert_eq!(Outcome::classify(255, false), Outcome::Crash(255));
src/types/mod.rs-1115-        // negative -> Crash
--
src/types/mod.rs-1130-        assert_eq!(Outcome::classify(137, true), Outcome::Interrupted);
src/types/mod.rs-1131-    }
src/types/mod.rs-1132-
src/types/mod.rs-1133-    #[test]
src/types/mod.rs-1134-    fn outcome_as_str() {
src/types/mod.rs-1135-        assert_eq!(Outcome::Success.as_str(), "success");
src/types/mod.rs:1136:        assert_eq!(Outcome::Failure.as_str(), "failure");
src/types/mod.rs-1137-        assert_eq!(Outcome::Timeout.as_str(), "timeout");
src/types/mod.rs-1138-        assert_eq!(Outcome::AgentNotFound.as_str(), "agent_not_found");
src/types/mod.rs-1139-        assert_eq!(Outcome::Interrupted.as_str(), "interrupted");
src/types/mod.rs-1140-        assert_eq!(Outcome::Crash(137).as_str(), "crash");
src/types/mod.rs-1141-    }
src/types/mod.rs-1142-
--
src/types/mod.rs-1351-    }
src/types/mod.rs-1352-
src/types/mod.rs-1353-    #[test]
src/types/mod.rs-1354-    fn outcome_classify_boundary_values() {
src/types/mod.rs-1355-        // Exact boundaries between ranges
src/types/mod.rs-1356-        assert_eq!(Outcome::classify(0, false), Outcome::Success);
src/types/mod.rs:1357:        assert_eq!(Outcome::classify(1, false), Outcome::Failure);
src/types/mod.rs:1358:        assert_eq!(Outcome::classify(2, false), Outcome::Failure);
src/types/mod.rs:1359:        assert_eq!(Outcome::classify(123, false), Outcome::Failure);
src/types/mod.rs-1360-        assert_eq!(Outcome::classify(124, false), Outcome::Timeout);
src/types/mod.rs:1361:        assert_eq!(Outcome::classify(125, false), Outcome::Failure);
src/types/mod.rs:1362:        assert_eq!(Outcome::classify(126, false), Outcome::Failure);
src/types/mod.rs-1363-        assert_eq!(Outcome::classify(127, false), Outcome::AgentNotFound);
src/types/mod.rs:1364:        assert_eq!(Outcome::classify(128, false), Outcome::Failure);
src/types/mod.rs-1365-        assert_eq!(Outcome::classify(129, false), Outcome::Crash(129)); // First crash code
src/types/mod.rs-1366-
src/types/mod.rs-1367-        // i32 extremes
src/types/mod.rs-1368-        assert_eq!(Outcome::classify(i32::MAX, false), Outcome::Crash(i32::MAX));
src/types/mod.rs-1369-        assert_eq!(Outcome::classify(i32::MIN, false), Outcome::Crash(i32::MIN));
src/types/mod.rs-1370-    }
--
src/types/mod.rs-1382-        // SIGSEGV (128 + 11 = 139)
src/types/mod.rs-1383-        assert_eq!(Outcome::classify(139, false), Outcome::Crash(139));
src/types/mod.rs-1384-    }
src/types/mod.rs-1385-
src/types/mod.rs-1386-    #[test]
src/types/mod.rs-1387-    fn bead_action_display() {
src/types/mod.rs:1388:        assert_eq!(BeadAction::Released.to_string(), "released");
src/types/mod.rs-1389-        assert_eq!(BeadAction::Deferred.to_string(), "deferred");
src/types/mod.rs-1390-        assert_eq!(BeadAction::Alerted.to_string(), "alerted");
src/types/mod.rs-1391-        assert_eq!(BeadAction::None.to_string(), "none");
src/types/mod.rs-1392-    }
src/types/mod.rs-1393-
src/types/mod.rs-1394-    #[test]
--
src/types/mod.rs-3071-// BeadAction
src/types/mod.rs-3072-// ──────────────────────────────────────────────────────────────────────────────
src/types/mod.rs-3073-
src/types/mod.rs-3074-/// Action taken on a bead by the outcome handler.
src/types/mod.rs-3075-#[derive(Debug, Clone, PartialEq, Eq)]
src/types/mod.rs-3076-pub enum BeadAction {
src/types/mod.rs:3077:    /// Bead was released back to open status.
src/types/mod.rs-3078-    Released,
src/types/mod.rs-3079-    /// Bead was deferred (e.g., timeout with deferred label).
src/types/mod.rs-3080-    Deferred,
src/types/mod.rs-3081-    /// An alert bead was created.
src/types/mod.rs-3082-    Alerted,
src/types/mod.rs-3083-    /// Bead was quarantined (status=blocked, labeled `cycling`) after
--
src/types/mod.rs-3087-    None,
src/types/mod.rs-3088-}
src/types/mod.rs-3089-
src/types/mod.rs-3090-impl fmt::Display for BeadAction {
src/types/mod.rs-3091-    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
src/types/mod.rs-3092-        match self {
src/types/mod.rs:3093:            BeadAction::Released => write!(f, "released"),
src/types/mod.rs-3094-            BeadAction::Deferred => write!(f, "deferred"),
src/types/mod.rs-3095-            BeadAction::Alerted => write!(f, "alerted"),
src/types/mod.rs-3096-            BeadAction::Quarantined => write!(f, "quarantined"),
src/types/mod.rs-3097-            BeadAction::None => write!(f, "none"),
src/types/mod.rs-3098-        }
src/types/mod.rs-3099-    }
--
src/types/mod.rs-5329-/// Contextual information about what was being done when the timeout occurred.
src/types/mod.rs-5330-#[derive(Debug, Clone, Serialize, Deserialize)]
src/types/mod.rs-5331-#[serde(rename_all = "snake_case")]
src/types/mod.rs-5332-pub enum TimeoutContext {
src/types/mod.rs-5333-    /// Agent was actively working on code compilation.
src/types/mod.rs-5334-    Compilation {
src/types/mod.rs:5335:        /// Which compilation stage (e.g., "debug", "release", "test").
src/types/mod.rs-5336-        stage: String,
src/types/mod.rs-5337-    },
src/types/mod.rs-5338-
src/types/mod.rs-5339-    /// Agent was running tests.
src/types/mod.rs-5340-    TestExecution {
src/types/mod.rs-5341-        /// Number of tests being run.
--
src/registry/mod.rs-254-
src/registry/mod.rs-255-        Ok(live_workers)
src/registry/mod.rs-256-    }
src/registry/mod.rs-257-
src/registry/mod.rs-258-    /// Write a cleaned registry file (used by list() to persist dead PID filtering).
src/registry/mod.rs-259-    ///
src/registry/mod.rs:260:    /// No file locking: read() already released its shared lock before returning.
src/registry/mod.rs-261-    /// The atomic rename ensures concurrent writers don't corrupt the file —
src/registry/mod.rs-262-    /// one write wins cleanly.
src/registry/mod.rs-263-    fn write_cleaned(&self, reg: RegistryFile) -> Result<()> {
src/registry/mod.rs-264-        let tmp_path = self.path.with_extension("json.tmp");
src/registry/mod.rs-265-        let json = serde_json::to_string_pretty(&reg).context("failed to serialize registry")?;
src/registry/mod.rs-266-        std::fs::write(&tmp_path, &json)
--
src/registry/mod.rs-287-            .with_context(|| format!("failed to acquire shared lock: {}", self.path.display()))?;
src/registry/mod.rs-288-
src/registry/mod.rs-289-        let content = std::fs::read_to_string(&self.path)
src/registry/mod.rs-290-            .with_context(|| format!("failed to read registry: {}", self.path.display()))?;
src/registry/mod.rs-291-
src/registry/mod.rs-292-        FileExt::unlock(&file)
src/registry/mod.rs:293:            .with_context(|| format!("failed to release lock: {}", self.path.display()))?;
src/registry/mod.rs-294-
src/registry/mod.rs-295-        if content.trim().is_empty() {
src/registry/mod.rs-296-            return Ok(RegistryFile::default());
src/registry/mod.rs-297-        }
src/registry/mod.rs-298-
src/registry/mod.rs-299-        serde_json::from_str(&content)
--
src/registry/mod.rs-346-        std::fs::rename(&tmp_path, &self.path).with_context(|| {
src/registry/mod.rs-347-            format!("failed to rename temp registry to: {}", self.path.display())
src/registry/mod.rs-348-        })?;
src/registry/mod.rs-349-
src/registry/mod.rs-350-        // Release lock (use fs2 trait method explicitly for MSRV compat).
src/registry/mod.rs-351-        FileExt::unlock(&file)
src/registry/mod.rs:352:            .with_context(|| format!("failed to release lock: {}", self.path.display()))?;
src/registry/mod.rs-353-
src/registry/mod.rs-354-        Ok(())
src/registry/mod.rs-355-    }
src/registry/mod.rs-356-}
src/registry/mod.rs-357-
src/registry/mod.rs-358-// ──────────────────────────────────────────────────────────────────────────────
--
src/config/mod.rs-3713-    /// fails repeatedly without ever splitting.
src/config/mod.rs-3714-    #[serde(default)]
src/config/mod.rs-3715-    pub force_failure_threshold: u32,
src/config/mod.rs-3716-
src/config/mod.rs-3717-    /// Re-run mitosis every N consecutive failures after the first (0 = disabled).
src/config/mod.rs-3718-    ///
src/config/mod.rs:3719:    /// Fires at failure_count == 1, 1+N, 1+2N, ...
src/config/mod.rs-3720-    /// Only when force_failure_threshold == 0.
src/config/mod.rs-3721-    /// Beads already carrying a mitosis-depth label are skipped.
src/config/mod.rs-3722-    #[serde(default)]
src/config/mod.rs-3723-    pub repeat_interval: u32,
src/config/mod.rs-3724-
src/config/mod.rs-3725-    /// Maximum mitosis generation depth (0 = unlimited).
--
src/config/mod.rs-4840-/// ```
src/config/mod.rs-4841-///
src/config/mod.rs-4842-/// ## Fields
src/config/mod.rs-4843-///
src/config/mod.rs-4844-/// - **heartbeat_path**: Path to the supervisor's heartbeat file for liveness detection
src/config/mod.rs-4845-/// - **socket_path**: Optional Unix domain socket path for supervisor IPC
src/config/mod.rs:4846:/// - **auto_upgrade_check**: Enable automatic GitHub release checking
src/config/mod.rs-4847-/// - **update_check_interval_secs**: Seconds between update checks (when auto_upgrade_check is true)
src/config/mod.rs-4848-#[derive(Debug, Clone, Serialize, Deserialize)]
src/config/mod.rs-4849-pub struct SupervisorConfig {
src/config/mod.rs-4850-    /// Path to the supervisor's heartbeat file.
src/config/mod.rs-4851-    ///
src/config/mod.rs-4852-    /// The supervisor writes this file at a regular interval to signal liveness.
--
src/config/mod.rs-4881-    /// supervisor:
src/config/mod.rs-4882-    ///   socket_path: ~/.needle/supervisor.sock
src/config/mod.rs-4883-    /// ```
src/config/mod.rs-4884-    #[serde(default)]
src/config/mod.rs-4885-    pub socket_path: Option<PathBuf>,
src/config/mod.rs-4886-
src/config/mod.rs:4887:    /// Whether to automatically check for GitHub releases and download to :testing channel.
src/config/mod.rs-4888-    ///
src/config/mod.rs:4889:    /// When enabled, the supervisor periodically checks GitHub for newer releases
src/config/mod.rs-4890-    /// and downloads them to `~/.needle/bin/needle-testing`. Workers with auto-promote
src/config/mod.rs-4891-    /// enabled will then run canary validation and promote to :stable if tests pass.
src/config/mod.rs-4892-    ///
src/config/mod.rs-4893-    /// **Environment Variable**: `NEEDLE_SUPERVISOR_AUTO_UPGRADE_CHECK`
src/config/mod.rs-4894-    ///
src/config/mod.rs-4895-    /// Default: `false` (opt-in for production fleets).
--
src/config/mod.rs-4904-    #[serde(default = "SupervisorConfig::default_auto_upgrade_check")]
src/config/mod.rs-4905-    pub auto_upgrade_check: bool,
src/config/mod.rs-4906-
src/config/mod.rs-4907-    /// Interval (seconds) between automatic update checks.
src/config/mod.rs-4908-    ///
src/config/mod.rs-4909-    /// Only applies when `auto_upgrade_check` is true. The supervisor polls
src/config/mod.rs:4910:    /// GitHub releases at this interval to detect new versions.
src/config/mod.rs-4911-    ///
src/config/mod.rs-4912-    /// **Environment Variable**: `NEEDLE_SUPERVISOR_UPDATE_CHECK_INTERVAL_SECS`
src/config/mod.rs-4913-    ///
src/config/mod.rs-4914-    /// Default: `21600` (6 hours).
src/config/mod.rs-4915-    #[serde(default = "SupervisorConfig::default_update_check_interval_secs")]
src/config/mod.rs-4916-    pub update_check_interval_secs: u64,
--
src/bead_store/cli_store.rs-469-    }
src/bead_store/cli_store.rs-470-
src/bead_store/cli_store.rs-471-    async fn claim_auto(&self, actor: &str) -> Result<ClaimResult> {
src/bead_store/cli_store.rs-472-        self.claim_auto_inner(actor).await
src/bead_store/cli_store.rs-473-    }
src/bead_store/cli_store.rs-474-
src/bead_store/cli_store.rs:475:    async fn release(&self, id: &BeadId) -> Result<()> {
src/bead_store/cli_store.rs-476-        if self.backend.name == "bead-forge" {
src/bead_store/cli_store.rs-477-            return self.run_bf_update_batch(id, Some("open"), Some("")).await;
src/bead_store/cli_store.rs-478-        }
src/bead_store/cli_store.rs:479:        self.mutate("release", &[("id", id.to_string())]).await
src/bead_store/cli_store.rs-480-    }
src/bead_store/cli_store.rs-481-    async fn block(&self, id: &BeadId) -> Result<()> {
src/bead_store/cli_store.rs-482-        self.mutate("block", &[("id", id.to_string())]).await
src/bead_store/cli_store.rs-483-    }
src/bead_store/cli_store.rs-484-    async fn clear_assignee(&self, id: &BeadId) -> Result<()> {
src/bead_store/cli_store.rs-485-        if self.backend.name == "bead-forge" {
--
src/mitosis/mod.rs-247-                    current_depth, self.config.max_depth
src/mitosis/mod.rs-248-                ),
src/mitosis/mod.rs-249-            });
src/mitosis/mod.rs-250-        }
src/mitosis/mod.rs-251-
src/mitosis/mod.rs-252-        // Check failure count conditions.
src/mitosis/mod.rs:253:        let failure_count = self.get_failure_count(store, &bead.id).await?;
src/mitosis/mod.rs-254-
src/mitosis/mod.rs:255:        // force_failure_threshold: trigger only when failure_count reaches the threshold.
src/mitosis/mod.rs-256-        if self.config.force_failure_threshold > 0 {
src/mitosis/mod.rs:257:            if failure_count < self.config.force_failure_threshold {
src/mitosis/mod.rs-258-                tracing::debug!(
src/mitosis/mod.rs-259-                    bead_id = %bead.id,
src/mitosis/mod.rs:260:                    failure_count,
src/mitosis/mod.rs-261-                    threshold = self.config.force_failure_threshold,
src/mitosis/mod.rs-262-                    "mitosis skipped: below force_failure_threshold"
src/mitosis/mod.rs-263-                );
src/mitosis/mod.rs-264-                return Ok(MitosisResult::Skipped {
src/mitosis/mod.rs-265-                    reason: format!(
src/mitosis/mod.rs-266-                        "failure count {} below threshold {}",
src/mitosis/mod.rs:267:                        failure_count, self.config.force_failure_threshold
src/mitosis/mod.rs-268-                    ),
src/mitosis/mod.rs-269-                });
src/mitosis/mod.rs-270-            }
src/mitosis/mod.rs:271:        } else if self.config.first_failure_only && failure_count != 1 {
src/mitosis/mod.rs-272-            // Allow periodic retries after the first failure when configured,
src/mitosis/mod.rs-273-            // but never re-split a bead that is already a mitosis child.
src/mitosis/mod.rs-274-            let has_mitosis_depth_label =
src/mitosis/mod.rs-275-                bead.labels.iter().any(|l| l.starts_with("mitosis-depth:"));
src/mitosis/mod.rs:276:            let is_repeat_tick = failure_count > 1
src/mitosis/mod.rs-277-                && self.config.repeat_interval > 0
src/mitosis/mod.rs:278:                && (failure_count - 1) % self.config.repeat_interval == 0
src/mitosis/mod.rs-279-                && !has_mitosis_depth_label;
src/mitosis/mod.rs-280-
src/mitosis/mod.rs-281-            if !is_repeat_tick {
src/mitosis/mod.rs-282-                tracing::debug!(
src/mitosis/mod.rs-283-                    bead_id = %bead.id,
src/mitosis/mod.rs:284:                    failure_count,
src/mitosis/mod.rs-285-                    first_failure_only = self.config.first_failure_only,
src/mitosis/mod.rs-286-                    repeat_interval = self.config.repeat_interval,
src/mitosis/mod.rs-287-                    "mitosis skipped: not at trigger point"
src/mitosis/mod.rs-288-                );
src/mitosis/mod.rs-289-                return Ok(MitosisResult::Skipped {
src/mitosis/mod.rs-290-                    reason: format!(
src/mitosis/mod.rs-291-                        "not at trigger point (count={}, first_failure_only={}, repeat_interval={})",
src/mitosis/mod.rs:292:                        failure_count, self.config.first_failure_only, self.config.repeat_interval
src/mitosis/mod.rs-293-                    ),
src/mitosis/mod.rs-294-                });
src/mitosis/mod.rs-295-            }
src/mitosis/mod.rs-296-        }
src/mitosis/mod.rs-297-
src/mitosis/mod.rs-298-        // Resolve the agent adapter.
--
src/mitosis/mod.rs-325-        // Build mitosis prompt.
src/mitosis/mod.rs-326-        let prompt = prompt_builder
src/mitosis/mod.rs-327-            .build_mitosis(bead, workspace, "mitosis", &existing_children_text)
src/mitosis/mod.rs-328-            .context("failed to build mitosis prompt")?;
src/mitosis/mod.rs-329-
src/mitosis/mod.rs-330-        // Acquire workspace flock for atomicity.
src/mitosis/mod.rs:331:        // The lock is automatically released when _lock goes out of scope.
src/mitosis/mod.rs:332:        // Note: Lock file persists but the file lock is released on drop.
src/mitosis/mod.rs-333-        let lock_path = self.lock_dir.join(format!(
src/mitosis/mod.rs-334-            "needle-mitosis-{}.lock",
src/mitosis/mod.rs-335-            sanitize_path_component(&workspace.display().to_string())
src/mitosis/mod.rs-336-        ));
src/mitosis/mod.rs-337-        let _lock = acquire_flock(&lock_path)
src/mitosis/mod.rs-338-            .await
--
src/mitosis/mod.rs-426-    /// * `outcome` - Agent execution result (exit code, stdout, stderr)
src/mitosis/mod.rs-427-    /// * `duration` - Wall-clock duration of the agent execution
src/mitosis/mod.rs-428-    ///
src/mitosis/mod.rs-429-    /// # Returns
src/mitosis/mod.rs-430-    ///
src/mitosis/mod.rs-431-    /// * `Ok(MitosisResult)` - Outcome of the evaluation (Split, NotSplittable, Skipped, OutOfScope)
src/mitosis/mod.rs:432:    /// * `Err(e)` - Error during evaluation (non-fatal, bead is already released)
src/mitosis/mod.rs-433-    #[allow(clippy::too_many_arguments)]
src/mitosis/mod.rs-434-    pub async fn evaluate_timeout(
src/mitosis/mod.rs-435-        &self,
src/mitosis/mod.rs-436-        store: &dyn BeadStore,
src/mitosis/mod.rs-437-        bead: &Bead,
src/mitosis/mod.rs-438-        workspace: &Path,
--
src/mitosis/mod.rs-569-                &existing_children_text,
src/mitosis/mod.rs-570-                &timeout_context,
src/mitosis/mod.rs-571-            )
src/mitosis/mod.rs-572-            .context("failed to build timeout mitosis prompt")?;
src/mitosis/mod.rs-573-
src/mitosis/mod.rs-574-        // Step 9: Acquire workspace flock for atomicity.
src/mitosis/mod.rs:575:        // The lock is automatically released when _lock goes out of scope.
src/mitosis/mod.rs:576:        // Note: Lock file persists but the file lock is released on drop.
src/mitosis/mod.rs-577-        let lock_path = self.lock_dir.join(format!(
src/mitosis/mod.rs-578-            "needle-mitosis-timeout-{}.lock",
src/mitosis/mod.rs-579-            sanitize_path_component(&workspace.display().to_string())
src/mitosis/mod.rs-580-        ));
src/mitosis/mod.rs-581-        let _lock = acquire_flock(&lock_path)
src/mitosis/mod.rs-582-            .await
--
src/mitosis/mod.rs-805-        })
src/mitosis/mod.rs-806-    }
src/mitosis/mod.rs-807-
src/mitosis/mod.rs-808-    /// Read the failure count label from a bead.
src/mitosis/mod.rs-809-    ///
src/mitosis/mod.rs-810-    /// All br calls are wrapped in timeouts to prevent indefinite hang.
src/mitosis/mod.rs:811:    async fn get_failure_count(&self, store: &dyn BeadStore, bead_id: &BeadId) -> Result<u32> {
src/mitosis/mod.rs-812-        let labels =
src/mitosis/mod.rs-813-            match tokio::time::timeout(std::time::Duration::from_secs(30), store.labels(bead_id))
src/mitosis/mod.rs-814-                .await
src/mitosis/mod.rs-815-            {
src/mitosis/mod.rs-816-                Ok(Ok(l)) => l,
src/mitosis/mod.rs-817-                Ok(Err(e)) => {
--
src/mitosis/mod.rs-1396-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/mitosis/mod.rs-1397-            Ok(ClaimResult::NotClaimable {
src/mitosis/mod.rs-1398-                reason: "mock".to_string(),
src/mitosis/mod.rs-1399-            })
src/mitosis/mod.rs-1400-        }
src/mitosis/mod.rs-1401-
src/mitosis/mod.rs:1402:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/mitosis/mod.rs-1403-            Ok(())
src/mitosis/mod.rs-1404-        }
src/mitosis/mod.rs-1405-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/mitosis/mod.rs-1406-            Ok(())
src/mitosis/mod.rs-1407-        }
src/mitosis/mod.rs-1408-        async fn flush(&self) -> Result<()> {
--
src/mitosis/mod.rs-2260-            max_depth: 0,
src/mitosis/mod.rs-2261-            timeout_triggered: crate::config::TimeoutTriggeredPolicy::default(),
src/mitosis/mod.rs-2262-        };
src/mitosis/mod.rs-2263-        let telemetry = crate::telemetry::Telemetry::new("test".to_string());
src/mitosis/mod.rs-2264-        let evaluator = MitosisEvaluator::new(config, telemetry, PathBuf::from("/tmp"));
src/mitosis/mod.rs-2265-
src/mitosis/mod.rs:2266:        // Test failure_count = 1 (should fire)
src/mitosis/mod.rs-2267-        let store = MockStore::new().with_labels(vec!["failure-count:1".to_string()]);
src/mitosis/mod.rs-2268-        let bead = test_bead();
src/mitosis/mod.rs-2269-        let result = evaluator
src/mitosis/mod.rs-2270-            .evaluate(
src/mitosis/mod.rs-2271-                &store,
src/mitosis/mod.rs-2272-                &bead,
--
src/mitosis/mod.rs-2277-            )
src/mitosis/mod.rs-2278-            .await
src/mitosis/mod.rs-2279-            .unwrap();
src/mitosis/mod.rs-2280-        // Should not skip (adapter not found is a different skip, but we check the gate passes)
src/mitosis/mod.rs-2281-        assert!(
src/mitosis/mod.rs-2282-            !matches!(result, MitosisResult::Skipped { reason } if reason.contains("not at trigger point")),
src/mitosis/mod.rs:2283:            "failure_count=1 should trigger mitosis"
src/mitosis/mod.rs-2284-        );
src/mitosis/mod.rs-2285-
src/mitosis/mod.rs:2286:        // Test failure_count = 51 (should fire: (51-1) % 50 == 0)
src/mitosis/mod.rs-2287-        let store = MockStore::new().with_labels(vec!["failure-count:51".to_string()]);
src/mitosis/mod.rs-2288-        let bead = test_bead();
src/mitosis/mod.rs-2289-        let result = evaluator
src/mitosis/mod.rs-2290-            .evaluate(
src/mitosis/mod.rs-2291-                &store,
src/mitosis/mod.rs-2292-                &bead,
--
src/mitosis/mod.rs-2296-                "claude-sonnet",
src/mitosis/mod.rs-2297-            )
src/mitosis/mod.rs-2298-            .await
src/mitosis/mod.rs-2299-            .unwrap();
src/mitosis/mod.rs-2300-        assert!(
src/mitosis/mod.rs-2301-            !matches!(result, MitosisResult::Skipped { reason } if reason.contains("not at trigger point")),
src/mitosis/mod.rs:2302:            "failure_count=51 should trigger mitosis (1+50)"
src/mitosis/mod.rs-2303-        );
src/mitosis/mod.rs-2304-
src/mitosis/mod.rs:2305:        // Test failure_count = 101 (should fire: (101-1) % 50 == 0)
src/mitosis/mod.rs-2306-        let store = MockStore::new().with_labels(vec!["failure-count:101".to_string()]);
src/mitosis/mod.rs-2307-        let bead = test_bead();
src/mitosis/mod.rs-2308-        let result = evaluator
src/mitosis/mod.rs-2309-            .evaluate(
src/mitosis/mod.rs-2310-                &store,
src/mitosis/mod.rs-2311-                &bead,
--
src/mitosis/mod.rs-2315-                "claude-sonnet",
src/mitosis/mod.rs-2316-            )
src/mitosis/mod.rs-2317-            .await
src/mitosis/mod.rs-2318-            .unwrap();
src/mitosis/mod.rs-2319-        assert!(
src/mitosis/mod.rs-2320-            !matches!(result, MitosisResult::Skipped { reason } if reason.contains("not at trigger point")),
src/mitosis/mod.rs:2321:            "failure_count=101 should trigger mitosis (1+2*50)"
src/mitosis/mod.rs-2322-        );
src/mitosis/mod.rs-2323-
src/mitosis/mod.rs:2324:        // Test failure_count = 25 (should NOT fire: not 1, 1+50, 1+100, ...)
src/mitosis/mod.rs-2325-        let store = MockStore::new().with_labels(vec!["failure-count:25".to_string()]);
src/mitosis/mod.rs-2326-        let bead = test_bead();
src/mitosis/mod.rs-2327-        let result = evaluator
src/mitosis/mod.rs-2328-            .evaluate(
src/mitosis/mod.rs-2329-                &store,
src/mitosis/mod.rs-2330-                &bead,
--
src/mitosis/mod.rs-2334-                "claude-sonnet",
src/mitosis/mod.rs-2335-            )
src/mitosis/mod.rs-2336-            .await
src/mitosis/mod.rs-2337-            .unwrap();
src/mitosis/mod.rs-2338-        assert!(
src/mitosis/mod.rs-2339-            matches!(result, MitosisResult::Skipped { reason } if reason.contains("not at trigger point")),
src/mitosis/mod.rs:2340:            "failure_count=25 should NOT trigger mitosis"
src/mitosis/mod.rs-2341-        );
src/mitosis/mod.rs-2342-    }
src/mitosis/mod.rs-2343-
src/mitosis/mod.rs-2344-    #[tokio::test]
src/mitosis/mod.rs-2345-    async fn repeat_interval_skips_mitosis_depth_beads() {
src/mitosis/mod.rs-2346-        // Beads with mitosis-depth:1 label should be skipped even at repeat ticks
--
src/mitosis/mod.rs-2352-            max_depth: 0,
src/mitosis/mod.rs-2353-            timeout_triggered: crate::config::TimeoutTriggeredPolicy::default(),
src/mitosis/mod.rs-2354-        };
src/mitosis/mod.rs-2355-        let telemetry = crate::telemetry::Telemetry::new("test".to_string());
src/mitosis/mod.rs-2356-        let evaluator = MitosisEvaluator::new(config, telemetry, PathBuf::from("/tmp"));
src/mitosis/mod.rs-2357-
src/mitosis/mod.rs:2358:        // Bead with mitosis-depth:1 label at failure_count = 51 (repeat tick)
src/mitosis/mod.rs-2359-        let store = MockStore::new().with_labels(vec![
src/mitosis/mod.rs-2360-            "failure-count:51".to_string(),
src/mitosis/mod.rs-2361-            "mitosis-depth:1".to_string(),
src/mitosis/mod.rs-2362-        ]);
src/mitosis/mod.rs-2363-        let mut bead = test_bead();
src/mitosis/mod.rs-2364-        bead.labels = vec![
--
src/mitosis/mod.rs-2395-            max_depth: 0,
src/mitosis/mod.rs-2396-            timeout_triggered: crate::config::TimeoutTriggeredPolicy::default(),
src/mitosis/mod.rs-2397-        };
src/mitosis/mod.rs-2398-        let telemetry = crate::telemetry::Telemetry::new("test".to_string());
src/mitosis/mod.rs-2399-        let evaluator = MitosisEvaluator::new(config, telemetry, PathBuf::from("/tmp"));
src/mitosis/mod.rs-2400-
src/mitosis/mod.rs:2401:        // Test failure_count = 1 (should fire)
src/mitosis/mod.rs-2402-        let store = MockStore::new().with_labels(vec!["failure-count:1".to_string()]);
src/mitosis/mod.rs-2403-        let bead = test_bead();
src/mitosis/mod.rs-2404-        let result = evaluator
src/mitosis/mod.rs-2405-            .evaluate(
src/mitosis/mod.rs-2406-                &store,
src/mitosis/mod.rs-2407-                &bead,
--
src/mitosis/mod.rs-2411-                "claude-sonnet",
src/mitosis/mod.rs-2412-            )
src/mitosis/mod.rs-2413-            .await
src/mitosis/mod.rs-2414-            .unwrap();
src/mitosis/mod.rs-2415-        assert!(
src/mitosis/mod.rs-2416-            !matches!(result, MitosisResult::Skipped { reason } if reason.contains("not at trigger point")),
src/mitosis/mod.rs:2417:            "failure_count=1 should trigger mitosis"
src/mitosis/mod.rs-2418-        );
src/mitosis/mod.rs-2419-
src/mitosis/mod.rs:2420:        // Test failure_count = 2 (should NOT fire - first_failure_only mode)
src/mitosis/mod.rs-2421-        let store = MockStore::new().with_labels(vec!["failure-count:2".to_string()]);
src/mitosis/mod.rs-2422-        let bead = test_bead();
src/mitosis/mod.rs-2423-        let result = evaluator
src/mitosis/mod.rs-2424-            .evaluate(
src/mitosis/mod.rs-2425-                &store,
src/mitosis/mod.rs-2426-                &bead,
--
src/mitosis/mod.rs-2430-                "claude-sonnet",
src/mitosis/mod.rs-2431-            )
src/mitosis/mod.rs-2432-            .await
src/mitosis/mod.rs-2433-            .unwrap();
src/mitosis/mod.rs-2434-        assert!(
src/mitosis/mod.rs-2435-            matches!(result, MitosisResult::Skipped { reason } if reason.contains("not at trigger point")),
src/mitosis/mod.rs:2436:            "failure_count=2 should NOT trigger mitosis (first_failure_only mode)"
src/mitosis/mod.rs-2437-        );
src/mitosis/mod.rs-2438-    }
src/mitosis/mod.rs-2439-
src/mitosis/mod.rs-2440-    #[tokio::test]
src/mitosis/mod.rs-2441-    async fn repeat_interval_skips_max_depth_beads() {
src/mitosis/mod.rs-2442-        // Test that beads with mitosis-depth:1 label are skipped during repeat tick.
--
src/mitosis/mod.rs-2449-            max_depth: 0,
src/mitosis/mod.rs-2450-            timeout_triggered: crate::config::TimeoutTriggeredPolicy::default(),
src/mitosis/mod.rs-2451-        };
src/mitosis/mod.rs-2452-        let telemetry = crate::telemetry::Telemetry::new("test".to_string());
src/mitosis/mod.rs-2453-        let evaluator = MitosisEvaluator::new(config, telemetry, PathBuf::from("/tmp"));
src/mitosis/mod.rs-2454-
src/mitosis/mod.rs:2455:        // Bead with mitosis-depth:1 label at failure_count = 51 (repeat tick)
src/mitosis/mod.rs-2456-        // This should be skipped because it's a mitosis child bead (depth-limited).
src/mitosis/mod.rs-2457-        let store = MockStore::new().with_labels(vec![
src/mitosis/mod.rs-2458-            "failure-count:51".to_string(),
src/mitosis/mod.rs-2459-            "mitosis-depth:1".to_string(),
src/mitosis/mod.rs-2460-        ]);
src/mitosis/mod.rs-2461-        let mut bead = test_bead();
--
src/mitosis/mod.rs-2475-            )
src/mitosis/mod.rs-2476-            .await
src/mitosis/mod.rs-2477-            .unwrap();
src/mitosis/mod.rs-2478-
src/mitosis/mod.rs-2479-        assert!(
src/mitosis/mod.rs-2480-            matches!(result, MitosisResult::Skipped { .. }),
src/mitosis/mod.rs:2481:            "bead with mitosis-depth:1 should be skipped even at repeat tick (failure_count=51)"
src/mitosis/mod.rs-2482-        );
src/mitosis/mod.rs-2483-    }
src/mitosis/mod.rs-2484-
src/mitosis/mod.rs-2485-    // ── OutOfScope tests (ADR-002 Phase 6.1) ──
src/mitosis/mod.rs-2486-
src/mitosis/mod.rs-2487-    #[tokio::test]
--
src/mitosis/timeout_eligibility.rs-181-                reason: format!(
src/mitosis/timeout_eligibility.rs-182-                    "process killed by signal (exit code {}), not a timeout",
src/mitosis/timeout_eligibility.rs-183-                    code
src/mitosis/timeout_eligibility.rs-184-                ),
src/mitosis/timeout_eligibility.rs-185-            };
src/mitosis/timeout_eligibility.rs-186-        }
src/mitosis/timeout_eligibility.rs:187:        Outcome::Success | Outcome::Failure | Outcome::AgentNotFound => {
src/mitosis/timeout_eligibility.rs-188-            return TimeoutEligibility::NotEligible {
src/mitosis/timeout_eligibility.rs-189-                reason: format!(
src/mitosis/timeout_eligibility.rs-190-                    "exit code {} is not a timeout (expected 124)",
src/mitosis/timeout_eligibility.rs-191-                    outcome.exit_code
src/mitosis/timeout_eligibility.rs-192-                ),
src/mitosis/timeout_eligibility.rs-193-            };
--
src/bead_store/mod.rs-1176-    /// Returns a `ClaimResult` describing the outcome:
src/bead_store/mod.rs-1177-    /// - `Claimed(bead)` — success, returns the full bead.
src/bead_store/mod.rs-1178-    /// - `NotClaimable { reason }` — no beads available to claim.
src/bead_store/mod.rs-1179-    async fn claim_auto(&self, actor: &str) -> Result<ClaimResult>;
src/bead_store/mod.rs-1180-
src/bead_store/mod.rs-1181-    /// Release a claimed bead back to open (e.g., after agent failure).
src/bead_store/mod.rs:1182:    async fn release(&self, id: &BeadId) -> Result<()>;
src/bead_store/mod.rs-1183-
src/bead_store/mod.rs-1184-    /// Quarantine a bead by setting status=blocked (e.g., after it exceeds the
src/bead_store/mod.rs-1185-    /// consecutive-failure threshold in `OutcomeConfig::quarantine_after_failures`).
src/bead_store/mod.rs-1186-    ///
src/bead_store/mod.rs:1187:    /// Unlike `release`, this deliberately does NOT clear the assignee or return
src/bead_store/mod.rs-1188-    /// the bead to a claimable state — the whole point is to stop Pluck from
src/bead_store/mod.rs-1189-    /// re-selecting it until a human (or a future auto-split) intervenes.
src/bead_store/mod.rs-1190-    async fn block(&self, id: &BeadId) -> Result<()>;
src/bead_store/mod.rs-1191-
src/bead_store/mod.rs-1192-    /// Clear the assignee on a bead without changing its status.
src/bead_store/mod.rs-1193-    ///
src/bead_store/mod.rs-1194-    /// Used by mend to heal open beads with stale assignees (e.g., after reopen).
src/bead_store/mod.rs-1195-    async fn clear_assignee(&self, id: &BeadId) -> Result<()>;
src/bead_store/mod.rs-1196-
src/bead_store/mod.rs:1197:    /// Flush local bead changes to JSONL before release.
src/bead_store/mod.rs-1198-    ///
src/bead_store/mod.rs-1199-    /// Runs `br sync --flush-only` to ensure any local writes are persisted
src/bead_store/mod.rs:1200:    /// to JSONL before attempting to release a bead. This prevents SYNC_CONFLICT
src/bead_store/mod.rs-1201-    /// errors when the JSONL has newer remote changes.
src/bead_store/mod.rs-1202-    async fn flush(&self) -> Result<()>;
src/bead_store/mod.rs-1203-
src/bead_store/mod.rs-1204-    /// Reopen a closed (Done) bead back to open status.
src/bead_store/mod.rs-1205-    ///
src/bead_store/mod.rs-1206-    /// Used by validation gates when verification fails after an agent has
--
src/telemetry/otlp.rs-1462-            | "fleet.memory_low" // System memory below threshold
src/telemetry/otlp.rs-1463-            => (Severity::Warn, "WARN"),
src/telemetry/otlp.rs-1464-
src/telemetry/otlp.rs-1465-            // INFO events (default)
src/telemetry/otlp.rs-1466-            // Includes: worker.started, worker.stopped, health.check, effort.recorded,
src/telemetry/otlp.rs-1467-            //           bead.orphaned, bead.claim.race_lost, budget.warning, rate_limit.wait,
src/telemetry/otlp.rs:1468:            //           bead.released, transform.failed, worker.handling.timeout,
src/telemetry/otlp.rs-1469-            //           telemetry.otlp.shutdown_timeout, and everything else
src/telemetry/otlp.rs-1470-            _ => (Severity::Info, "INFO"),
src/telemetry/otlp.rs-1471-        }
src/telemetry/otlp.rs-1472-    }
src/telemetry/otlp.rs-1473-
src/telemetry/otlp.rs-1474-    /// Update the queue depth observable gauge.
--
src/strand/weave.rs-741-        async fn show(&self, _id: &BeadId) -> Result<Bead> {
src/strand/weave.rs-742-            anyhow::bail!("not implemented")
src/strand/weave.rs-743-        }
src/strand/weave.rs-744-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<ClaimResult> {
src/strand/weave.rs-745-            anyhow::bail!("not implemented")
src/strand/weave.rs-746-        }
src/strand/weave.rs:747:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/weave.rs-748-            Ok(())
src/strand/weave.rs-749-        }
src/strand/weave.rs-750-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/weave.rs-751-            Ok(())
src/strand/weave.rs-752-        }
src/strand/weave.rs-753-        async fn flush(&self) -> Result<()> {
--
src/validation/shipped_work.rs-80-    workspace: &Path,
src/validation/shipped_work.rs-81-    snapshot: Option<&predispatch::PreDispatch>,
src/validation/shipped_work.rs-82-    post_notes: &str,
src/validation/shipped_work.rs-83-) -> Result<GateResult> {
src/validation/shipped_work.rs-84-    // No baseline means the gate has no basis to judge this dispatch: it cannot
src/validation/shipped_work.rs-85-    // tell a commit made during the dispatch from one that predates it. Fail
src/validation/shipped_work.rs:86:    // OPEN. Releasing on "don't know" would re-release every closure whenever
src/validation/shipped_work.rs-87-    // snapshot recording failed — the same unbounded retry loop this gate
src/validation/shipped_work.rs-88-    // exists to stop, just triggered from the other side.
src/validation/shipped_work.rs-89-    let Some(snapshot) = snapshot else {
src/validation/shipped_work.rs-90-        tracing::debug!(
src/validation/shipped_work.rs-91-            workspace = %workspace.display(),
src/validation/shipped_work.rs-92-            "no pre-dispatch snapshot — shipped-work gate passing without a verdict"
--
src/validation/shipped_work.rs-237-    }
src/validation/shipped_work.rs-238-
src/validation/shipped_work.rs-239-    // ── fallback: bead notes ──
src/validation/shipped_work.rs-240-
src/validation/shipped_work.rs-241-    /// Without a snapshot the gate cannot distinguish a commit made during this
src/validation/shipped_work.rs-242-    /// dispatch from one that predates it, so it must not render a verdict.
src/validation/shipped_work.rs:243:    /// Failing closed here would release every closure whenever snapshot
src/validation/shipped_work.rs-244-    /// recording failed — the same unbounded retry loop, triggered from the
src/validation/shipped_work.rs-245-    /// other side.
src/validation/shipped_work.rs-246-    #[tokio::test]
src/validation/shipped_work.rs-247-    async fn no_snapshot_fails_open() {
src/validation/shipped_work.rs-248-        let dir = TempDir::new().unwrap();
src/validation/shipped_work.rs-249-        init_repo(dir.path()).await;
--
src/outcome/mod.rs-74-    }
src/outcome/mod.rs-75-
src/outcome/mod.rs-76-    /// Release a bead with sync conflict recovery and graceful failure handling.
src/outcome/mod.rs-77-    ///
src/outcome/mod.rs-78-    /// Flow:
src/outcome/mod.rs-79-    /// 1. Run `br sync --flush-only` to push local writes to JSONL first.
src/outcome/mod.rs:80:    /// 2. Attempt `br update` to release the bead (set status=open, assignee="").
src/outcome/mod.rs-81-    /// 3. If SYNC_CONFLICT recovery fails, emit `BeadReleaseFailed` and return Ok.
src/outcome/mod.rs-82-    ///
src/outcome/mod.rs:83:    /// This method never returns an error — release failures are non-fatal.
src/outcome/mod.rs:84:    /// The worker can continue processing other beads even if release fails.
src/outcome/mod.rs:85:    async fn release_bead(&self, store: &dyn BeadStore, bead: &Bead) -> Result<Vec<EventKind>> {
src/outcome/mod.rs-86-        let mut events = Vec::new();
src/outcome/mod.rs-87-
src/outcome/mod.rs:88:        // Step 1: Flush local writes to JSONL before attempting release.
src/outcome/mod.rs-89-        // Emit heartbeat before br call to track where hang occurs.
src/outcome/mod.rs-90-        let _ = self.telemetry.emit_try_lock(EventKind::HeartbeatEmitted {
src/outcome/mod.rs-91-            bead_id: Some(bead.id.clone()),
src/outcome/mod.rs-92-            state: "HANDLING_FLUSH".to_string(),
src/outcome/mod.rs-93-        });
src/outcome/mod.rs-94-
src/outcome/mod.rs-95-        match self.timeout_op(|| store.flush(), "flush").await {
src/outcome/mod.rs-96-            Ok(Some(())) => {
src/outcome/mod.rs-97-                tracing::debug!(
src/outcome/mod.rs-98-                    bead_id = %bead.id,
src/outcome/mod.rs:99:                    "flushed local changes to JSONL before release"
src/outcome/mod.rs-100-                );
src/outcome/mod.rs-101-                // Emit heartbeat after successful flush.
src/outcome/mod.rs-102-                let _ = self.telemetry.emit_try_lock(EventKind::HeartbeatEmitted {
src/outcome/mod.rs-103-                    bead_id: Some(bead.id.clone()),
src/outcome/mod.rs-104-                    state: "HANDLING_FLUSH_DONE".to_string(),
src/outcome/mod.rs-105-                });
src/outcome/mod.rs-106-            }
src/outcome/mod.rs-107-            Ok(None) => {
src/outcome/mod.rs-108-                tracing::warn!(
src/outcome/mod.rs-109-                    bead_id = %bead.id,
src/outcome/mod.rs:110:                    "flush timed out before release, attempting release anyway"
src/outcome/mod.rs-111-                );
src/outcome/mod.rs-112-                events.push(EventKind::WorkerHandlingTimeout {
src/outcome/mod.rs-113-                    bead_id: bead.id.clone(),
src/outcome/mod.rs:114:                    outcome: "release".to_string(),
src/outcome/mod.rs-115-                    operation: "flush".to_string(),
src/outcome/mod.rs-116-                    error: "timeout after 30s".to_string(),
src/outcome/mod.rs-117-                });
src/outcome/mod.rs-118-            }
src/outcome/mod.rs-119-            Err(e) => {
src/outcome/mod.rs-120-                tracing::warn!(
src/outcome/mod.rs-121-                    bead_id = %bead.id,
src/outcome/mod.rs-122-                    error = %e,
src/outcome/mod.rs:123:                    "flush failed before release, attempting release anyway"
src/outcome/mod.rs-124-                );
src/outcome/mod.rs-125-                events.push(EventKind::WorkerHandlingTimeout {
src/outcome/mod.rs-126-                    bead_id: bead.id.clone(),
src/outcome/mod.rs:127:                    outcome: "release".to_string(),
src/outcome/mod.rs-128-                    operation: "flush".to_string(),
src/outcome/mod.rs-129-                    error: e.to_string(),
src/outcome/mod.rs-130-                });
src/outcome/mod.rs-131-            }
src/outcome/mod.rs-132-        }
src/outcome/mod.rs-133-
src/outcome/mod.rs:134:        // Step 2: Attempt release with timeout protection.
src/outcome/mod.rs:135:        // Emit heartbeat before br release call to track where hang occurs.
src/outcome/mod.rs-136-        let _ = self.telemetry.emit_try_lock(EventKind::HeartbeatEmitted {
src/outcome/mod.rs-137-            bead_id: Some(bead.id.clone()),
src/outcome/mod.rs-138-            state: "HANDLING_RELEASE".to_string(),
src/outcome/mod.rs-139-        });
src/outcome/mod.rs-140-
src/outcome/mod.rs:141:        let release_result =
src/outcome/mod.rs:142:            tokio::time::timeout(std::time::Duration::from_secs(30), store.release(&bead.id)).await;
src/outcome/mod.rs-143-
src/outcome/mod.rs:144:        match release_result {
src/outcome/mod.rs-145-            Ok(Ok(())) => {
src/outcome/mod.rs-146-                tracing::debug!(
src/outcome/mod.rs-147-                    bead_id = %bead.id,
src/outcome/mod.rs:148:                    "bead released successfully"
src/outcome/mod.rs-149-                );
src/outcome/mod.rs:150:                // Emit heartbeat after successful release.
src/outcome/mod.rs-151-                let _ = self.telemetry.emit_try_lock(EventKind::HeartbeatEmitted {
src/outcome/mod.rs-152-                    bead_id: Some(bead.id.clone()),
src/outcome/mod.rs-153-                    state: "HANDLING_RELEASE_DONE".to_string(),
src/outcome/mod.rs-154-                });
src/outcome/mod.rs-155-                events.push(EventKind::BeadReleased {
src/outcome/mod.rs-156-                    bead_id: bead.id.clone(),
src/outcome/mod.rs:157:                    reason: "release_success".to_string(),
src/outcome/mod.rs-158-                });
src/outcome/mod.rs-159-            }
src/outcome/mod.rs-160-            Ok(Err(e)) => {
src/outcome/mod.rs-161-                // Release failed (br error, not timeout).
src/outcome/mod.rs-162-                // Check if this is a SyncRecoveryError (sync recovery was attempted but failed).
src/outcome/mod.rs-163-                if e.downcast_ref::<SyncRecoveryError>().is_some() {
src/outcome/mod.rs-164-                    tracing::error!(
src/outcome/mod.rs-165-                        bead_id = %bead.id,
src/outcome/mod.rs-166-                        error = %e,
src/outcome/mod.rs:167:                        "br release failed after SYNC_CONFLICT recovery — \
src/outcome/mod.rs-168-                         worker will continue (bead may remain assigned)"
src/outcome/mod.rs-169-                    );
src/outcome/mod.rs-170-                } else {
src/outcome/mod.rs-171-                    tracing::error!(
src/outcome/mod.rs-172-                        bead_id = %bead.id,
src/outcome/mod.rs-173-                        error = %e,
src/outcome/mod.rs:174:                        "br release failed — worker will continue (bead may remain assigned)"
src/outcome/mod.rs-175-                    );
src/outcome/mod.rs-176-                }
src/outcome/mod.rs-177-                events.push(EventKind::BeadReleaseFailed {
src/outcome/mod.rs-178-                    bead_id: bead.id.clone(),
src/outcome/mod.rs-179-                    reason: e.to_string(),
src/outcome/mod.rs-180-                });
src/outcome/mod.rs-181-            }
src/outcome/mod.rs-182-            Err(_) => {
src/outcome/mod.rs-183-                // Timeout after 30s.
src/outcome/mod.rs-184-                tracing::error!(
src/outcome/mod.rs-185-                    bead_id = %bead.id,
src/outcome/mod.rs:186:                    "br release timed out after 30s — worker will continue (bead may remain assigned)"
src/outcome/mod.rs-187-                );
src/outcome/mod.rs-188-                events.push(EventKind::BeadReleaseFailed {
src/outcome/mod.rs-189-                    bead_id: bead.id.clone(),
src/outcome/mod.rs-190-                    reason: "timeout after 30s".to_string(),
src/outcome/mod.rs-191-                });
src/outcome/mod.rs-192-            }
--
src/outcome/mod.rs-294-            outcome: outcome.as_str().to_string(),
src/outcome/mod.rs-295-            exit_code: output.exit_code,
src/outcome/mod.rs-296-        });
src/outcome/mod.rs-297-
src/outcome/mod.rs-298-        let (bead_action, telemetry_events) = match outcome.clone() {
src/outcome/mod.rs-299-            Outcome::Success => self.handle_success(store, bead, gate_report).await?,
src/outcome/mod.rs:300:            Outcome::Failure => {
src/outcome/mod.rs-301-                // If we have a gate report with failures, handle as gate failure.
src/outcome/mod.rs-302-                if let Some(report) = gate_report {
src/outcome/mod.rs-303-                    if !report.all_passed {
src/outcome/mod.rs-304-                        self.handle_gate_failure(store, bead, &report).await?
src/outcome/mod.rs-305-                    } else {
src/outcome/mod.rs-306-                        self.handle_failure(store, bead).await?
--
src/outcome/mod.rs-414-                        .as_str()
src/outcome/mod.rs-415-                        .to_string(),
src/outcome/mod.rs-416-                    operation: "handle".to_string(),
src/outcome/mod.rs-417-                    error: format!("timeout after {}s", timeout_secs),
src/outcome/mod.rs-418-                });
src/outcome/mod.rs-419-                // Return a default HandlerResult to allow the worker to recover.
src/outcome/mod.rs:420:                // The worker's 60-second timeout wrapper will handle best-effort release.
src/outcome/mod.rs-421-                // Verification never ran, so conservatively treat as unverified.
src/outcome/mod.rs-422-                Ok(HandlerResult {
src/outcome/mod.rs-423-                    outcome: classify(output.exit_code, was_interrupted, false),
src/outcome/mod.rs-424-                    bead_action: BeadAction::Released,
src/outcome/mod.rs-425-                    telemetry_events: vec![EventKind::BeadReleased {
src/outcome/mod.rs-426-                        bead_id,
--
src/outcome/mod.rs-465-                "all validation gates passed"
src/outcome/mod.rs-466-            );
src/outcome/mod.rs-467-        }
src/outcome/mod.rs-468-
src/outcome/mod.rs-469-        // Reset failure count on successful gate completion.
src/outcome/mod.rs-470-        // This ensures the bead starts fresh on the next cycle if re-claimed.
src/outcome/mod.rs:471:        let _ = self.reset_failure_count(store, bead).await;
src/outcome/mod.rs-472-
src/outcome/mod.rs-473-        // Normal success flow: check if agent closed the bead.
src/outcome/mod.rs-474-        let mut events = Vec::new();
src/outcome/mod.rs-475-
src/outcome/mod.rs-476-        // Use timeout for show() to prevent indefinite hang in HANDLING state.
src/outcome/mod.rs-477-        match self.timeout_op(|| store.show(&bead.id), "show").await {
--
src/outcome/mod.rs-527-                    "agent exited successfully but bead is still open (orphaned)"
src/outcome/mod.rs-528-                );
src/outcome/mod.rs-529-                events.push(EventKind::BeadOrphaned {
src/outcome/mod.rs-530-                    bead_id: bead.id.clone(),
src/outcome/mod.rs-531-                });
src/outcome/mod.rs-532-
src/outcome/mod.rs:533:                // Use verify_shipped_work to decide close-vs-release.
src/outcome/mod.rs:534:                // If the agent shipped work, mark as completed. Otherwise, release
src/outcome/mod.rs-535-                // and increment failure count so repeat offenders quarantine.
src/outcome/mod.rs-536-                if self.config.worker.enforce_shipped_work {
src/outcome/mod.rs-537-                    match verify_shipped_work(&current, &bead.workspace, store).await {
src/outcome/mod.rs-538-                        Ok(crate::validation::GateResult::Pass) => {
src/outcome/mod.rs-539-                            tracing::info!(
src/outcome/mod.rs-540-                                bead_id = %bead.id,
src/outcome/mod.rs-541-                                "shipped work detected — marking orphaned bead as completed"
src/outcome/mod.rs-542-                            );
src/outcome/mod.rs-543-                            // Clear the predispatch snapshot since work is complete.
src/outcome/mod.rs-544-                            crate::validation::predispatch::clear(&bead.workspace, &bead.id).await;
src/outcome/mod.rs-545-                            // Mark as completed - the agent shipped work but forgot to close.
src/outcome/mod.rs-546-                            // We can't close via bead store (no close method), so emit completion
src/outcome/mod.rs:547:                            // event and release. The bead remains open but work is done.
src/outcome/mod.rs-548-                            events.push(EventKind::BeadCompleted {
src/outcome/mod.rs-549-                                bead_id: bead.id.clone(),
src/outcome/mod.rs-550-                                duration_ms: 0,
src/outcome/mod.rs-551-                            });
src/outcome/mod.rs:552:                            let mut release_events = self.release_bead(store, bead).await?;
src/outcome/mod.rs:553:                            events.append(&mut release_events);
src/outcome/mod.rs-554-                            return Ok((BeadAction::Released, events));
src/outcome/mod.rs-555-                        }
src/outcome/mod.rs-556-                        Ok(crate::validation::GateResult::Fail(reason)) => {
src/outcome/mod.rs-557-                            tracing::warn!(
src/outcome/mod.rs-558-                                bead_id = %bead.id,
src/outcome/mod.rs-559-                                reason = %reason,
src/outcome/mod.rs-560-                                "no shipped work detected — releasing orphaned bead with failure increment"
src/outcome/mod.rs-561-                            );
src/outcome/mod.rs-562-                            // Release and increment failure count to apply quarantine.
src/outcome/mod.rs:563:                            let mut release_events = self.release_bead(store, bead).await?;
src/outcome/mod.rs:564:                            events.append(&mut release_events);
src/outcome/mod.rs:565:                            let release_succeeded = events
src/outcome/mod.rs-566-                                .iter()
src/outcome/mod.rs-567-                                .any(|e| matches!(e, EventKind::BeadReleased { .. }));
src/outcome/mod.rs:568:                            if release_succeeded {
src/outcome/mod.rs:569:                                let _ = self.increment_failure_count(store, bead).await;
src/outcome/mod.rs-570-                            }
src/outcome/mod.rs-571-                            return Ok((BeadAction::Released, events));
src/outcome/mod.rs-572-                        }
src/outcome/mod.rs-573-                        Err(e) => {
src/outcome/mod.rs-574-                            tracing::warn!(
src/outcome/mod.rs-575-                                bead_id = %bead.id,
src/outcome/mod.rs-576-                                error = %e,
src/outcome/mod.rs-577-                                "shipped-work check errored — releasing orphaned bead with failure increment"
src/outcome/mod.rs-578-                            );
src/outcome/mod.rs:579:                            // On error, release and increment failure count.
src/outcome/mod.rs:580:                            let mut release_events = self.release_bead(store, bead).await?;
src/outcome/mod.rs:581:                            events.append(&mut release_events);
src/outcome/mod.rs:582:                            let release_succeeded = events
src/outcome/mod.rs-583-                                .iter()
src/outcome/mod.rs-584-                                .any(|e| matches!(e, EventKind::BeadReleased { .. }));
src/outcome/mod.rs:585:                            if release_succeeded {
src/outcome/mod.rs:586:                                let _ = self.increment_failure_count(store, bead).await;
src/outcome/mod.rs-587-                            }
src/outcome/mod.rs-588-                            return Ok((BeadAction::Released, events));
src/outcome/mod.rs-589-                        }
src/outcome/mod.rs-590-                    }
src/outcome/mod.rs-591-                } else {
src/outcome/mod.rs-592-                    tracing::warn!(
src/outcome/mod.rs-593-                        bead_id = %bead.id,
src/outcome/mod.rs-594-                        "enforce_shipped_work disabled — releasing orphaned bead"
src/outcome/mod.rs-595-                    );
src/outcome/mod.rs:596:                    // If enforce_shipped_work is disabled, just release without closing.
src/outcome/mod.rs:597:                    let mut release_events = self.release_bead(store, bead).await?;
src/outcome/mod.rs:598:                    events.append(&mut release_events);
src/outcome/mod.rs-599-                    return Ok((BeadAction::Released, events));
src/outcome/mod.rs-600-                }
src/outcome/mod.rs-601-            }
src/outcome/mod.rs-602-            Ok(None) => {
src/outcome/mod.rs:603:                // Timeout - we cannot verify bead closure, so release to enforce postcondition.
src/outcome/mod.rs-604-                tracing::warn!(
src/outcome/mod.rs-605-                    bead_id = %bead.id,
src/outcome/mod.rs-606-                    "show() timed out, releasing bead to enforce postcondition"
src/outcome/mod.rs-607-                );
src/outcome/mod.rs-608-                events.push(EventKind::WorkerHandlingTimeout {
src/outcome/mod.rs-609-                    bead_id: bead.id.clone(),
src/outcome/mod.rs-610-                    outcome: "success".to_string(),
src/outcome/mod.rs-611-                    operation: "show".to_string(),
src/outcome/mod.rs-612-                    error: "timeout after 30s".to_string(),
src/outcome/mod.rs-613-                });
src/outcome/mod.rs:614:                let mut release_events = self.release_bead(store, bead).await?;
src/outcome/mod.rs:615:                events.append(&mut release_events);
src/outcome/mod.rs-616-                return Ok((BeadAction::Released, events));
src/outcome/mod.rs-617-            }
src/outcome/mod.rs-618-            Err(e) => {
src/outcome/mod.rs:619:                // Error - we cannot verify bead closure, so release to enforce postcondition.
src/outcome/mod.rs-620-                tracing::warn!(
src/outcome/mod.rs-621-                    bead_id = %bead.id,
src/outcome/mod.rs-622-                    error = %e,
src/outcome/mod.rs-623-                    "show() failed, releasing bead to enforce postcondition"
src/outcome/mod.rs-624-                );
src/outcome/mod.rs-625-                events.push(EventKind::WorkerHandlingTimeout {
src/outcome/mod.rs-626-                    bead_id: bead.id.clone(),
src/outcome/mod.rs-627-                    outcome: "success".to_string(),
src/outcome/mod.rs-628-                    operation: "show".to_string(),
src/outcome/mod.rs-629-                    error: e.to_string(),
src/outcome/mod.rs-630-                });
src/outcome/mod.rs:631:                let mut release_events = self.release_bead(store, bead).await?;
src/outcome/mod.rs:632:                events.append(&mut release_events);
src/outcome/mod.rs-633-                return Ok((BeadAction::Released, events));
src/outcome/mod.rs-634-            }
src/outcome/mod.rs-635-        }
src/outcome/mod.rs-636-
src/outcome/mod.rs-637-        // Flush SQLite state to JSONL so the closed/orphaned bead is captured in
src/outcome/mod.rs-638-        // the git-committed backup. Non-fatal: a flush failure is logged but does
--
src/outcome/mod.rs-649-            }
src/outcome/mod.rs-650-        }
src/outcome/mod.rs-651-
src/outcome/mod.rs-652-        Ok((BeadAction::None, events))
src/outcome/mod.rs-653-    }
src/outcome/mod.rs-654-
src/outcome/mod.rs:655:    /// Handle gate failure: reopen the bead if it was closed, then release it.
src/outcome/mod.rs-656-    async fn handle_gate_failure(
src/outcome/mod.rs-657-        &self,
src/outcome/mod.rs-658-        store: &dyn BeadStore,
src/outcome/mod.rs-659-        bead: &Bead,
src/outcome/mod.rs-660-        report: &crate::validation::GateReport,
src/outcome/mod.rs-661-    ) -> Result<(BeadAction, Vec<EventKind>)> {
--
src/outcome/mod.rs-695-                match self.timeout_op(|| store.reopen(&bead.id), "reopen").await {
src/outcome/mod.rs-696-                    Ok(_) => {}
src/outcome/mod.rs-697-                    Err(e) => {
src/outcome/mod.rs-698-                        tracing::warn!(
src/outcome/mod.rs-699-                            bead_id = %bead.id,
src/outcome/mod.rs-700-                            error = %e,
src/outcome/mod.rs:701:                            "failed to reopen bead — attempting release anyway"
src/outcome/mod.rs-702-                        );
src/outcome/mod.rs-703-                        events.push(EventKind::WorkerHandlingTimeout {
src/outcome/mod.rs-704-                            bead_id: bead.id.clone(),
src/outcome/mod.rs-705-                            outcome: "gate_failure".to_string(),
src/outcome/mod.rs-706-                            operation: "reopen".to_string(),
src/outcome/mod.rs-707-                            error: e.to_string(),
--
src/outcome/mod.rs-728-                    "show() failed during gate failure handling"
src/outcome/mod.rs-729-                );
src/outcome/mod.rs-730-            }
src/outcome/mod.rs-731-            _ => {}
src/outcome/mod.rs-732-        }
src/outcome/mod.rs-733-
src/outcome/mod.rs:734:        // Release the bead back to open with flush-before-release and sync recovery.
src/outcome/mod.rs:735:        let mut release_events = self.release_bead(store, bead).await?;
src/outcome/mod.rs:736:        events.append(&mut release_events);
src/outcome/mod.rs-737-
src/outcome/mod.rs:738:        // If release succeeded, increment the failure count and apply the same
src/outcome/mod.rs-739-        // quarantine ceiling `handle_failure` uses. Without this, a bead that
src/outcome/mod.rs:740:        // fails a gate every cycle is released back to open forever: the
src/outcome/mod.rs-741-        // ARMOR/bf-135k storm ran one bead 24 times in a single day, each
src/outcome/mod.rs-742-        // attempt leaving another commit behind. A gate failure is no less
src/outcome/mod.rs-743-        // repeatable than an agent failure and must respect the same ceiling.
src/outcome/mod.rs:744:        let release_succeeded = events
src/outcome/mod.rs-745-            .iter()
src/outcome/mod.rs-746-            .any(|e| matches!(e, EventKind::BeadReleased { .. }));
src/outcome/mod.rs-747-        let mut action = BeadAction::Released;
src/outcome/mod.rs:748:        if release_succeeded {
src/outcome/mod.rs:749:            match self.increment_failure_count(store, bead).await {
src/outcome/mod.rs-750-                Ok(new_count) => {
src/outcome/mod.rs-751-                    let threshold = self.config.outcome.quarantine_after_failures;
src/outcome/mod.rs-752-                    if threshold > 0 && new_count >= threshold {
src/outcome/mod.rs-753-                        match self
src/outcome/mod.rs-754-                            .quarantine_bead(store, bead, new_count, threshold)
src/outcome/mod.rs-755-                            .await
--
src/outcome/mod.rs-792-            );
src/outcome/mod.rs-793-        }
src/outcome/mod.rs-794-
src/outcome/mod.rs-795-        Ok((action, events))
src/outcome/mod.rs-796-    }
src/outcome/mod.rs-797-
src/outcome/mod.rs:798:    /// Failure: release bead and increment failure count.
src/outcome/mod.rs-799-    ///
src/outcome/mod.rs-800-    /// Mitosis evaluation (for multi-task splitting) is handled externally by
src/outcome/mod.rs-801-    /// the worker after outcome handling — see `MitosisEvaluator`.
src/outcome/mod.rs-802-    ///
src/outcome/mod.rs-803-    /// If br calls timeout or fail, logs the error and continues — does not
src/outcome/mod.rs-804-    /// block the worker in HANDLING state indefinitely.
--
src/outcome/mod.rs-806-        &self,
src/outcome/mod.rs-807-        store: &dyn BeadStore,
src/outcome/mod.rs-808-        bead: &Bead,
src/outcome/mod.rs-809-    ) -> Result<(BeadAction, Vec<EventKind>)> {
src/outcome/mod.rs-810-        tracing::warn!(bead_id = %bead.id, "agent failure — releasing bead");
src/outcome/mod.rs-811-
src/outcome/mod.rs:812:        let mut events = self.release_bead(store, bead).await?;
src/outcome/mod.rs-813-
src/outcome/mod.rs:814:        // If release succeeded, increment failure count and check the
src/outcome/mod.rs-815-        // quarantine threshold. A bead that has exceeded
src/outcome/mod.rs-816-        // `outcome.quarantine_after_failures` consecutive failures is
src/outcome/mod.rs:817:        // quarantined (status=blocked) instead of being left released-to-open
src/outcome/mod.rs-818-        // for the next cycle to re-claim and fail again indefinitely. This
src/outcome/mod.rs-819-        // also closes the mitosis `NotSplittable` fallthrough (worker/mod.rs):
src/outcome/mod.rs-820-        // that verdict no longer matters for beads at or past the ceiling,
src/outcome/mod.rs-821-        // since this check already ran before mitosis evaluation this cycle.
src/outcome/mod.rs:822:        let release_succeeded = events
src/outcome/mod.rs-823-            .iter()
src/outcome/mod.rs-824-            .any(|e| matches!(e, EventKind::BeadReleased { .. }));
src/outcome/mod.rs-825-        let mut action = BeadAction::Released;
src/outcome/mod.rs:826:        if release_succeeded {
src/outcome/mod.rs:827:            match self.increment_failure_count(store, bead).await {
src/outcome/mod.rs-828-                Ok(new_count) => {
src/outcome/mod.rs-829-                    let threshold = self.config.outcome.quarantine_after_failures;
src/outcome/mod.rs-830-                    if threshold > 0 && new_count >= threshold {
src/outcome/mod.rs-831-                        match self
src/outcome/mod.rs-832-                            .quarantine_bead(store, bead, new_count, threshold)
src/outcome/mod.rs-833-                            .await
--
src/outcome/mod.rs-847-                    }
src/outcome/mod.rs-848-                }
src/outcome/mod.rs-849-                Err(e) => {
src/outcome/mod.rs-850-                    tracing::warn!(
src/outcome/mod.rs-851-                        bead_id = %bead.id,
src/outcome/mod.rs-852-                        error = %e,
src/outcome/mod.rs:853:                        "failed to increment failure count after release"
src/outcome/mod.rs-854-                    );
src/outcome/mod.rs-855-                }
src/outcome/mod.rs-856-            }
src/outcome/mod.rs-857-        }
src/outcome/mod.rs-858-
src/outcome/mod.rs-859-        // Ensure we emit a reason event for telemetry.
--
src/outcome/mod.rs-869-            });
src/outcome/mod.rs-870-        }
src/outcome/mod.rs-871-
src/outcome/mod.rs-872-        Ok((action, events))
src/outcome/mod.rs-873-    }
src/outcome/mod.rs-874-
src/outcome/mod.rs:875:    /// Timeout: release bead and add `deferred` label.
src/outcome/mod.rs-876-    ///
src/outcome/mod.rs-877-    /// If br calls timeout or fail, logs the error and continues — does not
src/outcome/mod.rs-878-    /// block the worker in HANDLING state indefinitely.
src/outcome/mod.rs-879-    async fn handle_timeout(
src/outcome/mod.rs-880-        &self,
src/outcome/mod.rs-881-        store: &dyn BeadStore,
src/outcome/mod.rs-882-        bead: &Bead,
src/outcome/mod.rs-883-    ) -> Result<(BeadAction, Vec<EventKind>)> {
src/outcome/mod.rs-884-        tracing::warn!(bead_id = %bead.id, "agent timed out — releasing bead as deferred");
src/outcome/mod.rs-885-
src/outcome/mod.rs:886:        let mut events = self.release_bead(store, bead).await?;
src/outcome/mod.rs-887-
src/outcome/mod.rs:888:        // If release succeeded, increment failure count and add deferred label.
src/outcome/mod.rs:889:        let release_succeeded = events
src/outcome/mod.rs-890-            .iter()
src/outcome/mod.rs-891-            .any(|e| matches!(e, EventKind::BeadReleased { .. }));
src/outcome/mod.rs:892:        if release_succeeded {
src/outcome/mod.rs-893-            // Increment failure count for auto-split tracking.
src/outcome/mod.rs:894:            if let Err(e) = self.increment_failure_count(store, bead).await {
src/outcome/mod.rs-895-                tracing::warn!(
src/outcome/mod.rs-896-                    bead_id = %bead.id,
src/outcome/mod.rs-897-                    error = %e,
src/outcome/mod.rs-898-                    "failed to increment failure count after timeout"
src/outcome/mod.rs-899-                );
src/outcome/mod.rs-900-            }
--
src/outcome/mod.rs-905-            )
src/outcome/mod.rs-906-            .await
src/outcome/mod.rs-907-            {
src/outcome/mod.rs-908-                tracing::warn!(
src/outcome/mod.rs-909-                    bead_id = %bead.id,
src/outcome/mod.rs-910-                    error = %e,
src/outcome/mod.rs:911:                    "add_label deferred timed out or failed after timeout release"
src/outcome/mod.rs-912-                );
src/outcome/mod.rs-913-                events.push(EventKind::WorkerHandlingTimeout {
src/outcome/mod.rs-914-                    bead_id: bead.id.clone(),
src/outcome/mod.rs-915-                    outcome: "timeout".to_string(),
src/outcome/mod.rs-916-                    operation: "add_label".to_string(),
src/outcome/mod.rs-917-                    error: e.to_string(),
--
src/outcome/mod.rs-919-            }
src/outcome/mod.rs-920-        }
src/outcome/mod.rs-921-
src/outcome/mod.rs-922-        Ok((BeadAction::Deferred, events))
src/outcome/mod.rs-923-    }
src/outcome/mod.rs-924-
src/outcome/mod.rs:925:    /// Crash: release bead and create alert bead with diagnostic info.
src/outcome/mod.rs-926-    async fn handle_crash(
src/outcome/mod.rs-927-        &self,
src/outcome/mod.rs-928-        store: &dyn BeadStore,
src/outcome/mod.rs-929-        bead: &Bead,
src/outcome/mod.rs-930-        signal_code: i32,
src/outcome/mod.rs-931-    ) -> Result<(BeadAction, Vec<EventKind>)> {
--
src/outcome/mod.rs-933-            bead_id = %bead.id,
src/outcome/mod.rs-934-            signal_code,
src/outcome/mod.rs-935-            agent = %self.config.agent.default,
src/outcome/mod.rs-936-            "agent crashed — releasing bead and creating alert"
src/outcome/mod.rs-937-        );
src/outcome/mod.rs-938-
src/outcome/mod.rs:939:        let events = self.release_bead(store, bead).await?;
src/outcome/mod.rs-940-
src/outcome/mod.rs-941-        // Create alert bead with diagnostic info (best-effort).
src/outcome/mod.rs-942-        let signal_num = if signal_code > 128 {
src/outcome/mod.rs-943-            signal_code - 128
src/outcome/mod.rs-944-        } else {
src/outcome/mod.rs-945-            signal_code
--
src/outcome/mod.rs-952-             - **Bead ID**: {}\n\
src/outcome/mod.rs-953-             - **Agent**: {}\n\
src/outcome/mod.rs-954-             - **Exit code**: {} (signal {})\n\
src/outcome/mod.rs-955-             - **Workspace**: {}\n\
src/outcome/mod.rs-956-             - **Timestamp**: {}\n\
src/outcome/mod.rs-957-             \n\
src/outcome/mod.rs:958:             The agent process was killed. This bead has been released for retry.",
src/outcome/mod.rs-959-            bead.id,
src/outcome/mod.rs-960-            self.config.agent.default,
src/outcome/mod.rs-961-            signal_code,
src/outcome/mod.rs-962-            signal_num,
src/outcome/mod.rs-963-            bead.workspace.display(),
src/outcome/mod.rs-964-            timestamp,
--
src/outcome/mod.rs-997-            }
src/outcome/mod.rs-998-        }
src/outcome/mod.rs-999-
src/outcome/mod.rs-1000-        Ok((BeadAction::Alerted, events))
src/outcome/mod.rs-1001-    }
src/outcome/mod.rs-1002-
src/outcome/mod.rs:1003:    /// AgentNotFound: release bead, emit error. No retry — this is a config issue.
src/outcome/mod.rs-1004-    async fn handle_agent_not_found(
src/outcome/mod.rs-1005-        &self,
src/outcome/mod.rs-1006-        store: &dyn BeadStore,
src/outcome/mod.rs-1007-        bead: &Bead,
src/outcome/mod.rs-1008-    ) -> Result<(BeadAction, Vec<EventKind>)> {
src/outcome/mod.rs-1009-        tracing::error!(
src/outcome/mod.rs-1010-            bead_id = %bead.id,
src/outcome/mod.rs-1011-            agent = %self.config.agent.default,
src/outcome/mod.rs-1012-            "agent binary not found — releasing bead (config issue, no retry)"
src/outcome/mod.rs-1013-        );
src/outcome/mod.rs-1014-
src/outcome/mod.rs:1015:        let events = self.release_bead(store, bead).await?;
src/outcome/mod.rs-1016-        Ok((BeadAction::Released, events))
src/outcome/mod.rs-1017-    }
src/outcome/mod.rs-1018-
src/outcome/mod.rs:1019:    /// Interrupted: release bead for graceful shutdown.
src/outcome/mod.rs-1020-    async fn handle_interrupted(
src/outcome/mod.rs-1021-        &self,
src/outcome/mod.rs-1022-        store: &dyn BeadStore,
src/outcome/mod.rs-1023-        bead: &Bead,
src/outcome/mod.rs-1024-    ) -> Result<(BeadAction, Vec<EventKind>)> {
src/outcome/mod.rs-1025-        tracing::info!(bead_id = %bead.id, "agent interrupted — releasing bead for clean shutdown");
src/outcome/mod.rs-1026-
src/outcome/mod.rs:1027:        let events = self.release_bead(store, bead).await?;
src/outcome/mod.rs-1028-        Ok((BeadAction::Released, events))
src/outcome/mod.rs-1029-    }
src/outcome/mod.rs-1030-
src/outcome/mod.rs-1031-    /// Increment the failure count label on a bead.
src/outcome/mod.rs-1032-    ///
src/outcome/mod.rs-1033-    /// Labels follow the pattern `failure-count:N`. If `failure-count:2` exists,
src/outcome/mod.rs-1034-    /// the old label is removed and `failure-count:3` is added.
src/outcome/mod.rs-1035-    ///
src/outcome/mod.rs-1036-    /// Returns the new failure count (or 0 if the operation failed).
src/outcome/mod.rs-1037-    ///
src/outcome/mod.rs-1038-    /// All `br` calls are wrapped in timeouts to prevent indefinite hang in
src/outcome/mod.rs-1039-    /// HANDLING state. Failures are non-fatal — we log and continue.
src/outcome/mod.rs:1040:    async fn increment_failure_count(&self, store: &dyn BeadStore, bead: &Bead) -> Result<u32> {
src/outcome/mod.rs-1041-        // Read labels with timeout.
src/outcome/mod.rs-1042-        let labels =
src/outcome/mod.rs-1043-            match tokio::time::timeout(std::time::Duration::from_secs(30), store.labels(&bead.id))
src/outcome/mod.rs-1044-                .await
src/outcome/mod.rs-1045-            {
src/outcome/mod.rs-1046-                Ok(Ok(l)) => l,
--
src/outcome/mod.rs-1137-    ///
src/outcome/mod.rs-1138-    /// Called on success to clear the failure counter so the bead starts fresh
src/outcome/mod.rs-1139-    /// on the next cycle.
src/outcome/mod.rs-1140-    ///
src/outcome/mod.rs-1141-    /// All `br` calls are wrapped in timeouts to prevent indefinite hang in
src/outcome/mod.rs-1142-    /// HANDLING state. Failures are non-fatal — we log and continue.
src/outcome/mod.rs:1143:    async fn reset_failure_count(&self, store: &dyn BeadStore, bead: &Bead) -> Result<()> {
src/outcome/mod.rs-1144-        // Read labels with timeout.
src/outcome/mod.rs-1145-        let labels =
src/outcome/mod.rs-1146-            match tokio::time::timeout(std::time::Duration::from_secs(30), store.labels(&bead.id))
src/outcome/mod.rs-1147-                .await
src/outcome/mod.rs-1148-            {
src/outcome/mod.rs-1149-                Ok(Ok(l)) => l,
--
src/outcome/mod.rs-1215-    /// All `br` calls are wrapped in timeouts to prevent indefinite hang in
src/outcome/mod.rs-1216-    /// HANDLING state. Failures are non-fatal — we log and continue.
src/outcome/mod.rs-1217-    async fn quarantine_bead(
src/outcome/mod.rs-1218-        &self,
src/outcome/mod.rs-1219-        store: &dyn BeadStore,
src/outcome/mod.rs-1220-        bead: &Bead,
src/outcome/mod.rs:1221:        failure_count: u32,
src/outcome/mod.rs-1222-        threshold: u32,
src/outcome/mod.rs-1223-    ) -> Result<Vec<EventKind>> {
src/outcome/mod.rs-1224-        let mut events = Vec::new();
src/outcome/mod.rs-1225-
src/outcome/mod.rs-1226-        tracing::warn!(
src/outcome/mod.rs-1227-            bead_id = %bead.id,
src/outcome/mod.rs:1228:            failure_count,
src/outcome/mod.rs-1229-            threshold,
src/outcome/mod.rs-1230-            "quarantining bead after exceeding failure threshold"
src/outcome/mod.rs-1231-        );
src/outcome/mod.rs-1232-
src/outcome/mod.rs-1233-        // Set status=blocked with timeout. This is what actually stops Pluck
src/outcome/mod.rs-1234-        // from re-selecting the bead — the 'cycling' label below is a
--
src/outcome/mod.rs-1305-            }
src/outcome/mod.rs-1306-        }
src/outcome/mod.rs-1307-
src/outcome/mod.rs-1308-        // Emit the BeadQuarantined event.
src/outcome/mod.rs-1309-        events.push(EventKind::BeadQuarantined {
src/outcome/mod.rs-1310-            bead_id: bead.id.clone(),
src/outcome/mod.rs:1311:            failure_count,
src/outcome/mod.rs-1312-            threshold,
src/outcome/mod.rs-1313-        });
src/outcome/mod.rs-1314-
src/outcome/mod.rs-1315-        Ok(events)
src/outcome/mod.rs-1316-    }
src/outcome/mod.rs-1317-}
--
src/outcome/mod.rs-1321-// ──────────────────────────────────────────────────────────────────────────────
src/outcome/mod.rs-1322-
src/outcome/mod.rs-1323-impl fmt::Display for Outcome {
src/outcome/mod.rs-1324-    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
src/outcome/mod.rs-1325-        match self {
src/outcome/mod.rs-1326-            Outcome::Success => write!(f, "Success"),
src/outcome/mod.rs:1327:            Outcome::Failure => write!(f, "Failure"),
src/outcome/mod.rs-1328-            Outcome::Timeout => write!(f, "Timeout"),
src/outcome/mod.rs-1329-            Outcome::AgentNotFound => write!(f, "AgentNotFound"),
src/outcome/mod.rs-1330-            Outcome::Interrupted => write!(f, "Interrupted"),
src/outcome/mod.rs-1331-            Outcome::Crash(code) => write!(f, "Crash({})", code),
src/outcome/mod.rs-1332-        }
src/outcome/mod.rs-1333-    }
--
src/outcome/mod.rs-1435-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/outcome/mod.rs-1436-            Ok(ClaimResult::NotClaimable {
src/outcome/mod.rs-1437-                reason: "mock".to_string(),
src/outcome/mod.rs-1438-            })
src/outcome/mod.rs-1439-        }
src/outcome/mod.rs-1440-
src/outcome/mod.rs:1441:        async fn release(&self, id: &BeadId) -> Result<()> {
src/outcome/mod.rs-1442-            self.actions
src/outcome/mod.rs-1443-                .lock()
src/outcome/mod.rs-1444-                .unwrap()
src/outcome/mod.rs-1445-                .push(StoreAction::Release(id.to_string()));
src/outcome/mod.rs-1446-            Ok(())
src/outcome/mod.rs-1447-        }
--
src/outcome/mod.rs-1558-
src/outcome/mod.rs-1559-    #[test]
src/outcome/mod.rs-1560-    fn classify_not_interrupted_uses_exit_code_and_verification() {
src/outcome/mod.rs-1561-        // Exit code 0 with verification passes → Success
src/outcome/mod.rs-1562-        assert_eq!(classify(0, false, true), Outcome::Success);
src/outcome/mod.rs-1563-        // Exit code 0 with verification fails → Failure
src/outcome/mod.rs:1564:        assert_eq!(classify(0, false, false), Outcome::Failure);
src/outcome/mod.rs-1565-        // Non-zero exit codes always fail regardless of verification
src/outcome/mod.rs:1566:        assert_eq!(classify(1, false, true), Outcome::Failure);
src/outcome/mod.rs:1567:        assert_eq!(classify(1, false, false), Outcome::Failure);
src/outcome/mod.rs-1568-        assert_eq!(classify(124, false, true), Outcome::Timeout);
src/outcome/mod.rs-1569-        assert_eq!(classify(127, false, true), Outcome::AgentNotFound);
src/outcome/mod.rs-1570-        assert_eq!(classify(129, false, true), Outcome::Crash(129));
src/outcome/mod.rs-1571-    }
src/outcome/mod.rs-1572-
src/outcome/mod.rs-1573-    #[test]
src/outcome/mod.rs-1574-    fn classify_no_wildcard_arms() {
src/outcome/mod.rs-1575-        // Verify key exit codes map correctly per spec.
src/outcome/mod.rs-1576-        // Exit code 0 ONLY succeeds when verification passes
src/outcome/mod.rs-1577-        assert_eq!(classify(0, false, true), Outcome::Success);
src/outcome/mod.rs:1578:        assert_eq!(classify(0, false, false), Outcome::Failure);
src/outcome/mod.rs:1579:        assert_eq!(classify(1, false, true), Outcome::Failure);
src/outcome/mod.rs:1580:        assert_eq!(classify(2, false, true), Outcome::Failure);
src/outcome/mod.rs:1581:        assert_eq!(classify(99, false, true), Outcome::Failure);
src/outcome/mod.rs:1582:        assert_eq!(classify(100, false, true), Outcome::Failure);
src/outcome/mod.rs-1583-        assert_eq!(classify(124, false, true), Outcome::Timeout);
src/outcome/mod.rs:1584:        assert_eq!(classify(125, false, true), Outcome::Failure);
src/outcome/mod.rs:1585:        assert_eq!(classify(128, false, true), Outcome::Failure); // not >128 per spec
src/outcome/mod.rs-1586-        assert_eq!(classify(129, false, true), Outcome::Crash(129));
src/outcome/mod.rs-1587-        assert_eq!(classify(137, false, true), Outcome::Crash(137));
src/outcome/mod.rs-1588-        assert_eq!(classify(-9, false, true), Outcome::Crash(-9));
src/outcome/mod.rs-1589-    }
src/outcome/mod.rs-1590-
src/outcome/mod.rs-1591-    #[test]
src/outcome/mod.rs-1592-    fn classify_verification_gate() {
src/outcome/mod.rs-1593-        // The core fix: exit code 0 does NOT guarantee Success.
src/outcome/mod.rs-1594-        // Verification is the gate.
src/outcome/mod.rs-1595-        assert_eq!(
src/outcome/mod.rs-1596-            classify(0, false, false),
src/outcome/mod.rs:1597:            Outcome::Failure,
src/outcome/mod.rs-1598-            "exit_code=0, verified=false must return Failure"
src/outcome/mod.rs-1599-        );
src/outcome/mod.rs-1600-        assert_eq!(
src/outcome/mod.rs-1601-            classify(0, false, true),
src/outcome/mod.rs-1602-            Outcome::Success,
src/outcome/mod.rs-1603-            "exit_code=0, verified=true must return Success"
--
src/outcome/mod.rs-1620-        assert_eq!(result.outcome, Outcome::Success);
src/outcome/mod.rs-1621-        assert_eq!(result.bead_action, BeadAction::None);
src/outcome/mod.rs-1622-        assert!(!result.telemetry_events.is_empty());
src/outcome/mod.rs-1623-        let actions = store.actions();
src/outcome/mod.rs-1624-        assert!(
src/outcome/mod.rs-1625-            !actions.iter().any(|a| matches!(a, StoreAction::Release(_))),
src/outcome/mod.rs:1626:            "success should not release bead"
src/outcome/mod.rs-1627-        );
src/outcome/mod.rs-1628-    }
src/outcome/mod.rs-1629-
src/outcome/mod.rs-1630-    #[tokio::test]
src/outcome/mod.rs:1631:    async fn handle_success_bead_still_open_releases_to_prevent_leak() {
src/outcome/mod.rs-1632-        let handler = test_handler();
src/outcome/mod.rs-1633-        let store = test_store(BeadStatus::InProgress);
src/outcome/mod.rs-1634-        let bead = test_bead(BeadStatus::InProgress);
src/outcome/mod.rs-1635-
src/outcome/mod.rs-1636-        let result = handler
src/outcome/mod.rs-1637-            .handle(&store, &bead, &test_output(0), false)
--
src/outcome/mod.rs-1639-            .unwrap();
src/outcome/mod.rs-1640-
src/outcome/mod.rs-1641-        assert_eq!(result.outcome, Outcome::Success);
src/outcome/mod.rs-1642-        assert_eq!(
src/outcome/mod.rs-1643-            result.bead_action,
src/outcome/mod.rs-1644-            BeadAction::Released,
src/outcome/mod.rs:1645:            "bead still open after success must be released to enforce postcondition"
src/outcome/mod.rs-1646-        );
src/outcome/mod.rs-1647-        let actions = store.actions();
src/outcome/mod.rs-1648-        assert!(
src/outcome/mod.rs-1649-            actions.iter().any(|a| matches!(a, StoreAction::Show(_))),
src/outcome/mod.rs-1650-            "success should check bead status"
src/outcome/mod.rs-1651-        );
src/outcome/mod.rs-1652-        assert!(
src/outcome/mod.rs-1653-            result
src/outcome/mod.rs-1654-                .telemetry_events
src/outcome/mod.rs-1655-                .iter()
src/outcome/mod.rs-1656-                .any(|e| matches!(e, EventKind::BeadOrphaned { .. })),
src/outcome/mod.rs:1657:            "orphan event should be emitted even though bead is released"
src/outcome/mod.rs-1658-        );
src/outcome/mod.rs-1659-        assert!(
src/outcome/mod.rs-1660-            actions.iter().any(|a| matches!(a, StoreAction::Release(_))),
src/outcome/mod.rs:1661:            "bead must be released when still open after success"
src/outcome/mod.rs-1662-        );
src/outcome/mod.rs-1663-    }
src/outcome/mod.rs-1664-
src/outcome/mod.rs-1665-    #[tokio::test]
src/outcome/mod.rs:1666:    async fn handle_failure_releases_and_increments_count() {
src/outcome/mod.rs-1667-        let handler = test_handler();
src/outcome/mod.rs-1668-        let store = test_store(BeadStatus::InProgress);
src/outcome/mod.rs-1669-        let bead = test_bead(BeadStatus::InProgress);
src/outcome/mod.rs-1670-
src/outcome/mod.rs-1671-        let result = handler
src/outcome/mod.rs-1672-            .handle(&store, &bead, &test_output(1), false)
src/outcome/mod.rs-1673-            .await
src/outcome/mod.rs-1674-            .unwrap();
src/outcome/mod.rs-1675-
src/outcome/mod.rs:1676:        assert_eq!(result.outcome, Outcome::Failure);
src/outcome/mod.rs-1677-        assert_eq!(result.bead_action, BeadAction::Released);
src/outcome/mod.rs-1678-        assert!(!result.telemetry_events.is_empty());
src/outcome/mod.rs-1679-
src/outcome/mod.rs-1680-        let actions = store.actions();
src/outcome/mod.rs-1681-        assert!(
src/outcome/mod.rs-1682-            actions
src/outcome/mod.rs-1683-                .iter()
src/outcome/mod.rs-1684-                .any(|a| matches!(a, StoreAction::Release(id) if id == "needle-test")),
src/outcome/mod.rs:1685:            "failure must release bead"
src/outcome/mod.rs-1686-        );
src/outcome/mod.rs-1687-        assert!(
src/outcome/mod.rs-1688-            actions.iter().any(
src/outcome/mod.rs-1689-                |a| matches!(a, StoreAction::AddLabel(_, label) if label == "failure-count:1")
src/outcome/mod.rs-1690-            ),
src/outcome/mod.rs-1691-            "failure must add failure-count:1"
--
src/outcome/mod.rs-1753-            "quarantine must add the cycling label"
src/outcome/mod.rs-1754-        );
src/outcome/mod.rs-1755-        assert!(
src/outcome/mod.rs-1756-            result.telemetry_events.iter().any(|e| matches!(
src/outcome/mod.rs-1757-                e,
src/outcome/mod.rs-1758-                EventKind::BeadQuarantined {
src/outcome/mod.rs:1759:                    failure_count: 5,
src/outcome/mod.rs-1760-                    threshold: 5,
src/outcome/mod.rs-1761-                    ..
src/outcome/mod.rs-1762-                }
src/outcome/mod.rs-1763-            )),
src/outcome/mod.rs-1764-            "must emit BeadQuarantined with the crossing count and configured threshold"
src/outcome/mod.rs-1765-        );
--
src/outcome/mod.rs-1767-
src/outcome/mod.rs-1768-    #[tokio::test]
src/outcome/mod.rs-1769-    async fn handle_failure_below_threshold_does_not_quarantine() {
src/outcome/mod.rs-1770-        // Same setup as the threshold test, one failure count lower — this is
src/outcome/mod.rs-1771-        // the regression case for the mitosis NotSplittable fallthrough
src/outcome/mod.rs-1772-        // (ADR-006 Context point 2): a bead below the ceiling still just
src/outcome/mod.rs:1773:        // releases normally, it does not get blocked prematurely.
src/outcome/mod.rs-1774-        let handler = test_handler();
src/outcome/mod.rs-1775-        let store = MockBeadStore::new(BeadStatus::InProgress)
src/outcome/mod.rs-1776-            .with_labels(vec!["failure-count:3".to_string()]);
src/outcome/mod.rs-1777-        let bead = test_bead(BeadStatus::InProgress);
src/outcome/mod.rs-1778-
src/outcome/mod.rs-1779-        let result = handler
--
src/outcome/mod.rs-1809-            !actions.iter().any(|a| matches!(a, StoreAction::Block(_))),
src/outcome/mod.rs-1810-            "threshold=0 must disable quarantine entirely, regardless of failure count"
src/outcome/mod.rs-1811-        );
src/outcome/mod.rs-1812-    }
src/outcome/mod.rs-1813-
src/outcome/mod.rs-1814-    #[tokio::test]
src/outcome/mod.rs:1815:    async fn handle_success_resets_failure_count() {
src/outcome/mod.rs-1816-        // Success should reset failure count by removing all failure-count:N labels.
src/outcome/mod.rs-1817-        let handler = test_handler();
src/outcome/mod.rs-1818-        let store =
src/outcome/mod.rs-1819-            MockBeadStore::new(BeadStatus::Done).with_labels(vec!["failure-count:3".to_string()]);
src/outcome/mod.rs-1820-        let bead = test_bead(BeadStatus::InProgress);
src/outcome/mod.rs-1821-
--
src/outcome/mod.rs-1832-            ),
src/outcome/mod.rs-1833-            "should remove failure-count:3 on success"
src/outcome/mod.rs-1834-        );
src/outcome/mod.rs-1835-    }
src/outcome/mod.rs-1836-
src/outcome/mod.rs-1837-    #[tokio::test]
src/outcome/mod.rs:1838:    async fn handle_timeout_releases_and_adds_deferred() {
src/outcome/mod.rs-1839-        let handler = test_handler();
src/outcome/mod.rs-1840-        let store = test_store(BeadStatus::InProgress);
src/outcome/mod.rs-1841-        let bead = test_bead(BeadStatus::InProgress);
src/outcome/mod.rs-1842-
src/outcome/mod.rs-1843-        let result = handler
src/outcome/mod.rs-1844-            .handle(&store, &bead, &test_output(124), false)
--
src/outcome/mod.rs-1850-
src/outcome/mod.rs-1851-        let actions = store.actions();
src/outcome/mod.rs-1852-        assert!(
src/outcome/mod.rs-1853-            actions
src/outcome/mod.rs-1854-                .iter()
src/outcome/mod.rs-1855-                .any(|a| matches!(a, StoreAction::Release(id) if id == "needle-test")),
src/outcome/mod.rs:1856:            "timeout must release bead"
src/outcome/mod.rs-1857-        );
src/outcome/mod.rs-1858-        assert!(
src/outcome/mod.rs-1859-            actions
src/outcome/mod.rs-1860-                .iter()
src/outcome/mod.rs-1861-                .any(|a| matches!(a, StoreAction::AddLabel(_, label) if label == "deferred")),
src/outcome/mod.rs-1862-            "timeout must add deferred label"
src/outcome/mod.rs-1863-        );
src/outcome/mod.rs-1864-    }
src/outcome/mod.rs-1865-
src/outcome/mod.rs-1866-    #[tokio::test]
src/outcome/mod.rs:1867:    async fn handle_crash_releases_and_creates_alert_bead() {
src/outcome/mod.rs-1868-        let handler = test_handler();
src/outcome/mod.rs-1869-        let store = test_store(BeadStatus::InProgress);
src/outcome/mod.rs-1870-        let bead = test_bead(BeadStatus::InProgress);
src/outcome/mod.rs-1871-
src/outcome/mod.rs-1872-        let result = handler
src/outcome/mod.rs-1873-            .handle(&store, &bead, &test_output(137), false)
--
src/outcome/mod.rs-1879-
src/outcome/mod.rs-1880-        let actions = store.actions();
src/outcome/mod.rs-1881-        assert!(
src/outcome/mod.rs-1882-            actions
src/outcome/mod.rs-1883-                .iter()
src/outcome/mod.rs-1884-                .any(|a| matches!(a, StoreAction::Release(id) if id == "needle-test")),
src/outcome/mod.rs:1885:            "crash must release bead"
src/outcome/mod.rs-1886-        );
src/outcome/mod.rs-1887-        assert!(
src/outcome/mod.rs-1888-            actions.iter().any(
src/outcome/mod.rs-1889-                |a| matches!(a, StoreAction::CreateBead(title, _) if title.contains("needle-test"))
src/outcome/mod.rs-1890-            ),
src/outcome/mod.rs-1891-            "crash must create alert bead referencing the original bead"
--
src/outcome/mod.rs-1905-
src/outcome/mod.rs-1906-        assert_eq!(result.outcome, Outcome::Crash(-1));
src/outcome/mod.rs-1907-        assert_eq!(result.bead_action, BeadAction::Alerted);
src/outcome/mod.rs-1908-    }
src/outcome/mod.rs-1909-
src/outcome/mod.rs-1910-    #[tokio::test]
src/outcome/mod.rs:1911:    async fn handle_agent_not_found_releases() {
src/outcome/mod.rs-1912-        let handler = test_handler();
src/outcome/mod.rs-1913-        let store = test_store(BeadStatus::InProgress);
src/outcome/mod.rs-1914-        let bead = test_bead(BeadStatus::InProgress);
src/outcome/mod.rs-1915-
src/outcome/mod.rs-1916-        let result = handler
src/outcome/mod.rs-1917-            .handle(&store, &bead, &test_output(127), false)
--
src/outcome/mod.rs-1923-
src/outcome/mod.rs-1924-        let actions = store.actions();
src/outcome/mod.rs-1925-        assert!(
src/outcome/mod.rs-1926-            actions
src/outcome/mod.rs-1927-                .iter()
src/outcome/mod.rs-1928-                .any(|a| matches!(a, StoreAction::Release(id) if id == "needle-test")),
src/outcome/mod.rs:1929:            "agent_not_found must release bead"
src/outcome/mod.rs-1930-        );
src/outcome/mod.rs-1931-    }
src/outcome/mod.rs-1932-
src/outcome/mod.rs-1933-    #[tokio::test]
src/outcome/mod.rs:1934:    async fn handle_interrupted_releases() {
src/outcome/mod.rs-1935-        let handler = test_handler();
src/outcome/mod.rs-1936-        let store = test_store(BeadStatus::InProgress);
src/outcome/mod.rs-1937-        let bead = test_bead(BeadStatus::InProgress);
src/outcome/mod.rs-1938-
src/outcome/mod.rs-1939-        let result = handler
src/outcome/mod.rs-1940-            .handle(&store, &bead, &test_output(0), true)
--
src/outcome/mod.rs-1946-
src/outcome/mod.rs-1947-        let actions = store.actions();
src/outcome/mod.rs-1948-        assert!(
src/outcome/mod.rs-1949-            actions
src/outcome/mod.rs-1950-                .iter()
src/outcome/mod.rs-1951-                .any(|a| matches!(a, StoreAction::Release(id) if id == "needle-test")),
src/outcome/mod.rs:1952:            "interrupted must release bead"
src/outcome/mod.rs-1953-        );
src/outcome/mod.rs-1954-    }
src/outcome/mod.rs-1955-
src/outcome/mod.rs-1956-    #[tokio::test]
src/outcome/mod.rs-1957-    async fn handle_failure_emits_telemetry_events() {
src/outcome/mod.rs-1958-        let handler = test_handler();
--
src/outcome/mod.rs-1973-        );
src/outcome/mod.rs-1974-    }
src/outcome/mod.rs-1975-
src/outcome/mod.rs-1976-    #[test]
src/outcome/mod.rs-1977-    fn outcome_display_covers_all_variants() {
src/outcome/mod.rs-1978-        assert_eq!(format!("{}", Outcome::Success), "Success");
src/outcome/mod.rs:1979:        assert_eq!(format!("{}", Outcome::Failure), "Failure");
src/outcome/mod.rs-1980-        assert_eq!(format!("{}", Outcome::Timeout), "Timeout");
src/outcome/mod.rs-1981-        assert_eq!(format!("{}", Outcome::AgentNotFound), "AgentNotFound");
src/outcome/mod.rs-1982-        assert_eq!(format!("{}", Outcome::Interrupted), "Interrupted");
src/outcome/mod.rs-1983-        assert_eq!(format!("{}", Outcome::Crash(-9)), "Crash(-9)");
src/outcome/mod.rs-1984-    }
src/outcome/mod.rs-1985-
--
src/outcome/mod.rs-2028-            .telemetry_events
src/outcome/mod.rs-2029-            .iter()
src/outcome/mod.rs-2030-            .any(|e| matches!(e, EventKind::BeadCompleted { .. })));
src/outcome/mod.rs-2031-    }
src/outcome/mod.rs-2032-
src/outcome/mod.rs-2033-    #[tokio::test]
src/outcome/mod.rs:2034:    async fn handle_success_verification_fails_releases_bead() {
src/outcome/mod.rs:2035:        // Verification fails → bead released.
src/outcome/mod.rs-2036-        let handler = test_handler_with_verification(vec!["false".to_string()]);
src/outcome/mod.rs-2037-        let store = test_store(BeadStatus::InProgress);
src/outcome/mod.rs-2038-        let bead = test_bead(BeadStatus::InProgress);
src/outcome/mod.rs-2039-
src/outcome/mod.rs-2040-        let result = handler
src/outcome/mod.rs-2041-            .handle(&store, &bead, &test_output(0), false)
--
src/outcome/mod.rs-2047-
src/outcome/mod.rs-2048-        let actions = store.actions();
src/outcome/mod.rs-2049-        assert!(
src/outcome/mod.rs-2050-            actions
src/outcome/mod.rs-2051-                .iter()
src/outcome/mod.rs-2052-                .any(|a| matches!(a, StoreAction::Release(id) if id == "needle-test")),
src/outcome/mod.rs:2053:            "verification failure must release bead"
src/outcome/mod.rs-2054-        );
src/outcome/mod.rs-2055-        assert!(
src/outcome/mod.rs-2056-            actions.iter().any(
src/outcome/mod.rs-2057-                |a| matches!(a, StoreAction::AddLabel(_, label) if label == "verification-failed")
src/outcome/mod.rs-2058-            ),
src/outcome/mod.rs-2059-            "verification failure must add verification-failed label"
src/outcome/mod.rs-2060-        );
src/outcome/mod.rs-2061-    }
src/outcome/mod.rs-2062-
src/outcome/mod.rs-2063-    #[tokio::test]
src/outcome/mod.rs-2064-    async fn handle_success_verification_fails_reopens_closed_bead() {
src/outcome/mod.rs:2065:        // Agent closed the bead, but verification fails → reopen then release.
src/outcome/mod.rs-2066-        let handler = test_handler_with_verification(vec!["false".to_string()]);
src/outcome/mod.rs-2067-        let store = test_store(BeadStatus::Done);
src/outcome/mod.rs-2068-        let bead = test_bead(BeadStatus::InProgress);
src/outcome/mod.rs-2069-
src/outcome/mod.rs-2070-        let result = handler
src/outcome/mod.rs-2071-            .handle(&store, &bead, &test_output(0), false)
--
src/outcome/mod.rs-2082-            "verification failure on closed bead must reopen it first"
src/outcome/mod.rs-2083-        );
src/outcome/mod.rs-2084-        assert!(
src/outcome/mod.rs-2085-            actions
src/outcome/mod.rs-2086-                .iter()
src/outcome/mod.rs-2087-                .any(|a| matches!(a, StoreAction::Release(id) if id == "needle-test")),
src/outcome/mod.rs:2088:            "verification failure must release bead after reopening"
src/outcome/mod.rs-2089-        );
src/outcome/mod.rs-2090-    }
src/outcome/mod.rs-2091-
src/outcome/mod.rs-2092-    #[tokio::test]
src/outcome/mod.rs:2093:    async fn handle_success_verification_fails_increments_failure_count() {
src/outcome/mod.rs-2094-        let handler = test_handler_with_verification(vec!["false".to_string()]);
src/outcome/mod.rs-2095-        let store = test_store(BeadStatus::InProgress);
src/outcome/mod.rs-2096-        let bead = test_bead(BeadStatus::InProgress);
src/outcome/mod.rs-2097-
src/outcome/mod.rs-2098-        let _result = handler
src/outcome/mod.rs-2099-            .handle(&store, &bead, &test_output(0), false)
--
src/outcome/mod.rs-2108-            "verification failure must increment failure count"
src/outcome/mod.rs-2109-        );
src/outcome/mod.rs-2110-    }
src/outcome/mod.rs-2111-
src/outcome/mod.rs-2112-    #[tokio::test]
src/outcome/mod.rs-2113-    async fn handle_success_multiple_gates_first_fails() {
src/outcome/mod.rs:2114:        // First gate passes, second fails → should stop and release.
src/outcome/mod.rs-2115-        let handler = test_handler_with_verification(vec![
src/outcome/mod.rs-2116-            "true".to_string(),
src/outcome/mod.rs-2117-            "false".to_string(),
src/outcome/mod.rs-2118-            "echo should-not-run".to_string(),
src/outcome/mod.rs-2119-        ]);
src/outcome/mod.rs-2120-        let store = test_store(BeadStatus::InProgress);
--
src/outcome/mod.rs-2157-            }
src/outcome/mod.rs-2158-
src/outcome/mod.rs-2159-            async fn claim_auto(&self, actor: &str) -> Result<ClaimResult> {
src/outcome/mod.rs-2160-                self.inner.claim_auto(actor).await
src/outcome/mod.rs-2161-            }
src/outcome/mod.rs-2162-
src/outcome/mod.rs:2163:            async fn release(&self, id: &BeadId) -> Result<()> {
src/outcome/mod.rs:2164:                self.inner.release(id).await
src/outcome/mod.rs-2165-            }
src/outcome/mod.rs-2166-            async fn block(&self, id: &BeadId) -> Result<()> {
src/outcome/mod.rs-2167-                self.inner.block(id).await
src/outcome/mod.rs-2168-            }
src/outcome/mod.rs-2169-            async fn flush(&self) -> Result<()> {
src/outcome/mod.rs-2170-                // Simulate a slow flush that times out.
--
src/outcome/mod.rs-2225-        let result = handler
src/outcome/mod.rs-2226-            .handle(store.as_ref(), &bead, &test_output(1), false)
src/outcome/mod.rs-2227-            .await
src/outcome/mod.rs-2228-            .unwrap();
src/outcome/mod.rs-2229-
src/outcome/mod.rs-2230-        // Should still complete with Released action despite flush timeout.
src/outcome/mod.rs:2231:        assert_eq!(result.outcome, Outcome::Failure);
src/outcome/mod.rs-2232-        assert_eq!(result.bead_action, BeadAction::Released);
src/outcome/mod.rs-2233-
src/outcome/mod.rs-2234-        // Should emit a timeout event.
src/outcome/mod.rs-2235-        assert!(result
src/outcome/mod.rs-2236-            .telemetry_events
src/outcome/mod.rs-2237-            .iter()
src/outcome/mod.rs-2238-            .any(|e| matches!(e, EventKind::WorkerHandlingTimeout { operation, .. } if operation == "flush")));
src/outcome/mod.rs-2239-    }
src/outcome/mod.rs-2240-
src/outcome/mod.rs-2241-    #[tokio::test]
src/outcome/mod.rs:2242:    async fn handle_failure_with_release_timeout_continues_gracefully() {
src/outcome/mod.rs:2243:        // Test that release timeout doesn't block the worker in HANDLING state.
src/outcome/mod.rs-2244-        struct SlowReleaseStore {
src/outcome/mod.rs-2245-            inner: MockBeadStore,
src/outcome/mod.rs-2246-        }
src/outcome/mod.rs-2247-
src/outcome/mod.rs-2248-        #[async_trait]
src/outcome/mod.rs-2249-        impl BeadStore for SlowReleaseStore {
--
src/outcome/mod.rs-2265-            }
src/outcome/mod.rs-2266-
src/outcome/mod.rs-2267-            async fn claim_auto(&self, actor: &str) -> Result<ClaimResult> {
src/outcome/mod.rs-2268-                self.inner.claim_auto(actor).await
src/outcome/mod.rs-2269-            }
src/outcome/mod.rs-2270-
src/outcome/mod.rs:2271:            async fn release(&self, _id: &BeadId) -> Result<()> {
src/outcome/mod.rs:2272:                // Simulate a slow release that times out.
src/outcome/mod.rs-2273-                tokio::time::sleep(std::time::Duration::from_secs(35)).await;
src/outcome/mod.rs-2274-                Ok(())
src/outcome/mod.rs-2275-            }
src/outcome/mod.rs-2276-            async fn block(&self, id: &BeadId) -> Result<()> {
src/outcome/mod.rs-2277-                self.inner.block(id).await
src/outcome/mod.rs-2278-            }
--
src/outcome/mod.rs-2332-
src/outcome/mod.rs-2333-        let result = handler
src/outcome/mod.rs-2334-            .handle(store.as_ref(), &bead, &test_output(1), false)
src/outcome/mod.rs-2335-            .await
src/outcome/mod.rs-2336-            .unwrap();
src/outcome/mod.rs-2337-
src/outcome/mod.rs:2338:        // Should still complete despite release timeout.
src/outcome/mod.rs:2339:        assert_eq!(result.outcome, Outcome::Failure);
src/outcome/mod.rs-2340-        // The bead action should still be Released (best-effort).
src/outcome/mod.rs-2341-        assert_eq!(result.bead_action, BeadAction::Released);
src/outcome/mod.rs-2342-
src/outcome/mod.rs:2343:        // Should emit a release failure event.
src/outcome/mod.rs-2344-        assert!(result
src/outcome/mod.rs-2345-            .telemetry_events
src/outcome/mod.rs-2346-            .iter()
src/outcome/mod.rs-2347-            .any(|e| matches!(e, EventKind::BeadReleaseFailed { .. })));
src/outcome/mod.rs-2348-    }
src/outcome/mod.rs-2349-
--
src/outcome/mod.rs-2358-        let result = handler
src/outcome/mod.rs-2359-            .handle_with_cancellation(&store, &bead, &test_output(1), false, cancelled)
src/outcome/mod.rs-2360-            .await
src/outcome/mod.rs-2361-            .unwrap();
src/outcome/mod.rs-2362-
src/outcome/mod.rs-2363-        // Should return a default result without calling the store.
src/outcome/mod.rs:2364:        assert_eq!(result.outcome, Outcome::Failure);
src/outcome/mod.rs-2365-        assert_eq!(result.bead_action, BeadAction::None);
src/outcome/mod.rs-2366-        assert!(result.telemetry_events.is_empty());
src/outcome/mod.rs-2367-    }
src/outcome/mod.rs-2368-
src/outcome/mod.rs-2369-    // ── configurable outcome timeout tests (GitHub issue jedarden/NEEDLE#8) ──
src/outcome/mod.rs-2370-
--
src/outcome/mod.rs-2431-            async fn claim(&self, id: &BeadId, actor: &str) -> Result<ClaimResult> {
src/outcome/mod.rs-2432-                self.inner.claim(id, actor).await
src/outcome/mod.rs-2433-            }
src/outcome/mod.rs-2434-            async fn claim_auto(&self, actor: &str) -> Result<ClaimResult> {
src/outcome/mod.rs-2435-                self.inner.claim_auto(actor).await
src/outcome/mod.rs-2436-            }
src/outcome/mod.rs:2437:            async fn release(&self, id: &BeadId) -> Result<()> {
src/outcome/mod.rs:2438:                self.inner.release(id).await
src/outcome/mod.rs-2439-            }
src/outcome/mod.rs-2440-            async fn block(&self, id: &BeadId) -> Result<()> {
src/outcome/mod.rs-2441-                self.inner.block(id).await
src/outcome/mod.rs-2442-            }
src/outcome/mod.rs-2443-            async fn flush(&self) -> Result<()> {
src/outcome/mod.rs-2444-                self.inner.flush().await
--
src/outcome/mod.rs-2578-        );
src/outcome/mod.rs-2579-    }
src/outcome/mod.rs-2580-
src/outcome/mod.rs-2581-    // ── Regression tests for needle-3386daef: dispatch postcondition ──
src/outcome/mod.rs-2582-
src/outcome/mod.rs-2583-    #[tokio::test]
src/outcome/mod.rs:2584:    async fn regression_2026_08_17_orphan_loop_bead_released_not_leaked() {
src/outcome/mod.rs-2585-        // Regression test for the 2026-08-17 loop where needle-55ec0193 was worked
src/outcome/mod.rs-2586-        // THREE times and leaked every time. Each worker exited with success but the
src/outcome/mod.rs-2587-        // bead remained in_progress, allowing re-claim in the same second as the
src/outcome/mod.rs-2588-        // success. This test verifies that an agent exiting 0 without closing the
src/outcome/mod.rs:2589:        // bead now releases it back to open, preventing the leak.
src/outcome/mod.rs-2590-        let handler = test_handler();
src/outcome/mod.rs-2591-        let store = test_store(BeadStatus::InProgress);
src/outcome/mod.rs-2592-        let bead = test_bead(BeadStatus::InProgress);
src/outcome/mod.rs-2593-
src/outcome/mod.rs-2594-        // Simulate agent exiting 0 without closing the bead
src/outcome/mod.rs-2595-        let result = handler
src/outcome/mod.rs-2596-            .handle(&store, &bead, &test_output(0), false)
src/outcome/mod.rs-2597-            .await
src/outcome/mod.rs-2598-            .unwrap();
src/outcome/mod.rs-2599-
src/outcome/mod.rs:2600:        // Postcondition: bead must be released, not left in_progress
src/outcome/mod.rs-2601-        assert_eq!(
src/outcome/mod.rs-2602-            result.bead_action,
src/outcome/mod.rs-2603-            BeadAction::Released,
src/outcome/mod.rs:2604:            "agent exit 0 without closure must release bead to prevent claim leak"
src/outcome/mod.rs-2605-        );
src/outcome/mod.rs-2606-
src/outcome/mod.rs:2607:        // Verify the release was actually performed
src/outcome/mod.rs-2608-        let actions = store.actions();
src/outcome/mod.rs-2609-        assert!(
src/outcome/mod.rs-2610-            actions
src/outcome/mod.rs-2611-                .iter()
src/outcome/mod.rs-2612-                .any(|a| matches!(a, StoreAction::Release(id) if id == "needle-test")),
src/outcome/mod.rs:2613:            "bead must be released to open/unassigned state"
src/outcome/mod.rs-2614-        );
src/outcome/mod.rs-2615-
src/outcome/mod.rs-2616-        // Verify BeadOrphaned event was emitted for observability
src/outcome/mod.rs-2617-        assert!(
src/outcome/mod.rs-2618-            result
src/outcome/mod.rs-2619-                .telemetry_events
src/outcome/mod.rs-2620-                .iter()
src/outcome/mod.rs-2621-                .any(|e| matches!(e, EventKind::BeadOrphaned { .. })),
src/outcome/mod.rs:2622:            "orphan event must be emitted even though bead is released"
src/outcome/mod.rs-2623-        );
src/outcome/mod.rs-2624-    }
src/outcome/mod.rs-2625-}
--
src/validation/predispatch.rs-329-            async fn claim_auto(&self, _actor: &str) -> anyhow::Result<crate::types::ClaimResult> {
src/validation/predispatch.rs-330-                Ok(crate::types::ClaimResult::NotClaimable {
src/validation/predispatch.rs-331-                    reason: "mock".to_string(),
src/validation/predispatch.rs-332-                })
src/validation/predispatch.rs-333-            }
src/validation/predispatch.rs-334-
src/validation/predispatch.rs:335:            async fn release(&self, _id: &BeadId) -> anyhow::Result<()> {
src/validation/predispatch.rs-336-                Ok(())
src/validation/predispatch.rs-337-            }
src/validation/predispatch.rs-338-
src/validation/predispatch.rs-339-            async fn block(&self, _id: &BeadId) -> anyhow::Result<()> {
src/validation/predispatch.rs-340-                Ok(())
src/validation/predispatch.rs-341-            }
--
src/strand/unravel.rs-659-        }
src/strand/unravel.rs-660-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<ClaimResult> {
src/strand/unravel.rs-661-            Ok(ClaimResult::NotClaimable {
src/strand/unravel.rs-662-                reason: "mock".to_string(),
src/strand/unravel.rs-663-            })
src/strand/unravel.rs-664-        }
src/strand/unravel.rs:665:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/unravel.rs-666-            Ok(())
src/strand/unravel.rs-667-        }
src/strand/unravel.rs-668-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/unravel.rs-669-            Ok(())
src/strand/unravel.rs-670-        }
src/strand/unravel.rs-671-        async fn flush(&self) -> Result<()> {
--
src/strand/pluck.rs-156-    }
src/strand/pluck.rs-157-
src/strand/pluck.rs-158-    /// Extract the failure count from a bead's labels.
src/strand/pluck.rs-159-    ///
src/strand/pluck.rs-160-    /// Labels follow the pattern `failure-count:N`. Returns the count if found,
src/strand/pluck.rs-161-    /// or 0 if no failure-count label exists.
src/strand/pluck.rs:162:    fn extract_failure_count(bead: &Bead) -> u32 {
src/strand/pluck.rs-163-        bead.labels
src/strand/pluck.rs-164-            .iter()
src/strand/pluck.rs-165-            .filter_map(|l| l.strip_prefix("failure-count:"))
src/strand/pluck.rs-166-            .filter_map(|s| s.parse::<u32>().ok())
src/strand/pluck.rs-167-            .max()
src/strand/pluck.rs-168-            .unwrap_or(0)
src/strand/pluck.rs-169-    }
src/strand/pluck.rs-170-
src/strand/pluck.rs-171-    /// Sort candidates in deterministic priority order.
src/strand/pluck.rs-172-    ///
src/strand/pluck.rs:173:    /// Sort key: `(priority ASC, failure_count ASC, created_at ASC, id ASC)`.
src/strand/pluck.rs-174-    ///
src/strand/pluck.rs:175:    /// `failure_count` sits ahead of `created_at` deliberately: without it, a
src/strand/pluck.rs-176-    /// bead that keeps failing is (by construction) always at least as old as
src/strand/pluck.rs-177-    /// its ready siblings, so it sorts to slot 1 every single cycle and the
src/strand/pluck.rs-178-    /// worker never even tries the other ready work sitting behind it. This
src/strand/pluck.rs-179-    /// does not by itself stop the retry loop — `split_after_failures` /
src/strand/pluck.rs-180-    /// `OutcomeConfig::quarantine_after_failures` own that — it just stops a
src/strand/pluck.rs-181-    /// struggling bead from starving healthier ready beads at the same
src/strand/pluck.rs-182-    /// priority while it climbs toward the quarantine threshold.
src/strand/pluck.rs-183-    /// The id tie-breaker ensures identical ordering across platforms.
src/strand/pluck.rs-184-    fn sort_candidates(candidates: &mut [Bead]) {
src/strand/pluck.rs-185-        candidates.sort_by(|a, b| {
src/strand/pluck.rs-186-            a.priority
src/strand/pluck.rs-187-                .cmp(&b.priority)
src/strand/pluck.rs:188:                .then_with(|| Self::extract_failure_count(a).cmp(&Self::extract_failure_count(b)))
src/strand/pluck.rs-189-                .then_with(|| a.created_at.cmp(&b.created_at))
src/strand/pluck.rs-190-                .then_with(|| a.id.as_ref().cmp(b.id.as_ref()))
src/strand/pluck.rs-191-        });
src/strand/pluck.rs-192-    }
src/strand/pluck.rs-193-
src/strand/pluck.rs-194-    /// Get the filtering statistics from the most recent evaluation.
--
src/strand/pluck.rs-516-
src/strand/pluck.rs-517-        // 5. Check for split trigger: if the first candidate has accumulated
src/strand/pluck.rs-518-        //    enough consecutive failures, dispatch a SPLIT instruction instead
src/strand/pluck.rs-519-        //    of returning the bead for normal processing.
src/strand/pluck.rs-520-        if self.split_after_failures > 0 {
src/strand/pluck.rs-521-            if let Some(first_candidate) = candidates.first() {
src/strand/pluck.rs:522:                let failure_count = Self::extract_failure_count(first_candidate);
src/strand/pluck.rs-523-                tracing::debug!(
src/strand/pluck.rs-524-                    bead_id = %first_candidate.id,
src/strand/pluck.rs:525:                    failure_count = failure_count,
src/strand/pluck.rs-526-                    threshold = self.split_after_failures,
src/strand/pluck.rs:527:                    split_triggered = failure_count >= self.split_after_failures,
src/strand/pluck.rs-528-                    "Checking split trigger for first candidate"
src/strand/pluck.rs-529-                );
src/strand/pluck.rs-530-
src/strand/pluck.rs:531:                if failure_count >= self.split_after_failures {
src/strand/pluck.rs-532-                    // Check if this bead references NEEDLE-internal configuration.
src/strand/pluck.rs-533-                    // Such beads have no legitimate resolution path from inside a target repo
src/strand/pluck.rs-534-                    // and should not be split into child beads there.
src/strand/pluck.rs-535-                    if detects_needle_internal_config(first_candidate) {
src/strand/pluck.rs-536-                        tracing::info!(
src/strand/pluck.rs-537-                            bead_id = %first_candidate.id,
src/strand/pluck.rs-538-                            title = %first_candidate.title,
src/strand/pluck.rs:539:                            failure_count = failure_count,
src/strand/pluck.rs-540-                            "Split skipped: bead references NEEDLE-internal configuration, out of scope for target workspace"
src/strand/pluck.rs-541-                        );
src/strand/pluck.rs-542-                        // Filter out this candidate and re-evaluate the remaining candidates.
src/strand/pluck.rs-543-                        let excluded_id = first_candidate.id.clone();
src/strand/pluck.rs-544-                        candidates.retain(|b| b.id != excluded_id);
src/strand/pluck.rs-545-                        if candidates.is_empty() {
--
src/strand/pluck.rs-551-                        // (Skip the rest of the split trigger logic for this iteration.)
src/strand/pluck.rs-552-                        return self.evaluate(store, exclusions).await;
src/strand/pluck.rs-553-                    }
src/strand/pluck.rs-554-
src/strand/pluck.rs-555-                    tracing::info!(
src/strand/pluck.rs-556-                        bead_id = %first_candidate.id,
src/strand/pluck.rs:557:                        failure_count = failure_count,
src/strand/pluck.rs-558-                        threshold = self.split_after_failures,
src/strand/pluck.rs-559-                        "Split threshold reached, returning Split instruction"
src/strand/pluck.rs-560-                    );
src/strand/pluck.rs:561:                    return StrandResult::Split(Box::new(first_candidate.clone()), failure_count);
src/strand/pluck.rs-562-                }
src/strand/pluck.rs-563-            }
src/strand/pluck.rs-564-        } else {
src/strand/pluck.rs-565-            tracing::debug!("Split trigger disabled (threshold = 0)");
src/strand/pluck.rs-566-        }
src/strand/pluck.rs-567-
--
src/strand/pluck.rs-764-        }
src/strand/pluck.rs-765-
src/strand/pluck.rs-766-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<ClaimResult> {
src/strand/pluck.rs-767-            anyhow::bail!("not implemented")
src/strand/pluck.rs-768-        }
src/strand/pluck.rs-769-
src/strand/pluck.rs:770:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/pluck.rs-771-            Ok(())
src/strand/pluck.rs-772-        }
src/strand/pluck.rs-773-
src/strand/pluck.rs-774-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/pluck.rs-775-            Ok(())
src/strand/pluck.rs-776-        }
--
src/strand/pluck.rs-860-        }
src/strand/pluck.rs-861-
src/strand/pluck.rs-862-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<ClaimResult> {
src/strand/pluck.rs-863-            anyhow::bail!("not implemented")
src/strand/pluck.rs-864-        }
src/strand/pluck.rs-865-
src/strand/pluck.rs:866:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/pluck.rs-867-            Ok(())
src/strand/pluck.rs-868-        }
src/strand/pluck.rs-869-
src/strand/pluck.rs-870-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/pluck.rs-871-            Ok(())
src/strand/pluck.rs-872-        }
--
src/strand/pluck.rs-946-        }
src/strand/pluck.rs-947-
src/strand/pluck.rs-948-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<ClaimResult> {
src/strand/pluck.rs-949-            anyhow::bail!("store connection failed")
src/strand/pluck.rs-950-        }
src/strand/pluck.rs-951-
src/strand/pluck.rs:952:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/pluck.rs-953-            anyhow::bail!("store connection failed")
src/strand/pluck.rs-954-        }
src/strand/pluck.rs-955-
src/strand/pluck.rs-956-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/pluck.rs-957-            anyhow::bail!("store connection failed")
src/strand/pluck.rs-958-        }
--
src/strand/pluck.rs-1113-            }
src/strand/pluck.rs-1114-            other => panic!("expected BeadFound, got: {other:?}"),
src/strand/pluck.rs-1115-        }
src/strand/pluck.rs-1116-    }
src/strand/pluck.rs-1117-
src/strand/pluck.rs-1118-    #[tokio::test]
src/strand/pluck.rs:1119:    async fn candidates_sorted_by_failure_count_within_same_priority() {
src/strand/pluck.rs-1120-        // ADR-012: a struggling bead must not monopolize slot 1 forever just
src/strand/pluck.rs-1121-        // because it's the oldest ready bead at its priority. Both beads
src/strand/pluck.rs-1122-        // share priority and created_at (make_bead_with_labels hardcodes the
src/strand/pluck.rs-1123-        // latter) so only the failure-count component of the sort key can
src/strand/pluck.rs-1124-        // explain the ordering.
src/strand/pluck.rs-1125-        let store = MemoryStore {
--
src/strand/pluck.rs-1143-            }
src/strand/pluck.rs-1144-            other => panic!("expected BeadFound, got: {other:?}"),
src/strand/pluck.rs-1145-        }
src/strand/pluck.rs-1146-    }
src/strand/pluck.rs-1147-
src/strand/pluck.rs-1148-    #[tokio::test]
src/strand/pluck.rs:1149:    async fn priority_still_wins_over_failure_count() {
src/strand/pluck.rs:1150:        // failure_count is the second sort key, not the first — a healthy
src/strand/pluck.rs-1151-        // low-priority-number... i.e. HIGHER-priority (lower number = more
src/strand/pluck.rs-1152-        // urgent) struggling bead must still be preferred over a healthy
src/strand/pluck.rs-1153-        // bead at a less urgent priority.
src/strand/pluck.rs-1154-        let store = MemoryStore {
src/strand/pluck.rs-1155-            beads: vec![
src/strand/pluck.rs-1156-                // failure-count:2 stays below the default split_after_failures
src/strand/pluck.rs-1157-                // threshold (3) — this test is about sort order, not the
src/strand/pluck.rs:1158:                // separate split-trigger path (see split_triggered_when_failure_count_exceeds_threshold).
src/strand/pluck.rs-1159-                make_bead_with_labels("urgent-but-struggling", 1, vec!["failure-count:2"]),
src/strand/pluck.rs-1160-                make_bead_with_labels("healthy-but-low-priority", 2, vec![]),
src/strand/pluck.rs-1161-            ],
src/strand/pluck.rs-1162-        };
src/strand/pluck.rs-1163-
src/strand/pluck.rs-1164-        let strand = PluckStrand::new(vec![], Telemetry::new("test-worker".to_string()));
--
src/strand/pluck.rs-1404-
src/strand/pluck.rs-1405-    // ──────────────────────────────────────────────────────────────────────────
src/strand/pluck.rs-1406-    // Split trigger tests
src/strand/pluck.rs-1407-    // ──────────────────────────────────────────────────────────────────────────
src/strand/pluck.rs-1408-
src/strand/pluck.rs-1409-    #[tokio::test]
src/strand/pluck.rs:1410:    async fn split_triggered_when_failure_count_exceeds_threshold() {
src/strand/pluck.rs-1411-        let bead_with_failures = make_bead_with_labels("failing-bead", 1, vec!["failure-count:3"]);
src/strand/pluck.rs-1412-        let store = MemoryStore {
src/strand/pluck.rs-1413-            beads: vec![bead_with_failures],
src/strand/pluck.rs-1414-        };
src/strand/pluck.rs-1415-
src/strand/pluck.rs-1416-        let strand =
src/strand/pluck.rs-1417-            PluckStrand::with_split_threshold(vec![], 3, Telemetry::new("test-worker".to_string()));
src/strand/pluck.rs-1418-        let result = strand.evaluate(&store, &HashSet::new()).await;
src/strand/pluck.rs-1419-
src/strand/pluck.rs-1420-        match result {
src/strand/pluck.rs:1421:            StrandResult::Split(bead, failure_count) => {
src/strand/pluck.rs-1422-                assert_eq!(bead.id.as_ref(), "failing-bead");
src/strand/pluck.rs:1423:                assert_eq!(failure_count, 3);
src/strand/pluck.rs-1424-            }
src/strand/pluck.rs-1425-            other => panic!("expected Split, got: {:?}", other),
src/strand/pluck.rs-1426-        }
src/strand/pluck.rs-1427-    }
src/strand/pluck.rs-1428-
src/strand/pluck.rs-1429-    #[tokio::test]
src/strand/pluck.rs:1430:    async fn split_not_triggered_when_failure_count_below_threshold() {
src/strand/pluck.rs-1431-        let bead_with_failures = make_bead_with_labels("failing-bead", 1, vec!["failure-count:2"]);
src/strand/pluck.rs-1432-        let store = MemoryStore {
src/strand/pluck.rs-1433-            beads: vec![bead_with_failures],
src/strand/pluck.rs-1434-        };
src/strand/pluck.rs-1435-
src/strand/pluck.rs-1436-        let strand =
--
src/strand/pluck.rs-1462-            }
src/strand/pluck.rs-1463-            other => panic!("expected BeadFound, got: {:?}", other),
src/strand/pluck.rs-1464-        }
src/strand/pluck.rs-1465-    }
src/strand/pluck.rs-1466-
src/strand/pluck.rs-1467-    #[test]
src/strand/pluck.rs:1468:    fn extract_failure_count_returns_max_when_multiple_labels() {
src/strand/pluck.rs-1469-        let bead =
src/strand/pluck.rs-1470-            make_bead_with_labels("multi-fail", 1, vec!["failure-count:1", "failure-count:5"]);
src/strand/pluck.rs:1471:        assert_eq!(PluckStrand::extract_failure_count(&bead), 5);
src/strand/pluck.rs-1472-    }
src/strand/pluck.rs-1473-
src/strand/pluck.rs-1474-    #[test]
src/strand/pluck.rs:1475:    fn extract_failure_count_returns_zero_when_no_label() {
src/strand/pluck.rs-1476-        let bead = make_bead("normal", 1, "2026-01-01 00:00:00");
src/strand/pluck.rs:1477:        assert_eq!(PluckStrand::extract_failure_count(&bead), 0);
src/strand/pluck.rs-1478-    }
src/strand/pluck.rs-1479-
src/strand/pluck.rs-1480-    // ──────────────────────────────────────────────────────────────────────────
src/strand/pluck.rs-1481-    // NEEDLE-internal config filter tests (ADR-002 Phase 6.1)
src/strand/pluck.rs-1482-    // ──────────────────────────────────────────────────────────────────────────
src/strand/pluck.rs-1483-
--
src/strand/pluck.rs-1561-        let strand =
src/strand/pluck.rs-1562-            PluckStrand::with_split_threshold(vec![], 3, Telemetry::new("test-worker".to_string()));
src/strand/pluck.rs-1563-        let result = strand.evaluate(&store, &HashSet::new()).await;
src/strand/pluck.rs-1564-
src/strand/pluck.rs-1565-        // Should trigger split normally for non-NEEDLE-internal beads
src/strand/pluck.rs-1566-        match result {
src/strand/pluck.rs:1567:            StrandResult::Split(bead, failure_count) => {
src/strand/pluck.rs-1568-                assert_eq!(bead.id.as_ref(), "Add authentication endpoint");
src/strand/pluck.rs:1569:                assert_eq!(failure_count, 3);
src/strand/pluck.rs-1570-            }
src/strand/pluck.rs-1571-            other => panic!("expected Split for normal failing bead, got: {:?}", other),
src/strand/pluck.rs-1572-        }
src/strand/pluck.rs-1573-    }
src/strand/pluck.rs-1574-
src/strand/pluck.rs-1575-    // ──────────────────────────────────────────────────────────────────────────
--
src/commit_hook.rs-203-fn trailer_lock_path(workspace: &Path) -> PathBuf {
src/commit_hook.rs-204-    workspace.join(".git").join("needle-trailer.lock")
src/commit_hook.rs-205-}
src/commit_hook.rs-206-
src/commit_hook.rs-207-/// Acquire an exclusive flock with a timeout.
src/commit_hook.rs-208-///
src/commit_hook.rs:209:/// Returns the locked file on success. The lock is released when the
src/commit_hook.rs:210:/// file is dropped (flock auto-releases on close).
src/commit_hook.rs-211-async fn acquire_flock(lock_path: &Path) -> Result<std::fs::File> {
src/commit_hook.rs-212-    if let Some(parent) = lock_path.parent() {
src/commit_hook.rs-213-        std::fs::create_dir_all(parent)?;
src/commit_hook.rs-214-    }
src/commit_hook.rs-215-
src/commit_hook.rs-216-    let file = std::fs::OpenOptions::new()
--
src/strand/pulse.rs-543-        }
src/strand/pulse.rs-544-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<ClaimResult> {
src/strand/pulse.rs-545-            Ok(ClaimResult::NotClaimable {
src/strand/pulse.rs-546-                reason: "mock".to_string(),
src/strand/pulse.rs-547-            })
src/strand/pulse.rs-548-        }
src/strand/pulse.rs:549:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/pulse.rs-550-            Ok(())
src/strand/pulse.rs-551-        }
src/strand/pulse.rs-552-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/pulse.rs-553-            Ok(())
src/strand/pulse.rs-554-        }
src/strand/pulse.rs-555-        async fn flush(&self) -> Result<()> {
--
src/validation/mod.rs-1-//! Validation gates: pre-closure verification.
src/validation/mod.rs-2-//!
src/validation/mod.rs-3-//! After an agent exits successfully (code 0), validation gates run to verify
src/validation/mod.rs-4-//! the work before accepting bead closure. If any gate fails, the bead is
src/validation/mod.rs:5://! released instead of having its closure accepted.
src/validation/mod.rs-6-//!
src/validation/mod.rs-7-//! # Pluggable Gate System
src/validation/mod.rs-8-//!
src/validation/mod.rs-9-//! Gates implement the [`Gate`] trait and are registered in the [`GateRegistry`].
src/validation/mod.rs-10-//! Built-in gate types:
src/validation/mod.rs-11-//! - `command`: Runs shell commands in the workspace directory
--
src/validation/mod.rs-154-// Gate Trait
src/validation/mod.rs-155-// ──────────────────────────────────────────────────────────────────────────────
src/validation/mod.rs-156-
src/validation/mod.rs-157-/// A validation gate that can check bead work before accepting closure.
src/validation/mod.rs-158-///
src/validation/mod.rs-159-/// Gates are run after an agent exits successfully (code 0). If any gate
src/validation/mod.rs:160:/// returns `GateResult::Fail`, the bead is released instead of having its
src/validation/mod.rs-161-/// closure accepted.
src/validation/mod.rs-162-///
src/validation/mod.rs-163-/// `validate` is async so a slow gate is a genuine `.await` yield point:
src/validation/mod.rs-164-/// `OutcomeHandler::handle_with_cancellation`'s `tokio::time::timeout` can
src/validation/mod.rs-165-/// only preempt a future at such a point (see bf-3saat) — a synchronous,
src/validation/mod.rs-166-/// blocking implementation would let the configured
--
src/strand/knot.rs-326-        }
src/strand/knot.rs-327-
src/strand/knot.rs-328-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/strand/knot.rs-329-            anyhow::bail!("not implemented")
src/strand/knot.rs-330-        }
src/strand/knot.rs-331-
src/strand/knot.rs:332:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/knot.rs-333-            Ok(())
src/strand/knot.rs-334-        }
src/strand/knot.rs-335-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/knot.rs-336-            Ok(())
src/strand/knot.rs-337-        }
src/strand/knot.rs-338-        async fn flush(&self) -> Result<()> {
--
src/strand/knot.rs-405-        }
src/strand/knot.rs-406-
src/strand/knot.rs-407-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/strand/knot.rs-408-            anyhow::bail!("store connection failed")
src/strand/knot.rs-409-        }
src/strand/knot.rs-410-
src/strand/knot.rs:411:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/knot.rs-412-            anyhow::bail!("store connection failed")
src/strand/knot.rs-413-        }
src/strand/knot.rs-414-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/knot.rs-415-            anyhow::bail!("store connection failed")
src/strand/knot.rs-416-        }
src/strand/knot.rs-417-        async fn flush(&self) -> Result<()> {
--
src/strand/knot.rs-853-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<ClaimResult> {
src/strand/knot.rs-854-            anyhow::bail!("no .beads/ directory")
src/strand/knot.rs-855-        }
src/strand/knot.rs-856-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/strand/knot.rs-857-            anyhow::bail!("no .beads/ directory")
src/strand/knot.rs-858-        }
src/strand/knot.rs:859:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/knot.rs-860-            anyhow::bail!("no .beads/ directory")
src/strand/knot.rs-861-        }
src/strand/knot.rs-862-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/knot.rs-863-            anyhow::bail!("no .beads/ directory")
src/strand/knot.rs-864-        }
src/strand/knot.rs-865-        async fn flush(&self) -> Result<()> {
--
src/resolve/mod.rs-13-//! 5. Parser validates the response exhaustively
src/resolve/mod.rs-14-//! 6. Invalid/incomplete/unknown decisions fall back to safe defaults
src/resolve/mod.rs-15-//!
src/resolve/mod.rs-16-//! ## Decision Types
src/resolve/mod.rs-17-//!
src/resolve/mod.rs-18-//! - **Complete**: Task succeeded, bead should be closed
src/resolve/mod.rs:19://! - **Retry**: Temporary failure, release for immediate retry
src/resolve/mod.rs-20-//! - **Blocked**: External dependency, cannot proceed without human input
src/resolve/mod.rs-21-//! - **Split**: Task decomposes into multiple independent subtasks
src/resolve/mod.rs-22-//!
src/resolve/mod.rs-23-//! ## Safety Guarantees
src/resolve/mod.rs-24-//!
src/resolve/mod.rs-25-//! - Resolver NEVER implements changes or mutates beads
--
src/resolve/mod.rs-57-        /// Evidence that the task succeeded (git commits, tests passed, etc.)
src/resolve/mod.rs-58-        evidence: String,
src/resolve/mod.rs-59-        /// Human-readable summary of what was accomplished.
src/resolve/mod.rs-60-        summary: String,
src/resolve/mod.rs-61-    },
src/resolve/mod.rs-62-
src/resolve/mod.rs:63:    /// Temporary failure — release bead for immediate retry.
src/resolve/mod.rs-64-    Retry {
src/resolve/mod.rs-65-        /// Why the attempt failed (error message, diagnostic info).
src/resolve/mod.rs-66-        reason: String,
src/resolve/mod.rs-67-        /// Suggested retry strategy (same approach, different timeout, etc.).
src/resolve/mod.rs-68-        strategy: RetryStrategy,
src/resolve/mod.rs-69-    },
--
src/resolve/mod.rs-219-#[serde(rename_all = "snake_case")]
src/resolve/mod.rs-220-pub enum BlockSeverity {
src/resolve/mod.rs-221-    /// Cosmetic issue, nice-to-have enhancement, or low-priority cleanup.
src/resolve/mod.rs-222-    Low,
src/resolve/mod.rs-223-    /// Feature complete but blocked on polish, tests, or documentation.
src/resolve/mod.rs-224-    Medium,
src/resolve/mod.rs:225:    /// Critical blocker preventing core functionality or release.
src/resolve/mod.rs-226-    High,
src/resolve/mod.rs-227-}
src/resolve/mod.rs-228-
src/resolve/mod.rs-229-impl BlockSeverity {
src/resolve/mod.rs-230-    fn validate(&self) -> Result<()> {
src/resolve/mod.rs-231-        // All variants are valid by construction
--
src/strand/splice.rs-599-                _ => {}
src/strand/splice.rs-600-            }
src/strand/splice.rs-601-        }
src/strand/splice.rs-602-
src/strand/splice.rs-603-        // Check for beads with the problematic pattern:
src/strand/splice.rs-604-        // - High claim_succeeded count (indicating repeated claims)
src/strand/splice.rs:605:        // - High orphaned count (indicating the bead is being released without closure)
src/strand/splice.rs-606-        // - High agent_completed count (agents are succeeding)
src/strand/splice.rs-607-        // - NO bead.completed event (the bead never actually closes)
src/strand/splice.rs-608-        //
src/strand/splice.rs-609-        // The bf-34xw9 incident showed: 41 claim.succeeded, 25 orphaned, 42 agent.completed, 1 bead.completed
src/strand/splice.rs-610-        // We need thresholds that catch this pattern. Using claim_churn_threshold (20) as baseline.
src/strand/splice.rs-611-
--
src/strand/splice.rs-1169-            &self,
src/strand/splice.rs-1170-            _id: &crate::types::BeadId,
src/strand/splice.rs-1171-            _actor: &str,
src/strand/splice.rs-1172-        ) -> Result<crate::types::ClaimResult> {
src/strand/splice.rs-1173-            anyhow::bail!("not implemented")
src/strand/splice.rs-1174-        }
src/strand/splice.rs:1175:        async fn release(&self, _id: &crate::types::BeadId) -> Result<()> {
src/strand/splice.rs-1176-            Ok(())
src/strand/splice.rs-1177-        }
src/strand/splice.rs-1178-        async fn block(&self, _id: &crate::types::BeadId) -> Result<()> {
src/strand/splice.rs-1179-            Ok(())
src/strand/splice.rs-1180-        }
src/strand/splice.rs-1181-        async fn flush(&self) -> Result<()> {
--
src/strand/reflect.rs-1556-                &self,
src/strand/reflect.rs-1557-                _id: &crate::types::BeadId,
src/strand/reflect.rs-1558-                _a: &str,
src/strand/reflect.rs-1559-            ) -> anyhow::Result<crate::types::ClaimResult> {
src/strand/reflect.rs-1560-                anyhow::bail!("not impl")
src/strand/reflect.rs-1561-            }
src/strand/reflect.rs:1562:            async fn release(&self, _id: &crate::types::BeadId) -> anyhow::Result<()> {
src/strand/reflect.rs-1563-                Ok(())
src/strand/reflect.rs-1564-            }
src/strand/reflect.rs-1565-            async fn block(&self, _id: &crate::types::BeadId) -> anyhow::Result<()> {
src/strand/reflect.rs-1566-                Ok(())
src/strand/reflect.rs-1567-            }
src/strand/reflect.rs-1568-            async fn flush(&self) -> anyhow::Result<()> {
--
src/strand/explore.rs-144-    /// Wrapped in Mutex for interior mutability during re-discovery.
src/strand/explore.rs-145-    workspaces: std::sync::Mutex<Vec<PathBuf>>,
src/strand/explore.rs-146-    /// Home workspace path — excluded from exploration.
src/strand/explore.rs-147-    home_workspace: PathBuf,
src/strand/explore.rs-148-    /// Worker registry for orphan detection.
src/strand/explore.rs-149-    registry: Registry,
src/strand/explore.rs:150:    /// Telemetry emitter for orphan release events.
src/strand/explore.rs-151-    telemetry: Telemetry,
src/strand/explore.rs-152-    /// Fully-qualified worker identity (`{adapter}-{worker_id}`).
src/strand/explore.rs-153-    qualified_id: String,
src/strand/explore.rs-154-    /// Store factory for creating workspace stores.
src/strand/explore.rs-155-    store_factory: Arc<dyn StoreFactory>,
src/strand/explore.rs-156-    /// Cycles since last workspace re-discovery (for periodic refresh).
--
src/strand/explore.rs-507-        let workspaces = self.workspaces.lock().unwrap();
src/strand/explore.rs-508-        if workspaces.is_empty() {
src/strand/explore.rs-509-            return vec![];
src/strand/explore.rs-510-        }
src/strand/explore.rs-511-
src/strand/explore.rs-512-        let n = workspaces.len();
src/strand/explore.rs:513:        // Defensive: `compute_start_index()` released its lock before we
src/strand/explore.rs-514-        // re-acquired ours above, so guard against the (currently
src/strand/explore.rs-515-        // never-concurrent, but cheap to guard) case where the count
src/strand/explore.rs-516-        // changed in between.
src/strand/explore.rs-517-        let start = start % n;
src/strand/explore.rs-518-        let mut rotated = Vec::with_capacity(n);
src/strand/explore.rs-519-
--
src/strand/explore.rs-728-
src/strand/explore.rs-729-                    if filtered_count > 0 {
src/strand/explore.rs-730-                        exclusion_reasons.insert(format!("filtered_{}", filtered_count));
src/strand/explore.rs-731-                    }
src/strand/explore.rs-732-
src/strand/explore.rs-733-                    if candidates.is_empty() {
src/strand/explore.rs:734:                        // No ready candidates. Run cross-workspace mend to release
src/strand/explore.rs-735-                        // orphaned in-progress beads, then re-query.
src/strand/explore.rs-736-                        tracing::debug!(
src/strand/explore.rs-737-                            workspace = %workspace.display(),
src/strand/explore.rs-738-                            "no ready candidates, running cross-workspace mend"
src/strand/explore.rs-739-                        );
src/strand/explore.rs-740-                        exclusion_reasons.insert("no_ready_candidates".to_string());
--
src/strand/explore.rs-773-                            &self.registry,
src/strand/explore.rs-774-                            &self.telemetry,
src/strand/explore.rs-775-                            &self.qualified_id,
src/strand/explore.rs-776-                        )
src/strand/explore.rs-777-                        .await
src/strand/explore.rs-778-                        {
src/strand/explore.rs:779:                            Ok(released) if released > 0 => {
src/strand/explore.rs-780-                                tracing::info!(
src/strand/explore.rs-781-                                    workspace = %workspace.display(),
src/strand/explore.rs:782:                                    released,
src/strand/explore.rs:783:                                    "cross-workspace mend released orphans, re-querying"
src/strand/explore.rs-784-                                );
src/strand/explore.rs-785-
src/strand/explore.rs-786-                                // Re-query ready after cleanup.
src/strand/explore.rs-787-                                match remote_store.ready(&filters).await {
src/strand/explore.rs-788-                                    Ok(mut retry_candidates) => {
src/strand/explore.rs-789-                                        // Apply the same defensive filtering.
--
src/strand/explore.rs-823-                                            );
src/strand/explore.rs-824-
src/strand/explore.rs-825-                                            // Accumulate instead of returning early (bf-4df1e):
src/strand/explore.rs-826-                                            // keep scanning the remaining workspaces this cycle.
src/strand/explore.rs-827-                                            all_candidates.append(&mut retry_candidates);
src/strand/explore.rs-828-                                        } else {
src/strand/explore.rs:829:                                            // Orphans were released but re-query found no candidates.
src/strand/explore.rs-830-                                            // Do NOT return WorkCreated — the beads will become available
src/strand/explore.rs-831-                                            // in the next natural selection cycle when Pluck re-scans the
src/strand/explore.rs-832-                                            // ready queue. Returning WorkCreated here causes restart loops
src/strand/explore.rs:833:                                            // when released beads don't pass filters (e.g., still blocked).
src/strand/explore.rs-834-                                            tracing::info!(
src/strand/explore.rs-835-                                                workspace = %workspace.display(),
src/strand/explore.rs:836:                                                released,
src/strand/explore.rs:837:                                                "cross-workspace mend released orphans but re-query found no candidates (beads may not pass filters), continuing to next workspace"
src/strand/explore.rs-838-                                            );
src/strand/explore.rs-839-                                            exclusion_reasons.insert(
src/strand/explore.rs:840:                                                "orphans_released_no_candidates".to_string(),
src/strand/explore.rs-841-                                            );
src/strand/explore.rs-842-                                        }
src/strand/explore.rs-843-                                    }
src/strand/explore.rs-844-                                    Err(e) => {
src/strand/explore.rs-845-                                        tracing::warn!(
src/strand/explore.rs-846-                                            workspace = %workspace.display(),
--
src/strand/explore.rs-849-                                        );
src/strand/explore.rs-850-                                        exclusion_reasons.insert(format!("requery_error: {}", e));
src/strand/explore.rs-851-                                    }
src/strand/explore.rs-852-                                }
src/strand/explore.rs-853-                            }
src/strand/explore.rs-854-                            Ok(_) => {
src/strand/explore.rs:855:                                // No orphans released, workspace is truly empty.
src/strand/explore.rs-856-                                tracing::debug!(
src/strand/explore.rs-857-                                    workspace = %workspace.display(),
src/strand/explore.rs-858-                                    "cross-workspace mend found no orphans"
src/strand/explore.rs-859-                                );
src/strand/explore.rs-860-                                exclusion_reasons.insert("no_orphans".to_string());
src/strand/explore.rs-861-                            }
--
src/strand/explore.rs-1049-        }
src/strand/explore.rs-1050-
src/strand/explore.rs-1051-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/strand/explore.rs-1052-            anyhow::bail!("not implemented")
src/strand/explore.rs-1053-        }
src/strand/explore.rs-1054-
src/strand/explore.rs:1055:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/explore.rs-1056-            Ok(())
src/strand/explore.rs-1057-        }
src/strand/explore.rs-1058-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/explore.rs-1059-            Ok(())
src/strand/explore.rs-1060-        }
src/strand/explore.rs-1061-        async fn flush(&self) -> Result<()> {
--
src/strand/explore.rs-1171-        }
src/strand/explore.rs-1172-
src/strand/explore.rs-1173-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/strand/explore.rs-1174-            anyhow::bail!("not implemented")
src/strand/explore.rs-1175-        }
src/strand/explore.rs-1176-
src/strand/explore.rs:1177:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/explore.rs-1178-            Ok(())
src/strand/explore.rs-1179-        }
src/strand/explore.rs-1180-
src/strand/explore.rs-1181-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/explore.rs-1182-            Ok(())
src/strand/explore.rs-1183-        }
--
src/strand/explore.rs-2562-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<ClaimResult> {
src/strand/explore.rs-2563-            anyhow::bail!("not implemented")
src/strand/explore.rs-2564-        }
src/strand/explore.rs-2565-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/strand/explore.rs-2566-            anyhow::bail!("not implemented")
src/strand/explore.rs-2567-        }
src/strand/explore.rs:2568:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/explore.rs-2569-            Ok(())
src/strand/explore.rs-2570-        }
src/strand/explore.rs-2571-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/explore.rs-2572-            Ok(())
src/strand/explore.rs-2573-        }
src/strand/explore.rs-2574-        async fn flush(&self) -> Result<()> {
--
src/strand/explore.rs-2690-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<ClaimResult> {
src/strand/explore.rs-2691-            anyhow::bail!("not implemented")
src/strand/explore.rs-2692-        }
src/strand/explore.rs-2693-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/strand/explore.rs-2694-            anyhow::bail!("not implemented")
src/strand/explore.rs-2695-        }
src/strand/explore.rs:2696:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/explore.rs-2697-            Ok(())
src/strand/explore.rs-2698-        }
src/strand/explore.rs-2699-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/explore.rs-2700-            Ok(())
src/strand/explore.rs-2701-        }
src/strand/explore.rs-2702-        async fn flush(&self) -> Result<()> {
--
src/strand/explore.rs-2798-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<ClaimResult> {
src/strand/explore.rs-2799-            anyhow::bail!("not implemented")
src/strand/explore.rs-2800-        }
src/strand/explore.rs-2801-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/strand/explore.rs-2802-            anyhow::bail!("not implemented")
src/strand/explore.rs-2803-        }
src/strand/explore.rs:2804:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/explore.rs-2805-            Ok(())
src/strand/explore.rs-2806-        }
src/strand/explore.rs-2807-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/explore.rs-2808-            Ok(())
src/strand/explore.rs-2809-        }
src/strand/explore.rs-2810-        async fn flush(&self) -> Result<()> {
--
src/strand/explore.rs-2946-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<ClaimResult> {
src/strand/explore.rs-2947-            anyhow::bail!("not implemented")
src/strand/explore.rs-2948-        }
src/strand/explore.rs-2949-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/strand/explore.rs-2950-            anyhow::bail!("not implemented")
src/strand/explore.rs-2951-        }
src/strand/explore.rs:2952:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/explore.rs-2953-            Ok(())
src/strand/explore.rs-2954-        }
src/strand/explore.rs-2955-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/explore.rs-2956-            Ok(())
src/strand/explore.rs-2957-        }
src/strand/explore.rs-2958-        async fn flush(&self) -> Result<()> {
--
src/strand/explore.rs-3040-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<ClaimResult> {
src/strand/explore.rs-3041-            anyhow::bail!("not implemented")
src/strand/explore.rs-3042-        }
src/strand/explore.rs-3043-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/strand/explore.rs-3044-            anyhow::bail!("not implemented")
src/strand/explore.rs-3045-        }
src/strand/explore.rs:3046:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/explore.rs-3047-            Ok(())
src/strand/explore.rs-3048-        }
src/strand/explore.rs-3049-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/explore.rs-3050-            Ok(())
src/strand/explore.rs-3051-        }
src/strand/explore.rs-3052-        async fn flush(&self) -> Result<()> {
--
src/telemetry/mod.rs-246-    },
src/telemetry/mod.rs-247-    BeadOrphaned {
src/telemetry/mod.rs-248-        bead_id: BeadId,
src/telemetry/mod.rs-249-    },
src/telemetry/mod.rs-250-    BeadQuarantined {
src/telemetry/mod.rs-251-        bead_id: BeadId,
src/telemetry/mod.rs:252:        failure_count: u32,
src/telemetry/mod.rs-253-        threshold: u32,
src/telemetry/mod.rs-254-    },
src/telemetry/mod.rs-255-
src/telemetry/mod.rs-256-    // ── Agent dispatch ──
src/telemetry/mod.rs-257-    DispatchStarted {
src/telemetry/mod.rs-258-        bead_id: BeadId,
--
src/telemetry/mod.rs-358-    MendDbRepaired {
src/telemetry/mod.rs-359-        warnings: u32,
src/telemetry/mod.rs-360-        fixed: u32,
src/telemetry/mod.rs-361-    },
src/telemetry/mod.rs-362-    MendDbRebuilt,
src/telemetry/mod.rs-363-    MendCycleSummary {
src/telemetry/mod.rs:364:        beads_released: u32,
src/telemetry/mod.rs-365-        locks_removed: u32,
src/telemetry/mod.rs-366-        deps_cleaned: u32,
src/telemetry/mod.rs-367-        db_repaired: bool,
src/telemetry/mod.rs-368-        db_rebuilt: bool,
src/telemetry/mod.rs-369-        agent_logs_cleaned: u32,
src/telemetry/mod.rs-370-        zero_activity_logs_cleaned: u32,
--
src/telemetry/mod.rs-656-        /// Current version
src/telemetry/mod.rs-657-        current_version: String,
src/telemetry/mod.rs-658-        /// Latest available version
src/telemetry/mod.rs-659-        latest_version: String,
src/telemetry/mod.rs-660-        /// Whether an update is available
src/telemetry/mod.rs-661-        update_available: bool,
src/telemetry/mod.rs:662:        /// Whether release notes were included
src/telemetry/mod.rs:663:        has_release_notes: bool,
src/telemetry/mod.rs-664-    },
src/telemetry/mod.rs-665-    /// Emitted when an upgrade check fails
src/telemetry/mod.rs-666-    UpgradeCheckFailed {
src/telemetry/mod.rs-667-        /// Source of the check
src/telemetry/mod.rs-668-        source: String,
src/telemetry/mod.rs-669-        /// Error message describing what went wrong
--
src/telemetry/mod.rs-894-            EventKind::ClaimSuccess { .. } => "bead.claim.succeeded",
src/telemetry/mod.rs-895-            EventKind::ClaimRaceLost { .. } => "bead.claim.race_lost",
src/telemetry/mod.rs-896-            EventKind::ClaimRaceLostSkipped { .. } => "bead.claim.race_lost_skipped",
src/telemetry/mod.rs-897-            EventKind::ClaimFailed { .. } => "bead.claim.failed",
src/telemetry/mod.rs-898-            EventKind::ClaimErrorThreshold { .. } => "bead.claim.error_threshold",
src/telemetry/mod.rs-899-            EventKind::ClaimVerifyFailed { .. } => "bead.claim.verify_failed",
src/telemetry/mod.rs:900:            EventKind::BeadReleased { .. } => "bead.released",
src/telemetry/mod.rs:901:            EventKind::BeadReleaseFailed { .. } => "bead.release.failed",
src/telemetry/mod.rs-902-            EventKind::BeadCompleted { .. } => "bead.completed",
src/telemetry/mod.rs-903-            EventKind::BeadOrphaned { .. } => "bead.orphaned",
src/telemetry/mod.rs-904-            EventKind::BeadQuarantined { .. } => "bead.quarantined",
src/telemetry/mod.rs-905-            EventKind::DispatchStarted { .. } => "agent.dispatched",
src/telemetry/mod.rs-906-            EventKind::DispatchCompleted { .. } => "agent.completed",
src/telemetry/mod.rs-907-            EventKind::AgentTimeout { .. } => "agent.timeout",
--
src/telemetry/mod.rs-926-            EventKind::MendTraceCleanup { .. } => "mend.trace_cleanup",
src/telemetry/mod.rs-927-            EventKind::MendLearningCleanup { .. } => "mend.learning_cleanup",
src/telemetry/mod.rs-928-            EventKind::MendIdleWorkerFlagged { .. } => "mend.idle_worker_flagged",
src/telemetry/mod.rs-929-            EventKind::MendWorkerDeregistered { .. } => "mend.worker_deregistered",
src/telemetry/mod.rs-930-            EventKind::MendOrphanedHeartbeatRemoved { .. } => "mend.orphaned_heartbeat_removed",
src/telemetry/mod.rs-931-            EventKind::MendDependencyRemoved { .. } => "mend.dependency_removed",
src/telemetry/mod.rs:932:            EventKind::MendBeadReleaseFailed { .. } => "mend.bead_release_failed",
src/telemetry/mod.rs-933-            EventKind::MendDependencyCleanupFailed { .. } => "mend.dependency_cleanup_failed",
src/telemetry/mod.rs-934-            EventKind::MendLockRemoveFailed { .. } => "mend.lock_remove_failed",
src/telemetry/mod.rs-935-            EventKind::MendRateLimitCleaned { .. } => "mend.rate_limit_cleaned",
src/telemetry/mod.rs-936-            EventKind::MendRateLimitProviderRemoved { .. } => "mend.rate_limit_provider_removed",
src/telemetry/mod.rs-937-            EventKind::MendRateLimitProviderReset { .. } => "mend.rate_limit_provider_reset",
src/telemetry/mod.rs-938-            EventKind::MendZeroActivityLogCleaned { .. } => "mend.zero_activity_log_cleaned",
--
src/telemetry/mod.rs-1538-            }
src/telemetry/mod.rs-1539-            EventKind::MendDbRepaired { warnings, fixed } => {
src/telemetry/mod.rs-1540-                serde_json::json!({ "warnings": warnings, "fixed": fixed })
src/telemetry/mod.rs-1541-            }
src/telemetry/mod.rs-1542-            EventKind::MendDbRebuilt => serde_json::json!({}),
src/telemetry/mod.rs-1543-            EventKind::MendCycleSummary {
src/telemetry/mod.rs:1544:                beads_released,
src/telemetry/mod.rs-1545-                locks_removed,
src/telemetry/mod.rs-1546-                deps_cleaned,
src/telemetry/mod.rs-1547-                db_repaired,
src/telemetry/mod.rs-1548-                db_rebuilt,
src/telemetry/mod.rs-1549-                agent_logs_cleaned,
src/telemetry/mod.rs-1550-                zero_activity_logs_cleaned,
--
src/telemetry/mod.rs-1553-                workers_deregistered,
src/telemetry/mod.rs-1554-                idle_workers_flagged,
src/telemetry/mod.rs-1555-                rate_limits_cleaned,
src/telemetry/mod.rs-1556-                assignees_cleared,
src/telemetry/mod.rs-1557-            } => {
src/telemetry/mod.rs-1558-                serde_json::json!({
src/telemetry/mod.rs:1559:                    "beads_released": beads_released,
src/telemetry/mod.rs-1560-                    "locks_removed": locks_removed,
src/telemetry/mod.rs-1561-                    "deps_cleaned": deps_cleaned,
src/telemetry/mod.rs-1562-                    "db_repaired": db_repaired,
src/telemetry/mod.rs-1563-                    "db_rebuilt": db_rebuilt,
src/telemetry/mod.rs-1564-                    "agent_logs_cleaned": agent_logs_cleaned,
src/telemetry/mod.rs-1565-                    "zero_activity_logs_cleaned": zero_activity_logs_cleaned,
--
src/telemetry/mod.rs-1770-            }
src/telemetry/mod.rs-1771-            EventKind::UpgradeCheckCompleted {
src/telemetry/mod.rs-1772-                source,
src/telemetry/mod.rs-1773-                current_version,
src/telemetry/mod.rs-1774-                latest_version,
src/telemetry/mod.rs-1775-                update_available,
src/telemetry/mod.rs:1776:                has_release_notes,
src/telemetry/mod.rs-1777-            } => {
src/telemetry/mod.rs-1778-                serde_json::json!({
src/telemetry/mod.rs-1779-                    "source": source,
src/telemetry/mod.rs-1780-                    "current_version": current_version,
src/telemetry/mod.rs-1781-                    "latest_version": latest_version,
src/telemetry/mod.rs-1782-                    "update_available": update_available,
src/telemetry/mod.rs:1783:                    "has_release_notes": has_release_notes,
src/telemetry/mod.rs-1784-                })
src/telemetry/mod.rs-1785-            }
src/telemetry/mod.rs-1786-            EventKind::UpgradeCheckFailed {
src/telemetry/mod.rs-1787-                source,
src/telemetry/mod.rs-1788-                error_message,
src/telemetry/mod.rs-1789-                error_type,
--
src/telemetry/mod.rs-2285-                "bead_id": bead_id,
src/telemetry/mod.rs-2286-                "assignee": assignee,
src/telemetry/mod.rs-2287-                "error": error,
src/telemetry/mod.rs-2288-            }),
src/telemetry/mod.rs-2289-            EventKind::BeadQuarantined {
src/telemetry/mod.rs-2290-                bead_id,
src/telemetry/mod.rs:2291:                failure_count,
src/telemetry/mod.rs-2292-                threshold,
src/telemetry/mod.rs-2293-            } => serde_json::json!({
src/telemetry/mod.rs-2294-                "bead_id": bead_id,
src/telemetry/mod.rs:2295:                "failure_count": failure_count,
src/telemetry/mod.rs-2296-                "threshold": threshold,
src/telemetry/mod.rs-2297-            }),
src/telemetry/mod.rs-2298-            EventKind::SpawnPathModifiedInPlace {
src/telemetry/mod.rs-2299-                path,
src/telemetry/mod.rs-2300-                old_metadata,
src/telemetry/mod.rs-2301-                new_metadata,
--
src/telemetry/mod.rs-2658-            "strand.skipped" => "STRAND_SKIP",
src/telemetry/mod.rs-2659-            "bead.claim.attempted" => "CLAIMING",
src/telemetry/mod.rs-2660-            "bead.claim.succeeded" => "CLAIMED",
src/telemetry/mod.rs-2661-            "bead.claim.race_lost" => "RACE_LOST",
src/telemetry/mod.rs-2662-            "bead.claim.race_lost_skipped" => "RACE_LOST_SKIP",
src/telemetry/mod.rs-2663-            "bead.claim.failed" => "CLAIM_FAIL",
src/telemetry/mod.rs:2664:            "bead.released" => "RELEASED",
src/telemetry/mod.rs-2665-            "bead.completed" => "COMPLETED",
src/telemetry/mod.rs-2666-            "bead.orphaned" => "ORPHANED",
src/telemetry/mod.rs-2667-            "agent.dispatched" => "DISPATCHED",
src/telemetry/mod.rs-2668-            "agent.completed" => "AGENT_DONE",
src/telemetry/mod.rs-2669-            "outcome.classified" => "OUTCOME",
src/telemetry/mod.rs-2670-            "outcome.handled" => "HANDLED",
--
src/telemetry/mod.rs-2748-            t if t.starts_with("worker.errored") || t.starts_with("budget.stop") => "\x1b[31m", // red
src/telemetry/mod.rs-2749-            t if t.starts_with("bead.claim.succeeded") || t == "bead.completed" => "\x1b[32m", // green
src/telemetry/mod.rs-2750-            t if t.starts_with("agent.") || t.starts_with("outcome.") => "\x1b[36m", // cyan
src/telemetry/mod.rs-2751-            t if t.starts_with("bead.claim.race_lost")
src/telemetry/mod.rs-2752-                || t.starts_with("bead.claim.race_lost_skipped")
src/telemetry/mod.rs-2753-                || t.starts_with("bead.claim.failed")
src/telemetry/mod.rs:2754:                || t == "bead.released"
src/telemetry/mod.rs-2755-                || t == "bead.orphaned" =>
src/telemetry/mod.rs-2756-            {
src/telemetry/mod.rs-2757-                "\x1b[33m"
src/telemetry/mod.rs-2758-            } // yellow
src/telemetry/mod.rs-2759-            t if t.starts_with("budget.warning") || t.starts_with("rate_limit") => "\x1b[33m", // yellow
src/telemetry/mod.rs-2760-            t if t.starts_with("mend.") || t.starts_with("peer.") => "\x1b[35m", // magenta
--
src/strand/mod.rs-52-    pub restart_triggers: Vec<String>,
src/strand/mod.rs-53-    /// All strand evaluations in order, across all waterfall passes.
src/strand/mod.rs-54-    /// Each entry is (strand_name, result, duration_ms).
src/strand/mod.rs-55-    pub strand_evaluations: Vec<StrandEvaluation>,
src/strand/mod.rs-56-    /// If set, this bead should be dispatched with a split prompt instead of
src/strand/mod.rs-57-    /// the normal work prompt. Contains the consecutive failure count.
src/strand/mod.rs:58:    pub split_failure_count: Option<u32>,
src/strand/mod.rs-59-}
src/strand/mod.rs-60-
src/strand/mod.rs-61-pub use explore::ExploreStrand;
src/strand/mod.rs-62-pub use knot::KnotStrand;
src/strand/mod.rs-63-pub use mend::{cleanup_orphaned_in_progress, MendStrand};
src/strand/mod.rs-64-pub use pluck::PluckStrand;
--
src/strand/mod.rs-305-                        )
src/strand/mod.rs-306-                    }
src/strand/mod.rs-307-                    StrandResult::WorkCreated => (strand_results::WORK_CREATED.to_string(), true),
src/strand/mod.rs-308-                    StrandResult::NoWork => (strand_results::NO_WORK.to_string(), true),
src/strand/mod.rs-309-                    StrandResult::Error(_) => (strand_results::ERROR.to_string(), true),
src/strand/mod.rs-310-                    StrandResult::Skipped { reason } => (format!("skipped({})", reason), true),
src/strand/mod.rs:311:                    StrandResult::Split(_, _failure_count) => {
src/strand/mod.rs-312-                        (format!("{}({})", strand_results::BEAD_FOUND, 1), true)
src/strand/mod.rs-313-                    }
src/strand/mod.rs-314-                    StrandResult::FoundButExcluded => ("found_but_excluded".to_string(), true),
src/strand/mod.rs-315-                };
src/strand/mod.rs-316-                tracing::Span::current().record(attrs::NEEDLE_STRAND_RESULT, &result_str);
src/strand/mod.rs-317-                tracing::Span::current().record(attrs::NEEDLE_STRAND_DURATION_MS, elapsed_ms);
--
src/strand/mod.rs-384-                        if let Some(bead) = filtered.into_iter().next() {
src/strand/mod.rs-385-                            return Ok(SelectOutcome {
src/strand/mod.rs-386-                                bead: Some((bead, strand_name.clone())),
src/strand/mod.rs-387-                                waterfall_restarts: restarts,
src/strand/mod.rs-388-                                restart_triggers,
src/strand/mod.rs-389-                                strand_evaluations,
src/strand/mod.rs:390:                                split_failure_count: None,
src/strand/mod.rs-391-                            });
src/strand/mod.rs-392-                        }
src/strand/mod.rs-393-                        continue;
src/strand/mod.rs-394-                    }
src/strand/mod.rs:395:                    StrandResult::Split(bead, failure_count) => {
src/strand/mod.rs-396-                        let bead = *bead;
src/strand/mod.rs-397-                        if let Err(e) =
src/strand/mod.rs-398-                            self.telemetry
src/strand/mod.rs-399-                                .emit(crate::telemetry::EventKind::StrandEvaluated {
src/strand/mod.rs-400-                                    strand_name: strand_name.clone(),
src/strand/mod.rs-401-                                    result: "split".to_string(),
--
src/strand/mod.rs-408-                                "failed to emit strand evaluated telemetry"
src/strand/mod.rs-409-                            );
src/strand/mod.rs-410-                        }
src/strand/mod.rs-411-                        tracing::info!(
src/strand/mod.rs-412-                            strand = %strand_name,
src/strand/mod.rs-413-                            bead_id = %bead.id,
src/strand/mod.rs:414:                            failure_count,
src/strand/mod.rs-415-                            "strand triggered split for bead with excessive failures"
src/strand/mod.rs-416-                        );
src/strand/mod.rs-417-                        return Ok(SelectOutcome {
src/strand/mod.rs-418-                            bead: Some((bead, strand_name.clone())),
src/strand/mod.rs-419-                            waterfall_restarts: restarts,
src/strand/mod.rs-420-                            restart_triggers,
src/strand/mod.rs-421-                            strand_evaluations,
src/strand/mod.rs:422:                            split_failure_count: Some(failure_count),
src/strand/mod.rs-423-                        });
src/strand/mod.rs-424-                    }
src/strand/mod.rs-425-                    StrandResult::WorkCreated => {
src/strand/mod.rs-426-                        if let Err(e) =
src/strand/mod.rs-427-                            self.telemetry
src/strand/mod.rs-428-                                .emit(crate::telemetry::EventKind::StrandEvaluated {
--
src/strand/mod.rs-553-            // All strands evaluated without finding work or triggering a restart.
src/strand/mod.rs-554-            return Ok(SelectOutcome {
src/strand/mod.rs-555-                bead: None,
src/strand/mod.rs-556-                waterfall_restarts: restarts,
src/strand/mod.rs-557-                restart_triggers,
src/strand/mod.rs-558-                strand_evaluations,
src/strand/mod.rs:559:                split_failure_count: None,
src/strand/mod.rs-560-            });
src/strand/mod.rs-561-        }
src/strand/mod.rs-562-    }
src/strand/mod.rs-563-
src/strand/mod.rs-564-    /// Return the names of all configured strands (for telemetry/debugging).
src/strand/mod.rs-565-    pub fn strand_names(&self) -> Vec<&str> {
--
src/strand/mod.rs-664-        async fn show(&self, _id: &BeadId) -> Result<Bead> {
src/strand/mod.rs-665-            anyhow::bail!("not found")
src/strand/mod.rs-666-        }
src/strand/mod.rs-667-        async fn claim(&self, _id: &BeadId, _actor: &str) -> Result<crate::types::ClaimResult> {
src/strand/mod.rs-668-            anyhow::bail!("not implemented")
src/strand/mod.rs-669-        }
src/strand/mod.rs:670:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/mod.rs-671-            Ok(())
src/strand/mod.rs-672-        }
src/strand/mod.rs-673-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/mod.rs-674-            Ok(())
src/strand/mod.rs-675-        }
src/strand/mod.rs-676-        async fn flush(&self) -> Result<()> {
--
src/supervisor/mod.rs-38-/// Maximum consecutive errors before entering long backoff.
src/supervisor/mod.rs-39-const MAX_CONSECUTIVE_ERRORS: u32 = 5;
src/supervisor/mod.rs-40-
src/supervisor/mod.rs-41-/// Long backoff duration after error threshold (seconds).
src/supervisor/mod.rs-42-const ERROR_BACKOFF_SECS: u64 = 60;
src/supervisor/mod.rs-43-
src/supervisor/mod.rs:44:/// Default interval for checking GitHub releases (seconds).
src/supervisor/mod.rs-45-const DEFAULT_UPDATE_CHECK_INTERVAL_SECS: u64 = 21600;
src/supervisor/mod.rs-46-
src/supervisor/mod.rs-47-/// Upgrade-check callback used by [`UpgradePoller`].
src/supervisor/mod.rs-48-pub type UpgradeCheckFn = Arc<dyn Fn(&Telemetry) -> Result<()> + Send + Sync>;
src/supervisor/mod.rs-49-
src/supervisor/mod.rs-50-/// State machine for periodic supervisor upgrade checks.
src/supervisor/mod.rs-51-///
src/supervisor/mod.rs-52-/// The poller deliberately owns its clock state separately from the queue
src/supervisor/mod.rs:53:/// polling loop. A busy or empty queue must not change when the next release
src/supervisor/mod.rs-54-/// check is due, and an upgrade-check failure must not stop the supervisor.
src/supervisor/mod.rs-55-pub struct UpgradePoller {
src/supervisor/mod.rs-56-    enabled: bool,
src/supervisor/mod.rs-57-    interval: Duration,
src/supervisor/mod.rs-58-    last_check: Option<Instant>,
src/supervisor/mod.rs-59-    check: UpgradeCheckFn,
src/supervisor/mod.rs-60-}
src/supervisor/mod.rs-61-
src/supervisor/mod.rs-62-impl UpgradePoller {
src/supervisor/mod.rs:63:    /// Create a production poller using the GitHub-release testing-channel
src/supervisor/mod.rs-64-    /// downloader.
src/supervisor/mod.rs-65-    pub fn new(enabled: bool, interval_secs: u64) -> Self {
src/supervisor/mod.rs-66-        Self::with_checker(
src/supervisor/mod.rs-67-            enabled,
src/supervisor/mod.rs-68-            interval_secs,
src/supervisor/mod.rs-69-            Arc::new(|telemetry| {
--
src/supervisor/mod.rs-142-    #[serde(default)]
src/supervisor/mod.rs-143-    pub agent_timeout: Option<u64>,
src/supervisor/mod.rs-144-    /// Explicit override for the worker binary path (`worker.worker_binary_path`).
src/supervisor/mod.rs-145-    /// When `None`, the supervisor resolves `std::env::current_exe()`.
src/supervisor/mod.rs-146-    #[serde(default)]
src/supervisor/mod.rs-147-    pub worker_binary_path: Option<PathBuf>,
src/supervisor/mod.rs:148:    /// Whether the supervisor periodically checks for GitHub releases.
src/supervisor/mod.rs-149-    #[serde(default = "SupervisorConfig::default_auto_upgrade_check")]
src/supervisor/mod.rs-150-    pub auto_upgrade_check: bool,
src/supervisor/mod.rs:151:    /// Seconds between automatic GitHub release checks.
src/supervisor/mod.rs-152-    #[serde(default = "SupervisorConfig::default_update_check_interval_secs")]
src/supervisor/mod.rs-153-    pub update_check_interval_secs: u64,
src/supervisor/mod.rs-154-}
src/supervisor/mod.rs-155-
src/supervisor/mod.rs-156-impl Default for SupervisorConfig {
src/supervisor/mod.rs-157-    fn default() -> Self {
--
src/supervisor/mod.rs-268-    /// Worker registry for tracking active workers.
src/supervisor/mod.rs-269-    registry: Registry,
src/supervisor/mod.rs-270-    /// Telemetry instance.
src/supervisor/mod.rs-271-    telemetry: Telemetry,
src/supervisor/mod.rs-272-    /// Shutdown flag for graceful termination.
src/supervisor/mod.rs-273-    shutdown: Arc<AtomicBool>,
src/supervisor/mod.rs:274:    /// Periodic GitHub release checker.
src/supervisor/mod.rs-275-    upgrade_poller: UpgradePoller,
src/supervisor/mod.rs-276-    /// Binary freshness checker for detecting new worker builds.
src/supervisor/mod.rs-277-    binary_freshness: Option<BinaryFreshnessChecker>,
src/supervisor/mod.rs-278-    /// Whether we're currently draining workers for rotation.
src/supervisor/mod.rs-279-    draining_workers: Arc<AtomicBool>,
src/supervisor/mod.rs-280-    /// Path to the current worker binary being used.
--
src/supervisor/mod.rs-623-    ///
src/supervisor/mod.rs-624-    /// Returns true if a worker was spawned, false otherwise.
src/supervisor/mod.rs-625-    async fn tick(&mut self) -> Result<bool> {
src/supervisor/mod.rs-626-        reap_zombie_children();
src/supervisor/mod.rs-627-
src/supervisor/mod.rs-628-        // Check independently of queue depth and worker capacity so a full or
src/supervisor/mod.rs:629:        // idle fleet still notices an available release.
src/supervisor/mod.rs-630-        self.upgrade_poller.poll(&self.telemetry);
src/supervisor/mod.rs-631-
src/supervisor/mod.rs-632-        // Check for binary freshness and rotate workers if needed
src/supervisor/mod.rs-633-        if let Some(checker) = &mut self.binary_freshness {
src/supervisor/mod.rs-634-            if let Ok(Some(check_result)) = checker.poll() {
src/supervisor/mod.rs-635-                match check_result {
--
src/strand/mend.rs-1-//! Mend strand: maintenance and self-healing.
src/strand/mend.rs-2-//!
src/strand/mend.rs-3-//! Strand 2 in the waterfall. Runs after Pluck finds no work. Cleans up
src/strand/mend.rs-4-//! stale claims, orphaned locks, broken dependency links, and database
src/strand/mend.rs-5-//! corruption. If any cleanup is performed, returns `WorkCreated` so the
src/strand/mend.rs:6://! waterfall restarts from Pluck (released beads may now be claimable).
src/strand/mend.rs-7-//!
src/strand/mend.rs-8-//! Depends on: `bead_store`, `config`, `health`, `peer`, `registry`,
src/strand/mend.rs-9-//!             `telemetry`, `types`, `trace`.
src/strand/mend.rs-10-
src/strand/mend.rs-11-use std::collections::HashSet;
src/strand/mend.rs-12-use std::path::{Path, PathBuf};
--
src/strand/mend.rs-26-use crate::types::{BeadId, BeadStatus, StrandError, StrandResult};
src/strand/mend.rs-27-
src/strand/mend.rs-28-// ──────────────────────────────────────────────────────────────────────────────
src/strand/mend.rs-29-// Store-scoped orphan cleanup (shared between Mend and Explore)
src/strand/mend.rs-30-// ──────────────────────────────────────────────────────────────────────────────
src/strand/mend.rs-31-
src/strand/mend.rs:32:/// Scan all in-progress beads in a store and release any whose assignee does
src/strand/mend.rs-33-/// not correspond to a live worker. This catches beads orphaned by workers
src/strand/mend.rs-34-/// that died without leaving a heartbeat file (or whose heartbeat was already
src/strand/mend.rs-35-/// cleaned up).
src/strand/mend.rs-36-///
src/strand/mend.rs-37-/// This is a store-scoped function that can be called against any BeadStore,
src/strand/mend.rs-38-/// not just the home workspace. Used by both MendStrand (for home) and
src/strand/mend.rs-39-/// ExploreStrand (for remote workspaces).
src/strand/mend.rs-40-///
src/strand/mend.rs-41-/// # Arguments
src/strand/mend.rs-42-/// * `store` - The bead store to scan (can be home or remote)
src/strand/mend.rs-43-/// * `registry` - Worker registry for live worker lookup
src/strand/mend.rs:44:/// * `telemetry` - Telemetry emitter for orphan release events
src/strand/mend.rs-45-/// * `qualified_id` - This worker's fully-qualified identity (excluded from orphan detection)
src/strand/mend.rs-46-///
src/strand/mend.rs-47-/// # Returns
src/strand/mend.rs:48:/// * `Ok(u32)` - Number of orphans released
src/strand/mend.rs-49-/// * `Err(anyhow::Error)` - Store read failure
src/strand/mend.rs-50-pub async fn cleanup_orphaned_in_progress(
src/strand/mend.rs-51-    store: &dyn BeadStore,
src/strand/mend.rs-52-    registry: &Registry,
src/strand/mend.rs-53-    telemetry: &Telemetry,
src/strand/mend.rs-54-    qualified_id: &str,
--
src/strand/mend.rs-68-    telemetry: &Telemetry,
src/strand/mend.rs-69-    qualified_id: &str,
src/strand/mend.rs-70-) -> Result<u32> {
src/strand/mend.rs-71-    cleanup_in_progress(store, registry, telemetry, qualified_id, None).await
src/strand/mend.rs-72-}
src/strand/mend.rs-73-
src/strand/mend.rs:74:/// Scan all in-progress beads in a store and release any whose assignee is
src/strand/mend.rs-75-/// orphaned, whose claim is older than `claim_ttl`, or whose claim was
src/strand/mend.rs-76-/// superseded by a newer claim from the same assignee. Claim age and claim
src/strand/mend.rs-77-/// recency are independent of worker liveness so a worker name that is reused
src/strand/mend.rs-78-/// by a relaunch cannot pin an abandoned bead forever.
src/strand/mend.rs-79-async fn cleanup_in_progress(
src/strand/mend.rs-80-    store: &dyn BeadStore,
--
src/strand/mend.rs-129-        // Update if this bead is newer than the current newest for this assignee
src/strand/mend.rs-130-        if bead.updated_at > entry.0 {
src/strand/mend.rs-131-            *entry = (bead.updated_at, bead.id.clone());
src/strand/mend.rs-132-        }
src/strand/mend.rs-133-    }
src/strand/mend.rs-134-
src/strand/mend.rs:135:    let mut released = 0u32;
src/strand/mend.rs-136-    let now = Utc::now();
src/strand/mend.rs-137-
src/strand/mend.rs-138-    for bead in &all_beads {
src/strand/mend.rs-139-        if bead.status != BeadStatus::InProgress {
src/strand/mend.rs-140-            continue;
src/strand/mend.rs-141-        }
--
src/strand/mend.rs-166-                    newest_bead_id = %newest_bead_id,
src/strand/mend.rs-167-                    "releasing in-progress bead superseded by newer claim from same assignee"
src/strand/mend.rs-168-                );
src/strand/mend.rs-169-            }
src/strand/mend.rs-170-        }
src/strand/mend.rs-171-
src/strand/mend.rs:172:        // Claim age is an independent release condition. A live worker may
src/strand/mend.rs-173-        // still hold an abandoned claim, especially when --count 1 relaunches
src/strand/mend.rs-174-        // under the same worker name indefinitely.
src/strand/mend.rs-175-        if !is_superseded && !stale_by_age && assignee == qualified_id {
src/strand/mend.rs-176-            continue;
src/strand/mend.rs-177-        }
src/strand/mend.rs-178-
--
src/strand/mend.rs-201-            age_secs = claim_age.map_or(0, |age| age.as_secs()),
src/strand/mend.rs-202-            stale_by_age,
src/strand/mend.rs-203-            workspace = %bead.workspace.display(),
src/strand/mend.rs-204-            "releasing stale in-progress bead"
src/strand/mend.rs-205-        );
src/strand/mend.rs-206-
src/strand/mend.rs:207:        match store.release(&bead.id).await {
src/strand/mend.rs-208-            Ok(()) => {
src/strand/mend.rs-209-                let _ = telemetry.emit(EventKind::BeadReleased {
src/strand/mend.rs-210-                    bead_id: bead.id.clone(),
src/strand/mend.rs-211-                    reason,
src/strand/mend.rs-212-                });
src/strand/mend.rs-213-                let _ = telemetry.emit(EventKind::StuckReleased {
src/strand/mend.rs-214-                    bead_id: bead.id.clone(),
src/strand/mend.rs-215-                    peer_worker: assignee.clone(),
src/strand/mend.rs-216-                });
src/strand/mend.rs:217:                released += 1;
src/strand/mend.rs-218-            }
src/strand/mend.rs-219-            Err(e) => {
src/strand/mend.rs-220-                tracing::warn!(
src/strand/mend.rs-221-                    bead_id = %bead.id,
src/strand/mend.rs-222-                    error = %e,
src/strand/mend.rs:223:                    "failed to release orphaned in-progress bead"
src/strand/mend.rs-224-                );
src/strand/mend.rs-225-                let _ = telemetry.emit(EventKind::MendBeadReleaseFailed {
src/strand/mend.rs-226-                    bead_id: bead.id.to_string(),
src/strand/mend.rs-227-                    assignee: assignee.clone(),
src/strand/mend.rs-228-                    error: e.to_string(),
src/strand/mend.rs-229-                });
src/strand/mend.rs-230-            }
src/strand/mend.rs-231-        }
src/strand/mend.rs-232-    }
src/strand/mend.rs-233-
src/strand/mend.rs:234:    Ok(released)
src/strand/mend.rs-235-}
src/strand/mend.rs-236-
src/strand/mend.rs-237-/// Summary of work performed during one Mend evaluation cycle.
src/strand/mend.rs-238-#[derive(Debug, Default)]
src/strand/mend.rs-239-struct MendSummary {
src/strand/mend.rs:240:    beads_released: u32,
src/strand/mend.rs-241-    locks_removed: u32,
src/strand/mend.rs-242-    deps_cleaned: u32,
src/strand/mend.rs-243-    db_repaired: bool,
src/strand/mend.rs-244-    db_rebuilt: bool,
src/strand/mend.rs-245-    agent_logs_cleaned: u32,
src/strand/mend.rs-246-    zero_activity_logs_cleaned: u32,
--
src/strand/mend.rs-257-    assignees_cleared: u32,
src/strand/mend.rs-258-}
src/strand/mend.rs-259-
src/strand/mend.rs-260-impl MendSummary {
src/strand/mend.rs-261-    /// Whether mend performed work that changes bead store state.
src/strand/mend.rs-262-    ///
src/strand/mend.rs:263:    /// Only operations that add or release claimable beads should return true.
src/strand/mend.rs-264-    /// A strand MUST return `WorkCreated` only when it inserts a claimable bead
src/strand/mend.rs-265-    /// into the store that the waterfall will re-scan. No-op cleanup (pruning
src/strand/mend.rs-266-    /// traces, removing stale locks, repairing DB) should return `NoWork`.
src/strand/mend.rs-267-    ///
src/strand/mend.rs-268-    /// Operations that return `WorkCreated`:
src/strand/mend.rs:269:    /// - `beads_released > 0`: Orphaned beads were released back to Open status.
src/strand/mend.rs-270-    /// - `deps_cleaned > 0`: Stale dependency links were removed (beads become claimable).
src/strand/mend.rs-271-    /// - `assignees_cleared > 0`: Stale assignees were cleared (beads become claimable).
src/strand/mend.rs-272-    ///
src/strand/mend.rs-273-    /// Operations that return `NoWork` (maintenance, not work creation):
src/strand/mend.rs-274-    /// - `locks_removed > 0`: Lock file cleanup (doesn't add beads to queue).
src/strand/mend.rs-275-    /// - `db_repaired`: Doctor repair fixed index corruption (doesn't add beads).
src/strand/mend.rs-276-    /// - `db_rebuilt`: Full rebuild from JSONL (doesn't add beads).
src/strand/mend.rs-277-    /// - `traces_pruned`, `agent_logs_cleaned`, `learnings_*`: File cleanup.
src/strand/mend.rs-278-    ///
src/strand/mend.rs-279-    /// A `WorkCreated` return must be paired with a telemetry event identifying
src/strand/mend.rs-280-    /// the created bead(s) so operators can see what the restart is chasing.
src/strand/mend.rs-281-    fn did_work(&self) -> bool {
src/strand/mend.rs:282:        // Bead release, dependency removal, and assignee clearing add claimable items to the queue.
src/strand/mend.rs-283-        // Lock removal, DB repair, and DB rebuild are maintenance operations
src/strand/mend.rs-284-        // that don't create new work and must not trigger a waterfall restart.
src/strand/mend.rs:285:        self.beads_released > 0 || self.deps_cleaned > 0 || self.assignees_cleared > 0
src/strand/mend.rs-286-    }
src/strand/mend.rs-287-}
src/strand/mend.rs-288-
src/strand/mend.rs-289-/// The Mend strand — maintenance and self-healing.
src/strand/mend.rs-290-pub struct MendStrand {
src/strand/mend.rs-291-    config: MendConfig,
--
src/strand/mend.rs-368-            limits_config,
src/strand/mend.rs-369-        }
src/strand/mend.rs-370-    }
src/strand/mend.rs-371-
src/strand/mend.rs-372-    // ── Step 1: Stale claim cleanup via peer monitoring ──────────────────────
src/strand/mend.rs-373-
src/strand/mend.rs:374:    /// Check for crashed workers and release their orphaned beads.
src/strand/mend.rs-375-    async fn cleanup_stale_claims(
src/strand/mend.rs-376-        &self,
src/strand/mend.rs-377-        store: &dyn BeadStore,
src/strand/mend.rs-378-        summary: &mut MendSummary,
src/strand/mend.rs-379-    ) -> Result<()> {
src/strand/mend.rs-380-        let peer_monitor = PeerMonitor::new(
--
src/strand/mend.rs-384-            store,
src/strand/mend.rs-385-            &self.registry,
src/strand/mend.rs-386-            self.telemetry.clone(),
src/strand/mend.rs-387-        );
src/strand/mend.rs-388-
src/strand/mend.rs-389-        let peer_result = peer_monitor.check_peers().await?;
src/strand/mend.rs:390:        summary.beads_released += peer_result.beads_released;
src/strand/mend.rs-391-
src/strand/mend.rs-392-        Ok(())
src/strand/mend.rs-393-    }
src/strand/mend.rs-394-
src/strand/mend.rs-395-    // ── Step 1.5: Orphaned in-progress bead recovery ────────────────────────
src/strand/mend.rs-396-
src/strand/mend.rs:397:    /// Scan all in-progress beads and release any whose assignee does not
src/strand/mend.rs-398-    /// correspond to a live worker. This catches beads orphaned by workers
src/strand/mend.rs-399-    /// that died without leaving a heartbeat file (or whose heartbeat was
src/strand/mend.rs-400-    /// already cleaned up).
src/strand/mend.rs-401-    async fn cleanup_orphaned_in_progress(
src/strand/mend.rs-402-        &self,
src/strand/mend.rs-403-        store: &dyn BeadStore,
src/strand/mend.rs-404-        summary: &mut MendSummary,
src/strand/mend.rs-405-    ) -> Result<()> {
src/strand/mend.rs:406:        let released = cleanup_in_progress(
src/strand/mend.rs-407-            store,
src/strand/mend.rs-408-            &self.registry,
src/strand/mend.rs-409-            &self.telemetry,
src/strand/mend.rs-410-            &self.qualified_id,
src/strand/mend.rs-411-            Some(Duration::from_secs(self.config.stuck_threshold_secs)),
src/strand/mend.rs-412-        )
src/strand/mend.rs-413-        .await?;
src/strand/mend.rs:414:        summary.beads_released += released;
src/strand/mend.rs-415-        Ok(())
src/strand/mend.rs-416-    }
src/strand/mend.rs-417-
src/strand/mend.rs-418-    // ── Step 1.6: Stale assignee cleanup on open beads ─────────────────────────
src/strand/mend.rs-419-
src/strand/mend.rs-420-    /// Scan all open beads and clear assignees whose workers have no live heartbeat.
--
src/strand/mend.rs-608-    // ── Step 2: Orphaned lock file removal ───────────────────────────────────
src/strand/mend.rs-609-
src/strand/mend.rs-610-    /// Remove claim lock files that are not actively held by any process.
src/strand/mend.rs-611-    ///
src/strand/mend.rs-612-    /// This function immediately cleans up lock files whose holding process
src/strand/mend.rs-613-    /// has died, without waiting for an age-based timeout. The flock(2) lock
src/strand/mend.rs:614:    /// is automatically released by the kernel when the holder process dies,
src/strand/mend.rs-615-    /// so try_acquire_flock() will succeed immediately and we can remove the
src/strand/mend.rs-616-    /// stale metadata file.
src/strand/mend.rs-617-    ///
src/strand/mend.rs-618-    /// Lock files older than lock_ttl_secs are also cleaned up as a fallback
src/strand/mend.rs-619-    /// to handle edge cases where the flock probe might fail.
src/strand/mend.rs-620-    fn cleanup_orphaned_locks(&self, summary: &mut MendSummary) -> Result<()> {
--
src/strand/mend.rs-656-                None => continue,
src/strand/mend.rs-657-            };
src/strand/mend.rs-658-
src/strand/mend.rs-659-            // Try to acquire flock (non-blocking). If we can acquire it,
src/strand/mend.rs-660-            // no one is holding it — safe to delete immediately.
src/strand/mend.rs-661-            //
src/strand/mend.rs:662:            // The kernel releases flocks when the holder process dies, so this
src/strand/mend.rs-663-            // probe succeeds immediately for dead PIDs regardless of file age.
src/strand/mend.rs-664-            match try_acquire_flock(&path) {
src/strand/mend.rs-665-                Ok(Some(_file)) => {
src/strand/mend.rs-666-                    // Lock acquired — no one holds it. Remove the file.
src/strand/mend.rs-667-                    let age_secs = age.as_secs();
src/strand/mend.rs-668-                    if let Err(e) = std::fs::remove_file(&path) {
--
src/strand/mend.rs-1559-            tracing::warn!(error = %e, "mend: database health check failed");
src/strand/mend.rs-1560-            // DB check failure is non-fatal — continue with the summary.
src/strand/mend.rs-1561-        }
src/strand/mend.rs-1562-
src/strand/mend.rs-1563-        // Emit cycle summary telemetry.
src/strand/mend.rs-1564-        let _ = self.telemetry.emit(EventKind::MendCycleSummary {
src/strand/mend.rs:1565:            beads_released: summary.beads_released,
src/strand/mend.rs-1566-            locks_removed: summary.locks_removed,
src/strand/mend.rs-1567-            deps_cleaned: summary.deps_cleaned,
src/strand/mend.rs-1568-            db_repaired: summary.db_repaired,
src/strand/mend.rs-1569-            db_rebuilt: summary.db_rebuilt,
src/strand/mend.rs-1570-            agent_logs_cleaned: summary.agent_logs_cleaned,
src/strand/mend.rs-1571-            zero_activity_logs_cleaned: summary.zero_activity_logs_cleaned,
--
src/strand/mend.rs-1576-            rate_limits_cleaned: summary.rate_limits_cleaned,
src/strand/mend.rs-1577-            assignees_cleared: summary.assignees_cleared,
src/strand/mend.rs-1578-        });
src/strand/mend.rs-1579-
src/strand/mend.rs-1580-        let result = if summary.did_work() {
src/strand/mend.rs-1581-            tracing::info!(
src/strand/mend.rs:1582:                beads_released = summary.beads_released,
src/strand/mend.rs-1583-                locks_removed = summary.locks_removed,
src/strand/mend.rs-1584-                deps_cleaned = summary.deps_cleaned,
src/strand/mend.rs-1585-                db_repaired = summary.db_repaired,
src/strand/mend.rs-1586-                db_rebuilt = summary.db_rebuilt,
src/strand/mend.rs-1587-                agent_logs_cleaned = summary.agent_logs_cleaned,
src/strand/mend.rs-1588-                traces_pruned = summary.traces_pruned,
--
src/strand/mend.rs-1650-    use std::sync::{Arc, Mutex};
src/strand/mend.rs-1651-
src/strand/mend.rs-1652-    // ── Mock bead store ─────────────────────────────────────────────────────
src/strand/mend.rs-1653-
src/strand/mend.rs-1654-    struct MockBeadStore {
src/strand/mend.rs-1655-        all_beads: Mutex<Vec<Bead>>,
src/strand/mend.rs:1656:        release_count: Arc<AtomicU32>,
src/strand/mend.rs-1657-        clear_assignee_count: Arc<AtomicU32>,
src/strand/mend.rs-1658-        /// Warnings returned by doctor_check (probe-only).
src/strand/mend.rs-1659-        check_warnings: Vec<String>,
src/strand/mend.rs-1660-        /// If Some, doctor_repair succeeds with this report. If None, it fails.
src/strand/mend.rs-1661-        repair_report: Option<RepairReport>,
src/strand/mend.rs-1662-        /// Whether full_rebuild() should fail.
src/strand/mend.rs-1663-        rebuild_fails: bool,
src/strand/mend.rs-1664-    }
src/strand/mend.rs-1665-
src/strand/mend.rs-1666-    impl MockBeadStore {
src/strand/mend.rs-1667-        fn new(beads: Vec<Bead>) -> (Self, Arc<AtomicU32>, Arc<AtomicU32>) {
src/strand/mend.rs:1668:            let release_count = Arc::new(AtomicU32::new(0));
src/strand/mend.rs-1669-            let clear_assignee_count = Arc::new(AtomicU32::new(0));
src/strand/mend.rs-1670-            (
src/strand/mend.rs-1671-                MockBeadStore {
src/strand/mend.rs-1672-                    all_beads: Mutex::new(beads),
src/strand/mend.rs:1673:                    release_count: release_count.clone(),
src/strand/mend.rs-1674-                    clear_assignee_count: clear_assignee_count.clone(),
src/strand/mend.rs-1675-                    check_warnings: vec![],
src/strand/mend.rs-1676-                    repair_report: Some(RepairReport::default()),
src/strand/mend.rs-1677-                    rebuild_fails: false,
src/strand/mend.rs-1678-                },
src/strand/mend.rs:1679:                release_count,
src/strand/mend.rs-1680-                clear_assignee_count,
src/strand/mend.rs-1681-            )
src/strand/mend.rs-1682-        }
src/strand/mend.rs-1683-
src/strand/mend.rs-1684-        /// doctor_check returns warnings → escalates to repair (which succeeds).
src/strand/mend.rs-1685-        fn with_doctor_report(mut self, report: RepairReport) -> Self {
--
src/strand/mend.rs-1721-        }
src/strand/mend.rs-1722-
src/strand/mend.rs-1723-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/strand/mend.rs-1724-            anyhow::bail!("not implemented in mock")
src/strand/mend.rs-1725-        }
src/strand/mend.rs-1726-
src/strand/mend.rs:1727:        async fn release(&self, id: &BeadId) -> Result<()> {
src/strand/mend.rs-1728-            // Find and update the bead's status, simulating real bead store behavior.
src/strand/mend.rs-1729-            let mut beads = self.all_beads.lock().unwrap();
src/strand/mend.rs-1730-            if let Some(bead) = beads.iter_mut().find(|b| &b.id == id) {
src/strand/mend.rs:1731:                // Only count releases for InProgress beads (real behavior).
src/strand/mend.rs-1732-                if bead.status == BeadStatus::InProgress {
src/strand/mend.rs-1733-                    bead.status = BeadStatus::Open;
src/strand/mend.rs-1734-                    bead.assignee = None;
src/strand/mend.rs:1735:                    self.release_count.fetch_add(1, Ordering::Relaxed);
src/strand/mend.rs-1736-                }
src/strand/mend.rs-1737-                Ok(())
src/strand/mend.rs-1738-            } else {
src/strand/mend.rs-1739-                anyhow::bail!("bead not found: {}", id)
src/strand/mend.rs-1740-            }
src/strand/mend.rs-1741-        }
--
src/strand/mend.rs-1828-        }
src/strand/mend.rs-1829-
src/strand/mend.rs-1830-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/strand/mend.rs-1831-            anyhow::bail!("store error")
src/strand/mend.rs-1832-        }
src/strand/mend.rs-1833-
src/strand/mend.rs:1834:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/mend.rs-1835-            anyhow::bail!("store error")
src/strand/mend.rs-1836-        }
src/strand/mend.rs-1837-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/mend.rs-1838-            anyhow::bail!("store error")
src/strand/mend.rs-1839-        }
src/strand/mend.rs-1840-        async fn flush(&self) -> Result<()> {
--
src/strand/mend.rs-2026-
src/strand/mend.rs-2027-    use super::super::Strand;
src/strand/mend.rs-2028-
src/strand/mend.rs-2029-    // ── Stale claim cleanup tests ────────────────────────────────────────────
src/strand/mend.rs-2030-
src/strand/mend.rs-2031-    #[tokio::test]
src/strand/mend.rs:2032:    async fn crashed_peer_bead_released_returns_work_created() {
src/strand/mend.rs-2033-        let hb_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2034-        let lock_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2035-        let reg_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2036-
src/strand/mend.rs-2037-        // Write a stale heartbeat with dead PID.
src/strand/mend.rs-2038-        write_heartbeat(
src/strand/mend.rs-2039-            hb_dir.path(),
src/strand/mend.rs-2040-            &make_stale_heartbeat("dead-worker", 99_999_999, Some("nd-orphan")),
src/strand/mend.rs-2041-        );
src/strand/mend.rs-2042-
src/strand/mend.rs-2043-        // Create an in-progress bead assigned to the dead worker.
src/strand/mend.rs-2044-        let bead = make_in_progress_bead("nd-orphan", "dead-worker");
src/strand/mend.rs:2045:        let (store, release_count, _) = MockBeadStore::new(vec![bead]);
src/strand/mend.rs-2046-        let mend = make_mend_strand(hb_dir.path(), lock_dir.path(), reg_dir.path());
src/strand/mend.rs-2047-
src/strand/mend.rs-2048-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-2049-        assert!(
src/strand/mend.rs-2050-            matches!(result, StrandResult::WorkCreated),
src/strand/mend.rs-2051-            "expected WorkCreated after releasing crashed peer's bead, got: {result:?}"
src/strand/mend.rs-2052-        );
src/strand/mend.rs:2053:        assert_eq!(release_count.load(Ordering::Relaxed), 1);
src/strand/mend.rs-2054-    }
src/strand/mend.rs-2055-
src/strand/mend.rs-2056-    #[tokio::test]
src/strand/mend.rs-2057-    async fn no_stale_peers_returns_no_work() {
src/strand/mend.rs-2058-        let hb_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2059-        let lock_dir = tempfile::tempdir().unwrap();
--
src/strand/mend.rs-2069-        );
src/strand/mend.rs-2070-    }
src/strand/mend.rs-2071-
src/strand/mend.rs-2072-    // ── Orphaned in-progress bead tests ─────────────────────────────────────
src/strand/mend.rs-2073-
src/strand/mend.rs-2074-    #[tokio::test]
src/strand/mend.rs:2075:    async fn orphaned_in_progress_bead_released() {
src/strand/mend.rs-2076-        let hb_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2077-        let lock_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2078-        let reg_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2079-
src/strand/mend.rs-2080-        // An in-progress bead assigned to a worker that doesn't exist.
src/strand/mend.rs-2081-        let bead = make_in_progress_bead("nd-orphan", "dead-worker");
src/strand/mend.rs-2082-
src/strand/mend.rs:2083:        let (store, release_count, _) = MockBeadStore::new(vec![bead]);
src/strand/mend.rs-2084-        let mend = make_mend_strand(hb_dir.path(), lock_dir.path(), reg_dir.path());
src/strand/mend.rs-2085-
src/strand/mend.rs-2086-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-2087-        assert!(
src/strand/mend.rs-2088-            matches!(result, StrandResult::WorkCreated),
src/strand/mend.rs-2089-            "expected WorkCreated after releasing orphaned in-progress bead, got: {result:?}"
src/strand/mend.rs-2090-        );
src/strand/mend.rs:2091:        assert_eq!(release_count.load(Ordering::Relaxed), 1);
src/strand/mend.rs-2092-    }
src/strand/mend.rs-2093-
src/strand/mend.rs-2094-    #[tokio::test]
src/strand/mend.rs:2095:    async fn in_progress_bead_with_live_worker_not_released() {
src/strand/mend.rs-2096-        let hb_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2097-        let lock_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2098-        let reg_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2099-
src/strand/mend.rs-2100-        // An in-progress bead assigned to a worker that is registered and alive.
src/strand/mend.rs-2101-        let bead = make_recent_in_progress_bead("nd-active", "alive-worker");
--
src/strand/mend.rs-2112-                provider: None,
src/strand/mend.rs-2113-                started_at: Utc::now(),
src/strand/mend.rs-2114-                beads_processed: 0,
src/strand/mend.rs-2115-            })
src/strand/mend.rs-2116-            .unwrap();
src/strand/mend.rs-2117-
src/strand/mend.rs:2118:        let (store, release_count, _) = MockBeadStore::new(vec![bead]);
src/strand/mend.rs-2119-        let mend = MendStrand::new(
src/strand/mend.rs-2120-            MendConfig::default(),
src/strand/mend.rs-2121-            hb_dir.path().to_path_buf(),
src/strand/mend.rs-2122-            Duration::from_secs(300),
src/strand/mend.rs-2123-            lock_dir.path().to_path_buf(),
src/strand/mend.rs-2124-            "test-worker".to_string(),
--
src/strand/mend.rs-2137-
src/strand/mend.rs-2138-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-2139-        assert!(
src/strand/mend.rs-2140-            matches!(result, StrandResult::NoWork),
src/strand/mend.rs-2141-            "expected NoWork when assignee is a live worker, got: {result:?}"
src/strand/mend.rs-2142-        );
src/strand/mend.rs:2143:        assert_eq!(release_count.load(Ordering::Relaxed), 0);
src/strand/mend.rs-2144-    }
src/strand/mend.rs-2145-
src/strand/mend.rs-2146-    #[tokio::test]
src/strand/mend.rs:2147:    async fn stale_in_progress_bead_released_even_when_worker_is_alive() {
src/strand/mend.rs-2148-        let hb_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2149-        let lock_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2150-        let reg_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2151-
src/strand/mend.rs-2152-        // The worker is alive, but the claim is far older than Mend's configured
src/strand/mend.rs-2153-        // claim TTL. This is the failure mode that liveness-only cleanup misses.
--
src/strand/mend.rs-2164-                provider: None,
src/strand/mend.rs-2165-                started_at: Utc::now(),
src/strand/mend.rs-2166-                beads_processed: 1,
src/strand/mend.rs-2167-            })
src/strand/mend.rs-2168-            .unwrap();
src/strand/mend.rs-2169-
src/strand/mend.rs:2170:        let (store, release_count, _) = MockBeadStore::new(vec![bead]);
src/strand/mend.rs-2171-        let mend = MendStrand::new(
src/strand/mend.rs-2172-            MendConfig {
src/strand/mend.rs-2173-                stuck_threshold_secs: 60,
src/strand/mend.rs-2174-                ..MendConfig::default()
src/strand/mend.rs-2175-            },
src/strand/mend.rs-2176-            hb_dir.path().to_path_buf(),
--
src/strand/mend.rs-2192-
src/strand/mend.rs-2193-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-2194-        assert!(
src/strand/mend.rs-2195-            matches!(result, StrandResult::WorkCreated),
src/strand/mend.rs-2196-            "expected WorkCreated after reclaiming stale live claim, got: {result:?}"
src/strand/mend.rs-2197-        );
src/strand/mend.rs:2198:        assert_eq!(release_count.load(Ordering::Relaxed), 1);
src/strand/mend.rs-2199-    }
src/strand/mend.rs-2200-
src/strand/mend.rs-2201-    #[tokio::test]
src/strand/mend.rs:2202:    async fn in_progress_bead_own_worker_not_released() {
src/strand/mend.rs-2203-        let hb_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2204-        let lock_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2205-        let reg_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2206-
src/strand/mend.rs-2207-        // An in-progress bead assigned to ourselves (using qualified_id).
src/strand/mend.rs-2208-        let bead = make_recent_in_progress_bead("nd-mine", "claude-test-worker");
src/strand/mend.rs-2209-
src/strand/mend.rs:2210:        let (store, release_count, _) = MockBeadStore::new(vec![bead]);
src/strand/mend.rs-2211-        let mend = make_mend_strand(hb_dir.path(), lock_dir.path(), reg_dir.path());
src/strand/mend.rs-2212-
src/strand/mend.rs-2213-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-2214-        assert!(
src/strand/mend.rs-2215-            matches!(result, StrandResult::NoWork),
src/strand/mend.rs-2216-            "expected NoWork when bead is assigned to ourselves, got: {result:?}"
src/strand/mend.rs-2217-        );
src/strand/mend.rs:2218:        assert_eq!(release_count.load(Ordering::Relaxed), 0);
src/strand/mend.rs-2219-    }
src/strand/mend.rs-2220-
src/strand/mend.rs-2221-    #[tokio::test]
src/strand/mend.rs:2222:    async fn in_progress_bead_with_dead_registered_worker_released() {
src/strand/mend.rs-2223-        let hb_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2224-        let lock_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2225-        let reg_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2226-
src/strand/mend.rs-2227-        // An in-progress bead assigned to a worker that is registered but dead.
src/strand/mend.rs-2228-        let bead = make_in_progress_bead("nd-stale", "dead-registered");
--
src/strand/mend.rs-2239-                provider: None,
src/strand/mend.rs-2240-                started_at: Utc::now(),
src/strand/mend.rs-2241-                beads_processed: 0,
src/strand/mend.rs-2242-            })
src/strand/mend.rs-2243-            .unwrap();
src/strand/mend.rs-2244-
src/strand/mend.rs:2245:        let (store, release_count, _) = MockBeadStore::new(vec![bead]);
src/strand/mend.rs-2246-        let mend = MendStrand::new(
src/strand/mend.rs-2247-            MendConfig::default(),
src/strand/mend.rs-2248-            hb_dir.path().to_path_buf(),
src/strand/mend.rs-2249-            Duration::from_secs(300),
src/strand/mend.rs-2250-            lock_dir.path().to_path_buf(),
src/strand/mend.rs-2251-            "test-worker".to_string(),
--
src/strand/mend.rs-2264-
src/strand/mend.rs-2265-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-2266-        assert!(
src/strand/mend.rs-2267-            matches!(result, StrandResult::WorkCreated),
src/strand/mend.rs-2268-            "expected WorkCreated after releasing bead from dead registered worker, got: {result:?}"
src/strand/mend.rs-2269-        );
src/strand/mend.rs:2270:        assert_eq!(release_count.load(Ordering::Relaxed), 1);
src/strand/mend.rs-2271-    }
src/strand/mend.rs-2272-
src/strand/mend.rs-2273-    // ── Superseded claim tests ───────────────────────────────────────────────────
src/strand/mend.rs-2274-
src/strand/mend.rs-2275-    /// Multiple in_progress beads for same assignee: only newest is valid.
src/strand/mend.rs-2276-    /// Older beads are reaped even if worker is alive (dispatch leak detection).
--
src/strand/mend.rs-2280-        let lock_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2281-        let reg_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2282-
src/strand/mend.rs-2283-        let assignee = "claude-code-glm-5-commitgraph-2";
src/strand/mend.rs-2284-
src/strand/mend.rs-2285-        // Create 3 recent in_progress beads for the same assignee with distinct
src/strand/mend.rs:2286:        // timestamps, so the stale-age path cannot account for the releases.
src/strand/mend.rs-2287-        let mut bead_old = make_in_progress_bead("nd-old", assignee);
src/strand/mend.rs-2288-        bead_old.updated_at = Utc::now() - chrono::Duration::seconds(3);
src/strand/mend.rs-2289-
src/strand/mend.rs-2290-        let mut bead_medium = make_in_progress_bead("nd-medium", assignee);
src/strand/mend.rs-2291-        bead_medium.updated_at = Utc::now() - chrono::Duration::seconds(2);
src/strand/mend.rs-2292-
--
src/strand/mend.rs-2305-                provider: None,
src/strand/mend.rs-2306-                started_at: Utc::now() - chrono::Duration::seconds(7200),
src/strand/mend.rs-2307-                beads_processed: 10,
src/strand/mend.rs-2308-            })
src/strand/mend.rs-2309-            .unwrap();
src/strand/mend.rs-2310-
src/strand/mend.rs:2311:        let (store, release_count, _) = MockBeadStore::new(vec![bead_old, bead_medium, bead_new]);
src/strand/mend.rs-2312-        let mend = MendStrand::new(
src/strand/mend.rs-2313-            MendConfig::default(),
src/strand/mend.rs-2314-            hb_dir.path().to_path_buf(),
src/strand/mend.rs-2315-            Duration::from_secs(300),
src/strand/mend.rs-2316-            lock_dir.path().to_path_buf(),
src/strand/mend.rs-2317-            assignee.to_string(),
--
src/strand/mend.rs-2331-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-2332-        assert!(
src/strand/mend.rs-2333-            matches!(result, StrandResult::WorkCreated),
src/strand/mend.rs-2334-            "expected WorkCreated after reaping superseded claims, got: {result:?}"
src/strand/mend.rs-2335-        );
src/strand/mend.rs-2336-        assert_eq!(
src/strand/mend.rs:2337:            release_count.load(Ordering::Relaxed),
src/strand/mend.rs-2338-            2,
src/strand/mend.rs:2339:            "should release exactly 2 older beads (newest bead should be untouched)"
src/strand/mend.rs-2340-        );
src/strand/mend.rs-2341-    }
src/strand/mend.rs-2342-
src/strand/mend.rs-2343-    /// Single in_progress bead for assignee: NOT reaped if worker alive.
src/strand/mend.rs-2344-    /// Verifies the superseded check doesn't regress the normal liveness path.
src/strand/mend.rs-2345-    #[tokio::test]
--
src/strand/mend.rs-2365-                provider: None,
src/strand/mend.rs-2366-                started_at: Utc::now(),
src/strand/mend.rs-2367-                beads_processed: 1,
src/strand/mend.rs-2368-            })
src/strand/mend.rs-2369-            .unwrap();
src/strand/mend.rs-2370-
src/strand/mend.rs:2371:        let (store, release_count, _) = MockBeadStore::new(vec![bead]);
src/strand/mend.rs-2372-        let mend = MendStrand::new(
src/strand/mend.rs-2373-            MendConfig::default(),
src/strand/mend.rs-2374-            hb_dir.path().to_path_buf(),
src/strand/mend.rs-2375-            Duration::from_secs(300),
src/strand/mend.rs-2376-            lock_dir.path().to_path_buf(),
src/strand/mend.rs-2377-            "test-worker".to_string(),
--
src/strand/mend.rs-2391-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-2392-        assert!(
src/strand/mend.rs-2393-            matches!(result, StrandResult::NoWork),
src/strand/mend.rs-2394-            "expected NoWork when single claim is from live worker, got: {result:?}"
src/strand/mend.rs-2395-        );
src/strand/mend.rs-2396-        assert_eq!(
src/strand/mend.rs:2397:            release_count.load(Ordering::Relaxed),
src/strand/mend.rs-2398-            0,
src/strand/mend.rs-2399-            "single claim should NOT be reaped when worker is alive"
src/strand/mend.rs-2400-        );
src/strand/mend.rs-2401-    }
src/strand/mend.rs-2402-
src/strand/mend.rs-2403-    // ── Stale assignee on open bead tests ────────────────────────────────────
--
src/strand/mend.rs-2409-        let lock_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2410-        let reg_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2411-
src/strand/mend.rs-2412-        // An open bead assigned to a worker that doesn't exist.
src/strand/mend.rs-2413-        let bead = make_open_bead_with_assignee("nd-stale-open", "dead-worker");
src/strand/mend.rs-2414-
src/strand/mend.rs:2415:        let (store, _release_count, clear_count) = MockBeadStore::new(vec![bead]);
src/strand/mend.rs-2416-        let mend = make_mend_strand(hb_dir.path(), lock_dir.path(), reg_dir.path());
src/strand/mend.rs-2417-
src/strand/mend.rs-2418-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-2419-        assert!(
src/strand/mend.rs-2420-            matches!(result, StrandResult::WorkCreated),
src/strand/mend.rs-2421-            "expected WorkCreated after clearing stale assignee from open bead, got: {result:?}"
--
src/strand/mend.rs-2445-                provider: None,
src/strand/mend.rs-2446-                started_at: Utc::now(),
src/strand/mend.rs-2447-                beads_processed: 5,
src/strand/mend.rs-2448-            })
src/strand/mend.rs-2449-            .unwrap();
src/strand/mend.rs-2450-
src/strand/mend.rs:2451:        let (store, _release_count, clear_count) = MockBeadStore::new(vec![bead]);
src/strand/mend.rs-2452-        let mend = MendStrand::new(
src/strand/mend.rs-2453-            MendConfig::default(),
src/strand/mend.rs-2454-            hb_dir.path().to_path_buf(),
src/strand/mend.rs-2455-            Duration::from_secs(300),
src/strand/mend.rs-2456-            lock_dir.path().to_path_buf(),
src/strand/mend.rs-2457-            "test-worker".to_string(),
--
src/strand/mend.rs-2483-        let lock_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2484-        let reg_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2485-
src/strand/mend.rs-2486-        // An open bead assigned to ourselves (using qualified_id).
src/strand/mend.rs-2487-        let bead = make_open_bead_with_assignee("nd-own-open", "claude-test-worker");
src/strand/mend.rs-2488-
src/strand/mend.rs:2489:        let (store, _release_count, clear_count) = MockBeadStore::new(vec![bead]);
src/strand/mend.rs-2490-        let mend = make_mend_strand(hb_dir.path(), lock_dir.path(), reg_dir.path());
src/strand/mend.rs-2491-
src/strand/mend.rs-2492-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-2493-        assert!(
src/strand/mend.rs-2494-            matches!(result, StrandResult::NoWork),
src/strand/mend.rs-2495-            "expected NoWork when bead is assigned to ourselves, got: {result:?}"
--
src/strand/mend.rs-2519-                provider: None,
src/strand/mend.rs-2520-                started_at: Utc::now(),
src/strand/mend.rs-2521-                beads_processed: 10,
src/strand/mend.rs-2522-            })
src/strand/mend.rs-2523-            .unwrap();
src/strand/mend.rs-2524-
src/strand/mend.rs:2525:        let (store, _release_count, clear_count) = MockBeadStore::new(vec![bead]);
src/strand/mend.rs-2526-        let mend = MendStrand::new(
src/strand/mend.rs-2527-            MendConfig::default(),
src/strand/mend.rs-2528-            hb_dir.path().to_path_buf(),
src/strand/mend.rs-2529-            Duration::from_secs(300),
src/strand/mend.rs-2530-            lock_dir.path().to_path_buf(),
src/strand/mend.rs-2531-            "test-worker".to_string(),
--
src/strand/mend.rs-2572-            dependents: vec![],
src/strand/mend.rs-2573-            comments: vec![],
src/strand/mend.rs-2574-            created_at: dt,
src/strand/mend.rs-2575-            updated_at: dt,
src/strand/mend.rs-2576-        };
src/strand/mend.rs-2577-
src/strand/mend.rs:2578:        let (store, _release_count, clear_count) = MockBeadStore::new(vec![bead]);
src/strand/mend.rs-2579-        let mend = make_mend_strand(hb_dir.path(), lock_dir.path(), reg_dir.path());
src/strand/mend.rs-2580-
src/strand/mend.rs-2581-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-2582-        assert!(
src/strand/mend.rs-2583-            matches!(result, StrandResult::NoWork),
src/strand/mend.rs-2584-            "expected NoWork when bead has no assignee, got: {result:?}"
--
src/strand/mend.rs-2590-
src/strand/mend.rs-2591-    /// When two workers from different adapter pools share a NATO name (e.g.
src/strand/mend.rs-2592-    /// "foxtrot"), their qualified IDs differ (e.g. "glm-5-foxtrot" vs
src/strand/mend.rs-2593-    /// "glm-4_7-foxtrot"). A bead assigned to one must NOT be treated as
src/strand/mend.rs-2594-    /// orphaned just because the other worker of the same NATO name is alive.
src/strand/mend.rs-2595-    #[tokio::test]
src/strand/mend.rs:2596:    async fn collision_same_nato_different_adapter_live_worker_not_released() {
src/strand/mend.rs-2597-        let hb_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2598-        let lock_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2599-        let reg_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2600-
src/strand/mend.rs-2601-        // Two workers share NATO name "foxtrot" but have different adapters.
src/strand/mend.rs-2602-        let glm5_foxtrot = "claude-code-glm-5-foxtrot";
--
src/strand/mend.rs-2630-                started_at: Utc::now(),
src/strand/mend.rs-2631-                beads_processed: 100,
src/strand/mend.rs-2632-            })
src/strand/mend.rs-2633-            .unwrap();
src/strand/mend.rs-2634-
src/strand/mend.rs-2635-        // Run mend as glm-4_7-foxtrot.
src/strand/mend.rs:2636:        let (store, release_count, _) = MockBeadStore::new(vec![bead]);
src/strand/mend.rs-2637-        let mend = MendStrand::new(
src/strand/mend.rs-2638-            MendConfig::default(),
src/strand/mend.rs-2639-            hb_dir.path().to_path_buf(),
src/strand/mend.rs-2640-            Duration::from_secs(300),
src/strand/mend.rs-2641-            lock_dir.path().to_path_buf(),
src/strand/mend.rs-2642-            glm47_foxtrot.to_string(),
--
src/strand/mend.rs-2656-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-2657-        assert!(
src/strand/mend.rs-2658-            matches!(result, StrandResult::NoWork),
src/strand/mend.rs-2659-            "expected NoWork — bead assigned to glm-5-foxtrot should NOT be orphaned, got: {result:?}"
src/strand/mend.rs-2660-        );
src/strand/mend.rs-2661-        assert_eq!(
src/strand/mend.rs:2662:            release_count.load(Ordering::Relaxed),
src/strand/mend.rs-2663-            0,
src/strand/mend.rs:2664:            "bead must not be released when its owner (glm-5-foxtrot) is alive"
src/strand/mend.rs-2665-        );
src/strand/mend.rs-2666-    }
src/strand/mend.rs-2667-
src/strand/mend.rs-2668-    /// When only one worker of a shared NATO name is dead, the bead assigned
src/strand/mend.rs:2669:    /// to the dead worker's qualified ID should be released even though the
src/strand/mend.rs-2670-    /// other worker with the same NATO name is alive.
src/strand/mend.rs-2671-    #[tokio::test]
src/strand/mend.rs:2672:    async fn collision_same_nato_different_adapter_dead_worker_released() {
src/strand/mend.rs-2673-        let hb_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2674-        let lock_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2675-        let reg_dir = tempfile::tempdir().unwrap();
src/strand/mend.rs-2676-
src/strand/mend.rs-2677-        let glm5_foxtrot = "claude-code-glm-5-foxtrot";
src/strand/mend.rs-2678-        let glm47_foxtrot = "claude-code-glm-4_7-foxtrot";
--
src/strand/mend.rs-2705-                started_at: Utc::now(),
src/strand/mend.rs-2706-                beads_processed: 0,
src/strand/mend.rs-2707-            })
src/strand/mend.rs-2708-            .unwrap();
src/strand/mend.rs-2709-
src/strand/mend.rs-2710-        // Run mend as glm-4_7-foxtrot.
src/strand/mend.rs:2711:        let (store, release_count, _) = MockBeadStore::new(vec![bead]);
src/strand/mend.rs-2712-        let mend = MendStrand::new(
src/strand/mend.rs-2713-            MendConfig::default(),
src/strand/mend.rs-2714-            hb_dir.path().to_path_buf(),
src/strand/mend.rs-2715-            Duration::from_secs(300),
src/strand/mend.rs-2716-            lock_dir.path().to_path_buf(),
src/strand/mend.rs-2717-            glm47_foxtrot.to_string(),
--
src/strand/mend.rs-2731-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-2732-        assert!(
src/strand/mend.rs-2733-            matches!(result, StrandResult::WorkCreated),
src/strand/mend.rs-2734-            "expected WorkCreated — bead assigned to dead glm-5-foxtrot should be orphaned, got: {result:?}"
src/strand/mend.rs-2735-        );
src/strand/mend.rs-2736-        assert_eq!(
src/strand/mend.rs:2737:            release_count.load(Ordering::Relaxed),
src/strand/mend.rs-2738-            1,
src/strand/mend.rs:2739:            "bead must be released when its owner (glm-5-foxtrot) is dead"
src/strand/mend.rs-2740-        );
src/strand/mend.rs-2741-    }
src/strand/mend.rs-2742-
src/strand/mend.rs-2743-    // ── Orphaned lock file tests ─────────────────────────────────────────────
src/strand/mend.rs-2744-
src/strand/mend.rs-2745-    #[tokio::test]
--
src/strand/mend.rs-2979-        let blocked_bead = make_bead_with_deps(
src/strand/mend.rs-2980-            "blocked-bead",
src/strand/mend.rs-2981-            BeadStatus::Open,
src/strand/mend.rs-2982-            vec![make_dep("done-blocker", "closed", "blocks")],
src/strand/mend.rs-2983-        );
src/strand/mend.rs-2984-
src/strand/mend.rs:2985:        let (store, release_count, _) = MockBeadStore::new(vec![orphan_bead, blocked_bead]);
src/strand/mend.rs-2986-        let mend = make_mend_strand(hb_dir.path(), lock_dir.path(), reg_dir.path());
src/strand/mend.rs-2987-
src/strand/mend.rs-2988-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-2989-        assert!(
src/strand/mend.rs-2990-            matches!(result, StrandResult::WorkCreated),
src/strand/mend.rs-2991-            "expected WorkCreated after combined cleanup, got: {result:?}"
src/strand/mend.rs-2992-        );
src/strand/mend.rs:2993:        assert_eq!(release_count.load(Ordering::Relaxed), 1);
src/strand/mend.rs-2994-    }
src/strand/mend.rs-2995-
src/strand/mend.rs-2996-    // ── file_age helper test ─────────────────────────────────────────────────
src/strand/mend.rs-2997-
src/strand/mend.rs-2998-    #[test]
src/strand/mend.rs-2999-    fn file_age_returns_some_for_existing_file() {
--
src/strand/mend.rs-3268-            log_dir.path(),
src/strand/mend.rs-3269-            1,
src/strand/mend.rs-3270-        );
src/strand/mend.rs-3271-
src/strand/mend.rs-3272-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-3273-        // The in-progress bead is also "orphaned" by our mock (some-worker isn't
src/strand/mend.rs:3274:        // registered), so mend will release it. The log must survive though.
src/strand/mend.rs-3275-        assert!(
src/strand/mend.rs-3276-            log_path.exists(),
src/strand/mend.rs-3277-            "agent log for in-progress bead must not be deleted"
src/strand/mend.rs-3278-        );
src/strand/mend.rs-3279-        let _ = result; // outcome depends on whether orphan cleanup fires
src/strand/mend.rs-3280-    }
--
src/strand/mend.rs-4671-        }
src/strand/mend.rs-4672-
src/strand/mend.rs-4673-        async fn claim_auto(&self, _actor: &str) -> Result<ClaimResult> {
src/strand/mend.rs-4674-            anyhow::bail!("not implemented in mock")
src/strand/mend.rs-4675-        }
src/strand/mend.rs-4676-
src/strand/mend.rs:4677:        async fn release(&self, _id: &BeadId) -> Result<()> {
src/strand/mend.rs-4678-            anyhow::bail!("not implemented in mock")
src/strand/mend.rs-4679-        }
src/strand/mend.rs-4680-        async fn block(&self, _id: &BeadId) -> Result<()> {
src/strand/mend.rs-4681-            anyhow::bail!("not implemented in mock")
src/strand/mend.rs-4682-        }
src/strand/mend.rs-4683-        async fn flush(&self) -> Result<()> {

exec
/bin/bash -lc "rg -n \"impl Outcome|pub fn classify\" src/types.rs src/outcome/mod.rs && sed -n '480,620p' src/types.rs && sed -n '210,470p' src/outcome/mod.rs && sed -n '470,850p' src/outcome/mod.rs && git diff --name-only && git diff --stat && git status --short --untracked-files=no | tail -n 40 && git show HEAD:src/outcome/mod.rs | sed -n '20,42p' && git show HEAD:src/types.rs | rg -n -C 8 \"pub fn classify\"" in /home/coding/NEEDLE
 exited 2 in 0ms:
rg: src/types.rs: No such file or directory (os error 2)
src/outcome/mod.rs:30:pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
src/outcome/mod.rs:44:impl OutcomeHandler {

exec
/bin/bash -lc "rg -n \"impl Outcome|pub fn classify\" src --glob '*.rs'
sed -n '210,520p' src/outcome/mod.rs
sed -n '520,980p' src/outcome/mod.rs
git diff --name-only
git diff --stat
git status --short --untracked-files=no | tail -n 40
git show HEAD:src/outcome/mod.rs | sed -n '20,42p'
git show HEAD:src/types/mod.rs | rg -n -C 10 \"pub fn classify\"" in /home/coding/NEEDLE
 succeeded in 683ms:
 .beads/bypasses.jsonl                         |    27 +
 .beads/checkpoint/current.json                |    54 +-
 .beads/checkpoint/forensic.jsonl              |   264 +-
 .beads/checkpoint/previous.json               |    54 +-
 .beads/events.jsonl                           |   150 +
 .beads/heartbeats.jsonl                       |   192 +
 .beads/traces/needle-068b926f/metadata.json   |     4 +-
 .beads/traces/needle-068b926f/stdout.txt      |  8608 ++-
 .beads/traces/needle-12433d46/metadata.json   |     4 +-
 .beads/traces/needle-12433d46/stderr.txt      |     2 +
 .beads/traces/needle-12433d46/stdout.txt      | 12580 ++--
 .beads/traces/needle-1ef0ec6e/metadata.json   |     4 +-
 .beads/traces/needle-1ef0ec6e/stdout.txt      | 11683 ++--
 .beads/traces/needle-208840e4/metadata.json   |     4 +-
 .beads/traces/needle-208840e4/stdout.txt      |  5856 +-
 .beads/traces/needle-24e42694/metadata.json   |    12 +-
 .beads/traces/needle-24e42694/stderr.txt      |  5750 +-
 .beads/traces/needle-24e42694/stdout.txt      |  8429 ++-
 .beads/traces/needle-3a6e1ecd/metadata.json   |     4 +-
 .beads/traces/needle-3a6e1ecd/stdout.txt      | 19949 +++++-
 .beads/traces/needle-44a0f603/metadata.json   |     4 +-
 .beads/traces/needle-44a0f603/stdout.txt      | 11942 ++--
 .beads/traces/needle-4b602eed/metadata.json   |    12 +-
 .beads/traces/needle-4b602eed/stderr.txt      |  4592 +-
 .beads/traces/needle-4b602eed/stdout.txt      |  2572 +-
 .beads/traces/needle-4c48325a/metadata.json   |    12 +-
 .beads/traces/needle-4c48325a/stderr.txt      |  5651 +-
 .beads/traces/needle-4c48325a/stdout.txt      | 19916 +++++-
 .beads/traces/needle-51084068/metadata.json   |     4 +-
 .beads/traces/needle-51084068/stdout.txt      |  5851 +-
 .beads/traces/needle-597972f3/metadata.json   |     4 +-
 .beads/traces/needle-597972f3/stdout.txt      | 28791 ++------
 .beads/traces/needle-5c826b55/metadata.json   |    14 +-
 .beads/traces/needle-5c826b55/stdout.txt      |  7500 +--
 .beads/traces/needle-615b57e9/metadata.json   |     4 +-
 .beads/traces/needle-615b57e9/stdout.txt      | 41720 ++++++++----
 .beads/traces/needle-677fcad2/metadata.json   |    14 +-
 .beads/traces/needle-677fcad2/stdout.txt      | 16943 +++--
 .beads/traces/needle-67f3bb56/metadata.json   |     4 +-
 .beads/traces/needle-67f3bb56/stdout.txt      | 63420 +++++++++++++++++-
 .beads/traces/needle-6b4aabcf/metadata.json   |    14 +-
 .beads/traces/needle-6b4aabcf/stdout.txt      | 24218 +++++--
 .beads/traces/needle-6f242187/metadata.json   |     4 +-
 .beads/traces/needle-6f242187/stdout.txt      | 22357 ++++---
 .beads/traces/needle-6f50ee12/metadata.json   |     4 +-
 .beads/traces/needle-6f50ee12/stdout.txt      | 10164 +--
 .beads/traces/needle-791e962b/metadata.json   |     4 +-
 .beads/traces/needle-791e962b/stdout.txt      | 11377 +++-
 .beads/traces/needle-8cb71c7c/metadata.json   |     4 +-
 .beads/traces/needle-8cb71c7c/stderr.txt      |     2 +
 .beads/traces/needle-8cb71c7c/stdout.txt      | 21143 +++---
 .beads/traces/needle-99c51a9d/metadata.json   |     4 +-
 .beads/traces/needle-99c51a9d/stdout.txt      | 10432 ++-
 .beads/traces/needle-b3617592/metadata.json   |     4 +-
 .beads/traces/needle-b3617592/stdout.txt      | 19298 +++---
 .beads/traces/needle-b3dc9203/metadata.json   |    14 +-
 .beads/traces/needle-b3dc9203/stdout.txt      |  7819 +--
 .beads/traces/needle-b93a96bd/metadata.json   |     4 +-
 .beads/traces/needle-b93a96bd/stdout.txt      | 55474 ++++++++--------
 .beads/traces/needle-c0464426/metadata.json   |    14 +-
 .beads/traces/needle-c0464426/stdout.txt      |  9831 ++-
 .beads/traces/needle-c3e2cb9a/metadata.json   |     4 +-
 .beads/traces/needle-c3e2cb9a/stdout.txt      | 31735 ++-------
 .beads/traces/needle-c8c008be/metadata.json   |     4 +-
 .beads/traces/needle-c8c008be/stdout.txt      | 12379 ++--
 .beads/traces/needle-ceea8c53/metadata.json   |     4 +-
 .beads/traces/needle-ceea8c53/stdout.txt      | 17548 +++--
 .beads/traces/needle-cfe0ad63/metadata.json   |     4 +-
 .beads/traces/needle-cfe0ad63/stdout.txt      |  8407 ++-
 .beads/traces/needle-d1b2ee0d/metadata.json   |    14 +-
 .beads/traces/needle-d1b2ee0d/stdout.txt      | 84695 +++++++++++++++++++++---
 .beads/traces/needle-de6bb8ad/metadata.json   |     4 +-
 .beads/traces/needle-de6bb8ad/stdout.txt      | 26105 ++++----
 .beads/traces/needle-e9df85fc/metadata.json   |    14 +-
 .beads/traces/needle-e9df85fc/stdout.txt      |  5789 +-
 .beads/traces/needle-ea26063c/metadata.json   |     4 +-
 .beads/traces/needle-ea26063c/stdout.txt      | 30812 +++++----
 .beads/traces/needle-ea87796e/metadata.json   |     4 +-
 .beads/traces/needle-ea87796e/stdout.txt      | 20066 ++----
 .beads/traces/needle-ebede907/metadata.json   |     4 +-
 .beads/traces/needle-ebede907/stdout.txt      | 46084 +++++--------
 .beads/traces/needle-f78eebbb/metadata.json   |     4 +-
 .beads/traces/needle-f78eebbb/stdout.txt      | 21382 +++---
 .needle-predispatch-sha                       |     2 +-
 .needle.yaml                                  |     2 +-
 install.sh                                    |     2 +-
 src/outcome/mod.rs                            |     4 +-
 tests/installer/test_checksum_verification.sh |   140 +
 tests/integration_tests.rs                    |     1 +
 tests/property_tests.rs                       |    12 +-
 90 files changed, 477596 insertions(+), 302418 deletions(-)
 M .beads/traces/needle-8cb71c7c/stdout.txt
 M .beads/traces/needle-99c51a9d/metadata.json
 M .beads/traces/needle-99c51a9d/stdout.txt
 M .beads/traces/needle-b3617592/metadata.json
 M .beads/traces/needle-b3617592/stdout.txt
 M .beads/traces/needle-b3dc9203/metadata.json
 M .beads/traces/needle-b3dc9203/stdout.txt
 M .beads/traces/needle-b93a96bd/metadata.json
 M .beads/traces/needle-b93a96bd/stdout.txt
 M .beads/traces/needle-c0464426/metadata.json
 M .beads/traces/needle-c0464426/stdout.txt
 M .beads/traces/needle-c3e2cb9a/metadata.json
 M .beads/traces/needle-c3e2cb9a/stdout.txt
 M .beads/traces/needle-c8c008be/metadata.json
 M .beads/traces/needle-c8c008be/stdout.txt
 M .beads/traces/needle-ceea8c53/metadata.json
 M .beads/traces/needle-ceea8c53/stdout.txt
 M .beads/traces/needle-cfe0ad63/metadata.json
 M .beads/traces/needle-cfe0ad63/stdout.txt
 M .beads/traces/needle-d1b2ee0d/metadata.json
 M .beads/traces/needle-d1b2ee0d/stdout.txt
 M .beads/traces/needle-de6bb8ad/metadata.json
 M .beads/traces/needle-de6bb8ad/stdout.txt
 M .beads/traces/needle-e9df85fc/metadata.json
 M .beads/traces/needle-e9df85fc/stdout.txt
 M .beads/traces/needle-ea26063c/metadata.json
 M .beads/traces/needle-ea26063c/stdout.txt
 M .beads/traces/needle-ea87796e/metadata.json
 M .beads/traces/needle-ea87796e/stdout.txt
 M .beads/traces/needle-ebede907/metadata.json
 M .beads/traces/needle-ebede907/stdout.txt
 M .beads/traces/needle-f78eebbb/metadata.json
 M .beads/traces/needle-f78eebbb/stdout.txt
 M .needle-predispatch-sha
 M .needle.yaml
 M install.sh
 M src/outcome/mod.rs
 M tests/installer/test_checksum_verification.sh
 M tests/integration_tests.rs
 M tests/property_tests.rs
use crate::types::{AgentOutcome, Bead, BeadAction, HandlerResult, Outcome};
use crate::validation::{verify_shipped_work, GateConfig, GateReport, ValidationGate};

// ──────────────────────────────────────────────────────────────────────────────
// classify (convenience re-export)
// ──────────────────────────────────────────────────────────────────────────────

/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
///
/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
    Outcome::classify(exit_code, was_interrupted, verified)
}

// ──────────────────────────────────────────────────────────────────────────────
// OutcomeHandler
// ──────────────────────────────────────────────────────────────────────────────

/// Routes agent outcomes to their explicit handlers.
pub struct OutcomeHandler {
    config: Config,
    telemetry: Telemetry,
}
236-    ///
237-    /// # Mapping (per spec)
238-    /// - `was_interrupted=true` → `Interrupted` (checked first)
239-    /// - exit 0 → `Success`
240-    /// - exit 1 → `Failure`
241-    /// - exit 124 → `Timeout`
242-    /// - exit 127 → `AgentNotFound`
243-    /// - exit >128 → `Crash(exit_code)`
244-    /// - exit <0 → `Crash(exit_code)`
245-    /// - all other → `Failure`
246:    pub fn classify(exit_code: i32, was_interrupted: bool) -> Self {
247-        // Interrupted flag takes precedence (graceful shutdown path).
248-        if was_interrupted {
249-            return Outcome::Interrupted;
250-        }
251-
252-        // Explicit mapping for every exit code range — NO wildcards.
253-        // Each range is explicitly enumerated to ensure compile errors
254-        // if a new Outcome variant is added without updating this match.
255-        match exit_code {
256-            // Success
--
3608-///
3609-/// # Examples
3610-///
3611-/// ```
3612-/// use needle::types::{classify_error_code, ErrorCategory};
3613-///
3614-/// assert_eq!(classify_error_code("E0308"), ErrorCategory::TypeMismatch);
3615-/// assert_eq!(classify_error_code("E0382"), ErrorCategory::BorrowChecker);
3616-/// assert_eq!(classify_error_code("E9999"), ErrorCategory::Unknown);
3617-/// ```
3618:pub fn classify_error_code(code: &str) -> ErrorCategory {
3619-    ERROR_CODE_CATEGORIES
3620-        .iter()
3621-        .find_map(|(known_code, category)| (*known_code == code).then_some(*category))
3622-        .unwrap_or(ErrorCategory::Unknown)
3623-}
3624-
3625-const ERROR_CODE_CATEGORIES: &[(&str, ErrorCategory)] = &[
3626-    // Keep the mapping explicit. In particular, do not classify unassigned
3627-    // E0807+ values by range; rustc does not emit those error codes.
3628-    // Type mismatch errors

exec
/bin/bash -lc "git log --oneline --decorate --all -- src/outcome/mod.rs | head -n 20
git log -p -5 -- src/outcome/mod.rs | sed -n '1,360p'
git blame -L 20,38 HEAD -- src/outcome/mod.rs
git blame -L 225,270 HEAD -- src/types/mod.rs
stat -c '%y %n' src/outcome/mod.rs tests/property_tests.rs tests/integration_tests.rs
rg -n 'needle-7e9ea193|needle-3386daef|classification|verification' .beads/checkpoint/forensic.jsonl .beads/events.jsonl | tail -n 80" in /home/coding/NEEDLE
 succeeded in 0ms:
81cebd6 fix(needle-092b40ea): harden install.sh checksum verification
9a71057 fix(outcome): correct ValidationGate option handling
7a63762 fix(needle-3386daef): restore complete orphan branch postcondition enforcement
8c64f77 style(needle-0cd93f10): apply cargo fmt formatting fixes
96e691e feat(needle-3386daef): enforce dispatch postcondition - release orphaned beads
44628f7 fix(bf-3lr72): route predispatch through bead store
6e317e9 test(needle-bf-5f2s6): verify full test suite passes with HOME isolation fixes
5a9e629 fix(validation): make the shipped-work gate actually able to fail
fe32114 fix(needle): wire the failure-quarantine circuit breaker, add failure-aware Pluck ordering
bb45aab feat(bf-1i9): enforce shipped work before accepting bead closure
27437fb fix(bf-3saat): gate commands are now genuinely killed on timeout, not just reported late
4d8820f test(bf-3pc1r): fix outcome-timeout test to use a genuinely-async slow op
e97e88a fix: harden gates/spawn/bead-status per external adopter (GH #7-#11)
e5b32cb feat(needle-bf-4qyh6): implement load-adaptive launch stagger for --count=N batch launches
82404c6 feat(needle-bf-5rhtq): extend mend to clear stale assignees on open beads
62541cb feat(bf-6c8vp): add has_valid_store check to knot strand
714d347 fix(outcome): flush JSONL after every success outcome
4d5eb64 docs(needle-bf-4hq): update Property 3 comment to reference real concurrent test
2ba6164 feat(needle-ry21): complete OTLP sink implementation for OpenTelemetry export
afb9927 feat(needle-ry21): complete OTLP sink implementation for OpenTelemetry export
commit 9a71057a89ff73f237883992f1249a43609b6717
Author: jedarden <github@jedarden.com>
Date:   Thu Aug 20 01:24:39 2026 -0400

    fix(outcome): correct ValidationGate option handling
    
    ValidationGate::new() and from_commands_with_stderr_cap() already return
    Option<Self>, so wrapping them in Some() created a double Option.
    Remove the Some() wrapper and use ok_or_else() for proper error handling.
    
    Also fix test calls to Outcome::classify to include the verified parameter
    that was added to the function signature.
    
    Fixes compilation error: no method named 'run' found for Option<ValidationGate>.

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index b799ec7..996c217 100644
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -27,8 +27,8 @@ use crate::validation::{verify_shipped_work, GateConfig, GateReport, ValidationG
 /// Classify an agent exit code into an `Outcome`, with shutdown signal support.
 ///
 /// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    Outcome::classify(exit_code, was_interrupted, verified)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -195,11 +195,60 @@ impl OutcomeHandler {
         Ok(events)
     }
 
+    /// Run verification gates for a bead, returning whether all passed.
+    ///
+    /// This is extracted as a helper so it can be called BEFORE outcome classification.
+    /// Verification must determine Success, not just exit code 0.
+    async fn run_verification_gates(&self, bead: &Bead) -> Result<(bool, Option<GateReport>)> {
+        // Try pluggable gates first, fall back to legacy verification commands.
+        let gate_opt = if !self.config.gates.is_empty() {
+            // New pluggable gate system. Fill in each command gate's stderr
+            // cap from `validation.stderr_cap_bytes` unless the gate already
+            // set its own override — see GitHub issue jedarden/NEEDLE#9.
+            let default_stderr_cap = self.config.validation.stderr_cap_bytes;
+            let gate_configs: Vec<(String, GateConfig)> = self
+                .config
+                .gates
+                .iter()
+                .enumerate()
+                .map(|(i, config)| {
+                    let mut config = config.clone();
+                    let GateConfig::Command {
+                        stderr_cap_bytes, ..
+                    } = &mut config;
+                    if stderr_cap_bytes.is_none() {
+                        *stderr_cap_bytes = Some(default_stderr_cap);
+                    }
+                    (format!("gate_{}", i), config)
+                })
+                .collect();
+            ValidationGate::new(gate_configs, bead.workspace.clone())
+        } else if !self.config.verification.is_empty() {
+            // Legacy verification command format.
+            ValidationGate::from_commands_with_stderr_cap(
+                self.config.verification.clone(),
+                bead.workspace.clone(),
+                self.config.validation.stderr_cap_bytes,
+            )
+        } else {
+            // No gates configured — treat as verified (backward compatible).
+            return Ok((true, None));
+        };
+
+        // Run the gate and return the result.
+        let gate = gate_opt.ok_or_else(|| {
+            anyhow::anyhow!("validation gate creation failed - all gates failed to initialize")
+        })?;
+        let report = gate.run(bead).await?;
+        let all_passed = report.all_passed;
+        Ok((all_passed, Some(report)))
+    }
+
     /// Handle a process output for the given bead.
     ///
-    /// Uses `classify()` to determine the outcome, then dispatches to the
-    /// per-outcome handler. Every `Outcome` variant has an explicit arm —
-    /// no wildcards. Returns a `HandlerResult` describing what happened.
+    /// CRITICAL: For exit code 0, verification gates run BEFORE classification.
+    /// This ensures Success means verification passed, not just that the agent exited 0.
+    /// An agent that exits 0 but fails verification produces Failure, not Success.
     #[tracing::instrument(
         name = "bead.outcome",
         skip(self, store, bead, output),
@@ -216,7 +265,16 @@ impl OutcomeHandler {
         output: &AgentOutcome,
         was_interrupted: bool,
     ) -> Result<HandlerResult> {
-        let outcome = classify(output.exit_code, was_interrupted);
+        // For exit code 0, run verification BEFORE classification.
+        // This is the core fix: Success must mean verification passed.
+        let (verified, gate_report) = if output.exit_code == 0 && !was_interrupted {
+            self.run_verification_gates(bead).await?
+        } else {
+            // Non-zero exit or interrupted — verification irrelevant.
+            (true, None)
+        };
+
+        let outcome = classify(output.exit_code, was_interrupted, verified);
 
         // Set outcome as span attribute
         tracing::Span::current().record("needle.outcome", outcome.as_str());
@@ -224,6 +282,7 @@ impl OutcomeHandler {
         tracing::info!(
             bead_id = %bead.id,
             exit_code = output.exit_code,
+            verified,
             outcome = %outcome,
             "handling agent outcome"
         );
@@ -237,8 +296,19 @@ impl OutcomeHandler {
         });
 
         let (bead_action, telemetry_events) = match outcome.clone() {
-            Outcome::Success => self.handle_success(store, bead).await?,
-            Outcome::Failure => self.handle_failure(store, bead).await?,
+            Outcome::Success => self.handle_success(store, bead, gate_report).await?,
+            Outcome::Failure => {
+                // If we have a gate report with failures, handle as gate failure.
+                if let Some(report) = gate_report {
+                    if !report.all_passed {
+                        self.handle_gate_failure(store, bead, &report).await?
+                    } else {
+                        self.handle_failure(store, bead).await?
+                    }
+                } else {
+                    self.handle_failure(store, bead).await?
+                }
+            }
             Outcome::Timeout => self.handle_timeout(store, bead).await?,
             Outcome::AgentNotFound => self.handle_agent_not_found(store, bead).await?,
             Outcome::Interrupted => self.handle_interrupted(store, bead).await?,
@@ -296,8 +366,9 @@ impl OutcomeHandler {
                 "outcome handler cancelled before starting, returning early"
             );
             // Return a default HandlerResult to allow the worker to recover.
+            // Verification never ran, so conservatively treat as unverified.
             return Ok(HandlerResult {
-                outcome: classify(output.exit_code, was_interrupted),
+                outcome: classify(output.exit_code, was_interrupted, false),
                 bead_action: BeadAction::None,
                 telemetry_events: vec![],
             });
@@ -339,7 +410,7 @@ impl OutcomeHandler {
                 // Emit a timeout event for observability.
                 let _ = telemetry.emit(EventKind::WorkerHandlingTimeout {
                     bead_id: bead_id.clone(),
-                    outcome: classify(output.exit_code, was_interrupted)
+                    outcome: classify(output.exit_code, was_interrupted, false)
                         .as_str()
                         .to_string(),
                     operation: "handle".to_string(),
@@ -347,8 +418,9 @@ impl OutcomeHandler {
                 });
                 // Return a default HandlerResult to allow the worker to recover.
                 // The worker's 60-second timeout wrapper will handle best-effort release.
+                // Verification never ran, so conservatively treat as unverified.
                 Ok(HandlerResult {
-                    outcome: classify(output.exit_code, was_interrupted),
+                    outcome: classify(output.exit_code, was_interrupted, false),
                     bead_action: BeadAction::Released,
                     telemetry_events: vec![EventKind::BeadReleased {
                         bead_id,
@@ -359,12 +431,15 @@ impl OutcomeHandler {
         }
     }
 
-    /// Success: run validation gates (if configured), then verify bead closure.
+    /// Success: verification already passed, now verify bead closure.
+    ///
+    /// CRITICAL: This is only called when verification PASSED.
+    /// If verification failed, classification produces Failure and handle_failure
+    /// is called instead. This ensures Success means verification passed.
     ///
     /// Flow:
-    /// 1. If validation gates are configured (new or legacy format), run them.
-    /// 2. If any gate fails: reopen the bead (if agent closed it) and release it.
-    /// 3. If all gates pass (or none configured): check if agent closed the bead.
+    /// 1. If gates ran, emit VerificationPassed telemetry.
+    /// 2. Check if agent closed the bead.
     ///    - Closed → emit BeadCompleted.
     ///    - Still open → emit BeadOrphaned warning.
     ///
@@ -373,51 +448,12 @@ impl OutcomeHandler {
         &self,
         store: &dyn BeadStore,
         bead: &Bead,
+        gate_report: Option<GateReport>,
     ) -> Result<(BeadAction, Vec<EventKind>)> {
         tracing::info!(bead_id = %bead.id, "agent completed successfully");
 
-        // Try pluggable gates first, fall back to legacy verification commands.
-        let gate = if !self.config.gates.is_empty() {
-            // New pluggable gate system. Fill in each command gate's stderr
-            // cap from `validation.stderr_cap_bytes` unless the gate already
-            // set its own override — see GitHub issue jedarden/NEEDLE#9.
-            let default_stderr_cap = self.config.validation.stderr_cap_bytes;
-            let gate_configs: Vec<(String, GateConfig)> = self
-                .config
-                .gates
-                .iter()
-                .enumerate()
-                .map(|(i, config)| {
-                    let mut config = config.clone();
-                    let GateConfig::Command {
-                        stderr_cap_bytes, ..
-                    } = &mut config;
-                    if stderr_cap_bytes.is_none() {
-                        *stderr_cap_bytes = Some(default_stderr_cap);
-                    }
-                    (format!("gate_{}", i), config)
-                })
-                .collect();
-            ValidationGate::new(gate_configs, bead.workspace.clone())
-        } else if !self.config.verification.is_empty() {
-            // Legacy verification command format.
-            ValidationGate::from_commands_with_stderr_cap(
-                self.config.verification.clone(),
-                bead.workspace.clone(),
-                self.config.validation.stderr_cap_bytes,
-            )
-        } else {
-            None
-        };
-
-        if let Some(gate) = gate {
-            let report = gate.run(bead).await?;
-
-            if !report.all_passed {
-                return self.handle_gate_failure(store, bead, &report).await;
-            }
-
-            // All gates passed — emit telemetry.
+        // If gates ran and passed, emit telemetry.
+        if let Some(report) = gate_report {
             let gates_run = report.results.len() as u32;
             self.telemetry.emit(EventKind::VerificationPassed {
                 bead_id: bead.id.clone(),
@@ -1515,34 +1551,57 @@ mod tests {
 
     #[test]
     fn classify_was_interrupted_always_returns_interrupted() {
-        assert_eq!(classify(0, true), Outcome::Interrupted);
-        assert_eq!(classify(1, true), Outcome::Interrupted);
-        assert_eq!(classify(127, true), Outcome::Interrupted);
+        assert_eq!(classify(0, true, true), Outcome::Interrupted);
+        assert_eq!(classify(1, true, false), Outcome::Interrupted);
+        assert_eq!(classify(127, true, true), Outcome::Interrupted);
     }
 
     #[test]
-    fn classify_not_interrupted_uses_exit_code() {
-        assert_eq!(classify(0, false), Outcome::Success);
-        assert_eq!(classify(1, false), Outcome::Failure);
-        assert_eq!(classify(124, false), Outcome::Timeout);
-        assert_eq!(classify(127, false), Outcome::AgentNotFound);
-        assert_eq!(classify(129, false), Outcome::Crash(129));
+    fn classify_not_interrupted_uses_exit_code_and_verification() {
+        // Exit code 0 with verification passes → Success
+        assert_eq!(classify(0, false, true), Outcome::Success);
+        // Exit code 0 with verification fails → Failure
+        assert_eq!(classify(0, false, false), Outcome::Failure);
+        // Non-zero exit codes always fail regardless of verification
+        assert_eq!(classify(1, false, true), Outcome::Failure);
+        assert_eq!(classify(1, false, false), Outcome::Failure);
+        assert_eq!(classify(124, false, true), Outcome::Timeout);
+        assert_eq!(classify(127, false, true), Outcome::AgentNotFound);
+        assert_eq!(classify(129, false, true), Outcome::Crash(129));
     }
 
     #[test]
     fn classify_no_wildcard_arms() {
         // Verify key exit codes map correctly per spec.
-        assert_eq!(classify(0, false), Outcome::Success);
-        assert_eq!(classify(1, false), Outcome::Failure);
-        assert_eq!(classify(2, false), Outcome::Failure);
-        assert_eq!(classify(99, false), Outcome::Failure);
-        assert_eq!(classify(100, false), Outcome::Failure);
-        assert_eq!(classify(124, false), Outcome::Timeout);
-        assert_eq!(classify(125, false), Outcome::Failure);
-        assert_eq!(classify(128, false), Outcome::Failure); // not >128 per spec
-        assert_eq!(classify(129, false), Outcome::Crash(129));
-        assert_eq!(classify(137, false), Outcome::Crash(137));
-        assert_eq!(classify(-9, false), Outcome::Crash(-9));
+        // Exit code 0 ONLY succeeds when verification passes
+        assert_eq!(classify(0, false, true), Outcome::Success);
+        assert_eq!(classify(0, false, false), Outcome::Failure);
+        assert_eq!(classify(1, false, true), Outcome::Failure);
+        assert_eq!(classify(2, false, true), Outcome::Failure);
+        assert_eq!(classify(99, false, true), Outcome::Failure);
+        assert_eq!(classify(100, false, true), Outcome::Failure);
+        assert_eq!(classify(124, false, true), Outcome::Timeout);
+        assert_eq!(classify(125, false, true), Outcome::Failure);
+        assert_eq!(classify(128, false, true), Outcome::Failure); // not >128 per spec
+        assert_eq!(classify(129, false, true), Outcome::Crash(129));
+        assert_eq!(classify(137, false, true), Outcome::Crash(137));
+        assert_eq!(classify(-9, false, true), Outcome::Crash(-9));
+    }
+
+    #[test]
+    fn classify_verification_gate() {
+        // The core fix: exit code 0 does NOT guarantee Success.
+        // Verification is the gate.
+        assert_eq!(
+            classify(0, false, false),
+            Outcome::Failure,
+            "exit_code=0, verified=false must return Failure"
+        );
+        assert_eq!(
+            classify(0, false, true),
+            Outcome::Success,
+            "exit_code=0, verified=true must return Success"
+        );
     }
 
     // ── handle tests ──

commit 7a6376230ded1492e1cea4d8a320871c7f14bfc4
Author: jedarden <github@jedarden.com>
Date:   Mon Aug 17 13:51:00 2026 -0400

    fix(needle-3386daef): restore complete orphan branch postcondition enforcement
    
    Restore the full fix from commit 96e691e that was partially reverted in
    commit 8c64f77 (cargo fmt). The fix enforces the dispatch postcondition:
    a worker never leaves a bead in_progress - every dispatch path returns the
    bead in a terminal state (closed, open/unassigned, or errored+failure-counted).
    
    CHANGES
    1. Orphan branch (handle_success): Use verify_shipped_work to distinguish
       between shipped work (mark completed, release) vs. not-shipped (release
       and increment failure count for quarantine). This restores the close-vs-
       release logic and increment_failure_count calls that were deleted.
    2. Show() timeout path: Release the bead to enforce postcondition instead
       of just emitting telemetry. This fixes one of the two leak paths recorded
       in audit commit 63b01b8.
    3. Show() error path: Release the bead to enforce postcondition instead of
       just emitting telemetry. This fixes the second leak path.
    
    LIVE EVIDENCE -- This fix prevents the 2026-08-17 loop where needle-55ec0193
    was worked THREE times and leaked every time:
      02:52 claim -> 02:58 complete outcome=success (6min)   worker needle-otlp-test
      04:49 claim -> 04:59 timeout                (10min)    worker seam-2
      05:04 claim -> 05:35 complete outcome=success (32min)  worker luna-needle
      05:35 re-claimed by luna-needle in the SAME SECOND as its own success
    
    ~48 minutes of agent time, zero commits referencing the bead, and it never
    left in_progress. The postcondition enforcement prevents this class of leak.
    
    Co-Authored-By: Claude <noreply@anthropic.com>
9d13b657 (jedarden 2026-03-21 11:39:51 -0400 20) use crate::types::{AgentOutcome, Bead, BeadAction, HandlerResult, Outcome};
bb45aab1 (jedarden 2026-07-30 12:59:52 -0400 21) use crate::validation::{verify_shipped_work, GateConfig, GateReport, ValidationGate};
f703759f (jedarden 2026-03-21 02:26:42 -0400 22) 
f703759f (jedarden 2026-03-21 02:26:42 -0400 23) // ──────────────────────────────────────────────────────────────────────────────
1962b94d (jedarden 2026-03-21 02:54:28 -0400 24) // classify (convenience re-export)
f703759f (jedarden 2026-03-21 02:26:42 -0400 25) // ──────────────────────────────────────────────────────────────────────────────
f703759f (jedarden 2026-03-21 02:26:42 -0400 26) 
f703759f (jedarden 2026-03-21 02:26:42 -0400 27) /// Classify an agent exit code into an `Outcome`, with shutdown signal support.
f703759f (jedarden 2026-03-21 02:26:42 -0400 28) ///
f703759f (jedarden 2026-03-21 02:26:42 -0400 29) /// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
9a71057a (jedarden 2026-08-20 01:24:39 -0400 30) pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
9a71057a (jedarden 2026-08-20 01:24:39 -0400 31)     Outcome::classify(exit_code, was_interrupted, verified)
f703759f (jedarden 2026-03-21 02:26:42 -0400 32) }
f703759f (jedarden 2026-03-21 02:26:42 -0400 33) 
f703759f (jedarden 2026-03-21 02:26:42 -0400 34) // ──────────────────────────────────────────────────────────────────────────────
f703759f (jedarden 2026-03-21 02:26:42 -0400 35) // OutcomeHandler
f703759f (jedarden 2026-03-21 02:26:42 -0400 36) // ──────────────────────────────────────────────────────────────────────────────
66be30c1 (jedarden 2026-03-21 01:26:39 -0400 37) 
66be30c1 (jedarden 2026-03-21 01:26:39 -0400 38) /// Routes agent outcomes to their explicit handlers.
2dc1a64d (jedarden 2026-03-21 01:31:03 -0400 225)     /// Exit 130 (SIGINT) or 143 (SIGTERM) — agent was interrupted.
2dc1a64d (jedarden 2026-03-21 01:31:03 -0400 226)     Interrupted,
3ed27e54 (jedarden 2026-03-21 01:35:51 -0400 227)     /// Negative exit code — process crashed or was killed by a signal.
3ed27e54 (jedarden 2026-03-21 01:35:51 -0400 228)     Crash(i32),
2dc1a64d (jedarden 2026-03-21 01:31:03 -0400 229) }
2dc1a64d (jedarden 2026-03-21 01:31:03 -0400 230) 
2dc1a64d (jedarden 2026-03-21 01:31:03 -0400 231) impl Outcome {
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 232)     /// Classify an exit code into an `Outcome` variant.
2dc1a64d (jedarden 2026-03-21 01:31:03 -0400 233)     ///
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 234)     /// Every exit code range has an explicit match arm — no wildcards.
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 235)     /// The `was_interrupted` flag takes precedence over exit code analysis.
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 236)     ///
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 237)     /// # Mapping (per spec)
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 238)     /// - `was_interrupted=true` → `Interrupted` (checked first)
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 239)     /// - exit 0 → `Success`
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 240)     /// - exit 1 → `Failure`
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 241)     /// - exit 124 → `Timeout`
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 242)     /// - exit 127 → `AgentNotFound`
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 243)     /// - exit >128 → `Crash(exit_code)`
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 244)     /// - exit <0 → `Crash(exit_code)`
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 245)     /// - all other → `Failure`
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 246)     pub fn classify(exit_code: i32, was_interrupted: bool) -> Self {
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 247)         // Interrupted flag takes precedence (graceful shutdown path).
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 248)         if was_interrupted {
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 249)             return Outcome::Interrupted;
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 250)         }
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 251) 
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 252)         // Explicit mapping for every exit code range — NO wildcards.
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 253)         // Each range is explicitly enumerated to ensure compile errors
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 254)         // if a new Outcome variant is added without updating this match.
3ed27e54 (jedarden 2026-03-21 01:35:51 -0400 255)         match exit_code {
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 256)             // Success
2dc1a64d (jedarden 2026-03-21 01:31:03 -0400 257)             0 => Outcome::Success,
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 258)             // Explicit failure code
2dc1a64d (jedarden 2026-03-21 01:31:03 -0400 259)             1 => Outcome::Failure,
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 260)             // Timeout (GNU timeout exit code)
2dc1a64d (jedarden 2026-03-21 01:31:03 -0400 261)             124 => Outcome::Timeout,
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 262)             // Agent not found (shell exit code for missing command)
3ed27e54 (jedarden 2026-03-21 01:35:51 -0400 263)             127 => Outcome::AgentNotFound,
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 264)             // Failure range: 2-123
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 265)             2..=123 => Outcome::Failure,
1962b94d (jedarden 2026-03-21 02:54:28 -0400 266)             // Failure: 125-128 (not >128 per spec, so 128 is not Crash)
1962b94d (jedarden 2026-03-21 02:54:28 -0400 267)             125..=128 => Outcome::Failure,
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 268)             // Signal exits: >128 (128 + signal number)
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 269)             // These are all crashes per the spec.
b57ab63f (jedarden 2026-03-21 02:28:33 -0400 270)             129..=i32::MAX => Outcome::Crash(exit_code),
2026-08-21 00:14:51.943837053 -0400 src/outcome/mod.rs
2026-08-21 00:14:51.947837036 -0400 tests/property_tests.rs
2026-08-21 00:14:51.947837036 -0400 tests/integration_tests.rs
.beads/checkpoint/forensic.jsonl:3301:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2153,"issue_id":"needle-85488dd4","kind":"closed","actor":"system","time":"2026-08-16T14:47:45.313144353Z","detail":{"prior_base_status":"in_progress","reason":"Implemented and enabled shipped commit verification gate. Created scripts/verify-shipped-commit.sh that builds HEAD in an isolated detached worktree (immune to sibling workers' uncommitted edits), then updated .needle.yaml to enable the gate with outcome_timeout_seconds raised to 300. Verified end-to-end: current HEAD passes the gate, a deliberately broken commit fails correctly with exit code 101. The gate will now automatically reopen and release any bead closed with a non-compiling commit, preventing the systemic premature closure pattern seen in needle-04df9025 and needle-ea87796e."}}}
.beads/checkpoint/forensic.jsonl:3308:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2160,"issue_id":"needle-6123129c","kind":"closed","actor":"system","time":"2026-08-16T14:57:27.862111481Z","detail":{"prior_base_status":"in_progress","reason":"All verification complete: cargo fmt successful, cargo check passed, all 3 subprocess sites (CLI, Canary, Supervisor) now use retry wrapper, original semantics preserved with ETXTBSY-specific retry logic"}}}
.beads/checkpoint/forensic.jsonl:3328:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2180,"issue_id":"needle-e56fa055","kind":"closed","actor":"system","time":"2026-08-16T15:33:54.32733951Z","detail":{"prior_base_status":"in_progress","reason":"Cargo.lock is already tracked and not in .gitignore - verification confirmed no action needed"}}}
.beads/checkpoint/forensic.jsonl:3512:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2364,"issue_id":"needle-93a28495","kind":"closed","actor":"system","time":"2026-08-16T19:42:10.412432125Z","detail":{"prior_base_status":"in_progress","reason":"Investigation complete: Cannot verify shim behavior as requested tools are not installed. The NEEDLE workspace has been migrated to bead-rs backend and only has `bead` (0.1.3) installed. `bead --version` outputs \"bead 0.1.3\" and `bead --help` describes it as \"bead-rs\". The requested `bf` (0.4.1 shim) and `br` (0.1.28) tools do not exist on this system. This verification would need to be performed in a bead-forge workspace or environment where those tools are present."}}}
.beads/checkpoint/forensic.jsonl:3531:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2383,"issue_id":"needle-e3ecf7cf","kind":"closed","actor":"system","time":"2026-08-16T20:26:36.588852532Z","detail":{"prior_base_status":"in_progress","reason":"Complete audit inventory delivered to bead body. Identified 13 spawn() sites total: 7 already protected with ETXTBSY retry wrappers, 4 need protection (bead_store/mod.rs:128, strand/weave.rs:641, telemetry/mod.rs:2781, strand/resolve.rs:338), 2 are thread spawns (N/A). Categorized by risk level (high: upgrade, medium: CLI execution, low: installed tools) and complexity (simple: 3 sites, complex: 1 site with unsafe pre_exec). No file changes - verification-only work."}}}
.beads/checkpoint/forensic.jsonl:3583:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2435,"issue_id":"needle-753f338e","kind":"closed","actor":"system","time":"2026-08-16T21:56:50.43465195Z","detail":{"prior_base_status":"in_progress","reason":"Successfully pushed test-breaking change 6fca653 to origin/main and triggered needle-ci workflow 'needle-ci-manual-bq84s' on iad-ci. Workflow completed with phase 'Failed' as expected (deliberate test failure for CI verification)."}}}
.beads/checkpoint/forensic.jsonl:3632:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2484,"issue_id":"needle-b8a82ac2","kind":"closed","actor":"system","time":"2026-08-16T23:23:06.759323536Z","detail":{"prior_base_status":"in_progress","reason":"Verification step structure successfully added to resolver module. Implementation includes:\n- verify_binary_identity() function with stub implementation returning Ok(())\n- VerificationError enum with VerificationFailed and NotSupported variants\n- Verification called after successful resolution in resolve() method\n- Safe fallback to Retry with DifferentApproach strategy on verification failure\n- Comprehensive test coverage including stub behavior and error display tests"}}}
.beads/checkpoint/forensic.jsonl:3920:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2772,"issue_id":"needle-ea87796e","kind":"closed","actor":"system","time":"2026-08-17T05:20:48.979245685Z","detail":{"prior_base_status":"in_progress","reason":"Implemented checkpoint commit fix with dynamic active root resolution:\n\n1. Created scripts/commit-checkpoint.sh that reads current.json and previous.json,\n   extracts their active_root paths, and stages only those two objects (not superseded\n   ones listed in deleted_paths).\n\n2. Fixed .beads/checkpoint/.gitignore to remove overly-broad gen-*.jsonl pattern that\n   was blocking all generation objects from being tracked.\n\n3. Committed active root objects gen-25702af29089e2d7d126b4f816c7e904.jsonl (current)\n   and gen-fd06f87c35b4fd76b384bbd6a03157b8.jsonl (previous) alongside their pointers.\n\n4. Added docs/checkpoint-commit-workflow.md documenting the proper workflow.\n\nVerification: bead doctor passes without 'Root object file missing' integrity error.\nFresh clones will now have both pointer files AND the objects they reference,\nallowing checkpoint verification to succeed."}}}
.beads/checkpoint/forensic.jsonl:3941:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2793,"issue_id":"needle-0e52b812","kind":"closed","actor":"system","time":"2026-08-17T05:29:14.494502393Z","detail":{"prior_base_status":"in_progress","reason":"Default Anthropic routing, fallback adapter, integration coverage, and June 15 policy documentation were already present on main; no scoped file changes were required. Focused verification was blocked by an unrelated concurrent compile error in src/outcome/mod.rs."}}}
.beads/checkpoint/forensic.jsonl:3980:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2832,"issue_id":"needle-3386daef","kind":"claimed","actor":"claude-code-glm-5-needle-otlp-test","time":"2026-08-17T05:45:13.894415629Z","detail":{"policy":"fifo-v1","resulting_base_status":"in_progress","with_lease":false}}}
.beads/checkpoint/forensic.jsonl:3989:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2841,"issue_id":"needle-de6bb8ad","kind":"closed","actor":"system","time":"2026-08-17T05:47:59.416733375Z","detail":{"prior_base_status":"in_progress","reason":"Added telemetry events for upgrade check attempts and outcomes. The implementation includes:\n\n1. UpgradeCheckStarted - emitted when an upgrade check begins with source context\n2. UpgradeCheckCompleted - emitted on successful checks with version information (current_version, latest_version, update_available, has_release_notes)\n3. UpgradeCheckFailed - emitted on errors with classified error types (network, parse, api, unknown)\n\nModified files:\n- src/upgrade/mod.rs: Added check_for_update_with_telemetry() and download_to_testing_channel_with_telemetry() functions that emit telemetry events\n- src/cli/mod.rs: Updated cmd_upgrade() and background update check to use telemetry-enabled functions\n\nThe implementation follows existing telemetry patterns in the codebase and includes proper error classification for better debugging and monitoring."}}}
.beads/checkpoint/forensic.jsonl:3991:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2843,"issue_id":"needle-fae23441","kind":"closed","actor":"system","time":"2026-08-17T05:48:07.88772938Z","detail":{"prior_base_status":"in_progress","reason":"Completed the verification audit and documented all missing/inconsistent Explore isolation comments, exact current file:line references, stale checklist classifications, and template-style inconsistencies in the bead notes. No source or documentation files were changed."}}}
.beads/checkpoint/forensic.jsonl:4015:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2867,"issue_id":"needle-3386daef","kind":"released","actor":"system","time":"2026-08-17T05:53:35.747064264Z","detail":{"prior_assignee":"claude-code-glm-5-needle-otlp-test","resulting_base_status":"open"}}}
.beads/checkpoint/forensic.jsonl:4018:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2870,"issue_id":"needle-3386daef","kind":"updated","actor":"system","time":"2026-08-17T05:53:49.520539205Z","detail":{}}}
.beads/checkpoint/forensic.jsonl:4029:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2881,"issue_id":"needle-3386daef","kind":"released","actor":"system","time":"2026-08-17T06:03:50.150458342Z","detail":{"prior_assignee":"claude-code-glm-4.7-icg-1","resulting_base_status":"open"}}}
.beads/checkpoint/forensic.jsonl:4041:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2893,"issue_id":"needle-3386daef","kind":"claimed","actor":"codex-gpt-5.6-terra-xhigh-terra-needle","time":"2026-08-17T06:08:39.404317603Z","detail":{"policy":"fifo-v1","resulting_base_status":"in_progress","with_lease":false}}}
.beads/checkpoint/forensic.jsonl:4094:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2946,"issue_id":"needle-0ef20bf6","kind":"closed","actor":"system","time":"2026-08-17T06:43:48.715705131Z","detail":{"prior_base_status":"in_progress","reason":"No ETXTBSY retry-related test failure was found. The predecessor report was blocked by unrelated compilation errors; targeted ETXTBSY verification passed with 4 integration tests and 28 bead-store unit tests. No source change was needed."}}}
.beads/checkpoint/forensic.jsonl:4097:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2949,"issue_id":"needle-3386daef","kind":"closed","actor":"system","time":"2026-08-17T06:44:52.301826798Z","detail":{"prior_base_status":"in_progress","reason":"Implemented fix for dispatch postcondition enforcement. The orphan branch in handle_success() now releases the bead instead of leaving it in_progress, preventing claim leaks. Added regression test for the 2026-08-17 loop case. Committed and pushed to main."}}}
.beads/checkpoint/forensic.jsonl:4098:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2950,"issue_id":"needle-3386daef","kind":"reopened","actor":"system","time":"2026-08-17T06:45:15.943031194Z","detail":{"prior_base_status":"closed","resulting_base_status":"open"}}}
.beads/checkpoint/forensic.jsonl:4103:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2955,"issue_id":"needle-de6bb8ad","kind":"closed","actor":"system","time":"2026-08-17T06:46:29.449022703Z","detail":{"prior_base_status":"in_progress","reason":"Task already complete: Upgrade check telemetry is fully implemented in the codebase with all required events (UpgradeCheckStarted, UpgradeCheckCompleted, UpgradeCheckFailed), rich context including version info and error classification, and proper integration throughout the system."}}}
.beads/checkpoint/forensic.jsonl:4132:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":2984,"issue_id":"needle-ea87796e","kind":"closed","actor":"system","time":"2026-08-17T07:10:09.971882292Z","detail":{"prior_base_status":"in_progress","reason":"Implemented checkpoint tracking fix - active root objects now committed atomically with pointer files. A fresh clone now passes bead doctor verification. Created scripts/commit-checkpoint.sh for future checkpoint commits."}}}
.beads/checkpoint/forensic.jsonl:4162:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3014,"issue_id":"needle-586f0f4b","kind":"closed","actor":"system","time":"2026-08-17T07:26:55.349051772Z","detail":{"prior_base_status":"in_progress","reason":"Completed the Explore access audit for every cataloged candidate. Found two unisolated Explore-capable tests: real_br_strand_waterfall_exhaustion and real_br_strand_waterfall_exhaustion_with_telemetry_jsonl, both selecting from Config::default with Explore enabled and the default /home/coding root. All other candidates are explicitly pinned or disabled, subprocess HOME-isolated, or do not evaluate Explore. Full evidence and per-file classifications are recorded in the bead notes."}}}
.beads/checkpoint/forensic.jsonl:4189:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3041,"issue_id":"needle-b93a96bd","kind":"closed","actor":"system","time":"2026-08-17T07:39:48.448805532Z","detail":{"prior_base_status":"in_progress","reason":"Tilde expansion integration tests were already added in commit 50bca54. Tests cover all required config path fields (workspace.home, workspace.default, agent.adapters_dir, bead_cli.path, strands.explore.*, telemetry.file_sink.log_dir, supervisor.heartbeat_path, prompt.context_files). All tests verify both tilde-prefixed paths expand correctly and non-tilde paths pass through unchanged with proper HOME isolation.\n\nHowever, test verification is blocked by 41 unrelated compilation errors documented in failing_tests_list.txt (syntax errors in src/dispatch/mod.rs, type mismatches in src/outcome/mod.rs, missing Result type alias in src/validation/predispatch.rs). These errors prevent ALL tests from running, not just tilde expansion tests. The compilation errors are a separate issue tracked outside this bead.\n\nThe bead requirements are complete - the comprehensive tilde expansion test suite exists and follows the specified pattern. Test execution will be possible once the compilation errors are fixed."}}}
.beads/checkpoint/forensic.jsonl:4236:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3088,"issue_id":"needle-068b926f","kind":"closed","actor":"system","time":"2026-08-17T08:04:25.758436598Z","detail":{"prior_base_status":"in_progress","reason":"All acceptance criteria verified:\n\n✅ Added tracing/log capture verification to error case tests (cleanup_heartbeat_file_logs_warning_on_error, healthmonitor_cleanup_heartbeat_file_logs_errors_on_failure)\n✅ Verified error messages logged at appropriate levels (WARN for removal failures, DEBUG for NotFound/idempotent case)\n✅ Ran cargo test cleanup_heartbeat_file --lib: all 13 tests passed in 0.23s\n✅ Verified no panics occur in any error condition\n✅ Verified test output shows all assertions passing (13 passed, 0 failed)\n✅ Temporary test files auto-cleaned via tempfile::tempdir()\n\nError simulation tests from child beads 1-3 all functional with proper logging:\n- Permission denied errors → logged at WARN level\n- Removal failures (directory instead of file) → logged at WARN level\n- File not found → logged at DEBUG level (idempotent)\n- IO errors → properly propagated with context\n\nBead bf-1bdjl (Add test for heartbeat cleanup error handling) complete."}}}
.beads/checkpoint/forensic.jsonl:4251:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3103,"issue_id":"needle-dadb01e8","kind":"closed","actor":"system","time":"2026-08-17T08:08:09.613007068Z","detail":{"prior_base_status":"in_progress","reason":"Completed verification: run() helper is still in use by git_head() and was correctly NOT removed. Fixed all clippy warnings and formatted code. Verified no bf string references remain in validation module. All changes committed and pushed."}}}
.beads/checkpoint/forensic.jsonl:4375:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3227,"issue_id":"needle-5b33d251","kind":"closed","actor":"system","time":"2026-08-17T10:55:02.554732707Z","detail":{"prior_base_status":"in_progress","reason":"Implemented atomic claim verification at dispatch time. Added dispatch-time check that queries the live bead store immediately before agent execution to verify the bead is still in_progress and assigned to the expected worker. If verification fails, dispatch is aborted and the bead is released back to ready state. This prevents double-dispatch where two workers dispatch the same bead concurrently. The verification is atomic within the same execution flow with no gap between verification and dispatch."}}}
.beads/checkpoint/forensic.jsonl:4446:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3298,"issue_id":"needle-10d8e7c5","kind":"closed","actor":"system","time":"2026-08-17T12:37:16.74963242Z","detail":{"prior_base_status":"in_progress","reason":"All deliverables were already implemented in the codebase. DecompositionDecision enum with Split/Refuse variants, decompose_bead_decision() function with clear thresholds, and comprehensive unit tests all exist at src/types/mod.rs:478-3050. Task verification confirmed complete implementation matching all acceptance criteria."}}}
.beads/checkpoint/forensic.jsonl:4461:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3313,"issue_id":"needle-2de52de0","kind":"closed","actor":"system","time":"2026-08-17T13:06:32.665929698Z","detail":{"prior_base_status":"in_progress","reason":"Implemented HardDeadlineTimer with tokio::time::sleep-based absolute timeout\n\n- Created HardDeadlineTimer struct with absolute deadline (Instant::now() + duration) computed once at creation\n- Timer is non-resettable by design - deadline never changes after creation\n- Provides methods: new(), is_expired(), is_enabled(), deadline(), remaining(), wait_until_expired()\n- Clone-safe: clones share the same absolute deadline moment\n- Added comprehensive test suite with 10 tests covering all functionality:\n  * Disabled state (zero duration)\n  * Enabled state with absolute time calculation\n  * Expiry detection and remaining time tracking\n  * Async wait behavior and cancellation\n  * Clone behavior and time decay verification\n- All tests pass successfully\n- Ready for integration into process termination logic in follow-up task"}}}
.beads/checkpoint/forensic.jsonl:4528:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3380,"issue_id":"needle-3653fee9","kind":"closed","actor":"system","time":"2026-08-17T14:27:57.383671529Z","detail":{"prior_base_status":"open","reason":"GREEN. needle-ci-verify-fix-68fxh: verify Succeeded, check-version Succeeded -- first passing run after 6+ consecutive failures.\n\nVerified against a clean 'git archive HEAD' (what CI checks out): cargo fmt --check 0 diffs; cargo clippy --all-targets -- -D warnings 0 errors; cargo test --lib 2431 passed / 0 failed; cargo test --test sanitize_latency_assertion 7 passed.\n\nRoot causes were three, and the fmt gate masked the other two for 6+ runs:\n 1. fmt: dispatch/mod.rs + health/mod.rs were the last committed offenders.\n 2. worker/mod.rs verify_claim_at_dispatch compared the bare worker_name, but the\n    claim at :1296 is made with qualified_id() ('{agent}-{worker_name}'), which is\n    what the store records as assignee -- they can never match, so every dispatch\n    would abort. LATENT, not a live outage: the running needle-stable binary\n    (built 2026-08-16 02:38Z) predates the feature and contains zero occurrences\n    of the verification string. It would have broken all dispatch on the next\n    binary build. Fixed in 33b16ad, with a follow-up 3d8dd0f hoisting\n    qualified_id() out of the coroutine -- as a method call it borrows all of\n    *self and collides with the mutable self.exec_started_at (E0500), whereas the\n    old field access was a disjoint capture under Rust 2021.\n 3. canary load_expected_outcome_success_yaml wrote a fixture with\n    'final_status: closed' and asserted 'done'; migrated for bead-rs, assertion\n    missed.\n\nDEVIATION from this bead's own acceptance criteria: the criterion said the\nformatting commit must touch formatting only. 33b16ad mixes fmt with the\nworker/canary fixes, because uncommitted work in this shared checkout was\ndestroyed three times by concurrent worker commits and only an atomic\npatch-and-commit survived. Recorded rather than hidden.\n\nFOLLOW-UP, NOT FIXED HERE: a one-time fmt cleanup cannot converge while the fleet\ncommits unformatted code to the same files. This bead had two prior 'successful'\nattempts that left the tree dirty, and 8ccd7a8 was the THIRD cargo-fmt commit.\nThat is a race, not a task -- it needs a commit-time gate, which is\nneedle-d1b2ee0d."}}}
.beads/checkpoint/forensic.jsonl:4555:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3407,"issue_id":"needle-3386daef","kind":"assignment_cleared","actor":"system","time":"2026-08-17T17:38:39.724152891Z","detail":{"prior_assignee":"codex-gpt-5.6-terra-xhigh-terra-needle","resulting_base_status":"open"}}}
.beads/checkpoint/forensic.jsonl:4556:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3408,"issue_id":"needle-3386daef","kind":"claimed","actor":"codex-gpt-5.6-sol-xhigh-sol-needle","time":"2026-08-17T17:38:46.453678859Z","detail":{"policy":"fifo-v1","resulting_base_status":"in_progress","with_lease":false}}}
.beads/checkpoint/forensic.jsonl:4557:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3409,"issue_id":"needle-3386daef","kind":"updated","actor":"system","time":"2026-08-17T17:39:06.043758475Z","detail":{}}}
.beads/checkpoint/forensic.jsonl:4559:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3411,"issue_id":"needle-3386daef","kind":"closed","actor":"system","time":"2026-08-17T17:52:19.798453461Z","detail":{"prior_base_status":"in_progress","reason":"Dispatch postcondition enforcement restored. All three leak paths (orphan branch, show() timeout, show() error) now release the bead, ensuring a worker never leaves in_progress. The fix uses verify_shipped_work to distinguish shipped work (mark completed) from no work (release+quarantine), preventing the 2026-08-17 loop where needle-55ec0193 leaked across three workers. Committed as 7a63762 and pushed."}}}
.beads/checkpoint/forensic.jsonl:4561:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3413,"issue_id":"needle-3386daef","kind":"reopened","actor":"system","time":"2026-08-17T17:52:33.715514772Z","detail":{"prior_base_status":"closed","resulting_base_status":"open"}}}
.beads/checkpoint/forensic.jsonl:4566:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3418,"issue_id":"needle-3386daef","kind":"assignment_cleared","actor":"system","time":"2026-08-17T20:09:41.318737803Z","detail":{"prior_assignee":"codex-gpt-5.6-sol-xhigh-sol-needle","resulting_base_status":"open"}}}
.beads/checkpoint/forensic.jsonl:4649:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3501,"issue_id":"needle-3386daef","kind":"claimed","actor":"claude-code-glm-5-needle-otlp-test","time":"2026-08-17T20:09:51.569860864Z","detail":{"policy":"fifo-v1","resulting_base_status":"in_progress","with_lease":false}}}
.beads/checkpoint/forensic.jsonl:4678:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3530,"issue_id":"needle-de6bb8ad","kind":"closed","actor":"system","time":"2026-08-17T20:18:47.724506518Z","detail":{"prior_base_status":"in_progress","reason":"Telemetry for upgrade checks was already fully implemented. All deliverables met: UpgradeCheckStarted, UpgradeCheckCompleted, and UpgradeCheckFailed events with comprehensive context (source, versions, update availability, error classification) are emitted by check_for_update_with_telemetry() and download_to_testing_channel_with_telemetry(). Events follow existing telemetry patterns and are validated by integration tests."}}}
.beads/checkpoint/forensic.jsonl:4688:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3540,"issue_id":"needle-3d681f12","kind":"closed","actor":"system","time":"2026-08-17T20:21:07.247793497Z","detail":{"prior_base_status":"in_progress","reason":"Completed static catalog of all HealthMonitor and Worker-construction tests, grouped by module, with HOME-guard/temp-config classification. Identified src/worker/mod.rs::full_cycle_with_echo_agent as the only test that runs with the real default workspace.home and writes ~/.needle/state/heartbeats/echo-test-test-worker.json; recorded all other unguarded-but-temp-root groups and non-writing raw-default tests in bead notes. No source changes were needed."}}}
.beads/checkpoint/forensic.jsonl:4708:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3560,"issue_id":"needle-ea87796e","kind":"closed","actor":"system","time":"2026-08-17T20:41:59.955185879Z","detail":{"prior_base_status":"in_progress","reason":"Fixed checkpoint commit to include active root objects. Updated scripts/commit-checkpoint.sh to:\n1. Commit both current.json and previous.json active_root objects atomically with pointers\n2. Delete superseded objects from disk (not just untrack) to prevent accumulation\n3. Resolve active_root paths dynamically via jq instead of hardcoded names\n\nVerification:\n- Both active roots now tracked in git (fresh clone passes bead doctor)\n- Script uses dynamic resolution (jq -r '.active_root.path')\n- Zero untracked superseded objects accumulating on disk\n\nThis fixes the root cause where fresh clones failed pointer verification because committed pointers referenced objects not in git."}}}
.beads/checkpoint/forensic.jsonl:4711:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3563,"issue_id":"needle-615b57e9","kind":"closed","actor":"system","time":"2026-08-17T20:44:01.28802385Z","detail":{"prior_base_status":"in_progress","reason":"sccache-garage SealedSecret created and operational:\n\n✅ Infrastructure complete:\n- SealedSecret committed to declarative-config (commit 28393cd9, 2026-08-17 01:58:53)\n- Secret synced to cluster (created 2026-08-17 06:11:23Z, age ~14.5h)\n- Properly managed by SealedSecret controller (ownerReference verified)\n- All required keys present: bucket, endpoint, access-key-id, secret-access-key\n\n✅ Functional verification:\n- rust-verify workflows successfully enable sccache: 'sccache enabled (bucket=sccache)'\n- sccache --show-stats executes successfully and reports cache location: 's3, name: sccache, prefix: /'\n- sccache connects to Garage endpoint https://s3.ardenone.com\n\nAcceptance note: Non-zero cache hits cannot be verified until a successful compilation occurs. The rust-verify template has a separate bug (appends [profile.test] to Cargo.toml line 115, but NEEDLE already has this section at line 145) causing duplicate key errors before compilation. This is a template fix, not an sccache infrastructure issue. Cold builds are eliminated—sccache is functional and ready to cache."}}}
.beads/checkpoint/forensic.jsonl:4716:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3568,"issue_id":"needle-3386daef","kind":"updated","actor":"system","time":"2026-08-17T20:54:53.106574417Z","detail":{}}}
.beads/checkpoint/forensic.jsonl:4723:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3575,"issue_id":"needle-3386daef","kind":"closed","actor":"system","time":"2026-08-17T20:56:36.971662014Z","detail":{"prior_base_status":"in_progress","reason":"Bead completed. Investigation confirmed: (1) All 32 outcome tests pass including regression_2026_08_17_orphan_loop_bead_released_not_leaked. (2) Postcondition enforcement implemented in commits 96e691e and 7a63762 - orphan branch now releases beads structurally instead of leaving them in_progress. (3) Test handle_success_bead_still_open_releases_to_prevent_leak verifies the fix (test was renamed from 'emits_orphaned' to assert correct behavior). The dispatch postcondition is enforced: no dispatch path returns leaving the bead in_progress. Verification gates run in handle_success and call handle_gate_failure on failure, which releases the bead and increments failure count for quarantine. The 2026-08-17 loop (needle-55ec0193 worked 3 times, 48min agent time, never left in_progress) is prevented by structural enforcement."}}}
.beads/checkpoint/forensic.jsonl:4726:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3578,"issue_id":"needle-3386daef","kind":"reopened","actor":"system","time":"2026-08-17T20:57:10.495519429Z","detail":{"prior_base_status":"closed","resulting_base_status":"open"}}}
.beads/checkpoint/forensic.jsonl:4795:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3647,"issue_id":"needle-bc785934","kind":"closed","actor":"system","time":"2026-08-17T21:47:35.172771213Z","detail":{"prior_base_status":"open","reason":"Added unit tests for resolve_bead_cli Bf backend discovery. Three new tests cover PATH discovery, fallback to ~/.local/bin/bf, and correct Backend variant return. Changes pushed to main and CI triggered for verification."}}}
.beads/checkpoint/forensic.jsonl:4804:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3656,"issue_id":"needle-068b926f","kind":"closed","actor":"system","time":"2026-08-17T21:54:50.575211981Z","detail":{"prior_base_status":"in_progress","reason":"✅ Verified all cleanup_heartbeat_file error handling tests pass (13/13). Confirmed log capture verification in cleanup_heartbeat_file_logs_warning_on_error test. Verified appropriate log levels (debug for success/NotFound, warn for errors). No panics occur in any error condition. All acceptance criteria met."}}}
.beads/checkpoint/forensic.jsonl:4837:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3689,"issue_id":"needle-208840e4","kind":"closed","actor":"system","time":"2026-08-17T23:05:55.365183866Z","detail":{"prior_base_status":"in_progress","reason":"Added releases/latest API verification to needle-ci workflow\n\nThe --latest flag fix was already implemented (commit 678e9bd9), preventing\nsilent draft degradation. I've now added the final verification step that\nwas specified in the acceptance criteria: after publishing, the workflow\nqueries the public releases/latest API and confirms the tag matches what\nwas just published.\n\nThis ensures the release is actually visible to 'needle upgrade --check'\nand fails loudly if there's any permission issue causing the release to\nnot appear in the public API.\n\nChanges:\n- k8s/iad-ci/argo-workflows/needle-workflowtemplate.yml: Added API\n  verification step that queries releases/latest and compares the tag\n\nThe workflow now satisfies the acceptance criteria: a needle-ci run leaves\na published, non-draft release that 'needle upgrade --check' can see, and\nthe workflow fails if it does not."}}}
.beads/checkpoint/forensic.jsonl:4845:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3697,"issue_id":"needle-6b4aabcf","kind":"closed","actor":"system","time":"2026-08-17T23:16:13.374646138Z","detail":{"prior_base_status":"in_progress","reason":"Verification complete: Tilde expansion tests confirmed passing (93/94 tests, with 1 intentionally broken for CI verification). Found and fixed 1 regression in RoutingConfig field usage (default -> default_adapter). Zero new non-tilde regressions introduced. Test suite health confirmed."}}}
.beads/checkpoint/forensic.jsonl:4848:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3700,"issue_id":"needle-c8c008be","kind":"closed","actor":"system","time":"2026-08-17T23:21:12.382454658Z","detail":{"prior_base_status":"in_progress","reason":"Task complete - is_tilde_prefix function already existed in src/util.rs with correct implementation. Fixed test_is_tilde_prefix_with_tilde_slash which had an inverted assertion for CI verification. All 9 tests now pass, clippy is clean, no unwrap/expect in implementation."}}}
.beads/checkpoint/forensic.jsonl:4922:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3774,"issue_id":"needle-5b33d251","kind":"closed","actor":"system","time":"2026-08-18T01:23:31.122167764Z","detail":{"prior_base_status":"in_progress","reason":"Implementation already complete and committed in b5780d6. Added atomic claim verification at dispatch time: (1) Queries live bead store via Claimer::verify_claim_at_dispatch() immediately before agent spawn, (2) Verifies bead is still in_progress and assigned to expected worker, (3) Aborts dispatch and releases bead back to ready state if verification fails. Second worker cannot dispatch a bead that another worker has already claimed - verified by live store query at dispatch moment, not stale snapshot."}}}
.beads/checkpoint/forensic.jsonl:5005:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3857,"issue_id":"needle-9689e85e","kind":"closed","actor":"system","time":"2026-08-18T03:31:04.849141257Z","detail":{"prior_base_status":"in_progress","reason":"Added intentional test failure to tests/placeholder_validation_tests.rs: test_backend_validate_with_valid_placeholders() now includes assert!(false, 'intentional test failure for CI verification') to trigger needle-ci and verify retryStrategy fix. Code compiles (cargo check passed), change is minimal (1 line), and easily reversible."}}}
.beads/checkpoint/forensic.jsonl:5009:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3861,"issue_id":"needle-f634ae79","kind":"closed","actor":"system","time":"2026-08-18T03:49:11.182231744Z","detail":{"prior_base_status":"in_progress","reason":"FABRIC telemetry enabled and configured\n\nConfiguration complete:\n✅ OTLP already enabled at http://needle-otel-ex44-apexalgo-iad-ts.ardenone.com:4318 (working)\n✅ FABRIC enabled at http://needle-feed-ex44-apexalgo-iad-ts.ardenone.com/ingest (updated from localhost:3000)\n✅ IngressRoute needle-fabric-feed-vpn created and synced to apexalgo-iad\n✅ Both NEEDLE and declarative-config repos committed and pushed\n\nThe .needle.yaml now points both otlp_sink.enabled: true and fabric.enabled: true to real, reachable endpoints. The IngressRoute has been applied to the cluster (Age: 2m37s). DNS propagation for the FABRIC endpoint is pending but the configuration is complete and deployed.\n\nChanges:\n- NEEDLE: 78c19a5 feat(needle-f634ae79): enable FABRIC telemetry to real endpoint\n- declarative-config: fefc3f81 feat(needle-f634ae79): add IngressRoute for FABRIC feed endpoint\n\nEnd-to-end verification with a real bead lifecycle can proceed once DNS propagation completes."}}}
.beads/checkpoint/forensic.jsonl:5015:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3867,"issue_id":"needle-8011a4a3","kind":"closed","actor":"system","time":"2026-08-18T04:03:55.588948522Z","detail":{"prior_base_status":"in_progress","reason":"Integration test worker_liveness_detection_integration already exists in src/health/mod.rs (lines 4544-4663) and fully covers all acceptance criteria. No implementation needed - verification complete."}}}
.beads/checkpoint/forensic.jsonl:5050:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3902,"issue_id":"needle-b129f9ba","kind":"closed","actor":"system","time":"2026-08-18T06:40:24.239988041Z","detail":{"prior_base_status":"in_progress","reason":"Fixed all remaining test failures unrelated to ETXTBSY retry integration:\n1. Removed deliberate CI verification test break in integration_tests.rs (outcome_path_success_exit_0 test was asserting 999 beads instead of 1)\n2. Removed deliberate CI verification test break in placeholder_validation_tests.rs (intentional assert!(false))\n3. Fixed clippy warnings in adapter_validation_tests.rs:\n   - Replaced manual Iterator::find with iterator method\n   - Removed #[test] attributes from doctest examples\n   - Added #[allow(dead_code)] to struct fields used only in Drop implementations\n\nAll tests now pass clippy and fmt checks. Committed and pushed to origin/main."}}}
.beads/checkpoint/forensic.jsonl:5088:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":3940,"issue_id":"needle-3386daef","kind":"assignment_cleared","actor":"system","time":"2026-08-19T15:17:52.028298328Z","detail":{"prior_assignee":"claude-code-glm-5-needle-otlp-test","resulting_base_status":"open"}}}
.beads/checkpoint/forensic.jsonl:5171:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4023,"issue_id":"needle-3386daef","kind":"claimed","actor":"codex-gpt-5.6-sol-xhigh-sol-needle","time":"2026-08-19T15:18:35.116393436Z","detail":{"policy":"fifo-v1","resulting_base_status":"in_progress","with_lease":false}}}
.beads/checkpoint/forensic.jsonl:5180:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4032,"issue_id":"needle-de6bb8ad","kind":"closed","actor":"system","time":"2026-08-19T15:44:19.236013231Z","detail":{"prior_base_status":"in_progress","reason":"Telemetry for upgrade checks is already fully implemented in the codebase. All deliverables met:\n\n1. ✅ UpgradeCheckStarted event - emitted when check begins\n2. ✅ UpgradeCheckCompleted event - emitted on success with version details\n3. ✅ UpgradeCheckFailed event - emitted on failure with error classification\n4. ✅ Error classification (network/parse/api/unknown) implemented\n5. ✅ Events follow existing telemetry patterns in EventKind enum\n\nFunctions check_for_update_with_telemetry() and download_to_testing_channel_with_telemmetry() already emit all required events with proper context. Integration complete in CLI and supervisor modules."}}}
.beads/checkpoint/forensic.jsonl:5187:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4039,"issue_id":"needle-3386daef","kind":"updated","actor":"system","time":"2026-08-19T15:52:47.653229473Z","detail":{}}}
.beads/checkpoint/forensic.jsonl:5192:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4044,"issue_id":"needle-3386daef","kind":"updated","actor":"system","time":"2026-08-19T15:54:54.695841535Z","detail":{}}}
.beads/checkpoint/forensic.jsonl:5198:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4050,"issue_id":"needle-3386daef","kind":"released","actor":"system","time":"2026-08-19T15:56:01.050567462Z","detail":{"prior_assignee":"codex-gpt-5.6-sol-xhigh-sol-needle","resulting_base_status":"open"}}}
.beads/checkpoint/forensic.jsonl:5199:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4051,"issue_id":"needle-3386daef","kind":"claimed","actor":"codex-gpt-5.6-sol-xhigh-sol-needle","time":"2026-08-19T16:02:40.109754054Z","detail":{"policy":"fifo-v1","resulting_base_status":"in_progress","with_lease":false}}}
.beads/checkpoint/forensic.jsonl:5200:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4052,"issue_id":"needle-3386daef","kind":"released","actor":"system","time":"2026-08-19T16:24:25.749215251Z","detail":{"prior_assignee":"codex-gpt-5.6-sol-xhigh-sol-needle","resulting_base_status":"open"}}}
.beads/checkpoint/forensic.jsonl:5201:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4053,"issue_id":"needle-3386daef","kind":"claimed","actor":"codex-gpt-5.6-sol-xhigh-sol-needle","time":"2026-08-19T16:45:25.889575703Z","detail":{"policy":"fifo-v1","resulting_base_status":"in_progress","with_lease":false}}}
.beads/checkpoint/forensic.jsonl:5202:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4054,"issue_id":"needle-3386daef","kind":"released","actor":"system","time":"2026-08-19T16:53:53.360037416Z","detail":{"prior_assignee":"codex-gpt-5.6-sol-xhigh-sol-needle","resulting_base_status":"open"}}}
.beads/checkpoint/forensic.jsonl:5203:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4055,"issue_id":"needle-7e9ea193","kind":"claimed","actor":"codex-gpt-5.6-sol-xhigh-sol-needle","time":"2026-08-19T17:03:27.386978363Z","detail":{"policy":"fifo-v1","resulting_base_status":"in_progress","with_lease":false}}}
.beads/checkpoint/forensic.jsonl:5204:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4056,"issue_id":"needle-7e9ea193","kind":"released","actor":"system","time":"2026-08-19T17:46:44.377898708Z","detail":{"prior_assignee":"codex-gpt-5.6-sol-xhigh-sol-needle","resulting_base_status":"open"}}}
.beads/checkpoint/forensic.jsonl:5223:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4075,"issue_id":"needle-7e9ea193","kind":"claimed","actor":"codex-gpt-5.6-terra-xhigh-needle","time":"2026-08-20T05:43:58.466205311Z","detail":{"policy":"fifo-v1","resulting_base_status":"in_progress","with_lease":false}}}
.beads/checkpoint/forensic.jsonl:5224:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4076,"issue_id":"needle-7e9ea193","kind":"released","actor":"system","time":"2026-08-20T06:05:27.040594718Z","detail":{"prior_assignee":"codex-gpt-5.6-terra-xhigh-needle","resulting_base_status":"open"}}}
.beads/checkpoint/forensic.jsonl:5225:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4077,"issue_id":"needle-7e9ea193","kind":"claimed","actor":"codex-gpt-5.6-terra-xhigh-needle","time":"2026-08-20T06:05:27.080701554Z","detail":{"policy":"fifo-v1","resulting_base_status":"in_progress","with_lease":false}}}
.beads/checkpoint/forensic.jsonl:5226:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4078,"issue_id":"needle-7e9ea193","kind":"updated","actor":"system","time":"2026-08-20T06:24:39.043099904Z","detail":{}}}
.beads/checkpoint/forensic.jsonl:5227:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4079,"issue_id":"needle-7e9ea193","kind":"closed","actor":"system","time":"2026-08-20T06:25:44.490298398Z","detail":{"prior_base_status":"in_progress","reason":"Fix verified as already implemented and working correctly. Success classification depends on verification result (verified flag), not exit code alone. classify(0, false, false) returns Failure as required. All 24 classification tests pass including verification gate tests."}}}
.beads/checkpoint/forensic.jsonl:5228:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4080,"issue_id":"needle-7e9ea193","kind":"reopened","actor":"system","time":"2026-08-20T06:27:24.09336331Z","detail":{"prior_base_status":"closed","resulting_base_status":"open"}}}
.beads/checkpoint/forensic.jsonl:5231:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4083,"issue_id":"needle-48adb04c","kind":"closed","actor":"system","time":"2026-08-20T06:38:10.140044506Z","detail":{"prior_base_status":"in_progress","reason":"Implemented checksums.txt generation and validation in needle-ci WorkflowTemplate\n\nThe workflow now:\n- Generates SHA-256 checksums for all 7 release artifacts\n- Validates checksums.txt completeness and correctness before publishing\n- Includes checksums.txt in release assets\n- Verifies checksums.txt is downloadable after release\n\nThis enables install.sh's built-in checksum verification to work for users.\nChanges committed to declarative-config and pushed to main; ArgoCD will\nreconcile the updated template to iad-ci cluster."}}}
.beads/checkpoint/forensic.jsonl:5261:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4113,"issue_id":"needle-de6bb8ad","kind":"closed","actor":"system","time":"2026-08-21T00:12:52.195700406Z","detail":{"prior_base_status":"in_progress","reason":"Fixed upgrade check telemetry implementation\n\nFixed typos in function names that prevented compilation:\n- perform_upgrade_with_telemmetry → perform_upgrade_with_telemetry\n- check_for_update_with_telemmetry → check_for_update_with_telemetry\n\nVerified telemetry implementation is complete:\n✅ UpgradeCheckStarted event emitted at each check attempt\n✅ UpgradeCheckCompleted event with version info and update availability\n✅ UpgradeCheckFailed event with error classification (network/parse/api)\n✅ All events include source, timestamp, and relevant context\n✅ Follows existing telemetry patterns in the codebase\n\nCode compiles successfully and all 25 upgrade tests pass."}}}
.beads/checkpoint/forensic.jsonl:5267:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4119,"issue_id":"needle-ea87796e","kind":"closed","actor":"system","time":"2026-08-21T00:29:55.632974788Z","detail":{"prior_base_status":"in_progress","reason":"Fixed checkpoint commit to include active root objects. The existing commit-checkpoint.sh script was enhanced and run, which:\n1. Flushes checkpoint to get latest state\n2. Extracts current and previous active_root paths dynamically\n3. Stages both pointer files and their referenced objects atomically\n4. Removes tracked superseded objects (via git rm)\n5. Deletes untracked superseded objects from disk\n6. Commits with proper message\n\nVerified:\n- Active roots are now tracked (gen-719533b6e35c9c085ecffee8f1e08d8e.jsonl, gen-26bc450fa581cb0e3e22833a945ea500.jsonl)\n- Superseded objects cleaned up (5 deleted)\n- Objects directory no longer accumulates untracked files (63 tracked = 63 on disk)\n- Fresh clone will now pass bead doctor verification"}}}
.beads/checkpoint/forensic.jsonl:5275:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4127,"issue_id":"needle-6f242187","kind":"closed","actor":"system","time":"2026-08-21T00:59:20.075089216Z","detail":{"prior_base_status":"in_progress","reason":"ADR-016 Decision 3 already implemented and shipped in commit ff05fd3 (2026-08-17) for bead needle-5359b52a. All acceptance criteria met: current_workspace cell exists, set_workspace() method exists and is called from claim/mod.rs when beads are claimed, both emit() and emit_sync() use workspace_snapshot() instead of hardcoding None. Tests pass. Wire capture verification blocked by unrelated tokio runtime panic (separate issue needed)."}}}
.beads/checkpoint/forensic.jsonl:5326:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4178,"issue_id":"needle-ceea8c53","kind":"closed","actor":"system","time":"2026-08-21T03:15:43.264439084Z","detail":{"prior_base_status":"in_progress","reason":"Verification complete. Validated failing tests list format and confirmed all 5 failing tests from raw output are captured in organized list. All acceptance criteria met: format validated (✅), count matches raw output (✅), verification report created at /home/coding/NEEDLE/verification-failing-tests-list-2026-08-20.md"}}}
.beads/checkpoint/forensic.jsonl:5330:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4182,"issue_id":"needle-068b926f","kind":"closed","actor":"system","time":"2026-08-21T03:22:23.8123142Z","detail":{"prior_base_status":"in_progress","reason":"Logging verification and test suite completed successfully. All 14 cleanup_heartbeat_file tests passed with no failures or panics. Verified that error messages are logged at appropriate levels (DEBUG for NotFound, WARN for removal failures). Test coverage includes error simulation, log capture verification, permission denied scenarios, symlink handling, and HealthMonitor integration. All acceptance criteria met."}}}
.beads/checkpoint/forensic.jsonl:5346:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4198,"issue_id":"needle-208840e4","kind":"closed","actor":"system","time":"2026-08-21T03:58:25.376267331Z","detail":{"prior_base_status":"in_progress","reason":"Fix already implemented in commits 678e9bd9 (2026-08-15, added --latest flag) and 18a11275 (2026-08-17, added releases/latest API verification). Workflow now ensures releases are published (not drafts) and fails loudly if publication fails silently."}}}
.beads/checkpoint/forensic.jsonl:5354:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4206,"issue_id":"needle-44a0f603","kind":"closed","actor":"system","time":"2026-08-21T04:21:25.174042914Z","detail":{"prior_base_status":"in_progress","reason":"ProcessGuard migration verification complete. All 4 wait() calls in dispatch/mod.rs are using ProcessGuard (no direct child.wait() calls remain). Code compiles cleanly and passes clippy with no warnings. Work was already completed and committed."}}}
.beads/checkpoint/forensic.jsonl:5434:{"record_type":"event","event":{"$schema":"urn:bead-rs:schema:event:native-v1","origin_store_uuid":"8bc2f8fe-6018-36a1-5555-6b38232a1155","origin_event_sequence":4286,"issue_id":"needle-7e9ea193","kind":"assignment_cleared","actor":"system","time":"2026-08-21T08:57:16.379120305Z","detail":{"prior_assignee":"codex-gpt-5.6-terra-xhigh-needle","resulting_base_status":"open"}}}

exec
/bin/bash -lc 'git log --oneline -12 -- src/types/mod.rs
git log -p -8 -- src/types/mod.rs | rg -n -C 20 "pub fn classify|verified"
git show 9a71057''^:src/types/mod.rs | sed -n '"'228,272p'
git show 9a71057:src/types/mod.rs | sed -n '228,276p'
sed -n '1520,1635p' src/outcome/mod.rs
rg -n -C 8 \"handle_gate_failure|increment_failure_count\" src/outcome/mod.rs | head -n 180
stat -c '%y %n' src/outcome/mod.rs tests/property_tests.rs tests/integration_tests.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
8ccd7a8 style(needle-22e225cf): format codebase with cargo fmt
4975b8d test(needle-2d1daeea): add Auto backend cargo bin fallback test
8c64f77 style(needle-0cd93f10): apply cargo fmt formatting fixes
97ad556 fix(needle-4a1a9975): make error code categories unique
ec49a8d feat(needle-e647f937): emit SupervisorBinaryResolved telemetry event at startup
ff5eeb8 feat(needle-dea46401): add DecompositionMode enum for timeout decomposition
4c39970 feat(needle-87d9ff51): add ChildBeadProposal struct for timeout decomposition
704ad72 feat(needle-cdcc8041): design timeout decomposition types and interfaces
8d30fb4 feat(needle-bead-rs): add bead-rs backend and BrDependency compat shape
df89ab6 fix(needle-bf-3b8x4): add bead comments to agent prompts for in-flight corrections
54192e4 fix(bf-2unnq): fix cargo test --lib deadlock in explore strand tests
0249d2e fix(ci): unbreak the build — gate integration_t, pin the toolchain, apply fmt
866- 
867-     /// Returns true if the analysis suggests productive work was in progress.
868-+    #[allow(clippy::match_like_matches_macro)]
869-     pub fn suggests_productive_work(&self) -> bool {
870-         self.has_activity_evidence
871-             || match &self.timeout_context {
872-
873-commit 97ad556b1aca83555f2316886f34f2825075548c
874-Author: jedarden <github@jedarden.com>
875-Date:   Mon Aug 17 01:46:14 2026 -0400
876-
877-    fix(needle-4a1a9975): make error code categories unique
878-
879-diff --git a/src/types/mod.rs b/src/types/mod.rs
880-index 84190d0..a3786dc 100644
881---- a/src/types/mod.rs
882-+++ b/src/types/mod.rs
883-@@ -3040,125 +3040,535 @@ pub fn parse_error_code(input: &str) -> Option<String> {
884- /// assert_eq!(classify_error_code("E9999"), ErrorCategory::Unknown);
885- /// ```
886: pub fn classify_error_code(code: &str) -> ErrorCategory {
887--    match code {
888--        // Type mismatch errors
889--        "E0308" | "E0309" | "E0310" | "E0311" | "E0312" | "E0313" | "E0314" | "E0315" | "E0316"
890--        | "E0317" | "E0369" | "E0370" => ErrorCategory::TypeMismatch,
891--
892--        // Borrow checker and lifetime errors
893--        "E0382" | "E0502" | "E0503" | "E0505" | "E0506" | "E0507" | "E0508" | "E0509" | "E0510"
894--        | "E0511" | "E0512" | "E0515" | "E0516" | "E0517" | "E0597" | "E0623" | "E0624"
895--        | "E0625" | "E0626" | "E0716" | "E0782" | "E0783" | "E0937" | "E0980" => {
896--            ErrorCategory::BorrowChecker
897--        }
898--
899--        // Trait implementation errors
900--        "E0038" | "E0046" | "E0117" | "E0118" | "E0119" | "E0120" | "E0183" | "E0207" | "E0210"
901--        | "E0220" | "E0227" | "E0229" | "E0230" | "E0277" | "E0365" | "E0366" | "E0367"
902--        | "E0368" | "E0381" | "E0390" | "E0391" | "E0412" | "E0423" | "E0437" | "E0558"
903--        | "E0574" | "E0647" | "E0699" | "E0708" | "E0719" | "E0781" => ErrorCategory::TraitImpl,
904--
905--        // Pattern matching errors
906--        "E0002" | "E0009" | "E0007" | "E0010" | "E0011" | "E0012" | "E0013" | "E0014" | "E0015"
    Crash(i32),
}

impl Outcome {
    /// Classify an exit code into an `Outcome` variant.
    ///
    /// Every exit code range has an explicit match arm — no wildcards.
    /// The `was_interrupted` flag takes precedence over exit code analysis.
    ///
    /// # Mapping (per spec)
    /// - `was_interrupted=true` → `Interrupted` (checked first)
    /// - exit 0 → `Success`
    /// - exit 1 → `Failure`
    /// - exit 124 → `Timeout`
    /// - exit 127 → `AgentNotFound`
    /// - exit >128 → `Crash(exit_code)`
    /// - exit <0 → `Crash(exit_code)`
    /// - all other → `Failure`
    pub fn classify(exit_code: i32, was_interrupted: bool) -> Self {
        // Interrupted flag takes precedence (graceful shutdown path).
        if was_interrupted {
            return Outcome::Interrupted;
        }

        // Explicit mapping for every exit code range — NO wildcards.
        // Each range is explicitly enumerated to ensure compile errors
        // if a new Outcome variant is added without updating this match.
        match exit_code {
            // Success
            0 => Outcome::Success,
            // Explicit failure code
            1 => Outcome::Failure,
            // Timeout (GNU timeout exit code)
            124 => Outcome::Timeout,
            // Agent not found (shell exit code for missing command)
            127 => Outcome::AgentNotFound,
            // Failure range: 2-123
            2..=123 => Outcome::Failure,
            // Failure: 125-128 (not >128 per spec, so 128 is not Crash)
            125..=128 => Outcome::Failure,
            // Signal exits: >128 (128 + signal number)
            // These are all crashes per the spec.
            129..=i32::MAX => Outcome::Crash(exit_code),
            // Negative exit codes (abnormal termination)
            i32::MIN..=-1 => Outcome::Crash(exit_code),
    Crash(i32),
}

impl Outcome {
    /// Classify an exit code into an `Outcome` variant.
    ///
    /// Every exit code range has an explicit match arm — no wildcards.
    /// The `was_interrupted` flag takes precedence over exit code analysis.
    ///
    /// # Mapping (per spec)
    /// - `was_interrupted=true` → `Interrupted` (checked first)
    /// - exit 0 → `Success`
    /// - exit 1 → `Failure`
    /// - exit 124 → `Timeout`
    /// - exit 127 → `AgentNotFound`
    /// - exit >128 → `Crash(exit_code)`
    /// - exit <0 → `Crash(exit_code)`
    /// - all other → `Failure`
    pub fn classify(exit_code: i32, was_interrupted: bool) -> Self {
        // Interrupted flag takes precedence (graceful shutdown path).
        if was_interrupted {
            return Outcome::Interrupted;
        }

        // Explicit mapping for every exit code range — NO wildcards.
        // Each range is explicitly enumerated to ensure compile errors
        // if a new Outcome variant is added without updating this match.
        match exit_code {
            // Success
            0 => Outcome::Success,
            // Explicit failure code
            1 => Outcome::Failure,
            // Timeout (GNU timeout exit code)
            124 => Outcome::Timeout,
            // Agent not found (shell exit code for missing command)
            127 => Outcome::AgentNotFound,
            // Failure range: 2-123
            2..=123 => Outcome::Failure,
            // Failure: 125-128 (not >128 per spec, so 128 is not Crash)
            125..=128 => Outcome::Failure,
            // Signal exits: >128 (128 + signal number)
            // These are all crashes per the spec.
            129..=i32::MAX => Outcome::Crash(exit_code),
            // Negative exit codes (abnormal termination)
            i32::MIN..=-1 => Outcome::Crash(exit_code),
        }
    }

    /// Return a string representation for telemetry/logging.
        fn has_valid_store(&self) -> bool {
            true // Mock store always has a valid store
        }
    }

    struct NopSink;

    impl Sink for NopSink {
        fn accept(&self, _event: &crate::telemetry::TelemetryEvent) -> Result<()> {
            Ok(())
        }
        fn flush(&self, _deadline: std::time::Duration) -> Result<()> {
            Ok(())
        }
    }

    fn test_handler() -> OutcomeHandler {
        let config = Config::default();
        let telemetry = Telemetry::with_sink("test-worker".to_string(), NopSink);
        OutcomeHandler::new(config, telemetry)
    }

    fn test_output(exit_code: i32) -> AgentOutcome {
        AgentOutcome {
            exit_code,
            stdout: String::new(),
            stderr: String::new(),
        }
    }

    // ── classify tests ──

    #[test]
    fn classify_was_interrupted_always_returns_interrupted() {
        assert_eq!(classify(0, true, true), Outcome::Interrupted);
        assert_eq!(classify(1, true, false), Outcome::Interrupted);
        assert_eq!(classify(127, true, true), Outcome::Interrupted);
    }

    #[test]
    fn classify_not_interrupted_uses_exit_code_and_verification() {
        // Exit code 0 with verification passes → Success
        assert_eq!(classify(0, false, true), Outcome::Success);
        // Exit code 0 with verification fails → Failure
        assert_eq!(classify(0, false, false), Outcome::Failure);
        // Non-zero exit codes always fail regardless of verification
        assert_eq!(classify(1, false, true), Outcome::Failure);
        assert_eq!(classify(1, false, false), Outcome::Failure);
        assert_eq!(classify(124, false, true), Outcome::Timeout);
        assert_eq!(classify(127, false, true), Outcome::AgentNotFound);
        assert_eq!(classify(129, false, true), Outcome::Crash(129));
    }

    #[test]
    fn classify_no_wildcard_arms() {
        // Verify key exit codes map correctly per spec.
        // Exit code 0 ONLY succeeds when verification passes
        assert_eq!(classify(0, false, true), Outcome::Success);
        assert_eq!(classify(0, false, false), Outcome::Failure);
        assert_eq!(classify(1, false, true), Outcome::Failure);
        assert_eq!(classify(2, false, true), Outcome::Failure);
        assert_eq!(classify(99, false, true), Outcome::Failure);
        assert_eq!(classify(100, false, true), Outcome::Failure);
        assert_eq!(classify(124, false, true), Outcome::Timeout);
        assert_eq!(classify(125, false, true), Outcome::Failure);
        assert_eq!(classify(128, false, true), Outcome::Failure); // not >128 per spec
        assert_eq!(classify(129, false, true), Outcome::Crash(129));
        assert_eq!(classify(137, false, true), Outcome::Crash(137));
        assert_eq!(classify(-9, false, true), Outcome::Crash(-9));
    }

    #[test]
    fn classify_verification_gate() {
        // The core fix: exit code 0 does NOT guarantee Success.
        // Verification is the gate.
        assert_eq!(
            classify(0, false, false),
            Outcome::Failure,
            "exit_code=0, verified=false must return Failure"
        );
        assert_eq!(
            classify(0, false, true),
            Outcome::Success,
            "exit_code=0, verified=true must return Success"
        );
    }

    // ── handle tests ──

    #[tokio::test]
    async fn handle_success_bead_closed_by_agent() {
        let handler = test_handler();
        let store = test_store(BeadStatus::Done);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(0), false)
            .await
            .unwrap();

        assert_eq!(result.outcome, Outcome::Success);
        assert_eq!(result.bead_action, BeadAction::None);
        assert!(!result.telemetry_events.is_empty());
        let actions = store.actions();
        assert!(
            !actions.iter().any(|a| matches!(a, StoreAction::Release(_))),
            "success should not release bead"
        );
    }

    #[tokio::test]
    async fn handle_success_bead_still_open_releases_to_prevent_leak() {
        let handler = test_handler();
        let store = test_store(BeadStatus::InProgress);
        let bead = test_bead(BeadStatus::InProgress);

296-        });
297-
298-        let (bead_action, telemetry_events) = match outcome.clone() {
299-            Outcome::Success => self.handle_success(store, bead, gate_report).await?,
300-            Outcome::Failure => {
301-                // If we have a gate report with failures, handle as gate failure.
302-                if let Some(report) = gate_report {
303-                    if !report.all_passed {
304:                        self.handle_gate_failure(store, bead, &report).await?
305-                    } else {
306-                        self.handle_failure(store, bead).await?
307-                    }
308-                } else {
309-                    self.handle_failure(store, bead).await?
310-                }
311-            }
312-            Outcome::Timeout => self.handle_timeout(store, bead).await?,
--
480-                    match verify_shipped_work(&current, &bead.workspace, store).await {
481-                        Ok(crate::validation::GateResult::Fail(reason)) => {
482-                            tracing::warn!(
483-                                bead_id = %bead.id,
484-                                reason = %reason,
485-                                "bead closed but shipped-work check failed — reopening and releasing"
486-                            );
487-                            let report = GateReport::single_failure("shipped_work", reason);
488:                            return self.handle_gate_failure(store, bead, &report).await;
489-                        }
490-                        Ok(crate::validation::GateResult::Pass) => {}
491-                        Err(e) => {
492-                            tracing::warn!(
493-                                bead_id = %bead.id,
494-                                error = %e,
495-                                "shipped-work check errored — failing open"
496-                            );
--
561-                            );
562-                            // Release and increment failure count to apply quarantine.
563-                            let mut release_events = self.release_bead(store, bead).await?;
564-                            events.append(&mut release_events);
565-                            let release_succeeded = events
566-                                .iter()
567-                                .any(|e| matches!(e, EventKind::BeadReleased { .. }));
568-                            if release_succeeded {
569:                                let _ = self.increment_failure_count(store, bead).await;
570-                            }
571-                            return Ok((BeadAction::Released, events));
572-                        }
573-                        Err(e) => {
574-                            tracing::warn!(
575-                                bead_id = %bead.id,
576-                                error = %e,
577-                                "shipped-work check errored — releasing orphaned bead with failure increment"
578-                            );
579-                            // On error, release and increment failure count.
580-                            let mut release_events = self.release_bead(store, bead).await?;
581-                            events.append(&mut release_events);
582-                            let release_succeeded = events
583-                                .iter()
584-                                .any(|e| matches!(e, EventKind::BeadReleased { .. }));
585-                            if release_succeeded {
586:                                let _ = self.increment_failure_count(store, bead).await;
587-                            }
588-                            return Ok((BeadAction::Released, events));
589-                        }
590-                    }
591-                } else {
592-                    tracing::warn!(
593-                        bead_id = %bead.id,
594-                        "enforce_shipped_work disabled — releasing orphaned bead"
--
648-                tracing::warn!(bead_id = %bead.id, error = %e, "flush failed after success — JSONL may lag SQLite");
649-            }
650-        }
651-
652-        Ok((BeadAction::None, events))
653-    }
654-
655-    /// Handle gate failure: reopen the bead if it was closed, then release it.
656:    async fn handle_gate_failure(
657-        &self,
658-        store: &dyn BeadStore,
659-        bead: &Bead,
660-        report: &crate::validation::GateReport,
661-    ) -> Result<(BeadAction, Vec<EventKind>)> {
662-        // Find the first failing gate for telemetry.
663-        let (failed_gate, reason) = report
664-            .results
--
741-        // ARMOR/bf-135k storm ran one bead 24 times in a single day, each
742-        // attempt leaving another commit behind. A gate failure is no less
743-        // repeatable than an agent failure and must respect the same ceiling.
744-        let release_succeeded = events
745-            .iter()
746-            .any(|e| matches!(e, EventKind::BeadReleased { .. }));
747-        let mut action = BeadAction::Released;
748-        if release_succeeded {
749:            match self.increment_failure_count(store, bead).await {
750-                Ok(new_count) => {
751-                    let threshold = self.config.outcome.quarantine_after_failures;
752-                    if threshold > 0 && new_count >= threshold {
753-                        match self
754-                            .quarantine_bead(store, bead, new_count, threshold)
755-                            .await
756-                        {
757-                            Ok(quarantine_events) => {
--
819-        // also closes the mitosis `NotSplittable` fallthrough (worker/mod.rs):
820-        // that verdict no longer matters for beads at or past the ceiling,
821-        // since this check already ran before mitosis evaluation this cycle.
822-        let release_succeeded = events
823-            .iter()
824-            .any(|e| matches!(e, EventKind::BeadReleased { .. }));
825-        let mut action = BeadAction::Released;
826-        if release_succeeded {
827:            match self.increment_failure_count(store, bead).await {
828-                Ok(new_count) => {
829-                    let threshold = self.config.outcome.quarantine_after_failures;
830-                    if threshold > 0 && new_count >= threshold {
831-                        match self
832-                            .quarantine_bead(store, bead, new_count, threshold)
833-                            .await
834-                        {
835-                            Ok(quarantine_events) => {
--
886-        let mut events = self.release_bead(store, bead).await?;
887-
888-        // If release succeeded, increment failure count and add deferred label.
889-        let release_succeeded = events
890-            .iter()
891-            .any(|e| matches!(e, EventKind::BeadReleased { .. }));
892-        if release_succeeded {
893-            // Increment failure count for auto-split tracking.
894:            if let Err(e) = self.increment_failure_count(store, bead).await {
895-                tracing::warn!(
896-                    bead_id = %bead.id,
897-                    error = %e,
898-                    "failed to increment failure count after timeout"
899-                );
900-            }
901-
902-            if let Err(e) = tokio::time::timeout(
--
1032-    ///
1033-    /// Labels follow the pattern `failure-count:N`. If `failure-count:2` exists,
1034-    /// the old label is removed and `failure-count:3` is added.
1035-    ///
1036-    /// Returns the new failure count (or 0 if the operation failed).
1037-    ///
1038-    /// All `br` calls are wrapped in timeouts to prevent indefinite hang in
1039-    /// HANDLING state. Failures are non-fatal — we log and continue.
1040:    async fn increment_failure_count(&self, store: &dyn BeadStore, bead: &Bead) -> Result<u32> {
1041-        // Read labels with timeout.
1042-        let labels =
1043-            match tokio::time::timeout(std::time::Duration::from_secs(30), store.labels(&bead.id))
1044-                .await
1045-            {
1046-                Ok(Ok(l)) => l,
1047-                Ok(Err(e)) => {
1048-                    tracing::warn!(
2026-08-21 00:14:51.943837053 -0400 src/outcome/mod.rs
2026-08-21 00:14:51.947837036 -0400 tests/property_tests.rs
2026-08-21 00:14:51.947837036 -0400 tests/integration_tests.rs

codex
The defect is isolated: the public three-input classifier currently discards `verified`. The downstream failure handler already releases the bead and increments its failure count after a successful release, and an exact exit-0/verification-false regression test already exists. I’m restoring verification as a hard gate while preserving interruption precedence and the existing nonzero exit classifications.
apply patch
patch: completed
/home/coding/NEEDLE/src/outcome/mod.rs
diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..6603d4303f4cddbc9774591af4a8d1c7d6bbd299
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -27,8 +27,16 @@
 /// Classify an agent exit code into an `Outcome`, with shutdown signal support.
 ///
 /// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..6603d4303f4cddbc9774591af4a8d1c7d6bbd299
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -27,8 +27,16 @@
 /// Classify an agent exit code into an `Outcome`, with shutdown signal support.
 ///
 /// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────

exec
/bin/bash -lc 'git diff --check -- src/outcome/mod.rs && git diff -- src/outcome/mod.rs && cargo test --lib outcome::tests::classify' in /home/coding/NEEDLE
diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..6603d4303f4cddbc9774591af4a8d1c7d6bbd299
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -27,8 +27,16 @@
 /// Classify an agent exit code into an `Outcome`, with shutdown signal support.
 ///
 /// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..6603d4303f4cddbc9774591af4a8d1c7d6bbd299
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -27,8 +27,16 @@
 /// Classify an agent exit code into an `Outcome`, with shutdown signal support.
 ///
 /// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..6603d4303f4cddbc9774591af4a8d1c7d6bbd299
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -27,8 +27,16 @@
 /// Classify an agent exit code into an `Outcome`, with shutdown signal support.
 ///
 /// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────

codex
The focused test build is still compiling under the host’s resource-limited local Cargo path. The code diff is clean; once this targeted suite returns, I’ll run the mandated fast gate against the shared checkout and distinguish any unrelated failures from this change.
 succeeded in 105367ms:
   Compiling needle v0.4.2 (/home/coding/NEEDLE)
    Finished `test` profile [optimized + debuginfo] target(s) in 1m 45s
     Running unittests src/lib.rs (/home/coding/target/debug/deps/needle-6b596088731b1bec)

running 4 tests
test outcome::tests::classify_no_wildcard_arms ... ok
test outcome::tests::classify_not_interrupted_uses_exit_code_and_verification ... ok
test outcome::tests::classify_verification_gate ... ok
test outcome::tests::classify_was_interrupted_always_returns_interrupted ... ok

test result: ok. 4 passed; 0 failed; 0 ignored; 0 measured; 2527 filtered out; finished in 0.00s


diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..6603d4303f4cddbc9774591af4a8d1c7d6bbd299
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -27,8 +27,16 @@
 /// Classify an agent exit code into an `Outcome`, with shutdown signal support.
 ///
 /// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────

exec
/bin/bash -lc "rg -n \"gate_failure|verification.*fail|failure_count.*release|release.*failure_count\" src/outcome/mod.rs | tail -n 100
sed -n '650,790p' src/outcome/mod.rs
sed -n '1630,1900p' src/outcome/mod.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
312:                        self.handle_gate_failure(store, bead, &report).await?
445:    /// If verification failed, classification produces Failure and handle_failure
496:                            return self.handle_gate_failure(store, bead, &report).await;
664:    async fn handle_gate_failure(
685:        // Emit verification failure telemetry.
701:                    "reopening bead closed by agent (verification failed)"
713:                            outcome: "gate_failure".to_string(),
727:                    outcome: "gate_failure".to_string(),
789:        // Add a label indicating verification failure.
792:            store.add_label(&bead.id, "verification-failed"),
799:                "failed to add verification-failed label"
1571:        // Exit code 0 with verification fails → Failure
2042:    async fn handle_success_verification_fails_releases_bead() {
2061:            "verification failure must release bead"
2065:                |a| matches!(a, StoreAction::AddLabel(_, label) if label == "verification-failed")
2067:            "verification failure must add verification-failed label"
2072:    async fn handle_success_verification_fails_reopens_closed_bead() {
2073:        // Agent closed the bead, but verification fails → reopen then release.
2090:            "verification failure on closed bead must reopen it first"
2096:            "verification failure must release bead after reopening"
2101:    async fn handle_success_verification_fails_increments_failure_count() {
2116:            "verification failure must increment failure count"
                tracing::debug!(bead_id = %bead.id, "flushed bead state to JSONL after success");
            }
            Ok(None) => {
                tracing::warn!(bead_id = %bead.id, "flush timed out after success — JSONL may lag SQLite");
            }
            Err(e) => {
                tracing::warn!(bead_id = %bead.id, error = %e, "flush failed after success — JSONL may lag SQLite");
            }
        }

        Ok((BeadAction::None, events))
    }

    /// Handle gate failure: reopen the bead if it was closed, then release it.
    async fn handle_gate_failure(
        &self,
        store: &dyn BeadStore,
        bead: &Bead,
        report: &crate::validation::GateReport,
    ) -> Result<(BeadAction, Vec<EventKind>)> {
        // Find the first failing gate for telemetry.
        let (failed_gate, reason) = report
            .results
            .iter()
            .find(|(_, r)| !r.passed())
            .map(|(name, r)| (name.clone(), r.failure_reason().unwrap_or("").to_string()))
            .unwrap_or_else(|| ("unknown".to_string(), "unknown error".to_string()));

        tracing::warn!(
            bead_id = %bead.id,
            gate = %failed_gate,
            reason = %reason,
            "validation gate failed — releasing bead"
        );

        // Emit verification failure telemetry.
        self.telemetry.emit(EventKind::VerificationFailed {
            bead_id: bead.id.clone(),
            command: failed_gate.clone(),
            exit_code: None,
            output: reason,
        })?;

        let mut events = Vec::new();

        // If the agent already closed the bead, reopen it before releasing.
        // Use timeout to prevent indefinite hang in HANDLING state.
        match self.timeout_op(|| store.show(&bead.id), "show").await {
            Ok(Some(current)) if current.status.is_done() => {
                tracing::info!(
                    bead_id = %bead.id,
                    "reopening bead closed by agent (verification failed)"
                );
                match self.timeout_op(|| store.reopen(&bead.id), "reopen").await {
                    Ok(_) => {}
                    Err(e) => {
                        tracing::warn!(
                            bead_id = %bead.id,
                            error = %e,
                            "failed to reopen bead — attempting release anyway"
                        );
                        events.push(EventKind::WorkerHandlingTimeout {
                            bead_id: bead.id.clone(),
                            outcome: "gate_failure".to_string(),
                            operation: "reopen".to_string(),
                            error: e.to_string(),
                        });
                    }
                }
            }
            Ok(None) => {
                tracing::warn!(
                    bead_id = %bead.id,
                    "show() timed out during gate failure handling, skipping reopen"
                );
                events.push(EventKind::WorkerHandlingTimeout {
                    bead_id: bead.id.clone(),
                    outcome: "gate_failure".to_string(),
                    operation: "show".to_string(),
                    error: "timeout after 30s".to_string(),
                });
            }
            Err(e) => {
                tracing::warn!(
                    bead_id = %bead.id,
                    error = %e,
                    "show() failed during gate failure handling"
                );
            }
            _ => {}
        }

        // Release the bead back to open with flush-before-release and sync recovery.
        let mut release_events = self.release_bead(store, bead).await?;
        events.append(&mut release_events);

        // If release succeeded, increment the failure count and apply the same
        // quarantine ceiling `handle_failure` uses. Without this, a bead that
        // fails a gate every cycle is released back to open forever: the
        // ARMOR/bf-135k storm ran one bead 24 times in a single day, each
        // attempt leaving another commit behind. A gate failure is no less
        // repeatable than an agent failure and must respect the same ceiling.
        let release_succeeded = events
            .iter()
            .any(|e| matches!(e, EventKind::BeadReleased { .. }));
        let mut action = BeadAction::Released;
        if release_succeeded {
            match self.increment_failure_count(store, bead).await {
                Ok(new_count) => {
                    let threshold = self.config.outcome.quarantine_after_failures;
                    if threshold > 0 && new_count >= threshold {
                        match self
                            .quarantine_bead(store, bead, new_count, threshold)
                            .await
                        {
                            Ok(quarantine_events) => {
                                events.extend(quarantine_events);
                                action = BeadAction::Quarantined;
                            }
                            Err(e) => {
                                tracing::warn!(
                                    bead_id = %bead.id,
                                    error = %e,
                                    "failed to quarantine bead after exceeding gate-failure threshold"
                                );
                            }
                        }
                    }
                }
                Err(e) => {
                    tracing::warn!(
                        bead_id = %bead.id,
                        error = %e,
                        "failed to increment failure count after gate failure"
                    );
                }
            }
        }

        // Add a label indicating verification failure.
        if let Err(e) = tokio::time::timeout(
        assert!(!result.telemetry_events.is_empty());
        let actions = store.actions();
        assert!(
            !actions.iter().any(|a| matches!(a, StoreAction::Release(_))),
            "success should not release bead"
        );
    }

    #[tokio::test]
    async fn handle_success_bead_still_open_releases_to_prevent_leak() {
        let handler = test_handler();
        let store = test_store(BeadStatus::InProgress);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(0), false)
            .await
            .unwrap();

        assert_eq!(result.outcome, Outcome::Success);
        assert_eq!(
            result.bead_action,
            BeadAction::Released,
            "bead still open after success must be released to enforce postcondition"
        );
        let actions = store.actions();
        assert!(
            actions.iter().any(|a| matches!(a, StoreAction::Show(_))),
            "success should check bead status"
        );
        assert!(
            result
                .telemetry_events
                .iter()
                .any(|e| matches!(e, EventKind::BeadOrphaned { .. })),
            "orphan event should be emitted even though bead is released"
        );
        assert!(
            actions.iter().any(|a| matches!(a, StoreAction::Release(_))),
            "bead must be released when still open after success"
        );
    }

    #[tokio::test]
    async fn handle_failure_releases_and_increments_count() {
        let handler = test_handler();
        let store = test_store(BeadStatus::InProgress);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(1), false)
            .await
            .unwrap();

        assert_eq!(result.outcome, Outcome::Failure);
        assert_eq!(result.bead_action, BeadAction::Released);
        assert!(!result.telemetry_events.is_empty());

        let actions = store.actions();
        assert!(
            actions
                .iter()
                .any(|a| matches!(a, StoreAction::Release(id) if id == "needle-test")),
            "failure must release bead"
        );
        assert!(
            actions.iter().any(
                |a| matches!(a, StoreAction::AddLabel(_, label) if label == "failure-count:1")
            ),
            "failure must add failure-count:1"
        );
    }

    #[tokio::test]
    async fn handle_failure_increments_existing_count() {
        let handler = test_handler();
        let store = Arc::new(
            MockBeadStore::new(BeadStatus::InProgress)
                .with_labels(vec!["failure-count:2".to_string()]),
        );
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(store.as_ref(), &bead, &test_output(1), false)
            .await
            .unwrap();

        assert_eq!(result.bead_action, BeadAction::Released);
        let actions = store.actions();
        assert!(
            actions.iter().any(
                |a| matches!(a, StoreAction::RemoveLabel(_, label) if label == "failure-count:2")
            ),
            "should remove old failure-count label"
        );
        assert!(
            actions.iter().any(
                |a| matches!(a, StoreAction::AddLabel(_, label) if label == "failure-count:3")
            ),
            "should add failure-count:3"
        );
    }

    // ── ADR-012: failure-quarantine circuit breaker ──

    #[tokio::test]
    async fn handle_failure_quarantines_bead_at_threshold() {
        // Default quarantine_after_failures is 5. A bead already at
        // failure-count:4 crosses the threshold on this attempt.
        let handler = test_handler();
        let store = MockBeadStore::new(BeadStatus::InProgress)
            .with_labels(vec!["failure-count:4".to_string()]);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(1), false)
            .await
            .unwrap();

        assert_eq!(result.bead_action, BeadAction::Quarantined);
        let actions = store.actions();
        assert!(
            actions
                .iter()
                .any(|a| matches!(a, StoreAction::Block(id) if id == "needle-test")),
            "5th consecutive failure must set status=blocked"
        );
        assert!(
            actions
                .iter()
                .any(|a| matches!(a, StoreAction::AddLabel(_, label) if label == "cycling")),
            "quarantine must add the cycling label"
        );
        assert!(
            result.telemetry_events.iter().any(|e| matches!(
                e,
                EventKind::BeadQuarantined {
                    failure_count: 5,
                    threshold: 5,
                    ..
                }
            )),
            "must emit BeadQuarantined with the crossing count and configured threshold"
        );
    }

    #[tokio::test]
    async fn handle_failure_below_threshold_does_not_quarantine() {
        // Same setup as the threshold test, one failure count lower — this is
        // the regression case for the mitosis NotSplittable fallthrough
        // (ADR-006 Context point 2): a bead below the ceiling still just
        // releases normally, it does not get blocked prematurely.
        let handler = test_handler();
        let store = MockBeadStore::new(BeadStatus::InProgress)
            .with_labels(vec!["failure-count:3".to_string()]);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(1), false)
            .await
            .unwrap();

        assert_eq!(result.bead_action, BeadAction::Released);
        let actions = store.actions();
        assert!(
            !actions.iter().any(|a| matches!(a, StoreAction::Block(_))),
            "4th consecutive failure must not yet quarantine"
        );
    }

    #[tokio::test]
    async fn handle_failure_quarantine_disabled_when_threshold_zero() {
        let mut config = Config::default();
        config.outcome.quarantine_after_failures = 0;
        let handler = test_handler_with_config(config);
        let store = MockBeadStore::new(BeadStatus::InProgress)
            .with_labels(vec!["failure-count:99".to_string()]);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(1), false)
            .await
            .unwrap();

        assert_eq!(result.bead_action, BeadAction::Released);
        let actions = store.actions();
        assert!(
            !actions.iter().any(|a| matches!(a, StoreAction::Block(_))),
            "threshold=0 must disable quarantine entirely, regardless of failure count"
        );
    }

    #[tokio::test]
    async fn handle_success_resets_failure_count() {
        // Success should reset failure count by removing all failure-count:N labels.
        let handler = test_handler();
        let store =
            MockBeadStore::new(BeadStatus::Done).with_labels(vec!["failure-count:3".to_string()]);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(0), false)
            .await
            .unwrap();

        assert_eq!(result.outcome, Outcome::Success);
        let actions = store.actions();
        assert!(
            actions.iter().any(
                |a| matches!(a, StoreAction::RemoveLabel(_, label) if label == "failure-count:3")
            ),
            "should remove failure-count:3 on success"
        );
    }

    #[tokio::test]
    async fn handle_timeout_releases_and_adds_deferred() {
        let handler = test_handler();
        let store = test_store(BeadStatus::InProgress);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(124), false)
            .await
            .unwrap();

        assert_eq!(result.outcome, Outcome::Timeout);
        assert_eq!(result.bead_action, BeadAction::Deferred);

        let actions = store.actions();
        assert!(
            actions
                .iter()
                .any(|a| matches!(a, StoreAction::Release(id) if id == "needle-test")),
            "timeout must release bead"
        );
        assert!(
            actions
                .iter()
                .any(|a| matches!(a, StoreAction::AddLabel(_, label) if label == "deferred")),
            "timeout must add deferred label"
        );
    }

    #[tokio::test]
    async fn handle_crash_releases_and_creates_alert_bead() {
        let handler = test_handler();
        let store = test_store(BeadStatus::InProgress);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(137), false)
            .await
            .unwrap();

        assert_eq!(result.outcome, Outcome::Crash(137));
        assert_eq!(result.bead_action, BeadAction::Alerted);

        let actions = store.actions();
        assert!(
            actions
                .iter()
                .any(|a| matches!(a, StoreAction::Release(id) if id == "needle-test")),
            "crash must release bead"
        );
        assert!(
            actions.iter().any(
                |a| matches!(a, StoreAction::CreateBead(title, _) if title.contains("needle-test"))
            ),
            "crash must create alert bead referencing the original bead"
        );

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..6603d4303f4cddbc9774591af4a8d1c7d6bbd299
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -27,8 +27,16 @@
 /// Classify an agent exit code into an `Outcome`, with shutdown signal support.
 ///
 /// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────

exec
/bin/bash -lc 'cargo test --lib handle_success_verification_fails' in /home/coding/NEEDLE
 exited 101 in 9ms:
     Running unittests src/lib.rs (/home/coding/target/debug/deps/needle-6b596088731b1bec)

running 3 tests
test outcome::tests::handle_success_verification_fails_increments_failure_count ... ok
test outcome::tests::handle_success_verification_fails_releases_bead ... FAILED
test outcome::tests::handle_success_verification_fails_reopens_closed_bead ... ok

failures:

---- outcome::tests::handle_success_verification_fails_releases_bead stdout ----

thread 'outcome::tests::handle_success_verification_fails_releases_bead' (2193490) panicked at src/outcome/mod.rs:2053:9:
assertion `left == right` failed
  left: Failure
 right: Success
note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace


failures:
    outcome::tests::handle_success_verification_fails_releases_bead

test result: FAILED. 2 passed; 1 failed; 0 ignored; 0 measured; 2528 filtered out; finished in 0.00s

error: test failed, to rerun pass `--lib`

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..6603d4303f4cddbc9774591af4a8d1c7d6bbd299
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -27,8 +27,16 @@
 /// Classify an agent exit code into an `Outcome`, with shutdown signal support.
 ///
 /// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────

codex
The downstream regression suite exposed one stale assertion: the verification-failure path correctly releases and counts the failure, but the test still expected the old `Success` outcome. I’m updating that assertion to the new contract; the release and failure-count tests themselves already pass.
exec
/bin/bash -lc "sed -n '2010,2130p' src/outcome/mod.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(0), false)
            .await
            .unwrap();

        assert_eq!(result.outcome, Outcome::Success);
        assert_eq!(result.bead_action, BeadAction::None);
    }

    #[tokio::test]
    async fn handle_success_verification_passes_accepts_closure() {
        // Verification passes → bead closure accepted.
        let handler = test_handler_with_verification(vec!["true".to_string()]);
        let store = test_store(BeadStatus::Done);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(0), false)
            .await
            .unwrap();

        assert_eq!(result.outcome, Outcome::Success);
        assert_eq!(result.bead_action, BeadAction::None);
        assert!(result
            .telemetry_events
            .iter()
            .any(|e| matches!(e, EventKind::BeadCompleted { .. })));
    }

    #[tokio::test]
    async fn handle_success_verification_fails_releases_bead() {
        // Verification fails → bead released.
        let handler = test_handler_with_verification(vec!["false".to_string()]);
        let store = test_store(BeadStatus::InProgress);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(0), false)
            .await
            .unwrap();

        assert_eq!(result.outcome, Outcome::Success);
        assert_eq!(result.bead_action, BeadAction::Released);

        let actions = store.actions();
        assert!(
            actions
                .iter()
                .any(|a| matches!(a, StoreAction::Release(id) if id == "needle-test")),
            "verification failure must release bead"
        );
        assert!(
            actions.iter().any(
                |a| matches!(a, StoreAction::AddLabel(_, label) if label == "verification-failed")
            ),
            "verification failure must add verification-failed label"
        );
    }

    #[tokio::test]
    async fn handle_success_verification_fails_reopens_closed_bead() {
        // Agent closed the bead, but verification fails → reopen then release.
        let handler = test_handler_with_verification(vec!["false".to_string()]);
        let store = test_store(BeadStatus::Done);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(0), false)
            .await
            .unwrap();

        assert_eq!(result.bead_action, BeadAction::Released);

        let actions = store.actions();
        assert!(
            actions
                .iter()
                .any(|a| matches!(a, StoreAction::Reopen(id) if id == "needle-test")),
            "verification failure on closed bead must reopen it first"
        );
        assert!(
            actions
                .iter()
                .any(|a| matches!(a, StoreAction::Release(id) if id == "needle-test")),
            "verification failure must release bead after reopening"
        );
    }

    #[tokio::test]
    async fn handle_success_verification_fails_increments_failure_count() {
        let handler = test_handler_with_verification(vec!["false".to_string()]);
        let store = test_store(BeadStatus::InProgress);
        let bead = test_bead(BeadStatus::InProgress);

        let _result = handler
            .handle(&store, &bead, &test_output(0), false)
            .await
            .unwrap();

        let actions = store.actions();
        assert!(
            actions.iter().any(
                |a| matches!(a, StoreAction::AddLabel(_, label) if label == "failure-count:1")
            ),
            "verification failure must increment failure count"
        );
    }

    #[tokio::test]
    async fn handle_success_multiple_gates_first_fails() {
        // First gate passes, second fails → should stop and release.
        let handler = test_handler_with_verification(vec![
            "true".to_string(),
            "false".to_string(),
            "echo should-not-run".to_string(),
        ]);
        let store = test_store(BeadStatus::InProgress);
        let bead = test_bead(BeadStatus::InProgress);


diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..6603d4303f4cddbc9774591af4a8d1c7d6bbd299
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -27,8 +27,16 @@
 /// Classify an agent exit code into an `Outcome`, with shutdown signal support.
 ///
 /// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────

apply patch
patch: completed
/home/coding/NEEDLE/src/outcome/mod.rs
diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..a8549ef801608b1e2730a2bdb3caf266f574180c
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -1617,7 +1627,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::None);
         assert!(!result.telemetry_events.is_empty());
         let actions = store.actions();

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..a8549ef801608b1e2730a2bdb3caf266f574180c
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -1617,7 +1627,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::None);
         assert!(!result.telemetry_events.is_empty());
         let actions = store.actions();

exec
/bin/bash -lc "git diff -- src/outcome/mod.rs && sed -n '2000,2070p' src/outcome/mod.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 996c217..a8549ef 100644
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@ use crate::validation::{verify_shipped_work, GateConfig, GateReport, ValidationG
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
 pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted, verified)
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -1617,7 +1627,7 @@ mod tests {
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::None);
         assert!(!result.telemetry_events.is_empty());
         let actions = store.actions();
            verification: commands,
            ..Config::default()
        };
        let telemetry = Telemetry::with_sink("test-worker".to_string(), NopSink);
        OutcomeHandler::new(config, telemetry)
    }

    #[tokio::test]
    async fn handle_success_no_verification_default_behavior() {
        // No verification configured → normal success flow (unchanged behavior).
        let handler = test_handler();
        let store = test_store(BeadStatus::Done);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(0), false)
            .await
            .unwrap();

        assert_eq!(result.outcome, Outcome::Success);
        assert_eq!(result.bead_action, BeadAction::None);
    }

    #[tokio::test]
    async fn handle_success_verification_passes_accepts_closure() {
        // Verification passes → bead closure accepted.
        let handler = test_handler_with_verification(vec!["true".to_string()]);
        let store = test_store(BeadStatus::Done);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(0), false)
            .await
            .unwrap();

        assert_eq!(result.outcome, Outcome::Success);
        assert_eq!(result.bead_action, BeadAction::None);
        assert!(result
            .telemetry_events
            .iter()
            .any(|e| matches!(e, EventKind::BeadCompleted { .. })));
    }

    #[tokio::test]
    async fn handle_success_verification_fails_releases_bead() {
        // Verification fails → bead released.
        let handler = test_handler_with_verification(vec!["false".to_string()]);
        let store = test_store(BeadStatus::InProgress);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(0), false)
            .await
            .unwrap();

        assert_eq!(result.outcome, Outcome::Success);
        assert_eq!(result.bead_action, BeadAction::Released);

        let actions = store.actions();
        assert!(
            actions
                .iter()
                .any(|a| matches!(a, StoreAction::Release(id) if id == "needle-test")),
            "verification failure must release bead"
        );
        assert!(
            actions.iter().any(
                |a| matches!(a, StoreAction::AddLabel(_, label) if label == "verification-failed")
            ),
            "verification failure must add verification-failed label"
        );

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..a8549ef801608b1e2730a2bdb3caf266f574180c
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -1617,7 +1627,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::None);
         assert!(!result.telemetry_events.is_empty());
         let actions = store.actions();

exec
/bin/bash -lc "sed -n '1585,1640p' src/outcome/mod.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
        // Verify key exit codes map correctly per spec.
        // Exit code 0 ONLY succeeds when verification passes
        assert_eq!(classify(0, false, true), Outcome::Success);
        assert_eq!(classify(0, false, false), Outcome::Failure);
        assert_eq!(classify(1, false, true), Outcome::Failure);
        assert_eq!(classify(2, false, true), Outcome::Failure);
        assert_eq!(classify(99, false, true), Outcome::Failure);
        assert_eq!(classify(100, false, true), Outcome::Failure);
        assert_eq!(classify(124, false, true), Outcome::Timeout);
        assert_eq!(classify(125, false, true), Outcome::Failure);
        assert_eq!(classify(128, false, true), Outcome::Failure); // not >128 per spec
        assert_eq!(classify(129, false, true), Outcome::Crash(129));
        assert_eq!(classify(137, false, true), Outcome::Crash(137));
        assert_eq!(classify(-9, false, true), Outcome::Crash(-9));
    }

    #[test]
    fn classify_verification_gate() {
        // The core fix: exit code 0 does NOT guarantee Success.
        // Verification is the gate.
        assert_eq!(
            classify(0, false, false),
            Outcome::Failure,
            "exit_code=0, verified=false must return Failure"
        );
        assert_eq!(
            classify(0, false, true),
            Outcome::Success,
            "exit_code=0, verified=true must return Success"
        );
    }

    // ── handle tests ──

    #[tokio::test]
    async fn handle_success_bead_closed_by_agent() {
        let handler = test_handler();
        let store = test_store(BeadStatus::Done);
        let bead = test_bead(BeadStatus::InProgress);

        let result = handler
            .handle(&store, &bead, &test_output(0), false)
            .await
            .unwrap();

        assert_eq!(result.outcome, Outcome::Failure);
        assert_eq!(result.bead_action, BeadAction::None);
        assert!(!result.telemetry_events.is_empty());
        let actions = store.actions();
        assert!(
            !actions.iter().any(|a| matches!(a, StoreAction::Release(_))),
            "success should not release bead"
        );
    }

    #[tokio::test]

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..a8549ef801608b1e2730a2bdb3caf266f574180c
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -1617,7 +1627,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::None);
         assert!(!result.telemetry_events.is_empty());
         let actions = store.actions();

apply patch
patch: completed
/home/coding/NEEDLE/src/outcome/mod.rs
diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..5dc20f7833575e91a64cf378d2e2200e1a79ac6a
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..5dc20f7833575e91a64cf378d2e2200e1a79ac6a
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();

exec
/bin/bash -lc 'git diff --check -- src/outcome/mod.rs && cargo test --lib outcome::tests::classify && cargo test --lib handle_success_verification_fails' in /home/coding/NEEDLE
diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..5dc20f7833575e91a64cf378d2e2200e1a79ac6a
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..5dc20f7833575e91a64cf378d2e2200e1a79ac6a
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..5dc20f7833575e91a64cf378d2e2200e1a79ac6a
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();

 succeeded in 117565ms:
   Compiling needle v0.4.2 (/home/coding/NEEDLE)
    Finished `test` profile [optimized + debuginfo] target(s) in 1m 55s
     Running unittests src/lib.rs (/home/coding/target/debug/deps/needle-6b596088731b1bec)

running 4 tests
test outcome::tests::classify_no_wildcard_arms ... ok
test outcome::tests::classify_not_interrupted_uses_exit_code_and_verification ... ok
test outcome::tests::classify_verification_gate ... ok
test outcome::tests::classify_was_interrupted_always_returns_interrupted ... ok

test result: ok. 4 passed; 0 failed; 0 ignored; 0 measured; 2527 filtered out; finished in 0.00s

    Blocking waiting for file lock on artifact directory
    Finished `test` profile [optimized + debuginfo] target(s) in 2.50s
     Running unittests src/lib.rs (/home/coding/target/debug/deps/needle-6b596088731b1bec)

running 3 tests
test outcome::tests::handle_success_verification_fails_increments_failure_count ... ok
test outcome::tests::handle_success_verification_fails_releases_bead ... ok
test outcome::tests::handle_success_verification_fails_reopens_closed_bead ... ok

test result: ok. 3 passed; 0 failed; 0 ignored; 0 measured; 2528 filtered out; finished in 0.00s


diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..5dc20f7833575e91a64cf378d2e2200e1a79ac6a
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();

codex
The focused checks now pass: all four classification tests, plus all three verification-failure routing tests covering release, reopen, and failure-count increment. I’m starting the required fast definition-of-done gate now; it may report shared-checkout issues from unrelated in-flight files, which I’ll keep separate from this bead’s one-file diff.
exec
/bin/bash -lc 'scripts/definition-of-done.sh --fast' in /home/coding/NEEDLE
diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..5dc20f7833575e91a64cf378d2e2200e1a79ac6a
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();

 succeeded in 56284ms:
✓ cargo fmt --check passed
Running: cargo clippy...
✓ cargo clippy passed
Running: cargo check...
✓ cargo check passed

=== Definition of Done Summary ===
Lane: fast
Checks run: 3
Failures: 0
✓ Definition of Done

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..5dc20f7833575e91a64cf378d2e2200e1a79ac6a
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();

codex
The mandated fast gate is clean: formatting, Clippy across all targets, and `cargo check` all passed. I’m reviewing and staging only [src/outcome/mod.rs](/home/coding/NEEDLE/src/outcome/mod.rs), leaving every unrelated shared-checkout change untouched.
exec
/bin/bash -lc 'git status --short --untracked-files=no | tail -n 30
git diff --check -- src/outcome/mod.rs
git diff -- src/outcome/mod.rs
git add src/outcome/mod.rs
git diff --cached --check
git diff --cached --stat
git diff --cached -- src/outcome/mod.rs' in /home/coding/NEEDLE
 succeeded in 41ms:
 M .beads/traces/needle-c0464426/stdout.txt
 M .beads/traces/needle-c3e2cb9a/metadata.json
 M .beads/traces/needle-c3e2cb9a/stdout.txt
 M .beads/traces/needle-c8c008be/metadata.json
 M .beads/traces/needle-c8c008be/stdout.txt
 M .beads/traces/needle-ceea8c53/metadata.json
 M .beads/traces/needle-ceea8c53/stdout.txt
 M .beads/traces/needle-cfe0ad63/metadata.json
 M .beads/traces/needle-cfe0ad63/stdout.txt
 M .beads/traces/needle-d1b2ee0d/metadata.json
 M .beads/traces/needle-d1b2ee0d/stdout.txt
 M .beads/traces/needle-de6bb8ad/metadata.json
 M .beads/traces/needle-de6bb8ad/stdout.txt
 M .beads/traces/needle-e9df85fc/metadata.json
 M .beads/traces/needle-e9df85fc/stdout.txt
 M .beads/traces/needle-ea26063c/metadata.json
 M .beads/traces/needle-ea26063c/stdout.txt
 M .beads/traces/needle-ea87796e/metadata.json
 M .beads/traces/needle-ea87796e/stdout.txt
 M .beads/traces/needle-ebede907/metadata.json
 M .beads/traces/needle-ebede907/stdout.txt
 M .beads/traces/needle-f78eebbb/metadata.json
 M .beads/traces/needle-f78eebbb/stdout.txt
 M .needle-predispatch-sha
 M .needle.yaml
 M install.sh
 M src/outcome/mod.rs
 M tests/installer/test_checksum_verification.sh
 M tests/integration_tests.rs
 M tests/property_tests.rs
diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 996c217..5dc20f7 100644
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@ use crate::validation::{verify_shipped_work, GateConfig, GateReport, ValidationG
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
 pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted, verified)
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@ mod tests {
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();
 src/outcome/mod.rs | 18 ++++++++++++++----
 1 file changed, 14 insertions(+), 4 deletions(-)
diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 996c217..5dc20f7 100644
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@ use crate::validation::{verify_shipped_work, GateConfig, GateReport, ValidationG
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
 pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted, verified)
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@ mod tests {
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..5dc20f7833575e91a64cf378d2e2200e1a79ac6a
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();

exec
/bin/bash -lc 'git commit -m "fix(needle-7e9ea193): gate success on verification"' in /home/coding/NEEDLE
 succeeded in 1259ms:
=== Running Definition of Done (fast lane) ===
NEEDLE_VERIFICATION_GATE: definition-of-done
=== Fast Lane Checks ===
Running: cargo fmt --check...
✓ cargo fmt --check passed
Running: cargo clippy...
✓ cargo clippy passed
Running: cargo check...
✓ cargo check passed

=== Definition of Done Summary ===
Lane: fast
Checks run: 3
Failures: 0
✓ Definition of Done
=== Running checkpoint verification ===
[main 5a97c02] fix(needle-7e9ea193): gate success on verification
 1 file changed, 14 insertions(+), 4 deletions(-)

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..5dc20f7833575e91a64cf378d2e2200e1a79ac6a
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();

codex
The implementation is committed as `5a97c02`, with the pre-commit fast gate also passing. I’m pushing that main-branch commit now, then I’ll close the bead with the verified behavior and checks recorded.
exec
/bin/bash -lc 'git push origin main' in /home/coding/NEEDLE
 succeeded in 1327ms:
To https://git.ardenone.com/jedarden/NEEDLE.git
   af97791..5a97c02  main -> main

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..5dc20f7833575e91a64cf378d2e2200e1a79ac6a
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();

exec
/bin/bash -lc 'bead close needle-7e9ea193 --reason "Fixed classification so interrupted runs remain Interrupted, unverified runs are Failure, and only verified runs can delegate to exit-code classification. Updated verification-failure routing test to expect Failure; confirmed failed verification releases the bead and increments failure-count. Focused tests and scripts/definition-of-done.sh --fast pass. Shipped in commit 5a97c02." && bead sync flush-only' in /home/coding/NEEDLE
 succeeded in 0ms:
needle-7e9ea193
Flushed forensic checkpoint:
  Mode: monolithic
  Generation: gen-2d243f49c38d709ac63374f63fc3f474
  Issues: 1148
  Events: 4295
  Receipts: 0
  Total records: 5443
  Root hash: 1814c195fa2b101ad2e4b96e9e8facae1c736b7ee6e711742ddac00505494ef7
  Covered sequence: 4295
  Changed paths: 4
    objects/gen-2d243f49c38d709ac63374f63fc3f474.jsonl
    forensic.jsonl
    previous.json
    current.json

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..5dc20f7833575e91a64cf378d2e2200e1a79ac6a
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();

exec
/bin/bash -lc "bead show needle-7e9ea193 | sed -n '1,80p'
git status --short -- src/outcome/mod.rs
git rev-parse HEAD
git ls-remote origin refs/heads/main" in /home/coding/NEEDLE
 succeeded in 516ms:
5a97c02a8ed43fbe6d56b47f35cab8fd821d0c11	refs/heads/main

diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..5dc20f7833575e91a64cf378d2e2200e1a79ac6a
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();

codex
Implemented and shipped in commit `5a97c02`.

- [src/outcome/mod.rs](/home/coding/NEEDLE/src/outcome/mod.rs:32) now requires verification before returning `Success`.
- Exit `0` with failed verification returns `Failure`.
- Updated the routing test to expect `Failure`; release and failure-count behavior is verified.

Checks passed:

- 4 classification tests
- 3 verification-failure handling tests
- `scripts/definition-of-done.sh --fast`

Pushed to `origin/main`; bead `needle-7e9ea193` is closed.
diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 8099cb92c2de7531345cc5b32740cb78bd8b9136..5dc20f7833575e91a64cf378d2e2200e1a79ac6a
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -24,11 +24,21 @@
 // classify (convenience re-export)
 // ──────────────────────────────────────────────────────────────────────────────
 
-/// Classify an agent exit code into an `Outcome`, with shutdown signal support.
+/// Classify an agent result into an `Outcome`, with verification and shutdown
+/// signal support.
 ///
-/// Delegates to `Outcome::classify()`. Provided here for ergonomic imports.
-pub fn classify(exit_code: i32, was_interrupted: bool, _verified: bool) -> Outcome {
-    Outcome::classify(exit_code, was_interrupted)
+/// Interruption takes precedence. Otherwise, failed verification is always a
+/// failure; only verified results are delegated to the exit-code classifier.
+pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
+    if was_interrupted {
+        return Outcome::Interrupted;
+    }
+
+    if !verified {
+        return Outcome::Failure;
+    }
+
+    Outcome::classify(exit_code, false)
 }
 
 // ──────────────────────────────────────────────────────────────────────────────
@@ -2042,7 +2052,7 @@
             .await
             .unwrap();
 
-        assert_eq!(result.outcome, Outcome::Success);
+        assert_eq!(result.outcome, Outcome::Failure);
         assert_eq!(result.bead_action, BeadAction::Released);
 
         let actions = store.actions();

tokens used
98,044
