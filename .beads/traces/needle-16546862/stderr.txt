shell-init: error retrieving current directory: getcwd: cannot access parent directories: No such file or directory
chdir: error retrieving current directory: getcwd: cannot access parent directories: No such file or directory
OpenAI Codex v0.149.0
--------
workdir: /home/coding/NEEDLE
model: gpt-5.6-terra
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: xhigh
reasoning summaries: none
session id: 01a027ce-9840-7fb2-91f9-25daf737e50e
--------
user
[needle:codex-gpt-5.6-terra-xhigh-terra-zaiproxy:needle-16546862:explore]
## Task

Inventory and document current canary expectations

## Description

Read all four expected/*.yaml files under ~/.needle/canary/expected/ and document:

1. What each test asserts (final_status, labels, etc.)
2. Which assertions are stale vs. current
3. What bead-forge's actual vocabulary is (open, in_progress, blocked, closed, completed, pending, ready)

Deliverable: A summary in this bead's body listing:
- All four expected files and their current assertions
- Current bead-forge vocabulary (verify with 'bf show --json' on a real bead)
- What needs to change in each file

Acceptance: Bead body contains complete inventory of current expectations and what's stale.



## Workspace

/home/coding/NEEDLE

## Context Files

(no context files found)

## Instructions

(no workspace instructions)

Complete the task described above. When finished:
- **If you changed files, commit and push them.** `git commit`, then `git push`. Real
  changes are source, tests, config, or documentation the task actually asked for.
- **If the task needed no file changes, do NOT manufacture a commit.** Verification-only
  work, work that turned out to be already done, and work you found blocked are all
  legitimate outcomes with nothing to commit. Record the finding on the bead instead:

`bead update needle-16546862 --notes "what you checked and what you found"`

  Never create `notes/needle-16546862.md`, a summary, a report, or a status file to satisfy a
  commit requirement. Status belongs on the bead, not in git history. A commit whose only
  changes are under `notes/` or `.beads/` does not count as shipped work and will be
  rejected.
- Close the bead:

`bead close needle-16546862 --reason "..."`

If you cannot complete the task:
- Do NOT close the bead
- The bead will be automatically released for retry

Bead ID: needle-16546862
hook: SessionStart
hook: SessionStart Completed
codex
I’ll inspect the canary fixtures and query a real bead’s JSON before recording a precise inventory directly on `needle-16546862`.
exec
/bin/bash -lc "git status --short && printf '\\n-- guidance --\\n' && find .. -name AGENTS.md -print && printf '\\n-- needle config --\\n' && sed -n '1,180p' .needle.yaml && printf '\\n-- bead layout --\\n' && find .beads -maxdepth 2 -type f | sort | sed -n '1,80p' && printf '\\n-- binaries --\\n' && command -v bead && bead --version && command -v bf && bf --version" in /home/coding/NEEDLE
 exited 1 in 1991ms:
find: ‘../.local/share/docker/volumes/b006a21209eae85011ee9064418b63d8d33acf62db1dd123396acc05b43412fa/_data’: Permission denied
../.local/share/Trash/files/HOOP-removed-20260821/AGENTS.md
../.local/share/Trash/files/claudepr-1a29fae3-index.nV4KoC/AGENTS.md
../.local/share/Trash/files/claude-print-v020-gate.VQ7TNM/AGENTS.md
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/100/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/104/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/61/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29899/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33131/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33865/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/101/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/101/fs/home/nonroot’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32918/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/110/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/47/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32896/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/114/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29915/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33138/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32916/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32908/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/46/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/97/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/108/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/108/fs/var/cache/apt/archives/partial’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/18/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33860/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33860/fs/var/cache/apt/archives/partial’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/45/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/54/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32129/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/39/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35793/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33147/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32979/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33121/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33863/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/111/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32147/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32909/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/113/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32160/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32917/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35792/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32131/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32154/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/51/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32919/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/9/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35795/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33128/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/50/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35797/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35786/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/102/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/17/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/34/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33864/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/115/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/15/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35790/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33137/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32920/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/105/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33123/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32894/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33129/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35791/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/14/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32139/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35794/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/19/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/7/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32980/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32907/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29914/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29903/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/13/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32124/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32910/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33869/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32145/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/37/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/37/fs/var/cache/apt/archives/partial’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29901/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33144/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33871/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35796/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33867/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33122/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29898/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/31880/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32987/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/57/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/41/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/11/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29902/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32985/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35788/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32187/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33/fs/var/cache/apt/archives/partial’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/58/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/48/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29904/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29904/fs/var/cache/apt/archives/partial’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33873/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32986/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33120/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/55/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33862/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33136/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29910/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/49/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/106/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/56/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/36/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29911/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/44/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/96/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33139/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32899/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33874/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33868/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35785/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32895/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32900/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29900/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33130/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29908/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/40/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33143/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35798/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32893/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32893/fs/var/lib/apt/lists/partial’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32893/fs/var/cache/apt/archives/partial’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32911/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/112/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33861/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/43/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/31879/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29906/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/98/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33145/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29905/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33870/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/42/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32897/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/109/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/60/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33872/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/38/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/62/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/31882/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29913/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/16/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/53/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/12/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/10/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33146/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32981/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35787/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/31881/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/35789/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32120/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33866/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/99/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32138/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29912/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29907/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/32898/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/33140/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/29909/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/103/work/work’: Permission denied
find: ‘../.local/share/docker/containerd/daemon/io.containerd.snapshotter.v1.overlayfs/snapshots/59/work/work’: Permission denied
../declarative-config/.claude/worktrees/agent-a54c92149120be9c7/k8s/apexalgo-iad/AGENTS.md
../declarative-config/.claude/worktrees/agent-a1ec9db3e206474d0/k8s/apexalgo-iad/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/nixos/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/ardenone-manager/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/iad-ci/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/apexalgo-iad/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/ord-devimprint/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/iad-kalshi/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/rs-manager/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/ardenone-cluster/devpod/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/ardenone-cluster/AGENTS.md
../declarative-config/.claude/worktrees/agent-a2a39bd7253e9ae68/k8s/iad-options/AGENTS.md
../declarative-config/AGENTS.md
../declarative-config/nixos/AGENTS.md
../declarative-config/k8s/AGENTS.md
../declarative-config/k8s/ardenone-manager/AGENTS.md
../declarative-config/k8s/iad-ci/AGENTS.md
../declarative-config/k8s/apexalgo-iad/AGENTS.md
../declarative-config/k8s/ord-devimprint/AGENTS.md
../declarative-config/k8s/iad-kalshi/AGENTS.md
../declarative-config/k8s/rs-manager/AGENTS.md
../declarative-config/k8s/ardenone-cluster/devpod/AGENTS.md
../declarative-config/k8s/ardenone-cluster/AGENTS.md
../declarative-config/k8s/iad-options/AGENTS.md
../perplexity-jedarden/slides/node_modules/.pnpm/cytoscape@3.34.0/node_modules/cytoscape/AGENTS.md
../.cargo/git/checkouts/needle-c229d4d3b22a5d4a/c63dc9e/AGENTS.md
../.cargo/git/checkouts/beads_rust-18649610c6bb6d7c/29afe25/AGENTS.md
../.cargo/git/checkouts/beads_rust-18649610c6bb6d7c/29afe25/docs/agent/AGENTS.md
../.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zerocopy-0.8.56/AGENTS.md
../AGENTS.md
../scratch/bead-rs-msrv-tagcheck.yrwqjN/repo/AGENTS.md
../scratch/adr004-verify.K2laHE/AGENTS.md
../scratch/r031-verify.LgLVHI/AGENTS.md
../scratch/r031-verify.9YcAqo/AGENTS.md
../scratch/r031-verify.8QdCXp/AGENTS.md
../scratch/beadrs-r036-verify-17613f82/AGENTS.md
../scratch/esphome-source/AGENTS.md
../scratch/r029-verify.qOtIH4/AGENTS.md
../scratch/beadrs-deps.hrMcJg/AGENTS.md
../scratch/r031-verify-final.YxtEQh/AGENTS.md
../scratch/adr004-verify.DI6CCX/AGENTS.md
../scratch/beadrs-clean.ZTXBXE/AGENTS.md
../scratch/r031-verify.VOP2GN/AGENTS.md
../scratch/r031-verify.5fUN4y/AGENTS.md
../scratch/beadrs-r036-verify-17613f82-v2/AGENTS.md
../scratch/acb-ci-verify.GH80av/AGENTS.md
../scratch/acb-ci-verify.GH80av/nixos/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/ardenone-manager/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/iad-ci/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/apexalgo-iad/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/ord-devimprint/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/iad-kalshi/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/rs-manager/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/ardenone-cluster/devpod/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/ardenone-cluster/AGENTS.md
../scratch/acb-ci-verify.GH80av/k8s/iad-options/AGENTS.md
../scratch/r031-head-verify.UIGSZf/AGENTS.md
../scratch/beadrs-r036-verify-17613f82-v3/AGENTS.md
../scratch/r029-final.zzGhP8/AGENTS.md
../scratch/r032-clean.hFk4EV/AGENTS.md
../scratch/beadrs-r036-head-17613f82/AGENTS.md
../scratch/r031-verify.vQgRCH/AGENTS.md
../scratch/needle-ci-09133765.Hg9GbD/AGENTS.md
../scratch/r029-verify.41jjvT/AGENTS.md
../scratch/r031-baseline.TsCysB/AGENTS.md
../scratch/r031-verify.pI6fNc/AGENTS.md
../go/src/github.com/Dicklesworthstone/coding_agent_account_manager/AGENTS.md
../go/pkg/mod/github.com/clipperhouse/uax29/v2@v2.5.0/AGENTS.md
../go/pkg/mod/github.com/clipperhouse/displaywidth@v0.9.0/AGENTS.md
../SEAM/AGENTS.md
../.tmp/claude-1000/-home-coding/4291894b-2341-45a9-9115-4172ccfbc1a0/scratchpad/declarative-config-work/AGENTS.md
../.tmp/claude-1000/-home-coding/4291894b-2341-45a9-9115-4172ccfbc1a0/scratchpad/declarative-config-work/nixos/AGENTS.md
../.tmp/claude-1000/-home-coding/4291894b-2341-45a9-9115-4172ccfbc1a0/scratchpad/declarative-config-work/k8s/AGENTS.md
../.tmp/claude-1000/-home-coding/4291894b-2341-45a9-9115-4172ccfbc1a0/scratchpad/declarative-config-work/k8s/ardenone-manager/AGENTS.md
../.tmp/claude-1000/-home-coding/4291894b-2341-45a9-9115-4172ccfbc1a0/scratchpad/declarative-config-work/k8s/iad-ci/AGENTS.md
../.tmp/claude-1000/-home-coding/4291894b-2341-45a9-9115-4172ccfbc1a0/scratchpad/declarative-config-work/k8s/apexalgo-iad/AGENTS.md
../.tmp/claude-1000/-home-coding/4291894b-2341-45a9-9115-4172ccfbc1a0/scratchpad/declarative-config-work/k8s/ord-devimprint/AGENTS.md
../.tmp/claude-1000/-home-coding/4291894b-2341-45a9-9115-4172ccfbc1a0/scratchpad/declarative-config-work/k8s/iad-kalshi/AGENTS.md
../.tmp/claude-1000/-home-coding/4291894b-2341-45a9-9115-4172ccfbc1a0/scratchpad/declarative-config-work/k8s/rs-manager/AGENTS.md
../.tmp/claude-1000/-home-coding/4291894b-2341-45a9-9115-4172ccfbc1a0/scratchpad/declarative-config-work/k8s/ardenone-cluster/devpod/AGENTS.md
../.tmp/claude-1000/-home-coding/4291894b-2341-45a9-9115-4172ccfbc1a0/scratchpad/declarative-config-work/k8s/ardenone-cluster/AGENTS.md
../.tmp/claude-1000/-home-coding/4291894b-2341-45a9-9115-4172ccfbc1a0/scratchpad/declarative-config-work/k8s/iad-options/AGENTS.md
../NEEDLE/AGENTS.md
../vibe-coding-discovery/AGENTS.md
../jedarden.com/node_modules/png-to-ico/AGENTS.md
../.codex/AGENTS.md
../.codex/.tmp/plugins/plugins/build-web-apps/skills/supabase-best-practices/AGENTS.md
../.codex/.tmp/plugins/plugins/build-web-apps/skills/react-best-practices/AGENTS.md
../.codex/.tmp/plugins/plugins/zoom/AGENTS.md
../src/declarative-config/AGENTS.md
../src/declarative-config/nixos/AGENTS.md
../src/declarative-config/k8s/AGENTS.md
../src/declarative-config/k8s/ardenone-manager/AGENTS.md
../src/declarative-config/k8s/iad-ci/AGENTS.md
../src/declarative-config/k8s/apexalgo-iad/AGENTS.md
../src/declarative-config/k8s/ord-devimprint/AGENTS.md
../src/declarative-config/k8s/iad-kalshi/AGENTS.md
../src/declarative-config/k8s/rs-manager/AGENTS.md
../src/declarative-config/k8s/ardenone-cluster/devpod/AGENTS.md
../src/declarative-config/k8s/ardenone-cluster/AGENTS.md
../src/declarative-config/k8s/iad-options/AGENTS.md
../tradegraph/AGENTS.md
../bead-rs/AGENTS.md
../apexalgo-growth-lab/AGENTS.md
../claude-print/.claude/worktrees/agent-a161678e24fbdc24d/AGENTS.md
../claude-print/.claude/worktrees/agent-a1de5ee8b82b9638c/AGENTS.md
../claude-print/AGENTS.md
../commitgraph/AGENTS.md
../ardenone-cluster/declarative-config/k8s/apexalgo-iad/AGENTS.md
../ardenone-cluster/project/mission-control/notes/claude-code-hooks-multi-agent-observability/AGENTS.md
../ardenone-cluster/cluster-configuration/apexalgo-iad/AGENTS.md
../dashboard-site/AGENTS.md

exec
/bin/bash -lc "sed -n '1,220p' .needle.yaml
printf '\\n-- relevant bead files --\\n'
find .beads -maxdepth 2 -type f -print | sort | sed -n '1,100p'
printf '\\n-- CLIs --\\n'
command -v bead; bead --version
command -v bf; bf --version" in /home/coding/NEEDLE
 exited 127 in 0ms:
agent:
  default: claude
  args: []
  timeout: 3600
  adapters_dir: /home/coding/.config/needle/adapters
  routing:
    rules:
    - match_model: (claude-)?(sonnet|opus|fable|haiku).*
      adapter: claude-print
    default_adapter: claude-code-glm-4.7
    strict: false
bead_cli:
  backend: bead-rs
worker:
  max_workers: 10
  launch_stagger_seconds: 2
  idle_timeout: 60
  idle_action: wait
  max_claim_retries: 3
  claim_race_lost_skip: 5
  identifier_scheme: hostname_random
  cpu_load_warn: 0.8
  memory_free_warn_mb: 512
  building_timeout: 600
workspace:
  default: /home/coding/NEEDLE
  home: /home/coding/.needle
  labels: []
strands:
  pluck:
    exclude_labels: []
    split_after_failures: 0
  mend:
    stuck_threshold_secs: 300
    lock_ttl_secs: 600
    db_check_interval: 50
    idle_timeout: 120
  explore:
    enabled: true
    workspaces: []
    workspace_root: /home/coding/
  knot:
    alert_destination: null
    alert_cooldown_minutes: 60
    exhaustion_threshold: 3
  mitosis:
    enabled: true
    first_failure_only: true
    force_failure_threshold: 0
    repeat_interval: 0
  weave:
    enabled: true
    max_beads_per_run: 5
    cooldown_hours: 24
    exclude_workspaces: []
    doc_patterns:
    - README*
    - AGENTS.md
    - docs/**/*
    prompt_template: null
  unravel:
    enabled: false
    max_beads_per_run: 5
    max_alternatives_per_bead: 3
    cooldown_hours: 168
    prompt_template: null
  pulse:
    enabled: true
    scanners:
      # File-based health checks (work even when code doesn't compile)
      - name: todo-comments
        command: grep -rn "TODO\|FIXME\|XXX\|HACK" src/ --include="*.rs" || true
        severity_threshold: 4
      - name: unwrap-usage
        command: grep -rn "\.unwrap()" src/ --include="*.rs" | grep -v "test\|test_modules" || true
        severity_threshold: 3
      - name: large-files
        command: find src/ -name "*.rs" -size +50k -exec ls -lh {} \; || true
        severity_threshold: 4
    max_beads_per_run: 3
    cooldown_hours: 24
    severity_threshold: 3
    prompt_template: null
  reflect:
    enabled: true
    min_beads_since_last: 10
    cooldown_hours: 24
    max_learnings_per_run: 10
    max_skills_per_run: 3
    learning_retention_days: 90
    max_learnings: 80
    extraction_agent: null
    extraction_prompt_template: null
    max_extraction_per_run: 5
    transcript_recency_days: 7
    transcript_max_sessions: 50
    drift_similarity_threshold: 0.6
    drift_enabled: true
    adr_enabled: true
    claude_md_placement: true
  splice:
    enabled: true
    stale_threshold_secs: 300
    report_workspace: /home/coding/NEEDLE
    detect_live_loops: true
    live_loop_scan_events: 200
    claim_churn_threshold: 20
    log_runaway_bytes: 10485760
    live_loop_window_secs: 300
  learning:
    trace_retention_failed_days: 30
    trace_retention_success_days: 7
    max_learnings: 80
    trace_sanitization:
      enabled: true
      custom_patterns: []
    global_learnings_file: /home/coding/.config/needle/global-learnings.md
    max_global_learnings: 40
telemetry:
  file_sink:
    enabled: true
    log_dir: null
    retention_days: 30
  stdout_sink:
    enabled: false
    format: normal
    color: auto
  hooks: []
  otlp_sink:
    enabled: false
    endpoint: http://needle-otel-ex44-apexalgo-iad-ts.ardenone.com:4318
    protocol: http
    timeout_secs: 10
    compression: gzip
    tls: none
    headers:
      - "Authorization: env:NEEDLE_OTLP_AUTHORIZATION"
    resource_attributes: []
    metrics_interval_secs: 10
    service_namespace: needle-fleet
    max_queue_size: 2048
prompt:
  context_files: []
  instructions: null
  templates: {}
  variants: {}
health:
  heartbeat_interval_secs: 30
  heartbeat_ttl_secs: 300
  heartbeat_dir: null
limits:
  providers: {}
  models: {}
pricing: {}
budget:
  warn_usd: 0.0
  stop_usd: 0.0
# NOTE: do not reintroduce a `verification:` mapping here. The workspace-level
# `verification` key is the LEGACY alias for `gates:` and must be a SEQUENCE of
# shell commands (WorkspaceOverrides.verification: Option<Vec<String>>, see
# src/config/mod.rs). A mapping makes this entire file fail to parse with
# "verification: invalid type: map, expected a sequence", which silently blocks
# EVERY new worker launch on this workspace — already-running workers keep going,
# so the breakage is invisible until you try to scale or restart the fleet.
# It shipped that way in 0bf3111 (2026-08-16 10:47) and capped NEEDLE at two
# workers for ~18h.
#
# `outcome_timeout_seconds` is NOT workspace-overridable; it lives on the global
# ValidationConfig in ~/.needle/config.yaml under `validation:`, where it has
# been set to 300. The `gates:` block below expresses this workspace's
# verification via the supported pluggable system.
#
# NEEDLE validation gate: Definition of Done (fast lane)
# This is the SAME command invoked by the pre-commit hook and CI verify step,
# ensuring a single source of truth for "is this work acceptable?"
# See scripts/definition-of-done.sh for the unified verification command.
# The fast lane runs locally in seconds: fmt, clippy, check.
gates:
  - type: command
    commands:
      - scripts/definition-of-done.sh --fast
self_modification:
  enabled: false
  canary_workspace: /home/coding/.needle/canary
  auto_promote: false
  canary_timeout: 300
  hot_reload: true
fabric:
  enabled: true
  endpoint: http://needle-feed-ex44-apexalgo-iad-ts.ardenone.com/ingest
  timeout: 2
  batching: false
supervisor:
  heartbeat_path: null
  socket_path: null
  # Whether to automatically check for GitHub releases and download to :testing channel
  auto_upgrade_check: false
  # Interval (seconds) between automatic update checks (default: 21600 = 6 hours)
  update_check_interval_secs: 21600

-- relevant bead files --
.beads/.br_recovery/beads.db-shm.20260427_171856_210299455.bak
.beads/.br_recovery/beads.db-shm.20260427_182738_013672967.bak
.beads/.br_recovery/beads.db-wal.20260427_171856_210299455.bak
.beads/.br_recovery/beads.db-wal.20260427_182738_013672967.bak
.beads/.br_recovery/beads.db.20260427_171856_210299455.bak
.beads/.br_recovery/beads.db.20260427_182738_013672967.bak
.beads/.br_recovery/beads.db.20260803_153537_597311124.bak
.beads/.gitignore
.beads/.local_version
.beads/archive/README.md
.beads/archive/checkpoint-generations-20260816.tar.zst
.beads/beads.db
.beads/beads.db.backup.20260624113203
.beads/beads.db.backup.20260624121327
.beads/beads.db.backup.20260624123835
.beads/beads.db.backup.20260624125337
.beads/beads.db.backup.20260624134637
.beads/beads.db.backup.20260625044056
.beads/beads.db.backup.20260625050947
.beads/beads.db.backup.20260625052325
.beads/beads.db.backup.20260625104702
.beads/beads.db.backup.20260625130642
.beads/beads.db.backup.20260625144637
.beads/beads.db.backup.20260625154103
.beads/beads.db.backup.20260625160401
.beads/beads.db.backup.20260702150941
.beads/beads.db.backup.20260702155521
.beads/beads.db.backup.20260702161054
.beads/beads.db.backup.20260702192200
.beads/beads.db.backup.20260702210200
.beads/beads.db.backup.20260702212203
.beads/beads.db.backup.20260702213120
.beads/beads.db.backup.20260703025330
.beads/beads.db.backup.20260703041514
.beads/beads.db.backup.20260703045942
.beads/beads.db.backup.20260703082831
.beads/beads.db.backup.20260703091955
.beads/beads.db.backup.20260703094011
.beads/beads.db.backup.20260703094347
.beads/beads.db.backup.20260703095255
.beads/beads.db.backup.20260703100300
.beads/beads.db.backup.20260703102213
.beads/beads.db.backup.20260703103720
.beads/beads.db.backup.20260703110552
.beads/beads.db.backup.20260703111856
.beads/beads.db.backup.20260703121542
.beads/beads.db.backup.20260703123033
.beads/beads.db.backup.20260703133458
.beads/beads.db.backup.20260703140825
.beads/beads.db.backup.20260703144138
.beads/beads.db.backup.20260703160237
.beads/beads.db.backup.20260703165111
.beads/beads.db.backup.20260709041101
.beads/beads.db.br-backup-20260509053256
.beads/beads.db.empty-backup
.beads/bypasses.jsonl
.beads/checkpoint/.gitignore
.beads/checkpoint/current.json
.beads/checkpoint/forensic.jsonl
.beads/checkpoint/previous.json
.beads/config.json
.beads/daemon-error
.beads/db
.beads/decisions/nd-02a9.md
.beads/decisions/nd-035e.md
.beads/decisions/nd-036d.md
.beads/decisions/nd-0426.md
.beads/decisions/nd-042e.md
.beads/decisions/nd-0666.md
.beads/decisions/nd-0702.md
.beads/decisions/nd-079c.md
.beads/decisions/nd-07f9.md
.beads/decisions/nd-086e.md
.beads/decisions/nd-088f.md
.beads/decisions/nd-09ca.md
.beads/decisions/nd-0a1c.md
.beads/decisions/nd-0a97.md
.beads/decisions/nd-0ab4.md
.beads/decisions/nd-0b69.md
.beads/decisions/nd-0c17.md
.beads/decisions/nd-0c24.md
.beads/decisions/nd-0c94.md
.beads/decisions/nd-0dd0.md
.beads/decisions/nd-0e2b.md
.beads/decisions/nd-0e3b.md
.beads/decisions/nd-0f0e.md
.beads/decisions/nd-10c0.md
.beads/decisions/nd-10f6.md
.beads/decisions/nd-1185.md
.beads/decisions/nd-11d5.md
.beads/decisions/nd-139a.md
.beads/decisions/nd-13ce.md
.beads/decisions/nd-1417.md
.beads/decisions/nd-1561.md
.beads/decisions/nd-15a3.md
.beads/decisions/nd-16b4.md
.beads/decisions/nd-1740.md
.beads/decisions/nd-1771.md
.beads/decisions/nd-1883.md
.beads/decisions/nd-18f5.md

-- CLIs --
/home/coding/.cargo/bin/bead
bead 0.1.3
/bin/bash: line 6: bf: command not found

exec
/bin/bash -lc "set -e
printf '%s\\n' '-- expected fixtures --'
find /home/coding/.needle/canary/expected -maxdepth 1 -type f -name '*.yaml' -print -exec sh -c 'echo \"--- "'$1"; sed -n "1,240p" "$1"'"' _ {} \\;
printf '%s\\n' '-- canary store layout --'
find /home/coding/.needle/canary -maxdepth 3 -type f -path '*/.beads/*' -print | sort | sed -n '1,100p'
printf '%s\\n' '-- legacy CLI candidates --'
for candidate in /home/coding/.cargo/bin/bf /usr/local/bin/bf /usr/bin/bf /home/coding/bead-forge/target/release/bf /home/coding/bead-forge/target/debug/bf; do
  test -x \""'$candidate" && printf '"'%s\\n' \""'$candidate"
done
printf '"'%s\\n' '-- fixture references --'
rg -n --glob '*.rs' --glob '*.yaml' --glob '*.md' 'canary|final_status|expected_labels|expected.*status|bf show|in_progress|completed' . /home/coding/.needle/canary 2>/dev/null | sed -n '1,360p'" in /home/coding/NEEDLE
 succeeded in 0ms:
-- expected fixtures --
/home/coding/.needle/canary/expected/bead-54b7174d.yaml
--- /home/coding/.needle/canary/expected/bead-54b7174d.yaml
type: timeout
final_status: closed
/home/coding/.needle/canary/expected/bead-e6f0b50d.yaml
--- /home/coding/.needle/canary/expected/bead-e6f0b50d.yaml
type: state_machine
transitions:
  - BOOTING
  - SELECTING
  - CLAIMING
/home/coding/.needle/canary/expected/bead-60c759fa.yaml
--- /home/coding/.needle/canary/expected/bead-60c759fa.yaml
type: failure
final_status: blocked
labels:
  - "failure-count:1"
/home/coding/.needle/canary/expected/bead-1b6a6357.yaml
--- /home/coding/.needle/canary/expected/bead-1b6a6357.yaml
type: success
final_status: closed
labels: []
-- canary store layout --
/home/coding/.needle/canary/.beads/.gitignore
/home/coding/.needle/canary/.beads/beads.db
/home/coding/.needle/canary/.beads/beads.db-shm
/home/coding/.needle/canary/.beads/beads.db-wal
/home/coding/.needle/canary/.beads/checkpoint/current.json
/home/coding/.needle/canary/.beads/checkpoint/forensic.jsonl
/home/coding/.needle/canary/.beads/checkpoint/previous.json
/home/coding/.needle/canary/.beads/config.json
/home/coding/.needle/canary/.beads/events.jsonl
/home/coding/.needle/canary/.beads/heartbeats.jsonl
-- legacy CLI candidates --
-- fixture references --
/home/coding/.needle/canary/expected/bead-1b6a6357.yaml:2:final_status: closed
/home/coding/.needle/canary/expected/bead-60c759fa.yaml:2:final_status: blocked
/home/coding/.needle/canary/expected/bead-54b7174d.yaml:2:final_status: closed
/home/coding/.needle/canary/notes/canary-atw.md:38:- Bead: canary-atw
/home/coding/.needle/canary/canary-bzl.yaml:2:final_status: done
./AGENTS.md:161:If CI fails, record the failure on the bead, fix it, and do not close the bead as successfully completed.
./CLAUDE.md:87:`in_progress` under assignee `echo-test-test-worker`, and truncate
./src/decision/mod.rs:300:            outcome: format!("{} completed the task successfully", recovery_tool),
./src/decision/mod.rs:794:        assert!(!detector.contains_error_indicator("Operation completed"));
./src/dispatch/mod.rs:1682:        //       includes the final line, e.g. codex's `turn.completed`) and
./src/dispatch/mod.rs:4401:        // Activity was detected (process completed without timeout)
./src/cli/mod.rs:298:    /// Run canary tests against a :testing binary.
./src/cli/mod.rs:475:        CliCommand::Canary { status } => cmd_canary(status),
./src/cli/mod.rs:544:        // Emit upgrade.completed telemetry.
./src/cli/mod.rs:1039:/// init step is wrapped with `init.step.started` / `init.step.completed` so a
./src/cli/mod.rs:1227:        "NEEDLE worker boot: all init steps completed in {elapsed_ms}ms, starting worker loop..."
./src/cli/mod.rs:1267:                    "status": "completed",
./src/cli/mod.rs:1310:    eprintln!("NEEDLE worker boot: init step '{name}' completed in {elapsed}ms");
./src/cli/mod.rs:2199:                        if !matches!(name, "state" | "logs" | "canary" | "config") {
./src/cli/mod.rs:2492:  canary_workspace: ~/.needle/canary
./src/cli/mod.rs:2494:  canary_timeout: 300
./src/cli/mod.rs:5295:// canary, upgrade, rollback
./src/cli/mod.rs:5298:/// `needle canary` — run canary tests or show channel status.
./src/cli/mod.rs:5299:fn cmd_canary(show_status: bool) -> Result<()> {
./src/cli/mod.rs:5302:    let runner = crate::canary::CanaryRunner::new(
./src/cli/mod.rs:5304:        config.self_modification.canary_workspace.clone(),
./src/cli/mod.rs:5305:        config.self_modification.canary_timeout,
./src/cli/mod.rs:5339:    let tel = crate::telemetry::Telemetry::from_config("canary".to_string(), &config.telemetry)
./src/cli/mod.rs:5342:            crate::telemetry::Telemetry::new("canary".to_string())
./src/cli/mod.rs:5350:    println!("Running canary tests...");
./src/cli/mod.rs:5372:            crate::canary::CanaryTestResult::Passed { bead_id, .. } => {
./src/cli/mod.rs:5375:            crate::canary::CanaryTestResult::Failed {
./src/cli/mod.rs:5378:            crate::canary::CanaryTestResult::TimedOut {
./src/cli/mod.rs:5386:            crate::canary::CanaryTestResult::Error { bead_id, message } => {
./src/cli/mod.rs:5403:            println!("\nAll tests passed. Run `needle canary --status` to verify, then promote manually.");
./src/cli/mod.rs:5458:    let runner = crate::canary::CanaryRunner::new(
./src/cli/mod.rs:5460:        config.self_modification.canary_workspace.clone(),
./src/cli/mod.rs:5461:        config.self_modification.canary_timeout,
./src/cli/mod.rs:6410:    fn cli_parses_canary() {
./src/cli/mod.rs:6411:        let cli = Cli::try_parse_from(["needle", "canary"]);
./src/cli/mod.rs:6412:        assert!(cli.is_ok(), "needle canary should parse");
./src/cli/mod.rs:6416:    fn cli_parses_canary_status() {
./src/cli/mod.rs:6417:        let cli = Cli::try_parse_from(["needle", "canary", "--status"]);
./src/cli/mod.rs:6418:        assert!(cli.is_ok(), "needle canary --status should parse");
./src/sanitize/mod.rs:790:            r#"{"type":"system","subtype":"bead_update","bead_id":"needle-wysd.2.2","status":"completed"}"#,
./src/cost/mod.rs:515:{{"timestamp":"{today}","event_type":"bead.completed","worker_id":"w1","session_id":"aa","sequence":2,"data":{{"bead_id":"nd-x"}}}}
./tests/template_rendering_tests.rs:83:            "in_progress",
./src/learning/mod.rs:10://! learnings from completed beads. These are automatically injected into prompts
./src/learning/mod.rs:95:/// A single learning entry from a completed bead.
./src/learning/mod.rs:566:        content.push_str("This file is automatically managed by NEEDLE. Learnings from completed beads are captured here.\n\n");
./src/learning/mod.rs:876:/// Retrospectives capture learning from each completed task:
./README.md:48:A **bead** is a work item — the unit of work NEEDLE processes. Think of it as a structured task ticket: a title, a body describing the deliverable and acceptance criteria, a status (`open`, `in_progress`, `done`), and optional metadata like priority and dependencies.
./README.md:272:│   ├── canary/            # Release channel promotion, canary tests
./README.md:319:| **Metrics** | `needle.beads.completed`, `needle.beads.duration`, `needle.agent.tokens.input`, `needle.cost.usd`, and more |
./src/mitosis/mod.rs:414:    /// - Distinguishes completed work from remaining work
./src/mitosis/mod.rs:800:            "mitosis split completed"
./src/mitosis/mod.rs:3189:            stdout: "substantial work completed".to_string(),
./src/mitosis/mod.rs:3455:    async fn timeout_mitosis_distinguishes_completed_from_remaining_work() {
./src/mitosis/mod.rs:3456:        // Test that the timeout prompt instructs the agent to distinguish completed vs remaining work
./src/mitosis/mod.rs:3517:            built_prompt.content.contains("completed")
./src/mitosis/mod.rs:3519:            "prompt should distinguish completed from remaining work"
./src/mitosis/mod.rs:3578:        // 2. Distinguishes completed from remaining work
./src/mitosis/mod.rs:3682:        // Requirement 2: Distinguishes completed from remaining work
./src/mitosis/mod.rs:3684:            built_prompt.content.contains("completed")
./src/mitosis/mod.rs:3686:            "prompt must distinguish completed from remaining work"
./docs/test-isolation-audit-report.md:86:**Impact:** 2,302 beads mutated to `in_progress` under `echo-test-test-worker`  
./src/validation/mod.rs:804:            "expected the 300ms timeout to fire before the 3s sleep completed"
./src/resolve/mod.rs:55:    /// Task completed successfully — close the bead.
./src/scratch_sweep.rs:38:/// Observable totals for a completed startup sweep.
./src/scratch_sweep.rs:222:        "scratch startup sweep completed"
./src/scratch_sweep.rs:673:    fn completed(outcome: SweepOutcome) -> SweepReport {
./src/scratch_sweep.rs:676:            other => panic!("expected completed sweep, got {other:?}"),
./src/scratch_sweep.rs:705:            completed(sweep_scratch_root(root, 48, &ClearProcesses, &SafeCheckout, now).unwrap());
./src/scratch_sweep.rs:726:        let report = completed(
./src/scratch_sweep.rs:747:            completed(sweep_scratch_root(temp.path(), 48, &inspector, &SafeCheckout, now).unwrap());
./src/scratch_sweep.rs:915:        let report = completed(
./docs/needle-ci-failure-investigation-2026-08-16.md:68:- `src/canary/mod.rs` (line 469, 1715, 1748, 1788, 1833)
./notes/bf-swt32.md:5:The zombie-aware `is_pid_alive` fix described in this bead was **already implemented** under bead `bf-z3yp0` (GitHub issue jedarden/NEEDLE#12, commit `81b0995`). This bead (`bf-swt32`) is a tracking bead created to document and verify the completed work for the compounding half of GH #12.
./src/telemetry/otlp.rs:481:    /// Counter: `needle.beads.completed`
./src/telemetry/otlp.rs:482:    beads_completed: Counter<u64>,
./src/telemetry/otlp.rs:630:            beads_completed: meter
./src/telemetry/otlp.rs:631:                .u64_counter("needle.beads.completed")
./src/telemetry/otlp.rs:1097:    ///   bead.claim.failed, agent.dispatched, agent.completed,
./src/telemetry/otlp.rs:1098:    ///   strand.evaluated, outcome.handled, bead.completed
./src/telemetry/otlp.rs:1100:    /// - Metrics: beads.completed, bead.duration, strand.duration, agent.duration, etc.
./src/telemetry/otlp.rs:1170:            "agent.completed" => {
./src/telemetry/otlp.rs:1225:            "bead.completed" => {
./src/telemetry/otlp.rs:1233:                        .beads_completed
./src/telemetry/otlp.rs:2311:            "agent.completed",
./src/telemetry/otlp.rs:2348:            "agent.completed",
./src/telemetry/otlp.rs:2480:            beads_completed: meter
./src/telemetry/otlp.rs:2481:                .u64_counter("needle.beads.completed")
./src/telemetry/otlp.rs:2588:    async fn test_beads_completed_increments_with_outcome() {
./src/telemetry/otlp.rs:2593:        let event = make_test_event("bead.completed", Some("bead-123"), data);
./src/telemetry/otlp.rs:2605:            "bead.completed",
./src/telemetry/otlp.rs:2667:            "agent.completed",
./src/telemetry/otlp.rs:2833:        // needle.beads.completed: {bead}
./src/telemetry/otlp.rs:2834:        let _ = &sink.metrics.beads_completed;
./src/telemetry/otlp.rs:3002:            beads_completed: meter
./src/telemetry/otlp.rs:3003:                .u64_counter("needle.beads.completed")
./src/telemetry/otlp.rs:3335:                "agent.completed",
./src/telemetry/otlp.rs:3340:                "bead.completed",
./src/telemetry/otlp.rs:3437:                "bead.completed",
./CHANGELOG.md:10:the release/canary path used to deploy it.
./CHANGELOG.md:27:  so the automatic updater can use the normal canary and promotion path.
./CHANGELOG.md:28:- The completed Jed Arden repository migration is documented, including the
./CHANGELOG.md:109:  scanned the whole home directory for bead workspaces. A canary run roamed out
./CHANGELOG.md:110:  of `~/.needle/canary` into real repos and dispatched agents there. Now pinned
./CHANGELOG.md:112:  the canary workspace.
./CHANGELOG.md:113:- **The canary could never pass** — `perform_upgrade` hardcoded a 300s test
./CHANGELOG.md:114:  timeout and ignored `self_modification.canary_timeout`. A canary test is a
./CHANGELOG.md:134:  every agent "Every completed bead MUST produce at least one commit. If your
./CHANGELOG.md:167:completed beads went unrecorded while the fleet looked healthy.
./CHANGELOG.md:241:  command-not-found and the bead stayed `in_progress` forever, while the worker
./test_failure_analysis_report.md:101:| Historical exit 1 from `cargo fmt --check` | **Deterministic repository quality failure** | Same report lists formatting changes in `src/canary/mod.rs`, `src/cli/mod.rs`, `src/config/mod.rs`, and `src/health/mod.rs`; not an environmental outage |
./.needle.yaml:184:  canary_workspace: /home/coding/.needle/canary
./.needle.yaml:186:  canary_timeout: 300
./src/telemetry/mod.rs:903:            EventKind::IdleSleepCompleted { .. } => "worker.idle_sleep_completed",
./src/telemetry/mod.rs:908:            EventKind::InitStepCompleted { .. } => "init.step.completed",
./src/telemetry/mod.rs:926:            EventKind::BeadCompleted { .. } => "bead.completed",
./src/telemetry/mod.rs:930:            EventKind::DispatchCompleted { .. } => "agent.completed",
./src/telemetry/mod.rs:991:            EventKind::DriftDetectionCompleted { .. } => "drift.completed",
./src/telemetry/mod.rs:995:            EventKind::DecisionDetectionCompleted { .. } => "decision.completed",
./src/telemetry/mod.rs:998:            EventKind::PulseScannerCompleted { .. } => "pulse.scanner_completed",
./src/telemetry/mod.rs:1003:            EventKind::UpgradeCompleted { .. } => "worker.upgrade.completed",
./src/telemetry/mod.rs:1004:            EventKind::RollbackCompleted { .. } => "rollback.completed",
./src/telemetry/mod.rs:1011:            EventKind::UpgradeCheckCompleted { .. } => "upgrade_check.completed",
./src/telemetry/mod.rs:1013:            EventKind::CanaryStarted { .. } => "canary.started",
./src/telemetry/mod.rs:1014:            EventKind::CanarySuiteCompleted { .. } => "canary.suite_completed",
./src/telemetry/mod.rs:1015:            EventKind::CanaryPromoted { .. } => "canary.promoted",
./src/telemetry/mod.rs:1016:            EventKind::CanaryRejected { .. } => "canary.rejected",
./src/telemetry/mod.rs:1019:            EventKind::CargoTestCompleted { .. } => "cargo_test.completed",
./src/telemetry/mod.rs:1024:            EventKind::TransformCompleted { .. } => "transform.completed",
./src/telemetry/mod.rs:1040:            EventKind::SupervisorWorkerDrainCompleted { .. } => "supervisor.worker_drain_completed",
./src/telemetry/mod.rs:2713:            "bead.completed" => "COMPLETED",
./src/telemetry/mod.rs:2716:            "agent.completed" => "AGENT_DONE",
./src/telemetry/mod.rs:2750:            "agent.completed" => {
./src/telemetry/mod.rs:2797:            t if t.starts_with("bead.claim.succeeded") || t == "bead.completed" => "\x1b[32m", // green
./src/telemetry/mod.rs:5825:            event_type: "agent.completed".to_string(),
./src/telemetry/mod.rs:5838:            event_type: "agent.completed".to_string(),
./src/telemetry/mod.rs:6179:        let event = make_test_event("bead.completed");
./src/strand/resolve.rs:399:            "Resolver completed"
./src/strand/pluck.rs:445:        // 3. Filter: remove beads that are actively in_progress (claimed by another worker)
./src/strand/pluck.rs:459:                    "status:in_progress".to_string()
./src/strand/pluck.rs:1740:    async fn starvation_when_all_beads_in_progress_emits_telemetry() {
./src/strand/pluck.rs:1803:        // Verify exclusion reasons - should include status:in_progress for each bead
./src/strand/pluck.rs:1815:                        .filter(|r| **r == "status:in_progress")
./src/strand/pluck.rs:1818:                    "all 3 beads should be excluded with status:in_progress reason"
./src/strand/splice.rs:77:    agent_completed_count: u32,
./src/strand/splice.rs:117:    /// Completion without resolution: many agent.completed events but bead never closes.
./src/strand/splice.rs:122:        agent_completed_count: u32,
./src/strand/splice.rs:390:        // Pattern 4: Completion without resolution — many agent.completed but bead never closes.
./src/strand/splice.rs:398:                    agent_completed_count: completion.agent_completed_count,
./src/strand/splice.rs:478:                .any(|ev| matches!(ev.event_type.as_str(), "agent.completed" | "bead.completed"));
./src/strand/splice.rs:508:        // Check for agent.completed events in the scanned window.
./src/strand/splice.rs:509:        let has_completed = events
./src/strand/splice.rs:511:            .any(|ev| ev.event_type == "agent.completed" || ev.event_type == "bead.completed");
./src/strand/splice.rs:513:        if !has_completed && file_size > self.config.log_runaway_bytes {
./src/strand/splice.rs:554:    /// Detect completion without resolution: many agent.completed events but bead never closes.
./src/strand/splice.rs:558:    /// The key signal is N agent.completed events for the same bead with 0 bead.completed events.
./src/strand/splice.rs:566:        let mut agent_completed_counts: HashMap<String, u32> = HashMap::new();
./src/strand/splice.rs:567:        let mut bead_completed_seen: HashSet<String> = HashSet::new();
./src/strand/splice.rs:590:                "agent.completed" => {
./src/strand/splice.rs:591:                    *agent_completed_counts.entry(bead_id.clone()).or_insert(0) += 1;
./src/strand/splice.rs:593:                        sample.push(format!("{} agent.completed {}", ev.timestamp, bead_id));
./src/strand/splice.rs:596:                "bead.completed" => {
./src/strand/splice.rs:597:                    bead_completed_seen.insert(bead_id);
./src/strand/splice.rs:606:        // - High agent_completed count (agents are succeeding)
./src/strand/splice.rs:607:        // - NO bead.completed event (the bead never actually closes)
./src/strand/splice.rs:609:        // The bf-34xw9 incident showed: 41 claim.succeeded, 25 orphaned, 42 agent.completed, 1 bead.completed
./src/strand/splice.rs:612:        for (bead_id, &agent_completed_count) in &agent_completed_counts {
./src/strand/splice.rs:613:            // Skip if bead was completed (not stuck)
./src/strand/splice.rs:614:            if bead_completed_seen.contains(bead_id) {
./src/strand/splice.rs:622:            // Threshold: at least claim_churn_threshold agent.completed events
./src/strand/splice.rs:624:            if agent_completed_count >= self.config.claim_churn_threshold
./src/strand/splice.rs:632:                    agent_completed_count,
./src/strand/splice.rs:904:                agent_completed_count,
./src/strand/splice.rs:915:                     **Agent completed events:** {}\n\
./src/strand/splice.rs:916:                     **No bead.completed events detected**\n",
./src/strand/splice.rs:917:                    bead_id, claim_succeeded_count, orphaned_count, agent_completed_count
./src/strand/splice.rs:1307:        // 41 claim.succeeded, 25 orphaned, 42 agent.completed, 0 bead.completed
./src/strand/splice.rs:1340:        // Create 42 agent.completed events
./src/strand/splice.rs:1344:                event_type: "agent.completed".to_string(),
./src/strand/splice.rs:1350:        // No bead.completed event - this is the key signal
./src/strand/splice.rs:1351:        // The detector should find this pattern since agent_completed_count (42) >= claim_churn_threshold (20)
./src/strand/splice.rs:1365:        assert_eq!(info.agent_completed_count, 42);
./src/strand/splice.rs:1405:                event_type: "agent.completed".to_string(),
./src/strand/splice.rs:1411:        // Add a bead.completed event - this should prevent detection
./src/strand/splice.rs:1414:            event_type: "bead.completed".to_string(),
./src/strand/splice.rs:1423:            "Detector should ignore beads that completed successfully"
./src/strand/splice.rs:1458:                event_type: "agent.completed".to_string(),
./src/strand/mod.rs:63:pub use mend::{cleanup_orphaned_in_progress, MendStrand};
./notes/needle-la6l.md:32:The bead was left open after session 1 completed because the session ended before closing.
./notes/bf-3e4do.md:158:3. A different code path entirely (health checks, canary tests, etc.)
./notes/bf-3e4do.md:162:**Canary Module** (`src/canary/mod.rs`):
./notes/bf-3e4do.md:168:        &self.canary_workspace.display().to_string(),
./notes/bf-3e4do.md:333:6. **Investigate canary module** for similar issues
./notes/bf-3e4do.md:345:3. `src/canary/mod.rs` - Add current_dir() to test binary spawn
./notes/needle-b8fv.md:43:No implementation work was required. The bead described work that was already completed in a prior session (likely needle-ry21 based on the OTLP commit history).
./docs/concurrent-claim-cycle-safety-validation.md:195:T13    │ ├─ close claim_span             │ │ ├─ Verify bead                │ B sees: status=in_progress
./notes/bf-12bim.md:5:The zombie reaping fix described in this bead was **already implemented** under bead `bf-z3yp0` (GitHub issue jedarden/NEEDLE#12). This bead (`bf-12bim`) is a tracking bead created to document and verify the completed work.
./notes/bf-12bim.md:63:- `ffa3ecf` (HEAD) - "docs(bf-12bim): document that zombie reaping fix was already completed"
./notes/bf-12bim.md:76:- Safety: Cannot race with other `.wait()` calls (dispatch/telemetry/canary run in separate PID tree under worker process)
./notes/needle-gvfw.md:5:The transcript discovery and parsing implementation was completed across prior
./src/strand/mend.rs:50:pub async fn cleanup_orphaned_in_progress(
./src/strand/mend.rs:56:    cleanup_in_progress(store, registry, telemetry, qualified_id, None).await
./src/strand/mend.rs:65:pub async fn cleanup_orphaned_in_progress_test_optimized(
./src/strand/mend.rs:71:    cleanup_in_progress(store, registry, telemetry, qualified_id, None).await
./src/strand/mend.rs:79:async fn cleanup_in_progress(
./src/strand/mend.rs:92:    let has_any_in_progress = all_beads.iter().any(|b| b.status == BeadStatus::InProgress);
./src/strand/mend.rs:93:    if !has_any_in_progress {
./src/strand/mend.rs:112:    // For each assignee with multiple in_progress beads, identify which bead
./src/strand/mend.rs:152:        // If this assignee has multiple in_progress beads, only the most
./src/strand/mend.rs:401:    async fn cleanup_orphaned_in_progress(
./src/strand/mend.rs:406:        let released = cleanup_in_progress(
./src/strand/mend.rs:902:        let in_progress_ids: std::collections::HashSet<String> = store
./src/strand/mend.rs:924:            let is_active = in_progress_ids
./src/strand/mend.rs:1391:                tracing::info!(warnings, fixed, "br doctor --repair completed");
./src/strand/mend.rs:1495:        if let Err(e) = self.cleanup_orphaned_in_progress(store, &mut summary).await {
./src/strand/mend.rs:1952:    fn make_in_progress_bead(id: &str, assignee: &str) -> Bead {
./src/strand/mend.rs:1971:    fn make_recent_in_progress_bead(id: &str, assignee: &str) -> Bead {
./src/strand/mend.rs:1972:        let mut bead = make_in_progress_bead(id, assignee);
./src/strand/mend.rs:2044:        let bead = make_in_progress_bead("nd-orphan", "dead-worker");
./src/strand/mend.rs:2075:    async fn orphaned_in_progress_bead_released() {
./src/strand/mend.rs:2081:        let bead = make_in_progress_bead("nd-orphan", "dead-worker");
./src/strand/mend.rs:2095:    async fn in_progress_bead_with_live_worker_not_released() {
./src/strand/mend.rs:2101:        let bead = make_recent_in_progress_bead("nd-active", "alive-worker");
./src/strand/mend.rs:2148:    async fn stale_in_progress_bead_released_even_when_worker_is_alive() {
./src/strand/mend.rs:2155:        let bead = make_in_progress_bead("nd-stale-live", "alive-worker");
./src/strand/mend.rs:2204:    async fn in_progress_bead_own_worker_not_released() {
./src/strand/mend.rs:2210:        let bead = make_recent_in_progress_bead("nd-mine", "claude-test-worker");
./src/strand/mend.rs:2224:    async fn in_progress_bead_with_dead_registered_worker_released() {
./src/strand/mend.rs:2230:        let bead = make_in_progress_bead("nd-stale", "dead-registered");
./src/strand/mend.rs:2278:    /// Multiple in_progress beads for same assignee: only newest is valid.
./src/strand/mend.rs:2288:        // Create 3 recent in_progress beads for the same assignee with distinct
./src/strand/mend.rs:2290:        let mut bead_old = make_in_progress_bead("nd-old", assignee);
./src/strand/mend.rs:2293:        let mut bead_medium = make_in_progress_bead("nd-medium", assignee);
./src/strand/mend.rs:2296:        let mut bead_new = make_in_progress_bead("nd-new", assignee);
./src/strand/mend.rs:2347:    /// Single in_progress bead for assignee: NOT reaped if worker alive.
./src/strand/mend.rs:2357:        // Single in_progress bead, not stale.
./src/strand/mend.rs:2358:        let bead = make_recent_in_progress_bead("nd-single", assignee);
./src/strand/mend.rs:2613:        let bead = make_recent_in_progress_bead("nd-collision-a", glm5_foxtrot);
./src/strand/mend.rs:2690:        let bead = make_in_progress_bead("nd-collision-b", glm5_foxtrot);
./src/strand/mend.rs:2987:        let orphan_bead = make_in_progress_bead("nd-orphan", "dead-worker");
./src/strand/mend.rs:3260:    async fn agent_log_cleanup_skips_in_progress_bead() {
./src/strand/mend.rs:3274:        let bead = make_in_progress_bead(active_bead_id, "some-worker");
./src/strand/mend.rs:6271:            This file is automatically managed by NEEDLE. Learnings from completed beads are captured here.\n\n\
./src/strand/mend.rs:6337:            This file is automatically managed by NEEDLE. Learnings from completed beads are captured here.\n\n"
./src/strand/mend.rs:6403:            This file is automatically managed by NEEDLE. Learnings from completed beads are captured here.\n\n\
./src/strand/mend.rs:6465:            This file is automatically managed by NEEDLE. Learnings from completed beads are captured here.\n\n"
./src/upgrade/mod.rs:72:    /// Download completed successfully.
./src/upgrade/mod.rs:536:    // Write the new binary to :testing channel for canary validation.
./src/upgrade/mod.rs:564:    // Run canary validation if canary workspace exists.
./src/upgrade/mod.rs:565:    let canary_workspace = home.join("canary");
./src/upgrade/mod.rs:566:    if canary_workspace.exists() {
./src/upgrade/mod.rs:567:        println!("Running canary validation...");
./src/upgrade/mod.rs:569:        use crate::canary::CanaryRunner;
./src/upgrade/mod.rs:571:        // Honour `self_modification.canary_timeout` instead of hardcoding 300s.
./src/upgrade/mod.rs:572:        // A canary test dispatches a real agent against a real bead, which
./src/upgrade/mod.rs:578:        let canary_timeout = crate::config::ConfigLoader::load_global()
./src/upgrade/mod.rs:579:            .map(|c| c.self_modification.canary_timeout)
./src/upgrade/mod.rs:582:        let runner = CanaryRunner::new(home.clone(), canary_workspace, canary_timeout);
./src/upgrade/mod.rs:584:        let report = runner.run().context("canary validation failed")?;
./src/upgrade/mod.rs:590:                .context("failed to reject testing binary after canary failure")?;
./src/upgrade/mod.rs:593:                "canary validation failed: {}/{} tests passed. \
./src/upgrade/mod.rs:595:                 Run 'needle canary --status' for details.",
./src/upgrade/mod.rs:613:            "No canary workspace found at {}. Installing without validation.",
./src/upgrade/mod.rs:614:            canary_workspace.display()
./src/upgrade/mod.rs:616:        println!("WARNING: Skipping canary validation is not recommended for production upgrades.");
./src/upgrade/mod.rs:618:            "         Set up a canary workspace at {} to enable validation.",
./src/upgrade/mod.rs:619:            canary_workspace.display()
./src/upgrade/mod.rs:622:        // Create a minimal canary runner for promotion only (no tests run)
./src/upgrade/mod.rs:623:        use crate::canary::CanaryRunner;
./src/upgrade/mod.rs:624:        let runner = CanaryRunner::new(home.clone(), canary_workspace, 300);
./src/upgrade/mod.rs:626:        // Promote without canary validation (fallback behavior)
./src/upgrade/mod.rs:630:        println!("Promoted to :stable (without canary validation)");
./src/strand/reflect.rs:72:const DEFAULT_EXTRACTION_PROMPT: &str = r#"You are analyzing a completed software task.
./src/strand/knot.rs:30:    /// All beads are claimed by workers (in_progress). Normal contention.
./src/strand/knot.rs:32:        in_progress_count: usize,
./src/strand/knot.rs:39:        in_progress_count: usize,
./src/strand/knot.rs:90:        let mut in_progress_count = 0usize;
./src/strand/knot.rs:99:                    in_progress_count += 1;
./src/strand/knot.rs:107:                // Non-exhaustive: treat unknown statuses as neither open nor in_progress.
./src/strand/knot.rs:113:        // If no open beads remain but some are in_progress, it's normal contention.
./src/strand/knot.rs:114:        if open_count == 0 && in_progress_count > 0 {
./src/strand/knot.rs:116:                in_progress_count,
./src/strand/knot.rs:121:        // If no open beads AND no in_progress, everything is Done/Blocked — genuinely idle.
./src/strand/knot.rs:122:        if open_count == 0 && in_progress_count == 0 {
./src/strand/knot.rs:130:            in_progress_count,
./src/strand/knot.rs:222:                in_progress_count,
./src/strand/knot.rs:228:                    let excluded_count = total - open_count - in_progress_count;
./src/strand/knot.rs:732:                in_progress_count,
./src/strand/knot.rs:735:                assert_eq!(in_progress_count, 2);
./src/strand/knot.rs:755:                in_progress_count,
./src/strand/knot.rs:760:                assert_eq!(in_progress_count, 1);
./src/strand/knot.rs:768:    async fn diagnose_mixed_done_and_in_progress_is_all_claimed() {
./src/claim/mod.rs:471:    /// - `Ok(true)`: bead is still in_progress and assigned to expected actor
./src/strand/explore.rs:340:    /// Record a completed Explore scan and update its future cadence.
./src/strand/explore.rs:758:                        let has_in_progress = all_beads
./src/strand/explore.rs:762:                        if !has_in_progress {
./src/strand/explore.rs:767:                            exclusion_reasons.insert("no_in_progress".to_string());
./src/strand/explore.rs:771:                        match super::cleanup_orphaned_in_progress(
./src/strand/explore.rs:971:    /// This is necessary for tests that call cleanup_orphaned_in_progress, which uses
./src/strand/explore.rs:2672:            // triggering the empty candidate path that leads to cleanup_orphaned_in_progress
./src/prompt/mod.rs:119:/// The agent must analyze what was completed and decompose remaining work
./src/prompt/mod.rs:149:1. **Distinguish completed from remaining work:**
./src/prompt/mod.rs:157:   - Focus on deliverables that can be completed within normal time limits
./src/prompt/mod.rs:162:   - If splitting would require redoing work that may have completed partially
./src/prompt/mod.rs:177:- **Split ONLY if:** Remaining work can be completed independently without redoing completed portions
./src/prompt/mod.rs:180:- **Valid split:** Original task had 3 phases, phase 1 completed, propose children for phases 2-3
./src/prompt/mod.rs:188:- Timeout evidence: Agent completed design and partial implementation
./src/prompt/mod.rs:279:Analyze the completed Pluck operation and determine what should happen next.
./src/prompt/mod.rs:335:- Each subtask can be completed separately and closed independently
./src/prompt/mod.rs:1004:    /// to help the agent distinguish completed from remaining work.
./src/bead_store/backend.rs:524:                "in_progress",
./src/checkpoint_utils.rs:82:    /// `Ok(())` if all cleanup operations completed (successfully or with logged errors)
./src/checkpoint_utils.rs:165:/// `Ok(())` if cleanup completed (successfully or with logged errors)
./src/checkpoint_utils.rs:534:/// `Ok(())` if the operation completed (some files may have failed but were logged)
./src/checkpoint_utils.rs:1431:        // Verify restore completed despite invalid JSON
./src/checkpoint_utils.rs:1463:        // Verify restore completed despite invalid JSON
./src/bead_store/mod.rs:283:    for status in ["open", "in_progress", "deferred", "closed"] {
./src/bead_store/mod.rs:383:/// 2. The kernel's page cache and filesystem synchronization haven't fully completed.
./src/bead_store/mod.rs:1161:    /// Attempt to atomically claim a bead (set status=in_progress, assignee=actor).
./src/bead_store/mod.rs:1312:                "#!/bin/sh\nif [ \"$1\" = capabilities ]; then\n  printf '%s\\n' '{{\"implementation\":\"bead-rs\",\"atomic_claim\":true,\"statuses\":[\"open\",\"in_progress\",\"deferred\",\"closed\"],\"schemas\":[{{\"schema_ref\":\"urn:bead-rs:schema:issue:native-v1\"}},{{\"schema_ref\":\"urn:bead-rs:schema:event:native-v1\"}},{{\"schema_ref\":\"urn:bead-rs:schema:field-guide:native-v1\"}}]}}'\nelse\n  echo '{version}'\nfi\n"
./src/bead_store/mod.rs:3141:  printf '{"implementation":"bead-rs","atomic_claim":true,"statuses":["open","in_progress","deferred","closed"],"schemas":[{"schema_ref":"urn:bead-rs:schema:issue:native-v1"}]}'
./src/bead_store/mod.rs:3273:  printf '{"implementation":"bead-rs","atomic_claim":true,"statuses":["open","in_progress","deferred","closed"],"schemas":[{"schema_ref":"urn:bead-rs:schema:issue:native-v1"},{"schema_ref":"urn:bead-rs:schema:event:native-v1"},{"schema_ref":"urn:bead-rs:schema:field-guide:native-v1"}]}'
./src/bead_store/mod.rs:3307:  printf '{"implementation":"bead-rs","atomic_claim":false,"statuses":["open","in_progress"],"schemas":[]}'
./src/bead_store/cli_store.rs:450:            "in_progress".to_string(),
./src/bead_store/cli_store.rs:795:        self.run_bf_update_batch(id, Some("in_progress"), Some(actor))
./src/stats/mod.rs:27:    /// Number of beads that completed with `"Success"` outcome.
./src/stats/mod.rs:29:    /// Number of beads that completed with `"Failure"` outcome.
./src/stats/mod.rs:31:    /// Number of beads that completed with `"Timeout"` outcome.
./src/stats/mod.rs:33:    /// Sum of dispatch durations in milliseconds (from `agent.completed`).
./src/stats/mod.rs:130:    /// Pending dispatch durations waiting for an `agent.completed` event,
./src/stats/mod.rs:219:            Some("agent.completed") => {
./src/stats/mod.rs:353:/// completed within this time, while the slowest 5% took longer.
./src/stats/mod.rs:533:/// completed within this time, while the slowest 1% took longer.
./src/stats/mod.rs:839:    /// Number of beads that completed with `"Success"` outcome.
./src/stats/mod.rs:841:    /// Number of beads that completed with `"Failure"` outcome.
./src/stats/mod.rs:843:    /// Number of beads that completed with `"Timeout"` outcome.
./src/stats/mod.rs:1006:    fn make_completed_event(bead_id: &str, duration_ms: u64) -> String {
./src/stats/mod.rs:1008:            "event_type": "agent.completed",
./src/stats/mod.rs:1095:        agg.process_event(&serde_json::from_str(&make_completed_event("nd-1", 5000)).unwrap());
./src/stats/mod.rs:1139:            make_completed_event("nd-1", 3000),
./src/canary/mod.rs:3://! The canary runner tests a :testing binary against a controlled workspace
./src/canary/mod.rs:9://! - `needle-testing` — Newly compiled binary awaiting canary validation
./src/canary/mod.rs:16://! The canary workspace (`~/.needle/canary/`) contains:
./src/canary/mod.rs:38:/// Outcome of a single canary test.
./src/canary/mod.rs:98:/// Expected outcome for a canary test bead.
./src/canary/mod.rs:105:        #[serde(default = "ExpectedOutcome::default_final_status")]
./src/canary/mod.rs:106:        final_status: String,
./src/canary/mod.rs:115:        final_status: String,
./src/canary/mod.rs:124:        final_status: String,
./src/canary/mod.rs:134:    fn default_final_status() -> String {
./src/canary/mod.rs:149:/// Actual outcome observed from running a canary test.
./src/canary/mod.rs:155:    pub final_status: String,
./src/canary/mod.rs:167:/// Summary report from running the full canary test suite.
./src/canary/mod.rs:184:    /// Total duration of the canary run.
./src/canary/mod.rs:188:    /// Whether the canary suite passed (all tests passed).
./src/canary/mod.rs:203:/// Runs canary tests against a :testing binary.

exec
/bin/bash -lc "sed -n '1,460p' src/canary/mod.rs
printf '\\n-- canary workspace manifests --\\n'
find /home/coding/.needle/canary -maxdepth 2 -type f \\( -name '*.yaml' -o -name '*.yml' -o -name 'README*' -o -name '*.md' \\) -print -exec sh -c 'echo \"--- "'$1"; sed -n "1,260p" "$1"'"' _ {} \\;
printf '\\n-- status-validation sites --\\n'
rg -n -C 10 'final_status|labels|transitions|ExpectedOutcome|expected' src/canary/mod.rs
printf '\\n-- command names --\\n'
command -v bd || true
command -v beads || true
find /home/coding -maxdepth 4 -type f -name bf -perm -u+x -print 2>/dev/null | sed -n '1,80p'" in /home/coding/NEEDLE
 succeeded in 289ms:
/home/coding/backups/bf-tooling-removed-20260814/bf

exec
/bin/bash -lc "sed -n '360,930p' src/canary/mod.rs
printf '\\n-- fixture bead records (bead-rs) --\\n'
for bead_id in bead-1b6a6357 bead-54b7174d bead-60c759fa bead-e6f0b50d; do
  echo \"--- "'${bead_id}"
  bead --db /home/coding/.needle/canary/.beads/beads.db show "$bead_id" --json || true
done
printf '"'\\n-- actual in-repo bead --\\n'
bead show needle-16546862 --json
printf '\\n-- legacy bf help/version --\\n'
/home/coding/backups/bf-tooling-removed-20260814/bf --version || true
/home/coding/backups/bf-tooling-removed-20260814/bf show --help || true" in /home/coding/NEEDLE
 succeeded in 0ms:
                )
            })?;

        if !binary_path.exists() {
            bail!(
                "bead CLI backend '{}' binary not found: {} (backend: {})\n\
                 Ensure the backend is installed and accessible, or update bead_cli.path in {}/.needle.yaml",
                backend,
                binary_path.display(),
                backend_type,
                self.canary_workspace.display()
            );
        }

        tracing::info!(
            backend = %backend,
            binary = %binary_path.display(),
            resolved_backend = %backend_type,
            "canary workspace bead backend validated"
        );

        Ok(())
    }

    /// Discover test bead IDs in the canary workspace.
    fn discover_test_beads(&self) -> Result<Vec<String>> {
        let expected_dir = self.canary_workspace.join("expected");
        if !expected_dir.exists() {
            bail!(
                "expected outcomes directory not found: {}",
                expected_dir.display()
            );
        }

        let mut bead_ids = Vec::new();
        for entry in std::fs::read_dir(&expected_dir)? {
            let entry = entry?;
            let path = entry.path();
            if path.extension().is_some_and(|e| e == "yaml") {
                if let Some(stem) = path.file_stem().and_then(|s| s.to_str()) {
                    bead_ids.push(stem.to_string());
                }
            }
        }

        Ok(bead_ids)
    }

    /// Load the expected outcome for a test bead.
    fn load_expected_outcome(&self, bead_id: &str) -> Result<ExpectedOutcome> {
        let path = self
            .canary_workspace
            .join("expected")
            .join(format!("{bead_id}.yaml"));
        let content = std::fs::read_to_string(&path)
            .with_context(|| format!("failed to read expected outcome: {}", path.display()))?;
        let outcome: ExpectedOutcome = serde_yaml::from_str(&content)
            .with_context(|| format!("invalid expected outcome YAML: {}", path.display()))?;

        // Validate that the expected final_status is a valid bead-forge status.
        // This prevents stale expectations from blocking upgrades (see needle-a55a9e18).
        self.validate_expected_status(&outcome, bead_id, &path)?;

        Ok(outcome)
    }

    /// Validate that expected final_status values are valid bead-forge statuses.
    ///
    /// bead-forge (bf) uses: open, in_progress, blocked, closed, completed, pending, ready
    /// bead-rs uses: open, in_progress, blocked, closed, deferred
    ///
    /// Both treat "closed" as equivalent to "done" — a successfully completed bead.
    fn validate_expected_status(
        &self,
        expected: &ExpectedOutcome,
        _bead_id: &str,
        path: &Path,
    ) -> Result<()> {
        // Valid statuses across both backends (union of both sets)
        let valid_statuses = [
            "open",
            "in_progress",
            "blocked",
            "closed",
            "completed",
            "pending",
            "ready",
            "deferred",
        ];

        let status_to_check = match expected {
            ExpectedOutcome::Success { final_status, .. } => Some(final_status.as_str()),
            ExpectedOutcome::Failure { final_status, .. } => Some(final_status.as_str()),
            ExpectedOutcome::Timeout { final_status } => Some(final_status.as_str()),
            ExpectedOutcome::StateMachine { .. } => None, // State machine tests don't check final_status
        };

        if let Some(status) = status_to_check {
            if !valid_statuses.contains(&status) {
                bail!(
                    "invalid expected final_status '{status}' in {}: must be one of {:?}. \
                     This indicates a stale canary expectation that must be updated to match \
                     current bead CLI vocabulary. Run 'bead show <bead_id> --json' to see \
                     actual statuses in use.",
                    path.display(),
                    valid_statuses
                );
            }

            // Additional check: "done" is not a valid bead-forge status (it's "closed")
            if status == "done" {
                bail!(
                    "invalid expected final_status 'done' in {}: bead-forge uses 'closed' \
                     for completed beads, not 'done'. Update the expectation to use 'closed'. \
                     See ADR for needle-a55a9e18 for details.",
                    path.display()
                );
            }
        }

        Ok(())
    }

    /// Run a single canary test.
    ///
    /// Spawns the testing binary and polls for completion, killing the process
    /// and returning [`CanaryTestResult::TimedOut`] if it exceeds `test_timeout`.
    fn run_test(
        &self,
        bead_id: &str,
        expected: &ExpectedOutcome,
        testing_binary: &Path,
    ) -> CanaryTestResult {
        let start = Instant::now();
        let timeout = Duration::from_secs(self.test_timeout);

        // Spawn the testing binary against the canary workspace.
        //
        // `--workspace` alone does NOT confine the worker. The Explore strand is
        // enabled by default and, with `workspaces` empty, scans
        // `workspace_root` — which defaults to $HOME — for every bead workspace
        // on the box. A canary worker spawned without isolation therefore roams
        // into the operator's real repos and dispatches agents against them: on
        // 2026-08-07 a single `needle upgrade` put four agents into spaxel and
        // bead-forge, where they ran git commands and left files behind. This is
        // the same escape ADR-006 documents for tests, and the isolation policy
        // in CLAUDE.md was never applied to the canary runner itself.
        //
        // Pin the scan root to the canary workspace and switch Explore off, so a
        // canary run can only ever touch beads that live in the canary.
        //
        // CRITICAL: Set NEEDLE_INNER=1 to prevent the worker from detaching into
        // tmux. Without this, the worker runs in the background and the canary
        // evaluates outcomes before work finishes, producing false negatives.
        //
        // CRITICAL: Isolate HOME to prevent reading global config at
        // ~/.config/needle/config.yaml or ~/.needle/. This ensures the canary
        // worker only reads from the canary workspace's .needle.yaml.
        //
        // CRITICAL: Use explicit adapter selection (echo) to avoid inheriting
        // obsolete adapter names from global config (e.g., 'opus' which was
        // deprecated in favor of 'claude-anthropic-opus'). The echo adapter is
        // hermetic and always available for testing.
        //
        // Use retry wrapper to handle ETXTBSY (errno 26) which can occur when
        // spawning a testing binary that was written to disk immediately before
        // execution (race condition between write close and kernel page cache sync).
        let mut child = match crate::bead_store::spawn_with_etxtbsy_retry_sync_exponential_child(
            || {
                Command::new(testing_binary)
                    .args([
                        "run",
                        "--workspace",
                        &self.canary_workspace.display().to_string(),
                        "--agent",
                        "echo", // Use explicit hermetic adapter, not global default
                        "--identifier",
                        &format!("canary-{bead_id}"),
                        "--count",
                        "1",
                    ])
                    .env("NEEDLE_INNER", "1")
                    .env("NEEDLE_STRANDS__EXPLORE__ENABLED", "false")
                    .env(
                        "NEEDLE_STRANDS__EXPLORE__WORKSPACE_ROOT",
                        &self.canary_workspace,
                    )
                    .env("HOME", &self.canary_workspace)
                    .spawn()
            },
            10,
            20,
        ) {
            Ok(c) => c,
            Err(e) => {
                return CanaryTestResult::Error {
                    bead_id: bead_id.to_string(),
                    message: format!("failed to spawn testing binary: {e}"),
                };
            }
        };

        // Poll for completion, enforcing the timeout.
        let exit_code = loop {
            match child.try_wait() {
                Ok(Some(status)) => break status.code(),
                Ok(None) => {
                    if start.elapsed() >= timeout {
                        child.kill().ok();
                        return CanaryTestResult::TimedOut {
                            bead_id: bead_id.to_string(),
                            elapsed_secs: start.elapsed().as_secs(),
                        };
                    }
                    std::thread::sleep(Duration::from_millis(500));
                }
                Err(e) => {
                    return CanaryTestResult::Error {
                        bead_id: bead_id.to_string(),
                        message: format!("failed to wait for testing binary: {e}"),
                    };
                }
            }
        };

        let actual = match self.get_actual_outcome(bead_id, exit_code) {
            Ok(a) => a,
            Err(e) => {
                return CanaryTestResult::Error {
                    bead_id: bead_id.to_string(),
                    message: format!("failed to get actual outcome: {e}"),
                };
            }
        };

        // Compare actual vs expected.
        if self.outcomes_match(expected, &actual) {
            CanaryTestResult::Passed {
                bead_id: bead_id.to_string(),
                expected: expected.clone(),
                actual,
            }
        } else {
            let reason = self.mismatch_reason(expected, &actual);
            CanaryTestResult::Failed {
                bead_id: bead_id.to_string(),
                expected: expected.clone(),
                actual,
                reason,
            }
        }
    }

    /// Get the actual outcome for a test bead by querying the bead store.
    fn get_actual_outcome(&self, bead_id: &str, exit_code: Option<i32>) -> Result<ActualOutcome> {
        let workspace_config = crate::config::ConfigLoader::load_workspace(&self.canary_workspace)?
            .with_context(|| {
                format!(
                    "canary workspace {} has no .needle.yaml",
                    self.canary_workspace.display()
                )
            })?;
        let bead_cli = workspace_config.bead_cli.with_context(|| {
            format!(
                "canary workspace {} has no authoritative bead_cli.backend binding",
                self.canary_workspace.display()
            )
        })?;
        let (_, binary) = crate::config::resolve_bead_cli(&bead_cli).with_context(|| {
            format!(
                "failed to resolve canary workspace bead backend '{}'",
                bead_cli.backend
            )
        })?;

        let output = crate::bead_store::spawn_with_etxtbsy_retry_sync(
            || {
                Command::new(&binary)
                    .args(["show", bead_id, "--json"])
                    .current_dir(&self.canary_workspace)
                    .output()
            },
            5,
            20,
        )
        .with_context(|| {
            format!(
                "failed to run {} show for backend '{}'",
                binary.display(),
                bead_cli.backend
            )
        })?;

        if !output.status.success() {
            bail!(
                "backend '{}' show failed for bead {}: {}",
                bead_cli.backend,
                bead_id,
                String::from_utf8_lossy(&output.stderr).trim()
            );
        }

        let stdout = String::from_utf8_lossy(&output.stdout);
        let projection: serde_json::Value = serde_json::from_str(&stdout)
            .with_context(|| format!("failed to parse '{}' show output", bead_cli.backend))?;
        // bead-rs returns a one-element array; bead-forge returns an object.
        let bead = projection
            .as_array()
            .and_then(|items| items.first())
            .unwrap_or(&projection);

        let final_status = bead["status"].as_str().unwrap_or("unknown").to_string();
        let labels = bead["labels"]
            .as_array()
            .map(|arr| {
                arr.iter()
                    .filter_map(|v| v.as_str().map(|s| s.to_string()))
                    .collect()
            })
            .unwrap_or_default();

        Ok(ActualOutcome {
            exit_code,
            final_status,
            labels,
            state_transitions: Vec::new(), // Not tracked in basic canary tests
        })
    }

    /// Check if actual outcome matches expected outcome.
    fn outcomes_match(&self, expected: &ExpectedOutcome, actual: &ActualOutcome) -> bool {
        match expected {
            ExpectedOutcome::Success {
                final_status,
                labels,
            } => {
                actual.final_status == *final_status
                    && labels.iter().all(|l| actual.labels.contains(l))
                    && actual.exit_code == Some(0)
            }
            ExpectedOutcome::Failure {
                final_status,
                labels,
            } => {
                actual.final_status == *final_status
                    && labels.iter().all(|l| actual.labels.contains(l))
                    && actual.exit_code.is_some_and(|c| c != 0)
            }
            ExpectedOutcome::Timeout { final_status } => {
                // Timeout is harder to detect from exit code alone.
                // We check that the bead was not closed (still open/in_progress).
                actual.final_status == *final_status
            }
            ExpectedOutcome::StateMachine { transitions } => {
                // Check that all expected transitions occurred.
                transitions
                    .iter()
                    .all(|t| actual.state_transitions.contains(t))
            }
        }
    }

    /// Generate a human-readable reason for mismatch.
    fn mismatch_reason(&self, expected: &ExpectedOutcome, actual: &ActualOutcome) -> String {
        match expected {
            ExpectedOutcome::Success {
                final_status,
                labels,
            } => {
                let mut reasons = Vec::new();
                if actual.final_status != *final_status {
                    reasons.push(format!(
                        "status mismatch: expected '{}', got '{}'",
                        final_status, actual.final_status
                    ));
                }
                for label in labels {
                    if !actual.labels.contains(label) {
                        reasons.push(format!("missing label: '{}'", label));
                    }
                }
                if actual.exit_code != Some(0) {
                    reasons.push(format!("expected exit 0, got {:?}", actual.exit_code));
                }
                reasons.join("; ")
            }
            ExpectedOutcome::Failure {
                final_status,
                labels,
            } => {
                let mut reasons = Vec::new();
                if actual.final_status != *final_status {
                    reasons.push(format!(
                        "status mismatch: expected '{}', got '{}'",
                        final_status, actual.final_status
                    ));
                }
                for label in labels {
                    if !actual.labels.contains(label) {
                        reasons.push(format!("missing label: '{}'", label));
                    }
                }
                if actual.exit_code == Some(0) {
                    reasons.push("expected non-zero exit, got 0".to_string());
                }
                reasons.join("; ")
            }
            ExpectedOutcome::Timeout { final_status } => {
                format!(
                    "status mismatch: expected '{}', got '{}'",
                    final_status, actual.final_status
                )
            }
            ExpectedOutcome::StateMachine { transitions } => {
                let missing: Vec<_> = transitions
                    .iter()
                    .filter(|t| !actual.state_transitions.contains(*t))
                    .collect();
                format!("missing state transitions: {:?}", missing)
            }
        }
    }

    /// Promote :testing to :stable, backing up the current :stable.
    ///
    /// This operation is atomic at the filesystem level:
    /// 1. Move :stable to :stable.prev
    /// 2. Move :testing to :stable
    /// 3. Update symlink
    pub fn promote(&self) -> Result<()> {
        let testing = self.testing_binary();
        let stable = self.stable_binary();
        let prev = self.prev_binary();
        let symlink = self.symlink_path();

        if !testing.exists() {
            bail!("testing binary not found: {}", testing.display());
        }

        // Ensure bin directory exists.
        let bin_dir = self.needle_home.join("bin");
        std::fs::create_dir_all(&bin_dir).context("failed to create bin directory")?;

        // Step 1: Remove old backup if exists.
        if prev.exists() {
            std::fs::remove_file(&prev).context("failed to remove old backup")?;
        }

        // Step 2: Move current stable to backup (if exists).
        if stable.exists() {
            std::fs::rename(&stable, &prev).context("failed to backup stable binary")?;
        }

        // Step 3: Move testing to stable.
        std::fs::rename(&testing, &stable).context("failed to promote testing to stable")?;

        // Step 4: Update symlink.
        if symlink.exists() || symlink.is_symlink() {
            std::fs::remove_file(&symlink).context("failed to remove old symlink")?;
        }
        #[cfg(unix)]
        std::os::unix::fs::symlink(&stable, &symlink).context("failed to create symlink")?;
        #[cfg(not(unix))]
        {
            // Fallback: copy instead of symlink on non-Unix.
            std::fs::copy(&stable, &symlink).context("failed to copy stable to symlink")?;
        }

        tracing::info!(
            testing = %testing.display(),
            stable = %stable.display(),
            prev = %prev.display(),
            symlink = %symlink.display(),
            "promoted testing to stable"
        );

        Ok(())
    }

    /// Reject :testing (discard without promoting).
    ///
    /// The fleet remains on :stable unchanged.
    pub fn reject(&self) -> Result<()> {
        let testing = self.testing_binary();

        if testing.exists() {
            std::fs::remove_file(&testing).context("failed to remove testing binary")?;
            tracing::info!(path = %testing.display(), "rejected testing binary");
        } else {
            tracing::warn!("no testing binary to reject");
        }

        Ok(())
    }

    /// Rollback to the previous stable binary.
    ///
    /// Restores :stable.prev as :stable.
    pub fn rollback(&self) -> Result<()> {
        let stable = self.stable_binary();
        let prev = self.prev_binary();
        let symlink = self.symlink_path();

        if !prev.exists() {
            bail!("no previous stable binary to rollback to");
        }

        // Move current stable aside (if exists) - it becomes the new backup.
        if stable.exists() {
            // Remove the old backup first since we're about to replace it.
            let old_backup = self.needle_home.join("bin/needle-stable.rollback");
            if old_backup.exists() {
                std::fs::remove_file(&old_backup)?;
            }
            std::fs::rename(&stable, &old_backup)?;
        }

        // Restore previous stable.
        std::fs::rename(&prev, &stable).context("failed to restore previous stable")?;

        // Update symlink.
        if symlink.exists() || symlink.is_symlink() {
            std::fs::remove_file(&symlink)?;
        }
        #[cfg(unix)]
        std::os::unix::fs::symlink(&stable, &symlink).context("failed to update symlink")?;

        tracing::info!(
            stable = %stable.display(),
            "rolled back to previous stable"
        );

        Ok(())
    }

    /// Check the current release channel status.
    pub fn status(&self) -> Result<ChannelStatus> {
        let testing = self.testing_binary();
        let stable = self.stable_binary();
        let prev = self.prev_binary();
        let symlink = self.symlink_path();

        let testing_exists = testing.exists();
        let stable_exists = stable.exists();
        let prev_exists = prev.exists();

        let symlink_target = if symlink.is_symlink() {
            std::fs::read_link(&symlink).ok()
        } else {
            None
        };

        Ok(ChannelStatus {
            testing_exists,
            stable_exists,
            prev_exists,
            symlink_target,
            testing_path: testing,
            stable_path: stable,
            prev_path: prev,
            symlink_path: symlink,
        })
    }
}

// ──────────────────────────────────────────────────────────────────────────────
// ChannelStatus
// ──────────────────────────────────────────────────────────────────────────────

/// Status of the release channels.
#[derive(Debug, Clone)]

-- fixture bead records (bead-rs) --
--- bead-1b6a6357
error: unexpected argument '--db' found

Usage: bead <COMMAND>

For more information, try '--help'.
--- bead-54b7174d
error: unexpected argument '--db' found

Usage: bead <COMMAND>

For more information, try '--help'.
--- bead-60c759fa
error: unexpected argument '--db' found

Usage: bead <COMMAND>

For more information, try '--help'.
--- bead-e6f0b50d
error: unexpected argument '--db' found

Usage: bead <COMMAND>

For more information, try '--help'.

-- actual in-repo bead --
[{"assignee":"codex-gpt-5.6-terra-xhigh-terra-zaiproxy","comments":[],"created_at":"2026-08-17T09:42:38.306399323Z","dependencies":[],"description":"Read all four expected/*.yaml files under ~/.needle/canary/expected/ and document:\n\n1. What each test asserts (final_status, labels, etc.)\n2. Which assertions are stale vs. current\n3. What bead-forge's actual vocabulary is (open, in_progress, blocked, closed, completed, pending, ready)\n\nDeliverable: A summary in this bead's body listing:\n- All four expected files and their current assertions\n- Current bead-forge vocabulary (verify with 'bf show --json' on a real bead)\n- What needs to change in each file\n\nAcceptance: Bead body contains complete inventory of current expectations and what's stale.","id":"needle-16546862","labels":["split-child","verification-failed"],"priority":3,"revision":14,"status":"in_progress","title":"Inventory and document current canary expectations","updated_at":"2026-08-22T04:50:45.307447333Z"}]

-- legacy bf help/version --
bf 0.4.1
Show bead details

Prints full details for a single bead: title, status, priority, type, description, assignee, labels, and dependencies. If the ID is not in the active database, archive files are searched as a fallback.

Usage: bf show [OPTIONS] <ID>

Arguments:
  <ID>
          Bead ID

Options:
      --format <FORMAT>
          Output format (text, json, toon)
          
          [default: text]

  -w, --workspace <WORKSPACE>
          Workspace directory (defaults to current directory's .beads/)

      --json
          Output JSON (alias for --format json)

      --no-auto-flush
          Disable the automatic SQLite→JSONL flush after mutating commands for this invocation. Overrides `sync.auto_flush: true` in config; a no-op when auto-flush is already off. See `crate::autoflush::enabled`

      --envelope
          Wrap --json output in a standard envelope: {version, kind, data, warning?}. This provides stable structure for programmatic consumers. See `bf robot-docs`

      --no-progress
          Disable progress indicators for this invocation. Useful for scripted environments or CI pipelines where visual progress output is unwanted

  -V, --version
          Print version information ("bf <version>") and exit 0.
          
          Declared by hand rather than via clap's built-in version flag (see `disable_version_flag` above) so the version is written to stdout by `run` instead of clap's own writer, and so it is accepted anywhere on the command line, including after a subcommand.

  -h, --help
          Print help (see a summary with '-h')

codex
The canary is currently bound to bead-rs, while its validator still describes a separate bead-forge vocabulary. I found an archived `bf` 0.4.1 binary and will use it read-only against a confirmed legacy store—never against this repository’s bead-rs database.
exec
/bin/bash -lc "printf '%s\\n' '-- confirmed legacy stores --'
find /home/coding -maxdepth 4 -type f -path '*/.beads/config.yaml' -print 2>/dev/null | sed -n '1,80p'
printf '%s\\n' '-- canary bead-rs records --'
for bead_id in bead-1b6a6357 bead-54b7174d bead-60c759fa bead-e6f0b50d; do
  echo \"--- "'${bead_id}"
  (cd /home/coding/.needle/canary && bead show "$bead_id" --json) || true
done
printf '"'%s\\n' '-- current bead status syntax --'
bead update --help | sed -n '1,220p'
printf '%s\\n' '-- archived bf global status hints --'
/home/coding/backups/bf-tooling-removed-20260814/bf --help | sed -n '1,260p'" in /home/coding/NEEDLE
 succeeded in 19141ms:
/home/coding/nixos-asterisk/docs/.beads/config.yaml
/home/coding/scratch/tcb-bisect/.beads/config.yaml
/home/coding/Research/weather-fast/.beads/config.yaml
/home/coding/Research/mission-control/.beads/config.yaml
/home/coding/Research/beads/.beads/config.yaml
/home/coding/Research/control-panel/.beads/config.yaml
/home/coding/Research/remote-control-android/.beads/config.yaml
-- canary bead-rs records --
--- bead-1b6a6357
[{"assignee":"claude-code-glm-4.7-canary-bead-54b7174d","comments":[],"created_at":"2026-08-17T09:29:00.600429407Z","dependencies":[],"description":"This is a happy path test bead. The agent should complete successfully and close this bead.","id":"bead-1b6a6357","labels":[],"priority":4,"revision":4,"status":"closed","title":"Happy path test","updated_at":"2026-08-17T19:08:36.814398183Z"}]
--- bead-54b7174d
[{"assignee":"claude-code-glm-4.7-canary-bead-60c759fa","comments":[],"created_at":"2026-08-17T09:29:37.759249506Z","dependencies":[],"description":"This is a timeout test bead. The agent should timeout during processing.","id":"bead-54b7174d","labels":[],"priority":4,"revision":4,"status":"closed","title":"Timeout test","updated_at":"2026-08-17T19:08:01.114440724Z"}]
--- bead-60c759fa
[{"assignee":null,"comments":[],"created_at":"2026-08-17T09:29:37.752834473Z","dependencies":[],"description":"This is a failure path test bead. The agent should fail and leave this bead blocked.","id":"bead-60c759fa","labels":[],"priority":4,"revision":4,"status":"open","title":"Failure path test","updated_at":"2026-08-17T19:09:26.801511102Z"}]
--- bead-e6f0b50d
[{"assignee":"claude-code-glm-4.7-canary-bead-1b6a6357","comments":[],"created_at":"2026-08-17T09:29:37.764262377Z","dependencies":[],"description":"This is a state machine integrity test. The worker should transition through all expected states.","id":"bead-e6f0b50d","labels":[],"priority":4,"revision":4,"status":"closed","title":"State machine test","updated_at":"2026-08-17T19:09:52.371422091Z"}]
-- current bead status syntax --
Update issue fields atomically.

Updates one or more issue fields in a single atomic transaction.
All validations and mutations occur together; any failure leaves the
issue unchanged. Changing status to 'closed' requires the 'close' command
instead. Use 'reopen' to transition from closed to open.

EXAMPLES:
  bead update ID --status in_progress                # Start working on an issue
  bead update ID --assignee alice                    # Assign to alice
  bead update ID --notes "Investigated root cause"   # Add investigation notes
  bead update ID --clear-assignee                    # Clear assignment (open only)
  bead update ID --status in_progress --if-revision 3 # With revision guard

STATUS TRANSITIONS:
  Valid transitions depend on the current base status:
  - open can transition to: in_progress, deferred
  - in_progress can transition to: open, deferred, closed (via 'close' command)
  - deferred can transition to: open, closed (via 'close' command)
  - closed can only transition to: open (via 'reopen' command)

FIELDS THIS COMMAND DOES NOT CHANGE:
  Title, description, priority, issue type, and labels are not editable here.
  Set them at creation time with 'bead create', and use 'bead label add' or
  'bead label remove' for labels. Passing --priority or --title is a usage error.

ASSIGNMENT:
  --assignee and --clear-assignee are mutually exclusive.
  --clear-assignee only works for open assigned issues.
  For in_progress issues, use 'bead release' to return to open/unassigned.

BLOCKED STATUS:
  Setting --status blocked sets manual_blocked=true and retains base status.
  Setting --status open clears manual blocking and sets base status to open.
  Closed issues cannot use --status open (use 'reopen' instead).

REVISION GUARDS:
  --if-revision N provides optimistic concurrency control.
  If the issue's current revision doesn't match N, the update fails with
  exit code 4 and a conflict message. This prevents silent lost updates
  when multiple agents or humans modify the same issue concurrently.

All updates are atomic. Invalid transitions or conflicts exit with code 4
without changing any fields or timestamps.

Usage: bead update [OPTIONS] <ID>

Arguments:
  <ID>
          Issue ID

Options:
      --status <STATUS>
          New status

      --assignee <ASSIGNEE>
          New assignee

      --clear-assignee
          Clear assignee (only for open assigned issues)

      --notes <NOTES>
          New notes

      --if-revision <IF_REVISION>
          Expected revision for optimistic concurrency control

      --fencing-token <FENCING_TOKEN>
          Fencing token for lease validation (advanced usage)

      --dry-run
          Dry run: show what would happen without making changes

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version
-- archived bf global status hints --
bead-forge - Drop-in replacement for beads_rust (br)

Usage: bf [OPTIONS] [COMMAND]

Commands:
  create         Create a new bead
  list           List beads
  show           Show bead details
  update         Update a bead
  close          Close a bead
  reopen         Reopen a bead
  delete         Delete a bead
  ready          Show ready (unblocked) beads
  claim          Claim a bead (atomic)
  init           Initialize a new workspace
  sync           Sync (flush to JSONL or import from JSONL)
  doctor         Doctor - check and repair
  merge-jsonl    Three-way merge of JSONL bead files (usable as a git merge driver)
  commit-check   Commit check - scan staged .beads/ changes for secrets (git pre-commit hook)
  count          Count beads
  batch          Batch operations (atomic)
  mitosis        Mitosis: split a bead into children atomically
  dep            Manage dependencies
  label          Manage labels
  labels         List labels for beads
  comments       Manage comments
  search         Search beads
  stats          Show statistics
  schema         Emit JSON Schema
  config         Configuration management
  velocity       Show velocity stats (bead-forge specific)
  annotate       Manage annotations
  log            Show event log for a bead
  critical-path  Show critical path (longest chain of blocking dependencies)
  rotate         Rotate (archive) closed beads older than threshold
  migrate        Migrate workspace from br to bf
  recent         Show recently modified beads
  robot-docs     Robot docs - machine-readable command contract documentation
  help           Print this message or the help of the given subcommand(s)

Options:
  -w, --workspace <WORKSPACE>
          Workspace directory (defaults to current directory's .beads/)

      --no-auto-flush
          Disable the automatic SQLite→JSONL flush after mutating commands for this invocation. Overrides `sync.auto_flush: true` in config; a no-op when auto-flush is already off. See `crate::autoflush::enabled`

      --envelope
          Wrap --json output in a standard envelope: {version, kind, data, warning?}. This provides stable structure for programmatic consumers. See `bf robot-docs`

      --no-progress
          Disable progress indicators for this invocation. Useful for scripted environments or CI pipelines where visual progress output is unwanted

  -V, --version
          Print version information ("bf <version>") and exit 0.
          
          Declared by hand rather than via clap's built-in version flag (see `disable_version_flag` above) so the version is written to stdout by `run` instead of clap's own writer, and so it is accepted anywhere on the command line, including after a subcommand.

  -h, --help
          Print help (see a summary with '-h')

exec
/bin/bash -lc "printf '%s\\n' '-- likely legacy workspace directories --'
for directory in /home/coding/bead-forge /home/coding/SEAM /home/coding/commitgraph /home/coding/NEEDLE /home/coding/backups; do
  if test -d \""'$directory"; then
    printf '"'%s: ' \""'$directory"
    if test -f "$directory/.beads/config.yaml"; then echo legacy; elif test -f "$directory/.beads/config.json"; then echo bead-rs; else echo no-store; fi
  fi
done
printf '"'%s\\n' '-- canary bead-rs records --'
for bead_id in bead-1b6a6357 bead-54b7174d bead-60c759fa bead-e6f0b50d; do
  echo \"--- "'${bead_id}"
  (cd /home/coding/.needle/canary && bead show "$bead_id" --json) || true
done
printf '"'%s\\n' '-- bead update syntax --'
bead update --help | sed -n '1,220p'
printf '%s\\n' '-- bf command list --'
/home/coding/backups/bf-tooling-removed-20260814/bf --help | sed -n '1,240p'" in /home/coding/NEEDLE
 succeeded in 0ms:
-- likely legacy workspace directories --
/home/coding/SEAM: bead-rs
/home/coding/commitgraph: bead-rs
/home/coding/NEEDLE: bead-rs
/home/coding/backups: no-store
-- canary bead-rs records --
--- bead-1b6a6357
[{"assignee":"claude-code-glm-4.7-canary-bead-54b7174d","comments":[],"created_at":"2026-08-17T09:29:00.600429407Z","dependencies":[],"description":"This is a happy path test bead. The agent should complete successfully and close this bead.","id":"bead-1b6a6357","labels":[],"priority":4,"revision":4,"status":"closed","title":"Happy path test","updated_at":"2026-08-17T19:08:36.814398183Z"}]
--- bead-54b7174d
[{"assignee":"claude-code-glm-4.7-canary-bead-60c759fa","comments":[],"created_at":"2026-08-17T09:29:37.759249506Z","dependencies":[],"description":"This is a timeout test bead. The agent should timeout during processing.","id":"bead-54b7174d","labels":[],"priority":4,"revision":4,"status":"closed","title":"Timeout test","updated_at":"2026-08-17T19:08:01.114440724Z"}]
--- bead-60c759fa
[{"assignee":null,"comments":[],"created_at":"2026-08-17T09:29:37.752834473Z","dependencies":[],"description":"This is a failure path test bead. The agent should fail and leave this bead blocked.","id":"bead-60c759fa","labels":[],"priority":4,"revision":4,"status":"open","title":"Failure path test","updated_at":"2026-08-17T19:09:26.801511102Z"}]
--- bead-e6f0b50d
[{"assignee":"claude-code-glm-4.7-canary-bead-1b6a6357","comments":[],"created_at":"2026-08-17T09:29:37.764262377Z","dependencies":[],"description":"This is a state machine integrity test. The worker should transition through all expected states.","id":"bead-e6f0b50d","labels":[],"priority":4,"revision":4,"status":"closed","title":"State machine test","updated_at":"2026-08-17T19:09:52.371422091Z"}]
-- bead update syntax --
Update issue fields atomically.

Updates one or more issue fields in a single atomic transaction.
All validations and mutations occur together; any failure leaves the
issue unchanged. Changing status to 'closed' requires the 'close' command
instead. Use 'reopen' to transition from closed to open.

EXAMPLES:
  bead update ID --status in_progress                # Start working on an issue
  bead update ID --assignee alice                    # Assign to alice
  bead update ID --notes "Investigated root cause"   # Add investigation notes
  bead update ID --clear-assignee                    # Clear assignment (open only)
  bead update ID --status in_progress --if-revision 3 # With revision guard

STATUS TRANSITIONS:
  Valid transitions depend on the current base status:
  - open can transition to: in_progress, deferred
  - in_progress can transition to: open, deferred, closed (via 'close' command)
  - deferred can transition to: open, closed (via 'close' command)
  - closed can only transition to: open (via 'reopen' command)

FIELDS THIS COMMAND DOES NOT CHANGE:
  Title, description, priority, issue type, and labels are not editable here.
  Set them at creation time with 'bead create', and use 'bead label add' or
  'bead label remove' for labels. Passing --priority or --title is a usage error.

ASSIGNMENT:
  --assignee and --clear-assignee are mutually exclusive.
  --clear-assignee only works for open assigned issues.
  For in_progress issues, use 'bead release' to return to open/unassigned.

BLOCKED STATUS:
  Setting --status blocked sets manual_blocked=true and retains base status.
  Setting --status open clears manual blocking and sets base status to open.
  Closed issues cannot use --status open (use 'reopen' instead).

REVISION GUARDS:
  --if-revision N provides optimistic concurrency control.
  If the issue's current revision doesn't match N, the update fails with
  exit code 4 and a conflict message. This prevents silent lost updates
  when multiple agents or humans modify the same issue concurrently.

All updates are atomic. Invalid transitions or conflicts exit with code 4
without changing any fields or timestamps.

Usage: bead update [OPTIONS] <ID>

Arguments:
  <ID>
          Issue ID

Options:
      --status <STATUS>
          New status

      --assignee <ASSIGNEE>
          New assignee

      --clear-assignee
          Clear assignee (only for open assigned issues)

      --notes <NOTES>
          New notes

      --if-revision <IF_REVISION>
          Expected revision for optimistic concurrency control

      --fencing-token <FENCING_TOKEN>
          Fencing token for lease validation (advanced usage)

      --dry-run
          Dry run: show what would happen without making changes

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version
-- bf command list --
bead-forge - Drop-in replacement for beads_rust (br)

Usage: bf [OPTIONS] [COMMAND]

Commands:
  create         Create a new bead
  list           List beads
  show           Show bead details
  update         Update a bead
  close          Close a bead
  reopen         Reopen a bead
  delete         Delete a bead
  ready          Show ready (unblocked) beads
  claim          Claim a bead (atomic)
  init           Initialize a new workspace
  sync           Sync (flush to JSONL or import from JSONL)
  doctor         Doctor - check and repair
  merge-jsonl    Three-way merge of JSONL bead files (usable as a git merge driver)
  commit-check   Commit check - scan staged .beads/ changes for secrets (git pre-commit hook)
  count          Count beads
  batch          Batch operations (atomic)
  mitosis        Mitosis: split a bead into children atomically
  dep            Manage dependencies
  label          Manage labels
  labels         List labels for beads
  comments       Manage comments
  search         Search beads
  stats          Show statistics
  schema         Emit JSON Schema
  config         Configuration management
  velocity       Show velocity stats (bead-forge specific)
  annotate       Manage annotations
  log            Show event log for a bead
  critical-path  Show critical path (longest chain of blocking dependencies)
  rotate         Rotate (archive) closed beads older than threshold
  migrate        Migrate workspace from br to bf
  recent         Show recently modified beads
  robot-docs     Robot docs - machine-readable command contract documentation
  help           Print this message or the help of the given subcommand(s)

Options:
  -w, --workspace <WORKSPACE>
          Workspace directory (defaults to current directory's .beads/)

      --no-auto-flush
          Disable the automatic SQLite→JSONL flush after mutating commands for this invocation. Overrides `sync.auto_flush: true` in config; a no-op when auto-flush is already off. See `crate::autoflush::enabled`

      --envelope
          Wrap --json output in a standard envelope: {version, kind, data, warning?}. This provides stable structure for programmatic consumers. See `bf robot-docs`

      --no-progress
          Disable progress indicators for this invocation. Useful for scripted environments or CI pipelines where visual progress output is unwanted

  -V, --version
          Print version information ("bf <version>") and exit 0.
          
          Declared by hand rather than via clap's built-in version flag (see `disable_version_flag` above) so the version is written to stdout by `run` instead of clap's own writer, and so it is accepted anywhere on the command line, including after a subcommand.

  -h, --help
          Print help (see a summary with '-h')

exec
/bin/bash -lc "printf '%s\\n' '-- candidate repositories --'
find /home/coding -mindepth 1 -maxdepth 1 -type d -printf '%f\\n' | sort | sed -n '1,240p'
printf '%s\\n' '-- legacy marker files in top-level projects --'
for directory in /home/coding/*; do
  test -d \""'$directory" || continue
  if test -f "$directory/.beads/config.yaml" || test -f "$directory/.beads/issues.jsonl"; then
    printf '"'%s\\n' \""'$directory"
    test -f "$directory/.beads/config.yaml" && sed -n '"'1,40p' \""'$directory/.beads/config.yaml"
    test -f "$directory/.beads/issues.jsonl" && wc -l "$directory/.beads/issues.jsonl"
  fi
done
printf '"'%s\\n' '-- archived bf status contract --'
/home/coding/backups/bf-tooling-removed-20260814/bf robot-docs --help | sed -n '1,240p'
/home/coding/backups/bf-tooling-removed-20260814/bf schema --help | sed -n '1,220p'" in /home/coding/NEEDLE
 succeeded in 0ms:
-- candidate repositories --
.age
.agents
.agentscribe
.android
.ansible
.aws
.beads
.beads-registry
.benchmarks
.bun
.caam
.cache
.cargo
.ccdash
.clasp
.claude
.cloudflared
.codex
.config
.copilot
.deno
.docker
.dolt
.dotnet
.duckdb
.espressif
.fonts
.forge
.git
.gnupg
.gradle
.hoop
.kube
.local
.marathon
.mc
.needle
.needle-backups
.needle-test-errors-406244
.needle-test-errors-repro-450162
.needle-test-otlp-1786814214
.nix-defexpr
.npm
.pki
.platformio
.pytest_cache
.restic
.rota
.rustup
.sdkman
.sigil
.sigil.backup
.ssh
.telegram-claude-bridge-deploy
.terraform.d
.tmp
.tmp-crashtest
.tmux
.vscode-server
ARMOR
AgentScribe
Android
CLASP
Downloads
FORGE
MANA
NEEDLE
PERCH
ROTA
Research
SEAM
TRACE
TUNNEL
agent-transcript-archive
agentists-quickstart
agentists-quickstart-deprecated
ai-code-battle
aide-de-camp
apexalgo-growth-lab
apexalgo-site
archives
ardenone-cluster
argo-workflows-exporter
artifacts.hardyrekshin.com
avallon
b2-usage-exporter
backups
bead-rs
bf-migration-backup-ex44
bin
bobbin
bootstrap
botburrow
botburrow-agents
brand-kit
ccdash
claude-code-rlm
claude-config
claude-governor
claude-print
claude-swap
claw-code
cluster-manifests
cluster-status-exporter
clustertop
codex-governor
commitgraph
commitgraph-deprecated
coned-rate-optimizer
dashboard-site
declarative-config
doublers-digest
duck-e
dwell
esp-idf
ex44-codex
face-detection
feelings-wheel
fig-risk
game-of-life
git-activity-exporter
go
go-install
go-sdk
gopath
gribtract
halfonadouble.com
hetzner-auction-dashboard
historical-options-mcp
ibkr-mcp
investment-research-mcp
irreversible-command-gate
jedarden
jedarden.com
jeds-curated-skills
jetstream-watchdog
kalshi-tape
kalshi-weather
keys
local
mastagoose-trading
mobile-gaming
moltbook-org
mta-my-way
needle-pod
news-trader
news-trading-ideas
nixos-asterisk
not-a-robot
odin-deep-research
options-analysis
options-downloader-status-exporter
palm-reading
perplexity-jedarden
personal-strategy
personal-work
pose-detection
rackspace-spot-terraform
reddit-user-archiver
remote-index
research
runsybil
scratch
scratch-rota-journal
sdk
shuffle-gallery
sigil-signatures
social-media-strategy
spaxel
src
sun-sim
target
telegram-claude-bridge
tftask
tradegraph
trail-boss
twitterapi-proxy
unfairmarket-research
vibe-coding-discovery
vibecodeleaderboard-backend
vibecodeleaderboard-frontend
vista
warden
website-builder
whofi
whofi-bench
workspace
yt-transcript-dl-mcp
ytt
zai-proxy
~
-- legacy marker files in top-level projects --
-- archived bf status contract --
Robot docs - machine-readable command contract documentation

Outputs a JSON schema describing every command's --json output contract, enabling agent consumers to parse responses programmatically without hardcoding shapes.

Usage: bf robot-docs [OPTIONS]

Options:
      --format <FORMAT>
          Output format (text, json)
          
          [default: json]

  -w, --workspace <WORKSPACE>
          Workspace directory (defaults to current directory's .beads/)

      --no-auto-flush
          Disable the automatic SQLite→JSONL flush after mutating commands for this invocation. Overrides `sync.auto_flush: true` in config; a no-op when auto-flush is already off. See `crate::autoflush::enabled`

      --envelope
          Wrap --json output in a standard envelope: {version, kind, data, warning?}. This provides stable structure for programmatic consumers. See `bf robot-docs`

      --no-progress
          Disable progress indicators for this invocation. Useful for scripted environments or CI pipelines where visual progress output is unwanted

  -V, --version
          Print version information ("bf <version>") and exit 0.
          
          Declared by hand rather than via clap's built-in version flag (see `disable_version_flag` above) so the version is written to stdout by `run` instead of clap's own writer, and so it is accepted anywhere on the command line, including after a subcommand.

  -h, --help
          Print help (see a summary with '-h')
Emit JSON Schema

With "all" (default) prints the SQLite DDL for every bf table. With a bead ID instead, prints that bead's full JSON representation including its annotations.

Usage: bf schema [OPTIONS] [TARGET]

Arguments:
  [TARGET]
          Schema target
          
          [default: all]

Options:
      --format <FORMAT>
          Output format (text, json)
          
          [default: json]

  -w, --workspace <WORKSPACE>
          Workspace directory (defaults to current directory's .beads/)

      --no-auto-flush
          Disable the automatic SQLite→JSONL flush after mutating commands for this invocation. Overrides `sync.auto_flush: true` in config; a no-op when auto-flush is already off. See `crate::autoflush::enabled`

      --envelope
          Wrap --json output in a standard envelope: {version, kind, data, warning?}. This provides stable structure for programmatic consumers. See `bf robot-docs`

      --no-progress
          Disable progress indicators for this invocation. Useful for scripted environments or CI pipelines where visual progress output is unwanted

  -V, --version
          Print version information ("bf <version>") and exit 0.
          
          Declared by hand rather than via clap's built-in version flag (see `disable_version_flag` above) so the version is written to stdout by `run` instead of clap's own writer, and so it is accepted anywhere on the command line, including after a subcommand.

  -h, --help
          Print help (see a summary with '-h')

exec
/bin/bash -lc "printf '%s\\n' '-- bf migration backup layout --'
find /home/coding/bf-migration-backup-ex44 -maxdepth 4 -type f -printf '%p\\n' 2>/dev/null | sort | sed -n '1,180p'
printf '%s\\n' '-- hidden store layouts --'
for directory in /home/coding/.beads /home/coding/.needle /home/coding/.needle-backups /home/coding/.needle-test-errors-406244 /home/coding/.needle-test-errors-repro-450162 /home/coding/.needle-test-otlp-1786814214; do
  if test -d \""'$directory"; then
    printf '"'%s\\n' \"--- "'$directory"
    find "$directory" -maxdepth 4 -type f '"\\( -name 'config.yaml' -o -name 'issues.jsonl' -o -name 'config.json' \\) -print 2>/dev/null | sort | sed -n '1,100p'
  fi
done
printf '%s\\n' '-- bf status parser / docs --'
rg -n -i -C 3 'status.*(open|blocked|closed|completed|pending|ready)|Status.*enum|enum.*Status' /home/coding/backups/bf-tooling-removed-20260814 /home/coding/bf-migration-backup-ex44 2>/dev/null | sed -n '1,360p'" in /home/coding/NEEDLE
 succeeded in 0ms:
-- bf migration backup layout --
/home/coding/bf-migration-backup-ex44/retired-bf/.cargo_bin_bf
/home/coding/bf-migration-backup-ex44/retired-bf/.cargo_bin_br
/home/coding/bf-migration-backup-ex44/retired-bf/.local_bin_.bf-version
/home/coding/bf-migration-backup-ex44/retired-bf/.local_bin_bf
/home/coding/bf-migration-backup-ex44/retired-bf/.local_bin_bf-update.sh
/home/coding/bf-migration-backup-ex44/retired-bf/beads-flush-commit.bf-version.bak
/home/coding/bf-migration-backup-ex44/retired-bf/bf-update.service
/home/coding/bf-migration-backup-ex44/retired-bf/bf-update.service.retired
/home/coding/bf-migration-backup-ex44/retired-bf/bf-update.timer
/home/coding/bf-migration-backup-ex44/retired-bf/bf-update.timer.retired
/home/coding/bf-migration-backup-ex44/retired-bf/br-wrapper.sh.bin
/home/coding/bf-migration-backup-ex44/retired-bf/br.fsqlite.bin
-- hidden store layouts --
--- /home/coding/.beads
--- /home/coding/.needle
/home/coding/.needle/canary/.beads/config.json
/home/coding/.needle/config.yaml
/home/coding/.needle/home/coding/.beads/issues.jsonl
--- /home/coding/.needle-backups
--- /home/coding/.needle-test-errors-406244
--- /home/coding/.needle-test-errors-repro-450162
--- /home/coding/.needle-test-otlp-1786814214
/home/coding/.needle-test-otlp-1786814214/.beads/config.json
-- bf status parser / docs --

exec
/bin/bash -lc "printf '%s\\n' '-- legacy checkpoint candidate --'
ls -la /home/coding/.needle/home/coding/.beads
printf '%s\\n' '-- first legacy records --'
sed -n '1,5p' /home/coding/.needle/home/coding/.beads/issues.jsonl
printf '%s\\n' '-- archived bf list (read-only) --'
/home/coding/backups/bf-tooling-removed-20260814/bf list --workspace /home/coding/.needle/home/coding --json | sed -n '1,160p'" in /home/coding/NEEDLE
 succeeded in 267ms:
{"acceptance_criteria":"","assignee":"test-from-home-coding","close_reason":"","closed_by_session":"","compaction_level":0,"created_at":"2026-03-21T21:42:50.889344800Z","created_by":"coding","delete_reason":"","deleted_by":"","description":"## Task\nThis is a test bead to verify the worker can be launched with the home workspace.\n\n## Acceptance Criteria\n- Worker launches successfully\n- Worker can claim and process this bead\n- Strand waterfall (Pluck → Mend → Explore → Knot) executes correctly\n","design":"","id":"bd-fvw","issue_type":"task","notes":"","original_size":0,"original_type":"","owner":"","priority":2,"sender":"","source_repo":".","source_system":"","status":"in_progress","title":"E2E Test: Home workspace worker launch","updated_at":"2026-05-11T11:50:18.421403047Z"}

exec
/bin/bash -lc "printf '%s\\n' '-- real bead-forge show JSON (legacy store) --'
/home/coding/backups/bf-tooling-removed-20260814/bf show bd-fvw --workspace /home/coding/.needle/home/coding --json
printf '%s\\n' '-- bead-forge status constraint --'
/home/coding/backups/bf-tooling-removed-20260814/bf schema all --workspace /home/coding/.needle/home/coding --format json | rg -n -i -C 3 'status|open|in_progress|blocked|closed|completed|pending|ready' | sed -n '1,300p'
printf '%s\\n' '-- canary state sources --'
rg -n -C 8 'enum .*State|BOOTING|SELECTING|CLAIMING|DISPATCHING|DONE|state_transitions|StateMachine' src tests | sed -n '1,520p'
printf '%s\\n' '-- resolver status behavior --'
rg -n -C 8 'failure-count|blocked|deferred|close.*reason|Outcome::|AgentOutcome' src/resolve.rs src/strand src/bead_store | sed -n '1,520p'" in /home/coding/NEEDLE
 succeeded in 0ms:
-- real bead-forge show JSON (legacy store) --
[{"acceptance_criteria":"","assignee":"test-from-home-coding","close_reason":"","closed_by_session":"","compaction_level":0,"created_at":"2026-03-21T21:42:50.889344800Z","created_by":"coding","delete_reason":"","deleted_by":"","description":"## Task\nThis is a test bead to verify the worker can be launched with the home workspace.\n\n## Acceptance Criteria\n- Worker launches successfully\n- Worker can claim and process this bead\n- Strand waterfall (Pluck → Mend → Explore → Knot) executes correctly\n","design":"","events":[{"actor":"coding","comment":"Added label e2e-test","created_at":"2026-03-21T21:42:50.889491948Z","id":1,"issue_id":"bd-fvw","type":"label_added"},{"actor":"coding","comment":"Added label home-workspace","created_at":"2026-03-21T21:42:50.889500146Z","id":2,"issue_id":"bd-fvw","type":"label_added"},{"actor":"coding","comment":"Created issue: E2E Test: Home workspace worker launch","created_at":"2026-03-21T21:42:50.889501667Z","id":3,"issue_id":"bd-fvw","type":"created"},{"actor":"coding","created_at":"2026-03-21T21:45:01.421842163Z","id":4,"issue_id":"bd-fvw","new_value":"in_progress","old_value":"open","type":"status_changed"},{"actor":"coding","created_at":"2026-03-21T21:45:01.421844797Z","id":5,"issue_id":"bd-fvw","new_value":"alpha","type":"assignee_changed"},{"actor":"coding","created_at":"2026-03-21T21:45:01.924263063Z","id":6,"issue_id":"bd-fvw","new_value":"open","old_value":"in_progress","type":"status_changed"},{"actor":"coding","created_at":"2026-03-21T21:45:01.924265611Z","id":7,"issue_id":"bd-fvw","old_value":"alpha","type":"assignee_changed"},{"actor":"coding","created_at":"2026-03-21T21:47:59.959732166Z","id":8,"issue_id":"bd-fvw","new_value":"in_progress","old_value":"open","type":"status_changed"},{"actor":"coding","created_at":"2026-03-21T21:47:59.959733873Z","id":9,"issue_id":"bd-fvw","new_value":"alpha","type":"assignee_changed"},{"actor":"coding","created_at":"2026-03-21T21:48:04.496552569Z","id":10,"issue_id":"bd-fvw","new_value":"open","old_value":"in_progress","type":"status_changed"},{"actor":"coding","created_at":"2026-03-21T21:48:04.496553867Z","id":11,"issue_id":"bd-fvw","old_value":"alpha","type":"assignee_changed"},{"actor":"coding","comment":"Added label failure-count:1","created_at":"2026-03-21T21:48:33.761320960Z","id":12,"issue_id":"bd-fvw","type":"label_added"},{"actor":"coding","created_at":"2026-03-21T21:56:04.521089684Z","id":13,"issue_id":"bd-fvw","new_value":"in_progress","old_value":"open","type":"status_changed"},{"actor":"coding","created_at":"2026-03-21T21:56:04.521092253Z","id":14,"issue_id":"bd-fvw","new_value":"home-test","type":"assignee_changed"},{"actor":"coding","created_at":"2026-03-21T21:56:06.055118356Z","id":15,"issue_id":"bd-fvw","new_value":"open","old_value":"in_progress","type":"status_changed"},{"actor":"coding","created_at":"2026-03-21T21:56:06.055119469Z","id":16,"issue_id":"bd-fvw","old_value":"home-test","type":"assignee_changed"},{"actor":"test-from-home-coding","comment":"{\"worker_id\":\"test-from-home-coding\",\"model\":null,\"harness\":null,\"harness_version\":null}","created_at":"2026-05-11T11:50:18.421403047Z","id":17,"issue_id":"bd-fvw","new_value":"test-from-home-coding","type":"claimed"}],"id":"bd-fvw","issue_type":"task","notes":"","original_size":0,"original_type":"","owner":"","priority":2,"sender":"","source_repo":".","source_system":"","status":"in_progress","status_transitions":[{"actor":"coding","new_status":"in_progress","old_status":"open","timestamp":"2026-03-21T21:45:01.421842163Z"},{"actor":"coding","new_status":"open","old_status":"in_progress","timestamp":"2026-03-21T21:45:01.924263063Z"},{"actor":"coding","new_status":"in_progress","old_status":"open","timestamp":"2026-03-21T21:47:59.959732166Z"},{"actor":"coding","new_status":"open","old_status":"in_progress","timestamp":"2026-03-21T21:48:04.496552569Z"},{"actor":"coding","new_status":"in_progress","old_status":"open","timestamp":"2026-03-21T21:56:04.521089684Z"},{"actor":"coding","new_status":"open","old_status":"in_progress","timestamp":"2026-03-21T21:56:06.055118356Z"}],"title":"E2E Test: Home workspace worker launch","updated_at":"2026-05-11T11:50:18.421403047Z"}]
-- bead-forge status constraint --
1-{
2:  "schema": "\n-- Issues table\nCREATE TABLE IF NOT EXISTS issues (\n    id TEXT PRIMARY KEY,\n    content_hash TEXT,\n    title TEXT NOT NULL CHECK(length(title) <= 500),\n    description TEXT NOT NULL DEFAULT '',\n    design TEXT NOT NULL DEFAULT '',\n    acceptance_criteria TEXT NOT NULL DEFAULT '',\n    notes TEXT NOT NULL DEFAULT '',\n    status TEXT NOT NULL DEFAULT 'open',\n    priority INTEGER NOT NULL DEFAULT 2 CHECK(priority >= 0 AND priority <= 4),\n    issue_type TEXT NOT NULL DEFAULT 'task',\n    assignee TEXT,\n    owner TEXT DEFAULT '',\n    estimated_minutes INTEGER,\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    created_by TEXT DEFAULT '',\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    closed_at DATETIME,\n    close_reason TEXT DEFAULT '',\n    closed_by_session TEXT DEFAULT '',\n    due_at DATETIME,\n    defer_until DATETIME,\n    external_ref TEXT,\n    source_system TEXT DEFAULT '',\n    source_repo TEXT NOT NULL DEFAULT '.',\n    deleted_at DATETIME,\n    deleted_by TEXT DEFAULT '',\n    delete_reason TEXT DEFAULT '',\n    original_type TEXT DEFAULT '',\n    compaction_level INTEGER DEFAULT 0,\n    compacted_at DATETIME,\n    compacted_at_commit TEXT,\n    original_size INTEGER,\n    sender TEXT DEFAULT '',\n    ephemeral INTEGER NOT NULL DEFAULT 0,\n    pinned INTEGER NOT NULL DEFAULT 0,\n    is_template INTEGER NOT NULL DEFAULT 0,\n    manual_status TEXT DEFAULT NULL,\n    CHECK (\n        (status = 'closed' AND closed_at IS NOT NULL) OR\n        (status = 'tombstone') OR\n        (status NOT IN ('closed', 'tombstone') AND closed_at IS NULL)\n    )\n);\n\n-- Issues indexes\nCREATE INDEX IF NOT EXISTS idx_issues_status ON issues(status);\nCREATE INDEX IF NOT EXISTS idx_issues_priority ON issues(priority);\nCREATE INDEX IF NOT EXISTS idx_issues_issue_type ON issues(issue_type);\nCREATE INDEX IF NOT EXISTS idx_issues_assignee ON issues(assignee) WHERE assignee IS NOT NULL;\nCREATE INDEX IF NOT EXISTS idx_issues_created_at ON issues(created_at);\nCREATE INDEX IF NOT EXISTS idx_issues_updated_at ON issues(updated_at);\nCREATE INDEX IF NOT EXISTS idx_issues_content_hash ON issues(content_hash);\nCREATE INDEX IF NOT EXISTS idx_issues_external_ref ON issues(external_ref) WHERE external_ref IS NOT NULL;\nCREATE UNIQUE INDEX IF NOT EXISTS idx_issues_external_ref_unique ON issues(external_ref) WHERE external_ref IS NOT NULL;\nCREATE INDEX IF NOT EXISTS idx_issues_ephemeral ON issues(ephemeral) WHERE ephemeral = 1;\nCREATE INDEX IF NOT EXISTS idx_issues_pinned ON issues(pinned) WHERE pinned = 1;\nCREATE INDEX IF NOT EXISTS idx_issues_tombstone ON issues(status) WHERE status = 'tombstone';\nCREATE INDEX IF NOT EXISTS idx_issues_due_at ON issues(due_at) WHERE due_at IS NOT NULL;\nCREATE INDEX IF NOT EXISTS idx_issues_defer_until ON issues(defer_until) WHERE defer_until IS NOT NULL;\nCREATE INDEX IF NOT EXISTS idx_issues_ready\n    ON issues(status, priority, created_at)\n    WHERE status = 'open'\n    AND ephemeral = 0\n    AND pinned = 0\n    AND is_template = 0;\nCREATE INDEX IF NOT EXISTS idx_issues_list_active_order\n    ON issues(priority, created_at DESC)\n    WHERE status NOT IN ('closed', 'tombstone')\n    AND (is_template = 0 OR is_template IS NULL);\n\n-- Dependencies table\nCREATE TABLE IF NOT EXISTS dependencies (\n    issue_id TEXT NOT NULL,\n    depends_on_id TEXT NOT NULL,\n    type TEXT NOT NULL DEFAULT 'blocks',\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    created_by TEXT NOT NULL DEFAULT '',\n    metadata TEXT DEFAULT '{}',\n    thread_id TEXT DEFAULT '',\n    PRIMARY KEY (issue_id, depends_on_id),\n    FOREIGN KEY (issue_id) REFERENCES issues(id) ON DELETE CASCADE\n);\n\n-- Dependencies indexes\nCREATE INDEX IF NOT EXISTS idx_dependencies_issue ON dependencies(issue_id);\nCREATE INDEX IF NOT EXISTS idx_dependencies_depends_on ON dependencies(depends_on_id);\nCREATE INDEX IF NOT EXISTS idx_dependencies_type ON dependencies(type);\nCREATE INDEX IF NOT EXISTS idx_dependencies_depends_on_type ON dependencies(depends_on_id, type);\nCREATE INDEX IF NOT EXISTS idx_dependencies_thread ON dependencies(thread_id) WHERE thread_id != '';\nCREATE INDEX IF NOT EXISTS idx_dependencies_blocking\n    ON dependencies(depends_on_id, issue_id)\n    WHERE (type = 'blocks' OR type = 'parent-child' OR type = 'conditional-blocks' OR type = 'waits-for');\n\n-- Comments table\nCREATE TABLE IF NOT EXISTS comments (\n    id INTEGER PRIMARY KEY AUTOINCREMENT,\n    issue_id TEXT NOT NULL,\n    author TEXT NOT NULL,\n    text TEXT NOT NULL,\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    FOREIGN KEY (issue_id) REFERENCES issues(id) ON DELETE CASCADE\n);\n\n-- Comments indexes\nCREATE INDEX IF NOT EXISTS idx_comments_issue ON comments(issue_id);\nCREATE INDEX IF NOT EXISTS idx_comments_created_at ON comments(created_at);\n\n-- Events table\nCREATE TABLE IF NOT EXISTS events (\n    id INTEGER PRIMARY KEY AUTOINCREMENT,\n    issue_id TEXT NOT NULL,\n    event_type TEXT NOT NULL,\n    actor TEXT NOT NULL DEFAULT '',\n    old_value TEXT,\n    new_value TEXT,\n    comment TEXT,\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    FOREIGN KEY (issue_id) REFERENCES issues(id) ON DELETE CASCADE\n);\n\n-- Events indexes\nCREATE INDEX IF NOT EXISTS idx_events_issue ON events(issue_id);\nCREATE INDEX IF NOT EXISTS idx_events_type ON events(event_type);\nCREATE INDEX IF NOT EXISTS idx_events_created_at ON events(created_at);\nCREATE INDEX IF NOT EXISTS idx_events_actor ON events(actor) WHERE actor != '';\n\n-- Labels table - direct issue-to-label mapping\nCREATE TABLE IF NOT EXISTS labels (\n    issue_id TEXT NOT NULL,\n    label TEXT NOT NULL,\n    PRIMARY KEY (issue_id, label),\n    FOREIGN KEY (issue_id) REFERENCES issues(id) ON DELETE CASCADE\n);\n\n-- Labels indexes\nCREATE INDEX IF NOT EXISTS idx_labels_label ON labels(label);\nCREATE INDEX IF NOT EXISTS idx_labels_issue ON labels(issue_id);\n\n-- Bead labels table - alternative label storage\nCREATE TABLE IF NOT EXISTS bead_labels (\n    bead_id TEXT NOT NULL REFERENCES issues(id) ON DELETE CASCADE,\n    label    TEXT NOT NULL,\n    PRIMARY KEY (bead_id, label)\n);\n\n-- Bead labels indexes\nCREATE INDEX IF NOT EXISTS idx_bead_labels_label ON bead_labels(label);\nCREATE INDEX IF NOT EXISTS idx_bead_labels_issue ON bead_labels(bead_id);\n\n-- Issue labels junction table\nCREATE TABLE IF NOT EXISTS issue_labels (\n    issue_id TEXT NOT NULL,\n    label_id TEXT NOT NULL,\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    PRIMARY KEY (issue_id, label_id),\n    FOREIGN KEY (issue_id) REFERENCES issues(id) ON DELETE CASCADE\n);\n\n-- Issue labels indexes\nCREATE INDEX IF NOT EXISTS idx_issue_labels_issue_id ON issue_labels(issue_id);\nCREATE INDEX IF NOT EXISTS idx_issue_labels_label_id ON issue_labels(label_id);\n\n-- Priorities reference table\nCREATE TABLE IF NOT EXISTS priorities (\n    id INTEGER PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    value INTEGER NOT NULL,\n    color TEXT,\n    description TEXT\n);\n\n-- Statuses reference table\nCREATE TABLE IF NOT EXISTS statuses (\n    id TEXT PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    category TEXT NOT NULL,\n    description TEXT,\n    is_terminal INTEGER NOT NULL DEFAULT 0\n);\n\n-- Issue types reference table\nCREATE TABLE IF NOT EXISTS issue_types (\n    id TEXT PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    description TEXT,\n    icon TEXT,\n    color TEXT\n);\n\n-- Issue relations table\nCREATE TABLE IF NOT EXISTS issue_relations (\n    issue_id TEXT NOT NULL,\n    related_id TEXT NOT NULL,\n    relation_type TEXT NOT NULL,\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    created_by TEXT DEFAULT '',\n    PRIMARY KEY (issue_id, related_id, relation_type),\n    FOREIGN KEY (issue_id) REFERENCES issues(id) ON DELETE CASCADE,\n    FOREIGN KEY (related_id) REFERENCES issues(id) ON DELETE CASCADE\n);\n\n-- Issue relations indexes\nCREATE INDEX IF NOT EXISTS idx_issue_relations_issue_id ON issue_relations(issue_id);\nCREATE INDEX IF NOT EXISTS idx_issue_relations_related_id ON issue_relations(related_id);\nCREATE INDEX IF NOT EXISTS idx_issue_relations_type ON issue_relations(relation_type);\n\n-- Assignees reference table\nCREATE TABLE IF NOT EXISTS assignees (\n    id TEXT PRIMARY KEY,\n    name TEXT NOT NULL UNIQUE,\n    email TEXT,\n    avatar_url TEXT,\n    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP\n);\n\n-- Issue assignees junction table\nCREATE TABLE IF NOT EXISTS issue_assignees (\n    issue_id TEXT NOT NULL,\n    assignee_id TEXT NOT NULL,\n    assigned_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    assigned_by TEXT DEFAULT '',\n    PRIMARY KEY (issue_id, assignee_id),\n    FOREIGN KEY (issue_id) REFERENCES issues(id) ON DELETE CASCADE,\n    FOREIGN KEY (assignee_id) REFERENCES assignees(id) ON DELETE CASCADE\n);\n\n-- Issue assignees indexes\nCREATE INDEX IF NOT EXISTS idx_issue_assignees_issue_id ON issue_assignees(issue_id);\nCREATE INDEX IF NOT EXISTS idx_issue_assignees_assignee_id ON issue_assignees(assignee_id);\nCREATE INDEX IF NOT EXISTS idx_issue_assignees_assigned_at ON issue_assignees(assigned_at);\n\n-- Bead annotations table (bf-only, never touched by br)\nCREATE TABLE IF NOT EXISTS bead_annotations (\n    bead_id TEXT NOT NULL REFERENCES issues(id) ON DELETE CASCADE,\n    key     TEXT NOT NULL,\n    value   TEXT NOT NULL,\n    PRIMARY KEY (bead_id, key)\n);\n\n-- Bead annotations indexes\nCREATE INDEX IF NOT EXISTS idx_bead_annotations_key_value\n    ON bead_annotations (key, value);\nCREATE INDEX IF NOT EXISTS idx_bead_annotations_bead_id\n    ON bead_annotations (bead_id);\n\n-- Dirty issues table (tracks beads that need flushing to JSONL)\nCREATE TABLE IF NOT EXISTS dirty_issues (\n    bead_id TEXT NOT NULL PRIMARY KEY,\n    marked_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    FOREIGN KEY (bead_id) REFERENCES issues(id) ON DELETE CASCADE\n);\n\n-- Worker sessions table (claim.rs/velocity.rs record every claim/close here for\n-- velocity stats). Was missing from SCHEMA_SQL entirely — every write path\n-- assumed it existed, but nothing ever created it for a fresh `bf init`, so a\n-- brand-new database crashed on the first claim: \"no such table: worker_sessions\".\nCREATE TABLE IF NOT EXISTS worker_sessions (\n    worker_id TEXT NOT NULL,\n    model TEXT,\n    harness TEXT,\n    harness_version TEXT,\n    claimed_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    bead_id TEXT REFERENCES issues(id) ON DELETE SET NULL,\n    workspace_path TEXT NOT NULL,\n    closed_at DATETIME,\n    duration_seconds INTEGER,\n    PRIMARY KEY (worker_id, claimed_at)\n);\n\n-- Export hashes table (sync.rs tracks per-issue content hash on every JSONL\n-- export, to skip re-exporting unchanged issues). Also missing from SCHEMA_SQL\n-- entirely — a fresh `bf init` degraded auto-flush to a silent warning\n-- (\"no such table: export_hashes\") on every create/claim/close instead of\n-- crashing outright, so it went unnoticed longer than worker_sessions did.\nCREATE TABLE IF NOT EXISTS export_hashes (\n    issue_id TEXT PRIMARY KEY,\n    content_hash TEXT NOT NULL,\n    exported_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    FOREIGN KEY (issue_id) REFERENCES issues(id) ON DELETE CASCADE\n);\n\n-- Velocity stats table (velocity.rs upserts p50/p90/avg duration per\n-- model+harness+issue_type, used to score claim priority by expected_seconds).\n-- Same missing-from-SCHEMA_SQL bug as worker_sessions and export_hashes.\nCREATE TABLE IF NOT EXISTS velocity_stats (\n    model TEXT NOT NULL,\n    harness TEXT NOT NULL,\n    issue_type TEXT NOT NULL,\n    sample_count INTEGER DEFAULT 0,\n    p50_seconds INTEGER,\n    p90_seconds INTEGER,\n    avg_seconds REAL,\n    last_updated DATETIME,\n    PRIMARY KEY (model, harness, issue_type)\n);\n\n-- Migration lock table (claim.rs checks this before claiming, migrate.rs\n-- acquires/releases it — a single-row advisory lock guarding concurrent\n-- migrations). Same missing-from-SCHEMA_SQL bug — this one is on the claim\n-- hot path, so a database missing it fails \"no such table: migration_lock\"\n-- on the very first claim, same failure class as worker_sessions.\nCREATE TABLE IF NOT EXISTS migration_lock (\n    id INTEGER PRIMARY KEY CHECK (id = 1),\n    locked_by TEXT NOT NULL,\n    locked_at DATETIME NOT NULL,\n    expires_at DATETIME NOT NULL\n);\n\n-- Critical path cache table (stores computed CPM float values for beads)\nCREATE TABLE IF NOT EXISTS critical_path_cache (\n    bead_id TEXT NOT NULL PRIMARY KEY,\n    epic_id TEXT,\n    es INTEGER NOT NULL DEFAULT 0,\n    ls INTEGER NOT NULL DEFAULT 0,\n    float INTEGER NOT NULL DEFAULT 0,\n    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    FOREIGN KEY (bead_id) REFERENCES issues(id) ON DELETE CASCADE,\n    FOREIGN KEY (epic_id) REFERENCES issues(id) ON DELETE CASCADE\n);\n\n-- Critical path cache indexes\nCREATE INDEX IF NOT EXISTS idx_critical_path_cache_epic_id\n    ON critical_path_cache (epic_id);\nCREATE INDEX IF NOT EXISTS idx_critical_path_cache_float\n    ON critical_path_cache (float);\n\n-- Blocked issues cache table (materialized view of blocked beads for Phase 7.8)\nCREATE TABLE IF NOT EXISTS blocked_issues_cache (\n    issue_id TEXT NOT NULL PRIMARY KEY,\n    blocked_by INTEGER NOT NULL DEFAULT 0,\n    blocked_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    FOREIGN KEY (issue_id) REFERENCES issues(id) ON DELETE CASCADE\n);\n\n-- Blocked issues cache indexes\nCREATE INDEX IF NOT EXISTS idx_blocked_issues_cache_blocked_at\n    ON blocked_issues_cache (blocked_at);\n"
3-}
-- canary state sources --
tests/integration_tests.rs-2213-    );
tests/integration_tests.rs-2214-}
tests/integration_tests.rs-2215-
tests/integration_tests.rs-2216-// ═════════════════════════════════════════════════════════════════════════════
tests/integration_tests.rs-2217-// Test 8: Full pipeline telemetry sequence
tests/integration_tests.rs-2218-// ═════════════════════════════════════════════════════════════════════════════
tests/integration_tests.rs-2219-
tests/integration_tests.rs-2220-#[tokio::test]
tests/integration_tests.rs:2221:async fn full_cycle_produces_telemetry_state_transitions() {
tests/integration_tests.rs-2222-    // Verify the expected state transition sequence occurs.
tests/integration_tests.rs-2223-    let bead = make_bead("needle-telem", 1);
tests/integration_tests.rs-2224-    let store: Arc<dyn BeadStore> = Arc::new(IntegrationMockStore::new(vec![bead]));
tests/integration_tests.rs-2225-
tests/integration_tests.rs-2226-    let (mut worker, _home_dir) = make_worker_with_adapter(store, "echo-test", "echo done", 10);
tests/integration_tests.rs-2227-
tests/integration_tests.rs-2228-    let result = worker.run().await.unwrap();
tests/integration_tests.rs-2229-
--
tests/fixtures/test-echo.yaml-1-# Minimal test agent adapter for E2E testing (no real LLM needed)
tests/fixtures/test-echo.yaml-2-#
tests/fixtures/test-echo.yaml-3-# This adapter simulates a successful agent invocation by:
tests/fixtures/test-echo.yaml:4:# 1. Writing "done" to a DONE file in the workspace
tests/fixtures/test-echo.yaml-5-# 2. Closing the bead via br CLI
tests/fixtures/test-echo.yaml-6-#
tests/fixtures/test-echo.yaml-7-# Usage: needle run --agent test-echo
tests/fixtures/test-echo.yaml-8-#
tests/fixtures/test-echo.yaml-9-# Template variables available:
tests/fixtures/test-echo.yaml-10-#   {workspace}   - Path to the workspace directory
tests/fixtures/test-echo.yaml-11-#   {prompt_file} - Path to the temp file containing the rendered prompt
tests/fixtures/test-echo.yaml-12-#   {bead_id}     - The bead identifier (e.g., needle-abc123)
--
tests/fixtures/test-echo.yaml-19-#     universal NEEDLE event format.  The binary must be on PATH; `needle
tests/fixtures/test-echo.yaml-20-#     test-agent <name>` reports an error if it cannot be found.
tests/fixtures/test-echo.yaml-21-#     Example:
tests/fixtures/test-echo.yaml-22-#       output_transform: "needle-transform-claude"
tests/fixtures/test-echo.yaml-23-
tests/fixtures/test-echo.yaml-24-name: test-echo
tests/fixtures/test-echo.yaml-25-agent_cli: bash
tests/fixtures/test-echo.yaml-26-invoke_template: |
tests/fixtures/test-echo.yaml:27:  cd {workspace} && echo "done" > DONE && bf close {bead_id} --reason "E2E test completed"
tests/fixtures/test-echo.yaml-28-timeout_secs: 30
tests/fixtures/test-echo.yaml-29-input_method:
tests/fixtures/test-echo.yaml-30-  method: stdin
--
tests/workspace_fixtures.rs-23-    /// Workspace path
tests/workspace_fixtures.rs-24-    pub path: PathBuf,
tests/workspace_fixtures.rs-25-    /// Workspace state (dead or alive)
tests/workspace_fixtures.rs-26-    pub state: WorkspaceState,
tests/workspace_fixtures.rs-27-}
tests/workspace_fixtures.rs-28-
tests/workspace_fixtures.rs-29-/// State of a mock workspace.
tests/workspace_fixtures.rs-30-#[derive(Debug, Clone, PartialEq, Eq)]
tests/workspace_fixtures.rs:31:pub enum WorkspaceState {
tests/workspace_fixtures.rs-32-    /// Workspace has no claimable candidates (all excluded or assigned)
tests/workspace_fixtures.rs-33-    Dead,
tests/workspace_fixtures.rs-34-    /// Workspace has valid claimable candidates
tests/workspace_fixtures.rs-35-    Alive,
tests/workspace_fixtures.rs-36-}
tests/workspace_fixtures.rs-37-
tests/workspace_fixtures.rs-38-/// Represents the state of a candidate bead.
tests/workspace_fixtures.rs-39-#[derive(Debug, Clone, PartialEq, Eq)]
tests/workspace_fixtures.rs:40:pub enum CandidateState {
tests/workspace_fixtures.rs-41-    /// Candidate is excluded by labels (deferred, human, blocked)
tests/workspace_fixtures.rs-42-    Excluded { labels: Vec<String> },
tests/workspace_fixtures.rs-43-    /// Candidate is assigned to another worker
tests/workspace_fixtures.rs-44-    Assigned { assignee: String },
tests/workspace_fixtures.rs-45-    /// Candidate is available to be claimed
tests/workspace_fixtures.rs-46-    Claimable,
tests/workspace_fixtures.rs-47-}
tests/workspace_fixtures.rs-48-
--
src/cli/mod.rs-528-    }
src/cli/mod.rs-529-
src/cli/mod.rs-530-    if let Some(hr) = hot_reload {
src/cli/mod.rs-531-        config.self_modification.hot_reload = hr;
src/cli/mod.rs-532-    }
src/cli/mod.rs-533-
src/cli/mod.rs-534-    if resume {
src/cli/mod.rs-535-        // Hot-reload resume: inherit worker identity from --identifier,
src/cli/mod.rs:536:        // load state from heartbeat file + registry, continue from SELECTING.
src/cli/mod.rs-537-        let worker_id = identifier
src/cli/mod.rs-538-            .clone()
src/cli/mod.rs-539-            .unwrap_or_else(|| NATO_ALPHABET[0].to_string());
src/cli/mod.rs-540-
src/cli/mod.rs-541-        // Load resume state from heartbeat and registry.
src/cli/mod.rs-542-        let resume_state = crate::upgrade::ResumeState::load(&config, &worker_id)?;
src/cli/mod.rs-543-
src/cli/mod.rs-544-        // Emit upgrade.completed telemetry.
--
tests/e2e/single_worker.sh-79-    echo "FATAL: br init failed"
tests/e2e/single_worker.sh-80-    exit 1
tests/e2e/single_worker.sh-81-}
tests/e2e/single_worker.sh-82-echo "  Workspace: $WORKSPACE"
tests/e2e/single_worker.sh-83-
tests/e2e/single_worker.sh-84-# ── Step 2: Create test bead ──────────────────────────────────────────────────
tests/e2e/single_worker.sh-85-
tests/e2e/single_worker.sh-86-echo "Step 2: Creating test bead..."
tests/e2e/single_worker.sh:87:BEAD_ID="$(cd "$WORKSPACE" && "$BR_BIN" create --title "Create a file called DONE" \
tests/e2e/single_worker.sh:88:    --description "Create a file called DONE in the workspace root" \
tests/e2e/single_worker.sh-89-    --silent 2>/dev/null)" || {
tests/e2e/single_worker.sh-90-    # Retry once after sync (FrankenSQLite WAL race).
tests/e2e/single_worker.sh-91-    (cd "$WORKSPACE" && "$BR_BIN" sync --flush-only 2>/dev/null) || true
tests/e2e/single_worker.sh:92:    BEAD_ID="$(cd "$WORKSPACE" && "$BR_BIN" create --title "Create a file called DONE" \
tests/e2e/single_worker.sh:93:        --description "Create a file called DONE in the workspace root" \
tests/e2e/single_worker.sh-94-        --silent)"
tests/e2e/single_worker.sh-95-}
tests/e2e/single_worker.sh-96-echo "  Bead: $BEAD_ID"
tests/e2e/single_worker.sh-97-
tests/e2e/single_worker.sh-98-# ── Step 3: Create test-echo agent adapter ────────────────────────────────────
tests/e2e/single_worker.sh-99-
tests/e2e/single_worker.sh-100-echo "Step 3: Creating test-echo adapter..."
tests/e2e/single_worker.sh-101-ADAPTERS_DIR="$HOME/.config/needle/adapters"
tests/e2e/single_worker.sh-102-mkdir -p "$ADAPTERS_DIR"
tests/e2e/single_worker.sh-103-
tests/e2e/single_worker.sh-104-cat > "$ADAPTERS_DIR/test-echo.yaml" <<YAML
tests/e2e/single_worker.sh-105-name: test-echo
tests/e2e/single_worker.sh-106-agent_cli: bash
tests/e2e/single_worker.sh:107:invoke_template: "cd {workspace} && echo done > DONE && $BR_BIN close {bead_id} --reason 'E2E test completed'"
tests/e2e/single_worker.sh-108-timeout_secs: 30
tests/e2e/single_worker.sh-109-YAML
tests/e2e/single_worker.sh-110-
tests/e2e/single_worker.sh-111-# ── Step 4: Configure needle ─────────────────────────────────────────────────
tests/e2e/single_worker.sh-112-
tests/e2e/single_worker.sh-113-echo "Step 4: Configuring needle..."
tests/e2e/single_worker.sh-114-CONFIG_DIR="$HOME/.config/needle"
tests/e2e/single_worker.sh-115-mkdir -p "$CONFIG_DIR"
--
tests/e2e/single_worker.sh-159-# 6b. Bead is closed
tests/e2e/single_worker.sh-160-BEAD_STATUS="$(cd "$WORKSPACE" && "$BR_BIN" show "$BEAD_ID" 2>/dev/null | head -1 || echo "ERROR")"
tests/e2e/single_worker.sh-161-if echo "$BEAD_STATUS" | grep -qi "closed\|CLOSED\|✓"; then
tests/e2e/single_worker.sh-162-    pass "Bead $BEAD_ID is closed"
tests/e2e/single_worker.sh-163-else
tests/e2e/single_worker.sh-164-    fail "Bead $BEAD_ID not closed: $BEAD_STATUS"
tests/e2e/single_worker.sh-165-fi
tests/e2e/single_worker.sh-166-
tests/e2e/single_worker.sh:167:# 6c. DONE file exists
tests/e2e/single_worker.sh:168:if [ -f "$WORKSPACE/DONE" ]; then
tests/e2e/single_worker.sh:169:    pass "DONE file exists"
tests/e2e/single_worker.sh-170-else
tests/e2e/single_worker.sh:171:    fail "DONE file does not exist in $WORKSPACE"
tests/e2e/single_worker.sh-172-fi
tests/e2e/single_worker.sh-173-
tests/e2e/single_worker.sh-174-# 6d. Telemetry events
tests/e2e/single_worker.sh-175-TELEMETRY_LOG="$(find "$TELEMETRY_DIR" -name "*.jsonl" 2>/dev/null | head -1 || echo "")"
tests/e2e/single_worker.sh-176-if [ -z "$TELEMETRY_LOG" ]; then
tests/e2e/single_worker.sh-177-    fail "No telemetry log found in $TELEMETRY_DIR"
tests/e2e/single_worker.sh-178-else
tests/e2e/single_worker.sh-179-    info "Telemetry log: $TELEMETRY_LOG"
--
tests/e2e/telemetry_completeness.sh-113-(cd "$WORKSPACE" && "$BR_BIN" init 2>&1) || {
tests/e2e/telemetry_completeness.sh-114-    echo "FATAL: br init failed"
tests/e2e/telemetry_completeness.sh-115-    exit 1
tests/e2e/telemetry_completeness.sh-116-}
tests/e2e/telemetry_completeness.sh-117-
tests/e2e/telemetry_completeness.sh-118-# ── Step 2: Create test bead ────────────────────────────────────────────────
tests/e2e/telemetry_completeness.sh-119-
tests/e2e/telemetry_completeness.sh-120-echo "Step 2: Creating test bead..."
tests/e2e/telemetry_completeness.sh:121:BEAD_ID="$(cd "$WORKSPACE" && "$BR_BIN" create --title "Create a file called DONE" \
tests/e2e/telemetry_completeness.sh:122:    --description "Create a file called DONE in the workspace root" \
tests/e2e/telemetry_completeness.sh-123-    --silent 2>/dev/null)" || {
tests/e2e/telemetry_completeness.sh-124-    (cd "$WORKSPACE" && "$BR_BIN" sync --flush-only 2>/dev/null) || true
tests/e2e/telemetry_completeness.sh:125:    BEAD_ID="$(cd "$WORKSPACE" && "$BR_BIN" create --title "Create a file called DONE" \
tests/e2e/telemetry_completeness.sh:126:        --description "Create a file called DONE in the workspace root" \
tests/e2e/telemetry_completeness.sh-127-        --silent)"
tests/e2e/telemetry_completeness.sh-128-}
tests/e2e/telemetry_completeness.sh-129-echo "  Bead: $BEAD_ID"
tests/e2e/telemetry_completeness.sh-130-
tests/e2e/telemetry_completeness.sh-131-# ── Step 3: Create test-echo agent adapter ──────────────────────────────────
tests/e2e/telemetry_completeness.sh-132-
tests/e2e/telemetry_completeness.sh-133-echo "Step 3: Creating test-echo adapter..."
tests/e2e/telemetry_completeness.sh-134-ADAPTERS_DIR="$HOME/.config/needle/adapters"
tests/e2e/telemetry_completeness.sh-135-mkdir -p "$ADAPTERS_DIR"
tests/e2e/telemetry_completeness.sh-136-
tests/e2e/telemetry_completeness.sh-137-cat > "$ADAPTERS_DIR/test-echo.yaml" <<YAML
tests/e2e/telemetry_completeness.sh-138-name: test-echo
tests/e2e/telemetry_completeness.sh-139-agent_cli: bash
tests/e2e/telemetry_completeness.sh:140:invoke_template: "cd {workspace} && echo done > DONE && $BR_BIN close {bead_id} --reason 'E2E test completed'"
tests/e2e/telemetry_completeness.sh-141-timeout_secs: 30
tests/e2e/telemetry_completeness.sh-142-YAML
tests/e2e/telemetry_completeness.sh-143-
tests/e2e/telemetry_completeness.sh-144-# ── Step 4: Configure needle ────────────────────────────────────────────────
tests/e2e/telemetry_completeness.sh-145-
tests/e2e/telemetry_completeness.sh-146-echo "Step 4: Configuring needle..."
tests/e2e/telemetry_completeness.sh-147-CONFIG_DIR="$HOME/.config/needle"
tests/e2e/telemetry_completeness.sh-148-mkdir -p "$CONFIG_DIR"
--
tests/e2e/telemetry_completeness.sh-257-# ── Step 9: Check state transitions ─────────────────────────────────────────
tests/e2e/telemetry_completeness.sh-258-
tests/e2e/telemetry_completeness.sh-259-echo "Step 9: Checking state transition sequence..."
tests/e2e/telemetry_completeness.sh-260-
tests/e2e/telemetry_completeness.sh-261-# Extract from/to pairs from state transition events (states are uppercase in JSONL).
tests/e2e/telemetry_completeness.sh-262-TRANSITIONS=$(jq -c 'select(.event_type == "worker.state_transition") | "\(.data.from) -> \(.data.to)"' "$TELEMETRY_LOG" | tr -d '"')
tests/e2e/telemetry_completeness.sh-263-
tests/e2e/telemetry_completeness.sh-264-EXPECTED_TRANSITIONS=(
tests/e2e/telemetry_completeness.sh:265:    "BOOTING -> SELECTING"
tests/e2e/telemetry_completeness.sh:266:    "SELECTING -> CLAIMING"
tests/e2e/telemetry_completeness.sh:267:    "CLAIMING -> BUILDING"
tests/e2e/telemetry_completeness.sh:268:    "BUILDING -> DISPATCHING"
tests/e2e/telemetry_completeness.sh:269:    "DISPATCHING -> EXECUTING"
tests/e2e/telemetry_completeness.sh-270-    "EXECUTING -> HANDLING"
tests/e2e/telemetry_completeness.sh-271-    "HANDLING -> LOGGING"
tests/e2e/telemetry_completeness.sh:272:    "LOGGING -> SELECTING"
tests/e2e/telemetry_completeness.sh:273:    "SELECTING -> EXHAUSTED"
tests/e2e/telemetry_completeness.sh-274-)
tests/e2e/telemetry_completeness.sh-275-
tests/e2e/telemetry_completeness.sh-276-for expected in "${EXPECTED_TRANSITIONS[@]}"; do
tests/e2e/telemetry_completeness.sh-277-    if echo "$TRANSITIONS" | grep -qF "$expected"; then
tests/e2e/telemetry_completeness.sh-278-        pass "State transition: $expected"
tests/e2e/telemetry_completeness.sh-279-    else
tests/e2e/telemetry_completeness.sh-280-        fail "Missing state transition: $expected"
tests/e2e/telemetry_completeness.sh-281-    fi
--
src/worker/mod.rs-510-    state: WorkerState,
src/worker/mod.rs-511-    current_bead: Option<Bead>,
src/worker/mod.rs-512-    exclusion_set: HashSet<BeadId>,
src/worker/mod.rs-513-    /// Race-lost exclusions with TTL — prevents re-selecting beads that just lost a claim race.
src/worker/mod.rs-514-    /// Each entry is (bead_id, expiration_time). Entries are pruned on access.
src/worker/mod.rs-515-    race_lost_exclusions: Vec<(BeadId, Instant)>,
src/worker/mod.rs-516-    /// Beads that lost a claim race in the current selection cycle.
src/worker/mod.rs-517-    /// These are added to exclusion_set to prevent immediate re-selection.
src/worker/mod.rs:518:    /// Cleared at the start of the next SELECTING cycle.
src/worker/mod.rs-519-    race_lost_this_cycle: HashSet<BeadId>,
src/worker/mod.rs-520-    retry_count: u32,
src/worker/mod.rs-521-    consecutive_race_lost: u32,
src/worker/mod.rs-522-    beads_processed: u64,
src/worker/mod.rs-523-    shutdown: Arc<AtomicBool>,
src/worker/mod.rs-524-    last_error: Option<anyhow::Error>,
src/worker/mod.rs-525-    boot_time: Option<Instant>,
src/worker/mod.rs-526-
--
src/worker/mod.rs-1590-                bail!(
src/worker/mod.rs-1591-                    "single-claim invariant blocked a second claim attempt; timed out releasing existing claim {}",
src/worker/mod.rs-1592-                    bead.id
src/worker/mod.rs-1593-                );
src/worker/mod.rs-1594-            }
src/worker/mod.rs-1595-        }
src/worker/mod.rs-1596-    }
src/worker/mod.rs-1597-
src/worker/mod.rs:1598:    /// SELECTING: run strand waterfall to find a candidate bead.
src/worker/mod.rs-1599-    async fn do_select(&mut self) -> Result<()> {
src/worker/mod.rs-1600-        // Claim ownership must be checked before clearing per-cycle state. A
src/worker/mod.rs-1601-        // leaked claim here would otherwise be forgotten and claim_auto could
src/worker/mod.rs-1602-        // assign this same worker a second bead.
src/worker/mod.rs-1603-        self.ensure_claim_slot_available().await?;
src/worker/mod.rs-1604-
src/worker/mod.rs-1605-        // Clear per-cycle state.
src/worker/mod.rs-1606-        // Preserve race-lost exclusions with TTL and beads that lost a race in the current cycle.
--
src/worker/mod.rs-1727-                    serde_json::json!({}),
src/worker/mod.rs-1728-                );
src/worker/mod.rs-1729-                self.health.update_strand(Some(strand_name.as_str()));
src/worker/mod.rs-1730-                self.current_bead = Some(bead);
src/worker/mod.rs-1731-                self.current_strand = Some(strand_name);
src/worker/mod.rs-1732-
src/worker/mod.rs-1733-                // Update heartbeat immediately with the bead's workspace so that
src/worker/mod.rs-1734-                // observers see the correct workspace even before transitioning to
src/worker/mod.rs:1735:                // CLAIMING. This ensures heartbeats are accurate for cross-workspace
src/worker/mod.rs-1736-                // work (see bead needle-c63c).
src/worker/mod.rs-1737-                self.health.update_state(
src/worker/mod.rs-1738-                    &WorkerState::Selecting,
src/worker/mod.rs-1739-                    Some(&self.current_bead.as_ref().unwrap().id),
src/worker/mod.rs-1740-                    Some(&bead_ws),
src/worker/mod.rs-1741-                );
src/worker/mod.rs-1742-
src/worker/mod.rs-1743-                self.set_state(WorkerState::Claiming)?;
--
src/worker/mod.rs-1948-                // Extract candidate count from telemetry data
src/worker/mod.rs-1949-                // This would need to be enhanced in production
src/worker/mod.rs-1950-            }
src/worker/mod.rs-1951-        }
src/worker/mod.rs-1952-
src/worker/mod.rs-1953-        (total_candidates, excluded_count)
src/worker/mod.rs-1954-    }
src/worker/mod.rs-1955-
src/worker/mod.rs:1956:    /// CLAIMING: attempt to claim the selected bead.
src/worker/mod.rs-1957-    async fn do_claim(&mut self) -> Result<()> {
src/worker/mod.rs-1958-        self.ensure_claim_slot_available().await?;
src/worker/mod.rs-1959-
src/worker/mod.rs-1960-        let bead_id = match self.current_bead {
src/worker/mod.rs-1961-            Some(ref b) => b.id.clone(),
src/worker/mod.rs-1962-            None => {
src/worker/mod.rs-1963-                self.set_state(WorkerState::Selecting)?;
src/worker/mod.rs-1964-                return Ok(());
--
src/worker/mod.rs-2418-
src/worker/mod.rs-2419-        // Store the prompt for the dispatch phase. We use a transient field pattern:
src/worker/mod.rs-2420-        // the prompt is passed via self.built_prompt.
src/worker/mod.rs-2421-        self.built_prompt = Some(prompt);
src/worker/mod.rs-2422-        self.set_state(WorkerState::Dispatching)?;
src/worker/mod.rs-2423-        Ok(())
src/worker/mod.rs-2424-    }
src/worker/mod.rs-2425-
src/worker/mod.rs:2426:    /// DISPATCHING: check rate limits, resolve adapter, and prepare for execution.
src/worker/mod.rs-2427-    async fn do_dispatch(&mut self) -> Result<()> {
src/worker/mod.rs-2428-        if self.current_bead.is_none() {
src/worker/mod.rs:2429:            bail!("DISPATCHING state without current_bead — invariant violated");
src/worker/mod.rs-2430-        }
src/worker/mod.rs-2431-
src/worker/mod.rs-2432-        // Check rate limits before dispatching.
src/worker/mod.rs-2433-        let adapter = self.resolve_adapter()?;
src/worker/mod.rs-2434-        let provider = adapter.provider.as_deref();
src/worker/mod.rs-2435-        let model = adapter.model.as_deref();
src/worker/mod.rs-2436-        self.health.update_adapter(Some(&adapter.name));
src/worker/mod.rs-2437-
--
src/worker/mod.rs-3580-            let _ = self.telemetry.emit(EventKind::CanaryRejected { reason });
src/worker/mod.rs-3581-        }
src/worker/mod.rs-3582-
src/worker/mod.rs-3583-        Ok(())
src/worker/mod.rs-3584-    }
src/worker/mod.rs-3585-
src/worker/mod.rs-3586-    /// Check for a new :stable binary and exit cleanly if detected.
src/worker/mod.rs-3587-    ///
src/worker/mod.rs:3588:    /// Called between LOGGING and SELECTING. If a new binary is found:
src/worker/mod.rs-3589-    /// 1. Emit `worker.binary_freshness_exit` telemetry
src/worker/mod.rs-3590-    /// 2. Exit with code 72 to signal supervisor to relaunch with new binary
src/worker/mod.rs-3591-    ///
src/worker/mod.rs-3592-    /// This ensures no bead is left mid-dispatch — the check only runs after
src/worker/mod.rs-3593-    /// a claim is closed and before the next claim is attempted.
src/worker/mod.rs-3594-    async fn check_hot_reload(&mut self) -> Result<()> {
src/worker/mod.rs-3595-        let needle_home = &self.config.workspace.home;
src/worker/mod.rs-3596-        match upgrade::check_hot_reload(needle_home) {
--
src/worker/mod.rs-4616-                            .await;
src/worker/mod.rs-4617-
src/worker/mod.rs-4618-                        return self.stop(&reason).await;
src/worker/mod.rs-4619-                    }
src/worker/mod.rs-4620-                }
src/worker/mod.rs-4621-
src/worker/mod.rs-4622-                // Final shutdown check after loop exits to handle the race where
src/worker/mod.rs-4623-                // a signal was received during the last sleep iteration. Without this
src/worker/mod.rs:4624:                // check, the worker would transition to SELECTING instead of stopping.
src/worker/mod.rs-4625-                if self.shutdown.load(Ordering::SeqCst) {
src/worker/mod.rs-4626-                    // Retrieve and clear the last received signal for logging.
src/worker/mod.rs-4627-                    #[cfg(unix)]
src/worker/mod.rs-4628-                    let signal_name = {
src/worker/mod.rs-4629-                        let sig = LAST_SIGNAL.swap(0, AtomicOrdering::SeqCst);
src/worker/mod.rs-4630-                        if sig == 0 {
src/worker/mod.rs-4631-                            None
src/worker/mod.rs-4632-                        } else {
--
src/worker/mod.rs-4696-                    )
src/worker/mod.rs-4697-                );
src/worker/mod.rs-4698-
src/worker/mod.rs-4699-                tracing::info!(
src/worker/mod.rs-4700-                    backoff_secs = backoff,
src/worker/mod.rs-4701-                    shutdown_checks_performed = shutdown_check_count,
src/worker/mod.rs-4702-                    elapsed_secs = elapsed,
src/worker/mod.rs-4703-                    woke_by_mtime_change = workspace_mtime_wake,
src/worker/mod.rs:4704:                    "idle sleep completed successfully, transitioning to SELECTING"
src/worker/mod.rs-4705-                );
src/worker/mod.rs-4706-
src/worker/mod.rs-4707-                // Note: Event-driven wakeup telemetry is already logged via tracing::info
src/worker/mod.rs-4708-                // at line 2797-2801 when workspace_mtime_wake is set to true.
src/worker/mod.rs-4709-                // The IdleSleepCompleted event above includes woke_by_mtime_change field.
src/worker/mod.rs-4710-
src/worker/mod.rs-4711-                // Force-flush BEFORE state transition to ensure the diagnostic event
src/worker/mod.rs-4712-                // is written even if the worker is killed during the transition.
--
src/worker/mod.rs-6599-            worker.current_bead.as_ref().map(|bead| &bead.id),
src/worker/mod.rs-6600-            Some(&bead_a.id)
src/worker/mod.rs-6601-        );
src/worker/mod.rs-6602-        assert_eq!(
src/worker/mod.rs-6603-            store.show(&bead_a.id).await.unwrap().status,
src/worker/mod.rs-6604-            BeadStatus::InProgress
src/worker/mod.rs-6605-        );
src/worker/mod.rs-6606-
src/worker/mod.rs:6607:        // Simulate the leaked transition that re-entered SELECTING without
src/worker/mod.rs-6608-        // applying a terminal BeadAction for bead A, then attempt to claim B.
src/worker/mod.rs-6609-        worker.set_state(WorkerState::Selecting).unwrap();
src/worker/mod.rs-6610-        let error = worker.do_select().await.unwrap_err();
src/worker/mod.rs-6611-
src/worker/mod.rs-6612-        assert!(
src/worker/mod.rs-6613-            error.to_string().contains("single-claim invariant"),
src/worker/mod.rs-6614-            "unexpected second-claim error: {error:#}"
src/worker/mod.rs-6615-        );
--
src/strand/pluck.rs-382-            ..Default::default()
src/strand/pluck.rs-383-        };
src/strand/pluck.rs-384-
src/strand/pluck.rs-385-        // 2. Filter: remove beads with excluded labels.
src/strand/pluck.rs-386-        //    Defensive guard — store.ready() passes exclude_labels in its Filters,
src/strand/pluck.rs-387-        //    but the backing CLI may not include label data in every query type.
src/strand/pluck.rs-388-        //    Filtering here guarantees excluded-label beads are never presented as
src/strand/pluck.rs-389-        //    candidates regardless of backend behaviour, preventing the
src/strand/pluck.rs:390:        //    SELECTING→CLAIMING→RETRYING spin loop observed when br ready --json
src/strand/pluck.rs-391-        //    omits label fields for some beads.
src/strand/pluck.rs-392-        let before_label_filter = candidates.len();
src/strand/pluck.rs-393-
src/strand/pluck.rs-394-        // First pass: collect excluded beads and their reasons for telemetry.
src/strand/pluck.rs-395-        let excluded_beads: Vec<_> = candidates
src/strand/pluck.rs-396-            .iter()
src/strand/pluck.rs-397-            .filter(|b| b.labels.iter().any(|l| self.exclude_labels.contains(l)))
src/strand/pluck.rs-398-            .map(|b| {
--
src/strand/pluck.rs-1230-            other => panic!("expected BeadFound, got: {other:?}"),
src/strand/pluck.rs-1231-        }
src/strand/pluck.rs-1232-    }
src/strand/pluck.rs-1233-
src/strand/pluck.rs-1234-    // These tests use UnfilteredStore, which returns all beads from ready()
src/strand/pluck.rs-1235-    // without applying label exclusion — simulating a backend (e.g. br ready
src/strand/pluck.rs-1236-    // --json) that omits label data.  They verify that the strand's own
src/strand/pluck.rs-1237-    // defensive retain catches excluded-label beads regardless of what the
src/strand/pluck.rs:1238:    // store returns, preventing the SELECTING→CLAIMING→RETRYING spin loop.
src/strand/pluck.rs-1239-
src/strand/pluck.rs-1240-    #[tokio::test]
src/strand/pluck.rs-1241-    async fn strand_filters_excluded_labels_when_store_does_not() {
src/strand/pluck.rs-1242-        let store = UnfilteredStore {
src/strand/pluck.rs-1243-            beads: vec![
src/strand/pluck.rs-1244-                make_bead_with_labels("deferred-bead", 1, vec!["deferred"]),
src/strand/pluck.rs-1245-                make_bead_with_labels("normal-bead", 1, vec![]),
src/strand/pluck.rs-1246-            ],
--
src/strand/weave.rs-541-#[async_trait::async_trait]
src/strand/weave.rs-542-impl super::Strand for WeaveStrand {
src/strand/weave.rs-543-    fn name(&self) -> &str {
src/strand/weave.rs-544-        "weave"
src/strand/weave.rs-545-    }
src/strand/weave.rs-546-
src/strand/weave.rs-547-    async fn evaluate(&self, store: &dyn BeadStore, _exclusions: &HashSet<BeadId>) -> StrandResult {
src/strand/weave.rs-548-        // Apply strand-level timeout to prevent a single weave from stalling
src/strand/weave.rs:549:        // the entire SELECTING cycle for minutes. See: needle-bf-5hlhn
src/strand/weave.rs-550-        let timeout_duration = std::time::Duration::from_secs(WEAVE_STRAND_TIMEOUT_SECS);
src/strand/weave.rs-551-
src/strand/weave.rs-552-        match tokio::time::timeout(timeout_duration, self.evaluate_internal(store)).await {
src/strand/weave.rs-553-            Ok(result) => result,
src/strand/weave.rs-554-            Err(_) => {
src/strand/weave.rs-555-                tracing::warn!(
src/strand/weave.rs-556-                    timeout_secs = WEAVE_STRAND_TIMEOUT_SECS,
src/strand/weave.rs:557:                    "weave strand evaluation timed out after {}s - returning NoWork to prevent SELECTING stall",
src/strand/weave.rs-558-                    WEAVE_STRAND_TIMEOUT_SECS
src/strand/weave.rs-559-                );
src/strand/weave.rs-560-                let _ = self.telemetry.emit(EventKind::StrandSkipped {
src/strand/weave.rs-561-                    strand_name: "weave".to_string(),
src/strand/weave.rs-562-                    reason: format!("timeout after {}s", WEAVE_STRAND_TIMEOUT_SECS),
src/strand/weave.rs-563-                });
src/strand/weave.rs-564-                StrandResult::NoWork
src/strand/weave.rs-565-            }
--
src/strand/weave.rs-571-
src/strand/weave.rs-572-/// Default timeout for weave agent calls (60 seconds).
src/strand/weave.rs-573-#[allow(dead_code)]
src/strand/weave.rs-574-const WEAVE_AGENT_TIMEOUT_SECS: u64 = 60;
src/strand/weave.rs-575-
src/strand/weave.rs-576-/// Maximum timeout for weave strand evaluation (120 seconds).
src/strand/weave.rs-577-///
src/strand/weave.rs-578-/// This prevents a single weave strand from stalling the entire
src/strand/weave.rs:579:/// SELECTING cycle for minutes. Without this timeout, issues like
src/strand/weave.rs-580-/// bead store problems or filesystem hangs can cause weave to take
src/strand/weave.rs-581-/// 237+ seconds while other strands fail in milliseconds.
src/strand/weave.rs-582-/// See: needle-bf-5hlhn (weave strand stall investigation).
src/strand/weave.rs-583-const WEAVE_STRAND_TIMEOUT_SECS: u64 = 120;
src/strand/weave.rs-584-
src/strand/weave.rs-585-/// Production `WeaveAgent` that shells out to a CLI agent (e.g., `claude`).
src/strand/weave.rs-586-///
src/strand/weave.rs-587-/// The agent is invoked in `--print` mode so it emits its analysis as plain
--
src/strand/splice.rs-435-        }
src/strand/splice.rs-436-
src/strand/splice.rs-437-        None
src/strand/splice.rs-438-    }
src/strand/splice.rs-439-
src/strand/splice.rs-440-    /// Detect state ping-pong: short state cycles without forward progress.
src/strand/splice.rs-441-    fn detect_state_ping_pong(&self, events: &[TelemetryEventLike]) -> Option<StatePingPongInfo> {
src/strand/splice.rs-442-        // Look for state_transition events and extract state sequences.
src/strand/splice.rs:443:        let mut state_transitions: Vec<(DateTime<Utc>, String, String)> = Vec::new();
src/strand/splice.rs-444-
src/strand/splice.rs-445-        for ev in events.iter().rev() {
src/strand/splice.rs-446-            if ev.event_type == "worker.state_transition" {
src/strand/splice.rs-447-                if let (Some(from), Some(to)) = (&ev.data.get("from"), &ev.data.get("to")) {
src/strand/splice.rs-448-                    if let (Some(from_str), Some(to_str)) = (from.as_str(), to.as_str()) {
src/strand/splice.rs:449:                        state_transitions.push((
src/strand/splice.rs-450-                            ev.timestamp,
src/strand/splice.rs-451-                            from_str.to_string(),
src/strand/splice.rs-452-                            to_str.to_string(),
src/strand/splice.rs-453-                        ));
src/strand/splice.rs-454-                    }
src/strand/splice.rs-455-                }
src/strand/splice.rs-456-            }
src/strand/splice.rs-457-        }
src/strand/splice.rs-458-
src/strand/splice.rs:459:        if state_transitions.len() < 8 {
src/strand/splice.rs-460-            return None; // Not enough transitions to detect a cycle.
src/strand/splice.rs-461-        }
src/strand/splice.rs-462-
src/strand/splice.rs-463-        // Check for cycles of length <= 4 states.
src/strand/splice.rs:464:        let window_size = 8.min(state_transitions.len());
src/strand/splice.rs:465:        let window: Vec<_> = state_transitions
src/strand/splice.rs-466-            .iter()
src/strand/splice.rs-467-            .take(window_size)
src/strand/splice.rs-468-            .map(|(_, _, to)| to.clone())
src/strand/splice.rs-469-            .collect();
src/strand/splice.rs-470-
src/strand/splice.rs-471-        // Count unique states in the window.
src/strand/splice.rs-472-        let unique_states: HashSet<_> = window.iter().collect();
src/strand/splice.rs-473-
src/strand/splice.rs-474-        if unique_states.len() <= 4 && window.len() >= 6 {
src/strand/splice.rs-475-            // Check if we're cycling without forward-progress events.
src/strand/splice.rs-476-            let has_forward_progress = events
src/strand/splice.rs-477-                .iter()
src/strand/splice.rs-478-                .any(|ev| matches!(ev.event_type.as_str(), "agent.completed" | "bead.completed"));
src/strand/splice.rs-479-
src/strand/splice.rs-480-            if !has_forward_progress {
src/strand/splice.rs:481:                let sample = state_transitions
src/strand/splice.rs-482-                    .iter()
src/strand/splice.rs-483-                    .take(10)
src/strand/splice.rs-484-                    .map(|(ts, from, to)| format!("{} {} -> {}", ts.format("%H:%M:%S"), from, to))
src/strand/splice.rs-485-                    .collect();
src/strand/splice.rs-486-
src/strand/splice.rs-487-                return Some(StatePingPongInfo {
src/strand/splice.rs-488-                    states: unique_states.iter().map(|s| (*s).clone()).collect(),
src/strand/splice.rs-489-                    cycle_count: (window.len() / unique_states.len()) as u32,
--
tests/e2e/lib/workspace.sh-91-#   title          - Optional bead title (default: "Remote task")
tests/e2e/lib/workspace.sh-92-#   description    - Optional bead description (default: "Remote workspace task")
tests/e2e/lib/workspace.sh-93-#
tests/e2e/lib/workspace.sh-94-# Environment:
tests/e2e/lib/workspace.sh-95-#   BR_BIN - path to br CLI (required)
tests/e2e/lib/workspace.sh-96-#
tests/e2e/lib/workspace.sh-97-# Example:
-- resolver status behavior --
rg: src/resolve.rs: No such file or directory (os error 2)
src/bead_store/backend.rs-325-        "create" => &[
src/bead_store/backend.rs-326-            "title",
src/bead_store/backend.rs-327-            "body",
src/bead_store/backend.rs-328-            "priority",
src/bead_store/backend.rs-329-            "assignee",
src/bead_store/backend.rs-330-            "issue_type",
src/bead_store/backend.rs-331-            "labels",
src/bead_store/backend.rs-332-        ],
src/bead_store/backend.rs:333:        "dep_add" | "dep_remove" => &["blocked", "blocker"],
src/bead_store/backend.rs-334-        "split" => &["parent", "children"],
src/bead_store/backend.rs:335:        "close" => &["id", "reason"],
src/bead_store/backend.rs-336-        "import" => &["input", "mode", "actor"],
src/bead_store/backend.rs-337-        "compare" => &["id", "profile"],
src/bead_store/backend.rs-338-        "query" => &["query"],
src/bead_store/backend.rs-339-        "changes" => &["since"],
src/bead_store/backend.rs-340-        "why" => &["id"],
src/bead_store/backend.rs-341-        "ref_add" => &["id", "namespace", "key", "value"],
src/bead_store/backend.rs-342-        "ref_remove" => &["id", "namespace", "key"],
src/bead_store/backend.rs-343-        "ref_list" => &["id"],
--
src/bead_store/backend.rs-538-        ),
src/bead_store/backend.rs-539-    );
src/bead_store/backend.rs-540-    operations.insert(
src/bead_store/backend.rs-541-        "release".into(),
src/bead_store/backend.rs-542-        operation(&["release", "{id}"], None, None),
src/bead_store/backend.rs-543-    );
src/bead_store/backend.rs-544-    operations.insert(
src/bead_store/backend.rs-545-        "block".into(),
src/bead_store/backend.rs:546:        operation(&["update", "{id}", "--status", "blocked"], None, None),
src/bead_store/backend.rs-547-    );
src/bead_store/backend.rs-548-    operations.insert(
src/bead_store/backend.rs-549-        "clear_assignee".into(),
src/bead_store/backend.rs-550-        operation(&["update", "{id}", "--clear-assignee"], None, None),
src/bead_store/backend.rs-551-    );
src/bead_store/backend.rs-552-    operations.insert(
src/bead_store/backend.rs-553-        "flush".into(),
src/bead_store/backend.rs-554-        operation(&["sync", "flush-only"], None, None),
--
src/bead_store/backend.rs-573-            &["create", "--title", "{title}", "--description", "{body}"],
src/bead_store/backend.rs-574-            None,
src/bead_store/backend.rs-575-            Some(ParseShape::BareId),
src/bead_store/backend.rs-576-        ),
src/bead_store/backend.rs-577-    );
src/bead_store/backend.rs-578-    operations.insert(
src/bead_store/backend.rs-579-        "dep_add".into(),
src/bead_store/backend.rs-580-        operation(
src/bead_store/backend.rs:581:            &["dep", "add", "{blocked}", "{blocker}", "--kind", "blocks"],
src/bead_store/backend.rs-582-            None,
src/bead_store/backend.rs-583-            None,
src/bead_store/backend.rs-584-        ),
src/bead_store/backend.rs-585-    );
src/bead_store/backend.rs-586-    operations.insert("split".into(), operation(&[], Some("sequential"), None));
src/bead_store/backend.rs-587-    operations.insert(
src/bead_store/backend.rs-588-        "dep_remove".into(),
src/bead_store/backend.rs:589:        operation(&["dep", "remove", "{blocked}", "{blocker}"], None, None),
src/bead_store/backend.rs-590-    );
src/bead_store/backend.rs-591-    operations.insert(
src/bead_store/backend.rs-592-        "close".into(),
src/bead_store/backend.rs:593:        operation(&["close", "{id}", "--reason", "{reason}"], None, None),
src/bead_store/backend.rs-594-    );
src/bead_store/backend.rs-595-    operations.insert("doctor_check".into(), operation(&["doctor"], None, None));
src/bead_store/backend.rs-596-    operations.insert(
src/bead_store/backend.rs-597-        "doctor_repair".into(),
src/bead_store/backend.rs-598-        operation(&["doctor", "--repair"], None, None),
src/bead_store/backend.rs-599-    );
src/bead_store/backend.rs-600-    operations.insert(
src/bead_store/backend.rs-601-        "import".into(),
--
src/bead_store/backend.rs-838-            Some(ParseShape::JsonObject),
src/bead_store/backend.rs-839-        ),
src/bead_store/backend.rs-840-    );
src/bead_store/backend.rs-841-    operations.insert("release".into(), operation(&[], None, None));
src/bead_store/backend.rs-842-    operations.insert("clear_assignee".into(), operation(&[], None, None));
src/bead_store/backend.rs-843-    operations.insert(
src/bead_store/backend.rs-844-        "dep_add".into(),
src/bead_store/backend.rs-845-        operation(
src/bead_store/backend.rs:846:            &["dep", "add", "{blocker}", "--blocks", "{blocked}"],
src/bead_store/backend.rs-847-            None,
src/bead_store/backend.rs-848-            None,
src/bead_store/backend.rs-849-        ),
src/bead_store/backend.rs-850-    );
src/bead_store/backend.rs-851-    operations.insert(
src/bead_store/backend.rs-852-        "split".into(),
src/bead_store/backend.rs-853-        operation(&[], Some("transactional_batch"), None),
src/bead_store/backend.rs-854-    );
src/bead_store/backend.rs-855-    operations.insert(
src/bead_store/backend.rs-856-        "dep_remove".into(),
src/bead_store/backend.rs:857:        operation(&["dep", "remove", "{blocked}", "{blocker}"], None, None),
src/bead_store/backend.rs-858-    );
src/bead_store/backend.rs-859-    operations.insert(
src/bead_store/backend.rs-860-        "import".into(),
src/bead_store/backend.rs-861-        operation(&["sync", "--import-only"], Some("bare"), None),
src/bead_store/backend.rs-862-    );
src/bead_store/backend.rs-863-    backend
src/bead_store/backend.rs-864-}
src/bead_store/backend.rs-865-
--
src/bead_store/backend.rs-986-                "title",
src/bead_store/backend.rs-987-                "body",
src/bead_store/backend.rs-988-                "priority",
src/bead_store/backend.rs-989-                "assignee",
src/bead_store/backend.rs-990-                "issue_type",
src/bead_store/backend.rs-991-                "labels"
src/bead_store/backend.rs-992-            ][..]
src/bead_store/backend.rs-993-        );
src/bead_store/backend.rs:994:        assert_eq!(allowed_placeholders("dep_add"), &["blocked", "blocker"][..]);
src/bead_store/backend.rs:995:        assert_eq!(allowed_placeholders("close"), &["id", "reason"][..]);
src/bead_store/backend.rs-996-    }
src/bead_store/backend.rs-997-
src/bead_store/backend.rs-998-    #[test]
src/bead_store/backend.rs-999-    fn validate_placeholders_accepts_all_allowed_placeholders() {
src/bead_store/backend.rs-1000-        let source = PathBuf::from("<test>");
src/bead_store/backend.rs-1001-        let operation = "show";
src/bead_store/backend.rs-1002-
src/bead_store/backend.rs-1003-        // Valid: {id} is allowed for "show"
--
src/bead_store/strategies.rs-404-                    return Ok(ClaimResult::RaceLost { claimed_by });
src/bead_store/strategies.rs-405-                }
src/bead_store/strategies.rs-406-
src/bead_store/strategies.rs-407-                let expected_version = bead.updated_at.to_rfc3339();
src/bead_store/strategies.rs-408-                match operations
src/bead_store/strategies.rs-409-                    .compare_and_set_claim(bead_id, actor, &expected_version)
src/bead_store/strategies.rs-410-                    .await?
src/bead_store/strategies.rs-411-                {
src/bead_store/strategies.rs:412:                    CompareAndSetOutcome::Claimed(bead) => {
src/bead_store/strategies.rs-413-                        return Ok(ClaimResult::Claimed(*bead));
src/bead_store/strategies.rs-414-                    }
src/bead_store/strategies.rs:415:                    CompareAndSetOutcome::VersionChanged => continue,
src/bead_store/strategies.rs:416:                    CompareAndSetOutcome::RaceLost { claimed_by } => {
src/bead_store/strategies.rs-417-                        return Ok(ClaimResult::RaceLost { claimed_by });
src/bead_store/strategies.rs-418-                    }
src/bead_store/strategies.rs:419:                    CompareAndSetOutcome::NotClaimable { reason } => {
src/bead_store/strategies.rs-420-                        return Ok(ClaimResult::NotClaimable { reason });
src/bead_store/strategies.rs-421-                    }
src/bead_store/strategies.rs-422-                }
src/bead_store/strategies.rs-423-            }
src/bead_store/strategies.rs-424-
src/bead_store/strategies.rs-425-            let latest = operations.show_for_claim(bead_id).await?;
src/bead_store/strategies.rs-426-            if let Some(claimed_by) = latest.assignee {
src/bead_store/strategies.rs-427-                Ok(ClaimResult::RaceLost { claimed_by })
--
src/bead_store/mod.rs-275-            .and_then(|value| value.as_bool())
src/bead_store/mod.rs-276-            != Some(true)
src/bead_store/mod.rs-277-    {
src/bead_store/mod.rs-278-        bail!(
src/bead_store/mod.rs-279-            "bead-rs capability mismatch for workspace {}: expected implementation=bead-rs and atomic_claim=true",
src/bead_store/mod.rs-280-            workspace.display()
src/bead_store/mod.rs-281-        );
src/bead_store/mod.rs-282-    }
src/bead_store/mod.rs:283:    for status in ["open", "in_progress", "deferred", "closed"] {
src/bead_store/mod.rs-284-        let present = capabilities["statuses"]
src/bead_store/mod.rs-285-            .as_array()
src/bead_store/mod.rs-286-            .is_some_and(|values| values.iter().any(|value| value == status));
src/bead_store/mod.rs-287-        if !present {
src/bead_store/mod.rs-288-            bail!(
src/bead_store/mod.rs-289-                "bead-rs capability mismatch for workspace {}: missing status {status}",
src/bead_store/mod.rs-290-                workspace.display()
src/bead_store/mod.rs-291-            );
--
src/bead_store/mod.rs-1176-    /// Returns a `ClaimResult` describing the outcome:
src/bead_store/mod.rs-1177-    /// - `Claimed(bead)` — success, returns the full bead.
src/bead_store/mod.rs-1178-    /// - `NotClaimable { reason }` — no beads available to claim.
src/bead_store/mod.rs-1179-    async fn claim_auto(&self, actor: &str) -> Result<ClaimResult>;
src/bead_store/mod.rs-1180-
src/bead_store/mod.rs-1181-    /// Release a claimed bead back to open (e.g., after agent failure).
src/bead_store/mod.rs-1182-    async fn release(&self, id: &BeadId) -> Result<()>;
src/bead_store/mod.rs-1183-
src/bead_store/mod.rs:1184:    /// Quarantine a bead by setting status=blocked (e.g., after it exceeds the
src/bead_store/mod.rs-1185-    /// consecutive-failure threshold in `OutcomeConfig::quarantine_after_failures`).
src/bead_store/mod.rs-1186-    ///
src/bead_store/mod.rs-1187-    /// Unlike `release`, this deliberately does NOT clear the assignee or return
src/bead_store/mod.rs-1188-    /// the bead to a claimable state — the whole point is to stop Pluck from
src/bead_store/mod.rs-1189-    /// re-selecting it until a human (or a future auto-split) intervenes.
src/bead_store/mod.rs-1190-    async fn block(&self, id: &BeadId) -> Result<()>;
src/bead_store/mod.rs-1191-
src/bead_store/mod.rs-1192-    /// Clear the assignee on a bead without changing its status.
--
src/bead_store/mod.rs-1214-    async fn add_label(&self, id: &BeadId, label: &str) -> Result<()>;
src/bead_store/mod.rs-1215-
src/bead_store/mod.rs-1216-    /// Remove a label from a bead.
src/bead_store/mod.rs-1217-    async fn remove_label(&self, id: &BeadId, label: &str) -> Result<()>;
src/bead_store/mod.rs-1218-
src/bead_store/mod.rs-1219-    /// Create a new bead and return its ID.
src/bead_store/mod.rs-1220-    async fn create_bead(&self, title: &str, body: &str, labels: &[&str]) -> Result<BeadId>;
src/bead_store/mod.rs-1221-
src/bead_store/mod.rs:1222:    /// Add a dependency link: `blocker_id` blocks `blocked_id`.
src/bead_store/mod.rs-1223-    ///
src/bead_store/mod.rs:1224:    /// Uses `br dep add <blocker_id> --blocks <blocked_id>`.
src/bead_store/mod.rs:1225:    async fn add_dependency(&self, blocker_id: &BeadId, blocked_id: &BeadId) -> Result<()>;
src/bead_store/mod.rs-1226-
src/bead_store/mod.rs-1227-    /// Atomically create `children` and link each as a blocker of `parent_id`.
src/bead_store/mod.rs-1228-    ///
src/bead_store/mod.rs-1229-    /// This is the "mitosis" / bead-splitting primitive. N child creations plus
src/bead_store/mod.rs-1230-    /// N dependency links must commit together: otherwise a crash between the
src/bead_store/mod.rs-1231-    /// `create` and the `dep add` (SIGKILL, OOM, pod eviction) leaves an orphaned
src/bead_store/mod.rs-1232-    /// child with no dependency link, and the parent never unblocks — plan.md
src/bead_store/mod.rs-1233-    /// Phase 5.3, Race 3.
src/bead_store/mod.rs-1234-    ///
src/bead_store/mod.rs-1235-    /// The parent is deliberately **not** closed: NEEDLE keeps a split parent
src/bead_store/mod.rs:1236:    /// open/blocked while its children are worked.
src/bead_store/mod.rs-1237-    ///
src/bead_store/mod.rs-1238-    /// The default implementation performs the historical non-atomic sequence
src/bead_store/mod.rs-1239-    /// (`create_bead` + `add_dependency`, one child at a time). Mock stores rely
src/bead_store/mod.rs-1240-    /// on it, and bf-backed stores fall back to it when the atomic path is
src/bead_store/mod.rs-1241-    /// unavailable. Backends that can run a transactional batch (bf) override
src/bead_store/mod.rs-1242-    /// this to make the whole split crash-safe.
src/bead_store/mod.rs-1243-    async fn split_bead(
src/bead_store/mod.rs-1244-        &self,
--
src/bead_store/mod.rs-1256-                .with_context(|| {
src/bead_store/mod.rs-1257-                    format!("failed to add dependency: {child_id} blocks {parent_id}")
src/bead_store/mod.rs-1258-                })?;
src/bead_store/mod.rs-1259-            created.push(child_id);
src/bead_store/mod.rs-1260-        }
src/bead_store/mod.rs-1261-        Ok(created)
src/bead_store/mod.rs-1262-    }
src/bead_store/mod.rs-1263-
src/bead_store/mod.rs:1264:    /// Remove a dependency link: `blocker_id` blocks `blocked_id`.
src/bead_store/mod.rs-1265-    ///
src/bead_store/mod.rs:1266:    /// Uses `br dep remove <blocked_id> <blocker_id>`.
src/bead_store/mod.rs:1267:    async fn remove_dependency(&self, blocked_id: &BeadId, blocker_id: &BeadId) -> Result<()>;
src/bead_store/mod.rs-1268-
src/bead_store/mod.rs-1269-    /// Run `br doctor --repair` and return the report.
src/bead_store/mod.rs-1270-    async fn doctor_repair(&self) -> Result<RepairReport>;
src/bead_store/mod.rs-1271-
src/bead_store/mod.rs-1272-    /// Run `br doctor` (without `--repair`) to check database health.
src/bead_store/mod.rs-1273-    ///
src/bead_store/mod.rs-1274-    /// Returns warnings if any issues are detected, without attempting to fix them.
src/bead_store/mod.rs-1275-    async fn doctor_check(&self) -> Result<RepairReport>;
--
src/bead_store/mod.rs-1304-    #[cfg(unix)]
src/bead_store/mod.rs-1305-    fn version_fixture(directory: &Path, name: &str, version: &str) -> PathBuf {
src/bead_store/mod.rs-1306-        use std::os::unix::fs::PermissionsExt;
src/bead_store/mod.rs-1307-
src/bead_store/mod.rs-1308-        let path = directory.join(name);
src/bead_store/mod.rs-1309-        std::fs::write(
src/bead_store/mod.rs-1310-            &path,
src/bead_store/mod.rs-1311-            format!(
src/bead_store/mod.rs:1312:                "#!/bin/sh\nif [ \"$1\" = capabilities ]; then\n  printf '%s\\n' '{{\"implementation\":\"bead-rs\",\"atomic_claim\":true,\"statuses\":[\"open\",\"in_progress\",\"deferred\",\"closed\"],\"schemas\":[{{\"schema_ref\":\"urn:bead-rs:schema:issue:native-v1\"}},{{\"schema_ref\":\"urn:bead-rs:schema:event:native-v1\"}},{{\"schema_ref\":\"urn:bead-rs:schema:field-guide:native-v1\"}}]}}'\nelse\n  echo '{version}'\nfi\n"
src/bead_store/mod.rs-1313-            ),
src/bead_store/mod.rs-1314-        )
src/bead_store/mod.rs-1315-        .unwrap();
src/bead_store/mod.rs-1316-        let mut permissions = std::fs::metadata(&path).unwrap().permissions();
src/bead_store/mod.rs-1317-        permissions.set_mode(0o755);
src/bead_store/mod.rs-1318-        std::fs::set_permissions(&path, permissions).unwrap();
src/bead_store/mod.rs-1319-        path
src/bead_store/mod.rs-1320-    }
--
src/bead_store/mod.rs-3133-        let workspace = tempfile::tempdir().unwrap();
src/bead_store/mod.rs-3134-
src/bead_store/mod.rs-3135-        // Create a healthy bead-rs binary that succeeds on first try
src/bead_store/mod.rs-3136-        let bead_rs = workspace.path().join("bead-rs-fixture");
src/bead_store/mod.rs-3137-        std::fs::write(
src/bead_store/mod.rs-3138-            &bead_rs,
src/bead_store/mod.rs-3139-            r#"#!/bin/bash
src/bead_store/mod.rs-3140-if [ "$1" = "capabilities" ]; then
src/bead_store/mod.rs:3141:  printf '{"implementation":"bead-rs","atomic_claim":true,"statuses":["open","in_progress","deferred","closed"],"schemas":[{"schema_ref":"urn:bead-rs:schema:issue:native-v1"}]}'
src/bead_store/mod.rs-3142-else
src/bead_store/mod.rs-3143-  echo "bead 0.1.1"
src/bead_store/mod.rs-3144-fi
src/bead_store/mod.rs-3145-"#,
src/bead_store/mod.rs-3146-        )
src/bead_store/mod.rs-3147-        .unwrap();
src/bead_store/mod.rs-3148-        let mut perm = std::fs::metadata(&bead_rs).unwrap().permissions();
src/bead_store/mod.rs-3149-        perm.set_mode(0o755);
--
src/bead_store/mod.rs-3265-        let workspace = tempfile::tempdir().unwrap();
src/bead_store/mod.rs-3266-
src/bead_store/mod.rs-3267-        // Create a healthy bead-rs binary with proper capabilities
src/bead_store/mod.rs-3268-        let bead_rs = workspace.path().join("bead-rs-caps-fixture");
src/bead_store/mod.rs-3269-        std::fs::write(
src/bead_store/mod.rs-3270-            &bead_rs,
src/bead_store/mod.rs-3271-            r#"#!/bin/bash
src/bead_store/mod.rs-3272-if [ "$1" = "capabilities" ]; then
src/bead_store/mod.rs:3273:  printf '{"implementation":"bead-rs","atomic_claim":true,"statuses":["open","in_progress","deferred","closed"],"schemas":[{"schema_ref":"urn:bead-rs:schema:issue:native-v1"},{"schema_ref":"urn:bead-rs:schema:event:native-v1"},{"schema_ref":"urn:bead-rs:schema:field-guide:native-v1"}]}'
src/bead_store/mod.rs-3274-else
src/bead_store/mod.rs-3275-  echo "bead 0.1.1"
src/bead_store/mod.rs-3276-fi
src/bead_store/mod.rs-3277-"#,
src/bead_store/mod.rs-3278-        )
src/bead_store/mod.rs-3279-        .unwrap();
src/bead_store/mod.rs-3280-        let mut perm = std::fs::metadata(&bead_rs).unwrap().permissions();
src/bead_store/mod.rs-3281-        perm.set_mode(0o755);
--
src/bead_store/cli_store.rs-532-            ),
src/bead_store/cli_store.rs-533-        };
src/bead_store/cli_store.rs-534-        Ok(BeadId::from(execute_create_id_strategy(
src/bead_store/cli_store.rs-535-            id_strategy,
src/bead_store/cli_store.rs-536-            &stdout,
src/bead_store/cli_store.rs-537-        )?))
src/bead_store/cli_store.rs-538-    }
src/bead_store/cli_store.rs-539-
src/bead_store/cli_store.rs:540:    async fn add_dependency(&self, blocker_id: &BeadId, blocked_id: &BeadId) -> Result<()> {
src/bead_store/cli_store.rs-541-        self.mutate(
src/bead_store/cli_store.rs-542-            "dep_add",
src/bead_store/cli_store.rs-543-            &[
src/bead_store/cli_store.rs-544-                ("blocker", blocker_id.to_string()),
src/bead_store/cli_store.rs:545:                ("blocked", blocked_id.to_string()),
src/bead_store/cli_store.rs-546-            ],
src/bead_store/cli_store.rs-547-        )
src/bead_store/cli_store.rs-548-        .await
src/bead_store/cli_store.rs-549-    }
src/bead_store/cli_store.rs-550-
src/bead_store/cli_store.rs-551-    async fn split_bead(
src/bead_store/cli_store.rs-552-        &self,
src/bead_store/cli_store.rs-553-        parent_id: &BeadId,
--
src/bead_store/cli_store.rs-597-                .create_bead(child.title, child.body, child.labels)
src/bead_store/cli_store.rs-598-                .await?;
src/bead_store/cli_store.rs-599-            self.add_dependency(&id, parent_id).await?;
src/bead_store/cli_store.rs-600-            ids.push(id);
src/bead_store/cli_store.rs-601-        }
src/bead_store/cli_store.rs-602-        Ok(ids)
src/bead_store/cli_store.rs-603-    }
src/bead_store/cli_store.rs-604-
src/bead_store/cli_store.rs:605:    async fn remove_dependency(&self, blocked_id: &BeadId, blocker_id: &BeadId) -> Result<()> {
src/bead_store/cli_store.rs-606-        self.mutate(
src/bead_store/cli_store.rs-607-            "dep_remove",
src/bead_store/cli_store.rs-608-            &[
src/bead_store/cli_store.rs:609:                ("blocked", blocked_id.to_string()),
src/bead_store/cli_store.rs-610-                ("blocker", blocker_id.to_string()),
src/bead_store/cli_store.rs-611-            ],
src/bead_store/cli_store.rs-612-        )
src/bead_store/cli_store.rs-613-        .await
src/bead_store/cli_store.rs-614-    }
src/bead_store/cli_store.rs-615-    async fn doctor_repair(&self) -> Result<RepairReport> {
src/bead_store/cli_store.rs-616-        let stdout = self.run_operation("doctor_repair", &HashMap::new()).await?;
src/bead_store/cli_store.rs-617-        Ok(parse_doctor_output(&stdout))
--
src/bead_store/cli_store.rs-830-        .await?;
src/bead_store/cli_store.rs-831-        Ok(())
src/bead_store/cli_store.rs-832-    }
src/bead_store/cli_store.rs-833-
src/bead_store/cli_store.rs-834-    /// Attempt public-CLI recovery, escalating from doctor repair to the
src/bead_store/cli_store.rs-835-    /// backend-specific checkpoint rebuild path.
src/bead_store/cli_store.rs-836-    pub async fn recover_db(&self) -> super::RecoveryOutcome {
src/bead_store/cli_store.rs-837-        match self.doctor_repair().await {
src/bead_store/cli_store.rs:838:            Ok(report) => super::RecoveryOutcome::Repaired(report),
src/bead_store/cli_store.rs-839-            Err(repair_error) => match self.full_rebuild().await {
src/bead_store/cli_store.rs:840:                Ok(()) => super::RecoveryOutcome::Rebuilt,
src/bead_store/cli_store.rs-841-                Err(rebuild_error) => {
src/bead_store/cli_store.rs:842:                    super::RecoveryOutcome::Failed(rebuild_error.context(format!(
src/bead_store/cli_store.rs-843-                        "{} doctor repair failed before rebuild: {repair_error:#}",
src/bead_store/cli_store.rs-844-                        self.backend.name
src/bead_store/cli_store.rs-845-                    )))
src/bead_store/cli_store.rs-846-                }
src/bead_store/cli_store.rs-847-            },
src/bead_store/cli_store.rs-848-        }
src/bead_store/cli_store.rs-849-    }
src/bead_store/cli_store.rs-850-}
--
src/bead_store/cli_store.rs-1307-            None,
src/bead_store/cli_store.rs-1308-            None,
src/bead_store/cli_store.rs-1309-            None,
src/bead_store/cli_store.rs-1310-        )
src/bead_store/cli_store.rs-1311-        .unwrap();
src/bead_store/cli_store.rs-1312-
src/bead_store/cli_store.rs-1313-        // Test multiple placeholders in one argument
src/bead_store/cli_store.rs-1314-        let values = HashMap::from([
src/bead_store/cli_store.rs:1315:            ("blocked", "bf-parent".to_string()),
src/bead_store/cli_store.rs-1316-            ("blocker", "bf-child".to_string()),
src/bead_store/cli_store.rs-1317-        ]);
src/bead_store/cli_store.rs-1318-        let result = store.render_operation("dep_add", &values).unwrap();
src/bead_store/cli_store.rs-1319-        assert_eq!(
src/bead_store/cli_store.rs-1320-            result,
src/bead_store/cli_store.rs-1321-            vec!["dep", "add", "bf-parent", "bf-child", "--kind", "blocks"]
src/bead_store/cli_store.rs-1322-        );
src/bead_store/cli_store.rs-1323-    }
--
src/strand/mend.rs-716-    }
src/strand/mend.rs-717-
src/strand/mend.rs-718-    // ── Step 3: Dependency link repair ───────────────────────────────────────
src/strand/mend.rs-719-
src/strand/mend.rs-720-    /// Find open beads that have closed blockers and clean up the stale
src/strand/mend.rs-721-    /// dependency links.
src/strand/mend.rs-722-    ///
src/strand/mend.rs-723-    /// br does not automatically resolve dependency links on bead closure,
src/strand/mend.rs:724:    /// so a bead can remain blocked even after its blocker is closed.
src/strand/mend.rs-725-    async fn cleanup_stale_dependencies(
src/strand/mend.rs-726-        &self,
src/strand/mend.rs-727-        store: &dyn BeadStore,
src/strand/mend.rs-728-        summary: &mut MendSummary,
src/strand/mend.rs-729-    ) -> Result<()> {
src/strand/mend.rs-730-        let all_beads = store.list_all().await?;
src/strand/mend.rs-731-
src/strand/mend.rs-732-        for bead in &all_beads {
--
src/strand/mend.rs-1777-        }
src/strand/mend.rs-1778-        async fn full_rebuild(&self) -> Result<()> {
src/strand/mend.rs-1779-            if self.rebuild_fails {
src/strand/mend.rs-1780-                anyhow::bail!("full rebuild failed: JSONL corrupt")
src/strand/mend.rs-1781-            } else {
src/strand/mend.rs-1782-                Ok(())
src/strand/mend.rs-1783-            }
src/strand/mend.rs-1784-        }
src/strand/mend.rs:1785:        async fn add_dependency(&self, _blocker_id: &BeadId, _blocked_id: &BeadId) -> Result<()> {
src/strand/mend.rs-1786-            Ok(())
src/strand/mend.rs-1787-        }
src/strand/mend.rs-1788-        async fn remove_dependency(
src/strand/mend.rs-1789-            &self,
src/strand/mend.rs:1790:            _blocked_id: &BeadId,
src/strand/mend.rs-1791-            _blocker_id: &BeadId,
src/strand/mend.rs-1792-        ) -> Result<()> {
src/strand/mend.rs-1793-            Ok(())
src/strand/mend.rs-1794-        }
src/strand/mend.rs-1795-        async fn clear_assignee(&self, id: &BeadId) -> Result<()> {
src/strand/mend.rs-1796-            // Find and update the bead's assignee, simulating real bead store behavior.
src/strand/mend.rs-1797-            let mut beads = self.all_beads.lock().unwrap();
src/strand/mend.rs-1798-            if let Some(bead) = beads.iter_mut().find(|b| &b.id == id) {
--
src/strand/mend.rs-1859-            anyhow::bail!("store error")
src/strand/mend.rs-1860-        }
src/strand/mend.rs-1861-        async fn doctor_check(&self) -> Result<RepairReport> {
src/strand/mend.rs-1862-            anyhow::bail!("store error")
src/strand/mend.rs-1863-        }
src/strand/mend.rs-1864-        async fn full_rebuild(&self) -> Result<()> {
src/strand/mend.rs-1865-            anyhow::bail!("store error")
src/strand/mend.rs-1866-        }
src/strand/mend.rs:1867:        async fn add_dependency(&self, _blocker_id: &BeadId, _blocked_id: &BeadId) -> Result<()> {
src/strand/mend.rs-1868-            Ok(())
src/strand/mend.rs-1869-        }
src/strand/mend.rs-1870-        async fn remove_dependency(
src/strand/mend.rs-1871-            &self,
src/strand/mend.rs:1872:            _blocked_id: &BeadId,
src/strand/mend.rs-1873-            _blocker_id: &BeadId,
src/strand/mend.rs-1874-        ) -> Result<()> {
src/strand/mend.rs-1875-            anyhow::bail!("store error")
src/strand/mend.rs-1876-        }
src/strand/mend.rs-1877-        async fn clear_assignee(&self, _id: &BeadId) -> Result<()> {
src/strand/mend.rs-1878-            anyhow::bail!("store error")
src/strand/mend.rs-1879-        }
src/strand/mend.rs-1880-
--
src/strand/mend.rs-2982-            hb_dir.path(),
src/strand/mend.rs-2983-            &make_stale_heartbeat("dead-worker", 99_999_999, Some("nd-orphan")),
src/strand/mend.rs-2984-        );
src/strand/mend.rs-2985-
src/strand/mend.rs-2986-        // In-progress bead assigned to dead worker (for peer cleanup).
src/strand/mend.rs-2987-        let orphan_bead = make_in_progress_bead("nd-orphan", "dead-worker");
src/strand/mend.rs-2988-
src/strand/mend.rs-2989-        // Stale dependency link.
src/strand/mend.rs:2990:        let blocked_bead = make_bead_with_deps(
src/strand/mend.rs:2991:            "blocked-bead",
src/strand/mend.rs-2992-            BeadStatus::Open,
src/strand/mend.rs-2993-            vec![make_dep("done-blocker", "closed", "blocks")],
src/strand/mend.rs-2994-        );
src/strand/mend.rs-2995-
src/strand/mend.rs:2996:        let (store, release_count, _) = MockBeadStore::new(vec![orphan_bead, blocked_bead]);
src/strand/mend.rs-2997-        let mend = make_mend_strand(hb_dir.path(), lock_dir.path(), reg_dir.path());
src/strand/mend.rs-2998-
src/strand/mend.rs-2999-        let result = mend.evaluate(&store, &HashSet::new()).await;
src/strand/mend.rs-3000-        assert!(
src/strand/mend.rs-3001-            matches!(result, StrandResult::WorkCreated),
src/strand/mend.rs-3002-            "expected WorkCreated after combined cleanup, got: {result:?}"
src/strand/mend.rs-3003-        );
src/strand/mend.rs-3004-        assert_eq!(release_count.load(Ordering::Relaxed), 1);
--
src/strand/mend.rs-4654-    // ── Stale dependency cleanup tests ─────────────────────────────────────────
src/strand/mend.rs-4655-
src/strand/mend.rs-4656-    /// Type alias for the removed dependencies tracking vector.
src/strand/mend.rs-4657-    type RemovedDeps = Arc<std::sync::Mutex<Vec<(BeadId, BeadId)>>>;
src/strand/mend.rs-4658-
src/strand/mend.rs-4659-    /// Mock bead store that tracks remove_dependency calls.
src/strand/mend.rs-4660-    struct DepTrackingMockStore {
src/strand/mend.rs-4661-        all_beads: Vec<Bead>,
src/strand/mend.rs:4662:        /// Records (blocked_id, blocker_id) pairs for remove_dependency calls.
src/strand/mend.rs-4663-        removed_deps: RemovedDeps,
src/strand/mend.rs-4664-        /// If true, remove_dependency fails.
src/strand/mend.rs-4665-        removal_fails: bool,
src/strand/mend.rs-4666-    }
src/strand/mend.rs-4667-
src/strand/mend.rs-4668-    impl DepTrackingMockStore {
src/strand/mend.rs-4669-        fn new(beads: Vec<Bead>) -> (Self, RemovedDeps) {
src/strand/mend.rs-4670-            let removed_deps = Arc::new(std::sync::Mutex::new(Vec::new()));
--
src/strand/mend.rs-4736-        async fn full_rebuild(&self) -> Result<()> {
src/strand/mend.rs-4737-            Ok(())
src/strand/mend.rs-4738-        }
src/strand/mend.rs-4739-
src/strand/mend.rs-4740-        fn has_valid_store(&self) -> bool {
src/strand/mend.rs-4741-            true
src/strand/mend.rs-4742-        }
src/strand/mend.rs-4743-
src/strand/mend.rs:4744:        async fn add_dependency(&self, _blocker_id: &BeadId, _blocked_id: &BeadId) -> Result<()> {
src/strand/mend.rs-4745-            Ok(())
src/strand/mend.rs-4746-        }
src/strand/mend.rs:4747:        async fn remove_dependency(&self, blocked_id: &BeadId, blocker_id: &BeadId) -> Result<()> {
src/strand/mend.rs-4748-            if self.removal_fails {
src/strand/mend.rs-4749-                anyhow::bail!("failed to remove dependency")
src/strand/mend.rs-4750-            }
src/strand/mend.rs-4751-            self.removed_deps
src/strand/mend.rs-4752-                .lock()
src/strand/mend.rs-4753-                .unwrap()
src/strand/mend.rs:4754:                .push((blocked_id.clone(), blocker_id.clone()));
src/strand/mend.rs-4755-            Ok(())
src/strand/mend.rs-4756-        }
src/strand/mend.rs-4757-        async fn clear_assignee(&self, _id: &BeadId) -> Result<()> {
src/strand/mend.rs-4758-            Ok(())
src/strand/mend.rs-4759-        }
src/strand/mend.rs-4760-    }
src/strand/mend.rs-4761-
src/strand/mend.rs-4762-    #[tokio::test]
--
src/strand/unravel.rs:1://! Unravel strand: propose automated alternatives for human-blocked beads.
src/strand/unravel.rs-2-//!
src/strand/unravel.rs-3-//! When beads carry the "human" label (requiring a human decision before
src/strand/unravel.rs-4-//! proceeding), Unravel dispatches an AI agent to propose workarounds that
src/strand/unravel.rs-5-//! could be executed autonomously. Alternative proposals are created as
src/strand/unravel.rs-6-//! child beads of the original human-labeled bead.
src/strand/unravel.rs-7-//!
src/strand/unravel.rs-8-//! **Guardrails:**
src/strand/unravel.rs-9-//! - Opt-in only (disabled by default).
--
src/strand/unravel.rs-35-#[async_trait::async_trait]
src/strand/unravel.rs-36-pub trait UnravelAgent: Send + Sync {
src/strand/unravel.rs-37-    /// Invoke an agent with the given prompt, returning its raw text response.
src/strand/unravel.rs-38-    async fn propose_alternatives(&self, prompt: &str, workspace: &Path) -> Result<String>;
src/strand/unravel.rs-39-}
src/strand/unravel.rs-40-
src/strand/unravel.rs-41-// ─── Proposed alternative (parsed from agent response) ──────────────────────

exec
/bin/bash -lc "printf '%s\\n' '-- bead-forge status reference rows --'
sqlite3 -header -column /home/coding/.needle/home/coding/.beads/beads.db 'SELECT id, name, category, description, is_terminal FROM statuses ORDER BY id;'
printf '%s\\n' '-- statuses actually stored in legacy DB --'
sqlite3 -header -column /home/coding/.needle/home/coding/.beads/beads.db 'SELECT status, COUNT(*) AS count FROM issues GROUP BY status ORDER BY status;'
printf '%s\\n' '-- status values accepted by archived bf update --'
/home/coding/backups/bf-tooling-removed-20260814/bf update --help | sed -n '1,260p'
printf '%s\\n' '-- canary worker states --'
rg -n -C 5 'enum WorkerState|WorkerState|BOOTING|SELECTING|CLAIMING|DISPATCHING|RESOLVING|DONE' src/worker.rs src 2>/dev/null | sed -n '1,520p'
printf '%s\\n' '-- agent failure and timeout resolution --'
rg -n -C 10 'Failure|Timeout|failure-count|add_label|release_bead|close_bead|BeadStatus::' src/resolve.rs src/worker.rs src/dispatch/mod.rs src/agent 2>/dev/null | sed -n '1,520p'" in /home/coding/NEEDLE
 succeeded in 0ms:
-- bead-forge status reference rows --
-- statuses actually stored in legacy DB --
status       count
-----------  -----
in_progress  1    
-- status values accepted by archived bf update --
Update a bead

Updates a single field on a bead. Exactly one of --title, --status, --priority, or --assignee must be provided. All other fields are preserved unchanged.

Usage: bf update [OPTIONS] <ID>

Arguments:
  <ID>
          Bead ID

Options:
      --title <TITLE>
          New title

  -w, --workspace <WORKSPACE>
          Workspace directory (defaults to current directory's .beads/)

      --no-auto-flush
          Disable the automatic SQLite→JSONL flush after mutating commands for this invocation. Overrides `sync.auto_flush: true` in config; a no-op when auto-flush is already off. See `crate::autoflush::enabled`

      --status <STATUS>
          New status

      --envelope
          Wrap --json output in a standard envelope: {version, kind, data, warning?}. This provides stable structure for programmatic consumers. See `bf robot-docs`

      --priority <PRIORITY>
          New priority

      --assignee <ASSIGNEE>
          New assignee (use "" to clear)

      --no-progress
          Disable progress indicators for this invocation. Useful for scripted environments or CI pipelines where visual progress output is unwanted

      --json
          Output JSON

  -V, --version
          Print version information ("bf <version>") and exit 0.
          
          Declared by hand rather than via clap's built-in version flag (see `disable_version_flag` above) so the version is written to stdout by `run` instead of clap's own writer, and so it is accepted anywhere on the command line, including after a subcommand.

  -h, --help
          Print help (see a summary with '-h')
-- canary worker states --
src/peer/mod.rs-280-#[cfg(test)]
src/peer/mod.rs-281-mod tests {
src/peer/mod.rs-282-    use super::*;
src/peer/mod.rs-283-    use crate::bead_store::{Filters, RepairReport};
src/peer/mod.rs-284-    use crate::health::HeartbeatData;
src/peer/mod.rs:285:    use crate::types::{Bead, BeadId, ClaimResult, WorkerState};
src/peer/mod.rs-286-    use async_trait::async_trait;
src/peer/mod.rs-287-    use chrono::Utc;
src/peer/mod.rs-288-    use std::path::PathBuf;
src/peer/mod.rs-289-    use std::sync::atomic::{AtomicU32, Ordering};
src/peer/mod.rs-290-    use std::sync::Arc;
--
src/peer/mod.rs-412-
src/peer/mod.rs-413-        HeartbeatData {
src/peer/mod.rs-414-            worker_id: worker_id.to_string(),
src/peer/mod.rs-415-            qualified_id: format!("claude-{}", worker_id),
src/peer/mod.rs-416-            pid,
src/peer/mod.rs:417:            state: WorkerState::Executing,
src/peer/mod.rs-418-            current_bead,
src/peer/mod.rs-419-            workspace: PathBuf::from("/tmp/test"),
src/peer/mod.rs-420-            last_heartbeat,
src/peer/mod.rs-421-            started_at: Utc::now() - chrono::Duration::seconds(3600),
src/peer/mod.rs-422-            beads_processed: 0,
--
src/canary/mod.rs-123-        #[serde(default = "ExpectedOutcome::default_timeout_status")]
src/canary/mod.rs-124-        final_status: String,
src/canary/mod.rs-125-    },
src/canary/mod.rs-126-    /// Worker state machine should transition correctly.
src/canary/mod.rs-127-    StateMachine {
src/canary/mod.rs:128:        /// Expected state transitions (e.g., ["BOOTING", "SELECTING", "CLAIMING", ...]).
src/canary/mod.rs-129-        transitions: Vec<String>,
src/canary/mod.rs-130-    },
src/canary/mod.rs-131-}
src/canary/mod.rs-132-
src/canary/mod.rs-133-impl ExpectedOutcome {
--
src/canary/mod.rs-1150-            300,
src/canary/mod.rs-1151-        );
src/canary/mod.rs-1152-
src/canary/mod.rs-1153-        let expected = ExpectedOutcome::StateMachine {
src/canary/mod.rs-1154-            transitions: vec![
src/canary/mod.rs:1155:                "BOOTING".to_string(),
src/canary/mod.rs:1156:                "SELECTING".to_string(),
src/canary/mod.rs:1157:                "CLAIMING".to_string(),
src/canary/mod.rs-1158-            ],
src/canary/mod.rs-1159-        };
src/canary/mod.rs-1160-
src/canary/mod.rs-1161-        let actual = ActualOutcome {
src/canary/mod.rs-1162-            exit_code: Some(0),
src/canary/mod.rs-1163-            final_status: "done".to_string(),
src/canary/mod.rs-1164-            labels: vec![],
src/canary/mod.rs-1165-            state_transitions: vec![
src/canary/mod.rs:1166:                "BOOTING".to_string(),
src/canary/mod.rs:1167:                "SELECTING".to_string(),
src/canary/mod.rs:1168:                "CLAIMING".to_string(),
src/canary/mod.rs:1169:                "DISPATCHING".to_string(),
src/canary/mod.rs-1170-            ],
src/canary/mod.rs-1171-        };
src/canary/mod.rs-1172-
src/canary/mod.rs-1173-        assert!(runner.outcomes_match(&expected, &actual));
src/canary/mod.rs-1174-
src/canary/mod.rs-1175-        // Missing transition
src/canary/mod.rs-1176-        let actual_missing = ActualOutcome {
src/canary/mod.rs:1177:            state_transitions: vec!["BOOTING".to_string(), "SELECTING".to_string()],
src/canary/mod.rs-1178-            ..actual
src/canary/mod.rs-1179-        };
src/canary/mod.rs-1180-        assert!(!runner.outcomes_match(&expected, &actual_missing));
src/canary/mod.rs-1181-    }
src/canary/mod.rs-1182-
--
src/canary/mod.rs-1265-            300,
src/canary/mod.rs-1266-        );
src/canary/mod.rs-1267-
src/canary/mod.rs-1268-        let expected = ExpectedOutcome::StateMachine {
src/canary/mod.rs-1269-            transitions: vec![
src/canary/mod.rs:1270:                "BOOTING".to_string(),
src/canary/mod.rs:1271:                "SELECTING".to_string(),
src/canary/mod.rs:1272:                "DISPATCHING".to_string(),
src/canary/mod.rs-1273-            ],
src/canary/mod.rs-1274-        };
src/canary/mod.rs-1275-
src/canary/mod.rs-1276-        let actual = ActualOutcome {
src/canary/mod.rs-1277-            exit_code: Some(0),
src/canary/mod.rs-1278-            final_status: "done".to_string(),
src/canary/mod.rs-1279-            labels: vec![],
src/canary/mod.rs:1280:            state_transitions: vec!["BOOTING".to_string()],
src/canary/mod.rs-1281-        };
src/canary/mod.rs-1282-
src/canary/mod.rs-1283-        let reason = runner.mismatch_reason(&expected, &actual);
src/canary/mod.rs-1284-        assert!(reason.contains("missing state transitions"));
src/canary/mod.rs:1285:        assert!(reason.contains("SELECTING"));
src/canary/mod.rs:1286:        assert!(reason.contains("DISPATCHING"));
src/canary/mod.rs-1287-    }
src/canary/mod.rs-1288-
src/canary/mod.rs-1289-    #[test]
src/canary/mod.rs-1290-    fn outcomes_match_timeout() {
src/canary/mod.rs-1291-        let runner = CanaryRunner::new(
--
src/canary/mod.rs-1659-            },
src/canary/mod.rs-1660-            ExpectedOutcome::Timeout {
src/canary/mod.rs-1661-                final_status: "open".to_string(),
src/canary/mod.rs-1662-            },
src/canary/mod.rs-1663-            ExpectedOutcome::StateMachine {
src/canary/mod.rs:1664:                transitions: vec!["BOOTING".to_string(), "SELECTING".to_string()],
src/canary/mod.rs-1665-            },
src/canary/mod.rs-1666-        ];
src/canary/mod.rs-1667-
src/canary/mod.rs-1668-        for expected in &outcomes {
src/canary/mod.rs-1669-            let yaml = serde_yaml::to_string(expected).unwrap();
--
src/outcome/mod.rs-109-                    "flushed local changes to JSONL before release"
src/outcome/mod.rs-110-                );
src/outcome/mod.rs-111-                // Emit heartbeat after successful flush.
src/outcome/mod.rs-112-                let _ = self.telemetry.emit_try_lock(EventKind::HeartbeatEmitted {
src/outcome/mod.rs-113-                    bead_id: Some(bead.id.clone()),
src/outcome/mod.rs:114:                    state: "HANDLING_FLUSH_DONE".to_string(),
src/outcome/mod.rs-115-                });
src/outcome/mod.rs-116-            }
src/outcome/mod.rs-117-            Ok(None) => {
src/outcome/mod.rs-118-                tracing::warn!(
src/outcome/mod.rs-119-                    bead_id = %bead.id,
--
src/outcome/mod.rs-158-                    "bead released successfully"
src/outcome/mod.rs-159-                );
src/outcome/mod.rs-160-                // Emit heartbeat after successful release.
src/outcome/mod.rs-161-                let _ = self.telemetry.emit_try_lock(EventKind::HeartbeatEmitted {
src/outcome/mod.rs-162-                    bead_id: Some(bead.id.clone()),
src/outcome/mod.rs:163:                    state: "HANDLING_RELEASE_DONE".to_string(),
src/outcome/mod.rs-164-                });
src/outcome/mod.rs-165-                events.push(EventKind::BeadReleased {
src/outcome/mod.rs-166-                    bead_id: bead.id.clone(),
src/outcome/mod.rs-167-                    reason: "release_success".to_string(),
src/outcome/mod.rs-168-                });
--
src/cli/mod.rs-531-        config.self_modification.hot_reload = hr;
src/cli/mod.rs-532-    }
src/cli/mod.rs-533-
src/cli/mod.rs-534-    if resume {
src/cli/mod.rs-535-        // Hot-reload resume: inherit worker identity from --identifier,
src/cli/mod.rs:536:        // load state from heartbeat file + registry, continue from SELECTING.
src/cli/mod.rs-537-        let worker_id = identifier
src/cli/mod.rs-538-            .clone()
src/cli/mod.rs-539-            .unwrap_or_else(|| NATO_ALPHABET[0].to_string());
src/cli/mod.rs-540-
src/cli/mod.rs-541-        // Load resume state from heartbeat and registry.
--
src/cli/mod.rs-6742-    }
src/cli/mod.rs-6743-
src/cli/mod.rs-6744-    #[test]
src/cli/mod.rs-6745-    fn doctor_check_heartbeats_stale_file_warns() {
src/cli/mod.rs-6746-        use crate::health::HeartbeatData;
src/cli/mod.rs:6747:        use crate::types::WorkerState;
src/cli/mod.rs-6748-
src/cli/mod.rs-6749-        let tmp = tempfile::tempdir().unwrap();
src/cli/mod.rs-6750-        let hb = HeartbeatData {
src/cli/mod.rs-6751-            worker_id: "test-w".to_string(),
src/cli/mod.rs-6752-            qualified_id: "claude-test-w".to_string(),
src/cli/mod.rs-6753-            pid: 999_999,
src/cli/mod.rs:6754:            state: WorkerState::Selecting,
src/cli/mod.rs-6755-            current_bead: None,
src/cli/mod.rs-6756-            workspace: PathBuf::from("/tmp"),
src/cli/mod.rs-6757-            last_heartbeat: chrono::Utc::now() - chrono::Duration::seconds(600),
src/cli/mod.rs-6758-            started_at: chrono::Utc::now(),
src/cli/mod.rs-6759-            beads_processed: 0,
--
src/cli/mod.rs-6775-    }
src/cli/mod.rs-6776-
src/cli/mod.rs-6777-    #[test]
src/cli/mod.rs-6778-    fn doctor_check_heartbeats_stale_file_repair() {
src/cli/mod.rs-6779-        use crate::health::HeartbeatData;
src/cli/mod.rs:6780:        use crate::types::WorkerState;
src/cli/mod.rs-6781-
src/cli/mod.rs-6782-        let tmp = tempfile::tempdir().unwrap();
src/cli/mod.rs-6783-        let hb = HeartbeatData {
src/cli/mod.rs-6784-            worker_id: "test-rm".to_string(),
src/cli/mod.rs-6785-            qualified_id: "claude-test-rm".to_string(),
src/cli/mod.rs-6786-            pid: 999_999,
src/cli/mod.rs:6787:            state: WorkerState::Selecting,
src/cli/mod.rs-6788-            current_bead: None,
src/cli/mod.rs-6789-            workspace: PathBuf::from("/tmp"),
src/cli/mod.rs-6790-            last_heartbeat: chrono::Utc::now() - chrono::Duration::seconds(600),
src/cli/mod.rs-6791-            started_at: chrono::Utc::now(),
src/cli/mod.rs-6792-            beads_processed: 0,
--
src/strand/pluck.rs-385-        // 2. Filter: remove beads with excluded labels.
src/strand/pluck.rs-386-        //    Defensive guard — store.ready() passes exclude_labels in its Filters,
src/strand/pluck.rs-387-        //    but the backing CLI may not include label data in every query type.
src/strand/pluck.rs-388-        //    Filtering here guarantees excluded-label beads are never presented as
src/strand/pluck.rs-389-        //    candidates regardless of backend behaviour, preventing the
src/strand/pluck.rs:390:        //    SELECTING→CLAIMING→RETRYING spin loop observed when br ready --json
src/strand/pluck.rs-391-        //    omits label fields for some beads.
src/strand/pluck.rs-392-        let before_label_filter = candidates.len();
src/strand/pluck.rs-393-
src/strand/pluck.rs-394-        // First pass: collect excluded beads and their reasons for telemetry.
src/strand/pluck.rs-395-        let excluded_beads: Vec<_> = candidates
--
src/strand/pluck.rs-1233-
src/strand/pluck.rs-1234-    // These tests use UnfilteredStore, which returns all beads from ready()
src/strand/pluck.rs-1235-    // without applying label exclusion — simulating a backend (e.g. br ready
src/strand/pluck.rs-1236-    // --json) that omits label data.  They verify that the strand's own
src/strand/pluck.rs-1237-    // defensive retain catches excluded-label beads regardless of what the
src/strand/pluck.rs:1238:    // store returns, preventing the SELECTING→CLAIMING→RETRYING spin loop.
src/strand/pluck.rs-1239-
src/strand/pluck.rs-1240-    #[tokio::test]
src/strand/pluck.rs-1241-    async fn strand_filters_excluded_labels_when_store_does_not() {
src/strand/pluck.rs-1242-        let store = UnfilteredStore {
src/strand/pluck.rs-1243-            beads: vec![
--
src/upgrade/mod.rs-700-    Skipped { reason: String },
src/upgrade/mod.rs-701-}
src/upgrade/mod.rs-702-
src/upgrade/mod.rs-703-/// Check whether the `:stable` binary differs from the currently running binary.
src/upgrade/mod.rs-704-///
src/upgrade/mod.rs:705:/// This is called between LOGGING and SELECTING in the worker loop.
src/upgrade/mod.rs-706-pub fn check_hot_reload(needle_home: &Path) -> Result<HotReloadCheck> {
src/upgrade/mod.rs-707-    let stable_path = needle_home.join("bin/needle-stable");
src/upgrade/mod.rs-708-
src/upgrade/mod.rs-709-    if !stable_path.exists() {
src/upgrade/mod.rs-710-        return Ok(HotReloadCheck::Skipped {
--
src/health/mod.rs-18-use chrono::{DateTime, Utc};
src/health/mod.rs-19-use serde::{Deserialize, Serialize};
src/health/mod.rs-20-
src/health/mod.rs-21-use crate::config::Config;
src/health/mod.rs-22-use crate::telemetry::Telemetry;
src/health/mod.rs:23:use crate::types::{BeadId, WorkerState};
src/health/mod.rs-24-
src/health/mod.rs-25-// ──────────────────────────────────────────────────────────────────────────────
src/health/mod.rs-26-// SupervisorDetectionConfig — configuration for supervisor detection
src/health/mod.rs-27-// ──────────────────────────────────────────────────────────────────────────────
src/health/mod.rs-28-
--
src/health/mod.rs-71-    pub worker_id: String,
src/health/mod.rs-72-    /// Fully-qualified identity: `{adapter}-{worker_id}` (e.g., "claude-code-glm-5-foxtrot").
src/health/mod.rs-73-    #[serde(default)]
src/health/mod.rs-74-    pub qualified_id: String,
src/health/mod.rs-75-    pub pid: u32,
src/health/mod.rs:76:    pub state: WorkerState,
src/health/mod.rs-77-    pub current_bead: Option<BeadId>,
src/health/mod.rs-78-    pub workspace: PathBuf,
src/health/mod.rs-79-    pub last_heartbeat: DateTime<Utc>,
src/health/mod.rs-80-    pub started_at: DateTime<Utc>,
src/health/mod.rs-81-    pub beads_processed: u64,
--
src/health/mod.rs-106-// SharedHeartbeatState — updated by worker, read by emitter
src/health/mod.rs-107-// ──────────────────────────────────────────────────────────────────────────────
src/health/mod.rs-108-
src/health/mod.rs-109-/// Shared state between the main worker loop and the heartbeat emitter thread.
src/health/mod.rs-110-struct SharedHeartbeatState {
src/health/mod.rs:111:    state: WorkerState,
src/health/mod.rs-112-    current_bead: Option<BeadId>,
src/health/mod.rs-113-    beads_processed: u64,
src/health/mod.rs-114-    /// The workspace of the current bead (updates dynamically during cross-workspace work).
src/health/mod.rs-115-    current_workspace: Option<PathBuf>,
src/health/mod.rs-116-    /// Model being used (from adapter configuration).
--
src/health/mod.rs-182-            worker_id: worker_name,
src/health/mod.rs-183-            qualified_id,
src/health/mod.rs-184-            workspace: config.workspace.default.clone(),
src/health/mod.rs-185-            started_at: Utc::now(),
src/health/mod.rs-186-            shared_state: Arc::new(Mutex::new(SharedHeartbeatState {
src/health/mod.rs:187:                state: WorkerState::Booting,
src/health/mod.rs-188-                current_bead: None,
src/health/mod.rs-189-                beads_processed: 0,
src/health/mod.rs-190-                current_workspace: None,
src/health/mod.rs-191-                model: config.agent.default.clone(),
src/health/mod.rs-192-                last_strand: None,
--
src/health/mod.rs-286-    /// Update the worker state visible to the heartbeat emitter.
src/health/mod.rs-287-    ///
src/health/mod.rs-288-    /// Called by the worker on every state transition.
src/health/mod.rs-289-    pub fn update_state(
src/health/mod.rs-290-        &self,
src/health/mod.rs:291:        state: &WorkerState,
src/health/mod.rs-292-        current_bead: Option<&BeadId>,
src/health/mod.rs-293-        workspace: Option<&Path>,
src/health/mod.rs-294-    ) {
src/health/mod.rs-295-        if let Ok(mut guard) = self.shared_state.lock() {
src/health/mod.rs-296-            guard.state = state.clone();
--
src/health/mod.rs-896-        };
src/health/mod.rs-897-
src/health/mod.rs-898-        // Use the current bead's workspace if set, otherwise fall back to home workspace.
src/health/mod.rs-899-        let effective_workspace = current_workspace.unwrap_or_else(|| self.workspace.clone());
src/health/mod.rs-900-
src/health/mod.rs:901:        let is_idle = state == WorkerState::Exhausted || current_bead.is_none();
src/health/mod.rs-902-        let current_task = current_bead.as_ref().map(|b| b.to_string());
src/health/mod.rs-903-
src/health/mod.rs-904-        // HOOP Hook 3 (heartbeat): append a JSONL line in HOOP's three-state
src/health/mod.rs-905-        // format alongside the existing per-worker JSON file below. Best
src/health/mod.rs-906-        // effort — see hoop_hooks module docs.
src/health/mod.rs:907:        if state == WorkerState::Exhausted {
src/health/mod.rs-908-            crate::hoop_hooks::emit_needle_heartbeat(
src/health/mod.rs-909-                &effective_workspace,
src/health/mod.rs-910-                &self.worker_id,
src/health/mod.rs-911-                "knot",
src/health/mod.rs-912-                serde_json::json!({"reason": "strands exhausted"}),
--
src/health/mod.rs-1256-            };
src/health/mod.rs-1257-
src/health/mod.rs-1258-        // Use the current bead's workspace if set, otherwise fall back to home workspace.
src/health/mod.rs-1259-        let effective_workspace = current_workspace.unwrap_or_else(|| workspace.clone());
src/health/mod.rs-1260-
src/health/mod.rs:1261:        let is_idle = state == WorkerState::Exhausted || current_bead.is_none();
src/health/mod.rs-1262-        let current_task = current_bead.as_ref().map(|b| b.to_string());
src/health/mod.rs-1263-
src/health/mod.rs-1264-        let data = HeartbeatData {
src/health/mod.rs-1265-            worker_id: worker_id.clone(),
src/health/mod.rs-1266-            qualified_id: qualified_id.clone(),
--
src/health/mod.rs-1471-
src/health/mod.rs-1472-        monitor.start_emitter().unwrap();
src/health/mod.rs-1473-
src/health/mod.rs-1474-        // Update shared state.
src/health/mod.rs-1475-        monitor.update_state(
src/health/mod.rs:1476:            &WorkerState::Executing,
src/health/mod.rs-1477-            Some(&BeadId::from("needle-abc")),
src/health/mod.rs-1478-            None,
src/health/mod.rs-1479-        );
src/health/mod.rs-1480-        monitor.update_beads_processed(5);
src/health/mod.rs-1481-
--
src/health/mod.rs-1484-        // Use tokio::time::sleep for consistency with async test framework.
src/health/mod.rs-1485-        tokio::time::sleep(Duration::from_millis(1500)).await;
src/health/mod.rs-1486-
src/health/mod.rs-1487-        let content = std::fs::read_to_string(monitor.heartbeat_path()).unwrap();
src/health/mod.rs-1488-        let data: HeartbeatData = serde_json::from_str(&content).unwrap();
src/health/mod.rs:1489:        assert_eq!(data.state, WorkerState::Executing);
src/health/mod.rs-1490-        assert_eq!(data.current_bead, Some(BeadId::from("needle-abc")));
src/health/mod.rs-1491-        assert_eq!(data.beads_processed, 5);
src/health/mod.rs-1492-
src/health/mod.rs-1493-        monitor.stop();
src/health/mod.rs-1494-    }
--
src/health/mod.rs-1525-        // Write two heartbeat files.
src/health/mod.rs-1526-        let hb1 = HeartbeatData {
src/health/mod.rs-1527-            worker_id: "worker-a".to_string(),
src/health/mod.rs-1528-            qualified_id: "claude-worker-a".to_string(),
src/health/mod.rs-1529-            pid: 1000,
src/health/mod.rs:1530:            state: WorkerState::Selecting,
src/health/mod.rs-1531-            current_bead: None,
src/health/mod.rs-1532-            workspace: PathBuf::from("/tmp"),
src/health/mod.rs-1533-            last_heartbeat: Utc::now(),
src/health/mod.rs-1534-            started_at: Utc::now(),
src/health/mod.rs-1535-            beads_processed: 0,
--
src/health/mod.rs-1541-        };
src/health/mod.rs-1542-        let hb2 = HeartbeatData {
src/health/mod.rs-1543-            worker_id: "worker-b".to_string(),
src/health/mod.rs-1544-            qualified_id: "claude-worker-b".to_string(),
src/health/mod.rs-1545-            pid: 2000,
src/health/mod.rs:1546:            state: WorkerState::Executing,
src/health/mod.rs-1547-            current_bead: Some(BeadId::from("nd-x")),
src/health/mod.rs-1548-            workspace: PathBuf::from("/tmp"),
src/health/mod.rs-1549-            last_heartbeat: Utc::now(),
src/health/mod.rs-1550-            started_at: Utc::now(),
src/health/mod.rs-1551-            beads_processed: 3,
--
src/health/mod.rs-1584-    fn is_stale_detects_old_heartbeats() {
src/health/mod.rs-1585-        let mut hb = HeartbeatData {
src/health/mod.rs-1586-            worker_id: "test".to_string(),
src/health/mod.rs-1587-            qualified_id: "claude-test".to_string(),
src/health/mod.rs-1588-            pid: 1,
src/health/mod.rs:1589:            state: WorkerState::Selecting,
src/health/mod.rs-1590-            current_bead: None,
src/health/mod.rs-1591-            workspace: PathBuf::from("/tmp"),
src/health/mod.rs-1592-            last_heartbeat: Utc::now(),
src/health/mod.rs-1593-            started_at: Utc::now(),
src/health/mod.rs-1594-            beads_processed: 0,
--
src/health/mod.rs-1658-    fn heartbeat_data_roundtrip() {
src/health/mod.rs-1659-        let data = HeartbeatData {
src/health/mod.rs-1660-            worker_id: "test-rt".to_string(),
src/health/mod.rs-1661-            qualified_id: "claude-test-rt".to_string(),
src/health/mod.rs-1662-            pid: 42,
src/health/mod.rs:1663:            state: WorkerState::Executing,
src/health/mod.rs-1664-            current_bead: Some(BeadId::from("nd-abc")),
src/health/mod.rs-1665-            workspace: PathBuf::from("/home/test"),
src/health/mod.rs-1666-            last_heartbeat: Utc::now(),
src/health/mod.rs-1667-            started_at: Utc::now(),
src/health/mod.rs-1668-            beads_processed: 10,
--
src/health/mod.rs-1700-        // Write a stale heartbeat for ourselves.
src/health/mod.rs-1701-        let hb = HeartbeatData {
src/health/mod.rs-1702-            worker_id: "self-worker".to_string(),
src/health/mod.rs-1703-            qualified_id: "claude-self-worker".to_string(),
src/health/mod.rs-1704-            pid: std::process::id(),
src/health/mod.rs:1705:            state: WorkerState::Selecting,
src/health/mod.rs-1706-            current_bead: None,
src/health/mod.rs-1707-            workspace: PathBuf::from("/tmp"),
src/health/mod.rs-1708-            last_heartbeat: Utc::now() - chrono::Duration::seconds(600),
src/health/mod.rs-1709-            started_at: Utc::now(),
src/health/mod.rs-1710-            beads_processed: 0,
--
src/health/mod.rs-1748-            let _ = std::fs::set_permissions(&hb_dir, std::fs::Permissions::from_mode(0o755));
src/health/mod.rs-1749-            return;
src/health/mod.rs-1750-        }
src/health/mod.rs-1751-
src/health/mod.rs-1752-        let shared_state = Arc::new(Mutex::new(SharedHeartbeatState {
src/health/mod.rs:1753:            state: WorkerState::Selecting,
src/health/mod.rs-1754-            current_bead: None,
src/health/mod.rs-1755-            beads_processed: 0,
src/health/mod.rs-1756-            current_workspace: None,
src/health/mod.rs-1757-            model: "claude-sonnet-4".to_string(),
src/health/mod.rs-1758-            last_strand: None,
--
src/health/mod.rs-1934-
src/health/mod.rs-1935-        monitor.start_emitter().unwrap();
src/health/mod.rs-1936-
src/health/mod.rs-1937-        // Simulate processing a bead from a different workspace
src/health/mod.rs-1938-        monitor.update_state(
src/health/mod.rs:1939:            &WorkerState::Executing,
src/health/mod.rs-1940-            Some(&BeadId::from("needle-abc")),
src/health/mod.rs-1941-            Some(remote_workspace.as_path()),
src/health/mod.rs-1942-        );
src/health/mod.rs-1943-
src/health/mod.rs-1944-        // Wait for the emitter to write a new heartbeat (uses tokio::time::sleep for
--
src/health/mod.rs-1997-        // Write initial heartbeat manually (simulating emitter behavior)
src/health/mod.rs-1998-        let initial_data = HeartbeatData {
src/health/mod.rs-1999-            worker_id: "validate-worker".to_string(),
src/health/mod.rs-2000-            qualified_id: monitor.qualified_id().to_string(),
src/health/mod.rs-2001-            pid: std::process::id(),
src/health/mod.rs:2002:            state: WorkerState::Selecting,
src/health/mod.rs-2003-            current_bead: None,
src/health/mod.rs-2004-            workspace: dir.path().to_path_buf(),
src/health/mod.rs-2005-            last_heartbeat: Utc::now(),
src/health/mod.rs-2006-            started_at: Utc::now(),
src/health/mod.rs-2007-            beads_processed: 0,
--
src/health/mod.rs-2323-        for i in 2..=3 {
src/health/mod.rs-2324-            let hb = HeartbeatData {
src/health/mod.rs-2325-                worker_id: format!("worker-{}", i),
src/health/mod.rs-2326-                qualified_id: format!("claude-worker-{}", i),
src/health/mod.rs-2327-                pid: 1000 + i as u32,
src/health/mod.rs:2328:                state: WorkerState::Selecting,
src/health/mod.rs-2329-                current_bead: None,
src/health/mod.rs-2330-                workspace: PathBuf::from("/tmp"),
src/health/mod.rs-2331-                last_heartbeat: now,
src/health/mod.rs-2332-                started_at: now - chrono::Duration::seconds(60),
src/health/mod.rs-2333-                beads_processed: 0,
--
src/health/mod.rs-2369-        let old_time = Utc::now() - chrono::Duration::seconds(600);
src/health/mod.rs-2370-        let hb = HeartbeatData {
src/health/mod.rs-2371-            worker_id: "worker-2".to_string(),
src/health/mod.rs-2372-            qualified_id: "claude-worker-2".to_string(),
src/health/mod.rs-2373-            pid: 2000,
src/health/mod.rs:2374:            state: WorkerState::Selecting,
src/health/mod.rs-2375-            current_bead: None,
src/health/mod.rs-2376-            workspace: PathBuf::from("/tmp"),
src/health/mod.rs-2377-            last_heartbeat: old_time,
src/health/mod.rs-2378-            started_at: old_time,
src/health/mod.rs-2379-            beads_processed: 0,
--
src/health/mod.rs-2415-        let recent_time = now - chrono::Duration::seconds(120);
src/health/mod.rs-2416-        let hb = HeartbeatData {
src/health/mod.rs-2417-            worker_id: "worker-2".to_string(),
src/health/mod.rs-2418-            qualified_id: "claude-worker-2".to_string(),
src/health/mod.rs-2419-            pid: 2000,
src/health/mod.rs:2420:            state: WorkerState::Selecting,
src/health/mod.rs-2421-            current_bead: None,
src/health/mod.rs-2422-            workspace: PathBuf::from("/tmp"),
src/health/mod.rs-2423-            last_heartbeat: now,
src/health/mod.rs-2424-            started_at: recent_time,
src/health/mod.rs-2425-            beads_processed: 0,
--
src/health/mod.rs-2599-        // Create a heartbeat file
src/health/mod.rs-2600-        let hb = HeartbeatData {
src/health/mod.rs-2601-            worker_id: "test-worker".to_string(),
src/health/mod.rs-2602-            qualified_id: "claude-code-glm-5-test-worker".to_string(),
src/health/mod.rs-2603-            pid: std::process::id(),
src/health/mod.rs:2604:            state: WorkerState::Selecting,
src/health/mod.rs-2605-            current_bead: None,
src/health/mod.rs-2606-            workspace: PathBuf::from("/tmp"),
src/health/mod.rs-2607-            last_heartbeat: Utc::now(),
src/health/mod.rs-2608-            started_at: Utc::now(),
src/health/mod.rs-2609-            beads_processed: 0,
--
src/health/mod.rs-3573-    fn is_stale_fresh_heartbeat_not_stale() {
src/health/mod.rs-3574-        let hb = HeartbeatData {
src/health/mod.rs-3575-            worker_id: "test".to_string(),
src/health/mod.rs-3576-            qualified_id: "claude-test".to_string(),
src/health/mod.rs-3577-            pid: 1,
src/health/mod.rs:3578:            state: WorkerState::Selecting,
src/health/mod.rs-3579-            current_bead: None,
src/health/mod.rs-3580-            workspace: PathBuf::from("/tmp"),
src/health/mod.rs-3581-            last_heartbeat: Utc::now() - chrono::Duration::seconds(60), // 1 minute ago
src/health/mod.rs-3582-            started_at: Utc::now(),
src/health/mod.rs-3583-            beads_processed: 0,
--
src/health/mod.rs-3604-    fn is_stale_stale_heartbeat_is_stale() {
src/health/mod.rs-3605-        let hb = HeartbeatData {
src/health/mod.rs-3606-            worker_id: "test".to_string(),
src/health/mod.rs-3607-            qualified_id: "claude-test".to_string(),
src/health/mod.rs-3608-            pid: 1,
src/health/mod.rs:3609:            state: WorkerState::Selecting,
src/health/mod.rs-3610-            current_bead: None,
src/health/mod.rs-3611-            workspace: PathBuf::from("/tmp"),
src/health/mod.rs-3612-            last_heartbeat: Utc::now() - chrono::Duration::seconds(600), // 10 minutes ago
src/health/mod.rs-3613-            started_at: Utc::now(),
src/health/mod.rs-3614-            beads_processed: 0,
--
src/health/mod.rs-3635-    fn is_stale_boundary_at_ttl() {
src/health/mod.rs-3636-        let hb = HeartbeatData {
src/health/mod.rs-3637-            worker_id: "test".to_string(),
src/health/mod.rs-3638-            qualified_id: "claude-test".to_string(),
src/health/mod.rs-3639-            pid: 1,
src/health/mod.rs:3640:            state: WorkerState::Selecting,
src/health/mod.rs-3641-            current_bead: None,
src/health/mod.rs-3642-            workspace: PathBuf::from("/tmp"),
src/health/mod.rs-3643-            last_heartbeat: Utc::now() - chrono::Duration::seconds(300), // exactly 5 minutes ago
src/health/mod.rs-3644-            started_at: Utc::now(),
src/health/mod.rs-3645-            beads_processed: 0,
--
src/health/mod.rs-3684-
src/health/mod.rs-3685-        let hb = HeartbeatData {
src/health/mod.rs-3686-            worker_id: "other-worker".to_string(),
src/health/mod.rs-3687-            qualified_id: qualified_id.to_string(),
src/health/mod.rs-3688-            pid: 2000,
src/health/mod.rs:3689:            state: WorkerState::Executing,
-- agent failure and timeout resolution --
src/dispatch/mod.rs-29-use crate::tsnet::{inject_identity_env, IdentityRegistry, TsnetConfig};
src/dispatch/mod.rs-30-use crate::types::{BeadId, InputMethod, Outcome};
src/dispatch/mod.rs-31-
src/dispatch/mod.rs-32-// ──────────────────────────────────────────────────────────────────────────────
src/dispatch/mod.rs-33-// ExecutionResult
src/dispatch/mod.rs-34-// ──────────────────────────────────────────────────────────────────────────────
src/dispatch/mod.rs-35-
src/dispatch/mod.rs-36-/// Reason why a process was terminated by timeout.
src/dispatch/mod.rs-37-#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
src/dispatch/mod.rs-38-#[serde(rename_all = "snake_case")]
src/dispatch/mod.rs:39:pub enum TimeoutReason {
src/dispatch/mod.rs-40-    /// No stdout/stderr activity for the configured idle timeout duration.
src/dispatch/mod.rs-41-    Idle {
src/dispatch/mod.rs-42-        /// Configured idle timeout in seconds.
src/dispatch/mod.rs-43-        timeout_secs: u64,
src/dispatch/mod.rs-44-        /// Time since last output byte when idle deadline expired.
src/dispatch/mod.rs-45-        last_output_age_secs: u64,
src/dispatch/mod.rs-46-    },
src/dispatch/mod.rs-47-    /// Absolute wall-clock time exceeded the configured hard deadline.
src/dispatch/mod.rs-48-    Hard {
src/dispatch/mod.rs-49-        /// Configured hard timeout in seconds.
--
src/dispatch/mod.rs-65-    pub stdout: String,
src/dispatch/mod.rs-66-    /// Captured stderr.
src/dispatch/mod.rs-67-    pub stderr: String,
src/dispatch/mod.rs-68-    /// Wall-clock time from spawn to exit.
src/dispatch/mod.rs-69-    pub elapsed: Duration,
src/dispatch/mod.rs-70-    /// OS process ID.
src/dispatch/mod.rs-71-    pub pid: u32,
src/dispatch/mod.rs-72-    /// Path to trace directory if trace capture was enabled.
src/dispatch/mod.rs-73-    pub trace_path: Option<std::path::PathBuf>,
src/dispatch/mod.rs-74-    /// Structured reason if terminated by timeout (exit_code 124).
src/dispatch/mod.rs:75:    pub timeout_reason: Option<TimeoutReason>,
src/dispatch/mod.rs-76-}
src/dispatch/mod.rs-77-
src/dispatch/mod.rs-78-// ──────────────────────────────────────────────────────────────────────────────
src/dispatch/mod.rs-79-// Hard deadline timer
src/dispatch/mod.rs-80-// ──────────────────────────────────────────────────────────────────────────────
src/dispatch/mod.rs-81-
src/dispatch/mod.rs-82-/// Hard deadline timer: a non-resettable absolute timeout.
src/dispatch/mod.rs-83-///
src/dispatch/mod.rs-84-/// Created once at process spawn with `Instant::now() + duration`, never modified.
src/dispatch/mod.rs-85-/// Unlike idle timeout (which resets on activity), the hard deadline is a strict
--
src/dispatch/mod.rs-372-            .ok()
src/dispatch/mod.rs-373-    };
src/dispatch/mod.rs-374-
src/dispatch/mod.rs-375-    TokenUsage {
src/dispatch/mod.rs-376-        input_tokens: parse_group(input_group),
src/dispatch/mod.rs-377-        output_tokens: parse_group(output_group),
src/dispatch/mod.rs-378-    }
src/dispatch/mod.rs-379-}
src/dispatch/mod.rs-380-
src/dispatch/mod.rs-381-// ──────────────────────────────────────────────────────────────────────────────
src/dispatch/mod.rs:382:// TimeoutPolicy
src/dispatch/mod.rs-383-// ──────────────────────────────────────────────────────────────────────────────
src/dispatch/mod.rs-384-
src/dispatch/mod.rs-385-/// Which timeout configuration mode is active for an adapter.
src/dispatch/mod.rs-386-///
src/dispatch/mod.rs-387-/// An adapter can use:
src/dispatch/mod.rs-388-/// - Legacy single timeout (`timeout_secs`)
src/dispatch/mod.rs-389-/// - New two-field timeout (`idle_timeout_secs` + `hard_timeout_secs`)
src/dispatch/mod.rs-390-/// - Global config fallback (no adapter-specific timeout set)
src/dispatch/mod.rs-391-#[derive(Debug, Clone, Copy, PartialEq, Eq)]
src/dispatch/mod.rs:392:pub enum TimeoutPolicy {
src/dispatch/mod.rs-393-    /// Legacy single timeout mode (`timeout_secs` is set).
src/dispatch/mod.rs-394-    Legacy,
src/dispatch/mod.rs-395-    /// New two-field timeout mode (at least one of `idle_timeout_secs` or `hard_timeout_secs` is set).
src/dispatch/mod.rs-396-    New {
src/dispatch/mod.rs-397-        /// Whether idle timeout is configured (non-zero).
src/dispatch/mod.rs-398-        idle_enabled: bool,
src/dispatch/mod.rs-399-        /// Whether hard timeout is configured (non-zero).
src/dispatch/mod.rs-400-        hard_enabled: bool,
src/dispatch/mod.rs-401-    },
src/dispatch/mod.rs-402-    /// No adapter-specific timeout; uses global config default.
--
src/dispatch/mod.rs-423-    /// How the prompt is delivered to the agent.
src/dispatch/mod.rs-424-    #[serde(default = "default_input_method")]
src/dispatch/mod.rs-425-    pub input_method: InputMethod,
src/dispatch/mod.rs-426-    /// Shell command template with variable placeholders.
src/dispatch/mod.rs-427-    ///
src/dispatch/mod.rs-428-    /// Supported variables: `{workspace}`, `{prompt_file}`, `{bead_id}`, `{model}`.
src/dispatch/mod.rs-429-    pub invoke_template: String,
src/dispatch/mod.rs-430-    /// Extra environment variables set for the agent process.
src/dispatch/mod.rs-431-    #[serde(default)]
src/dispatch/mod.rs-432-    pub environment: HashMap<String, String>,
src/dispatch/mod.rs:433:    /// Timeout in seconds (0 = use global config timeout).
src/dispatch/mod.rs-434-    #[serde(default)]
src/dispatch/mod.rs-435-    pub timeout_secs: u64,
src/dispatch/mod.rs-436-    /// Idle timeout in seconds (0 = no idle deadline).
src/dispatch/mod.rs-437-    ///
src/dispatch/mod.rs-438-    /// When implemented, will kill the agent if no stdout/stderr activity
src/dispatch/mod.rs-439-    /// is detected for this duration. This is a data-model-only field;
src/dispatch/mod.rs-440-    /// validation and enforcement logic will be added in a follow-up.
src/dispatch/mod.rs-441-    #[serde(default)]
src/dispatch/mod.rs-442-    pub idle_timeout_secs: u64,
src/dispatch/mod.rs-443-    /// Hard timeout in seconds (0 = no hard deadline).
--
src/dispatch/mod.rs-504-    /// the AI system/platform (e.g., "anthropic", "openai", "local").
src/dispatch/mod.rs-505-    /// Defaults to "local" if no provider is configured.
src/dispatch/mod.rs-506-    pub fn gen_ai_system(&self) -> &str {
src/dispatch/mod.rs-507-        self.provider.as_deref().unwrap_or("local")
src/dispatch/mod.rs-508-    }
src/dispatch/mod.rs-509-
src/dispatch/mod.rs-510-    /// Active timeout policy for this adapter.
src/dispatch/mod.rs-511-    ///
src/dispatch/mod.rs-512-    /// Distinguishes between legacy single timeout and the new two-field
src/dispatch/mod.rs-513-    /// (idle + hard) timeout model.
src/dispatch/mod.rs:514:    pub fn timeout_policy(&self) -> TimeoutPolicy {
src/dispatch/mod.rs-515-        let has_legacy = self.timeout_secs > 0;
src/dispatch/mod.rs-516-        let has_idle = self.idle_timeout_secs > 0;
src/dispatch/mod.rs-517-        let has_hard = self.hard_timeout_secs > 0;
src/dispatch/mod.rs-518-
src/dispatch/mod.rs-519-        if has_legacy {
src/dispatch/mod.rs:520:            TimeoutPolicy::Legacy
src/dispatch/mod.rs-521-        } else if has_idle || has_hard {
src/dispatch/mod.rs:522:            TimeoutPolicy::New {
src/dispatch/mod.rs-523-                idle_enabled: has_idle,
src/dispatch/mod.rs-524-                hard_enabled: has_hard,
src/dispatch/mod.rs-525-            }
src/dispatch/mod.rs-526-        } else {
src/dispatch/mod.rs:527:            TimeoutPolicy::Global
src/dispatch/mod.rs-528-        }
src/dispatch/mod.rs-529-    }
src/dispatch/mod.rs-530-
src/dispatch/mod.rs-531-    /// Human-readable description of the active timeout policy.
src/dispatch/mod.rs-532-    ///
src/dispatch/mod.rs-533-    /// Shows which timeout mode is active and the configured values.
src/dispatch/mod.rs-534-    pub fn timeout_description(&self, global_timeout_secs: u64) -> String {
src/dispatch/mod.rs-535-        match self.timeout_policy() {
src/dispatch/mod.rs:536:            TimeoutPolicy::Legacy => {
src/dispatch/mod.rs-537-                format!("legacy: {}s (single timeout)", self.timeout_secs)
src/dispatch/mod.rs-538-            }
src/dispatch/mod.rs:539:            TimeoutPolicy::New {
src/dispatch/mod.rs-540-                idle_enabled,
src/dispatch/mod.rs-541-                hard_enabled,
src/dispatch/mod.rs-542-            } => {
src/dispatch/mod.rs-543-                let mut parts = Vec::new();
src/dispatch/mod.rs-544-                parts.push("new:".to_string());
src/dispatch/mod.rs-545-                if idle_enabled {
src/dispatch/mod.rs-546-                    parts.push(format!("idle={}s", self.idle_timeout_secs));
src/dispatch/mod.rs-547-                }
src/dispatch/mod.rs-548-                if hard_enabled {
src/dispatch/mod.rs-549-                    parts.push(format!("hard={}s", self.hard_timeout_secs));
src/dispatch/mod.rs-550-                }
src/dispatch/mod.rs-551-                if !idle_enabled && !hard_enabled {
src/dispatch/mod.rs-552-                    parts.push("both disabled".to_string());
src/dispatch/mod.rs-553-                }
src/dispatch/mod.rs-554-                parts.join(" ")
src/dispatch/mod.rs-555-            }
src/dispatch/mod.rs:556:            TimeoutPolicy::Global => {
src/dispatch/mod.rs-557-                if global_timeout_secs > 0 {
src/dispatch/mod.rs-558-                    format!("global: {}s (from config)", global_timeout_secs)
src/dispatch/mod.rs-559-                } else {
src/dispatch/mod.rs-560-                    "global: unlimited (0 = no timeout)".to_string()
src/dispatch/mod.rs-561-                }
src/dispatch/mod.rs-562-            }
src/dispatch/mod.rs-563-        }
src/dispatch/mod.rs-564-    }
src/dispatch/mod.rs-565-
src/dispatch/mod.rs-566-    /// Validate timeout field mutual exclusivity.
--
src/dispatch/mod.rs-1089-                    bead_id: bead_id.clone(),
src/dispatch/mod.rs-1090-                    exit_code: exec.exit_code,
src/dispatch/mod.rs-1091-                    duration_ms: exec.elapsed.as_millis() as u64,
src/dispatch/mod.rs-1092-                    agent: agent_name,
src/dispatch/mod.rs-1093-                    model: agent_model,
src/dispatch/mod.rs-1094-                    provider: agent_provider,
src/dispatch/mod.rs-1095-                });
src/dispatch/mod.rs-1096-
src/dispatch/mod.rs-1097-                // Emit structured timeout event if applicable.
src/dispatch/mod.rs-1098-                if let Some(ref reason) = exec.timeout_reason {
src/dispatch/mod.rs:1099:                    let _ = self.telemetry.emit(EventKind::AgentTimeout {
src/dispatch/mod.rs-1100-                        bead_id: bead_id.clone(),
src/dispatch/mod.rs-1101-                        reason: reason.clone(),
src/dispatch/mod.rs-1102-                    });
src/dispatch/mod.rs-1103-                }
src/dispatch/mod.rs-1104-            }
src/dispatch/mod.rs-1105-            Err(_) => {
src/dispatch/mod.rs-1106-                // Set span status on error
src/dispatch/mod.rs-1107-                tracing::Span::current().record("otel.status_code", 2u64);
src/dispatch/mod.rs-1108-                tracing::Span::current().record("otel.status_description", "dispatch_error");
src/dispatch/mod.rs-1109-
--
src/dispatch/mod.rs-1487-                        let _ = activity_tx.send(Instant::now());
src/dispatch/mod.rs-1488-                    }
src/dispatch/mod.rs-1489-                }
src/dispatch/mod.rs-1490-                drop(activity_tx);
src/dispatch/mod.rs-1491-                String::from_utf8_lossy(&buf).into_owned()
src/dispatch/mod.rs-1492-            }
src/dispatch/mod.rs-1493-        });
src/dispatch/mod.rs-1494-
src/dispatch/mod.rs-1495-        // Compute timeout configuration based on adapter policy.
src/dispatch/mod.rs-1496-        let (idle_dur, hard_dur, use_legacy) = match adapter.timeout_policy() {
src/dispatch/mod.rs:1497:            TimeoutPolicy::Legacy => {
src/dispatch/mod.rs-1498-                // Legacy mode: single timeout, treated as both idle and hard.
src/dispatch/mod.rs-1499-                let legacy_dur = adapter.effective_timeout(self.global_timeout_secs);
src/dispatch/mod.rs-1500-                (legacy_dur, legacy_dur, true)
src/dispatch/mod.rs-1501-            }
src/dispatch/mod.rs:1502:            TimeoutPolicy::New {
src/dispatch/mod.rs-1503-                idle_enabled,
src/dispatch/mod.rs-1504-                hard_enabled,
src/dispatch/mod.rs-1505-            } => {
src/dispatch/mod.rs-1506-                // New mode: separate idle and hard timeouts.
src/dispatch/mod.rs-1507-                let idle = if idle_enabled {
src/dispatch/mod.rs-1508-                    Duration::from_secs(adapter.idle_timeout_secs)
src/dispatch/mod.rs-1509-                } else {
src/dispatch/mod.rs-1510-                    Duration::ZERO
src/dispatch/mod.rs-1511-                };
src/dispatch/mod.rs-1512-                let hard = if hard_enabled {
src/dispatch/mod.rs-1513-                    Duration::from_secs(adapter.hard_timeout_secs)
src/dispatch/mod.rs-1514-                } else {
src/dispatch/mod.rs-1515-                    Duration::ZERO
src/dispatch/mod.rs-1516-                };
src/dispatch/mod.rs-1517-                (idle, hard, false)
src/dispatch/mod.rs-1518-            }
src/dispatch/mod.rs:1519:            TimeoutPolicy::Global => {
src/dispatch/mod.rs-1520-                // No adapter-specific timeout; fall back to global config.
src/dispatch/mod.rs-1521-                let global_dur = Duration::from_secs(self.global_timeout_secs);
src/dispatch/mod.rs-1522-                (global_dur, global_dur, global_dur.is_zero())
src/dispatch/mod.rs-1523-            }
src/dispatch/mod.rs-1524-        };
src/dispatch/mod.rs-1525-
src/dispatch/mod.rs-1526-        // Determine which deadlines are active.
src/dispatch/mod.rs-1527-        let has_idle_deadline = !idle_dur.is_zero();
src/dispatch/mod.rs-1528-        let has_hard_deadline = !hard_dur.is_zero();
src/dispatch/mod.rs-1529-        let has_any_deadline = has_idle_deadline || has_hard_deadline;
--
src/dispatch/mod.rs-1557-                Some(tokio::time::Instant::now() + hard_dur)
src/dispatch/mod.rs-1558-            } else {
src/dispatch/mod.rs-1559-                None
src/dispatch/mod.rs-1560-            };
src/dispatch/mod.rs-1561-
src/dispatch/mod.rs-1562-            // Activity receiver for idle deadline resets.
src/dispatch/mod.rs-1563-            let mut activity_rcv = activity_rx;
src/dispatch/mod.rs-1564-
src/dispatch/mod.rs-1565-            // Outcome: (exit_code, timeout_reason).
src/dispatch/mod.rs-1566-            // We loop to handle idle deadline resets on activity.
src/dispatch/mod.rs:1567:            let outcome: (i32, Option<TimeoutReason>) = loop {
src/dispatch/mod.rs-1568-                // Determine the next deadline to wait for (if any).
src/dispatch/mod.rs-1569-                let next_idle = idle_deadline;
src/dispatch/mod.rs-1570-                let next_hard = hard_deadline;
src/dispatch/mod.rs-1571-                let _next_deadline = match (next_idle, next_hard) {
src/dispatch/mod.rs-1572-                    (Some(idle), Some(hard)) => Some(idle.min(hard)),
src/dispatch/mod.rs-1573-                    (Some(idle), None) => Some(idle),
src/dispatch/mod.rs-1574-                    (None, Some(hard)) => Some(hard),
src/dispatch/mod.rs-1575-                    (None, None) => None,
src/dispatch/mod.rs-1576-                };
src/dispatch/mod.rs-1577-
--
src/dispatch/mod.rs-1607-                                libc::killpg(pid as libc::pid_t, libc::SIGKILL);
src/dispatch/mod.rs-1608-                            }
src/dispatch/mod.rs-1609-                        }
src/dispatch/mod.rs-1610-                        let _ = guard.get_mut().and_then(|c| c.start_kill().ok());
src/dispatch/mod.rs-1611-                        let _ = guard.wait().await;
src/dispatch/mod.rs-1612-                        kill_guard.disarm();
src/dispatch/mod.rs-1613-
src/dispatch/mod.rs-1614-                        // Compute last output age for telemetry.
src/dispatch/mod.rs-1615-                        let last_output_age = activity_rcv.borrow().elapsed();
src/dispatch/mod.rs-1616-                        let reason = if use_legacy {
src/dispatch/mod.rs:1617:                            TimeoutReason::Legacy {
src/dispatch/mod.rs-1618-                                timeout_secs: idle_dur.as_secs(),
src/dispatch/mod.rs-1619-                            }
src/dispatch/mod.rs-1620-                        } else {
src/dispatch/mod.rs:1621:                            TimeoutReason::Idle {
src/dispatch/mod.rs-1622-                                timeout_secs: idle_dur.as_secs(),
src/dispatch/mod.rs-1623-                                last_output_age_secs: last_output_age.as_secs(),
src/dispatch/mod.rs-1624-                            }
src/dispatch/mod.rs-1625-                        };
src/dispatch/mod.rs-1626-                        break (124, Some(reason));
src/dispatch/mod.rs-1627-                    }
src/dispatch/mod.rs-1628-
src/dispatch/mod.rs-1629-                    // Branch 4: hard deadline expired
src/dispatch/mod.rs-1630-                    () = async {
src/dispatch/mod.rs-1631-                        if let Some(deadline) = next_hard {
--
src/dispatch/mod.rs-1637-                        // Hard timeout: kill the process group.
src/dispatch/mod.rs-1638-                        if pid > 0 {
src/dispatch/mod.rs-1639-                            unsafe {
src/dispatch/mod.rs-1640-                                libc::killpg(pid as libc::pid_t, libc::SIGKILL);
src/dispatch/mod.rs-1641-                            }
src/dispatch/mod.rs-1642-                        }
src/dispatch/mod.rs-1643-                        let _ = guard.get_mut().and_then(|c| c.start_kill().ok());
src/dispatch/mod.rs-1644-                        let _ = guard.wait().await;
src/dispatch/mod.rs-1645-                        kill_guard.disarm();
src/dispatch/mod.rs-1646-
src/dispatch/mod.rs:1647:                        let reason = TimeoutReason::Hard {
src/dispatch/mod.rs-1648-                            timeout_secs: hard_dur.as_secs(),
src/dispatch/mod.rs-1649-                        };
src/dispatch/mod.rs-1650-                        break (124, Some(reason));
src/dispatch/mod.rs-1651-                    }
src/dispatch/mod.rs-1652-                }
src/dispatch/mod.rs-1653-            };
src/dispatch/mod.rs-1654-
src/dispatch/mod.rs-1655-            outcome
src/dispatch/mod.rs-1656-        };
src/dispatch/mod.rs-1657-
--
src/dispatch/mod.rs-2022-/// Result of validating an agent adapter.
src/dispatch/mod.rs-2023-#[derive(Debug)]
src/dispatch/mod.rs-2024-pub struct AgentTestResult {
src/dispatch/mod.rs-2025-    pub adapter_name: String,
src/dispatch/mod.rs-2026-    pub cli_path: Option<String>,
src/dispatch/mod.rs-2027-    pub version: Option<String>,
src/dispatch/mod.rs-2028-    pub input_method: String,
src/dispatch/mod.rs-2029-    pub probe_result: Option<ProbeResult>,
src/dispatch/mod.rs-2030-    pub token_extraction_ok: Option<bool>,
src/dispatch/mod.rs-2031-    pub output_transform_ok: Option<bool>,
src/dispatch/mod.rs:2032:    pub timeout_policy: TimeoutPolicy,
src/dispatch/mod.rs-2033-    pub timeout_description: String,
src/dispatch/mod.rs-2034-    pub status: AgentTestStatus,
src/dispatch/mod.rs-2035-    pub errors: Vec<String>,
src/dispatch/mod.rs-2036-}
src/dispatch/mod.rs-2037-
src/dispatch/mod.rs-2038-/// Probe execution result.
src/dispatch/mod.rs-2039-#[derive(Debug)]
src/dispatch/mod.rs-2040-pub struct ProbeResult {
src/dispatch/mod.rs-2041-    pub exit_code: i32,
src/dispatch/mod.rs-2042-    pub elapsed_ms: u64,
--
src/dispatch/mod.rs-2284-    println!("Adapter: {}", result.adapter_name);
src/dispatch/mod.rs-2285-    match &result.cli_path {
src/dispatch/mod.rs-2286-        Some(path) => println!("CLI:     {} (found at {path})", result.adapter_name),
src/dispatch/mod.rs-2287-        None => println!("CLI:     NOT FOUND"),
src/dispatch/mod.rs-2288-    }
src/dispatch/mod.rs-2289-    match &result.version {
src/dispatch/mod.rs-2290-        Some(v) => println!("Version: {v}"),
src/dispatch/mod.rs-2291-        None => println!("Version: unknown"),
src/dispatch/mod.rs-2292-    }
src/dispatch/mod.rs-2293-    println!("Input:   {}", result.input_method);
src/dispatch/mod.rs:2294:    println!("Timeout: {}", result.timeout_description);
src/dispatch/mod.rs-2295-    match &result.probe_result {
src/dispatch/mod.rs-2296-        Some(pr) => println!("Probe:   exit {} ({}ms)", pr.exit_code, pr.elapsed_ms),
src/dispatch/mod.rs-2297-        None => println!("Probe:   skipped"),
src/dispatch/mod.rs-2298-    }
src/dispatch/mod.rs-2299-    match result.token_extraction_ok {
src/dispatch/mod.rs-2300-        Some(true) => println!("Tokens:  extraction working"),
src/dispatch/mod.rs-2301-        Some(false) => println!("Tokens:  extraction FAILED"),
src/dispatch/mod.rs-2302-        None => println!("Tokens:  none configured"),
src/dispatch/mod.rs-2303-    }
src/dispatch/mod.rs-2304-    match result.output_transform_ok {
--
src/dispatch/mod.rs-2548-
src/dispatch/mod.rs-2549-    #[test]
src/dispatch/mod.rs-2550-    fn effective_timeout_zero_when_both_zero() {
src/dispatch/mod.rs-2551-        let adapter = AgentAdapter {
src/dispatch/mod.rs-2552-            timeout_secs: 0,
src/dispatch/mod.rs-2553-            ..builtin_generic()
src/dispatch/mod.rs-2554-        };
src/dispatch/mod.rs-2555-        assert_eq!(adapter.effective_timeout(0), Duration::ZERO);
src/dispatch/mod.rs-2556-    }
src/dispatch/mod.rs-2557-
src/dispatch/mod.rs:2558:    // ── Timeout field validation (legacy vs new mutual exclusivity) ──
src/dispatch/mod.rs-2559-
src/dispatch/mod.rs-2560-    #[test]
src/dispatch/mod.rs-2561-    fn validate_timeouts_timeout_secs_alone_valid() {
src/dispatch/mod.rs-2562-        // Valid: legacy timeout_secs alone (backward compatibility)
src/dispatch/mod.rs-2563-        let adapter = AgentAdapter {
src/dispatch/mod.rs-2564-            name: "test-legacy".to_string(),
src/dispatch/mod.rs-2565-            timeout_secs: 3600,
src/dispatch/mod.rs-2566-            idle_timeout_secs: 0,
src/dispatch/mod.rs-2567-            hard_timeout_secs: 0,
src/dispatch/mod.rs-2568-            ..test_adapter("test-legacy", "test template")
--
src/dispatch/mod.rs-3475-            .dispatch(
src/dispatch/mod.rs-3476-                &BeadId::from("nd-exit1"),
src/dispatch/mod.rs-3477-                &test_prompt("t"),
src/dispatch/mod.rs-3478-                &adapter,
src/dispatch/mod.rs-3479-                Path::new("/tmp"),
src/dispatch/mod.rs-3480-            )
src/dispatch/mod.rs-3481-            .await
src/dispatch/mod.rs-3482-            .unwrap();
src/dispatch/mod.rs-3483-
src/dispatch/mod.rs-3484-        assert_eq!(result.exit_code, 1);
src/dispatch/mod.rs:3485:        assert_eq!(Outcome::classify(result.exit_code, false), Outcome::Failure);
src/dispatch/mod.rs-3486-    }
src/dispatch/mod.rs-3487-
src/dispatch/mod.rs-3488-    #[tokio::test]
src/dispatch/mod.rs-3489-    async fn e2e_exit_code_2_is_failure() {
src/dispatch/mod.rs-3490-        use crate::types::Outcome;
src/dispatch/mod.rs-3491-
src/dispatch/mod.rs-3492-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3493-        adapters.insert("f2".to_string(), test_adapter("f2", "exit 2"));
src/dispatch/mod.rs-3494-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs-3495-        let adapter = dispatcher.adapter("f2").unwrap().clone();
--
src/dispatch/mod.rs-3498-            .dispatch(
src/dispatch/mod.rs-3499-                &BeadId::from("nd-exit2"),
src/dispatch/mod.rs-3500-                &test_prompt("t"),
src/dispatch/mod.rs-3501-                &adapter,
src/dispatch/mod.rs-3502-                Path::new("/tmp"),
src/dispatch/mod.rs-3503-            )
src/dispatch/mod.rs-3504-            .await
src/dispatch/mod.rs-3505-            .unwrap();
src/dispatch/mod.rs-3506-
src/dispatch/mod.rs-3507-        assert_eq!(result.exit_code, 2);
src/dispatch/mod.rs:3508:        assert_eq!(Outcome::classify(result.exit_code, false), Outcome::Failure);
src/dispatch/mod.rs-3509-    }
src/dispatch/mod.rs-3510-
src/dispatch/mod.rs-3511-    #[tokio::test]
src/dispatch/mod.rs-3512-    async fn e2e_exit_code_137_is_crash() {
src/dispatch/mod.rs-3513-        use crate::types::Outcome;
src/dispatch/mod.rs-3514-
src/dispatch/mod.rs-3515-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3516-        adapters.insert("crash".to_string(), test_adapter("crash", "exit 137"));
src/dispatch/mod.rs-3517-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs-3518-        let adapter = dispatcher.adapter("crash").unwrap().clone();
--
src/dispatch/mod.rs-3866-        let adapter = builtin_codex();
src/dispatch/mod.rs-3867-        assert_eq!(adapter.gen_ai_system(), "openai");
src/dispatch/mod.rs-3868-    }
src/dispatch/mod.rs-3869-
src/dispatch/mod.rs-3870-    #[test]
src/dispatch/mod.rs-3871-    fn gen_ai_system_returns_local_for_adapter_without_provider() {
src/dispatch/mod.rs-3872-        let adapter = builtin_opencode();
src/dispatch/mod.rs-3873-        assert_eq!(adapter.gen_ai_system(), "local");
src/dispatch/mod.rs-3874-    }
src/dispatch/mod.rs-3875-
src/dispatch/mod.rs:3876:    // ── Timeout policy tests ──
src/dispatch/mod.rs-3877-
src/dispatch/mod.rs-3878-    #[test]
src/dispatch/mod.rs-3879-    fn timeout_policy_returns_legacy_for_legacy_timeout() {
src/dispatch/mod.rs-3880-        let adapter = AgentAdapter {
src/dispatch/mod.rs-3881-            timeout_secs: 3600,
src/dispatch/mod.rs-3882-            idle_timeout_secs: 0,
src/dispatch/mod.rs-3883-            hard_timeout_secs: 0,
src/dispatch/mod.rs-3884-            ..test_adapter("test", "test")
src/dispatch/mod.rs-3885-        };
src/dispatch/mod.rs:3886:        assert_eq!(adapter.timeout_policy(), TimeoutPolicy::Legacy);
src/dispatch/mod.rs-3887-    }
src/dispatch/mod.rs-3888-
src/dispatch/mod.rs-3889-    #[test]
src/dispatch/mod.rs-3890-    fn timeout_policy_returns_new_for_idle_only() {
src/dispatch/mod.rs-3891-        let adapter = AgentAdapter {
src/dispatch/mod.rs-3892-            timeout_secs: 0,
src/dispatch/mod.rs-3893-            idle_timeout_secs: 600,
src/dispatch/mod.rs-3894-            hard_timeout_secs: 0,
src/dispatch/mod.rs-3895-            ..test_adapter("test", "test")
src/dispatch/mod.rs-3896-        };
src/dispatch/mod.rs-3897-        assert!(matches!(
src/dispatch/mod.rs-3898-            adapter.timeout_policy(),
src/dispatch/mod.rs:3899:            TimeoutPolicy::New {
src/dispatch/mod.rs-3900-                idle_enabled: true,
src/dispatch/mod.rs-3901-                hard_enabled: false
src/dispatch/mod.rs-3902-            }
src/dispatch/mod.rs-3903-        ));
src/dispatch/mod.rs-3904-    }
src/dispatch/mod.rs-3905-
src/dispatch/mod.rs-3906-    #[test]
src/dispatch/mod.rs-3907-    fn timeout_policy_returns_new_for_hard_only() {
src/dispatch/mod.rs-3908-        let adapter = AgentAdapter {
src/dispatch/mod.rs-3909-            timeout_secs: 0,
src/dispatch/mod.rs-3910-            idle_timeout_secs: 0,
src/dispatch/mod.rs-3911-            hard_timeout_secs: 7200,
src/dispatch/mod.rs-3912-            ..test_adapter("test", "test")
src/dispatch/mod.rs-3913-        };
src/dispatch/mod.rs-3914-        assert!(matches!(
src/dispatch/mod.rs-3915-            adapter.timeout_policy(),
src/dispatch/mod.rs:3916:            TimeoutPolicy::New {
src/dispatch/mod.rs-3917-                idle_enabled: false,
src/dispatch/mod.rs-3918-                hard_enabled: true
src/dispatch/mod.rs-3919-            }
src/dispatch/mod.rs-3920-        ));
src/dispatch/mod.rs-3921-    }
src/dispatch/mod.rs-3922-
src/dispatch/mod.rs-3923-    #[test]
src/dispatch/mod.rs-3924-    fn timeout_policy_returns_new_for_both_timeouts() {
src/dispatch/mod.rs-3925-        let adapter = AgentAdapter {
src/dispatch/mod.rs-3926-            timeout_secs: 0,
src/dispatch/mod.rs-3927-            idle_timeout_secs: 600,
src/dispatch/mod.rs-3928-            hard_timeout_secs: 7200,
src/dispatch/mod.rs-3929-            ..test_adapter("test", "test")
src/dispatch/mod.rs-3930-        };
src/dispatch/mod.rs-3931-        assert!(matches!(
src/dispatch/mod.rs-3932-            adapter.timeout_policy(),
src/dispatch/mod.rs:3933:            TimeoutPolicy::New {
src/dispatch/mod.rs-3934-                idle_enabled: true,
src/dispatch/mod.rs-3935-                hard_enabled: true
src/dispatch/mod.rs-3936-            }
src/dispatch/mod.rs-3937-        ));
src/dispatch/mod.rs-3938-    }
src/dispatch/mod.rs-3939-
src/dispatch/mod.rs-3940-    #[test]
src/dispatch/mod.rs-3941-    fn timeout_policy_returns_global_when_all_zero() {
src/dispatch/mod.rs-3942-        let adapter = AgentAdapter {
src/dispatch/mod.rs-3943-            timeout_secs: 0,
src/dispatch/mod.rs-3944-            idle_timeout_secs: 0,
src/dispatch/mod.rs-3945-            hard_timeout_secs: 0,
src/dispatch/mod.rs-3946-            ..test_adapter("test", "test")
src/dispatch/mod.rs-3947-        };
src/dispatch/mod.rs:3948:        assert_eq!(adapter.timeout_policy(), TimeoutPolicy::Global);
src/dispatch/mod.rs-3949-    }
src/dispatch/mod.rs-3950-
src/dispatch/mod.rs-3951-    #[test]
src/dispatch/mod.rs-3952-    fn timeout_description_shows_legacy_mode() {
src/dispatch/mod.rs-3953-        let adapter = AgentAdapter {
src/dispatch/mod.rs-3954-            timeout_secs: 3600,
src/dispatch/mod.rs-3955-            idle_timeout_secs: 0,
src/dispatch/mod.rs-3956-            hard_timeout_secs: 0,
src/dispatch/mod.rs-3957-            ..test_adapter("test", "test")
src/dispatch/mod.rs-3958-        };
--
src/dispatch/mod.rs-4238-
src/dispatch/mod.rs-4239-        assert_eq!(
src/dispatch/mod.rs-4240-            result.exit_code, 124,
src/dispatch/mod.rs-4241-            "idle timeout should return exit code 124"
src/dispatch/mod.rs-4242-        );
src/dispatch/mod.rs-4243-        assert!(
src/dispatch/mod.rs-4244-            result.timeout_reason.is_some(),
src/dispatch/mod.rs-4245-            "should have timeout reason"
src/dispatch/mod.rs-4246-        );
src/dispatch/mod.rs-4247-        match result.timeout_reason {
src/dispatch/mod.rs:4248:            Some(TimeoutReason::Idle { timeout_secs, .. }) => {
src/dispatch/mod.rs-4249-                assert_eq!(timeout_secs, 1);
src/dispatch/mod.rs-4250-            }
src/dispatch/mod.rs-4251-            _ => panic!(
src/dispatch/mod.rs-4252-                "expected Idle timeout reason, got {:?}",
src/dispatch/mod.rs-4253-                result.timeout_reason
src/dispatch/mod.rs-4254-            ),
src/dispatch/mod.rs-4255-        }

exec
/bin/bash -lc "printf '%s\\n' '-- bf machine-readable contract references --'
/home/coding/backups/bf-tooling-removed-20260814/bf robot-docs --workspace /home/coding/.needle/home/coding --format json | rg -n -i -C 3 'status|open|in_progress|blocked|closed|completed|pending|ready' | sed -n '1,400p'
printf '%s\\n' '-- worker state enum --'
rg -n -C 12 'enum WorkerState' src
type_path="'$(rg -l '"'enum WorkerState' src | head -n 1)
test -n \""'$type_path" && sed -n '"'1,260p' \""'$type_path"
printf '"'%s\\n' '-- outcome resolution policy --'
sed -n '1,360p' src/outcome/mod.rs
printf '%s\\n' '-- canary source contradictions --'
rg -n -C 5 'default_(final|failure|timeout)_status|state_transitions: Vec::new|Timeout.*deferred|Failure.*open|Success.*closed' src/canary/mod.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
-- bf machine-readable contract references --
13-        "name": "kind",
14-        "type": "string",
15-        "required": true,
16:        "description": "Command identifier (e.g., 'list', 'ready', 'claim')."
17-      },
18-      {
19-        "name": "data",
--
57-    {
58-      "command": "list",
59-      "description": "List beads in the workspace.",
60:      "example": "bf list --status open --json",
61-      "data_shape": {
62-        "type": "Array"
63-      },
--
74-      ]
75-    },
76-    {
77:      "command": "ready",
78:      "description": "Show ready (unblocked) beads ranked by impact.",
79:      "example": "bf ready --limit 10 --json",
80-      "data_shape": {
81-        "type": "Array"
82-      },
--
161-    {
162-      "command": "update",
163-      "description": "Update one or more fields of a bead.",
164:      "example": "bf update bf-123 --status in_progress --json",
165-      "data_shape": {
166-        "type": "Structured"
167-      },
--
183-    },
184-    {
185-      "command": "close",
186:      "description": "Mark a bead as closed.",
187:      "example": "bf close bf-123 --reason 'Completed' --json",
188-      "data_shape": {
189-        "type": "Structured"
190-      },
191-      "data_schema": {
192-        "properties": {
193-          "id": {
194:            "description": "The closed bead ID",
195-            "type": "string"
196-          }
197-        },
--
205-      ]
206-    },
207-    {
208:      "command": "reopen",
209:      "description": "Reopen a closed bead.",
210:      "example": "bf reopen bf-123 --json",
211-      "data_shape": {
212-        "type": "Structured"
213-      },
214-      "data_schema": {
215-        "properties": {
216-          "id": {
217:            "description": "The reopened bead ID",
218-            "type": "string"
219-          }
220-        },
--
275-            "description": "Counts by issue type (present with --by-type)",
276-            "type": "object"
277-          },
278:          "closed": {
279-            "type": "integer"
280-          },
281:          "in_progress": {
282-            "type": "integer"
283-          },
284:          "open": {
285-            "type": "integer"
286-          },
287-          "total": {
--
290-        },
291-        "required": [
292-          "total",
293:          "open",
294:          "in_progress",
295:          "closed"
296-        ],
297-        "type": "object"
298-      },
--
313-            "assignee": {
314-              "type": "string"
315-            },
316:            "completed_24h": {
317-              "type": "integer"
318-            },
319:            "completed_30d": {
320-              "type": "integer"
321-            },
322:            "completed_7d": {
323-              "type": "integer"
324-            }
325-          },
--
334-    {
335-      "command": "search",
336-      "description": "Search beads by query text and filters.",
337:      "example": "bf search --query 'bug' --status open --json",
338-      "data_shape": {
339-        "type": "Array"
340-      },
--
441-        "type": "Array"
442-      },
443-      "data_schema": {
444:        "description": "Array of {id, title, status, priority, float} objects representing beads on the critical path",
445-        "items": {
446-          "type": "object"
447-        },
-- worker state enum --
src/types/mod.rs-158-
src/types/mod.rs-159-// ──────────────────────────────────────────────────────────────────────────────
src/types/mod.rs-160-// WorkerState
src/types/mod.rs-161-// ──────────────────────────────────────────────────────────────────────────────
src/types/mod.rs-162-
src/types/mod.rs-163-/// Worker finite-state-machine states.
src/types/mod.rs-164-///
src/types/mod.rs-165-/// Every state has defined entry conditions, actions, and exit transitions.
src/types/mod.rs-166-/// There are no implicit states or fallthrough paths.
src/types/mod.rs-167-#[non_exhaustive]
src/types/mod.rs-168-#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
src/types/mod.rs-169-#[serde(rename_all = "SCREAMING_SNAKE_CASE")]
src/types/mod.rs:170:pub enum WorkerState {
src/types/mod.rs-171-    Booting,
src/types/mod.rs-172-    Selecting,
src/types/mod.rs-173-    Claiming,
src/types/mod.rs-174-    Retrying,
src/types/mod.rs-175-    Building,
src/types/mod.rs-176-    Dispatching,
src/types/mod.rs-177-    Executing,
src/types/mod.rs-178-    Handling,
src/types/mod.rs-179-    Logging,
src/types/mod.rs-180-    /// All strands returned empty — worker has nothing to do.
src/types/mod.rs-181-    Exhausted,
src/types/mod.rs-182-    /// Received graceful shutdown signal.
//! Core types and enums for NEEDLE.
//!
//! This is a leaf module — it depends on nothing else in the crate.
//! Enums that may gain variants in the future are marked `#[non_exhaustive]`.
//!
//! Design invariant: no wildcard (`_`) arms in any `match` on these enums.
//! Every variant must be explicitly handled at every call site.

use std::fmt;
use std::ops::Deref;
use std::str::FromStr;
use std::time::Duration;

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};

// ──────────────────────────────────────────────────────────────────────────────
// BeadId newtype
// ──────────────────────────────────────────────────────────────────────────────

/// A validated bead identifier (e.g., `needle-gob`).
///
/// Wraps `String` with `Display`, `FromStr`, `Hash`, and `Eq` impls.
#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]
#[serde(transparent)]
pub struct BeadId(String);

impl fmt::Display for BeadId {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        f.write_str(&self.0)
    }
}

impl FromStr for BeadId {
    type Err = std::convert::Infallible;

    fn from_str(s: &str) -> Result<Self, Self::Err> {
        Ok(BeadId(s.to_owned()))
    }
}

impl From<String> for BeadId {
    fn from(s: String) -> Self {
        BeadId(s)
    }
}

impl From<&str> for BeadId {
    fn from(s: &str) -> Self {
        BeadId(s.to_owned())
    }
}

impl From<BeadId> for String {
    fn from(id: BeadId) -> Self {
        id.0
    }
}

impl AsRef<str> for BeadId {
    fn as_ref(&self) -> &str {
        &self.0
    }
}

impl Deref for BeadId {
    type Target = str;

    fn deref(&self) -> &Self::Target {
        &self.0
    }
}

// ──────────────────────────────────────────────────────────────────────────────
// WorkerId type alias
// ──────────────────────────────────────────────────────────────────────────────

/// Identifies a worker instance (e.g., `needle-01`).
pub type WorkerId = String;

// ──────────────────────────────────────────────────────────────────────────────
// Priority type alias
// ──────────────────────────────────────────────────────────────────────────────

/// Priority level of a bead. Lower number = higher priority (P1 > P2 > P3).
pub type Priority = u8;

// ──────────────────────────────────────────────────────────────────────────────
// HardDeadline type alias
// ──────────────────────────────────────────────────────────────────────────────

/// Hard deadline - absolute wall-clock timeout from process start (in seconds).
///
/// A hard deadline is a **non-resettable, absolute timeout** that starts counting
/// from the moment the agent process is spawned. Unlike idle timeout (which is
/// reset on any stdout/stderr activity), the hard deadline is a strict upper
/// bound on total execution time regardless of process activity.
///
/// **Key characteristics:**
/// - **Absolute**: Measured from process spawn time, not last activity
/// - **Non-resettable**: Cannot be extended or reset by any process behavior
/// - **Strict**: Process termination occurs immediately when deadline is reached
/// - **Independent**: Operates separately from idle timeout detection
///
/// Represented as `u64` seconds. A value of `0` means no deadline is enforced.
pub type HardDeadline = u64;

// ──────────────────────────────────────────────────────────────────────────────
// BeadStatus
// ──────────────────────────────────────────────────────────────────────────────

/// Lifecycle status of a bead in the store.
#[non_exhaustive]
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum BeadStatus {
    Open,
    InProgress,
    /// `bf` (bead-forge) has been observed emitting `"completed"` for done
    /// beads on some workspaces (bf's own `Status` enum has no `Completed`
    /// variant — it falls through to an untagged `Custom(String)`, so this
    /// slips through bf-side validation). Accept it as an alias so a single
    /// such bead doesn't fail `bf list --json` deserialization for every
    /// other bead in the same call — see needle-weave-completed-status.
    #[serde(alias = "completed")]
    Done,
    /// `br show --json` emits `"closed"` for done beads. Treat as equivalent
    /// to `Done` so deserialization succeeds.
    Closed,
    Blocked,
    /// `bf` (bead-forge) emits `"deferred"` for beads deliberately postponed
    /// rather than blocked by a dependency. Distinct from `Blocked`: a
    /// deferred bead has no unmet dependency, it was just set aside — see
    /// GitHub issue jedarden/NEEDLE#10. Without this variant, `bf list --json`
    /// fails deserialization for every such bead, and it silently disappears
    /// from strand/supervise visibility with no surfaced error.
    Deferred,
}

impl BeadStatus {
    /// Returns true if the bead is finished (either `Done` or `Closed`).
    pub fn is_done(&self) -> bool {
        matches!(self, BeadStatus::Done | BeadStatus::Closed)
    }
}

impl fmt::Display for BeadStatus {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        match self {
            BeadStatus::Open => write!(f, "open"),
            BeadStatus::InProgress => write!(f, "in_progress"),
            BeadStatus::Done | BeadStatus::Closed => write!(f, "done"),
            BeadStatus::Blocked => write!(f, "blocked"),
            BeadStatus::Deferred => write!(f, "deferred"),
        }
    }
}

// ──────────────────────────────────────────────────────────────────────────────
// WorkerState
// ──────────────────────────────────────────────────────────────────────────────

/// Worker finite-state-machine states.
///
/// Every state has defined entry conditions, actions, and exit transitions.
/// There are no implicit states or fallthrough paths.
#[non_exhaustive]
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "SCREAMING_SNAKE_CASE")]
pub enum WorkerState {
    Booting,
    Selecting,
    Claiming,
    Retrying,
    Building,
    Dispatching,
    Executing,
    Handling,
    Logging,
    /// All strands returned empty — worker has nothing to do.
    Exhausted,
    /// Received graceful shutdown signal.
    Stopped,
    /// Unrecoverable error.
    Errored,
}

impl fmt::Display for WorkerState {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        let s = match self {
            WorkerState::Booting => "BOOTING",
            WorkerState::Selecting => "SELECTING",
            WorkerState::Claiming => "CLAIMING",
            WorkerState::Retrying => "RETRYING",
            WorkerState::Building => "BUILDING",
            WorkerState::Dispatching => "DISPATCHING",
            WorkerState::Executing => "EXECUTING",
            WorkerState::Handling => "HANDLING",
            WorkerState::Logging => "LOGGING",
            WorkerState::Exhausted => "EXHAUSTED",
            WorkerState::Stopped => "STOPPED",
            WorkerState::Errored => "ERRORED",
        };
        f.write_str(s)
    }
}

// ──────────────────────────────────────────────────────────────────────────────
// Outcome
// ──────────────────────────────────────────────────────────────────────────────

/// The classified outcome of an agent process.
///
/// Every exit code maps to exactly one variant via `Outcome::classify()`.
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Outcome {
    /// Exit 0 — agent completed work successfully.
    Success,
    /// Non-zero exit indicating a failure (includes app errors 1-99 and unknown codes).
    Failure,
    /// Exit 100 or 124 — timeout wrapper expired.
    Timeout,
    /// Exit 126 or 127 — agent binary not found or not executable.
    AgentNotFound,
    /// Exit 130 (SIGINT) or 143 (SIGTERM) — agent was interrupted.
    Interrupted,
    /// Negative exit code — process crashed or was killed by a signal.
    Crash(i32),
}

impl Outcome {
    /// Classify an exit code into an `Outcome` variant.
    ///
    /// Every exit code range has an explicit match arm — no wildcards.
    /// The `was_interrupted` flag takes precedence over exit code analysis.
    ///
    /// # Mapping (per spec)
    /// - `was_interrupted=true` → `Interrupted` (checked first)
    /// - exit 0 → `Success`
    /// - exit 1 → `Failure`
    /// - exit 124 → `Timeout`
    /// - exit 127 → `AgentNotFound`
    /// - exit >128 → `Crash(exit_code)`
    /// - exit <0 → `Crash(exit_code)`
    /// - all other → `Failure`
    pub fn classify(exit_code: i32, was_interrupted: bool) -> Self {
        // Interrupted flag takes precedence (graceful shutdown path).
        if was_interrupted {
            return Outcome::Interrupted;
        }

        // Explicit mapping for every exit code range — NO wildcards.
        // Each range is explicitly enumerated to ensure compile errors
        // if a new Outcome variant is added without updating this match.
        match exit_code {
            // Success
            0 => Outcome::Success,
            // Explicit failure code
            1 => Outcome::Failure,
            // Timeout (GNU timeout exit code)
-- outcome resolution policy --
//! Outcome routing: map agent exit codes to explicit handlers.
//!
//! Every possible exit code has a named handler. The type system enforces
//! exhaustiveness — if an outcome can happen, it must have a handler.
//!
//! Depends on: `types`, `config`, `bead_store`, `telemetry`, `validation`.

use std::fmt;
use std::sync::atomic::{AtomicBool, Ordering};
use std::sync::Arc;

use anyhow::Result;
use chrono::Utc;

use crate::bead_store::{BeadStore, SyncRecoveryError};
use crate::config::Config;
use crate::telemetry::{EventKind, Telemetry};
#[cfg(test)]
use crate::types::BeadStatus;
use crate::types::{AgentOutcome, Bead, BeadAction, HandlerResult, Outcome};
use crate::validation::{verify_shipped_work, GateConfig, GateReport, ValidationGate};

// ──────────────────────────────────────────────────────────────────────────────
// classify (convenience re-export)
// ──────────────────────────────────────────────────────────────────────────────

/// Classify an agent result into an `Outcome`, with verification and shutdown
/// signal support.
///
/// Interruption takes precedence. Otherwise, failed verification is always a
/// failure; only verified results are delegated to the exit-code classifier.
pub fn classify(exit_code: i32, was_interrupted: bool, verified: bool) -> Outcome {
    if was_interrupted {
        return Outcome::Interrupted;
    }

    if !verified {
        return Outcome::Failure;
    }

    Outcome::classify(exit_code, false)
}

// ──────────────────────────────────────────────────────────────────────────────
// OutcomeHandler
// ──────────────────────────────────────────────────────────────────────────────

/// Routes agent outcomes to their explicit handlers.
pub struct OutcomeHandler {
    config: Config,
    telemetry: Telemetry,
}

impl OutcomeHandler {
    pub fn new(config: Config, telemetry: Telemetry) -> Self {
        OutcomeHandler { config, telemetry }
    }

    /// Run a bead store operation with a 30s timeout.
    ///
    /// Returns `Ok(Some(T))` on success, `Ok(None)` on timeout, and `Err(E)` on
    /// other errors. Callers should treat timeout and error as non-fatal — log
    /// and continue rather than blocking the worker in HANDLING state.
    async fn timeout_op<T, F, Fut>(
        &self,
        op: F,
        operation_name: &str,
    ) -> Result<Option<T>, anyhow::Error>
    where
        F: FnOnce() -> Fut,
        Fut: std::future::Future<Output = Result<T, anyhow::Error>>,
    {
        match tokio::time::timeout(std::time::Duration::from_secs(30), op()).await {
            Ok(Ok(result)) => Ok(Some(result)),
            Ok(Err(e)) => Err(e),
            Err(_) => {
                tracing::error!(
                    operation = operation_name,
                    "bead store operation timed out after 30s"
                );
                Ok(None)
            }
        }
    }

    /// Release a bead with sync conflict recovery and graceful failure handling.
    ///
    /// Flow:
    /// 1. Run `br sync --flush-only` to push local writes to JSONL first.
    /// 2. Attempt `br update` to release the bead (set status=open, assignee="").
    /// 3. If SYNC_CONFLICT recovery fails, emit `BeadReleaseFailed` and return Ok.
    ///
    /// This method never returns an error — release failures are non-fatal.
    /// The worker can continue processing other beads even if release fails.
    async fn release_bead(&self, store: &dyn BeadStore, bead: &Bead) -> Result<Vec<EventKind>> {
        let mut events = Vec::new();

        // Step 1: Flush local writes to JSONL before attempting release.
        // Emit heartbeat before br call to track where hang occurs.
        let _ = self.telemetry.emit_try_lock(EventKind::HeartbeatEmitted {
            bead_id: Some(bead.id.clone()),
            state: "HANDLING_FLUSH".to_string(),
        });

        match self.timeout_op(|| store.flush(), "flush").await {
            Ok(Some(())) => {
                tracing::debug!(
                    bead_id = %bead.id,
                    "flushed local changes to JSONL before release"
                );
                // Emit heartbeat after successful flush.
                let _ = self.telemetry.emit_try_lock(EventKind::HeartbeatEmitted {
                    bead_id: Some(bead.id.clone()),
                    state: "HANDLING_FLUSH_DONE".to_string(),
                });
            }
            Ok(None) => {
                tracing::warn!(
                    bead_id = %bead.id,
                    "flush timed out before release, attempting release anyway"
                );
                events.push(EventKind::WorkerHandlingTimeout {
                    bead_id: bead.id.clone(),
                    outcome: "release".to_string(),
                    operation: "flush".to_string(),
                    error: "timeout after 30s".to_string(),
                });
            }
            Err(e) => {
                tracing::warn!(
                    bead_id = %bead.id,
                    error = %e,
                    "flush failed before release, attempting release anyway"
                );
                events.push(EventKind::WorkerHandlingTimeout {
                    bead_id: bead.id.clone(),
                    outcome: "release".to_string(),
                    operation: "flush".to_string(),
                    error: e.to_string(),
                });
            }
        }

        // Step 2: Attempt release with timeout protection.
        // Emit heartbeat before br release call to track where hang occurs.
        let _ = self.telemetry.emit_try_lock(EventKind::HeartbeatEmitted {
            bead_id: Some(bead.id.clone()),
            state: "HANDLING_RELEASE".to_string(),
        });

        let release_result =
            tokio::time::timeout(std::time::Duration::from_secs(30), store.release(&bead.id)).await;

        match release_result {
            Ok(Ok(())) => {
                tracing::debug!(
                    bead_id = %bead.id,
                    "bead released successfully"
                );
                // Emit heartbeat after successful release.
                let _ = self.telemetry.emit_try_lock(EventKind::HeartbeatEmitted {
                    bead_id: Some(bead.id.clone()),
                    state: "HANDLING_RELEASE_DONE".to_string(),
                });
                events.push(EventKind::BeadReleased {
                    bead_id: bead.id.clone(),
                    reason: "release_success".to_string(),
                });
            }
            Ok(Err(e)) => {
                // Release failed (br error, not timeout).
                // Check if this is a SyncRecoveryError (sync recovery was attempted but failed).
                if e.downcast_ref::<SyncRecoveryError>().is_some() {
                    tracing::error!(
                        bead_id = %bead.id,
                        error = %e,
                        "br release failed after SYNC_CONFLICT recovery — \
                         worker will continue (bead may remain assigned)"
                    );
                } else {
                    tracing::error!(
                        bead_id = %bead.id,
                        error = %e,
                        "br release failed — worker will continue (bead may remain assigned)"
                    );
                }
                events.push(EventKind::BeadReleaseFailed {
                    bead_id: bead.id.clone(),
                    reason: e.to_string(),
                });
            }
            Err(_) => {
                // Timeout after 30s.
                tracing::error!(
                    bead_id = %bead.id,
                    "br release timed out after 30s — worker will continue (bead may remain assigned)"
                );
                events.push(EventKind::BeadReleaseFailed {
                    bead_id: bead.id.clone(),
                    reason: "timeout after 30s".to_string(),
                });
            }
        }

        Ok(events)
    }

    /// Run verification gates for a bead, returning whether all passed.
    ///
    /// This is extracted as a helper so it can be called BEFORE outcome classification.
    /// Verification must determine Success, not just exit code 0.
    async fn run_verification_gates(&self, bead: &Bead) -> Result<(bool, Option<GateReport>)> {
        // Try pluggable gates first, fall back to legacy verification commands.
        let gate_opt = if !self.config.gates.is_empty() {
            // New pluggable gate system. Fill in each command gate's stderr
            // cap from `validation.stderr_cap_bytes` unless the gate already
            // set its own override — see GitHub issue jedarden/NEEDLE#9.
            let default_stderr_cap = self.config.validation.stderr_cap_bytes;
            let gate_configs: Vec<(String, GateConfig)> = self
                .config
                .gates
                .iter()
                .enumerate()
                .map(|(i, config)| {
                    let mut config = config.clone();
                    let GateConfig::Command {
                        stderr_cap_bytes, ..
                    } = &mut config;
                    if stderr_cap_bytes.is_none() {
                        *stderr_cap_bytes = Some(default_stderr_cap);
                    }
                    (format!("gate_{}", i), config)
                })
                .collect();
            ValidationGate::new(gate_configs, bead.workspace.clone())
        } else if !self.config.verification.is_empty() {
            // Legacy verification command format.
            ValidationGate::from_commands_with_stderr_cap(
                self.config.verification.clone(),
                bead.workspace.clone(),
                self.config.validation.stderr_cap_bytes,
            )
        } else {
            // No gates configured — treat as verified (backward compatible).
            return Ok((true, None));
        };

        // Run the gate and return the result.
        let gate = gate_opt.ok_or_else(|| {
            anyhow::anyhow!("validation gate creation failed - all gates failed to initialize")
        })?;
        let report = gate.run(bead).await?;
        let all_passed = report.all_passed;
        Ok((all_passed, Some(report)))
    }

    /// Handle a process output for the given bead.
    ///
    /// CRITICAL: For exit code 0, verification gates run BEFORE classification.
    /// This ensures Success means verification passed, not just that the agent exited 0.
    /// An agent that exits 0 but fails verification produces Failure, not Success.
    #[tracing::instrument(
        name = "bead.outcome",
        skip(self, store, bead, output),
        fields(
            needle.bead.id = %bead.id,
            needle.outcome = tracing::field::Empty,
            needle.outcome.action = tracing::field::Empty,
        )
    )]
    pub async fn handle(
        &self,
        store: &dyn BeadStore,
        bead: &Bead,
        output: &AgentOutcome,
        was_interrupted: bool,
    ) -> Result<HandlerResult> {
        // For exit code 0, run verification BEFORE classification.
        // This is the core fix: Success must mean verification passed.
        let (verified, gate_report) = if output.exit_code == 0 && !was_interrupted {
            self.run_verification_gates(bead).await?
        } else {
            // Non-zero exit or interrupted — verification irrelevant.
            (true, None)
        };

        let outcome = classify(output.exit_code, was_interrupted, verified);

        // Set outcome as span attribute
        tracing::Span::current().record("needle.outcome", outcome.as_str());

        tracing::info!(
            bead_id = %bead.id,
            exit_code = output.exit_code,
            verified,
            outcome = %outcome,
            "handling agent outcome"
        );

        // Use emit_try_lock() to avoid blocking if telemetry writer is stuck.
        // This prevents worker hang in HANDLING state when telemetry is wedged.
        let _ = self.telemetry.emit_try_lock(EventKind::OutcomeClassified {
            bead_id: bead.id.clone(),
            outcome: outcome.as_str().to_string(),
            exit_code: output.exit_code,
        });

        let (bead_action, telemetry_events) = match outcome.clone() {
            Outcome::Success => self.handle_success(store, bead, gate_report).await?,
            Outcome::Failure => {
                // If we have a gate report with failures, handle as gate failure.
                if let Some(report) = gate_report {
                    if !report.all_passed {
                        self.handle_gate_failure(store, bead, &report).await?
                    } else {
                        self.handle_failure(store, bead).await?
                    }
                } else {
                    self.handle_failure(store, bead).await?
                }
            }
            Outcome::Timeout => self.handle_timeout(store, bead).await?,
            Outcome::AgentNotFound => self.handle_agent_not_found(store, bead).await?,
            Outcome::Interrupted => self.handle_interrupted(store, bead).await?,
            Outcome::Crash(code) => self.handle_crash(store, bead, code).await?,
        };

        // Emit sub-handler events (e.g. BeadCompleted, BeadOrphaned) to the
        // telemetry sink so they appear in the JSONL log.
        // Use emit_try_lock() to avoid blocking if telemetry writer is stuck.
        for event in &telemetry_events {
            let _ = self.telemetry.emit_try_lock(event.clone());
        }

        // Use emit_try_lock() to avoid blocking if telemetry writer is stuck.
        let _ = self.telemetry.emit_try_lock(EventKind::OutcomeHandled {
            bead_id: bead.id.clone(),
            outcome: outcome.as_str().to_string(),
            action: bead_action.to_string(),
        });

        // Set action as span attribute
        tracing::Span::current().record("needle.outcome.action", bead_action.to_string());

        // Set span status: Ok for success, Error otherwise
        if !matches!(outcome, Outcome::Success) {
            tracing::Span::current().record("otel.status_code", 2u64);
            tracing::Span::current().record("otel.status_description", outcome.as_str());
        }

        Ok(HandlerResult {
            outcome,
            bead_action,
            telemetry_events,
        })
    }

    /// Handle a process output with cancellation support.
    ///
    /// Checks the cancellation flag before starting the handler and returns
-- canary source contradictions --
19-//!
20-//! ## Test Scenarios
21-//!
22-//! - Happy path: Agent completes successfully, bead closed
23-//! - Failure path: Agent fails, bead released with failure label
24://! - Timeout: Agent times out, bead deferred
25-//! - State machine integrity: Worker state transitions are valid
26-
27-use std::path::{Path, PathBuf};
28-use std::process::Command;
29-use std::time::{Duration, Instant};
--
100-#[serde(tag = "type", rename_all = "snake_case")]
101-pub enum ExpectedOutcome {
102-    /// Agent should complete successfully (exit 0).
103-    Success {
104-        /// Expected final bead status (default: "done").
105:        #[serde(default = "ExpectedOutcome::default_final_status")]
106-        final_status: String,
107-        /// Expected labels to be present on the bead.
108-        #[serde(default)]
109-        labels: Vec<String>,
110-    },
111-    /// Agent should fail (non-zero exit).
112-    Failure {
113-        /// Expected final bead status.
114:        #[serde(default = "ExpectedOutcome::default_failure_status")]
115-        final_status: String,
116-        /// Expected failure labels (e.g., "failure-count:1").
117-        #[serde(default)]
118-        labels: Vec<String>,
119-    },
120-    /// Agent should timeout.
121-    Timeout {
122-        /// Expected final bead status.
123:        #[serde(default = "ExpectedOutcome::default_timeout_status")]
124-        final_status: String,
125-    },
126-    /// Worker state machine should transition correctly.
127-    StateMachine {
128-        /// Expected state transitions (e.g., ["BOOTING", "SELECTING", "CLAIMING", ...]).
129-        transitions: Vec<String>,
130-    },
131-}
132-
133-impl ExpectedOutcome {
134:    fn default_final_status() -> String {
135-        "done".to_string()
136-    }
137:    fn default_failure_status() -> String {
138-        "open".to_string()
139-    }
140:    fn default_timeout_status() -> String {
141-        "open".to_string()
142-    }
143-}
144-
145-// ──────────────────────────────────────────────────────────────────────────────
--
680-
681-        Ok(ActualOutcome {
682-            exit_code,
683-            final_status,
684-            labels,
685:            state_transitions: Vec::new(), // Not tracked in basic canary tests
686-        })
687-    }
688-
689-    /// Check if actual outcome matches expected outcome.
690-    fn outcomes_match(&self, expected: &ExpectedOutcome, actual: &ActualOutcome) -> bool {
--
1038-        assert_eq!(result.bead_id(), "test-timeout");
1039-    }
1040-
1041-    #[test]
1042-    fn expected_outcome_default_status() {
1043:        assert_eq!(ExpectedOutcome::default_final_status(), "done");
1044:        assert_eq!(ExpectedOutcome::default_failure_status(), "open");
1045:        assert_eq!(ExpectedOutcome::default_timeout_status(), "open");
1046-    }
1047-
1048-    #[test]
1049-    fn canary_report_can_promote() {
1050-        let report = CanaryReport {

exec
/bin/bash -lc "rg -n 'async fn handle_(success|failure|timeout|gate_failure)' src/outcome/mod.rs
sed -n '390,760p' src/outcome/mod.rs
printf '%s\\n' '-- canary event evidence --'
rg -n -C 3 'bead-(1b6a6357|54b7174d|60c759fa|e6f0b50d)' /home/coding/.needle/canary/.beads/events.jsonl /home/coding/.needle/canary/.beads/heartbeats.jsonl 2>/dev/null | sed -n '1,420p'
printf '%s\\n' '-- canary working metadata --'
ls -la /home/coding/.needle/canary
find /home/coding/.needle/canary -maxdepth 2 -type f "'! -path '"'*/.beads/*' -printf '%p\\n' | sort" in /home/coding/NEEDLE
 succeeded in 0ms:
455:    async fn handle_success(
664:    async fn handle_gate_failure(
813:    async fn handle_failure(
887:    async fn handle_timeout(
1618:    async fn handle_success_bead_closed_by_agent() {
1639:    async fn handle_success_bead_still_open_is_failure_not_orphaned() {
1678:    async fn handle_failure_releases_and_increments_count() {
1708:    async fn handle_failure_increments_existing_count() {
1740:    async fn handle_failure_quarantines_bead_at_threshold() {
1781:    async fn handle_failure_below_threshold_does_not_quarantine() {
1805:    async fn handle_failure_quarantine_disabled_when_threshold_zero() {
1827:    async fn handle_success_resets_failure_count() {
1850:    async fn handle_timeout_releases_and_adds_deferred() {
1969:    async fn handle_failure_emits_telemetry_events() {
2010:    async fn handle_success_no_verification_default_behavior() {
2026:    async fn handle_success_verification_passes_accepts_closure() {
2046:    async fn handle_success_verification_fails_releases_bead() {
2076:    async fn handle_success_verification_fails_reopens_closed_bead() {
2105:    async fn handle_success_verification_fails_increments_failure_count() {
2125:    async fn handle_success_multiple_gates_first_fails() {
2146:    async fn handle_failure_with_flush_timeout_continues_gracefully() {
2254:    async fn handle_failure_with_release_timeout_continues_gracefully() {
        // Configurable via `validation.outcome_timeout_seconds` (default 50) —
        // see GitHub issue jedarden/NEEDLE#8: a gate running a real verification
        // workload (container test suite, secret scan, fresh-model diff verifier)
        // needs minutes, not seconds.
        let bead_id = bead.id.clone();
        let telemetry = self.telemetry.clone();
        let timeout_secs = self.config.validation.outcome_timeout_seconds;

        match tokio::time::timeout(
            std::time::Duration::from_secs(timeout_secs),
            self.handle(store, bead, output, was_interrupted),
        )
        .await
        {
            Ok(Ok(result)) => Ok(result),
            Ok(Err(e)) => {
                // Handler returned an error.
                tracing::error!(
                    bead_id = %bead_id,
                    error = %e,
                    "outcome handler returned error"
                );
                Err(e)
            }
            Err(_) => {
                // Timeout after `timeout_secs` seconds.
                tracing::error!(
                    bead_id = %bead_id,
                    timeout_secs,
                    "outcome handler timed out, returning early to allow worker recovery"
                );
                // Emit a timeout event for observability.
                let _ = telemetry.emit(EventKind::WorkerHandlingTimeout {
                    bead_id: bead_id.clone(),
                    outcome: classify(output.exit_code, was_interrupted, false)
                        .as_str()
                        .to_string(),
                    operation: "handle".to_string(),
                    error: format!("timeout after {}s", timeout_secs),
                });
                // Return an explicit error action. The worker must apply it,
                // which runs release recovery before the cycle can advance.
                // Verification never ran, so conservatively treat as unverified.
                Ok(HandlerResult {
                    outcome: classify(output.exit_code, was_interrupted, false),
                    bead_action: BeadAction::Errored,
                    telemetry_events: vec![],
                })
            }
        }
    }

    /// Success: verification already passed, now verify bead closure.
    ///
    /// CRITICAL: This is only called when verification PASSED.
    /// If verification failed, classification produces Failure and handle_failure
    /// is called instead. This ensures Success means verification passed.
    ///
    /// Flow:
    /// 1. If gates ran, emit VerificationPassed telemetry.
    /// 2. Check if agent closed the bead.
    ///    - Closed → emit BeadCompleted.
    ///    - Still open → emit BeadOrphaned warning.
    ///
    /// NEEDLE does NOT auto-close — the agent owns closure via `br close`.
    async fn handle_success(
        &self,
        store: &dyn BeadStore,
        bead: &Bead,
        gate_report: Option<GateReport>,
    ) -> Result<(BeadAction, Vec<EventKind>)> {
        tracing::info!(bead_id = %bead.id, "agent completed successfully");

        // If gates ran and passed, emit telemetry.
        if let Some(report) = gate_report {
            let gates_run = report.results.len() as u32;
            self.telemetry.emit(EventKind::VerificationPassed {
                bead_id: bead.id.clone(),
                gates_run,
            })?;
            tracing::info!(
                bead_id = %bead.id,
                gates_run,
                "all validation gates passed"
            );
        }

        // Reset failure count on successful gate completion.
        // This ensures the bead starts fresh on the next cycle if re-claimed.
        let _ = self.reset_failure_count(store, bead).await;

        // Normal success flow: check if agent closed the bead.
        let mut events = Vec::new();

        // Use timeout for show() to prevent indefinite hang in HANDLING state.
        match self.timeout_op(|| store.show(&bead.id), "show").await {
            Ok(Some(current)) if current.status.is_done() => {
                if self.config.worker.enforce_shipped_work {
                    match verify_shipped_work(&current, &bead.workspace, store).await {
                        Ok(crate::validation::GateResult::Fail(reason)) => {
                            tracing::warn!(
                                bead_id = %bead.id,
                                reason = %reason,
                                "bead closed but shipped-work check failed — reopening and releasing"
                            );
                            let report = GateReport::single_failure("shipped_work", reason);
                            return self.handle_gate_failure(store, bead, &report).await;
                        }
                        Ok(crate::validation::GateResult::Pass) => {}
                        Err(e) => {
                            tracing::warn!(
                                bead_id = %bead.id,
                                error = %e,
                                "shipped-work check errored — failing open"
                            );
                        }
                    }
                }

                // Dispatch is fully accounted for — drop its snapshot so the
                // next claim of this bead starts from a fresh baseline.
                crate::validation::predispatch::clear(&bead.workspace, &bead.id).await;

                tracing::info!(bead_id = %bead.id, "bead confirmed closed by agent");
                events.push(EventKind::BeadCompleted {
                    bead_id: bead.id.clone(),
                    duration_ms: 0,
                });
                // Increment success_count for any skills that matched this bead.
                if !bead.workspace.as_os_str().is_empty() {
                    if let Ok(lib) = crate::skill::SkillLibrary::load(&bead.workspace) {
                        if let Err(e) = lib.increment_success_for_bead(&bead.labels, &bead.title) {
                            tracing::warn!(
                                bead_id = %bead.id,
                                error = %e,
                                "failed to increment skill success counts"
                            );
                        }
                    }
                }
            }
            Ok(Some(current)) => {
                tracing::warn!(
                    bead_id = %bead.id,
                    status = %current.status,
                    "agent exited successfully but bead is still open (orphaned)"
                );
                events.push(EventKind::BeadOrphaned {
                    bead_id: bead.id.clone(),
                });

                // Use verify_shipped_work to decide close-vs-release.
                // If the agent shipped work, mark as completed. Otherwise, release
                // and increment failure count so repeat offenders quarantine.
                if self.config.worker.enforce_shipped_work {
                    match verify_shipped_work(&current, &bead.workspace, store).await {
                        Ok(crate::validation::GateResult::Pass) => {
                            tracing::info!(
                                bead_id = %bead.id,
                                "shipped work detected — marking orphaned bead as completed"
                            );
                            // Clear the predispatch snapshot since work is complete.
                            crate::validation::predispatch::clear(&bead.workspace, &bead.id).await;
                            // Mark as completed - the agent shipped work but forgot to close.
                            // We can't close via bead store (no close method), so emit completion
                            // event and release. The bead remains open but work is done.
                            events.push(EventKind::BeadCompleted {
                                bead_id: bead.id.clone(),
                                duration_ms: 0,
                            });
                            let mut release_events = self.release_bead(store, bead).await?;
                            events.append(&mut release_events);
                            return Ok((BeadAction::Released, events));
                        }
                        Ok(crate::validation::GateResult::Fail(reason)) => {
                            tracing::warn!(
                                bead_id = %bead.id,
                                reason = %reason,
                                "no shipped work detected — releasing orphaned bead with failure increment"
                            );
                            // Release and increment failure count to apply quarantine.
                            let mut release_events = self.release_bead(store, bead).await?;
                            events.append(&mut release_events);
                            let release_succeeded = events
                                .iter()
                                .any(|e| matches!(e, EventKind::BeadReleased { .. }));
                            if release_succeeded {
                                let _ = self.increment_failure_count(store, bead).await;
                            }
                            return Ok((BeadAction::Released, events));
                        }
                        Err(e) => {
                            tracing::warn!(
                                bead_id = %bead.id,
                                error = %e,
                                "shipped-work check errored — releasing orphaned bead with failure increment"
                            );
                            // On error, release and increment failure count.
                            let mut release_events = self.release_bead(store, bead).await?;
                            events.append(&mut release_events);
                            let release_succeeded = events
                                .iter()
                                .any(|e| matches!(e, EventKind::BeadReleased { .. }));
                            if release_succeeded {
                                let _ = self.increment_failure_count(store, bead).await;
                            }
                            return Ok((BeadAction::Released, events));
                        }
                    }
                } else {
                    tracing::warn!(
                        bead_id = %bead.id,
                        "enforce_shipped_work disabled — releasing orphaned bead"
                    );
                    // If enforce_shipped_work is disabled, just release without closing.
                    let mut release_events = self.release_bead(store, bead).await?;
                    events.append(&mut release_events);
                    return Ok((BeadAction::Released, events));
                }
            }
            Ok(None) => {
                // Timeout - we cannot verify bead closure, so release to enforce postcondition.
                tracing::warn!(
                    bead_id = %bead.id,
                    "show() timed out, releasing bead to enforce postcondition"
                );
                events.push(EventKind::WorkerHandlingTimeout {
                    bead_id: bead.id.clone(),
                    outcome: "success".to_string(),
                    operation: "show".to_string(),
                    error: "timeout after 30s".to_string(),
                });
                let mut release_events = self.release_bead(store, bead).await?;
                events.append(&mut release_events);
                return Ok((BeadAction::Released, events));
            }
            Err(e) => {
                // Error - we cannot verify bead closure, so release to enforce postcondition.
                tracing::warn!(
                    bead_id = %bead.id,
                    error = %e,
                    "show() failed, releasing bead to enforce postcondition"
                );
                events.push(EventKind::WorkerHandlingTimeout {
                    bead_id: bead.id.clone(),
                    outcome: "success".to_string(),
                    operation: "show".to_string(),
                    error: e.to_string(),
                });
                let mut release_events = self.release_bead(store, bead).await?;
                events.append(&mut release_events);
                return Ok((BeadAction::Released, events));
            }
        }

        // Flush SQLite state to JSONL so the closed/orphaned bead is captured in
        // the git-committed backup. Non-fatal: a flush failure is logged but does
        // not block the worker from picking up the next bead.
        match self.timeout_op(|| store.flush(), "flush").await {
            Ok(Some(())) => {
                tracing::debug!(bead_id = %bead.id, "flushed bead state to JSONL after success");
            }
            Ok(None) => {
                tracing::warn!(bead_id = %bead.id, "flush timed out after success — JSONL may lag SQLite");
            }
            Err(e) => {
                tracing::warn!(bead_id = %bead.id, error = %e, "flush failed after success — JSONL may lag SQLite");
            }
        }

        Ok((BeadAction::Closed, events))
    }

    /// Handle gate failure: reopen the bead if it was closed, then release it.
    async fn handle_gate_failure(
        &self,
        store: &dyn BeadStore,
        bead: &Bead,
        report: &crate::validation::GateReport,
    ) -> Result<(BeadAction, Vec<EventKind>)> {
        // Find the first failing gate for telemetry.
        let (failed_gate, reason) = report
            .results
            .iter()
            .find(|(_, r)| !r.passed())
            .map(|(name, r)| (name.clone(), r.failure_reason().unwrap_or("").to_string()))
            .unwrap_or_else(|| ("unknown".to_string(), "unknown error".to_string()));

        tracing::warn!(
            bead_id = %bead.id,
            gate = %failed_gate,
            reason = %reason,
            "validation gate failed — releasing bead"
        );

        // Emit verification failure telemetry.
        self.telemetry.emit(EventKind::VerificationFailed {
            bead_id: bead.id.clone(),
            command: failed_gate.clone(),
            exit_code: None,
            output: reason,
        })?;

        let mut events = Vec::new();

        // If the agent already closed the bead, reopen it before releasing.
        // Use timeout to prevent indefinite hang in HANDLING state.
        match self.timeout_op(|| store.show(&bead.id), "show").await {
            Ok(Some(current)) if current.status.is_done() => {
                tracing::info!(
                    bead_id = %bead.id,
                    "reopening bead closed by agent (verification failed)"
                );
                match self.timeout_op(|| store.reopen(&bead.id), "reopen").await {
                    Ok(_) => {}
                    Err(e) => {
                        tracing::warn!(
                            bead_id = %bead.id,
                            error = %e,
                            "failed to reopen bead — attempting release anyway"
                        );
                        events.push(EventKind::WorkerHandlingTimeout {
                            bead_id: bead.id.clone(),
                            outcome: "gate_failure".to_string(),
                            operation: "reopen".to_string(),
                            error: e.to_string(),
                        });
                    }
                }
            }
            Ok(None) => {
                tracing::warn!(
                    bead_id = %bead.id,
                    "show() timed out during gate failure handling, skipping reopen"
                );
                events.push(EventKind::WorkerHandlingTimeout {
                    bead_id: bead.id.clone(),
                    outcome: "gate_failure".to_string(),
                    operation: "show".to_string(),
                    error: "timeout after 30s".to_string(),
                });
            }
            Err(e) => {
                tracing::warn!(
                    bead_id = %bead.id,
                    error = %e,
                    "show() failed during gate failure handling"
                );
            }
            _ => {}
        }

        // Release the bead back to open with flush-before-release and sync recovery.
        let mut release_events = self.release_bead(store, bead).await?;
        events.append(&mut release_events);

        // If release succeeded, increment the failure count and apply the same
        // quarantine ceiling `handle_failure` uses. Without this, a bead that
        // fails a gate every cycle is released back to open forever: the
        // ARMOR/bf-135k storm ran one bead 24 times in a single day, each
        // attempt leaving another commit behind. A gate failure is no less
        // repeatable than an agent failure and must respect the same ceiling.
        let release_succeeded = events
            .iter()
            .any(|e| matches!(e, EventKind::BeadReleased { .. }));
        let mut action = BeadAction::Released;
        if release_succeeded {
            match self.increment_failure_count(store, bead).await {
                Ok(new_count) => {
                    let threshold = self.config.outcome.quarantine_after_failures;
                    if threshold > 0 && new_count >= threshold {
-- canary event evidence --
/home/coding/.needle/canary/.beads/heartbeats.jsonl:1:{"last_strand":null,"state":"idle","ts":"2026-08-17T19:01:06.638122565+00:00","worker":"canary-bead-54b7174d"}
/home/coding/.needle/canary/.beads/heartbeats.jsonl:2:{"last_strand":null,"state":"idle","ts":"2026-08-17T19:01:07.185193046+00:00","worker":"canary-bead-e6f0b50d"}
/home/coding/.needle/canary/.beads/heartbeats.jsonl:3:{"last_strand":null,"state":"idle","ts":"2026-08-17T19:01:07.725277359+00:00","worker":"canary-bead-60c759fa"}
/home/coding/.needle/canary/.beads/heartbeats.jsonl:4:{"last_strand":null,"state":"idle","ts":"2026-08-17T19:01:08.269785383+00:00","worker":"canary-bead-1b6a6357"}
--
/home/coding/.needle/canary/.beads/events.jsonl:1:{"bead":"bead-1b6a6357","event":"claim","strand":"auto","ts":"2026-08-17T19:01:06.663554010+00:00","worker":"canary-bead-54b7174d"}
/home/coding/.needle/canary/.beads/events.jsonl:2:{"adapter":"claude-code-glm-4.7","bead":"bead-1b6a6357","event":"dispatch","model":"glm-4.7","strand":"auto","ts":"2026-08-17T19:01:06.666583271+00:00","worker":"canary-bead-54b7174d"}
/home/coding/.needle/canary/.beads/events.jsonl:3:{"bead":"bead-60c759fa","event":"claim","strand":"auto","ts":"2026-08-17T19:01:07.198884946+00:00","worker":"canary-bead-e6f0b50d"}
/home/coding/.needle/canary/.beads/events.jsonl:4:{"adapter":"claude-code-glm-4.7","bead":"bead-60c759fa","event":"dispatch","model":"glm-4.7","strand":"auto","ts":"2026-08-17T19:01:07.201800218+00:00","worker":"canary-bead-e6f0b50d"}
/home/coding/.needle/canary/.beads/events.jsonl:5:{"bead":"bead-54b7174d","event":"claim","strand":"auto","ts":"2026-08-17T19:01:07.734869076+00:00","worker":"canary-bead-60c759fa"}
/home/coding/.needle/canary/.beads/events.jsonl:6:{"adapter":"claude-code-glm-4.7","bead":"bead-54b7174d","event":"dispatch","model":"glm-4.7","strand":"auto","ts":"2026-08-17T19:01:07.737085536+00:00","worker":"canary-bead-60c759fa"}
/home/coding/.needle/canary/.beads/events.jsonl:7:{"bead":"bead-e6f0b50d","event":"claim","strand":"auto","ts":"2026-08-17T19:01:08.280313864+00:00","worker":"canary-bead-1b6a6357"}
/home/coding/.needle/canary/.beads/events.jsonl:8:{"adapter":"claude-code-glm-4.7","bead":"bead-e6f0b50d","event":"dispatch","model":"glm-4.7","strand":"auto","ts":"2026-08-17T19:01:08.283161678+00:00","worker":"canary-bead-1b6a6357"}
/home/coding/.needle/canary/.beads/events.jsonl:9:{"bead":"bead-54b7174d","duration_ms":437503,"event":"complete","exit_code":0,"outcome":"success","strand":"auto","ts":"2026-08-17T19:08:25.247479284+00:00","worker":"canary-bead-60c759fa"}
/home/coding/.needle/canary/.beads/events.jsonl:10:{"bead":"bead-60c759fa","duration_ms":499595,"event":"complete","exit_code":0,"outcome":"success","strand":"auto","ts":"2026-08-17T19:09:26.804727971+00:00","worker":"canary-bead-e6f0b50d"}
/home/coding/.needle/canary/.beads/events.jsonl:11:{"bead":"bead-1b6a6357","duration_ms":514547,"event":"complete","exit_code":0,"outcome":"success","strand":"auto","ts":"2026-08-17T19:09:41.222182967+00:00","worker":"canary-bead-54b7174d"}
/home/coding/.needle/canary/.beads/events.jsonl:12:{"bead":"bead-e6f0b50d","duration_ms":550800,"event":"complete","exit_code":0,"outcome":"success","strand":"auto","ts":"2026-08-17T19:10:19.090719526+00:00","worker":"canary-bead-1b6a6357"}
-- canary working metadata --
total 40
drwxr-xr-x  6 coding coding 4096 Aug 17 05:27 .
drwxr-xr-x 14 coding coding 4096 Aug 21 12:08 ..
drwxr-xr-x  5 coding coding 4096 Aug 22 00:52 .beads
drwxrwxr-x  8 coding coding 4096 Aug 17 15:09 .git
-rw-rw-r--  1 coding coding   41 Aug 17 15:01 .needle-predispatch-sha
-rw-r--r--  1 coding coding   29 Aug 17 05:27 .needle.yaml
-rw-r--r--  1 coding coding   44 Mar 21 08:17 canary-bzl.yaml
drwxr-xr-x  2 coding coding 4096 Aug 17 05:29 expected
drwxrwxr-x  2 coding coding 4096 Aug  2 13:52 notes
-rwxrwxr-x  1 coding coding 2136 Aug  2 13:49 state_machine.py
/home/coding/.needle/canary/.git/COMMIT_EDITMSG
/home/coding/.needle/canary/.git/FETCH_HEAD
/home/coding/.needle/canary/.git/HEAD
/home/coding/.needle/canary/.git/config
/home/coding/.needle/canary/.git/description
/home/coding/.needle/canary/.git/index
/home/coding/.needle/canary/.needle-predispatch-sha
/home/coding/.needle/canary/.needle.yaml
/home/coding/.needle/canary/canary-bzl.yaml
/home/coding/.needle/canary/expected/bead-1b6a6357.yaml
/home/coding/.needle/canary/expected/bead-54b7174d.yaml
/home/coding/.needle/canary/expected/bead-60c759fa.yaml
/home/coding/.needle/canary/expected/bead-e6f0b50d.yaml
/home/coding/.needle/canary/notes/canary-atw.md
/home/coding/.needle/canary/state_machine.py

2026-08-22T04:54:25.995904Z ERROR codex_core::tools::router: error=exec_command failed for `/bin/bash -lc "printf '%s\\n' '-- read-only bf filter behavior --'
for status in open in_progress blocked closed completed pending ready deferred; do
  printf '%-12s ' \""'$status"
  /home/coding/backups/bf-tooling-removed-20260814/bf list --workspace /home/coding/.needle/home/coding --status "$status" --json >/tmp/bf-status-output.$$ 2>/tmp/bf-status-error.$$
  result=$?
  if test "$result" -eq 0; then
    count=$(jq '"'length' /tmp/bf-status-output."'$$ 2>/dev/null || printf '"'?')
    printf 'accepted; %s matching record(s)\\n' \""'$count"
  else
    printf '"'rejected (exit %s): ' \""'$result"
    sed -n '"'1p' /tmp/bf-status-error."'$$
  fi
done
rm -f /tmp/bf-status-output.$$ /tmp/bf-status-error.$$
printf '"'%s\\n' '-- state-machine test implementation --'
sed -n '1,300p' /home/coding/.needle/canary/state_machine.py
printf '%s\\n' '-- task bead before note --'
bead show needle-16546862 --json"`: CreateProcess { message: "Rejected(\"`/bin/bash -lc \\\"printf '%s\\\\\\\\n' '-- read-only bf filter behavior --'\\nfor status in open in_progress blocked closed completed pending ready deferred; do\\n  printf '%-12s ' \\\\\\\"\\\"'$status\\\"\\n  /home/coding/backups/bf-tooling-removed-20260814/bf list --workspace /home/coding/.needle/home/coding --status \\\"$status\\\" --json >/tmp/bf-status-output.$$ 2>/tmp/bf-status-error.$$\\n  result=$?\\n  if test \\\"$result\\\" -eq 0; then\\n    count=$(jq '\\\"'length' /tmp/bf-status-output.\\\"'$$ 2>/dev/null || printf '\\\"'?')\\n    printf 'accepted; %s matching record(s)\\\\\\\\n' \\\\\\\"\\\"'$count\\\"\\n  else\\n    printf '\\\"'rejected (exit %s): ' \\\\\\\"\\\"'$result\\\"\\n    sed -n '\\\"'1p' /tmp/bf-status-error.\\\"'$$\\n  fi\\ndone\\nrm -f /tmp/bf-status-output.$$ /tmp/bf-status-error.$$\\nprintf '\\\"'%s\\\\\\\\n' '-- state-machine test implementation --'\\nsed -n '1,300p' /home/coding/.needle/canary/state_machine.py\\nprintf '%s\\\\\\\\n' '-- task bead before note --'\\nbead show needle-16546862 --json\\\"` rejected: rm -f style commands are not permitted. Use a safer approach\")" }
exec
/bin/bash -lc "set -o pipefail
printf '%s\\n' '-- read-only bf filter behavior --'
for status in open in_progress blocked closed completed pending ready deferred; do
  if count="'$(/home/coding/backups/bf-tooling-removed-20260814/bf list --workspace /home/coding/.needle/home/coding --status "$status" --json | jq '"'length'); then
    printf '%-12s accepted; %s matching record(s)\\n' \""'$status" "$count"
  else
    printf '"'%-12s rejected\\n' \""'$status"
  fi
done
printf '"'%s\\n' '-- state-machine test implementation --'
sed -n '1,300p' /home/coding/.needle/canary/state_machine.py
printf '%s\\n' '-- task bead before note --'
bead show needle-16546862 --json" in /home/coding/NEEDLE
 succeeded in 0ms:
-- read-only bf filter behavior --
open         accepted;  matching record(s)
in_progress  accepted; 24 matching record(s)
blocked      accepted;  matching record(s)
closed       accepted;  matching record(s)
completed    accepted;  matching record(s)
pending      accepted;  matching record(s)
ready        accepted;  matching record(s)
deferred     accepted;  matching record(s)
-- state-machine test implementation --
#!/usr/bin/env python3
"""
State Machine Test Worker
Transitions through: BOOTING → SELECTING → CLAIMING
"""

import sys
import time
from enum import Enum


class WorkerState(Enum):
    BOOTING = "BOOTING"
    SELECTING = "SELECTING"
    CLAIMING = "CLAIMING"
    DONE = "DONE"


class Worker:
    def __init__(self):
        self.state = WorkerState.BOOTING
        self.transitions = []

    def transition(self, new_state: WorkerState):
        """Record and execute state transition"""
        if new_state != self.state:
            old_state = self.state
            self.state = new_state
            self.transitions.append((old_state.value, new_state.value))
            print(f"[{old_state.value}] → [{new_state.value}]")

    def run(self):
        """Execute the full state machine"""
        print("Starting state machine worker...")

        # Initial state
        print(f"Initial state: {self.state.value}")
        self.transitions.append(("START", self.state.value))

        # BOOTING → SELECTING
        time.sleep(0.1)
        self.transition(WorkerState.SELECTING)

        # SELECTING → CLAIMING
        time.sleep(0.1)
        self.transition(WorkerState.CLAIMING)

        # CLAIMING → DONE
        time.sleep(0.1)
        self.transition(WorkerState.DONE)

        print(f"Final state: {self.state.value}")
        print(f"Total transitions: {len(self.transitions)}")

        return {
            "initial_state": "BOOTING",
            "final_state": self.state.value,
            "transitions": self.transitions,
            "success": True
        }


def main():
    worker = Worker()
    result = worker.run()

    # Verify all expected states were visited
    expected_states = ["BOOTING", "SELECTING", "CLAIMING"]
    visited_states = [t[1] for t in result["transitions"]]

    missing = [s for s in expected_states if s not in visited_states]
    if missing:
        print(f"ERROR: Missing states: {missing}")
        sys.exit(1)

    print("✓ All expected states visited")
    print(f"✓ State machine integrity test passed")

    return result


if __name__ == "__main__":
    main()
-- task bead before note --
[{"assignee":"codex-gpt-5.6-terra-xhigh-terra-zaiproxy","comments":[],"created_at":"2026-08-17T09:42:38.306399323Z","dependencies":[],"description":"Read all four expected/*.yaml files under ~/.needle/canary/expected/ and document:\n\n1. What each test asserts (final_status, labels, etc.)\n2. Which assertions are stale vs. current\n3. What bead-forge's actual vocabulary is (open, in_progress, blocked, closed, completed, pending, ready)\n\nDeliverable: A summary in this bead's body listing:\n- All four expected files and their current assertions\n- Current bead-forge vocabulary (verify with 'bf show --json' on a real bead)\n- What needs to change in each file\n\nAcceptance: Bead body contains complete inventory of current expectations and what's stale.","id":"needle-16546862","labels":["split-child","verification-failed"],"priority":3,"revision":14,"status":"in_progress","title":"Inventory and document current canary expectations","updated_at":"2026-08-22T04:50:45.307447333Z"}]

exec
/bin/bash -lc "sed -n '780,970p' src/outcome/mod.rs
printf '%s\\n' '-- canary git history for fixture provenance --'
git -C /home/coding/.needle/canary log --oneline --all -- expected/bead-1b6a6357.yaml expected/bead-54b7174d.yaml expected/bead-60c759fa.yaml expected/bead-e6f0b50d.yaml | sed -n '1,120p'
printf '%s\\n' '-- fixture changes --'
git -C /home/coding/.needle/canary log -p --all -- expected/bead-1b6a6357.yaml expected/bead-54b7174d.yaml expected/bead-60c759fa.yaml expected/bead-e6f0b50d.yaml | sed -n '1,640p'" in /home/coding/NEEDLE
 succeeded in 0ms:
                    tracing::warn!(
                        bead_id = %bead.id,
                        error = %e,
                        "failed to increment failure count after gate failure"
                    );
                }
            }
        }

        // Add a label indicating verification failure.
        if let Err(e) = tokio::time::timeout(
            std::time::Duration::from_secs(30),
            store.add_label(&bead.id, "verification-failed"),
        )
        .await
        {
            tracing::warn!(
                bead_id = %bead.id,
                error = %e,
                "failed to add verification-failed label"
            );
        }

        Ok((action, events))
    }

    /// Failure: release bead and increment failure count.
    ///
    /// Mitosis evaluation (for multi-task splitting) is handled externally by
    /// the worker after outcome handling — see `MitosisEvaluator`.
    ///
    /// If br calls timeout or fail, logs the error and continues — does not
    /// block the worker in HANDLING state indefinitely.
    async fn handle_failure(
        &self,
        store: &dyn BeadStore,
        bead: &Bead,
    ) -> Result<(BeadAction, Vec<EventKind>)> {
        tracing::warn!(bead_id = %bead.id, "agent failure — releasing bead");

        let mut events = self.release_bead(store, bead).await?;

        // If release succeeded, increment failure count and check the
        // quarantine threshold. A bead that has exceeded
        // `outcome.quarantine_after_failures` consecutive failures is
        // quarantined (status=blocked) instead of being left released-to-open
        // for the next cycle to re-claim and fail again indefinitely. This
        // also closes the mitosis `NotSplittable` fallthrough (worker/mod.rs):
        // that verdict no longer matters for beads at or past the ceiling,
        // since this check already ran before mitosis evaluation this cycle.
        let release_succeeded = events
            .iter()
            .any(|e| matches!(e, EventKind::BeadReleased { .. }));
        let mut action = BeadAction::Released;
        if release_succeeded {
            match self.increment_failure_count(store, bead).await {
                Ok(new_count) => {
                    let threshold = self.config.outcome.quarantine_after_failures;
                    if threshold > 0 && new_count >= threshold {
                        match self
                            .quarantine_bead(store, bead, new_count, threshold)
                            .await
                        {
                            Ok(quarantine_events) => {
                                events.extend(quarantine_events);
                                action = BeadAction::Quarantined;
                            }
                            Err(e) => {
                                tracing::warn!(
                                    bead_id = %bead.id,
                                    error = %e,
                                    "failed to quarantine bead after exceeding failure threshold"
                                );
                            }
                        }
                    }
                }
                Err(e) => {
                    tracing::warn!(
                        bead_id = %bead.id,
                        error = %e,
                        "failed to increment failure count after release"
                    );
                }
            }
        }

        // Ensure we emit a reason event for telemetry.
        if !events.iter().any(|e| {
            matches!(
                e,
                EventKind::BeadReleased { .. } | EventKind::BeadReleaseFailed { .. }
            )
        }) {
            events.push(EventKind::BeadReleased {
                bead_id: bead.id.clone(),
                reason: "failure".to_string(),
            });
        }

        Ok((action, events))
    }

    /// Timeout: release bead and add `deferred` label.
    ///
    /// If br calls timeout or fail, logs the error and continues — does not
    /// block the worker in HANDLING state indefinitely.
    async fn handle_timeout(
        &self,
        store: &dyn BeadStore,
        bead: &Bead,
    ) -> Result<(BeadAction, Vec<EventKind>)> {
        tracing::warn!(bead_id = %bead.id, "agent timed out — releasing bead as deferred");

        let mut events = self.release_bead(store, bead).await?;

        // If release succeeded, increment failure count and add deferred label.
        let release_succeeded = events
            .iter()
            .any(|e| matches!(e, EventKind::BeadReleased { .. }));
        if release_succeeded {
            // Increment failure count for auto-split tracking.
            if let Err(e) = self.increment_failure_count(store, bead).await {
                tracing::warn!(
                    bead_id = %bead.id,
                    error = %e,
                    "failed to increment failure count after timeout"
                );
            }

            if let Err(e) = tokio::time::timeout(
                std::time::Duration::from_secs(30),
                store.add_label(&bead.id, "deferred"),
            )
            .await
            {
                tracing::warn!(
                    bead_id = %bead.id,
                    error = %e,
                    "add_label deferred timed out or failed after timeout release"
                );
                events.push(EventKind::WorkerHandlingTimeout {
                    bead_id: bead.id.clone(),
                    outcome: "timeout".to_string(),
                    operation: "add_label".to_string(),
                    error: e.to_string(),
                });
            }
        }

        Ok((BeadAction::Deferred, events))
    }

    /// Crash: release bead and create alert bead with diagnostic info.
    async fn handle_crash(
        &self,
        store: &dyn BeadStore,
        bead: &Bead,
        signal_code: i32,
    ) -> Result<(BeadAction, Vec<EventKind>)> {
        tracing::error!(
            bead_id = %bead.id,
            signal_code,
            agent = %self.config.agent.default,
            "agent crashed — releasing bead and creating alert"
        );

        let events = self.release_bead(store, bead).await?;

        // Create alert bead with diagnostic info (best-effort).
        let signal_num = if signal_code > 128 {
            signal_code - 128
        } else {
            signal_code
        };
        let timestamp = Utc::now().to_rfc3339();
        let alert_title = format!("ALERT: Agent crash on bead {}", bead.id);
        let alert_body = format!(
            "## Agent Crash Report\n\
             \n\
             - **Bead ID**: {}\n\
             - **Agent**: {}\n\
             - **Exit code**: {} (signal {})\n\
             - **Workspace**: {}\n\
             - **Timestamp**: {}\n\
             \n\
             The agent process was killed. This bead has been released for retry.",
            bead.id,
            self.config.agent.default,
            signal_code,
            signal_num,
-- canary git history for fixture provenance --
f9891e3 Migrate canary workspace from bead-forge to bead-rs
-- fixture changes --
commit f9891e35668c5db1fb632396bb8ca0ef966de627
Author: jedarden <github@jedarden.com>
Date:   Mon Aug 17 05:30:10 2026 -0400

    Migrate canary workspace from bead-forge to bead-rs
    
    - Update .needle.yaml to use bead-rs backend
    - Rename expected files to match auto-generated bead IDs
    - Update expectations to use bead-rs vocabulary (closed, blocked)
    - Initialize bead-rs store and create test beads

diff --git a/expected/bead-1b6a6357.yaml b/expected/bead-1b6a6357.yaml
new file mode 100644
index 0000000..f785d1d
--- /dev/null
+++ b/expected/bead-1b6a6357.yaml
@@ -0,0 +1,3 @@
+type: success
+final_status: closed
+labels: []
diff --git a/expected/bead-54b7174d.yaml b/expected/bead-54b7174d.yaml
new file mode 100644
index 0000000..f6a6d6f
--- /dev/null
+++ b/expected/bead-54b7174d.yaml
@@ -0,0 +1,2 @@
+type: timeout
+final_status: closed
diff --git a/expected/bead-60c759fa.yaml b/expected/bead-60c759fa.yaml
new file mode 100644
index 0000000..09768b4
--- /dev/null
+++ b/expected/bead-60c759fa.yaml
@@ -0,0 +1,4 @@
+type: failure
+final_status: blocked
+labels:
+  - "failure-count:1"
diff --git a/expected/bead-e6f0b50d.yaml b/expected/bead-e6f0b50d.yaml
new file mode 100644
index 0000000..1342595
--- /dev/null
+++ b/expected/bead-e6f0b50d.yaml
@@ -0,0 +1,5 @@
+type: state_machine
+transitions:
+  - BOOTING
+  - SELECTING
+  - CLAIMING

exec
/bin/bash -lc "rg -n -C 8 'quarantine_after_failures|struct OutcomeConfig|impl Default for OutcomeConfig' src/config src tests .needle.yaml
printf '%s\\n' '-- canary migration commit metadata --'
git -C /home/coding/.needle/canary show --stat --oneline f9891e35668c5db1fb632396bb8ca0ef966de627
git -C /home/coding/.needle/canary show --format=fuller --no-patch f9891e35668c5db1fb632396bb8ca0ef966de627
printf '%s\\n' '-- current local canary git state --'
git -C /home/coding/.needle/canary status --short" in /home/coding/NEEDLE
 succeeded in 0ms:
src/config/tiers.rs-238-    ("strands.splice.live_loop_window_secs", ReloadTier::Live),
src/config/tiers.rs-239-    ("strands.knot.alert_cooldown_minutes", ReloadTier::Live),
src/config/tiers.rs-240-    ("strands.knot.exhaustion_threshold", ReloadTier::Live),
src/config/tiers.rs-241-    ("strands.knot.retry_backoff_secs", ReloadTier::Live),
src/config/tiers.rs-242-    // Mitosis (live)
src/config/tiers.rs-243-    ("strands.mitosis.enabled", ReloadTier::Live),
src/config/tiers.rs-244-    ("strands.mitosis.first_failure_only", ReloadTier::Live),
src/config/tiers.rs-245-    // Outcome (live)
src/config/tiers.rs:246:    ("outcome.quarantine_after_failures", ReloadTier::Live),
src/config/tiers.rs-247-    // ═══════════════════════════════════════════════════════════════════════════════
src/config/tiers.rs-248-    // TIER B: COMPONENT REBUILD
src/config/tiers.rs-249-    // ═══════════════════════════════════════════════════════════════════════════════
src/config/tiers.rs-250-    // These require component reconstruction at the cycle boundary.
src/config/tiers.rs-251-
src/config/tiers.rs-252-    // Telemetry (rebuild Telemetry, may reinstall OTLP layer)
src/config/tiers.rs-253-    ("telemetry.file_sink.enabled", ReloadTier::Rebuild),
src/config/tiers.rs-254-    ("telemetry.file_sink.log_dir", ReloadTier::Rebuild),
--
src/config/mod.rs-5362-impl FabricConfig {
src/config/mod.rs-5363-    fn default_timeout() -> u64 {
src/config/mod.rs-5364-        2
src/config/mod.rs-5365-    }
src/config/mod.rs-5366-}
src/config/mod.rs-5367-
src/config/mod.rs-5368-/// Outcome handling configuration (failure circuit-breaker, etc.).
src/config/mod.rs-5369-#[derive(Debug, Clone, Serialize, Deserialize)]
src/config/mod.rs:5370:pub struct OutcomeConfig {
src/config/mod.rs-5371-    /// Consecutive failures before quarantining a bead (0 = disabled).
src/config/mod.rs-5372-    ///
src/config/mod.rs-5373-    /// When a bead accumulates this many consecutive failures, it is automatically
src/config/mod.rs-5374-    /// quarantined: status is set to `blocked`, a `cycling` label is added, and a
src/config/mod.rs-5375-    /// `BeadQuarantined` telemetry event is emitted.
src/config/mod.rs-5376-    ///
src/config/mod.rs-5377-    /// The default (5) is above Pluck's `split_after_failures` default (3) so
src/config/mod.rs-5378-    /// mitosis gets first crack at splitting the bead before quarantine kicks in.
src/config/mod.rs:5379:    #[serde(default = "OutcomeConfig::default_quarantine_after_failures")]
src/config/mod.rs:5380:    pub quarantine_after_failures: u32,
src/config/mod.rs-5381-}
src/config/mod.rs-5382-
src/config/mod.rs:5383:impl Default for OutcomeConfig {
src/config/mod.rs-5384-    fn default() -> Self {
src/config/mod.rs-5385-        OutcomeConfig {
src/config/mod.rs:5386:            quarantine_after_failures: Self::default_quarantine_after_failures(),
src/config/mod.rs-5387-        }
src/config/mod.rs-5388-    }
src/config/mod.rs-5389-}
src/config/mod.rs-5390-
src/config/mod.rs-5391-impl OutcomeConfig {
src/config/mod.rs:5392:    fn default_quarantine_after_failures() -> u32 {
src/config/mod.rs-5393-        5
src/config/mod.rs-5394-    }
src/config/mod.rs-5395-}
src/config/mod.rs-5396-
src/config/mod.rs-5397-impl ConfigTier for OutcomeConfig {
src/config/mod.rs-5398-    fn reload_tier(&self) -> ReloadTier {
src/config/mod.rs-5399-        // Tier A: Quarantine threshold is read live during outcome handling
src/config/mod.rs-5400-        ReloadTier::Live
--
src/strand/pluck.rs-172-    ///
src/strand/pluck.rs-173-    /// Sort key: `(priority ASC, failure_count ASC, created_at ASC, id ASC)`.
src/strand/pluck.rs-174-    ///
src/strand/pluck.rs-175-    /// `failure_count` sits ahead of `created_at` deliberately: without it, a
src/strand/pluck.rs-176-    /// bead that keeps failing is (by construction) always at least as old as
src/strand/pluck.rs-177-    /// its ready siblings, so it sorts to slot 1 every single cycle and the
src/strand/pluck.rs-178-    /// worker never even tries the other ready work sitting behind it. This
src/strand/pluck.rs-179-    /// does not by itself stop the retry loop — `split_after_failures` /
src/strand/pluck.rs:180:    /// `OutcomeConfig::quarantine_after_failures` own that — it just stops a
src/strand/pluck.rs-181-    /// struggling bead from starving healthier ready beads at the same
src/strand/pluck.rs-182-    /// priority while it climbs toward the quarantine threshold.
src/strand/pluck.rs-183-    /// The id tie-breaker ensures identical ordering across platforms.
src/strand/pluck.rs-184-    fn sort_candidates(candidates: &mut [Bead]) {
src/strand/pluck.rs-185-        candidates.sort_by(|a, b| {
src/strand/pluck.rs-186-            a.priority
src/strand/pluck.rs-187-                .cmp(&b.priority)
src/strand/pluck.rs-188-                .then_with(|| Self::extract_failure_count(a).cmp(&Self::extract_failure_count(b)))
--
src/worker/mod.rs-4214-        replace!(
src/worker/mod.rs-4215-            "strands.mitosis.first_failure_only",
src/worker/mod.rs-4216-            next.strands.mitosis.first_failure_only,
src/worker/mod.rs-4217-            candidate.strands.mitosis.first_failure_only
src/worker/mod.rs-4218-        );
src/worker/mod.rs-4219-
src/worker/mod.rs-4220-        // These top-level sections are entirely Tier A.
src/worker/mod.rs-4221-        replace!(
src/worker/mod.rs:4222:            "outcome.quarantine_after_failures",
src/worker/mod.rs:4223:            next.outcome.quarantine_after_failures,
src/worker/mod.rs:4224:            candidate.outcome.quarantine_after_failures
src/worker/mod.rs-4225-        );
src/worker/mod.rs-4226-        replace!(
src/worker/mod.rs-4227-            "budget.warn_usd",
src/worker/mod.rs-4228-            next.budget.warn_usd,
src/worker/mod.rs-4229-            candidate.budget.warn_usd
src/worker/mod.rs-4230-        );
src/worker/mod.rs-4231-        replace!(
src/worker/mod.rs-4232-            "budget.stop_usd",
--
src/bead_store/mod.rs-1177-    /// - `Claimed(bead)` — success, returns the full bead.
src/bead_store/mod.rs-1178-    /// - `NotClaimable { reason }` — no beads available to claim.
src/bead_store/mod.rs-1179-    async fn claim_auto(&self, actor: &str) -> Result<ClaimResult>;
src/bead_store/mod.rs-1180-
src/bead_store/mod.rs-1181-    /// Release a claimed bead back to open (e.g., after agent failure).
src/bead_store/mod.rs-1182-    async fn release(&self, id: &BeadId) -> Result<()>;
src/bead_store/mod.rs-1183-
src/bead_store/mod.rs-1184-    /// Quarantine a bead by setting status=blocked (e.g., after it exceeds the
src/bead_store/mod.rs:1185:    /// consecutive-failure threshold in `OutcomeConfig::quarantine_after_failures`).
src/bead_store/mod.rs-1186-    ///
src/bead_store/mod.rs-1187-    /// Unlike `release`, this deliberately does NOT clear the assignee or return
src/bead_store/mod.rs-1188-    /// the bead to a claimable state — the whole point is to stop Pluck from
src/bead_store/mod.rs-1189-    /// re-selecting it until a human (or a future auto-split) intervenes.
src/bead_store/mod.rs-1190-    async fn block(&self, id: &BeadId) -> Result<()>;
src/bead_store/mod.rs-1191-
src/bead_store/mod.rs-1192-    /// Clear the assignee on a bead without changing its status.
src/bead_store/mod.rs-1193-    ///
--
src/config/tiers.rs-238-    ("strands.splice.live_loop_window_secs", ReloadTier::Live),
src/config/tiers.rs-239-    ("strands.knot.alert_cooldown_minutes", ReloadTier::Live),
src/config/tiers.rs-240-    ("strands.knot.exhaustion_threshold", ReloadTier::Live),
src/config/tiers.rs-241-    ("strands.knot.retry_backoff_secs", ReloadTier::Live),
src/config/tiers.rs-242-    // Mitosis (live)
src/config/tiers.rs-243-    ("strands.mitosis.enabled", ReloadTier::Live),
src/config/tiers.rs-244-    ("strands.mitosis.first_failure_only", ReloadTier::Live),
src/config/tiers.rs-245-    // Outcome (live)
src/config/tiers.rs:246:    ("outcome.quarantine_after_failures", ReloadTier::Live),
src/config/tiers.rs-247-    // ═══════════════════════════════════════════════════════════════════════════════
src/config/tiers.rs-248-    // TIER B: COMPONENT REBUILD
src/config/tiers.rs-249-    // ═══════════════════════════════════════════════════════════════════════════════
src/config/tiers.rs-250-    // These require component reconstruction at the cycle boundary.
src/config/tiers.rs-251-
src/config/tiers.rs-252-    // Telemetry (rebuild Telemetry, may reinstall OTLP layer)
src/config/tiers.rs-253-    ("telemetry.file_sink.enabled", ReloadTier::Rebuild),
src/config/tiers.rs-254-    ("telemetry.file_sink.log_dir", ReloadTier::Rebuild),
--
src/outcome/mod.rs-751-        // repeatable than an agent failure and must respect the same ceiling.
src/outcome/mod.rs-752-        let release_succeeded = events
src/outcome/mod.rs-753-            .iter()
src/outcome/mod.rs-754-            .any(|e| matches!(e, EventKind::BeadReleased { .. }));
src/outcome/mod.rs-755-        let mut action = BeadAction::Released;
src/outcome/mod.rs-756-        if release_succeeded {
src/outcome/mod.rs-757-            match self.increment_failure_count(store, bead).await {
src/outcome/mod.rs-758-                Ok(new_count) => {
src/outcome/mod.rs:759:                    let threshold = self.config.outcome.quarantine_after_failures;
src/outcome/mod.rs-760-                    if threshold > 0 && new_count >= threshold {
src/outcome/mod.rs-761-                        match self
src/outcome/mod.rs-762-                            .quarantine_bead(store, bead, new_count, threshold)
src/outcome/mod.rs-763-                            .await
src/outcome/mod.rs-764-                        {
src/outcome/mod.rs-765-                            Ok(quarantine_events) => {
src/outcome/mod.rs-766-                                events.extend(quarantine_events);
src/outcome/mod.rs-767-                                action = BeadAction::Quarantined;
--
src/outcome/mod.rs-816-        bead: &Bead,
src/outcome/mod.rs-817-    ) -> Result<(BeadAction, Vec<EventKind>)> {
src/outcome/mod.rs-818-        tracing::warn!(bead_id = %bead.id, "agent failure — releasing bead");
src/outcome/mod.rs-819-
src/outcome/mod.rs-820-        let mut events = self.release_bead(store, bead).await?;
src/outcome/mod.rs-821-
src/outcome/mod.rs-822-        // If release succeeded, increment failure count and check the
src/outcome/mod.rs-823-        // quarantine threshold. A bead that has exceeded
src/outcome/mod.rs:824:        // `outcome.quarantine_after_failures` consecutive failures is
src/outcome/mod.rs-825-        // quarantined (status=blocked) instead of being left released-to-open
src/outcome/mod.rs-826-        // for the next cycle to re-claim and fail again indefinitely. This
src/outcome/mod.rs-827-        // also closes the mitosis `NotSplittable` fallthrough (worker/mod.rs):
src/outcome/mod.rs-828-        // that verdict no longer matters for beads at or past the ceiling,
src/outcome/mod.rs-829-        // since this check already ran before mitosis evaluation this cycle.
src/outcome/mod.rs-830-        let release_succeeded = events
src/outcome/mod.rs-831-            .iter()
src/outcome/mod.rs-832-            .any(|e| matches!(e, EventKind::BeadReleased { .. }));
src/outcome/mod.rs-833-        let mut action = BeadAction::Released;
src/outcome/mod.rs-834-        if release_succeeded {
src/outcome/mod.rs-835-            match self.increment_failure_count(store, bead).await {
src/outcome/mod.rs-836-                Ok(new_count) => {
src/outcome/mod.rs:837:                    let threshold = self.config.outcome.quarantine_after_failures;
src/outcome/mod.rs-838-                    if threshold > 0 && new_count >= threshold {
src/outcome/mod.rs-839-                        match self
src/outcome/mod.rs-840-                            .quarantine_bead(store, bead, new_count, threshold)
src/outcome/mod.rs-841-                            .await
src/outcome/mod.rs-842-                        {
src/outcome/mod.rs-843-                            Ok(quarantine_events) => {
src/outcome/mod.rs-844-                                events.extend(quarantine_events);
src/outcome/mod.rs-845-                                action = BeadAction::Quarantined;
--
src/outcome/mod.rs-1733-            "should add failure-count:3"
src/outcome/mod.rs-1734-        );
src/outcome/mod.rs-1735-    }
src/outcome/mod.rs-1736-
src/outcome/mod.rs-1737-    // ── ADR-012: failure-quarantine circuit breaker ──
src/outcome/mod.rs-1738-
src/outcome/mod.rs-1739-    #[tokio::test]
src/outcome/mod.rs-1740-    async fn handle_failure_quarantines_bead_at_threshold() {
src/outcome/mod.rs:1741:        // Default quarantine_after_failures is 5. A bead already at
src/outcome/mod.rs-1742-        // failure-count:4 crosses the threshold on this attempt.
src/outcome/mod.rs-1743-        let handler = test_handler();
src/outcome/mod.rs-1744-        let store = MockBeadStore::new(BeadStatus::InProgress)
src/outcome/mod.rs-1745-            .with_labels(vec!["failure-count:4".to_string()]);
src/outcome/mod.rs-1746-        let bead = test_bead(BeadStatus::InProgress);
src/outcome/mod.rs-1747-
src/outcome/mod.rs-1748-        let result = handler
src/outcome/mod.rs-1749-            .handle(&store, &bead, &test_output(1), false)
--
src/outcome/mod.rs-1799-            !actions.iter().any(|a| matches!(a, StoreAction::Block(_))),
src/outcome/mod.rs-1800-            "4th consecutive failure must not yet quarantine"
src/outcome/mod.rs-1801-        );
src/outcome/mod.rs-1802-    }
src/outcome/mod.rs-1803-
src/outcome/mod.rs-1804-    #[tokio::test]
src/outcome/mod.rs-1805-    async fn handle_failure_quarantine_disabled_when_threshold_zero() {
src/outcome/mod.rs-1806-        let mut config = Config::default();
src/outcome/mod.rs:1807:        config.outcome.quarantine_after_failures = 0;
src/outcome/mod.rs-1808-        let handler = test_handler_with_config(config);
src/outcome/mod.rs-1809-        let store = MockBeadStore::new(BeadStatus::InProgress)
src/outcome/mod.rs-1810-            .with_labels(vec!["failure-count:99".to_string()]);
src/outcome/mod.rs-1811-        let bead = test_bead(BeadStatus::InProgress);
src/outcome/mod.rs-1812-
src/outcome/mod.rs-1813-        let result = handler
src/outcome/mod.rs-1814-            .handle(&store, &bead, &test_output(1), false)
src/outcome/mod.rs-1815-            .await
--
src/config/mod.rs-5362-impl FabricConfig {
src/config/mod.rs-5363-    fn default_timeout() -> u64 {
src/config/mod.rs-5364-        2
src/config/mod.rs-5365-    }
src/config/mod.rs-5366-}
src/config/mod.rs-5367-
src/config/mod.rs-5368-/// Outcome handling configuration (failure circuit-breaker, etc.).
src/config/mod.rs-5369-#[derive(Debug, Clone, Serialize, Deserialize)]
src/config/mod.rs:5370:pub struct OutcomeConfig {
src/config/mod.rs-5371-    /// Consecutive failures before quarantining a bead (0 = disabled).
src/config/mod.rs-5372-    ///
src/config/mod.rs-5373-    /// When a bead accumulates this many consecutive failures, it is automatically
src/config/mod.rs-5374-    /// quarantined: status is set to `blocked`, a `cycling` label is added, and a
src/config/mod.rs-5375-    /// `BeadQuarantined` telemetry event is emitted.
src/config/mod.rs-5376-    ///
src/config/mod.rs-5377-    /// The default (5) is above Pluck's `split_after_failures` default (3) so
src/config/mod.rs-5378-    /// mitosis gets first crack at splitting the bead before quarantine kicks in.
src/config/mod.rs:5379:    #[serde(default = "OutcomeConfig::default_quarantine_after_failures")]
src/config/mod.rs:5380:    pub quarantine_after_failures: u32,
src/config/mod.rs-5381-}
src/config/mod.rs-5382-
src/config/mod.rs:5383:impl Default for OutcomeConfig {
src/config/mod.rs-5384-    fn default() -> Self {
src/config/mod.rs-5385-        OutcomeConfig {
src/config/mod.rs:5386:            quarantine_after_failures: Self::default_quarantine_after_failures(),
src/config/mod.rs-5387-        }
src/config/mod.rs-5388-    }
src/config/mod.rs-5389-}
src/config/mod.rs-5390-
src/config/mod.rs-5391-impl OutcomeConfig {
src/config/mod.rs:5392:    fn default_quarantine_after_failures() -> u32 {
src/config/mod.rs-5393-        5
src/config/mod.rs-5394-    }
src/config/mod.rs-5395-}
src/config/mod.rs-5396-
src/config/mod.rs-5397-impl ConfigTier for OutcomeConfig {
src/config/mod.rs-5398-    fn reload_tier(&self) -> ReloadTier {
src/config/mod.rs-5399-        // Tier A: Quarantine threshold is read live during outcome handling
src/config/mod.rs-5400-        ReloadTier::Live
-- canary migration commit metadata --
f9891e3 Migrate canary workspace from bead-forge to bead-rs
 .needle.yaml                                     | 2 ++
 expected/{canary-bzl.yaml => bead-1b6a6357.yaml} | 2 +-
 expected/bead-54b7174d.yaml                      | 2 ++
 expected/{canary-8ej.yaml => bead-60c759fa.yaml} | 2 +-
 expected/{canary-atw.yaml => bead-e6f0b50d.yaml} | 0
 expected/canary-nnw.yaml                         | 2 --
 6 files changed, 6 insertions(+), 4 deletions(-)
commit f9891e35668c5db1fb632396bb8ca0ef966de627
Author:     jedarden <github@jedarden.com>
AuthorDate: Mon Aug 17 05:30:10 2026 -0400
Commit:     jedarden <github@jedarden.com>
CommitDate: Mon Aug 17 05:30:10 2026 -0400

    Migrate canary workspace from bead-forge to bead-rs
    
    - Update .needle.yaml to use bead-rs backend
    - Rename expected files to match auto-generated bead IDs
    - Update expectations to use bead-rs vocabulary (closed, blocked)
    - Initialize bead-rs store and create test beads
-- current local canary git state --
 D .beads/.bf_history/issues-20260802T174845-378084391.jsonl
 D .beads/.bf_history/issues-20260802T175103-597742125.jsonl
 M .beads/.gitignore
 D .beads/config.yaml
 M .beads/events.jsonl
 M .beads/heartbeats.jsonl
 D .beads/issues.jsonl
 D .beads/metadata.json
 D .beads/traces/canary-atw/metadata.json
 D .beads/traces/canary-atw/stderr.txt
 D .beads/traces/canary-atw/stdout.txt
 D .beads/traces/canary-atw/trace.jsonl
 M .needle-predispatch-sha
?? .beads/checkpoint/
?? .beads/config.json
?? .beads/traces/bead-1b6a6357/
?? .beads/traces/bead-54b7174d/
?? .beads/traces/bead-60c759fa/
?? .beads/traces/bead-e6f0b50d/

codex
The evidence is decisive: the timeout and first-failure fixtures conflict with the current outcome handlers, and the state-machine fixture cannot pass because the runner records no state transitions. I’m recording the full inventory and the cross-claim evidence on the bead now; no repository files need changing for this documentation task.
exec
/bin/bash -lc 'canary_inventory=$'"'CANARY EXPECTATION INVENTORY (2026-08-22)\\n\\nScope and evidence\\n- Canary workspace .needle.yaml binds bead_cli.backend: bead-rs.\\n- Read all four expected/*.yaml files and their bead-rs records.\\n- Verified the legacy CLI with: bf 0.4.1 show bd-fvw --workspace /home/coding/.needle/home/coding --json. It returned a real bead with status in_progress.\\n- bf robot-docs identifies the core lifecycle vocabulary as open, in_progress, closed. bf list accepts filter strings open, in_progress, blocked, closed, completed, pending, ready (and deferred), but ready is the computed ready/unblocked query, not a lifecycle status. The bf SQLite schema has no status CHECK constraint, so filter acceptance is not proof that all are canonical statuses. completed is an observed bf custom alias; blocked/pending are not declared by its robot contract.\\n\\nCurrent fixture inventory (and current canary store snapshot)\\n1. expected/bead-1b6a6357.yaml: success; final_status: closed; labels: []. Snapshot: closed, labels []. This is the correct success assertion. No YAML change needed, though the snapshot came from a cross-claimed run.\\n2. expected/bead-54b7174d.yaml: timeout; final_status: closed. Snapshot: closed, labels []. STALE: handle_timeout releases the bead to open, increments failure-count, and adds the deferred label. Change final_status to open. Timeout expectations currently cannot assert labels, so support for labels must be added to ExpectedOutcome::Timeout before asserting deferred/failure-count:1.\\n3. expected/bead-60c759fa.yaml: failure; final_status: blocked; labels: [failure-count:1]. Snapshot: open, labels []. STALE status: current first-failure behavior releases to open and adds failure-count:1; quarantine/blocked happens only at the configured threshold (default 5). Change final_status to open; retain failure-count:1. The snapshot lacks the label because this fixture did not receive a failure run.\\n4. expected/bead-e6f0b50d.yaml: state_machine; transitions [BOOTING, SELECTING, CLAIMING]. The three values match the standalone state_machine.py mini-test, but the production WorkerState has more states and CanaryRunner::get_actual_outcome always returns state_transitions: []. Thus this assertion is currently unobservable and cannot pass; no YAML-only fix exists. Wire state capture (and make the runner execute the intended test) before deciding the canonical sequence.\\n\\nCritical contamination / stale-baseline evidence\\nCanary events show named runners claimed different beads: canary-bead-54b7174d -> bead-1b6a6357; canary-bead-e6f0b50d -> bead-60c759fa; canary-bead-60c759fa -> bead-54b7174d; canary-bead-1b6a6357 -> bead-e6f0b50d. Therefore the current store snapshots are not valid per-scenario evidence until runs target and reset their own fixture bead.\\n\\nRepository source also still contains contradictory legacy defaults: success default done, failure default open, timeout default open; the source comment says timeout is deferred, while the handler actually releases to open and labels deferred. Use closed only for successful completion; do not treat done, completed, pending, or ready as interchangeable canonical final statuses for this bead-rs canary.'
bead update needle-16546862 --if-revision 14 --notes \""'$canary_inventory"
bead show needle-16546862 --json' in /home/coding/NEEDLE
 succeeded in 0ms:
needle-16546862
[{"assignee":"codex-gpt-5.6-terra-xhigh-terra-zaiproxy","comments":[],"created_at":"2026-08-17T09:42:38.306399323Z","dependencies":[],"description":"Read all four expected/*.yaml files under ~/.needle/canary/expected/ and document:\n\n1. What each test asserts (final_status, labels, etc.)\n2. Which assertions are stale vs. current\n3. What bead-forge's actual vocabulary is (open, in_progress, blocked, closed, completed, pending, ready)\n\nDeliverable: A summary in this bead's body listing:\n- All four expected files and their current assertions\n- Current bead-forge vocabulary (verify with 'bf show --json' on a real bead)\n- What needs to change in each file\n\nAcceptance: Bead body contains complete inventory of current expectations and what's stale.","id":"needle-16546862","labels":["split-child","verification-failed"],"priority":3,"revision":15,"status":"in_progress","title":"Inventory and document current canary expectations","updated_at":"2026-08-22T04:55:49.540564304Z"}]

exec
/bin/bash -lc 'bead comments --help && bead comments add --help' in /home/coding/NEEDLE
 exited 2 in 0ms:
error: unrecognized subcommand 'comments'

Usage: bead <COMMAND>

For more information, try '--help'.

exec
/bin/bash -lc "printf '%s\\n' '-- recorded bead notes --'
sqlite3 -header -column .beads/beads.db \"SELECT id, status, revision, length(notes) AS notes_length, substr(notes, 1, 200) AS notes_prefix FROM issues WHERE id = 'needle-16546862';\"
printf '%s\\n' '-- affected worktree paths --'
git status --short" in /home/coding/NEEDLE
 succeeded in 0ms:
-- recorded bead notes --
Error: in prepare, no such column: status
  SELECT id, status, revision, length(notes) AS notes_length, substr(notes, 1, 2
             ^--- error here
-- affected worktree paths --
 M .beads/bypasses.jsonl
 M .beads/checkpoint/current.json
 M .beads/checkpoint/forensic.jsonl
 M .beads/checkpoint/previous.json
 M .beads/events.jsonl
 M .beads/heartbeats.jsonl
 M .beads/traces/needle-17fabf0a/metadata.json
 M .beads/traces/needle-17fabf0a/stdout.txt
 M .beads/traces/needle-2a19d87b/metadata.json
 M .beads/traces/needle-2a19d87b/stderr.txt
 M .beads/traces/needle-2a19d87b/stdout.txt
 M .needle-predispatch-sha
?? .beads/checkpoint/objects/gen-c61dcd01141cbc6c5eb50c7316c4d3f3.jsonl
?? .beads/checkpoint/objects/gen-e8af9132418aef65afdbe945b5284b1e.jsonl
?? .beads/traces/needle-09133765/
?? .beads/traces/needle-5a7b6982/
?? .beads/traces/needle-f40a5de7/

exec
/bin/bash -lc "sqlite3 .beads/beads.db '.schema issues'
bead close --help" in /home/coding/NEEDLE
 succeeded in 0ms:
CREATE TABLE issues (
    id TEXT PRIMARY KEY,
    title TEXT NOT NULL,
    description TEXT DEFAULT '',
    notes TEXT DEFAULT '',
    priority INTEGER NOT NULL CHECK (priority >= 0 AND priority <= 4),
    issue_type TEXT NOT NULL DEFAULT 'task',
    base_status TEXT NOT NULL CHECK (base_status IN ('open', 'in_progress', 'deferred', 'closed')),
    manual_blocked INTEGER NOT NULL DEFAULT 0 CHECK (manual_blocked IN (0, 1)),
    assignee TEXT,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    closed_at TEXT,
    close_reason TEXT,
    source_repo TEXT,
    profile TEXT DEFAULT 'native-v1',
    schema_ref TEXT DEFAULT 'urn:bead-rs:schema:issue:native-v1'
, revision INTEGER NOT NULL DEFAULT 1, ready_since TEXT, last_claim_sequence INTEGER, attempt_tier INTEGER NOT NULL DEFAULT 0 CHECK (attempt_tier >= 0 AND attempt_tier <= 3), consecutive_failures INTEGER NOT NULL DEFAULT 0, retry_after_claim_sequence INTEGER);
CREATE INDEX issues_readiness ON issues (
    base_status, manual_blocked, assignee, priority, created_at, id
);
CREATE INDEX issues_revision ON issues (id, revision);
CREATE INDEX issues_ready_frontier ON issues (base_status, manual_blocked, assignee, priority, created_at, id);
CREATE INDEX issues_attempt_tier ON issues (attempt_tier, last_claim_sequence);
Close an issue with a required reason.

Close transitions an issue to closed status and records the close reason.
The reason must be non-empty and is preserved for audit and debugging.
Closing clears manual blocking and may expose dependent issues.

EXAMPLES:
  bead close bead-123abc456789def --reason "Completed successfully"
  bead close ID --reason "Fixed authentication bug"
  bead close ID --reason "Duplicate of ID-456"
  bead close ID --reason "Done" --if-revision 3    # With revision guard

SEMANTICS:
  - All non-closed states → closed (semantic close)
  - closed with matching reason → no-op, idempotent
  - closed with different reason → conflict (exit 4)

REQUIREMENTS:
  --reason TEXT must be non-empty after trimming whitespace

EFFECTS:
  - Sets base_status to closed
  - Clears manual_blocked flag
  - Sets closed_at to current time
  - Stores close_reason
  - Advances updated_at
  - Increments revision
  - Appends 'closed' audit event

REVISION GUARDS:
  --if-revision N provides optimistic concurrency control.
  If the issue's current revision doesn't match N, the close fails with
  exit code 4 and a conflict message. This prevents silent lost updates
  when multiple agents or humans modify the same issue concurrently.

IDEMPOTENCY:
  Repeating the same close command with the same reason is idempotent
  and does not change timestamps or append duplicate events.
  Changing the reason is a conflict (use update with new reason if needed).

Dependent issues may become ready when their last blocker is closed.

Usage: bead close [OPTIONS] --reason <REASON> <ID>

Arguments:
  <ID>
          Issue ID

Options:
      --reason <REASON>
          Close reason (required)

      --if-revision <IF_REVISION>
          Expected revision for optimistic concurrency control

      --fencing-token <FENCING_TOKEN>
          Fencing token for lease validation (advanced usage)

      --dry-run
          Dry run: show what would happen without making changes

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

exec
/bin/bash -lc "printf '%s\\n' '-- recorded notes verification --'
sqlite3 -header -column .beads/beads.db \"SELECT id, base_status, manual_blocked, revision, length(notes) AS notes_length, substr(notes, 1, 200) AS notes_prefix FROM issues WHERE id = 'needle-16546862';\"
bead close needle-16546862 --if-revision 15 --reason \"Documented all canary fixtures, the verified bead-forge vocabulary, stale assertions, and cross-claim evidence in bead notes.\"
bead show needle-16546862 --json" in /home/coding/NEEDLE
 succeeded in 0ms:
-- recorded notes verification --
id               base_status  manual_blocked  revision  notes_length  notes_prefix                                                    
---------------  -----------  --------------  --------  ------------  ----------------------------------------------------------------
needle-16546862  in_progress  0               15        3181          CANARY EXPECTATION INVENTORY (2026-08-22)                       
                                                                                                                                      
                                                                      Scope and evidence                                              
                                                                      - Canary workspace .needle.yaml binds bead_cli.backend: bead-rs.
                                                                      - Read all four expected/*.yaml files and their bead-rs records.
                                                                      - Verifi                                                        
needle-16546862
[{"assignee":"codex-gpt-5.6-terra-xhigh-terra-zaiproxy","comments":[],"created_at":"2026-08-17T09:42:38.306399323Z","dependencies":[],"description":"Read all four expected/*.yaml files under ~/.needle/canary/expected/ and document:\n\n1. What each test asserts (final_status, labels, etc.)\n2. Which assertions are stale vs. current\n3. What bead-forge's actual vocabulary is (open, in_progress, blocked, closed, completed, pending, ready)\n\nDeliverable: A summary in this bead's body listing:\n- All four expected files and their current assertions\n- Current bead-forge vocabulary (verify with 'bf show --json' on a real bead)\n- What needs to change in each file\n\nAcceptance: Bead body contains complete inventory of current expectations and what's stale.","id":"needle-16546862","labels":["split-child","verification-failed"],"priority":3,"revision":16,"status":"closed","title":"Inventory and document current canary expectations","updated_at":"2026-08-22T04:56:10.716229821Z"}]

codex
Completed and closed `needle-16546862`.

Recorded the full 3,181-character inventory in the bead notes. Key findings:

- Success fixture is current: `closed`, no labels.
- Timeout fixture is stale: should expect `open`; timeout labels are not currently assertable.
- Failure fixture is stale: first failure is `open` with `failure-count:1`, not `blocked` (quarantine starts at 5).
- State-machine fixture is untestable: the runner always records no transitions.
- Verified `bf show --json` on a real legacy bead; documented the distinction between lifecycle statuses and `ready` as a query concept.
- Canary event logs show every named runner claimed a different fixture bead, so current snapshots are contaminated as scenario evidence.

No repository files were changed, so no commit was created.
tokens used
138,227
