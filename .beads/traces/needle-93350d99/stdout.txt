Implemented and pushed commit `43540a2`.

Added a live config-reload regression test verifying:

- Invalid config emits `config.reload.rejected`
- Previous config remains active
- Worker stays running until clean shutdown
- No worker error or reload application occurs

Verification passed:

- Reload-focused tests
- `scripts/definition-of-done.sh --fast`

Bead `needle-93350d99` is closed.
