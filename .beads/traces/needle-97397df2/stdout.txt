Implemented, pushed, and closed `needle-97397df2`.

- Inverted the orphan test to require `Failure`, `Released`, and no `BeadOrphaned`: [src/outcome/mod.rs](/home/coding/NEEDLE/src/outcome/mod.rs:1639)
- Added the exact 2026-08-17 loop fixture and same-worker redispatch regression: [src/worker/mod.rs](/home/coding/NEEDLE/src/worker/mod.rs:5033)
- Both targeted tests pass.
- `scripts/definition-of-done.sh --fast` passes with zero failures.
- Pushed through commit `06a71a6`.
- Bead closed and checkpoint flushed.

A shared-index race caused the main test hunks to land in concurrent commit `acdffda`; history was preserved, with the bead-specific follow-up committed as `06a71a6`.
