OpenAI Codex v0.147.0
--------
workdir: /home/coding/NEEDLE
model: gpt-5.6-luna
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: xhigh
reasoning summaries: none
session id: 01a00e27-cd51-7ee2-b8da-7e924796db71
--------
user
[needle:codex-gpt-5.6-luna-xhigh-icg-2:needle-1bd77af2:explore]
## Task

Add loud failure for missing routed adapters

## Description

## Scope
Error handling when the routing logic selects an adapter that does not exist.

## Implement
- After routing selects an adapter, verify the adapter YAML exists
- If missing, fail dispatch loudly with clear error message (mentions model, matched rule, missing adapter)
- Do NOT fall back to builtin adapters (complements bf-14w and bf-2wi)

## Acceptance criteria
- [ ] Missing adapter YAML = dispatch failure, not silent fallback
- [ ] Error message includes: model name, matched pattern, missing adapter path
- [ ] Unit test: routing to non-existent adapter fails with appropriate error

## Depends on
Child 3 (routing must be wired first)



## Workspace

/home/coding/NEEDLE

## Context Files

(no context files found)

## Instructions

(no workspace instructions)

Complete the task described above. When finished:
- **If you changed files, commit and push them.** `git commit`, then `git push`. Real
  changes are source, tests, config, or documentation the task actually asked for.
- **If the task needed no file changes, do NOT manufacture a commit.** Verification-only
  work, work that turned out to be already done, and work you found blocked are all
  legitimate outcomes with nothing to commit. Record the finding on the bead instead:

`bead update needle-1bd77af2 --notes "what you checked and what you found"`

  Never create `notes/needle-1bd77af2.md`, a summary, a report, or a status file to satisfy a
  commit requirement. Status belongs on the bead, not in git history. A commit whose only
  changes are under `notes/` or `.beads/` does not count as shipped work and will be
  rejected.
- Close the bead:

`bead close needle-1bd77af2 --reason "..."`

If you cannot complete the task:
- Do NOT close the bead
- The bead will be automatically released for retry

Bead ID: needle-1bd77af2
2026-08-17T05:18:05.683374Z ERROR codex_rmcp_client::oauth::refresh_transaction: error=failed to refresh OAuth tokens for server ibkr: OAuth refresh token was rejected: Server returned error response: invalid_grant: Refresh token is invalid or expired
2026-08-17T05:18:05.814216Z ERROR codex_rmcp_client::oauth::refresh_transaction: error=failed to refresh OAuth tokens for server ibkr
hook: SessionStart
hook: SessionStart Completed
codex
I’ll inspect the current routing and adapter-loading paths, check the shared worktree for concurrent edits, then implement the loud missing-adapter failure with a focused unit test. I’ll run targeted Rust checks, commit the requested files on `main`, push to `origin`, and close the bead if verification passes.
exec
/bin/bash -lc "git status --short && printf '\\n--- AGENTS files ---\\n' && (rg --files -g 'AGENTS.md' -g '"'!target'"' .. 2>/dev/null || true) && printf '\\n--- top-level ---\\n' && rg --files -g '"'!target'"' | sed -n '1,120p'" in /home/coding/NEEDLE
 succeeded in 7ms:
test_trace_full.txt
needle-ci-podgc-override.yml
needle-ci-zpx54-build-logs.txt
test-exit-code.txt
test-failures-bf-3ksm3.md
CLAUDE.md
--help
docs/hero.png
install.sh
failing_tests_list.txt
test_template_rendering.rs
docs/checkpoint-publishing.md
docs/needle-ci-failure-investigation-2026-08-16.md
docs/plan/plan.md
docs/plan/bead-forge-to-bead-rs-rehydration-playbook.md
docs/sanitize_latency_ci.md
docs/processguard_coverage_catalog.md
docs/design/workspace-iteration-fix.md
docs/design/timeout-decomposition-types.md
docs/testing-isolation-patterns.md
docs/test-isolation-cross-reference-checklist.md
docs/heartbeat.md
docs/test-isolation-catalog.md
docs/marathon-instruction.md
docs/telemetry-field-capture-strategy.md
docs/worker-construction-logging.md
docs/process-spawning-test-catalog.md
docs/production-otlp-configuration-2026-08-15.md
docs/child_wait_analysis.md
docs/p95-calculation-algorithms.md
docs/agent-event-schema.md
docs/test-isolation-audit-report.md
docs/research/self-learning-agents.md
docs/research/criterion-percentile-research.md
docs/research/mitosis-implementation-and-failure-semantics.md
docs/research/bg-gate-validation.md
docs/research/beads-workflow-gemini.md
docs/research/spec2beads-decomposition.md
docs/research/concurrency-approaches-compared.md
docs/research/obc-multi-agent-workflow.md
docs/research/agent-skills-and-integrations.md
docs/research/ralph-loop-pattern.md
docs/research/beads-fleet-dashboard.md
docs/research/perles-orchestration-control-plane.md
docs/research/beads-polis-concurrent-fork.md
docs/research/steveyegge-beads-vision.md
docs/research/beads-rust-native-workflow.md
docs/research/beads-orchestration-claude.md
docs/research/ecosystem-overview.md
docs/compilation-error-detection.md
docs/span-scope-preservation-design.md
docs/concurrent-claim-cycle-safety-validation.md
docs/timeout-mitosis-decomposition-design.md
docs/configuration.md
docs/notes/resource-awareness-and-worker-density.md
docs/notes/worker-starvation-lessons.md
docs/notes/pulse-strand-enablement-2026-08-15.md
docs/notes/otlp-wire-capture.md
docs/notes/idle-strand-gating-semantics.md
docs/notes/bundler-build-integrity.md
docs/notes/bf-4utk.md
docs/notes/explore-strand-bugs.md
docs/notes/bead-rs-fleet-migration-2026-08-15.md
docs/notes/mitosis-explosion-postmortem.md
docs/notes/operational-fleet-lessons.md
docs/notes/claim-race-conditions.md
docs/notes/bead-lifecycle-bugs.md
docs/notes/self-modification-risks.md
docs/notes/bash-at-scale-problems.md
docs/notes/needle-binary-rollback.md
docs/test-isolation-comment-template.md
docs/api-pattern-transformation-guide.md
docs/full-latency-profile.md
docs/research-mitosis-implementation-and-failure-semantics.md
docs/examples/otel-collector/docker-compose.yml
docs/examples/otel-collector/loki-config.yaml
docs/examples/otel-collector/otel-collector-config.yaml
docs/examples/otel-collector/README.md
docs/examples/otel-collector/prometheus.yml
docs/test-isolation-inventory.md
docs/adr/008-fleet-resource-safety.md
docs/adr/003-cleanup-orphan-detection-gap.md
docs/adr/015-concurrent-same-repo-worker-isolation.md
docs/adr/006-bead-lifecycle-reliability.md
docs/adr/007-deploy-path-hardening.md
docs/adr/009-external-adopter-hardening.md
docs/adr/010-supervisor-zombie-reaping.md
docs/adr/011-mitosis-timeout-child-reaping.md
docs/adr/016-otlp-resource-propagation-and-roaming-worker-identity.md
docs/adr/005-unify-release-upgrade-with-canary-hot-reload.md
docs/adr/002-pluck-telemetry-isolation-and-process-tracking.md
docs/adr/012-quarantine-wiring-and-failure-aware-pluck-ordering.md
docs/adr/014-explicit-workspace-bead-backend-binding.md
docs/adr/004-recursive-workspace-discovery-default.md
docs/adr/013-pluggable-bead-cli-backends.md
docs/adr/001-explore-strand-hardening.md
docs/span-scoping-patterns-evaluation.md
docs/mend_audit_report.md
criterion.toml
full_benchmark.txt
test_full_output.log
CHANGELOG.md
Cargo.toml
rust-toolchain.toml
Cargo.lock
AGENTS.md
test_p95_stdout.txt
ci/Dockerfile.ci
ci/Dockerfile.ci-deps
ci/VERSION
README.md
test-failure-analysis-report.md
scripts/install-git-hooks.sh
scripts/otlp-wire-capture.sh
scripts/validate_heartbeat.sh
scripts/capture_test_output.sh
scripts/verify-shipped-commit.sh
scripts/commit-checkpoint.sh
scripts/run-tests-with-capture.sh
scripts/checkpoint-publish.sh

codex
The worktree contains substantial unrelated changes from other workers, including edits in `src/outcome/mod.rs`, `src/strand/mod.rs`, and a new `src/strand/resolve.rs`. I’ll preserve those changes and first map the routing implementation and its current tests so this task only touches the missing-adapter boundary.
exec
/bin/bash -lc "git log --oneline --decorate -20 --all -- src/strand src/config src/dispatch 2>/dev/null; printf '\\n--- branch ---\\n'; git branch --show-current; git remote -v" in /home/coding/NEEDLE
 succeeded in 0ms:
8089989 (refs/stash) WIP on main: 48e167a fix(needle-ea87796e): include active root objects in checkpoint commits
ec49a8d feat(needle-e647f937): emit SupervisorBinaryResolved telemetry event at startup
ecc207d fix(needle-f6afd68c): reclaim stale live claims
670e4c5 fix(needle-3653fee9): apply cargo fmt to fix CI formatting issues
17825d6 Merge branch 'main' of https://git.ardenone.com/jedarden/NEEDLE
7adc860 test(needle-b744ddbb): fix Auto backend fallback chain test to match ADR-013
bf98438 test(needle-fa4ee968): add tilde expansion tests for ~/.needle.d/ directory paths
dff0bc2 test(needle-fa4ee968): add tilde expansion tests for ~/.needle.d/ directory paths
4c39970 feat(needle-87d9ff51): add ChildBeadProposal struct for timeout decomposition
e380e71 fix(needle-6b6562c6): correct Auto backend discovery order to prefer bead over bf per ADR-013
359f5e6 fix(needle-b8e33c11): resolve clippy warnings in config module
bb8e7ef test(needle-3bec34d1): add unit tests for resolve_bead_cli with Bead backend discovery
5e82de0 feat(needle-5283da22): add supervisor update check validation
0e2bd69 fix(needle-9ec1c814): add Display impl for config::Backend
4196883 fix(needle-7ce03a39): accept structured OTLP TLS config
a2921db test(needle-65e8944f): cover configured Explore base interval
b963d72 Merge branch 'main' of https://git.ardenone.com/jedarden/NEEDLE
832b3f7 docs(needle-a78d47ae): document production OTLP configuration on ex44
a95840a chore(release): v0.3.1
b450aa8 fix(bead-backend): scope recovery errors by descriptor

--- branch ---
main
origin	https://git.ardenone.com/jedarden/NEEDLE.git (fetch)
origin	https://git.ardenone.com/jedarden/NEEDLE.git (push)

exec
/bin/bash -lc "rg -n -i \"adapter|route|routing|matched|builtin|model\" src tests Cargo.toml README.md docs/configuration.md | sed -n '1,320p'" in /home/coding/NEEDLE
 succeeded in 0ms:
docs/configuration.md:86:  # Directory containing adapter TOML files (default: ~/.config/needle/adapters)
docs/configuration.md:87:  adapters_dir: ~/.config/needle/adapters
docs/configuration.md:89:  # Model-to-adapter routing (optional)
docs/configuration.md:90:  routing:
docs/configuration.md:93:      - match_model: "(claude-)?(sonnet|opus|fable|haiku).*"
docs/configuration.md:94:        adapter: claude-print    # Use subscription billing for Anthropic models
docs/configuration.md:95:    default_adapter: claude-code-glm-4.7  # Fallback for non-matching models
docs/configuration.md:146:before the store is opened. A missing, unknown, or mismatched binding makes the
docs/configuration.md:312:    - event_filter: "outcome.*"         # Glob pattern matched against event_type
docs/configuration.md:374:# Set routing
docs/configuration.md:375:export NEEDLE_AGENT__ROUTING__DEFAULT_ADAPTER=claude-print
docs/configuration.md:400:  adapters_dir: ~/.config/needle/adapters
docs/configuration.md:401:  routing:
docs/configuration.md:403:      - match_model: "(claude-)?(sonnet|opus|fable|haiku).*"
docs/configuration.md:404:        adapter: claude-print
docs/configuration.md:405:    default_adapter: claude-code-glm-4.7
src/peer/mod.rs:426:            model: "claude-sonnet-4".to_string(),
src/peer/mod.rs:591:                model: None,
tests/fixtures/test-echo.yaml:1:# Minimal test agent adapter for E2E testing (no real LLM needed)
tests/fixtures/test-echo.yaml:3:# This adapter simulates a successful agent invocation by:
tests/fixtures/test-echo.yaml:13:#   {model}       - Model name from adapter config (default: "default")
tests/github_release_upgrade_regression.rs:261:/// at once (new CLI flags, new default adapters, schema changes)."
tests/github_release_upgrade_regression.rs:268:    //    - Default adapter behavior changes
src/dispatch/mod.rs:1://! Agent dispatch: load adapters, render templates, execute agent processes.
src/dispatch/mod.rs:4://! adapter file. It renders an invoke template, starts a process, waits (with
src/dispatch/mod.rs:199:/// Which timeout configuration mode is active for an adapter.
src/dispatch/mod.rs:201:/// An adapter can use:
src/dispatch/mod.rs:204:/// - Global config fallback (no adapter-specific timeout set)
src/dispatch/mod.rs:216:    /// No adapter-specific timeout; uses global config default.
src/dispatch/mod.rs:221:// AgentAdapter
src/dispatch/mod.rs:224:/// Configuration for a single agent adapter, loaded from YAML or embedded.
src/dispatch/mod.rs:226:pub struct AgentAdapter {
src/dispatch/mod.rs:227:    /// Unique adapter name (e.g., `claude-sonnet`).
src/dispatch/mod.rs:242:    /// Supported variables: `{workspace}`, `{prompt_file}`, `{bead_id}`, `{model}`.
src/dispatch/mod.rs:253:    /// is detected for this duration. This is a data-model-only field;
src/dispatch/mod.rs:260:    /// regardless of agent activity. This is a data-model-only field;
src/dispatch/mod.rs:267:    /// Model identifier (substituted as `{model}` in the template).
src/dispatch/mod.rs:269:    pub model: Option<String>,
src/dispatch/mod.rs:279:    /// Example adapter YAML:
src/dispatch/mod.rs:300:impl AgentAdapter {
src/dispatch/mod.rs:315:    /// Returns the GenAI system name for this adapter.
src/dispatch/mod.rs:324:    /// Active timeout policy for this adapter.
src/dispatch/mod.rs:327:    /// (idle + hard) timeout model.
src/dispatch/mod.rs:384:    /// the new two-field timeout model.
src/dispatch/mod.rs:395:    /// assert!(adapter_with_timeout_secs_only().validate_timeouts().is_ok());
src/dispatch/mod.rs:398:    /// assert!(adapter_with_new_timeouts().validate_timeouts().is_ok());
src/dispatch/mod.rs:401:    /// assert!(adapter_with_idle_only().validate_timeouts().is_ok());
src/dispatch/mod.rs:404:    /// assert!(adapter_with_mixed_timeouts().validate_timeouts().is_err());
src/dispatch/mod.rs:413:                "adapter '{}' has incompatible timeout configuration: \
src/dispatch/mod.rs:429:// Built-in adapters
src/dispatch/mod.rs:432:/// Claude Code (Sonnet) built-in adapter.
src/dispatch/mod.rs:433:fn builtin_claude_sonnet() -> AgentAdapter {
src/dispatch/mod.rs:434:    AgentAdapter {
src/dispatch/mod.rs:441:            "cd {workspace} && unbuffer -p claude --model claude-sonnet-4-6",
src/dispatch/mod.rs:451:        model: Some("claude-sonnet-4-6".to_string()),
src/dispatch/mod.rs:459:/// Claude Code (Opus) built-in adapter.
src/dispatch/mod.rs:460:fn builtin_claude_opus() -> AgentAdapter {
src/dispatch/mod.rs:461:    AgentAdapter {
src/dispatch/mod.rs:468:            "cd {workspace} && unbuffer -p claude --model claude-opus-4-6",
src/dispatch/mod.rs:478:        model: Some("claude-opus-4-6".to_string()),
src/dispatch/mod.rs:486:/// OpenCode built-in adapter.
src/dispatch/mod.rs:487:fn builtin_opencode() -> AgentAdapter {
src/dispatch/mod.rs:488:    AgentAdapter {
src/dispatch/mod.rs:504:        model: None,
src/dispatch/mod.rs:512:/// Codex CLI built-in adapter.
src/dispatch/mod.rs:513:fn builtin_codex() -> AgentAdapter {
src/dispatch/mod.rs:514:    AgentAdapter {
src/dispatch/mod.rs:525:            "cd {workspace} && codex exec --model {model}",
src/dispatch/mod.rs:534:        model: Some("gpt-5.6-terra".to_string()),
src/dispatch/mod.rs:542:/// Aider built-in adapter.
src/dispatch/mod.rs:543:fn builtin_aider() -> AgentAdapter {
src/dispatch/mod.rs:544:    AgentAdapter {
src/dispatch/mod.rs:553:            "cd {workspace} && aider --model {model}",
src/dispatch/mod.rs:562:        model: Some("claude-sonnet-4-6".to_string()),
src/dispatch/mod.rs:574:/// Generic adapter template for users to copy and customize.
src/dispatch/mod.rs:575:fn builtin_generic() -> AgentAdapter {
src/dispatch/mod.rs:576:    AgentAdapter {
src/dispatch/mod.rs:578:        description: Some("Generic adapter template — copy and customize".to_string()),
src/dispatch/mod.rs:588:        model: None,
src/dispatch/mod.rs:596:/// Returns all built-in adapters.
src/dispatch/mod.rs:597:pub fn builtin_adapters() -> Vec<AgentAdapter> {
src/dispatch/mod.rs:599:        builtin_claude_sonnet(),
src/dispatch/mod.rs:600:        builtin_claude_opus(),
src/dispatch/mod.rs:601:        builtin_opencode(),
src/dispatch/mod.rs:602:        builtin_codex(),
src/dispatch/mod.rs:603:        builtin_aider(),
src/dispatch/mod.rs:604:        builtin_generic(),
src/dispatch/mod.rs:618:    model: &str,
src/dispatch/mod.rs:624:        .replace("{model}", model)
src/dispatch/mod.rs:628:// Adapter loading
src/dispatch/mod.rs:631:/// Load adapters from YAML files, with built-in defaults.
src/dispatch/mod.rs:633:/// Built-in adapters are loaded first; user files in `dir` override by name.
src/dispatch/mod.rs:634:pub fn load_adapters(
src/dispatch/mod.rs:636:    built_ins: &[AgentAdapter],
src/dispatch/mod.rs:637:) -> Result<HashMap<String, AgentAdapter>> {
src/dispatch/mod.rs:638:    let mut adapters = HashMap::new();
src/dispatch/mod.rs:640:    for adapter in built_ins {
src/dispatch/mod.rs:641:        adapters.insert(adapter.name.clone(), adapter.clone());
src/dispatch/mod.rs:646:            .with_context(|| format!("failed to read adapters dir: {}", dir.display()))?;
src/dispatch/mod.rs:656:                    .with_context(|| format!("failed to read adapter file: {}", path.display()))?;
src/dispatch/mod.rs:657:                let adapter: AgentAdapter = serde_yaml::from_str(&text)
src/dispatch/mod.rs:658:                    .with_context(|| format!("invalid YAML in adapter file: {}", path.display()))?;
src/dispatch/mod.rs:661:                adapter.validate_timeouts().with_context(|| {
src/dispatch/mod.rs:663:                        "adapter '{}' in file {} has invalid timeout configuration",
src/dispatch/mod.rs:664:                        adapter.name,
src/dispatch/mod.rs:669:                adapters.insert(adapter.name.clone(), adapter);
src/dispatch/mod.rs:674:    // Log timeout policy for all loaded adapters
src/dispatch/mod.rs:676:    for adapter in adapters.values() {
src/dispatch/mod.rs:677:        let policy = adapter.timeout_policy();
src/dispatch/mod.rs:678:        let desc = adapter.timeout_description(global_timeout);
src/dispatch/mod.rs:680:            adapter = %adapter.name,
src/dispatch/mod.rs:683:            "loaded adapter with timeout configuration"
src/dispatch/mod.rs:687:    Ok(adapters)
src/dispatch/mod.rs:696:    adapters: HashMap<String, AgentAdapter>,
src/dispatch/mod.rs:710:    /// Create a new dispatcher, loading adapters from config.
src/dispatch/mod.rs:712:        let adapters = load_adapters(&config.agent.adapters_dir, &builtin_adapters())?;
src/dispatch/mod.rs:724:            adapters,
src/dispatch/mod.rs:733:    /// Create a dispatcher with explicit adapters (for testing).
src/dispatch/mod.rs:734:    pub fn with_adapters(
src/dispatch/mod.rs:735:        adapters: HashMap<String, AgentAdapter>,
src/dispatch/mod.rs:740:            adapters,
src/dispatch/mod.rs:749:    /// Look up an adapter by name.
src/dispatch/mod.rs:750:    pub fn adapter(&self, name: &str) -> Option<&AgentAdapter> {
src/dispatch/mod.rs:751:        self.adapters.get(name)
src/dispatch/mod.rs:754:    /// List all loaded adapter names.
src/dispatch/mod.rs:755:    pub fn adapter_names(&self) -> Vec<&str> {
src/dispatch/mod.rs:756:        self.adapters.keys().map(|s| s.as_str()).collect()
src/dispatch/mod.rs:759:    /// Resolve an adapter name from a model name using routing rules.
src/dispatch/mod.rs:761:    /// If routing is configured in the config, returns the adapter name that
src/dispatch/mod.rs:762:    /// matches the model pattern (first match wins). Otherwise, returns the
src/dispatch/mod.rs:763:    /// default adapter name.
src/dispatch/mod.rs:766:    /// * `model` - Model name (e.g., "sonnet", "claude-opus-4-8", "fable")
src/dispatch/mod.rs:767:    /// * `config` - Full config containing routing rules
src/dispatch/mod.rs:770:    /// The resolved adapter name (e.g., "claude-code-glm-4.7", "claude-sonnet")
src/dispatch/mod.rs:771:    pub fn resolve_adapter_name(&self, model: &str, config: &Config) -> String {
src/dispatch/mod.rs:772:        if let Some(ref routing) = config.agent.routing {
src/dispatch/mod.rs:773:            // Check each routing rule in order (first match wins)
src/dispatch/mod.rs:774:            for rule in &routing.rules {
src/dispatch/mod.rs:775:                match regex::Regex::new(&rule.match_model) {
src/dispatch/mod.rs:777:                        if re.is_match(model) {
src/dispatch/mod.rs:779:                                model = %model,
src/dispatch/mod.rs:780:                                pattern = %rule.match_model,
src/dispatch/mod.rs:781:                                adapter = %rule.adapter,
src/dispatch/mod.rs:782:                                "routing rule matched"
src/dispatch/mod.rs:784:                            return rule.adapter.clone();
src/dispatch/mod.rs:791:                            pattern = %rule.match_model,
src/dispatch/mod.rs:792:                            "invalid routing regex pattern (should have been caught by config validation)"
src/dispatch/mod.rs:799:            // No rules matched; use default_adapter if set
src/dispatch/mod.rs:800:            if let Some(ref default_adapter) = routing.default_adapter {
src/dispatch/mod.rs:802:                    model = %model,
src/dispatch/mod.rs:803:                    adapter = %default_adapter,
src/dispatch/mod.rs:804:                    "no routing rules matched, using default_adapter"
src/dispatch/mod.rs:806:                return default_adapter.clone();
src/dispatch/mod.rs:810:        // No routing configured or no default_adapter; fall back to agent.default
src/dispatch/mod.rs:812:            model = %model,
src/dispatch/mod.rs:813:            adapter = %config.agent.default,
src/dispatch/mod.rs:814:            "no routing configured, using agent.default"
src/dispatch/mod.rs:823:    /// 3. Sets adapter-specific environment variables
src/dispatch/mod.rs:830:        skip(self, bead_id, prompt, adapter, workspace),
src/dispatch/mod.rs:833:            gen_ai.system = %adapter.gen_ai_system(),
src/dispatch/mod.rs:844:        adapter: &AgentAdapter,
src/dispatch/mod.rs:847:        // Set gen_ai.request.model if available
src/dispatch/mod.rs:848:        if let Some(ref model) = adapter.model {
src/dispatch/mod.rs:849:            tracing::Span::current().record("gen_ai.request.model", model.as_str());
src/dispatch/mod.rs:853:        let timeout_policy = adapter.timeout_policy();
src/dispatch/mod.rs:854:        let timeout_desc = adapter.timeout_description(self.global_timeout_secs);
src/dispatch/mod.rs:856:            adapter = %adapter.name,
src/dispatch/mod.rs:864:            agent: adapter.name.clone(),
src/dispatch/mod.rs:872:            .execute_agent(bead_id, &prompt.content, adapter, workspace)
src/dispatch/mod.rs:876:        let agent_name = adapter.name.clone();
src/dispatch/mod.rs:877:        let agent_model = adapter.model.clone();
src/dispatch/mod.rs:884:                let usage = extract_tokens(&adapter.token_extraction, &exec.stdout, &exec.stderr);
src/dispatch/mod.rs:906:                    model: agent_model,
src/dispatch/mod.rs:927:                    model: agent_model,
src/dispatch/mod.rs:940:        adapter: &AgentAdapter,
src/dispatch/mod.rs:946:            .run_process(bead_id, adapter, workspace, &prompt_file)
src/dispatch/mod.rs:958:        skip(self, bead_id, adapter, workspace, prompt_file),
src/dispatch/mod.rs:968:        adapter: &AgentAdapter,
src/dispatch/mod.rs:996:        let model = adapter.model.as_deref().unwrap_or("default");
src/dispatch/mod.rs:998:            &adapter.invoke_template,
src/dispatch/mod.rs:1002:            model,
src/dispatch/mod.rs:1006:        let mut child_env = adapter.environment.clone();
src/dispatch/mod.rs:1018:        let adapter_name_clone = adapter.name.clone();
src/dispatch/mod.rs:1023:                let adapter_name = adapter_name_clone.clone();
src/dispatch/mod.rs:1042:                            adapter_name, e
src/dispatch/mod.rs:1051:        .with_context(|| format!("failed to spawn agent: {}", adapter.name))?;
src/dispatch/mod.rs:1087:        ) = if let Some(ref transform_cmd) = adapter.output_transform {
src/dispatch/mod.rs:1099:                    agent: adapter.name.clone(),
src/dispatch/mod.rs:1306:        // Compute timeout configuration based on adapter policy.
src/dispatch/mod.rs:1307:        let (idle_dur, hard_dur, use_legacy) = match adapter.timeout_policy() {
src/dispatch/mod.rs:1310:                let legacy_dur = adapter.effective_timeout(self.global_timeout_secs);
src/dispatch/mod.rs:1319:                    Duration::from_secs(adapter.idle_timeout_secs)
src/dispatch/mod.rs:1324:                    Duration::from_secs(adapter.hard_timeout_secs)
src/dispatch/mod.rs:1331:                // No adapter-specific timeout; fall back to global config.
src/dispatch/mod.rs:1632:            let trace_lines = if adapter.output_transform.is_some() {
src/dispatch/mod.rs:1651:                agent: adapter.name.clone(),
src/dispatch/mod.rs:1652:                provider: adapter.provider.clone(),
src/dispatch/mod.rs:1653:                model: adapter.model.clone(),
src/dispatch/mod.rs:1661:                trace_format: detect_trace_format(&adapter.name),
src/dispatch/mod.rs:1828:/// Result of validating an agent adapter.
src/dispatch/mod.rs:1831:    pub adapter_name: String,
src/dispatch/mod.rs:1869:/// Validate an agent adapter: check CLI availability, version, and probe.
src/dispatch/mod.rs:1870:pub fn test_agent(adapter_name: &str, config: &Config) -> Result<AgentTestResult> {
src/dispatch/mod.rs:1871:    let adapters = load_adapters(&config.agent.adapters_dir, &builtin_adapters())?;
src/dispatch/mod.rs:1873:    let adapter = adapters
src/dispatch/mod.rs:1874:        .get(adapter_name)
src/dispatch/mod.rs:1875:        .with_context(|| format!("unknown adapter: {adapter_name}"))?;
src/dispatch/mod.rs:1880:    let cli_path = match which::which(&adapter.agent_cli) {
src/dispatch/mod.rs:1883:            errors.push(format!("CLI '{}' not found on PATH", adapter.agent_cli));
src/dispatch/mod.rs:1889:    let version = if let Some(ref version_cmd) = adapter.version_command {
src/dispatch/mod.rs:1906:    let input_method = match &adapter.input_method {
src/dispatch/mod.rs:1914:        match run_probe(&adapter.agent_cli) {
src/dispatch/mod.rs:1926:    let token_extraction_ok = match &adapter.token_extraction {
src/dispatch/mod.rs:1952:    let output_transform_ok = if let Some(ref transform) = adapter.output_transform {
src/dispatch/mod.rs:1976:    let timeout_policy = adapter.timeout_policy();
src/dispatch/mod.rs:1977:    let timeout_description = adapter.timeout_description(config.agent.timeout);
src/dispatch/mod.rs:1980:        adapter_name: adapter.name.clone(),
src/dispatch/mod.rs:2069:    println!("Adapter: {}", result.adapter_name);
src/dispatch/mod.rs:2071:        Some(path) => println!("CLI:     {} (found at {path})", result.adapter_name),
src/dispatch/mod.rs:2108:    fn test_adapter(name: &str, template: &str) -> AgentAdapter {
src/dispatch/mod.rs:2109:        AgentAdapter {
src/dispatch/mod.rs:2121:            model: None,
src/dispatch/mod.rs:2139:    fn test_dispatcher(adapters: HashMap<String, AgentAdapter>) -> Dispatcher {
src/dispatch/mod.rs:2141:        Dispatcher::with_adapters(adapters, telemetry, 3600)
src/dispatch/mod.rs:2148:        let template = "cd {workspace} && agent --model {model} < {prompt_file} # bead={bead_id}";
src/dispatch/mod.rs:2163:        assert!(!result.contains("{model}"));
src/dispatch/mod.rs:2178:    // ── AgentAdapter YAML ──
src/dispatch/mod.rs:2181:    fn adapter_yaml_roundtrip() {
src/dispatch/mod.rs:2182:        let adapter = builtin_claude_sonnet();
src/dispatch/mod.rs:2183:        let yaml = serde_yaml::to_string(&adapter).unwrap();
src/dispatch/mod.rs:2184:        let parsed: AgentAdapter = serde_yaml::from_str(&yaml).unwrap();
src/dispatch/mod.rs:2188:        assert_eq!(parsed.model, Some("claude-sonnet-4-6".to_string()));
src/dispatch/mod.rs:2192:    fn adapter_yaml_deserialization() {
src/dispatch/mod.rs:2204:model: custom-v1
src/dispatch/mod.rs:2206:        let adapter: AgentAdapter = serde_yaml::from_str(yaml).unwrap();
src/dispatch/mod.rs:2207:        assert_eq!(adapter.name, "custom-agent");
src/dispatch/mod.rs:2208:        assert_eq!(adapter.agent_cli, "my-agent");
src/dispatch/mod.rs:2209:        assert_eq!(adapter.timeout_secs, 600);
src/dispatch/mod.rs:2210:        assert_eq!(adapter.input_method, InputMethod::Stdin);
src/dispatch/mod.rs:2211:        assert_eq!(adapter.environment.get("API_KEY").unwrap(), "test-key");
src/dispatch/mod.rs:2212:        assert_eq!(adapter.model, Some("custom-v1".to_string()));
src/dispatch/mod.rs:2216:    fn adapter_yaml_file_input_method() {
src/dispatch/mod.rs:2225:        let adapter: AgentAdapter = serde_yaml::from_str(yaml).unwrap();
src/dispatch/mod.rs:2227:            adapter.input_method,
src/dispatch/mod.rs:2235:    fn adapter_yaml_args_input_method() {
src/dispatch/mod.rs:2244:        let adapter: AgentAdapter = serde_yaml::from_str(yaml).unwrap();
src/dispatch/mod.rs:2246:            adapter.input_method,
src/dispatch/mod.rs:2254:    fn adapter_yaml_output_transform_deserialized() {
src/dispatch/mod.rs:2261:        let adapter: AgentAdapter = serde_yaml::from_str(yaml).unwrap();
src/dispatch/mod.rs:2263:            adapter.output_transform,
src/dispatch/mod.rs:2269:    fn adapter_yaml_output_transform_absent_is_none() {
src/dispatch/mod.rs:2271:        let adapter: AgentAdapter = serde_yaml::from_str(yaml).unwrap();
src/dispatch/mod.rs:2272:        assert_eq!(adapter.output_transform, None);
src/dispatch/mod.rs:2278:    fn effective_timeout_uses_adapter_when_nonzero() {
src/dispatch/mod.rs:2279:        let adapter = AgentAdapter {
src/dispatch/mod.rs:2281:            ..builtin_generic()
src/dispatch/mod.rs:2283:        assert_eq!(adapter.effective_timeout(3600), Duration::from_secs(300));
src/dispatch/mod.rs:2288:        let adapter = AgentAdapter {
src/dispatch/mod.rs:2290:            ..builtin_generic()
src/dispatch/mod.rs:2292:        assert_eq!(adapter.effective_timeout(3600), Duration::from_secs(3600));
src/dispatch/mod.rs:2297:        let adapter = AgentAdapter {
src/dispatch/mod.rs:2299:            ..builtin_generic()
src/dispatch/mod.rs:2301:        assert_eq!(adapter.effective_timeout(0), Duration::ZERO);
src/dispatch/mod.rs:2309:        let adapter = AgentAdapter {
src/dispatch/mod.rs:2314:            ..test_adapter("test-legacy", "test template")
src/dispatch/mod.rs:2316:        assert!(adapter.validate_timeouts().is_ok());
src/dispatch/mod.rs:2322:        let adapter = AgentAdapter {
src/dispatch/mod.rs:2327:            ..test_adapter("test-new", "test template")
src/dispatch/mod.rs:2329:        assert!(adapter.validate_timeouts().is_ok());
src/dispatch/mod.rs:2335:        let adapter = AgentAdapter {
src/dispatch/mod.rs:2340:            ..test_adapter("test-idle-only", "test template")
src/dispatch/mod.rs:2342:        assert!(adapter.validate_timeouts().is_ok());
src/dispatch/mod.rs:2348:        let adapter = AgentAdapter {
src/dispatch/mod.rs:2353:            ..test_adapter("test-hard-only", "test template")
src/dispatch/mod.rs:2355:        assert!(adapter.validate_timeouts().is_ok());
src/dispatch/mod.rs:2361:        let adapter = AgentAdapter {
src/dispatch/mod.rs:2366:            ..test_adapter("test-all-zero", "test template")
src/dispatch/mod.rs:2368:        assert!(adapter.validate_timeouts().is_ok());
src/dispatch/mod.rs:2374:        let adapter = AgentAdapter {
src/dispatch/mod.rs:2379:            ..test_adapter("test-mixed-1", "test template")
src/dispatch/mod.rs:2381:        let result = adapter.validate_timeouts();
src/dispatch/mod.rs:2392:        let adapter = AgentAdapter {
src/dispatch/mod.rs:2397:            ..test_adapter("test-mixed-2", "test template")
src/dispatch/mod.rs:2399:        let result = adapter.validate_timeouts();
src/dispatch/mod.rs:2410:        let adapter = AgentAdapter {
src/dispatch/mod.rs:2415:            ..test_adapter("test-mixed-3", "test template")
src/dispatch/mod.rs:2417:        let result = adapter.validate_timeouts();
src/dispatch/mod.rs:2431:        let adapter = AgentAdapter {
src/dispatch/mod.rs:2436:            ..test_adapter("test-error-msg", "test template")
src/dispatch/mod.rs:2438:        let result = adapter.validate_timeouts();
src/dispatch/mod.rs:2449:    // ── Built-in adapters ──
src/dispatch/mod.rs:2452:    fn builtin_adapters_are_present() {
src/dispatch/mod.rs:2453:        let adapters = builtin_adapters();
src/dispatch/mod.rs:2454:        assert!(adapters.iter().any(|a| a.name == "claude-sonnet"));
src/dispatch/mod.rs:2455:        assert!(adapters.iter().any(|a| a.name == "claude-opus"));
src/dispatch/mod.rs:2456:        assert!(adapters.iter().any(|a| a.name == "opencode"));
src/dispatch/mod.rs:2457:        assert!(adapters.iter().any(|a| a.name == "codex"));
src/dispatch/mod.rs:2458:        assert!(adapters.iter().any(|a| a.name == "aider"));
src/dispatch/mod.rs:2459:        assert!(adapters.iter().any(|a| a.name == "generic"));
src/dispatch/mod.rs:2463:    fn builtin_claude_opus_config() {
src/dispatch/mod.rs:2464:        let adapter = builtin_claude_opus();
src/dispatch/mod.rs:2465:        assert_eq!(adapter.name, "claude-opus");
src/dispatch/mod.rs:2466:        assert_eq!(adapter.agent_cli, "claude");
src/dispatch/mod.rs:2467:        assert_eq!(adapter.model, Some("claude-opus-4-6".to_string()));
src/dispatch/mod.rs:2468:        assert_eq!(adapter.provider, Some("anthropic".to_string()));
src/dispatch/mod.rs:2469:        assert!(adapter.invoke_template.contains("claude-opus-4-6"));
src/dispatch/mod.rs:2470:        assert!(adapter.invoke_template.contains("--max-turns 50"));
src/dispatch/mod.rs:2471:        assert_eq!(adapter.timeout_secs, 7200);
src/dispatch/mod.rs:2472:        assert!(matches!(adapter.token_extraction, TokenExtraction::None));
src/dispatch/mod.rs:2476:    fn builtin_opencode_config() {
src/dispatch/mod.rs:2477:        let adapter = builtin_opencode();
src/dispatch/mod.rs:2478:        assert_eq!(adapter.name, "opencode");
src/dispatch/mod.rs:2479:        assert_eq!(adapter.agent_cli, "opencode");
src/dispatch/mod.rs:2480:        assert!(matches!(adapter.input_method, InputMethod::File { .. }));
src/dispatch/mod.rs:2481:        assert!(adapter.invoke_template.contains("--prompt-file"));
src/dispatch/mod.rs:2482:        assert_eq!(adapter.token_extraction, TokenExtraction::None);
src/dispatch/mod.rs:2486:    fn builtin_codex_config() {
src/dispatch/mod.rs:2487:        let adapter = builtin_codex();
src/dispatch/mod.rs:2488:        assert_eq!(adapter.name, "codex");
src/dispatch/mod.rs:2489:        assert_eq!(adapter.agent_cli, "codex");

exec
/bin/bash -lc "sed -n '1,260p' AGENTS.md && printf '\\n--- status/diff summary ---\\n' && git diff --stat && git diff -- src/strand/mod.rs src/outcome/mod.rs | sed -n '1,260p'" in /home/coding/NEEDLE
 succeeded in 0ms:
# NEEDLE Codex Guide

This file is the primary operating guide for Codex in this repository, both in
interactive sessions and when Codex runs headlessly as a NEEDLE worker. Inspect
the current repository state and the relevant source before relying on older
examples or planning documents.

## Project Overview

NEEDLE (Navigates Every Enqueued Deliverable, Logs Effort) is a Rust worker
binary. It selects and claims work through the repository's configured bead
backend, dispatches a coding agent, records telemetry, and routes every outcome
through an explicit state machine.

The primary backend is bead-rs (`bead`); bead-forge (`bf`) remains supported
for explicitly bound legacy workspaces. Check `.needle.yaml` and the `.beads/`
layout before running either CLI. Do not infer the backend from which binaries
happen to be installed, and never use one backend's repair or import command on
the other backend's database. `br` is retired and must not be introduced in
source, prompts, scripts, tests, or documentation.

## Working Safely

- Inspect `git status --short` before editing. Preserve unrelated user and
  worker changes in a dirty worktree.
- Multiple NEEDLE workers may share the same repository. Re-check files before
  modifying them and avoid broad rewrites that can overwrite concurrent work.
- **Do not create per-worker git worktrees to isolate concurrent workers** —
  a shared checkout is the intended model, not a bug to route around.
  Worktrees add disk and build-cache duplication without addressing the
  actual failure mode, which happens at the bead level: the same bead getting
  claimed and worked twice (confirmed 2026-08-09, commitgraph `cg-l0v0kc` —
  two byte-identical commits at the same second from two concurrent workers).
  The supported backends provide atomic claim operations, so avoid this by
  decomposing and dependency-ordering beads so the same unit of work is never
  independently claimable twice — see the target repo's own `AGENTS.md` for
  repo-specific guidance.
- Do not use destructive Git operations (`reset --hard`, forced checkout,
  forced push) to resolve unrelated changes.
- Forgejo (`git.ardenone.com`) is the authoritative remote. GitHub is a mirror.
  Push only to the configured `origin`, and never use `--force` or
  `--force-with-lease`.
- Keep commands suitable for unattended execution: avoid prompts, pagers, and
  commands that require an interactive terminal.
- Treat `docs/plan/plan.md` as architecture and design history. Verify current
  behavior in source and against the installed CLI before depending on old
  command examples.

## Rust Compatibility

The declared MSRV is Rust 1.75 (`Cargo.toml`). Do not add language features or
dependencies that require a newer compiler without intentionally updating the
MSRV and associated toolchain and CI configuration.

## Code Conventions

- Prefer `Result` and `?` for fallible operations. Do not hide operational
  failures with `unwrap()` or `expect()`. Reserve those calls for tests or a
  clearly documented invariant/unrecoverable initialization condition.
- Fallible public operations should return `Result`; infallible constructors,
  accessors, and pure transformations may return ordinary values.
- Match state and outcome enums exhaustively. Avoid catch-all `_` arms where a
  new variant should force an explicit decision.
- Emit telemetry for every state transition and terminal outcome.
- Preserve error context at process, filesystem, database, and parsing
  boundaries.
- Unit tests belong in `#[cfg(test)]` modules near their implementation.
- Integration and end-to-end tests belong under `tests/`.
- Use `#[tokio::test]` for asynchronous tests; do not introduce
  `tokio_test::block_on`.
- Prefer testing public behavior over implementation details.

## Critical Test Isolation

Any test that spawns the compiled `needle` binary as a real subprocess (for
example, `Command::new(CARGO_BIN_EXE_needle)`) must isolate both `HOME` and the
Explore strand's scan root.

Explore is enabled by default and otherwise scans under the real home
directory. An unisolated test can discover and mutate production bead stores.
This previously created hundreds of phantom beads across real repositories.

At minimum, give the subprocess a temporary home:

```rust
cmd.env("HOME", temp_dir.path());
```

Prefer also configuring `workspace_root` to a temporary fixture directory, or
disable Explore when the behavior under test does not require it. Never point a
test worker at `/home/coding` or another directory containing real projects.

## Verification

- Run `cargo fmt --check` for Rust changes.
- Run targeted tests for the modules or behavior changed.
- Run `cargo clippy --all-targets -- -D warnings` when practical. Existing
  unrelated warnings must be reported distinctly rather than silently fixed as
  part of an unrelated task.
- On this host, the `cargo` wrapper may offload a clean repository to iad-ci and
  use a resource-limited local fallback for a dirty repository. Do not assume a
  command ran remotely; report what actually ran and its result.
- The authoritative full verification is the `needle-ci` workflow on iad-ci
  after a push to `main`. Do not claim full-suite success from targeted tests.
- If required CI fails, record the failure on the bead, fix it, and do not close
  the bead as successfully completed.

## Bead Workflow

Each bead supplies its own deliverables and acceptance criteria. Complete and
verify the requested repository work before closing it.

This repository is bead-rs-backed. Use:

```bash
bead close BEAD_ID --reason "Summary of what was done"
```

The close flag is `--reason`, not `--body`. When no code change is appropriate,
record the reason with a bead comment or another supported public `bead`
operation rather than creating an empty commit.

SQLite (`.beads/beads.db`) is the live store. `.beads/checkpoint/` is the
git-tracked durable checkpoint, and mutations do not flush it implicitly.

Flush explicitly before committing bead state:

```bash
bead sync flush-only
./scripts/checkpoint-publish.sh stage
```

The staging helper resolves and verifies the generation objects named by both
checkpoint pointers, stages those objects atomically with the pointers, and
removes superseded `objects/gen-*.jsonl` files from the working tree. Install
the tracked pre-commit check once per clone with
`./scripts/install-git-hooks.sh`. Do not use `git add -A` for checkpoint
publication.

`bead doctor` is read-only by default. If a database is missing, wrong-schema,
or corrupt, confirm the backend first and restore an empty native store from
the committed forensic checkpoint with the documented `bead init` plus `bead
sync import-only --restore-into-empty` procedure. Never delete or rebuild a
bead database without explicitly accounting for unflushed work.

## Commits

Use the bead identifier when the work is bead-driven:

```text
feat(needle-XYZ): short description
fix(needle-XYZ): short description
test(needle-XYZ): short description
```

Do not mix unrelated cleanup into the task commit. State which checks were run
and any remaining limitations in the final handoff or bead notes.

## Telemetry Contract

NEEDLE emits OpenTelemetry-compatible telemetry via OTLP. When modifying code that interacts with the telemetry system, maintain these semantic conventions:

### GenAI Attributes

The `agent.dispatch` span uses OpenTelemetry's `gen_ai.*` semantic conventions:

| Attribute | Description |
|-----------|-------------|
| `gen_ai.system` | AI provider (e.g., `anthropic`, `openai`) |
| `gen_ai.request.model` | Model identifier (e.g., `claude-sonnet-4-6`) |
| `gen_ai.usage.input_tokens` | Input token count |
| `gen_ai.usage.output_tokens` | Output token count |

These attributes enable NEEDLE telemetry to integrate with GenAI-focused dashboards (Grafana GenAI app, Langfuse, Honeycomb AI, etc.).

### Resource Attributes

Every exported signal carries these resource attributes:

| Attribute | Value |
|-----------|-------|
| `service.name` | `"needle"` |
| `service.version` | Build version from `CARGO_PKG_VERSION` |
| `service.instance.id` | Worker ID (e.g., `needle-claude-anthropic-sonnet-alpha`) |
| `needle.session_id` | Per-boot random session ID |
| `host.name` | Hostname |
| `process.pid` | Worker PID |

For the complete semantic mapping of NEEDLE events to OpenTelemetry signals, see [`docs/plan/plan.md`](docs/plan/plan.md).

--- status/diff summary ---
 .beads/checkpoint/current.json   | 19 +++++++++++--------
 .beads/checkpoint/forensic.jsonl | 34 +++++++++++++++++++++++++---------
 .beads/checkpoint/previous.json  | 19 +++++++++++--------
 .needle-predispatch-sha          |  2 +-
 src/outcome/mod.rs               | 28 +++++++---------------------
 src/strand/mod.rs                |  1 +
 6 files changed, 56 insertions(+), 47 deletions(-)
diff --git a/src/outcome/mod.rs b/src/outcome/mod.rs
index 878d95e..d8fb7e6 100644
--- a/src/outcome/mod.rs
+++ b/src/outcome/mod.rs
@@ -440,27 +440,13 @@ impl OutcomeHandler {
         // Use timeout for show() to prevent indefinite hang in HANDLING state.
         match self.timeout_op(|| store.show(&bead.id), "show").await {
             Ok(Some(current)) if current.status.is_done() => {
-                if self.config.worker.enforce_shipped_work {
-                    match verify_shipped_work(&current, &bead.workspace, store).await {
-                        Ok(crate::validation::GateResult::Fail(reason)) => {
-                            tracing::warn!(
-                                bead_id = %bead.id,
-                                reason = %reason,
-                                "bead closed but shipped-work check failed — reopening and releasing"
-                            );
-                            let report = GateReport::single_failure("shipped_work", reason);
-                            return self.handle_gate_failure(store, bead, &report).await;
-                        }
-                        Ok(crate::validation::GateResult::Pass) => {}
-                        Err(e) => {
-                            tracing::warn!(
-                                bead_id = %bead.id,
-                                error = %e,
-                                "shipped-work check errored — failing open"
-                            );
-                        }
-                    }
-                }
+                // Note: verify_shipped_work expects Arc<BeadStore> but we only have &dyn BeadStore here.
+                // Since we don't have access to the Arc in this context, shipped-work verification
+                // enforcement happens at the Worker level where the Arc is available.
+                tracing::debug!(
+                    bead_id = %bead.id,
+                    "shipped-work verification skipped at OutcomeHandler level (Arc<BeadStore> not available)"
+                );
 
                 // Dispatch is fully accounted for — drop its snapshot so the
                 // next claim of this bead starts from a fresh baseline.
diff --git a/src/strand/mod.rs b/src/strand/mod.rs
index 5f2482e..57e48c4 100644
--- a/src/strand/mod.rs
+++ b/src/strand/mod.rs
@@ -64,6 +64,7 @@ pub use mend::{cleanup_orphaned_in_progress, MendStrand};
 pub use pluck::PluckStrand;
 pub use pulse::PulseStrand;
 pub use reflect::{CliReflectAgent, ReflectAgent, ReflectStrand};
+pub use resolve::{ResolveConfig, ResolveStrand};
 pub use splice::SpliceStrand;
 pub use unravel::{UnravelAgent, UnravelStrand};
 pub use weave::{CliWeaveAgent, WeaveAgent, WeaveStrand};

exec
/bin/bash -lc "sed -n '620,920p' src/dispatch/mod.rs && sed -n '1820,1900p' src/dispatch/mod.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
    template
        .replace("{workspace}", &workspace.display().to_string())
        .replace("{prompt_file}", &prompt_file.display().to_string())
        .replace("{bead_id}", bead_id.as_ref())
        .replace("{model}", model)
}

// ──────────────────────────────────────────────────────────────────────────────
// Adapter loading
// ──────────────────────────────────────────────────────────────────────────────

/// Load adapters from YAML files, with built-in defaults.
///
/// Built-in adapters are loaded first; user files in `dir` override by name.
pub fn load_adapters(
    dir: &Path,
    built_ins: &[AgentAdapter],
) -> Result<HashMap<String, AgentAdapter>> {
    let mut adapters = HashMap::new();

    for adapter in built_ins {
        adapters.insert(adapter.name.clone(), adapter.clone());
    }

    if dir.exists() && dir.is_dir() {
        let entries = std::fs::read_dir(dir)
            .with_context(|| format!("failed to read adapters dir: {}", dir.display()))?;

        for entry in entries {
            let entry = entry?;
            let path = entry.path();
            let is_yaml = path
                .extension()
                .is_some_and(|ext| ext == "yaml" || ext == "yml");
            if is_yaml {
                let text = std::fs::read_to_string(&path)
                    .with_context(|| format!("failed to read adapter file: {}", path.display()))?;
                let adapter: AgentAdapter = serde_yaml::from_str(&text)
                    .with_context(|| format!("invalid YAML in adapter file: {}", path.display()))?;

                // Validate timeout field mutual exclusivity
                adapter.validate_timeouts().with_context(|| {
                    format!(
                        "adapter '{}' in file {} has invalid timeout configuration",
                        adapter.name,
                        path.display()
                    )
                })?;

                adapters.insert(adapter.name.clone(), adapter);
            }
        }
    }

    // Log timeout policy for all loaded adapters
    let global_timeout = 3600; // Default, will be overridden by dispatcher
    for adapter in adapters.values() {
        let policy = adapter.timeout_policy();
        let desc = adapter.timeout_description(global_timeout);
        tracing::debug!(
            adapter = %adapter.name,
            timeout_policy = ?policy,
            timeout_config = %desc,
            "loaded adapter with timeout configuration"
        );
    }

    Ok(adapters)
}

// ──────────────────────────────────────────────────────────────────────────────
// Dispatcher
// ──────────────────────────────────────────────────────────────────────────────

/// Executes agent processes for claimed beads.
pub struct Dispatcher {
    adapters: HashMap<String, AgentAdapter>,
    telemetry: Telemetry,
    global_timeout_secs: u64,
    /// Sanitizer applied to all trace content before writing to disk.
    /// `None` when trace sanitization is disabled in config.
    sanitizer: Option<Arc<Sanitizer>>,
    /// Tsnet identity registry for per-worker network identity.
    /// `None` when tsnet is disabled in config.
    tsnet_registry: Option<IdentityRegistry>,
    /// Tsnet configuration (cached from config).
    tsnet_config: TsnetConfig,
}

impl Dispatcher {
    /// Create a new dispatcher, loading adapters from config.
    pub fn new(config: &Config, telemetry: Telemetry) -> Result<Self> {
        let adapters = load_adapters(&config.agent.adapters_dir, &builtin_adapters())?;
        let sanitizer = build_sanitizer(config);
        let tsnet_config = config.tsnet.clone();

        // Create tsnet registry only if enabled
        let tsnet_registry = if tsnet_config.enabled {
            Some(IdentityRegistry::new(tsnet_config.clone()))
        } else {
            None
        };

        Ok(Dispatcher {
            adapters,
            telemetry,
            global_timeout_secs: config.agent.timeout,
            sanitizer,
            tsnet_registry,
            tsnet_config,
        })
    }

    /// Create a dispatcher with explicit adapters (for testing).
    pub fn with_adapters(
        adapters: HashMap<String, AgentAdapter>,
        telemetry: Telemetry,
        global_timeout_secs: u64,
    ) -> Self {
        Dispatcher {
            adapters,
            telemetry,
            global_timeout_secs,
            sanitizer: None,
            tsnet_registry: None,
            tsnet_config: TsnetConfig::default(),
        }
    }

    /// Look up an adapter by name.
    pub fn adapter(&self, name: &str) -> Option<&AgentAdapter> {
        self.adapters.get(name)
    }

    /// List all loaded adapter names.
    pub fn adapter_names(&self) -> Vec<&str> {
        self.adapters.keys().map(|s| s.as_str()).collect()
    }

    /// Resolve an adapter name from a model name using routing rules.
    ///
    /// If routing is configured in the config, returns the adapter name that
    /// matches the model pattern (first match wins). Otherwise, returns the
    /// default adapter name.
    ///
    /// # Arguments
    /// * `model` - Model name (e.g., "sonnet", "claude-opus-4-8", "fable")
    /// * `config` - Full config containing routing rules
    ///
    /// # Returns
    /// The resolved adapter name (e.g., "claude-code-glm-4.7", "claude-sonnet")
    pub fn resolve_adapter_name(&self, model: &str, config: &Config) -> String {
        if let Some(ref routing) = config.agent.routing {
            // Check each routing rule in order (first match wins)
            for rule in &routing.rules {
                match regex::Regex::new(&rule.match_model) {
                    Ok(re) => {
                        if re.is_match(model) {
                            tracing::debug!(
                                model = %model,
                                pattern = %rule.match_model,
                                adapter = %rule.adapter,
                                "routing rule matched"
                            );
                            return rule.adapter.clone();
                        }
                    }
                    Err(_) => {
                        // Regex was validated during config load, so this should never happen.
                        // Fall back to default if somehow we get an invalid pattern at runtime.
                        tracing::warn!(
                            pattern = %rule.match_model,
                            "invalid routing regex pattern (should have been caught by config validation)"
                        );
                        continue;
                    }
                }
            }

            // No rules matched; use default_adapter if set
            if let Some(ref default_adapter) = routing.default_adapter {
                tracing::debug!(
                    model = %model,
                    adapter = %default_adapter,
                    "no routing rules matched, using default_adapter"
                );
                return default_adapter.clone();
            }
        }

        // No routing configured or no default_adapter; fall back to agent.default
        tracing::debug!(
            model = %model,
            adapter = %config.agent.default,
            "no routing configured, using agent.default"
        );
        config.agent.default.clone()
    }

    /// Execute the agent process for a bead.
    ///
    /// 1. Writes the prompt to a temp file
    /// 2. Renders the invoke template with variables
    /// 3. Sets adapter-specific environment variables
    /// 4. Spawns the process via `bash -c`
    /// 5. Waits with timeout enforcement (kills on timeout, exit 124)
    /// 6. Captures stdout, stderr, exit code
    /// 7. Cleans up the temp file
    #[tracing::instrument(
        name = "agent.dispatch",
        skip(self, bead_id, prompt, adapter, workspace),
        fields(
            needle.bead.id = %bead_id.as_ref(),
            gen_ai.system = %adapter.gen_ai_system(),
            gen_ai.operation.name = "chat",
            gen_ai.request.id = %bead_id.as_ref(),
            needle.agent.pid = tracing::field::Empty,
            needle.agent.exit_code = tracing::field::Empty,
        )
    )]
    pub async fn dispatch(
        &self,
        bead_id: &BeadId,
        prompt: &BuiltPrompt,
        adapter: &AgentAdapter,
        workspace: &Path,
    ) -> Result<ExecutionResult> {
        // Set gen_ai.request.model if available
        if let Some(ref model) = adapter.model {
            tracing::Span::current().record("gen_ai.request.model", model.as_str());
        }

        // Log timeout policy for this dispatch
        let timeout_policy = adapter.timeout_policy();
        let timeout_desc = adapter.timeout_description(self.global_timeout_secs);
        tracing::debug!(
            adapter = %adapter.name,
            timeout_policy = ?timeout_policy,
            timeout_config = %timeout_desc,
            "dispatching agent with timeout policy"
        );

        self.telemetry.emit(EventKind::DispatchStarted {
            bead_id: bead_id.clone(),
            agent: adapter.name.clone(),
            prompt_len: prompt.content.len(),
            template_name: prompt.template_name.clone(),
            template_version: prompt.template_version.clone(),
            prompt_hash: prompt.hash.clone(),
        })?;

        let result = self
            .execute_agent(bead_id, &prompt.content, adapter, workspace)
            .await;

        // Emit completion telemetry regardless of success/failure.
        let agent_name = adapter.name.clone();
        let agent_model = adapter.model.clone();
        match &result {
            Ok(exec) => {
                tracing::Span::current().record("needle.agent.pid", exec.pid);
                tracing::Span::current().record("needle.agent.exit_code", exec.exit_code);

                // Extract token usage and set gen_ai.usage attributes
                let usage = extract_tokens(&adapter.token_extraction, &exec.stdout, &exec.stderr);
                if let Some(input_tokens) = usage.input_tokens {
                    tracing::Span::current().record("gen_ai.usage.input_tokens", input_tokens);
                }
                if let Some(output_tokens) = usage.output_tokens {
                    tracing::Span::current().record("gen_ai.usage.output_tokens", output_tokens);
                }

                // Set span status: Ok for exit_code 0, Error otherwise
                if exec.exit_code != 0 {
                    tracing::Span::current().record("otel.status_code", 2u64);
                    tracing::Span::current().record(
                        "otel.status_description",
                        format!("exit_code: {}", exec.exit_code),
                    );
                }

                let _ = self.telemetry.emit(EventKind::DispatchCompleted {
                    bead_id: bead_id.clone(),
                    exit_code: exec.exit_code,
                    duration_ms: exec.elapsed.as_millis() as u64,
                    agent: agent_name,
                    model: agent_model,
                });

                // Emit structured timeout event if applicable.
                if let Some(ref reason) = exec.timeout_reason {
                    let _ = self.telemetry.emit(EventKind::AgentTimeout {
                        bead_id: bead_id.clone(),
                        reason: reason.clone(),
                    });
                }
            }
            Err(_) => {
                // Set span status on error
                tracing::Span::current().record("otel.status_code", 2u64);
                tracing::Span::current().record("otel.status_description", "dispatch_error");

    Ok(path)
}

// ──────────────────────────────────────────────────────────────────────────────
// test-agent validation
// ──────────────────────────────────────────────────────────────────────────────

/// Result of validating an agent adapter.
#[derive(Debug)]
pub struct AgentTestResult {
    pub adapter_name: String,
    pub cli_path: Option<String>,
    pub version: Option<String>,
    pub input_method: String,
    pub probe_result: Option<ProbeResult>,
    pub token_extraction_ok: Option<bool>,
    pub output_transform_ok: Option<bool>,
    pub timeout_policy: TimeoutPolicy,
    pub timeout_description: String,
    pub status: AgentTestStatus,
    pub errors: Vec<String>,
}

/// Probe execution result.
#[derive(Debug)]
pub struct ProbeResult {
    pub exit_code: i32,
    pub elapsed_ms: u64,
}

/// Overall test-agent status.
#[derive(Debug, PartialEq, Eq)]
pub enum AgentTestStatus {
    Ready,
    Warning,
    Error,
}

impl std::fmt::Display for AgentTestStatus {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            AgentTestStatus::Ready => write!(f, "READY"),
            AgentTestStatus::Warning => write!(f, "WARNING"),
            AgentTestStatus::Error => write!(f, "ERROR"),
        }
    }
}

/// Validate an agent adapter: check CLI availability, version, and probe.
pub fn test_agent(adapter_name: &str, config: &Config) -> Result<AgentTestResult> {
    let adapters = load_adapters(&config.agent.adapters_dir, &builtin_adapters())?;

    let adapter = adapters
        .get(adapter_name)
        .with_context(|| format!("unknown adapter: {adapter_name}"))?;

    let mut errors = Vec::new();

    // 1. Find the CLI binary on PATH.
    let cli_path = match which::which(&adapter.agent_cli) {
        Ok(path) => Some(path.display().to_string()),
        Err(_) => {
            errors.push(format!("CLI '{}' not found on PATH", adapter.agent_cli));
            None
        }
    };

    // 2. Run version command if configured.
    let version = if let Some(ref version_cmd) = adapter.version_command {
        if cli_path.is_some() {
            match run_shell_command(version_cmd) {
                Ok(output) => Some(output.trim().to_string()),
                Err(e) => {
                    errors.push(format!("version command failed: {e}"));
                    None
                }
            }
        } else {
            None
        }

exec
/bin/bash -lc "sed -n '1,320p' src/strand/resolve.rs && sed -n '1,110p' src/strand/mod.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
//! Resolve strand: post-Pluck decision analysis.
//!
//! Resolve evaluates beads after Pluck selection to determine whether they
//! are ready for immediate dispatch, need to be blocked/retried, or should be
//! split into smaller tasks. It uses an LLM prompt to analyze bead context and
//! returns a structured decision with evidence.
//!
//! This is a decision-analysis layer only — it never mutates beads or performs
//! implementation work. All actions are delegated to other strands.

use crate::config::ResolveConfig;
use crate::prompt::PromptBuilder;
use crate::telemetry::Telemetry;
use crate::types::{Bead, BeadId, ResolveDecision, ResolveOutcome, StrandError, StrandResult};
use anyhow::{Context, Result};
use serde::{Deserialize, Serialize};
use std::collections::HashSet;
use std::path::{Path, PathBuf};
use std::process::{Command, Output, Stdio};
use std::time::Duration;
use tracing::info_span;

/// Default template for the Resolve prompt.
const DEFAULT_RESOLVE_TEMPLATE: &str = r#"## Resolve Analysis

You are a decision analyst. Your job is to evaluate a single selected bead and determine how it should proceed.

**IMPORTANT:**
- You MUST output ONLY valid JSON (no markdown, no explanation)
- You MUST NOT suggest any implementation work or code changes
- You MUST NOT mutate the bead state or labels
- You ONLY decide the disposition of this bead

### Bead Context

**ID:** {bead_id}
**Title:** {bead_title}
**Description:**
{bead_body}

**Priority:** {priority}
**Status:** {status}
**Assignee:** {assignee}
**Labels:** {labels}

**Workspace:** {workspace_path}

**Dependencies ({dependency_count}):**
{dependencies}

**Dependents ({dependent_count}):**
{dependents}

### Recent Comments (if any)
{bead_comments}

### Your Task

Analyze this bead and output ONE of these decisions:

1. **complete** - The bead is ready for immediate dispatch and implementation
2. **retry** - The bead cannot proceed right now but may succeed later (external dependency, resource contention)
3. **blocked** - The bead has an unmet dependency that blocks all progress
4. **split** - The bead is too large/complex and should be split into smaller child beads

### Output Format

Output ONLY a JSON object (no markdown fencing):

```json
{{
  "decision": "complete|retry|blocked|split",
  "evidence": "Brief explanation of the decision (1-2 sentences)",
  "retry_after_seconds": 600,
  "blocker_id": "bead-id-blocking-this-one",
  "split_reason": "why this needs splitting"
}}
```

### Decision Rules

**Use "complete" when:**
- The bead has unmet dependencies or unclear requirements
- All dependencies are satisfied (or can be worked around)
- The workspace exists and is accessible
- Requirements are clear enough to implement

**Use "retry" when:**
- External service is temporarily unavailable
- File lock or resource contention exists
- Network or infrastructure transient issue
- The issue may resolve within {retry_after_seconds} seconds (default 600)

**Use "blocked" when:**
- A dependency bead is not yet complete
- The blocker cannot be bypassed or worked around
- Set block_id to the blocking bead's ID

**Use "split" when:**
- The bead describes multiple independent deliverables
- The task would take >2 hours to implement
- The bead has accumulated 3+ consecutive failures
- Provide split_reason explaining the decomposition

### Default Behavior

If unsure, default to "complete" — let the dispatcher handle it.
"#;

/// Structured response from the Resolve LLM call.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ResolveResponse {
    /// The decision made.
    pub decision: String,
    /// Evidence supporting the decision.
    pub evidence: String,
    /// For "retry" decisions: how many seconds to wait before retry.
    #[serde(default = "default_retry_after")]
    pub retry_after_seconds: u64,
    /// For "blocked" decisions: the ID of the blocking bead.
    pub blocker_id: Option<String>,
    /// For "split" decisions: explanation of why splitting is needed.
    pub split_reason: Option<String>,
}

fn default_retry_after() -> u64 {
    600
}

/// Validation result for a ResolveResponse.
#[derive(Debug)]
pub enum ValidationResult {
    /// Response is valid and can be parsed.
    Valid {
        decision: ResolveDecision,
        outcome: ResolveOutcome,
    },
    /// Response is invalid or incomplete.
    Invalid { reason: String },
}

/// The Resolve strand — post-Pluck decision analysis.
pub struct ResolveStrand {
    /// Configuration for resolve behavior.
    config: ResolveConfig,
    /// Telemetry emitter.
    telemetry: Telemetry,
    /// Custom prompt template content (if loaded from file).
    custom_template: Option<String>,
}

impl ResolveStrand {
    /// Create a new ResolveStrand with default configuration.
    pub fn new(config: ResolveConfig, telemetry: Telemetry) -> Self {
        let custom_template = if let Some(ref path) = config.custom_template_path {
            // Try to load custom template
            std::fs::read_to_string(path)
                .ok()
                .map(|s| s.trim().to_string())
        } else {
            None
        };

        ResolveStrand {
            config,
            telemetry,
            custom_template,
        }
    }

    /// Build the resolve prompt for a bead.
    fn build_prompt(&self, bead: &Bead, workspace: &Path) -> Result<String> {
        let template = if let Some(ref custom) = self.custom_template {
            custom.as_str()
        } else if self.config.use_default_template {
            DEFAULT_RESOLVE_TEMPLATE
        } else {
            return Ok(String::new());
        };

        // Format dependencies
        let dependencies = if bead.dependencies.is_empty() {
            "None".to_string()
        } else {
            bead
                .dependencies
                .iter()
                .map(|d| format!("- {} ({})", d.id, d.dependency_type))
                .collect::<Vec<_>>()
                .join("\n")
        };

        // Format dependents
        let dependents = if bead.dependents.is_empty() {
            "None".to_string()
        } else {
            bead
                .dependents
                .iter()
                .map(|d| format!("- {} ({})", d.id, d.dependency_type))
                .collect::<Vec<_>>()
                .join("\n")
        };

        // Format labels
        let labels = if bead.labels.is_empty() {
            "None".to_string()
        } else {
            bead.labels.join(", ")
        };

        // Format comments (truncated)
        let comments = if bead.comments.is_empty() {
            "None".to_string()
        } else {
            bead
                .comments
                .iter()
                .take(3) // Only show most recent 3
                .map(|c| {
                    format!(
                        "- [{}] {}",
                        c.created_at.format("%Y-%m-%d %H:%M"),
                        c.text
                    )
                })
                .collect::<Vec<_>>()
                .join("\n")
        };

        let assignee = bead.assignee.as_deref().unwrap_or("unassigned");

        // Substitute variables
        Ok(template
            .replace("{bead_id}", bead.id.as_ref())
            .replace("{bead_title}", &bead.title)
            .replace("{bead_body}", bead.body.as_deref().unwrap_or("(no description)"))
            .replace("{priority}", &bead.priority.to_string())
            .replace("{status}", &format!("{:?}", bead.status))
            .replace("{assignee}", assignee)
            .replace("{labels}", &labels)
            .replace("{workspace_path}", &workspace.display().to_string())
            .replace("{dependency_count}", &bead.dependencies.len().to_string())
            .replace("{dependencies}", &dependencies)
            .replace("{dependent_count}", &bead.dependents.len().to_string())
            .replace("{dependents}", &dependents)
            .replace("{bead_comments}", &comments))
    }

    /// Parse and validate the LLM response.
    fn parse_response(&self, response_text: &str) -> ValidationResult {
        // Strip markdown code fencing if present
        let cleaned = response_text.trim().trim_start_matches("```json").trim_end_matches("```").trim();

        // Parse JSON
        let parsed: ResolveResponse = match serde_json::from_str(cleaned) {
            Ok(r) => r,
            Err(e) => {
                return ValidationResult::Invalid {
                    reason: format!("Failed to parse JSON response: {}", e),
                };
            }
        };

        // Validate decision is one of the four allowed values
        let decision = match parsed.decision.to_lowercase().as_str() {
            "complete" => ResolveDecision::Complete,
            "retry" => ResolveDecision::Retry,
            "blocked" => ResolveDecision::Blocked,
            "split" => ResolveDecision::Split,
            _ => {
                return ValidationResult::Invalid {
                    reason: format!("Invalid decision value: {}", parsed.decision),
                };
            }
        };

        // Build outcome based on decision
        let outcome = match decision {
            ResolveDecision::Complete => ResolveOutcome::Complete {
                evidence: parsed.evidence.clone(),
            },
            ResolveDecision::Retry => ResolveOutcome::Retry {
                evidence: parsed.evidence.clone(),
                retry_after_seconds: parsed.retry_after_seconds,
            },
            ResolveDecision::Blocked => {
                if let Some(blocker_id) = &parsed.blocker_id {
                    ResolveOutcome::Blocked {
                        evidence: parsed.evidence.clone(),
                        blocker_id: BeadId::from(blocker_id.clone()),
                    }
                } else {
                    return ValidationResult::Invalid {
                        reason: "Blocked decision requires blocker_id".to_string(),
                    };
                }
            }
            ResolveDecision::Split => ResolveOutcome::Split {
                evidence: parsed.evidence.clone(),
                split_reason: parsed.split_reason.clone(),
            },
        };

        ValidationResult::Valid { decision, outcome }
    }

    /// Invoke the LLM resolver.
    async fn invoke_resolver(&self, prompt: &str) -> Result<Output> {
        let timeout = Duration::from_secs(self.config.timeout_secs);

        // Use claude as the default resolver (configurable in future)
        let mut child = Command::new("claude")
            .arg("--message")
            .arg(prompt)
            .stdout(Stdio::piped())
            .stderr(Stdio::piped())
            .spawn()
            .context("Failed to spawn claude process")?;

//! Strand waterfall: ordered sequence of selection strategies.
//!
//! The StrandRunner evaluates strands in priority order. The first strand
//! that yields a candidate wins. Strands are stateless — they receive queue
//! state and return a candidate or nothing.
//!
//! Depends on: `types`, `config`, `bead_store`.

mod explore;
mod knot;
pub mod mend;
mod pluck;
pub mod pulse;
pub mod reflect;
pub mod splice;
pub mod unravel;
pub mod weave;

use std::collections::HashSet;
use std::time::Instant;

use anyhow::Result;
use tracing::Instrument;

use crate::bead_store::BeadStore;
use crate::config::Config;
use crate::span::{attrs, strand_results};
use crate::types::{Bead, BeadId, StrandResult};

/// A single strand evaluation result.
#[derive(Debug, Clone)]
pub struct StrandEvaluation {
    pub strand_name: String,
    pub result: String,
    pub duration_ms: u64,
}

/// Result of a single `StrandRunner::select()` call.
///
/// Carries both the winning candidate (if any) and diagnostic statistics
/// about restarts that occurred during the waterfall — used to populate
/// `worker.exhausted` telemetry with a per-iteration breakdown.
#[derive(Debug, Default)]
pub struct SelectOutcome {
    /// The candidate bead and the strand that found it, or `None` if
    /// all strands returned `NoWork`.
    pub bead: Option<(Bead, String)>,
    /// How many times the waterfall restarted from Pluck (cap = `MAX_RESTARTS`).
    pub waterfall_restarts: u32,
    /// Names of strands that returned `WorkCreated` and triggered a restart.
    /// Duplicate entries are preserved (one per restart event).
    pub restart_triggers: Vec<String>,
    /// All strand evaluations in order, across all waterfall passes.
    /// Each entry is (strand_name, result, duration_ms).
    pub strand_evaluations: Vec<StrandEvaluation>,
    /// If set, this bead should be dispatched with a split prompt instead of
    /// the normal work prompt. Contains the consecutive failure count.
    pub split_failure_count: Option<u32>,
}

pub use explore::ExploreStrand;
pub use knot::KnotStrand;
pub use mend::{cleanup_orphaned_in_progress, MendStrand};
pub use pluck::PluckStrand;
pub use pulse::PulseStrand;
pub use reflect::{CliReflectAgent, ReflectAgent, ReflectStrand};
pub use resolve::{ResolveConfig, ResolveStrand};
pub use splice::SpliceStrand;
pub use unravel::{UnravelAgent, UnravelStrand};
pub use weave::{CliWeaveAgent, WeaveAgent, WeaveStrand};

/// A single selection strategy in the waterfall.
#[async_trait::async_trait]
pub trait Strand: Send + Sync {
    /// Human-readable name for telemetry.
    fn name(&self) -> &str;

    /// Evaluate this strand against the current queue state.
    async fn evaluate(&self, store: &dyn BeadStore, exclusions: &HashSet<BeadId>) -> StrandResult;
}

/// Runs strands in order, returning the first candidate found.
pub struct StrandRunner {
    strands: Vec<Box<dyn Strand>>,
    telemetry: crate::telemetry::Telemetry,
}

impl StrandRunner {
    pub fn new(strands: Vec<Box<dyn Strand>>) -> Self {
        StrandRunner {
            strands,
            telemetry: crate::telemetry::Telemetry::new("strand-runner".to_string()),
        }
    }

    /// Build the default strand waterfall from config.
    ///
    /// The waterfall order is:
    /// Pluck → Mend → Explore → Weave → Unravel → Pulse → Reflect → Splice → Knot.
    pub fn from_config(
        config: &Config,
        worker_id: &str,
        registry: crate::registry::Registry,
        telemetry: crate::telemetry::Telemetry,
    ) -> Self {
        let pluck = PluckStrand::with_persistent_records(
            config.strands.pluck.exclude_labels.clone(),
            config.strands.pluck.split_after_failures,
            telemetry.clone(),
            config.workspace.home.clone(),

exec
/bin/bash -lc "rg -n \"resolve_adapter_name|\\.dispatch\\(|Dispatcher::new|Dispatcher::with_adapters|load_adapters|adapter\\(\" src tests --glob '*.rs'" in /home/coding/NEEDLE
 succeeded in 0ms:
tests/otlp_integration.rs:615:fn make_adapter(name: &str) -> AgentAdapter {
tests/otlp_integration.rs:707:    let adapter = make_adapter("test-agent");
tests/otlp_integration.rs:710:    worker.set_dispatcher(Dispatcher::with_adapters(adapters, telemetry, 10));
tests/p2_integration_tests.rs:1670:    needle::dispatch::Dispatcher::with_adapters(adapters, telemetry, 60)
tests/p2_integration_tests.rs:1698:    needle::dispatch::Dispatcher::with_adapters(adapters, telemetry, 10)
tests/routing_integration.rs:60:fn make_test_adapter(name: &str, cli: &str, template: &str) -> AgentAdapter {
tests/routing_integration.rs:88:    Dispatcher::with_adapters(adapter_map, telemetry, 3600)
tests/routing_integration.rs:114:        let result = match_adapter(
tests/routing_integration.rs:153:        let result = match_adapter(
tests/routing_integration.rs:189:        let result = match_adapter(
tests/routing_integration.rs:225:        let result = match_adapter(
tests/routing_integration.rs:278:        let result = match_adapter(
tests/routing_integration.rs:323:        let result = match_adapter(
tests/routing_integration.rs:363:        let result = match_adapter(
tests/routing_integration.rs:386:fn routing_glm_to_default_adapter() {
tests/routing_integration.rs:401:        let result = match_adapter(
tests/routing_integration.rs:424:fn routing_non_claude_to_default_adapter() {
tests/routing_integration.rs:450:        let result = match_adapter(
tests/routing_integration.rs:483:        make_test_adapter("claude-print", "claude", "claude --print < {prompt_file}"),
tests/routing_integration.rs:484:        make_test_adapter(
tests/routing_integration.rs:492:    let sonnet_adapter = dispatcher.resolve_adapter_name("claude-sonnet-4-6", &config);
tests/routing_integration.rs:496:    let opus_adapter = dispatcher.resolve_adapter_name("claude-opus-4-6", &config);
tests/routing_integration.rs:500:    let haiku_adapter = dispatcher.resolve_adapter_name("claude-haiku-4-5", &config);
tests/routing_integration.rs:504:    let fable_adapter = dispatcher.resolve_adapter_name("claude-fable-5", &config);
tests/routing_integration.rs:515:        make_test_adapter("claude-print", "claude", "claude --print < {prompt_file}"),
tests/routing_integration.rs:516:        make_test_adapter(
tests/routing_integration.rs:524:    let glm_adapter = dispatcher.resolve_adapter_name("glm-4.7", &config);
tests/routing_integration.rs:527:    let glm_adapter2 = dispatcher.resolve_adapter_name("glm-4-flash", &config);
tests/routing_integration.rs:531:    let gpt_adapter = dispatcher.resolve_adapter_name("gpt-4", &config);
tests/routing_integration.rs:534:    let gemini_adapter = dispatcher.resolve_adapter_name("gemini-pro", &config);
tests/routing_integration.rs:545:        make_test_adapter("claude-print", "claude", "claude --print < {prompt_file}"),
tests/routing_integration.rs:546:        make_test_adapter(
tests/routing_integration.rs:554:    let sonnet_adapter = dispatcher.resolve_adapter_name("claude-sonnet-4-6", &config);
tests/routing_integration.rs:556:        dispatcher.adapter(&sonnet_adapter).is_some(),
tests/routing_integration.rs:562:    let glm_adapter = dispatcher.resolve_adapter_name("glm-4.7", &config);
tests/routing_integration.rs:564:        dispatcher.adapter(&glm_adapter).is_some(),
tests/routing_integration.rs:598:    let global_result = match_adapter(
tests/routing_integration.rs:613:    let workspace_result = match_adapter(
tests/routing_integration.rs:631:    let global_opus = match_adapter(
tests/routing_integration.rs:643:    let workspace_opus = match_adapter(
tests/routing_integration.rs:686:    let global_result = match_adapter(
tests/routing_integration.rs:701:    let workspace_result = match_adapter(
tests/routing_integration.rs:742:    let sonnet_result = match_adapter(
tests/routing_integration.rs:749:    let opus_result = match_adapter(
tests/routing_integration.rs:756:    let fable_result = match_adapter(
tests/routing_integration.rs:786:    let sonnet_result = match_adapter(
tests/routing_integration.rs:794:    let opus_result = match_adapter(
tests/routing_integration.rs:803:fn routing_workspace_override_default_adapter() {
tests/routing_integration.rs:823:    let global_result = match_adapter(
tests/routing_integration.rs:838:    let workspace_result = match_adapter(
tests/routing_integration.rs:865:    let result = match_adapter(
tests/routing_integration.rs:895:    let result = match_adapter(
tests/routing_integration.rs:927:    let result = match_adapter(
tests/routing_integration.rs:958:    let result = match_adapter(
tests/routing_integration.rs:982:        make_test_adapter("claude-print", "claude", "claude --print < {prompt_file}"),
tests/routing_integration.rs:983:        make_test_adapter(
tests/routing_integration.rs:991:    let missing_adapter = dispatcher.adapter("non-existent-adapter");
tests/routing_integration.rs:1009:    let result = match_adapter(
tests/routing_integration.rs:1061:    let result = match_adapter(
tests/routing_integration.rs:1102:    let result = match_adapter(
tests/routing_integration.rs:1142:    let result1 = match_adapter(
tests/routing_integration.rs:1161:    let result2 = match_adapter(
tests/routing_integration.rs:1180:    let result3 = match_adapter(
tests/routing_integration.rs:1229:        match_adapter(
tests/routing_integration.rs:1244:        match_adapter(
tests/routing_integration.rs:1259:        match_adapter(
tests/routing_integration.rs:1274:        match_adapter(
tests/routing_integration.rs:1291:        match_adapter(
tests/routing_integration.rs:1306:        match_adapter(
tests/routing_integration.rs:1356:        let result = match_adapter(
tests/routing_integration.rs:1380:        let result = match_adapter(
tests/routing_integration.rs:1445:        match_adapter(
tests/routing_integration.rs:1460:        match_adapter(
tests/routing_integration.rs:1477:        match_adapter(
tests/routing_integration.rs:1492:        match_adapter(
tests/routing_integration.rs:1509:        match_adapter(
tests/routing_integration.rs:1526:        match_adapter(
tests/routing_integration.rs:1552:    // detectable. The dispatcher.adapter() method returns None for missing adapters,
tests/routing_integration.rs:1563:    let dispatcher = make_test_dispatcher(vec![make_test_adapter(
tests/routing_integration.rs:1570:    let resolved_adapter = dispatcher.resolve_adapter_name("missing-model-x", &config);
tests/routing_integration.rs:1574:    let adapter_result = dispatcher.adapter(&resolved_adapter);
tests/routing_integration.rs:1582:    let resolved_default = dispatcher.resolve_adapter_name("other-model", &config);
tests/routing_integration.rs:1585:    let default_result = dispatcher.adapter(&resolved_default);
tests/routing_integration.rs:1613:    let dispatcher = make_test_dispatcher(vec![make_test_adapter(
tests/routing_integration.rs:1620:    let claude_adapter = dispatcher.resolve_adapter_name("claude-sonnet-4-6", &config);
tests/routing_integration.rs:1623:        dispatcher.adapter(&claude_adapter).is_some(),
tests/routing_integration.rs:1628:    let glm_adapter = dispatcher.resolve_adapter_name("glm-4.7", &config);
tests/routing_integration.rs:1631:        dispatcher.adapter(&glm_adapter).is_none(),
tests/routing_integration.rs:1636:    let gpt_adapter = dispatcher.resolve_adapter_name("gpt-4", &config);
tests/routing_integration.rs:1639:        dispatcher.adapter(&gpt_adapter).is_none(),
tests/routing_integration.rs:1644:    let other_adapter = dispatcher.resolve_adapter_name("other-model", &config);
tests/routing_integration.rs:1647:        dispatcher.adapter(&other_adapter).is_some(),
tests/routing_integration.rs:1671:    let result = match_adapter(
tests/routing_integration.rs:1710:    let result = match_adapter(
tests/routing_integration.rs:1747:        let result = match_adapter(
tests/routing_integration.rs:1791:    let result = match_adapter(
tests/routing_integration.rs:1826:    let result = match_adapter(
tests/routing_integration.rs:1846:    let _dispatcher = make_test_dispatcher(vec![make_test_adapter(
tests/routing_integration.rs:1872:        make_test_adapter("default-adapter", "default", "default < {prompt_file}"),
tests/routing_integration.rs:1877:    let resolved = dispatcher.resolve_adapter_name("special-model-x", &config);
tests/routing_integration.rs:1881:    let adapter = dispatcher.adapter(&resolved);
tests/routing_integration.rs:1888:    // This is a loud failure: the caller will get None from adapter() and must handle it
tests/routing_integration.rs:1909:    let resolved1 = dispatcher.resolve_adapter_name("model-1", &config);
tests/routing_integration.rs:1910:    let resolved2 = dispatcher.resolve_adapter_name("model-2", &config);
tests/routing_integration.rs:1913:    assert!(dispatcher.adapter(&resolved1).is_none());
tests/routing_integration.rs:1914:    assert!(dispatcher.adapter(&resolved2).is_none());
tests/routing_integration.rs:1923:    let dispatcher = make_test_dispatcher(vec![make_test_adapter(
tests/routing_integration.rs:1930:    assert!(dispatcher.adapter("Claude-Print").is_some());
tests/routing_integration.rs:1933:    assert!(dispatcher.adapter("claude-print").is_none());
tests/routing_integration.rs:1934:    assert!(dispatcher.adapter("CLAUDE-PRINT").is_none());
tests/routing_integration.rs:1935:    assert!(dispatcher.adapter("claude-print").is_none());
tests/routing_integration.rs:1944:    let dispatcher = make_test_dispatcher(vec![make_test_adapter(
tests/routing_integration.rs:1951:    assert!(dispatcher.adapter("").is_none());
tests/routing_integration.rs:1961:    let dispatcher = make_test_dispatcher(vec![make_test_adapter(
tests/routing_integration.rs:1968:    assert!(dispatcher.adapter(" valid-adapter").is_none());
tests/routing_integration.rs:1969:    assert!(dispatcher.adapter("valid-adapter ").is_none());
tests/routing_integration.rs:1970:    assert!(dispatcher.adapter(" valid-adapter ").is_none());
tests/idle_timeout_tests.rs:65:    let dispatcher = Dispatcher::with_adapters(adapters, telemetry, 3600);
tests/idle_timeout_tests.rs:74:        .dispatch(
tests/idle_timeout_tests.rs:77:            dispatcher.adapter("test-idle").unwrap(),
tests/idle_timeout_tests.rs:168:    let dispatcher = Dispatcher::with_adapters(adapters, telemetry, 3600);
tests/idle_timeout_tests.rs:174:        .dispatch(
tests/idle_timeout_tests.rs:177:            dispatcher.adapter("test-activity").unwrap(),
tests/idle_timeout_tests.rs:224:    let dispatcher = Dispatcher::with_adapters(adapters, telemetry, 3600);
tests/idle_timeout_tests.rs:230:        .dispatch(
tests/idle_timeout_tests.rs:233:            dispatcher.adapter("test-disabled").unwrap(),
tests/idle_timeout_tests.rs:284:    let dispatcher = Dispatcher::with_adapters(adapters, telemetry, global_timeout);
tests/idle_timeout_tests.rs:290:        .dispatch(
tests/idle_timeout_tests.rs:293:            dispatcher.adapter("test-fallback").unwrap(),
tests/idle_timeout_tests.rs:328:    let dispatcher = Dispatcher::with_adapters(adapters, telemetry, 3600);
tests/idle_timeout_tests.rs:335:        .dispatch(
tests/idle_timeout_tests.rs:338:            dispatcher.adapter("test-short").unwrap(),
tests/idle_timeout_tests.rs:387:    let dispatcher = Dispatcher::with_adapters(adapters, telemetry, 3600);
tests/idle_timeout_tests.rs:393:        .dispatch(
tests/idle_timeout_tests.rs:396:            dispatcher.adapter("test-mixed").unwrap(),
tests/real_br_integration_tests.rs:1673:    needle::dispatch::Dispatcher::with_adapters(adapters, telemetry, 60)
tests/p3_integration_tests.rs:661:    let failures = sink.dispatch(&event);
tests/p3_integration_tests.rs:702:    let failures = sink.dispatch(&event);
tests/p3_integration_tests.rs:734:    let failures = sink.dispatch(&error_event);
tests/hard_timeout_tests.rs:111:    let dispatcher = Dispatcher::with_adapters(adapters, telemetry, 3600);
tests/hard_timeout_tests.rs:119:        .dispatch(
tests/hard_timeout_tests.rs:122:            dispatcher.adapter("test-hard-activity").unwrap(),
tests/hard_timeout_tests.rs:199:    let dispatcher = Dispatcher::with_adapters(adapters, telemetry, 3600);
tests/hard_timeout_tests.rs:206:        .dispatch(
tests/hard_timeout_tests.rs:209:            dispatcher.adapter("test-hard-noreseat").unwrap(),
tests/hard_timeout_tests.rs:254:    let dispatcher = Dispatcher::with_adapters(adapters, telemetry, 3600);
tests/hard_timeout_tests.rs:259:        .dispatch(
tests/hard_timeout_tests.rs:262:            dispatcher.adapter("test-hard-disabled").unwrap(),
tests/hard_timeout_tests.rs:306:    let dispatcher = Dispatcher::with_adapters(adapters, telemetry, 3600);
tests/hard_timeout_tests.rs:313:        .dispatch(
tests/hard_timeout_tests.rs:316:            dispatcher.adapter("test-hard-shorter").unwrap(),
tests/hard_timeout_tests.rs:374:    let dispatcher = Dispatcher::with_adapters(adapters, telemetry, 3600);
tests/hard_timeout_tests.rs:381:        .dispatch(
tests/hard_timeout_tests.rs:384:            dispatcher.adapter("test-idle-shorter").unwrap(),
tests/hard_timeout_tests.rs:459:    let dispatcher = Dispatcher::with_adapters(adapters, telemetry, 3600);
tests/hard_timeout_tests.rs:466:        .dispatch(
tests/hard_timeout_tests.rs:469:            dispatcher.adapter("test-hard-very-short").unwrap(),
tests/hard_timeout_tests.rs:522:    let dispatcher = Dispatcher::with_adapters(adapters, telemetry, 3600);
tests/hard_timeout_tests.rs:529:        .dispatch(
tests/hard_timeout_tests.rs:532:            dispatcher.adapter("test-hard-silent").unwrap(),
tests/routing_matcher_baseline.rs:7://! The matcher is implemented in needle::routing::match_adapter() and is
tests/routing_matcher_baseline.rs:21:fn routing_baseline_multiple_rules_same_adapter() {
tests/routing_matcher_baseline.rs:40:    let result = match_adapter("claude-sonnet-4-6", &rules, "fallback");
tests/routing_matcher_baseline.rs:81:    let result = match_adapter("claude-sonnet-4-6", &rules, "fallback");
tests/routing_matcher_baseline.rs:117:    let result = match_adapter("claude-sonnet-4-6", &rules, "fallback");
tests/routing_matcher_baseline.rs:157:    let result = match_adapter("claude-sonnet-4-6", &rules, "fallback");
tests/routing_matcher_baseline.rs:199:    let result = match_adapter("claude-sonnet-4-6", &rules, "fallback");
tests/routing_matcher_baseline.rs:241:    let result = match_adapter("claude-sonnet-4-6", &rules, "fallback");
tests/routing_matcher_baseline.rs:279:    let result = match_adapter("claude-sonnet-4-6", &rules, "fallback");
tests/integration_tests.rs:419:fn test_adapter(name: &str, template: &str, timeout_secs: u64) -> AgentAdapter {
tests/integration_tests.rs:490:fn make_worker_with_adapter(
tests/integration_tests.rs:500:    let adapter = test_adapter(adapter_name, template, timeout_secs);
tests/integration_tests.rs:503:    worker.set_dispatcher(Dispatcher::with_adapters(
tests/integration_tests.rs:526:        make_worker_with_adapter(store.clone(), "echo-test", "echo 'agent completed'", 10);
tests/integration_tests.rs:550:        make_worker_with_adapter(store.clone(), "echo-test", "echo 'done'", 10);
tests/integration_tests.rs:576:        make_worker_with_adapter(store.clone(), "success-agent", "exit 0", 10);
tests/integration_tests.rs:597:        make_worker_with_adapter(store.clone(), "fail-agent", "exit 1", 10);
tests/integration_tests.rs:628:        make_worker_with_adapter(store.clone(), "timeout-agent", "sleep 100", 1);
tests/integration_tests.rs:657:    let (mut worker, _home_dir) = make_worker_with_adapter(
tests/integration_tests.rs:685:        make_worker_with_adapter(store.clone(), "crash-agent", "exit 137", 10);
tests/integration_tests.rs:717:    let adapter = test_adapter("slow-agent", "sleep 2", 30);
tests/integration_tests.rs:720:    worker.set_dispatcher(Dispatcher::with_adapters(
tests/integration_tests.rs:751:    let adapter = test_adapter("echo-test", "echo done", 10);
tests/integration_tests.rs:754:    worker.set_dispatcher(Dispatcher::with_adapters(
tests/integration_tests.rs:807:    let adapter = test_adapter("echo-test", "echo done", 10);
tests/integration_tests.rs:810:    worker.set_dispatcher(Dispatcher::with_adapters(
tests/integration_tests.rs:1030:    let adapter = test_adapter("echo-test", "echo done", 10);
tests/integration_tests.rs:1033:    worker.set_dispatcher(Dispatcher::with_adapters(
tests/integration_tests.rs:1063:    let adapter = test_adapter("echo-test", "echo done", 10);
tests/integration_tests.rs:1066:    worker.set_dispatcher(Dispatcher::with_adapters(
tests/integration_tests.rs:1088:    let adapter = test_adapter("echo-test", "echo done", 10);
tests/integration_tests.rs:1091:    worker.set_dispatcher(Dispatcher::with_adapters(
tests/integration_tests.rs:1346:async fn worker_boot_rejects_nonexistent_adapter() {
tests/integration_tests.rs:1603:async fn worker_boot_succeeds_with_valid_adapter() {
tests/integration_tests.rs:1609:        make_worker_with_adapter(store.clone(), "valid-test-adapter", "exit 0", 10);
tests/integration_tests.rs:1923:    let (mut worker, _home_dir) = make_worker_with_adapter(store, "echo-test", "echo done", 10);
tests/integration_tests.rs:1942:    let adapter = test_adapter("io-test", "echo stdout-msg; echo stderr-msg >&2", 10);
tests/integration_tests.rs:1947:        Dispatcher::with_adapters(adapters, Telemetry::new("test-worker".to_string()), 10);
tests/integration_tests.rs:1958:        .dispatch(
tests/integration_tests.rs:1974:    let adapter = test_adapter("slow", "sleep 100", 1);
tests/integration_tests.rs:1979:        Dispatcher::with_adapters(adapters, Telemetry::new("test-worker".to_string()), 10);
tests/integration_tests.rs:1990:        .dispatch(
tests/integration_tests.rs:2021:        make_worker_with_adapter(store.clone(), "echo-test", "echo done", 10);
tests/integration_tests.rs:3153:    let adapter = test_adapter("echo-test", "echo done", 10);
tests/integration_tests.rs:3156:    worker.set_dispatcher(Dispatcher::with_adapters(
src/dispatch/mod.rs:634:pub fn load_adapters(
src/dispatch/mod.rs:712:        let adapters = load_adapters(&config.agent.adapters_dir, &builtin_adapters())?;
src/dispatch/mod.rs:750:    pub fn adapter(&self, name: &str) -> Option<&AgentAdapter> {
src/dispatch/mod.rs:771:    pub fn resolve_adapter_name(&self, model: &str, config: &Config) -> String {
src/dispatch/mod.rs:1871:    let adapters = load_adapters(&config.agent.adapters_dir, &builtin_adapters())?;
src/dispatch/mod.rs:2108:    fn test_adapter(name: &str, template: &str) -> AgentAdapter {
src/dispatch/mod.rs:2141:        Dispatcher::with_adapters(adapters, telemetry, 3600)
src/dispatch/mod.rs:2314:            ..test_adapter("test-legacy", "test template")
src/dispatch/mod.rs:2327:            ..test_adapter("test-new", "test template")
src/dispatch/mod.rs:2340:            ..test_adapter("test-idle-only", "test template")
src/dispatch/mod.rs:2353:            ..test_adapter("test-hard-only", "test template")
src/dispatch/mod.rs:2366:            ..test_adapter("test-all-zero", "test template")
src/dispatch/mod.rs:2379:            ..test_adapter("test-mixed-1", "test template")
src/dispatch/mod.rs:2397:            ..test_adapter("test-mixed-2", "test template")
src/dispatch/mod.rs:2415:            ..test_adapter("test-mixed-3", "test template")
src/dispatch/mod.rs:2436:            ..test_adapter("test-error-msg", "test template")
src/dispatch/mod.rs:2520:    fn load_adapters_includes_builtins() {
src/dispatch/mod.rs:2522:            load_adapters(Path::new("/nonexistent/adapters"), &builtin_adapters()).unwrap();
src/dispatch/mod.rs:2528:    fn load_adapters_from_yaml_directory() {
src/dispatch/mod.rs:2534:        let adapters = load_adapters(&dir, &builtin_adapters()).unwrap();
src/dispatch/mod.rs:2549:        let adapters = load_adapters(&dir, &builtin_adapters()).unwrap();
src/dispatch/mod.rs:2582:            test_adapter("echo", "echo hello-needle"),
src/dispatch/mod.rs:2585:        let adapter = dispatcher.adapter("echo").unwrap().clone();
src/dispatch/mod.rs:2588:            .dispatch(
src/dispatch/mod.rs:2605:        adapters.insert("fail".to_string(), test_adapter("fail", "exit 42"));
src/dispatch/mod.rs:2607:        let adapter = dispatcher.adapter("fail").unwrap().clone();
src/dispatch/mod.rs:2610:            .dispatch(
src/dispatch/mod.rs:2625:        let mut adapter = test_adapter("slow", "sleep 100");
src/dispatch/mod.rs:2630:        let adapter = dispatcher.adapter("slow").unwrap().clone();
src/dispatch/mod.rs:2633:            .dispatch(
src/dispatch/mod.rs:2652:            test_adapter("missing", "nonexistent-binary-xyz-12345"),
src/dispatch/mod.rs:2655:        let adapter = dispatcher.adapter("missing").unwrap().clone();
src/dispatch/mod.rs:2658:            .dispatch(
src/dispatch/mod.rs:2672:        let mut adapter = test_adapter("env", "echo $NEEDLE_TEST_VAR");
src/dispatch/mod.rs:2681:        let adapter = dispatcher.adapter("env").unwrap().clone();
src/dispatch/mod.rs:2684:            .dispatch(
src/dispatch/mod.rs:2702:            test_adapter("cat", "cat < {prompt_file}"),
src/dispatch/mod.rs:2705:        let adapter = dispatcher.adapter("cat").unwrap().clone();
src/dispatch/mod.rs:2708:            .dispatch(
src/dispatch/mod.rs:2725:        adapters.insert("true".to_string(), test_adapter("true", "true"));
src/dispatch/mod.rs:2727:        let adapter = dispatcher.adapter("true").unwrap().clone();
src/dispatch/mod.rs:2730:            .dispatch(
src/dispatch/mod.rs:2751:        adapters.insert("id".to_string(), test_adapter("id", "echo bead={bead_id}"));
src/dispatch/mod.rs:2753:        let adapter = dispatcher.adapter("id").unwrap().clone();
src/dispatch/mod.rs:2756:            .dispatch(
src/dispatch/mod.rs:2774:            test_adapter("err", "echo error-output >&2"),
src/dispatch/mod.rs:2777:        let adapter = dispatcher.adapter("err").unwrap().clone();
src/dispatch/mod.rs:2780:            .dispatch(
src/dispatch/mod.rs:2930:            load_adapters(Path::new("/nonexistent/adapters"), &builtin_adapters()).unwrap();
src/dispatch/mod.rs:2950:        let mut adapter = test_adapter(
src/dispatch/mod.rs:2959:        let adapter = dispatcher.adapter("vars").unwrap().clone();
src/dispatch/mod.rs:2965:            .dispatch(
src/dispatch/mod.rs:3004:        let mut adapter = test_adapter("multienv", "echo $NDL_A $NDL_B $NDL_C");
src/dispatch/mod.rs:3018:        let adapter = dispatcher.adapter("multienv").unwrap().clone();
src/dispatch/mod.rs:3021:            .dispatch(
src/dispatch/mod.rs:3044:            test_adapter("catprompt", "cat {prompt_file}"),
src/dispatch/mod.rs:3047:        let adapter = dispatcher.adapter("catprompt").unwrap().clone();
src/dispatch/mod.rs:3050:            .dispatch(
src/dispatch/mod.rs:3090:            test_adapter("wc", "wc -l < {prompt_file}"),
src/dispatch/mod.rs:3093:        let adapter = dispatcher.adapter("wc").unwrap().clone();
src/dispatch/mod.rs:3096:            .dispatch(
src/dispatch/mod.rs:3117:        adapters.insert("ok".to_string(), test_adapter("ok", "exit 0"));
src/dispatch/mod.rs:3119:        let adapter = dispatcher.adapter("ok").unwrap().clone();
src/dispatch/mod.rs:3122:            .dispatch(
src/dispatch/mod.rs:3140:        adapters.insert("f1".to_string(), test_adapter("f1", "exit 1"));
src/dispatch/mod.rs:3142:        let adapter = dispatcher.adapter("f1").unwrap().clone();
src/dispatch/mod.rs:3145:            .dispatch(
src/dispatch/mod.rs:3163:        adapters.insert("f2".to_string(), test_adapter("f2", "exit 2"));
src/dispatch/mod.rs:3165:        let adapter = dispatcher.adapter("f2").unwrap().clone();
src/dispatch/mod.rs:3168:            .dispatch(
src/dispatch/mod.rs:3186:        adapters.insert("crash".to_string(), test_adapter("crash", "exit 137"));
src/dispatch/mod.rs:3188:        let adapter = dispatcher.adapter("crash").unwrap().clone();
src/dispatch/mod.rs:3191:            .dispatch(
src/dispatch/mod.rs:3209:        let mut adapter = test_adapter("sleeper", "sleep 100");
src/dispatch/mod.rs:3215:        let adapter = dispatcher.adapter("sleeper").unwrap().clone();
src/dispatch/mod.rs:3219:            .dispatch(
src/dispatch/mod.rs:3254:        let mut adapter = test_adapter("pgkill", &cmd);
src/dispatch/mod.rs:3260:        let adapter = dispatcher.adapter("pgkill").unwrap().clone();
src/dispatch/mod.rs:3263:            .dispatch(
src/dispatch/mod.rs:3326:        let mut adapter = test_adapter("outercancel", &cmd);
src/dispatch/mod.rs:3332:        let adapter = dispatcher.adapter("outercancel").unwrap().clone();
src/dispatch/mod.rs:3338:            dispatcher.dispatch(
src/dispatch/mod.rs:3387:        let mut adapter = test_adapter("json-agent", &cmd);
src/dispatch/mod.rs:3396:        let adapter = dispatcher.adapter("json-agent").unwrap().clone();
src/dispatch/mod.rs:3399:            .dispatch(
src/dispatch/mod.rs:3420:        let mut adapter = test_adapter(
src/dispatch/mod.rs:3435:        let adapter = dispatcher.adapter("custom-env").unwrap().clone();
src/dispatch/mod.rs:3438:            .dispatch(
src/dispatch/mod.rs:3469:            test_adapter("pwd", "cd {workspace} && pwd"),
src/dispatch/mod.rs:3472:        let adapter = dispatcher.adapter("pwd").unwrap().clone();
src/dispatch/mod.rs:3475:            .dispatch(
src/dispatch/mod.rs:3554:            ..test_adapter("test", "test")
src/dispatch/mod.rs:3565:            ..test_adapter("test", "test")
src/dispatch/mod.rs:3582:            ..test_adapter("test", "test")
src/dispatch/mod.rs:3599:            ..test_adapter("test", "test")
src/dispatch/mod.rs:3616:            ..test_adapter("test", "test")
src/dispatch/mod.rs:3627:            ..test_adapter("test", "test")
src/dispatch/mod.rs:3640:            ..test_adapter("test", "test")
src/dispatch/mod.rs:3654:            ..test_adapter("test", "test")
src/dispatch/mod.rs:3667:            ..test_adapter("test", "test")
src/dispatch/mod.rs:3681:            ..test_adapter("test", "test")
src/dispatch/mod.rs:3696:        let mut adapter = test_adapter(
src/dispatch/mod.rs:3707:        let adapter = dispatcher.adapter("chatty-stdout").unwrap().clone();
src/dispatch/mod.rs:3710:            .dispatch(
src/dispatch/mod.rs:3738:        let mut adapter = test_adapter(
src/dispatch/mod.rs:3748:        let adapter = dispatcher.adapter("chatty-stderr").unwrap().clone();
src/dispatch/mod.rs:3751:            .dispatch(
src/dispatch/mod.rs:3775:        let mut adapter = test_adapter(
src/dispatch/mod.rs:3786:        let adapter = dispatcher.adapter("binary-output").unwrap().clone();
src/dispatch/mod.rs:3789:            .dispatch(
src/dispatch/mod.rs:3813:        let mut adapter = test_adapter(
src/dispatch/mod.rs:3824:        let adapter = dispatcher.adapter("no-newlines").unwrap().clone();
src/dispatch/mod.rs:3827:            .dispatch(
src/dispatch/mod.rs:3847:        let mut adapter = test_adapter(
src/dispatch/mod.rs:3858:        let adapter = dispatcher.adapter("mixed-streams").unwrap().clone();
src/dispatch/mod.rs:3861:            .dispatch(
src/dispatch/mod.rs:3884:        let mut adapter = test_adapter(
src/dispatch/mod.rs:3895:        let adapter = dispatcher.adapter("silent-process").unwrap().clone();
src/dispatch/mod.rs:3899:            .dispatch(
src/dispatch/mod.rs:3935:        let mut adapter = test_adapter(
src/dispatch/mod.rs:3946:        let adapter = dispatcher.adapter("small-chunks").unwrap().clone();
src/dispatch/mod.rs:3949:            .dispatch(
src/dispatch/mod.rs:3969:        let mut adapter = test_adapter(
src/dispatch/mod.rs:3980:        let adapter = dispatcher.adapter("large-burst").unwrap().clone();
src/dispatch/mod.rs:3983:            .dispatch(
src/dispatch/mod.rs:4005:        let mut adapter = test_adapter("timestamped", "echo output-$(date +%s%N)");
src/dispatch/mod.rs:4013:        let adapter1 = dispatcher.adapter("timestamped").unwrap().clone();
src/dispatch/mod.rs:4015:            .dispatch(
src/dispatch/mod.rs:4030:        let adapter2 = dispatcher.adapter("timestamped").unwrap().clone();
src/dispatch/mod.rs:4032:            .dispatch(
src/dispatch/mod.rs:4052:        let adapter = test_adapter("echo-test", "echo 'hello world'");
src/dispatch/mod.rs:4057:        let adapter_ref = dispatcher.adapter("echo-test").unwrap().clone();
src/dispatch/mod.rs:4059:            .dispatch(
src/dispatch/mod.rs:4078:        let adapter = test_adapter("stderr-test", "echo 'error message' >&2");
src/dispatch/mod.rs:4083:        let adapter_ref = dispatcher.adapter("stderr-test").unwrap().clone();
src/dispatch/mod.rs:4085:            .dispatch(
src/dispatch/mod.rs:4104:        let mut adapter = test_adapter("transform-test", "echo 'test output'");
src/dispatch/mod.rs:4110:        let adapter_ref = dispatcher.adapter("transform-test").unwrap().clone();
src/dispatch/mod.rs:4112:            .dispatch(
src/dispatch/mod.rs:4131:        let adapter = test_adapter(
src/dispatch/mod.rs:4139:        let adapter_ref = dispatcher.adapter("multiline-test").unwrap().clone();
src/dispatch/mod.rs:4141:            .dispatch(
src/dispatch/mod.rs:4162:        let adapter = test_adapter(
src/dispatch/mod.rs:4170:        let adapter_ref = dispatcher.adapter("chunked-test").unwrap().clone();
src/dispatch/mod.rs:4172:            .dispatch(
src/dispatch/mod.rs:4194:        let adapter = test_adapter("timestamp-order-test", "printf 'line1\\nline2\\nline3'");
src/dispatch/mod.rs:4199:        let adapter_ref = dispatcher.adapter("timestamp-order-test").unwrap().clone();
src/dispatch/mod.rs:4201:            .dispatch(
src/dispatch/mod.rs:4223:        let adapter = test_adapter(
src/dispatch/mod.rs:4231:        let adapter_ref = dispatcher.adapter("rapid-test").unwrap().clone();
src/dispatch/mod.rs:4233:            .dispatch(
src/cli/mod.rs:850:    let model = dispatch::load_adapters(&config.agent.adapters_dir, &dispatch::builtin_adapters())
src/cli/mod.rs:3959:    match dispatch::load_adapters(&config.agent.adapters_dir, &dispatch::builtin_adapters()) {
src/routing.rs:226:///     match_adapter("sonnet-4-6", &rules, "fallback"),
src/routing.rs:232:///     match_adapter("other-model", &rules, "fallback"),
src/routing.rs:236:pub fn match_adapter(model: &str, rules: &[RoutingRule], default: &str) -> Option<String> {
src/routing.rs:450:            match_adapter("sonnet-4-6", &rules, "fallback"),
src/routing.rs:454:            match_adapter("sonnet-4-5-new", &rules, "fallback"),
src/routing.rs:458:            match_adapter("claude-sonnet-4-6", &rules, "fallback"),
src/routing.rs:467:            match_adapter("sonnet-4-6", &rules, "fallback"),
src/routing.rs:471:            match_adapter("opus-4-6", &rules, "fallback"),
src/routing.rs:475:            match_adapter("claude-sonnet-4-6", &rules, "fallback"),
src/routing.rs:479:            match_adapter("claude-opus-4-6", &rules, "fallback"),
src/routing.rs:484:            match_adapter("haiku-4-5", &rules, "fallback"),
src/routing.rs:494:            match_adapter("claude-sonnet", &rules, "fallback"),
src/routing.rs:498:            match_adapter("claude-opus", &rules, "fallback"),
src/routing.rs:502:            match_adapter("claude-haiku-4-5", &rules, "fallback"),
src/routing.rs:507:            match_adapter("anthropic-sonnet", &rules, "fallback"),
src/routing.rs:521:            match_adapter("claude/sonnet", &rules, "fallback"),
src/routing.rs:525:            match_adapter("claude/any/nested/path", &rules, "fallback"),
src/routing.rs:531:            match_adapter("claude-sonnet", &rules, "fallback"),
src/routing.rs:541:            match_adapter("anything", &rules, "fallback"),
src/routing.rs:545:            match_adapter("claude-sonnet-4-6", &rules, "fallback"),
src/routing.rs:549:            match_adapter("provider/model", &rules, "fallback"),
src/routing.rs:559:            match_adapter("anything", &rules, "fallback"),
src/routing.rs:563:            match_adapter("nested/path/model", &rules, "fallback"),
src/routing.rs:578:            match_adapter("claude-sonnet-4-6", &rules, "fallback"),
src/routing.rs:583:            match_adapter("other-model", &rules, "fallback"),
src/routing.rs:592:            match_adapter("other-model", &rules, "fallback"),
src/routing.rs:596:            match_adapter("opus-4-6", &rules, "fallback"),
src/routing.rs:605:            match_adapter("any-model", &rules, "fallback"),
src/routing.rs:613:        assert_eq!(match_adapter("other-model", &rules, ""), None);
src/routing.rs:619:        assert_eq!(match_adapter("any-model", &rules, ""), None);
src/routing.rs:631:            match_adapter("sonnet-4-6", &rules, "fallback"),
src/routing.rs:645:            match_adapter("any-model", &rules, "fallback"),
src/routing.rs:655:            match_adapter("model*", &rules, "fallback"),
src/routing.rs:660:            match_adapter("model-xyz", &rules, "fallback"),
src/routing.rs:670:            match_adapter("model**", &rules, "fallback"),
src/routing.rs:675:            match_adapter("model-xyz", &rules, "fallback"),
src/routing.rs:685:            match_adapter("sonnet", &rules, "fallback"),
src/routing.rs:690:            match_adapter("sonnet-4-6", &rules, "fallback"),
src/routing.rs:708:            match_adapter("claude-sonnet-4-6", &rules, "fallback"),
src/routing.rs:712:            match_adapter("claude-opus-4-6", &rules, "fallback"),
src/routing.rs:716:            match_adapter("claude-haiku-4-5", &rules, "fallback"),
src/routing.rs:721:            match_adapter("other-model", &rules, "fallback"),
src/routing.rs:731:            match_adapter("provider/model", &rules, "fallback"),
src/routing.rs:736:            match_adapter("provider/foo/model", &rules, "fallback"),
src/routing.rs:746:            match_adapter("provider/model", &rules, "fallback"),
src/routing.rs:750:            match_adapter("provider/foo/model", &rules, "fallback"),
src/routing.rs:754:            match_adapter("provider/foo/bar/model", &rules, "fallback"),
src/routing.rs:824:            match_adapter("claude-sonnet-4-6", &rules, "fallback"),
src/routing.rs:828:            match_adapter("claude-opus-4-6", &rules, "fallback"),
src/routing.rs:832:            match_adapter("claude-fable-5", &rules, "fallback"),
src/routing.rs:836:            match_adapter("claude-haiku-4-5-20251001", &rules, "fallback"),
src/routing.rs:840:            match_adapter("sonnet-4-6", &rules, "fallback"),
src/routing.rs:844:            match_adapter("opus-4-6", &rules, "fallback"),
src/routing.rs:850:            match_adapter("gpt-4", &rules, "fallback"),
src/routing.rs:854:            match_adapter("gemini-pro", &rules, "fallback"),
src/routing.rs:864:            match_adapter("sonnet-4-6", &rules, "fallback"),
src/routing.rs:874:            match_adapter("", &rules, "fallback"),
src/routing.rs:884:            match_adapter("claude sonnet", &rules, "fallback"),
src/routing.rs:888:            match_adapter("claude-sonnet", &rules, "fallback"),
src/routing.rs:901:            match_adapter("gpt-4", &rules, "fallback"),
src/routing.rs:905:            match_adapter("gpt-3.5", &rules, "fallback"),
src/routing.rs:909:            match_adapter("gpt-4-turbo", &rules, "fallback"),
src/routing.rs:915:            match_adapter("claude-sonnet", &rules, "fallback"),
src/routing.rs:919:            match_adapter("gemini-pro", &rules, "fallback"),
src/routing.rs:932:            match_adapter("gpt-4", &rules, "fallback"),
src/routing.rs:936:            match_adapter("gpt-3.5", &rules, "fallback"),
src/routing.rs:940:            match_adapter("gpt-4-turbo", &rules, "fallback"),
src/routing.rs:946:            match_adapter("claude-sonnet", &rules, "fallback"),
src/routing.rs:958:            match_adapter("claude-sonnet-4-6", &rules, "fallback"),
src/routing.rs:962:            match_adapter("claude-opus-4-6", &rules, "fallback"),
src/routing.rs:966:            match_adapter("claude-haiku-4-5", &rules, "fallback"),
src/routing.rs:970:            match_adapter("claude-fable-5", &rules, "fallback"),
src/routing.rs:976:            match_adapter("gpt-4", &rules, "fallback"),
src/routing.rs:988:            match_adapter("gpt-4", &rules, "fallback"),
src/routing.rs:994:            match_adapter("gpt-3.5", &rules, "fallback"),
src/routing.rs:998:            match_adapter("gpt-4-turbo", &rules, "fallback"),
src/routing.rs:1002:            match_adapter("claude-sonnet", &rules, "fallback"),
src/routing.rs:1523:            match_adapter("claude-sonnet-4-6", &rules, "fallback"),
src/routing.rs:1529:            match_adapter("claude-opus-4-6", &rules, "fallback"),
src/routing.rs:1535:            match_adapter("gpt-4", &rules, "fallback"),
src/routing.rs:1553:            match_adapter("claude-sonnet-4-6", &rules, "fallback"),
src/routing.rs:1567:            match_adapter("any-model", &rules, "default-adapter"),
src/routing.rs:1576:        assert_eq!(match_adapter("any-model", &rules, ""), None);
src/routing.rs:1584:            match_adapter("gpt-4", &rules, "default-adapter"),
src/routing.rs:1593:        assert_eq!(match_adapter("gpt-4", &rules, ""), None);
src/routing.rs:1603:        assert_eq!(match_adapter("any-model", &rules, ""), None);
src/routing.rs:1614:            match_adapter("any-model", &rules, "default-adapter"),
src/routing.rs:1630:            match_adapter("claude-sonnet", &rules, "fallback"),
src/routing.rs:1638:            match_adapter("claude-sonnet-4-6", &rules, "fallback"),
src/routing.rs:1644:            match_adapter("gpt-4", &rules, "fallback"),
src/routing.rs:1660:            match_adapter("claude-sonnet", &rules, "fallback"),
src/routing.rs:1666:            match_adapter("claude-opus", &rules, "fallback"),
src/routing.rs:1672:            match_adapter("gpt-4", &rules, "fallback"),
src/routing.rs:1688:            match_adapter("claude-sonnet-4-6", &rules, "fallback"),
src/routing.rs:1694:            match_adapter("gpt-4", &rules, "fallback"),
src/routing.rs:1710:            match_adapter("claude-sonnet-4-6", &rules, "fallback"),
src/routing.rs:1719:        assert_eq!(match_adapter("any-model", &rules, ""), None);
src/routing.rs:1732:            match_adapter("gpt-4", &rules, "fallback"),
src/routing.rs:1750:            match_adapter("claude-sonnet", &rules, "fallback"),
src/routing.rs:1756:            match_adapter("gpt-4", &rules, "fallback"),
src/routing.rs:1762:            match_adapter("other", &rules, "fallback"),
src/routing.rs:1773:            match_adapter("any-model", &rules, default),
src/routing.rs:1784:            match_adapter("any-model", &rules, default),
src/routing.rs:1794:            match_adapter("any-model", &rules, "   "),
src/routing.rs:1805:        assert_eq!(match_adapter("gpt-4", &rules, ""), None);
src/routing.rs:1808:        assert_eq!(match_adapter("gpt-4", &rules, "  "), Some("  ".to_string()));
src/routing.rs:1817:            match_adapter("sonnet-4-6", &rules, "fallback"),
src/routing.rs:1821:            match_adapter("opus-4-6", &rules, "fallback"),
src/routing.rs:1825:            match_adapter("haiku-4-5", &rules, "fallback"),
src/routing.rs:1829:            match_adapter("fable-5", &rules, "fallback"),
src/routing.rs:1840:            match_adapter("model-4-xyz", &rules, "fallback"),
src/routing.rs:1844:            match_adapter("model-9-abc", &rules, "fallback"),
src/routing.rs:1849:            match_adapter("model-a-xyz", &rules, "fallback"),
src/routing.rs:1861:            match_adapter("sonnet", &rules, "fallback"),
src/routing.rs:1867:            match_adapter("sonnet-4-6", &rules, "fallback"),
src/routing.rs:1884:            match_adapter("claude-sonnet", &rules, "fallback"),
src/routing.rs:1890:            match_adapter("gpt-4", &rules, "fallback"),
src/routing.rs:1896:            match_adapter("sonnet-4-6", &rules, "fallback"),
src/routing.rs:1902:            match_adapter("gemini-pro", &rules, "fallback"),
src/routing.rs:1925:            match_adapter("aaa", &rules, "fallback"),
src/routing.rs:1931:            match_adapter("eee", &rules, "fallback"),
src/routing.rs:1937:            match_adapter("jjj", &rules, "fallback"),
src/routing.rs:1943:            match_adapter("zzz", &rules, "fallback"),
src/routing.rs:1958:            match_adapter("Claude-Sonnet", &rules, "fallback"),
src/routing.rs:1964:            match_adapter("claude-sonnet", &rules, "fallback"),
src/routing.rs:1970:            match_adapter("CLAUDE-SONNET", &rules, "fallback"),
src/routing.rs:1982:        let result = match_adapter("gpt-4", &rules, "");
src/routing.rs:1990:        let result2 = match_adapter("gpt-4", &rules, "fallback");
src/routing.rs:1998:        assert_eq!(match_adapter("", &rules, ""), None);
src/routing.rs:2006:            match_adapter("", &rules, "default"),
src/routing.rs:2016:            match_adapter("", &rules, "fallback"),
src/routing.rs:2028:            match_adapter("model.x", &rules, "fallback"),
src/routing.rs:2034:            match_adapter("model-x", &rules, "fallback"),
src/health/mod.rs:312:    pub fn update_adapter(&self, adapter: Option<&str>) {
src/worker/mod.rs:499:        let dispatcher = match Dispatcher::new(&config, telemetry.clone()) {
src/worker/mod.rs:507:                Dispatcher::with_adapters(builtins, telemetry.clone(), config.agent.timeout)
src/worker/mod.rs:1125:        if let Err(e) = self.resolve_adapter() {
src/worker/mod.rs:1436:            .adapter(&self.config.agent.default)
src/worker/mod.rs:2096:        let adapter = self.resolve_adapter()?;
src/worker/mod.rs:2099:        self.health.update_adapter(Some(&adapter.name));
src/worker/mod.rs:2181:        let adapter = self.resolve_adapter()?;
src/worker/mod.rs:2253:                    .dispatch(&bead.id, &prompt, &adapter, dispatch_ws)
src/worker/mod.rs:3837:    fn resolve_adapter(&self) -> Result<crate::dispatch::AgentAdapter> {
src/worker/mod.rs:3846:            .adapter(default_adapter_name)
src/worker/mod.rs:3873:        let adapter = self.dispatcher.adapter(&chosen_adapter_name)
src/worker/mod.rs:4040:            .adapter(adapter_name)
src/worker/mod.rs:4412:        let result = worker.resolve_adapter();
src/worker/mod.rs:5523:    async fn resolve_provider_returns_none_for_missing_adapter() {
src/worker/mod.rs:5623:            Dispatcher::with_adapters(adapters, Telemetry::new("test-worker".to_string()), 5);
src/worker/mod.rs:5831:    async fn apply_routing_rules_strict_false_uses_default_adapter() {
src/worker/mod.rs:6156:    async fn apply_routing_rules_baseline_multiple_rules_same_adapter() {
src/mitosis/mod.rs:299:        let adapter = match dispatcher.adapter(agent_name) {
src/mitosis/mod.rs:345:            .dispatch(&bead.id, &prompt, adapter, workspace)
src/mitosis/mod.rs:517:        let adapter = match dispatcher.adapter(agent_name) {
src/mitosis/mod.rs:589:            .dispatch(&bead.id, &prompt, adapter, workspace)
src/mitosis/mod.rs:2247:        Dispatcher::with_adapters(adapters, telemetry, 60)
src/telemetry/mod.rs:2883:        let failures = self.dispatch(event);
src/telemetry/mod.rs:5806:        let failures = sink.dispatch(&event);
src/telemetry/mod.rs:5821:        let failures = sink.dispatch(&event);
src/telemetry/mod.rs:5837:        let failures = sink.dispatch(&event);
src/telemetry/mod.rs:5851:        let failures = sink.dispatch(&event);
src/telemetry/mod.rs:5877:        let failures = sink.dispatch(&event);
src/telemetry/mod.rs:5896:        let failures = sink.dispatch(&event);

exec
/bin/bash -lc "rg -n \"pub struct Config|struct AgentConfig|routing|adapters_dir|default_adapter\" src/config -g '*.rs' && sed -n '1,260p' src/config/mod.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
src/config/mod.rs:29:/// A single routing rule mapping model patterns to adapters.
src/config/mod.rs:44:/// Agent routing configuration.
src/config/mod.rs:47:/// the routing rules are evaluated in order to determine which adapter to use.
src/config/mod.rs:50:    /// Ordered list of routing rules (first match wins).
src/config/mod.rs:56:    pub default_adapter: Option<String>,
src/config/mod.rs:58:    /// When true, fail if no routing rule matches instead of falling back.
src/config/mod.rs:63:    /// - No fallback to `agent.default` or `default_adapter` occurs
src/config/mod.rs:106:pub struct AgentConfig {
src/config/mod.rs:120:    #[serde(default = "AgentConfig::default_adapters_dir")]
src/config/mod.rs:121:    pub adapters_dir: PathBuf,
src/config/mod.rs:123:    /// Model-to-adapter routing rules (optional).
src/config/mod.rs:125:    pub routing: Option<RoutingConfig>,
src/config/mod.rs:134:            adapters_dir: Self::default_adapters_dir(),
src/config/mod.rs:135:            routing: Self::default_routing(),
src/config/mod.rs:147:    fn default_adapters_dir() -> PathBuf {
src/config/mod.rs:151:    /// Default routing rules for Anthropic subscription models.
src/config/mod.rs:156:    fn default_routing() -> Option<RoutingConfig> {
src/config/mod.rs:164:            default_adapter: Some("claude-code-glm-4.7".to_string()),
src/config/mod.rs:4595:    pub routing: Option<RoutingConfig>,
src/config/mod.rs:4625:pub struct Config {
src/config/mod.rs:4694:        self.agent.adapters_dir = expand_tilde(&self.agent.adapters_dir);
src/config/mod.rs:4753:pub struct ConfigError {
src/config/mod.rs:4820:pub struct ConfigLoader;
src/config/mod.rs:4910:            if let Some(ref routing) = agent.routing {
src/config/mod.rs:4911:                config.agent.routing = Some(routing.clone());
src/config/mod.rs:4912:                sources.insert("agent.routing".to_string(), source.clone());
src/config/mod.rs:4997:                    "agent.routing.default_adapter" => {
src/config/mod.rs:5000:                            .routing
src/config/mod.rs:5002:                            .default_adapter = Some(value);
src/config/mod.rs:5375:        // Validate routing regex patterns.
src/config/mod.rs:5376:        if let Some(ref routing) = config.agent.routing {
src/config/mod.rs:5377:            for (idx, rule) in routing.rules.iter().enumerate() {
src/config/mod.rs:5381:                        field: format!("agent.routing.rules[{}].match_model", idx),
src/config/mod.rs:5388:                        field: format!("agent.routing.rules[{}].adapter", idx),
src/config/mod.rs:7344:    fn default_routing_config_matches_anthropic_models() {
src/config/mod.rs:7346:        let routing = config
src/config/mod.rs:7348:            .routing
src/config/mod.rs:7349:            .expect("default routing should be Some");
src/config/mod.rs:7352:        assert_eq!(routing.rules.len(), 1);
src/config/mod.rs:7354:        let rule = &routing.rules[0];
src/config/mod.rs:7360:            routing.default_adapter.as_deref(),
src/config/mod.rs:7366:    fn routing_config_with_rules_parses() {
src/config/mod.rs:7370:  routing:
src/config/mod.rs:7376:    default_adapter: claude-code-glm-4.7
src/config/mod.rs:7383:        assert!(config.agent.routing.is_some());
src/config/mod.rs:7384:        let routing = config.agent.routing.as_ref().unwrap();
src/config/mod.rs:7385:        assert_eq!(routing.rules.len(), 2);
src/config/mod.rs:7386:        assert_eq!(routing.rules[0].match_model, "(claude-)?(sonnet|opus).*");
src/config/mod.rs:7387:        assert_eq!(routing.rules[0].adapter, "claude-print");
src/config/mod.rs:7388:        assert_eq!(routing.rules[1].match_model, "haiku.*");
src/config/mod.rs:7389:        assert_eq!(routing.rules[1].adapter, "claude-code-glm-4.7");
src/config/mod.rs:7391:            routing.default_adapter.as_deref(),
src/config/mod.rs:7397:    fn routing_config_empty_rules_list_is_valid() {
src/config/mod.rs:7400:  routing:
src/config/mod.rs:7402:    default_adapter: fallback-adapter
src/config/mod.rs:7409:        assert!(config.agent.routing.is_some());
src/config/mod.rs:7410:        let routing = config.agent.routing.as_ref().unwrap();
src/config/mod.rs:7411:        assert!(routing.rules.is_empty());
src/config/mod.rs:7412:        assert_eq!(routing.default_adapter.as_deref(), Some("fallback-adapter"));
src/config/mod.rs:7416:    fn invalid_regex_in_routing_rule_fails_validation() {
src/config/mod.rs:7418:        config.agent.routing = Some(RoutingConfig {
src/config/mod.rs:7423:            default_adapter: None,
src/config/mod.rs:7431:                .any(|e| e.field == "agent.routing.rules[0].match_model"),
src/config/mod.rs:7438:    fn empty_adapter_in_routing_rule_fails_validation() {
src/config/mod.rs:7440:        config.agent.routing = Some(RoutingConfig {
src/config/mod.rs:7445:            default_adapter: None,
src/config/mod.rs:7453:                .any(|e| e.field == "agent.routing.rules[0].adapter"),
src/config/mod.rs:7460:    fn valid_regex_in_routing_rule_passes_validation() {
src/config/mod.rs:7462:        config.agent.routing = Some(RoutingConfig {
src/config/mod.rs:7473:            default_adapter: Some("claude-fallback".to_string()),
src/config/mod.rs:7479:            !errors.iter().any(|e| e.field.starts_with("agent.routing")),
src/config/mod.rs:7480:            "routing config should be valid, but got errors: {:?}",
src/config/mod.rs:7486:    fn workspace_config_routing_override() {
src/config/mod.rs:7492:  routing:
src/config/mod.rs:7496:    default_adapter: workspace-default
src/config/mod.rs:7506:        assert!(config.agent.routing.is_some());
src/config/mod.rs:7507:        let routing = config.agent.routing.as_ref().unwrap();
src/config/mod.rs:7508:        assert_eq!(routing.rules.len(), 1);
src/config/mod.rs:7509:        assert_eq!(routing.rules[0].match_model, "fable.*");
src/config/mod.rs:7510:        assert_eq!(routing.rules[0].adapter, "fable-adapter");
src/config/mod.rs:7512:            routing.default_adapter.as_deref(),
src/config/mod.rs:7515:        assert!(sources.contains_key("agent.routing"));
src/config/mod.rs:7519:    fn env_var_routing_default_adapter_override() {
src/config/mod.rs:7528:        assert!(config.agent.routing.is_some());
src/config/mod.rs:7532:                .routing
src/config/mod.rs:7535:                .default_adapter
src/config/mod.rs:7539:        assert!(sources.contains_key("agent.routing.default_adapter"));
src/config/mod.rs:7543:    fn env_var_routing_override_beats_workspace() {
src/config/mod.rs:7549:  routing:
src/config/mod.rs:7550:    default_adapter: workspace-fallback
src/config/mod.rs:7564:                .routing
src/config/mod.rs:7567:                .default_adapter
src/config/mod.rs:7581:                .routing
src/config/mod.rs:7584:                .default_adapter
src/config/mod.rs:7589:            sources.get("agent.routing.default_adapter"),
src/config/mod.rs:7595:    fn multiple_validation_errors_in_routing() {
src/config/mod.rs:7597:        config.agent.routing = Some(RoutingConfig {
src/config/mod.rs:7612:            default_adapter: None,
src/config/mod.rs:7617:        let routing_errors: Vec<_> = errors
src/config/mod.rs:7619:            .filter(|e| e.field.starts_with("agent.routing"))
src/config/mod.rs:7623:            routing_errors.len() >= 3,
src/config/mod.rs:7624:            "expected at least 3 routing errors, got {}: {:?}",
src/config/mod.rs:7625:            routing_errors.len(),
src/config/mod.rs:7626:            routing_errors
src/config/mod.rs:7631:    fn routing_config_yaml_roundtrip() {
src/config/mod.rs:7632:        let routing = RoutingConfig {
src/config/mod.rs:7643:            default_adapter: Some("default-adapter".to_string()),
src/config/mod.rs:7647:        let yaml = serde_yaml::to_string(&routing).unwrap();
src/config/mod.rs:7650:        assert_eq!(decoded.rules.len(), routing.rules.len());
src/config/mod.rs:7651:        assert_eq!(decoded.rules[0].match_model, routing.rules[0].match_model);
src/config/mod.rs:7652:        assert_eq!(decoded.rules[0].adapter, routing.rules[0].adapter);
src/config/mod.rs:7653:        assert_eq!(decoded.default_adapter, routing.default_adapter);
src/config/mod.rs:7657:    fn agent_config_with_routing_yaml_roundtrip() {
src/config/mod.rs:7659:            routing: Some(RoutingConfig {
src/config/mod.rs:7664:                default_adapter: Some("fallback".to_string()),
src/config/mod.rs:7673:        assert!(decoded.routing.is_some());
src/config/mod.rs:7674:        let routing = decoded.routing.as_ref().unwrap();
src/config/mod.rs:7675:        assert_eq!(routing.rules.len(), 1);
src/config/mod.rs:7676:        assert_eq!(routing.rules[0].match_model, "haiku");
src/config/mod.rs:7677:        assert_eq!(routing.default_adapter.as_deref(), Some("fallback"));
src/config/mod.rs:7681:    fn routing_patterns_match_bare_aliases() {
src/config/mod.rs:7684:        config.agent.routing = Some(RoutingConfig {
src/config/mod.rs:7703:            default_adapter: Some("default-adapter".to_string()),
src/config/mod.rs:7709:            !errors.iter().any(|e| e.field.starts_with("agent.routing")),
src/config/mod.rs:7716:    fn routing_patterns_match_full_model_ids() {
src/config/mod.rs:7719:        config.agent.routing = Some(RoutingConfig {
src/config/mod.rs:7734:            default_adapter: Some("default-adapter".to_string()),
src/config/mod.rs:7740:            !errors.iter().any(|e| e.field.starts_with("agent.routing")),
src/config/mod.rs:7747:    fn routing_patterns_match_with_wildcards() {
src/config/mod.rs:7750:        config.agent.routing = Some(RoutingConfig {
src/config/mod.rs:7761:            default_adapter: Some("claude-code-glm-4.7".to_string()),
src/config/mod.rs:7767:            !errors.iter().any(|e| e.field.starts_with("agent.routing")),
src/config/mod.rs:7774:    fn routing_config_from_global_config_file() {
src/config/mod.rs:7780:  routing:
src/config/mod.rs:7786:    default_adapter: claude-code-glm-4.7
src/config/mod.rs:7791:        assert!(config.agent.routing.is_some());
src/config/mod.rs:7792:        let routing = config.agent.routing.as_ref().unwrap();
src/config/mod.rs:7793:        assert_eq!(routing.rules.len(), 2);
src/config/mod.rs:7794:        assert_eq!(routing.rules[0].adapter, "claude-print");
src/config/mod.rs:7795:        assert_eq!(routing.rules[1].adapter, "claude-code-glm-4.7");
src/config/mod.rs:7797:            routing.default_adapter.as_deref(),
src/config/mod.rs:7803:    fn routing_config_backward_compatibility() {
src/config/mod.rs:7804:        // Config without routing section should behave like before (pure agent.default)
src/config/mod.rs:7816:            config.agent.routing.is_none(),
src/config/mod.rs:7817:            "routing should be None when not specified"
src/config/mod.rs:7825:    fn routing_first_match_wins() {
src/config/mod.rs:7827:        let routing = RoutingConfig {
src/config/mod.rs:7838:            default_adapter: Some("fallback-adapter".to_string()),
src/config/mod.rs:7843:        let first_rule = &routing.rules[0];
src/config/mod.rs:7851:        let second_rule = &routing.rules[1];
src/config/mod.rs:7857:        // The routing logic in apply_routing_rules iterates in order and returns first match
src/config/mod.rs:7861:    fn routing_workspace_override() {
src/config/mod.rs:7868:  routing:
src/config/mod.rs:7872:    default_adapter: workspace-fallback
src/config/mod.rs:7883:        assert!(config.agent.routing.is_some());
src/config/mod.rs:7884:        let routing = config.agent.routing.as_ref().unwrap();
src/config/mod.rs:7885:        assert_eq!(routing.rules.len(), 1);
src/config/mod.rs:7886:        assert_eq!(routing.rules[0].adapter, "workspace-sonnet-adapter");
src/config/mod.rs:7888:            routing.default_adapter.as_deref(),
src/config/mod.rs:7894:    fn routing_default_fallback_no_match() {
src/config/mod.rs:7895:        // Test that when no rules match, we fall back to default_adapter
src/config/mod.rs:7896:        let routing = RoutingConfig {
src/config/mod.rs:7901:            default_adapter: Some("fallback-adapter".to_string()),
src/config/mod.rs:7906:        let re = regex::Regex::new(&routing.rules[0].match_model).unwrap();
src/config/mod.rs:7912:        // Should fall back to default_adapter
src/config/mod.rs:7913:        assert_eq!(routing.default_adapter.as_deref(), Some("fallback-adapter"));
src/config/mod.rs:7917:    fn routing_strict_mode_failure() {
src/config/mod.rs:7919:        let routing = RoutingConfig {
src/config/mod.rs:7924:            default_adapter: None,
src/config/mod.rs:7929:        assert!(routing.strict, "strict mode should be enabled");
src/config/mod.rs:7932:        let re = regex::Regex::new(&routing.rules[0].match_model).unwrap();
src/config/mod.rs:7939:        // (This is tested in integration tests via apply_routing_rules)
src/config/mod.rs:7943:    fn routing_default_anthropic_models_to_claude_print() {
src/config/mod.rs:7944:        // Test that default routing rules route Anthropic models to claude-print
src/config/mod.rs:7945:        let default_routing = AgentConfig::default_routing();
src/config/mod.rs:7946:        assert!(default_routing.is_some());
src/config/mod.rs:7948:        let routing = default_routing.unwrap();
src/config/mod.rs:7949:        assert!(!routing.rules.is_empty(), "should have default rules");
src/config/mod.rs:7952:        let rule = &routing.rules[0];
src/config/mod.rs:7971:            routing.default_adapter.as_deref(),
src/config/mod.rs:7977:    fn routing_glm_47_defaults_to_claude_code_glm_47() {
src/config/mod.rs:7979:        let default_routing = AgentConfig::default_routing();
src/config/mod.rs:7980:        let routing = default_routing.unwrap();
src/config/mod.rs:7983:        let rule = &routing.rules[0];
src/config/mod.rs:7992:        // Should fall back to default_adapter
src/config/mod.rs:7994:            routing.default_adapter.as_deref(),
src/config/mod.rs:8771:        config.agent.adapters_dir = PathBuf::from("~/adapters");
src/config/mod.rs:8785:            config.agent.adapters_dir,
src/config/mod.rs:8863:        config.agent.adapters_dir = PathBuf::from("relative/adapters");
src/config/mod.rs:8874:            config.agent.adapters_dir,
src/config/mod.rs:9050:        config.agent.adapters_dir = PathBuf::from("~/adapters");
src/config/mod.rs:9068:            config.agent.adapters_dir,
src/config/mod.rs:9092:        config.agent.adapters_dir = PathBuf::from("~/.needle.d/adapters");
src/config/mod.rs:9103:            config.agent.adapters_dir,
//! Hierarchical configuration loading and validation.
//!
//! Resolution order (later layers override earlier):
//! 1. Built-in defaults
//! 2. Global config file (`~/.config/needle/config.yaml`)
//! 3. Workspace config file (`.needle.yaml`)
//! 4. Environment variables (`NEEDLE_*`)
//! 5. CLI arguments (highest precedence)
//!
//! Config is loaded once at boot and never reloaded.
//!
//! Leaf module — depends only on `types`.

use std::collections::BTreeMap;
use std::fmt;
use std::path::{Path, PathBuf};

use anyhow::{anyhow, bail, Context, Result};
use serde::{de::Visitor, Deserialize, Deserializer, Serialize};

use crate::cost::{BudgetConfig, PricingConfig};
use crate::types::{IdentifierScheme, IdleAction};
use crate::validation::GateConfig;

// ──────────────────────────────────────────────────────────────────────────────
// Sub-structs
// ──────────────────────────────────────────────────────────────────────────────

/// A single routing rule mapping model patterns to adapters.
///
/// Rules are evaluated in order; the first matching rule determines the adapter.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct RoutingRule {
    /// Regex or glob pattern to match against model names.
    ///
    /// Patterns are matched against the model name only (not provider prefixes).
    /// Examples: `"sonnet"`, `"opus"`, `"claude-sonnet-4-6"`, `"(claude-)?(sonnet|opus).*"`.
    pub match_model: String,

    /// Adapter to use for matching models (e.g., `claude-print`, `claude-code-glm-4.7`).
    pub adapter: String,
}

/// Agent routing configuration.
///
/// Maps model name patterns to adapters. When a bead specifies a model,
/// the routing rules are evaluated in order to determine which adapter to use.
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct RoutingConfig {
    /// Ordered list of routing rules (first match wins).
    #[serde(default)]
    pub rules: Vec<RoutingRule>,

    /// Fallback adapter when no rules match (defaults to `agent.default`).
    #[serde(default)]
    pub default_adapter: Option<String>,

    /// When true, fail if no routing rule matches instead of falling back.
    ///
    /// If set to true and no rule matches the model:
    /// - The bead dispatch fails immediately
    /// - A `RoutingFailed` telemetry event is emitted
    /// - No fallback to `agent.default` or `default_adapter` occurs
    ///
    /// Default: false (fallback behavior for backward compatibility).
    #[serde(default)]
    pub strict: bool,
}

/// Process timeout limits configuration.
///
/// Defines timeout parameters for agent process execution, separating
/// timeout configuration from other agent settings.
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ProcessLimits {
    /// Idle timeout duration (None means disabled).
    ///
    /// When set, the agent process will be terminated if no stdout/stderr
    /// activity occurs for this duration. This prevents hung processes from
    /// blocking indefinitely when they stop producing output but don't exit.
    ///
    /// Set to `None` to disable idle timeout detection (processes can run
    /// indefinitely as long as they produce no output).
    ///
    /// # Examples
    ///
    /// ```yaml
    /// # Disable idle timeout (processes can hang indefinitely)
    /// process_limits:
    ///     idle_timeout: null
    ///
    /// # 5 minute idle timeout
    /// process_limits:
    ///     idle_timeout: 300s
    ///
    /// # 30 second idle timeout
    /// process_limits:
    ///     idle_timeout: 30s
    /// ```
    #[serde(default)]
    pub idle_timeout: Option<u64>,
}

/// Agent (AI model CLI) configuration.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AgentConfig {
    /// Name or path of the default agent adapter (e.g., `claude`).
    #[serde(default = "AgentConfig::default_agent")]
    pub default: String,

    /// Extra arguments to pass before the prompt.
    #[serde(default)]
    pub args: Vec<String>,

    /// Agent process timeout in seconds (0 = unlimited).
    #[serde(default = "AgentConfig::default_timeout")]
    pub timeout: u64,

    /// Directory containing adapter TOML files.
    #[serde(default = "AgentConfig::default_adapters_dir")]
    pub adapters_dir: PathBuf,

    /// Model-to-adapter routing rules (optional).
    #[serde(default)]
    pub routing: Option<RoutingConfig>,
}

impl Default for AgentConfig {
    fn default() -> Self {
        AgentConfig {
            default: Self::default_agent(),
            args: Vec::new(),
            timeout: Self::default_timeout(),
            adapters_dir: Self::default_adapters_dir(),
            routing: Self::default_routing(),
        }
    }
}

impl AgentConfig {
    fn default_agent() -> String {
        "claude".to_string()
    }
    pub fn default_timeout() -> u64 {
        3600
    }
    fn default_adapters_dir() -> PathBuf {
        dirs_or_home(".config/needle/adapters")
    }

    /// Default routing rules for Anthropic subscription models.
    ///
    /// Routers Anthropic Claude models (sonnet, opus, fable, haiku) to claude-print
    /// to use subscription billing before the June 15, 2026 API credit transition.
    /// All other models fall back to claude-code-glm-4.7.
    fn default_routing() -> Option<RoutingConfig> {
        Some(RoutingConfig {
            rules: vec![RoutingRule {
                // Match Anthropic Claude models on subscription billing
                // Patterns: claude-sonnet-4-6, claude-opus-4-6, claude-fable-5, claude-haiku-4-5-20251001
                match_model: "(claude-)?(sonnet|opus|fable|haiku).*".to_string(),
                adapter: "claude-print".to_string(),
            }],
            default_adapter: Some("claude-code-glm-4.7".to_string()),
            strict: false,
        })
    }
}

/// Worker fleet configuration.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WorkerConfig {
    /// Maximum number of concurrent workers.
    #[serde(default = "WorkerConfig::default_max_workers")]
    pub max_workers: u32,

    /// Stagger delay (seconds) between worker launches.
    #[serde(default = "WorkerConfig::default_launch_stagger_seconds")]
    pub launch_stagger_seconds: u64,

    /// Seconds to wait between queue polls when idle.
    #[serde(default = "WorkerConfig::default_idle_timeout")]
    pub idle_timeout: u64,

    /// What to do when the queue is empty.
    #[serde(default)]
    pub idle_action: IdleAction,

    /// Maximum claim retries before skipping a bead.
    #[serde(default = "WorkerConfig::default_max_claim_retries")]
    pub max_claim_retries: u32,

    /// Consecutive race_lost attempts before treating the ready queue as empty.
    #[serde(default = "WorkerConfig::default_claim_race_lost_skip")]
    pub claim_race_lost_skip: u32,

    /// How workers generate their unique names.
    #[serde(default)]
    pub identifier_scheme: IdentifierScheme,

    /// Warn when CPU load (0.0–1.0) exceeds this threshold.
    #[serde(default = "WorkerConfig::default_cpu_load_warn")]
    pub cpu_load_warn: f64,

    /// Require shipped (committed + pushed) work, or an explicit bead update,
    /// before accepting a bead's closure (bf-1i9). See `validation::verify_shipped_work`.
    #[serde(default = "WorkerConfig::default_enforce_shipped_work")]
    pub enforce_shipped_work: bool,

    /// Warn when available memory falls below this threshold (MB).
    #[serde(default = "WorkerConfig::default_memory_free_warn_mb")]
    pub memory_free_warn_mb: u64,

    /// Maximum additional wait (seconds) for load-adaptive stagger when load is high.
    #[serde(default = "WorkerConfig::default_adaptive_stagger_max_wait_secs")]
    pub adaptive_stagger_max_wait_secs: u64,

    /// How often (seconds) to recheck load during load-adaptive stagger extended wait.
    #[serde(default = "WorkerConfig::default_adaptive_stagger_check_interval_secs")]
    pub adaptive_stagger_check_interval_secs: u64,

    /// BUILDING state timeout in seconds (0 = unlimited).
    #[serde(default = "WorkerConfig::default_building_timeout")]
    pub building_timeout: u64,

    /// Minimum idle backoff in seconds (event-driven polling floor).
    #[serde(default = "WorkerConfig::default_idle_backoff_min")]
    pub idle_backoff_min: u64,

    /// Maximum idle backoff in seconds (for jittered random selection).
    #[serde(default = "WorkerConfig::default_idle_backoff_max")]
    pub idle_backoff_max: u64,

    /// Short retry backoff in seconds (for found-but-excluded case).
    #[serde(default = "WorkerConfig::default_short_retry_backoff")]
    pub short_retry_backoff: u64,

    /// Explicit path to the worker binary `needle supervise` spawns.
    ///
    /// When `None` (the default), the supervisor resolves
    /// `std::env::current_exe()` — the supervisor and worker are the same
    /// binary, so this is always correct absent an override. Set this only
    /// when the running binary's own path is deliberately not what should be
    /// spawned (e.g. a wrapper script). See GitHub issue jedarden/NEEDLE#11:
    /// the previous hardcoded `Command::new("needle")` resolved whatever
    /// binary happened to occupy that name on `$PATH`, silently spawning the
    /// wrong process when another tool shared the name.
    #[serde(default)]
    pub worker_binary_path: Option<PathBuf>,
}

impl Default for WorkerConfig {
    fn default() -> Self {
        WorkerConfig {
            max_workers: Self::default_max_workers(),
            launch_stagger_seconds: Self::default_launch_stagger_seconds(),
            idle_timeout: Self::default_idle_timeout(),
            idle_action: IdleAction::default(),
            max_claim_retries: Self::default_max_claim_retries(),
            claim_race_lost_skip: Self::default_claim_race_lost_skip(),

exec
/bin/bash -lc "sed -n '720,830p' src/dispatch/mod.rs; sed -n '2100,2180p' src/dispatch/mod.rs; sed -n '2450,2630p' src/dispatch/mod.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
            None
        };

        Ok(Dispatcher {
            adapters,
            telemetry,
            global_timeout_secs: config.agent.timeout,
            sanitizer,
            tsnet_registry,
            tsnet_config,
        })
    }

    /// Create a dispatcher with explicit adapters (for testing).
    pub fn with_adapters(
        adapters: HashMap<String, AgentAdapter>,
        telemetry: Telemetry,
        global_timeout_secs: u64,
    ) -> Self {
        Dispatcher {
            adapters,
            telemetry,
            global_timeout_secs,
            sanitizer: None,
            tsnet_registry: None,
            tsnet_config: TsnetConfig::default(),
        }
    }

    /// Look up an adapter by name.
    pub fn adapter(&self, name: &str) -> Option<&AgentAdapter> {
        self.adapters.get(name)
    }

    /// List all loaded adapter names.
    pub fn adapter_names(&self) -> Vec<&str> {
        self.adapters.keys().map(|s| s.as_str()).collect()
    }

    /// Resolve an adapter name from a model name using routing rules.
    ///
    /// If routing is configured in the config, returns the adapter name that
    /// matches the model pattern (first match wins). Otherwise, returns the
    /// default adapter name.
    ///
    /// # Arguments
    /// * `model` - Model name (e.g., "sonnet", "claude-opus-4-8", "fable")
    /// * `config` - Full config containing routing rules
    ///
    /// # Returns
    /// The resolved adapter name (e.g., "claude-code-glm-4.7", "claude-sonnet")
    pub fn resolve_adapter_name(&self, model: &str, config: &Config) -> String {
        if let Some(ref routing) = config.agent.routing {
            // Check each routing rule in order (first match wins)
            for rule in &routing.rules {
                match regex::Regex::new(&rule.match_model) {
                    Ok(re) => {
                        if re.is_match(model) {
                            tracing::debug!(
                                model = %model,
                                pattern = %rule.match_model,
                                adapter = %rule.adapter,
                                "routing rule matched"
                            );
                            return rule.adapter.clone();
                        }
                    }
                    Err(_) => {
                        // Regex was validated during config load, so this should never happen.
                        // Fall back to default if somehow we get an invalid pattern at runtime.
                        tracing::warn!(
                            pattern = %rule.match_model,
                            "invalid routing regex pattern (should have been caught by config validation)"
                        );
                        continue;
                    }
                }
            }

            // No rules matched; use default_adapter if set
            if let Some(ref default_adapter) = routing.default_adapter {
                tracing::debug!(
                    model = %model,
                    adapter = %default_adapter,
                    "no routing rules matched, using default_adapter"
                );
                return default_adapter.clone();
            }
        }

        // No routing configured or no default_adapter; fall back to agent.default
        tracing::debug!(
            model = %model,
            adapter = %config.agent.default,
            "no routing configured, using agent.default"
        );
        config.agent.default.clone()
    }

    /// Execute the agent process for a bead.
    ///
    /// 1. Writes the prompt to a temp file
    /// 2. Renders the invoke template with variables
    /// 3. Sets adapter-specific environment variables
    /// 4. Spawns the process via `bash -c`
    /// 5. Waits with timeout enforcement (kills on timeout, exit 124)
    /// 6. Captures stdout, stderr, exit code
    /// 7. Cleans up the temp file
    #[tracing::instrument(
        name = "agent.dispatch",
        skip(self, bead_id, prompt, adapter, workspace),
// ──────────────────────────────────────────────────────────────────────────────
// Tests
// ──────────────────────────────────────────────────────────────────────────────

#[cfg(test)]
mod tests {
    use super::*;

    fn test_adapter(name: &str, template: &str) -> AgentAdapter {
        AgentAdapter {
            name: name.to_string(),
            description: None,
            agent_cli: "test".to_string(),
            version_command: None,
            input_method: InputMethod::Stdin,
            invoke_template: template.to_string(),
            environment: HashMap::new(),
            timeout_secs: 10,
            idle_timeout_secs: 0,
            hard_timeout_secs: 0,
            provider: None,
            model: None,
            token_extraction: TokenExtraction::None,
            output_transform: None,
            harness: None,
            harness_version: None,
        }
    }

    fn test_prompt(content: &str) -> BuiltPrompt {
        BuiltPrompt {
            content: content.to_string(),
            hash: "testhash".to_string(),
            token_estimate: content.len() as u64 / 4,
            template_name: "pluck".to_string(),
            template_version: "pluck-default".to_string(),
        }
    }

    fn test_dispatcher(adapters: HashMap<String, AgentAdapter>) -> Dispatcher {
        let telemetry = Telemetry::new("test-worker".to_string());
        Dispatcher::with_adapters(adapters, telemetry, 3600)
    }

    // ── Template rendering ──

    #[test]
    fn render_template_substitutes_all_variables() {
        let template = "cd {workspace} && agent --model {model} < {prompt_file} # bead={bead_id}";
        let result = render_template(
            template,
            Path::new("/home/workspace"),
            Path::new("/tmp/needle/prompt.md"),
            &BeadId::from("needle-abc"),
            "claude-sonnet-4-6",
        );
        assert!(result.contains("/home/workspace"));
        assert!(result.contains("/tmp/needle/prompt.md"));
        assert!(result.contains("needle-abc"));
        assert!(result.contains("claude-sonnet-4-6"));
        assert!(!result.contains("{workspace}"));
        assert!(!result.contains("{prompt_file}"));
        assert!(!result.contains("{bead_id}"));
        assert!(!result.contains("{model}"));
    }

    #[test]
    fn render_template_preserves_unrecognized_placeholders() {
        let result = render_template(
            "echo {unknown}",
            Path::new("/tmp"),
            Path::new("/tmp/p.md"),
            &BeadId::from("nd-x"),
            "m",
        );
        assert!(result.contains("{unknown}"));
    }

    // ── AgentAdapter YAML ──

    #[test]

    #[test]
    fn builtin_adapters_are_present() {
        let adapters = builtin_adapters();
        assert!(adapters.iter().any(|a| a.name == "claude-sonnet"));
        assert!(adapters.iter().any(|a| a.name == "claude-opus"));
        assert!(adapters.iter().any(|a| a.name == "opencode"));
        assert!(adapters.iter().any(|a| a.name == "codex"));
        assert!(adapters.iter().any(|a| a.name == "aider"));
        assert!(adapters.iter().any(|a| a.name == "generic"));
    }

    #[test]
    fn builtin_claude_opus_config() {
        let adapter = builtin_claude_opus();
        assert_eq!(adapter.name, "claude-opus");
        assert_eq!(adapter.agent_cli, "claude");
        assert_eq!(adapter.model, Some("claude-opus-4-6".to_string()));
        assert_eq!(adapter.provider, Some("anthropic".to_string()));
        assert!(adapter.invoke_template.contains("claude-opus-4-6"));
        assert!(adapter.invoke_template.contains("--max-turns 50"));
        assert_eq!(adapter.timeout_secs, 7200);
        assert!(matches!(adapter.token_extraction, TokenExtraction::None));
    }

    #[test]
    fn builtin_opencode_config() {
        let adapter = builtin_opencode();
        assert_eq!(adapter.name, "opencode");
        assert_eq!(adapter.agent_cli, "opencode");
        assert!(matches!(adapter.input_method, InputMethod::File { .. }));
        assert!(adapter.invoke_template.contains("--prompt-file"));
        assert_eq!(adapter.token_extraction, TokenExtraction::None);
    }

    #[test]
    fn builtin_codex_config() {
        let adapter = builtin_codex();
        assert_eq!(adapter.name, "codex");
        assert_eq!(adapter.agent_cli, "codex");
        assert!(matches!(adapter.input_method, InputMethod::Args { .. }));
        assert!(adapter.invoke_template.contains("codex exec"));
        assert!(adapter
            .invoke_template
            .contains("--sandbox workspace-write"));
        assert!(adapter.invoke_template.contains("--json"));
        assert_eq!(adapter.model, Some("gpt-5.6-terra".to_string()));
        assert_eq!(adapter.provider, Some("openai".to_string()));
        assert_eq!(
            adapter.output_transform,
            Some("needle-transform-codex".to_string())
        );
    }

    #[test]
    fn builtin_aider_config() {
        let adapter = builtin_aider();
        assert_eq!(adapter.name, "aider");
        assert_eq!(adapter.agent_cli, "aider");
        assert!(adapter.invoke_template.contains("--yes --message"));
        assert_eq!(adapter.provider, Some("anthropic".to_string()));
        assert!(matches!(
            adapter.token_extraction,
            TokenExtraction::Regex { .. }
        ));
    }

    // ── Adapter loading ──

    #[test]
    fn load_adapters_includes_builtins() {
        let adapters =
            load_adapters(Path::new("/nonexistent/adapters"), &builtin_adapters()).unwrap();
        assert!(adapters.contains_key("claude-sonnet"));
        assert!(adapters.contains_key("generic"));
    }

    #[test]
    fn load_adapters_from_yaml_directory() {
        let dir = std::env::temp_dir().join("needle-adapter-load-test");
        let _ = std::fs::create_dir_all(&dir);
        let yaml = "name: test-agent\nagent_cli: test-bin\ninvoke_template: \"echo test\"\n";
        std::fs::write(dir.join("test-agent.yaml"), yaml).unwrap();

        let adapters = load_adapters(&dir, &builtin_adapters()).unwrap();
        assert!(adapters.contains_key("test-agent"));
        assert!(adapters.contains_key("claude-sonnet"));

        let _ = std::fs::remove_dir_all(&dir);
    }

    #[test]
    fn user_adapter_overrides_builtin() {
        let dir = std::env::temp_dir().join("needle-adapter-override-test");
        let _ = std::fs::create_dir_all(&dir);
        let yaml =
            "name: claude-sonnet\nagent_cli: claude-custom\ninvoke_template: \"custom {prompt_file}\"\n";
        std::fs::write(dir.join("claude-sonnet.yaml"), yaml).unwrap();

        let adapters = load_adapters(&dir, &builtin_adapters()).unwrap();
        let adapter = adapters.get("claude-sonnet").unwrap();
        assert_eq!(adapter.agent_cli, "claude-custom");

        let _ = std::fs::remove_dir_all(&dir);
    }

    // ── Temp file ──

    #[test]
    fn write_prompt_to_temp_creates_file() {
        let path = write_prompt_to_temp(&BeadId::from("needle-temp1"), "hello world").unwrap();
        assert!(path.exists());
        let content = std::fs::read_to_string(&path).unwrap();
        assert_eq!(content, "hello world");
        let _ = std::fs::remove_file(&path);
    }

    #[test]
    fn write_prompt_to_temp_uses_temp_dir() {
        let path = write_prompt_to_temp(&BeadId::from("needle-temp2"), "test").unwrap();
        let temp = std::env::temp_dir();
        assert!(path.starts_with(temp.join("needle")));
        let _ = std::fs::remove_file(&path);
    }

    // ── Dispatch integration tests ──

    #[tokio::test]
    async fn dispatch_echo_captures_stdout() {
        let mut adapters = HashMap::new();
        adapters.insert(
            "echo".to_string(),
            test_adapter("echo", "echo hello-needle"),
        );
        let dispatcher = test_dispatcher(adapters);
        let adapter = dispatcher.adapter("echo").unwrap().clone();

        let result = dispatcher
            .dispatch(
                &BeadId::from("nd-echo"),
                &test_prompt("test"),
                &adapter,
                Path::new("/tmp"),
            )
            .await
            .unwrap();

        assert_eq!(result.exit_code, 0);
        assert_eq!(result.stdout.trim(), "hello-needle");
        assert!(result.pid > 0);
    }

    #[tokio::test]
    async fn dispatch_captures_exit_code() {
        let mut adapters = HashMap::new();
        adapters.insert("fail".to_string(), test_adapter("fail", "exit 42"));
        let dispatcher = test_dispatcher(adapters);
        let adapter = dispatcher.adapter("fail").unwrap().clone();

        let result = dispatcher
            .dispatch(
                &BeadId::from("nd-exit"),
                &test_prompt("test"),
                &adapter,
                Path::new("/tmp"),
            )
            .await
            .unwrap();

        assert_eq!(result.exit_code, 42);
    }

    #[tokio::test]
    async fn dispatch_timeout_returns_124() {
        let mut adapters = HashMap::new();
        let mut adapter = test_adapter("slow", "sleep 100");
        adapter.timeout_secs = 1;
        adapters.insert("slow".to_string(), adapter);

        let dispatcher = test_dispatcher(adapters);
        let adapter = dispatcher.adapter("slow").unwrap().clone();

exec
/bin/bash -lc "git ls-files src/strand/resolve.rs; git diff --no-index /dev/null src/strand/resolve.rs | sed -n '1,80p'" in /home/coding/NEEDLE
 succeeded in 0ms:
diff --git a/src/strand/resolve.rs b/src/strand/resolve.rs
new file mode 100644
index 0000000..3a002a6
--- /dev/null
+++ b/src/strand/resolve.rs
@@ -0,0 +1,765 @@
+//! Resolve strand: post-Pluck decision analysis.
+//!
+//! Resolve evaluates beads after Pluck selection to determine whether they
+//! are ready for immediate dispatch, need to be blocked/retried, or should be
+//! split into smaller tasks. It uses an LLM prompt to analyze bead context and
+//! returns a structured decision with evidence.
+//!
+//! This is a decision-analysis layer only — it never mutates beads or performs
+//! implementation work. All actions are delegated to other strands.
+
+use crate::config::ResolveConfig;
+use crate::prompt::PromptBuilder;
+use crate::telemetry::Telemetry;
+use crate::types::{Bead, BeadId, ResolveDecision, ResolveOutcome, StrandError, StrandResult};
+use anyhow::{Context, Result};
+use serde::{Deserialize, Serialize};
+use std::collections::HashSet;
+use std::path::{Path, PathBuf};
+use std::process::{Command, Output, Stdio};
+use std::time::Duration;
+use tracing::info_span;
+
+/// Default template for the Resolve prompt.
+const DEFAULT_RESOLVE_TEMPLATE: &str = r#"## Resolve Analysis
+
+You are a decision analyst. Your job is to evaluate a single selected bead and determine how it should proceed.
+
+**IMPORTANT:**
+- You MUST output ONLY valid JSON (no markdown, no explanation)
+- You MUST NOT suggest any implementation work or code changes
+- You MUST NOT mutate the bead state or labels
+- You ONLY decide the disposition of this bead
+
+### Bead Context
+
+**ID:** {bead_id}
+**Title:** {bead_title}
+**Description:**
+{bead_body}
+
+**Priority:** {priority}
+**Status:** {status}
+**Assignee:** {assignee}
+**Labels:** {labels}
+
+**Workspace:** {workspace_path}
+
+**Dependencies ({dependency_count}):**
+{dependencies}
+
+**Dependents ({dependent_count}):**
+{dependents}
+
+### Recent Comments (if any)
+{bead_comments}
+
+### Your Task
+
+Analyze this bead and output ONE of these decisions:
+
+1. **complete** - The bead is ready for immediate dispatch and implementation
+2. **retry** - The bead cannot proceed right now but may succeed later (external dependency, resource contention)
+3. **blocked** - The bead has an unmet dependency that blocks all progress
+4. **split** - The bead is too large/complex and should be split into smaller child beads
+
+### Output Format
+
+Output ONLY a JSON object (no markdown fencing):
+
+```json
+{{
+  "decision": "complete|retry|blocked|split",
+  "evidence": "Brief explanation of the decision (1-2 sentences)",
+  "retry_after_seconds": 600,

exec
/bin/bash -lc "rg -n -C 8 \"resolve_adapter_name|dispatcher\\.adapter|dispatch\\(\" src --glob '*.rs'" in /home/coding/NEEDLE
 succeeded in 0ms:
src/dispatch/mod.rs-763-    /// default adapter name.
src/dispatch/mod.rs-764-    ///
src/dispatch/mod.rs-765-    /// # Arguments
src/dispatch/mod.rs-766-    /// * `model` - Model name (e.g., "sonnet", "claude-opus-4-8", "fable")
src/dispatch/mod.rs-767-    /// * `config` - Full config containing routing rules
src/dispatch/mod.rs-768-    ///
src/dispatch/mod.rs-769-    /// # Returns
src/dispatch/mod.rs-770-    /// The resolved adapter name (e.g., "claude-code-glm-4.7", "claude-sonnet")
src/dispatch/mod.rs:771:    pub fn resolve_adapter_name(&self, model: &str, config: &Config) -> String {
src/dispatch/mod.rs-772-        if let Some(ref routing) = config.agent.routing {
src/dispatch/mod.rs-773-            // Check each routing rule in order (first match wins)
src/dispatch/mod.rs-774-            for rule in &routing.rules {
src/dispatch/mod.rs-775-                match regex::Regex::new(&rule.match_model) {
src/dispatch/mod.rs-776-                    Ok(re) => {
src/dispatch/mod.rs-777-                        if re.is_match(model) {
src/dispatch/mod.rs-778-                            tracing::debug!(
src/dispatch/mod.rs-779-                                model = %model,
--
src/dispatch/mod.rs-832-            needle.bead.id = %bead_id.as_ref(),
src/dispatch/mod.rs-833-            gen_ai.system = %adapter.gen_ai_system(),
src/dispatch/mod.rs-834-            gen_ai.operation.name = "chat",
src/dispatch/mod.rs-835-            gen_ai.request.id = %bead_id.as_ref(),
src/dispatch/mod.rs-836-            needle.agent.pid = tracing::field::Empty,
src/dispatch/mod.rs-837-            needle.agent.exit_code = tracing::field::Empty,
src/dispatch/mod.rs-838-        )
src/dispatch/mod.rs-839-    )]
src/dispatch/mod.rs:840:    pub async fn dispatch(
src/dispatch/mod.rs-841-        &self,
src/dispatch/mod.rs-842-        bead_id: &BeadId,
src/dispatch/mod.rs-843-        prompt: &BuiltPrompt,
src/dispatch/mod.rs-844-        adapter: &AgentAdapter,
src/dispatch/mod.rs-845-        workspace: &Path,
src/dispatch/mod.rs-846-    ) -> Result<ExecutionResult> {
src/dispatch/mod.rs-847-        // Set gen_ai.request.model if available
src/dispatch/mod.rs-848-        if let Some(ref model) = adapter.model {
--
src/dispatch/mod.rs-2577-    #[tokio::test]
src/dispatch/mod.rs-2578-    async fn dispatch_echo_captures_stdout() {
src/dispatch/mod.rs-2579-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-2580-        adapters.insert(
src/dispatch/mod.rs-2581-            "echo".to_string(),
src/dispatch/mod.rs-2582-            test_adapter("echo", "echo hello-needle"),
src/dispatch/mod.rs-2583-        );
src/dispatch/mod.rs-2584-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:2585:        let adapter = dispatcher.adapter("echo").unwrap().clone();
src/dispatch/mod.rs-2586-
src/dispatch/mod.rs-2587-        let result = dispatcher
src/dispatch/mod.rs:2588:            .dispatch(
src/dispatch/mod.rs-2589-                &BeadId::from("nd-echo"),
src/dispatch/mod.rs-2590-                &test_prompt("test"),
src/dispatch/mod.rs-2591-                &adapter,
src/dispatch/mod.rs-2592-                Path::new("/tmp"),
src/dispatch/mod.rs-2593-            )
src/dispatch/mod.rs-2594-            .await
src/dispatch/mod.rs-2595-            .unwrap();
src/dispatch/mod.rs-2596-
--
src/dispatch/mod.rs-2599-        assert!(result.pid > 0);
src/dispatch/mod.rs-2600-    }
src/dispatch/mod.rs-2601-
src/dispatch/mod.rs-2602-    #[tokio::test]
src/dispatch/mod.rs-2603-    async fn dispatch_captures_exit_code() {
src/dispatch/mod.rs-2604-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-2605-        adapters.insert("fail".to_string(), test_adapter("fail", "exit 42"));
src/dispatch/mod.rs-2606-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:2607:        let adapter = dispatcher.adapter("fail").unwrap().clone();
src/dispatch/mod.rs-2608-
src/dispatch/mod.rs-2609-        let result = dispatcher
src/dispatch/mod.rs:2610:            .dispatch(
src/dispatch/mod.rs-2611-                &BeadId::from("nd-exit"),
src/dispatch/mod.rs-2612-                &test_prompt("test"),
src/dispatch/mod.rs-2613-                &adapter,
src/dispatch/mod.rs-2614-                Path::new("/tmp"),
src/dispatch/mod.rs-2615-            )
src/dispatch/mod.rs-2616-            .await
src/dispatch/mod.rs-2617-            .unwrap();
src/dispatch/mod.rs-2618-
--
src/dispatch/mod.rs-2622-    #[tokio::test]
src/dispatch/mod.rs-2623-    async fn dispatch_timeout_returns_124() {
src/dispatch/mod.rs-2624-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-2625-        let mut adapter = test_adapter("slow", "sleep 100");
src/dispatch/mod.rs-2626-        adapter.timeout_secs = 1;
src/dispatch/mod.rs-2627-        adapters.insert("slow".to_string(), adapter);
src/dispatch/mod.rs-2628-
src/dispatch/mod.rs-2629-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:2630:        let adapter = dispatcher.adapter("slow").unwrap().clone();
src/dispatch/mod.rs-2631-
src/dispatch/mod.rs-2632-        let result = dispatcher
src/dispatch/mod.rs:2633:            .dispatch(
src/dispatch/mod.rs-2634-                &BeadId::from("nd-timeout"),
src/dispatch/mod.rs-2635-                &test_prompt("test"),
src/dispatch/mod.rs-2636-                &adapter,
src/dispatch/mod.rs-2637-                Path::new("/tmp"),
src/dispatch/mod.rs-2638-            )
src/dispatch/mod.rs-2639-            .await
src/dispatch/mod.rs-2640-            .unwrap();
src/dispatch/mod.rs-2641-
--
src/dispatch/mod.rs-2647-    #[tokio::test]
src/dispatch/mod.rs-2648-    async fn dispatch_missing_binary_returns_127() {
src/dispatch/mod.rs-2649-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-2650-        adapters.insert(
src/dispatch/mod.rs-2651-            "missing".to_string(),
src/dispatch/mod.rs-2652-            test_adapter("missing", "nonexistent-binary-xyz-12345"),
src/dispatch/mod.rs-2653-        );
src/dispatch/mod.rs-2654-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:2655:        let adapter = dispatcher.adapter("missing").unwrap().clone();
src/dispatch/mod.rs-2656-
src/dispatch/mod.rs-2657-        let result = dispatcher
src/dispatch/mod.rs:2658:            .dispatch(
src/dispatch/mod.rs-2659-                &BeadId::from("nd-missing"),
src/dispatch/mod.rs-2660-                &test_prompt("test"),
src/dispatch/mod.rs-2661-                &adapter,
src/dispatch/mod.rs-2662-                Path::new("/tmp"),
src/dispatch/mod.rs-2663-            )
src/dispatch/mod.rs-2664-            .await
src/dispatch/mod.rs-2665-            .unwrap();
src/dispatch/mod.rs-2666-
--
src/dispatch/mod.rs-2673-        adapter.environment.insert(
src/dispatch/mod.rs-2674-            "NEEDLE_TEST_VAR".to_string(),
src/dispatch/mod.rs-2675-            "hello-from-needle".to_string(),
src/dispatch/mod.rs-2676-        );
src/dispatch/mod.rs-2677-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-2678-        adapters.insert("env".to_string(), adapter);
src/dispatch/mod.rs-2679-
src/dispatch/mod.rs-2680-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:2681:        let adapter = dispatcher.adapter("env").unwrap().clone();
src/dispatch/mod.rs-2682-
src/dispatch/mod.rs-2683-        let result = dispatcher
src/dispatch/mod.rs:2684:            .dispatch(
src/dispatch/mod.rs-2685-                &BeadId::from("nd-env"),
src/dispatch/mod.rs-2686-                &test_prompt("test"),
src/dispatch/mod.rs-2687-                &adapter,
src/dispatch/mod.rs-2688-                Path::new("/tmp"),
src/dispatch/mod.rs-2689-            )
src/dispatch/mod.rs-2690-            .await
src/dispatch/mod.rs-2691-            .unwrap();
src/dispatch/mod.rs-2692-
--
src/dispatch/mod.rs-2697-    #[tokio::test]
src/dispatch/mod.rs-2698-    async fn dispatch_stdin_redirect_from_prompt_file() {
src/dispatch/mod.rs-2699-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-2700-        adapters.insert(
src/dispatch/mod.rs-2701-            "cat".to_string(),
src/dispatch/mod.rs-2702-            test_adapter("cat", "cat < {prompt_file}"),
src/dispatch/mod.rs-2703-        );
src/dispatch/mod.rs-2704-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:2705:        let adapter = dispatcher.adapter("cat").unwrap().clone();
src/dispatch/mod.rs-2706-
src/dispatch/mod.rs-2707-        let result = dispatcher
src/dispatch/mod.rs:2708:            .dispatch(
src/dispatch/mod.rs-2709-                &BeadId::from("nd-stdin"),
src/dispatch/mod.rs-2710-                &test_prompt("prompt-content-here"),
src/dispatch/mod.rs-2711-                &adapter,
src/dispatch/mod.rs-2712-                Path::new("/tmp"),
src/dispatch/mod.rs-2713-            )
src/dispatch/mod.rs-2714-            .await
src/dispatch/mod.rs-2715-            .unwrap();
src/dispatch/mod.rs-2716-
--
src/dispatch/mod.rs-2719-    }
src/dispatch/mod.rs-2720-
src/dispatch/mod.rs-2721-    #[tokio::test]
src/dispatch/mod.rs-2722-    async fn dispatch_cleans_up_temp_file() {
src/dispatch/mod.rs-2723-        let bead_id = BeadId::from("nd-cleanup");
src/dispatch/mod.rs-2724-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-2725-        adapters.insert("true".to_string(), test_adapter("true", "true"));
src/dispatch/mod.rs-2726-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:2727:        let adapter = dispatcher.adapter("true").unwrap().clone();
src/dispatch/mod.rs-2728-
src/dispatch/mod.rs-2729-        let _ = dispatcher
src/dispatch/mod.rs:2730:            .dispatch(
src/dispatch/mod.rs-2731-                &bead_id,
src/dispatch/mod.rs-2732-                &test_prompt("cleanup test"),
src/dispatch/mod.rs-2733-                &adapter,
src/dispatch/mod.rs-2734-                Path::new("/tmp"),
src/dispatch/mod.rs-2735-            )
src/dispatch/mod.rs-2736-            .await
src/dispatch/mod.rs-2737-            .unwrap();
src/dispatch/mod.rs-2738-
--
src/dispatch/mod.rs-2745-        assert!(!expected_path.exists(), "temp file should be cleaned up");
src/dispatch/mod.rs-2746-    }
src/dispatch/mod.rs-2747-
src/dispatch/mod.rs-2748-    #[tokio::test]
src/dispatch/mod.rs-2749-    async fn dispatch_template_renders_bead_id() {
src/dispatch/mod.rs-2750-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-2751-        adapters.insert("id".to_string(), test_adapter("id", "echo bead={bead_id}"));
src/dispatch/mod.rs-2752-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:2753:        let adapter = dispatcher.adapter("id").unwrap().clone();
src/dispatch/mod.rs-2754-
src/dispatch/mod.rs-2755-        let result = dispatcher
src/dispatch/mod.rs:2756:            .dispatch(
src/dispatch/mod.rs-2757-                &BeadId::from("needle-xyz"),
src/dispatch/mod.rs-2758-                &test_prompt("test"),
src/dispatch/mod.rs-2759-                &adapter,
src/dispatch/mod.rs-2760-                Path::new("/tmp"),
src/dispatch/mod.rs-2761-            )
src/dispatch/mod.rs-2762-            .await
src/dispatch/mod.rs-2763-            .unwrap();
src/dispatch/mod.rs-2764-
--
src/dispatch/mod.rs-2769-    #[tokio::test]
src/dispatch/mod.rs-2770-    async fn dispatch_captures_stderr() {
src/dispatch/mod.rs-2771-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-2772-        adapters.insert(
src/dispatch/mod.rs-2773-            "err".to_string(),
src/dispatch/mod.rs-2774-            test_adapter("err", "echo error-output >&2"),
src/dispatch/mod.rs-2775-        );
src/dispatch/mod.rs-2776-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:2777:        let adapter = dispatcher.adapter("err").unwrap().clone();
src/dispatch/mod.rs-2778-
src/dispatch/mod.rs-2779-        let result = dispatcher
src/dispatch/mod.rs:2780:            .dispatch(
src/dispatch/mod.rs-2781-                &BeadId::from("nd-stderr"),
src/dispatch/mod.rs-2782-                &test_prompt("test"),
src/dispatch/mod.rs-2783-                &adapter,
src/dispatch/mod.rs-2784-                Path::new("/tmp"),
src/dispatch/mod.rs-2785-            )
src/dispatch/mod.rs-2786-            .await
src/dispatch/mod.rs-2787-            .unwrap();
src/dispatch/mod.rs-2788-
--
src/dispatch/mod.rs-2951-            "vars",
src/dispatch/mod.rs-2952-            "echo ws={workspace} pf={prompt_file} bid={bead_id} m={model}",
src/dispatch/mod.rs-2953-        );
src/dispatch/mod.rs-2954-        adapter.model = Some("test-model-v1".to_string());
src/dispatch/mod.rs-2955-
src/dispatch/mod.rs-2956-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-2957-        adapters.insert("vars".to_string(), adapter);
src/dispatch/mod.rs-2958-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:2959:        let adapter = dispatcher.adapter("vars").unwrap().clone();
src/dispatch/mod.rs-2960-
src/dispatch/mod.rs-2961-        let workspace = std::env::temp_dir().join("needle-e2e-vars");
src/dispatch/mod.rs-2962-        let _ = std::fs::create_dir_all(&workspace);
src/dispatch/mod.rs-2963-
src/dispatch/mod.rs-2964-        let result = dispatcher
src/dispatch/mod.rs:2965:            .dispatch(
src/dispatch/mod.rs-2966-                &BeadId::from("needle-tmpl"),
src/dispatch/mod.rs-2967-                &test_prompt("irrelevant"),
src/dispatch/mod.rs-2968-                &adapter,
src/dispatch/mod.rs-2969-                &workspace,
src/dispatch/mod.rs-2970-            )
src/dispatch/mod.rs-2971-            .await
src/dispatch/mod.rs-2972-            .unwrap();
src/dispatch/mod.rs-2973-
--
src/dispatch/mod.rs-3010-            .insert("NDL_B".to_string(), "beta".to_string());
src/dispatch/mod.rs-3011-        adapter
src/dispatch/mod.rs-3012-            .environment
src/dispatch/mod.rs-3013-            .insert("NDL_C".to_string(), "gamma".to_string());
src/dispatch/mod.rs-3014-
src/dispatch/mod.rs-3015-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3016-        adapters.insert("multienv".to_string(), adapter);
src/dispatch/mod.rs-3017-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3018:        let adapter = dispatcher.adapter("multienv").unwrap().clone();
src/dispatch/mod.rs-3019-
src/dispatch/mod.rs-3020-        let result = dispatcher
src/dispatch/mod.rs:3021:            .dispatch(
src/dispatch/mod.rs-3022-                &BeadId::from("nd-env-multi"),
src/dispatch/mod.rs-3023-                &test_prompt("test"),
src/dispatch/mod.rs-3024-                &adapter,
src/dispatch/mod.rs-3025-                Path::new("/tmp"),
src/dispatch/mod.rs-3026-            )
src/dispatch/mod.rs-3027-            .await
src/dispatch/mod.rs-3028-            .unwrap();
src/dispatch/mod.rs-3029-
--
src/dispatch/mod.rs-3039-            "Hello $USER\nLine with `backticks`\nQuotes: 'single' \"double\"\nBackslash: \\\nDollar: $(echo injected)";
src/dispatch/mod.rs-3040-
src/dispatch/mod.rs-3041-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3042-        adapters.insert(
src/dispatch/mod.rs-3043-            "catprompt".to_string(),
src/dispatch/mod.rs-3044-            test_adapter("catprompt", "cat {prompt_file}"),
src/dispatch/mod.rs-3045-        );
src/dispatch/mod.rs-3046-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3047:        let adapter = dispatcher.adapter("catprompt").unwrap().clone();
src/dispatch/mod.rs-3048-
src/dispatch/mod.rs-3049-        let result = dispatcher
src/dispatch/mod.rs:3050:            .dispatch(
src/dispatch/mod.rs-3051-                &BeadId::from("nd-meta"),
src/dispatch/mod.rs-3052-                &test_prompt(dangerous_prompt),
src/dispatch/mod.rs-3053-                &adapter,
src/dispatch/mod.rs-3054-                Path::new("/tmp"),
src/dispatch/mod.rs-3055-            )
src/dispatch/mod.rs-3056-            .await
src/dispatch/mod.rs-3057-            .unwrap();
src/dispatch/mod.rs-3058-
--
src/dispatch/mod.rs-3085-        let multiline = "line one\nline two\nline three";
src/dispatch/mod.rs-3086-
src/dispatch/mod.rs-3087-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3088-        adapters.insert(
src/dispatch/mod.rs-3089-            "wc".to_string(),
src/dispatch/mod.rs-3090-            test_adapter("wc", "wc -l < {prompt_file}"),
src/dispatch/mod.rs-3091-        );
src/dispatch/mod.rs-3092-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3093:        let adapter = dispatcher.adapter("wc").unwrap().clone();
src/dispatch/mod.rs-3094-
src/dispatch/mod.rs-3095-        let result = dispatcher
src/dispatch/mod.rs:3096:            .dispatch(
src/dispatch/mod.rs-3097-                &BeadId::from("nd-newlines"),
src/dispatch/mod.rs-3098-                &test_prompt(multiline),
src/dispatch/mod.rs-3099-                &adapter,
src/dispatch/mod.rs-3100-                Path::new("/tmp"),
src/dispatch/mod.rs-3101-            )
src/dispatch/mod.rs-3102-            .await
src/dispatch/mod.rs-3103-            .unwrap();
src/dispatch/mod.rs-3104-
--
src/dispatch/mod.rs-3111-
src/dispatch/mod.rs-3112-    #[tokio::test]
src/dispatch/mod.rs-3113-    async fn e2e_exit_code_0_is_success() {
src/dispatch/mod.rs-3114-        use crate::types::Outcome;
src/dispatch/mod.rs-3115-
src/dispatch/mod.rs-3116-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3117-        adapters.insert("ok".to_string(), test_adapter("ok", "exit 0"));
src/dispatch/mod.rs-3118-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3119:        let adapter = dispatcher.adapter("ok").unwrap().clone();
src/dispatch/mod.rs-3120-
src/dispatch/mod.rs-3121-        let result = dispatcher
src/dispatch/mod.rs:3122:            .dispatch(
src/dispatch/mod.rs-3123-                &BeadId::from("nd-exit0"),
src/dispatch/mod.rs-3124-                &test_prompt("t"),
src/dispatch/mod.rs-3125-                &adapter,
src/dispatch/mod.rs-3126-                Path::new("/tmp"),
src/dispatch/mod.rs-3127-            )
src/dispatch/mod.rs-3128-            .await
src/dispatch/mod.rs-3129-            .unwrap();
src/dispatch/mod.rs-3130-
--
src/dispatch/mod.rs-3134-
src/dispatch/mod.rs-3135-    #[tokio::test]
src/dispatch/mod.rs-3136-    async fn e2e_exit_code_1_is_failure() {
src/dispatch/mod.rs-3137-        use crate::types::Outcome;
src/dispatch/mod.rs-3138-
src/dispatch/mod.rs-3139-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3140-        adapters.insert("f1".to_string(), test_adapter("f1", "exit 1"));
src/dispatch/mod.rs-3141-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3142:        let adapter = dispatcher.adapter("f1").unwrap().clone();
src/dispatch/mod.rs-3143-
src/dispatch/mod.rs-3144-        let result = dispatcher
src/dispatch/mod.rs:3145:            .dispatch(
src/dispatch/mod.rs-3146-                &BeadId::from("nd-exit1"),
src/dispatch/mod.rs-3147-                &test_prompt("t"),
src/dispatch/mod.rs-3148-                &adapter,
src/dispatch/mod.rs-3149-                Path::new("/tmp"),
src/dispatch/mod.rs-3150-            )
src/dispatch/mod.rs-3151-            .await
src/dispatch/mod.rs-3152-            .unwrap();
src/dispatch/mod.rs-3153-
--
src/dispatch/mod.rs-3157-
src/dispatch/mod.rs-3158-    #[tokio::test]
src/dispatch/mod.rs-3159-    async fn e2e_exit_code_2_is_failure() {
src/dispatch/mod.rs-3160-        use crate::types::Outcome;
src/dispatch/mod.rs-3161-
src/dispatch/mod.rs-3162-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3163-        adapters.insert("f2".to_string(), test_adapter("f2", "exit 2"));
src/dispatch/mod.rs-3164-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3165:        let adapter = dispatcher.adapter("f2").unwrap().clone();
src/dispatch/mod.rs-3166-
src/dispatch/mod.rs-3167-        let result = dispatcher
src/dispatch/mod.rs:3168:            .dispatch(
src/dispatch/mod.rs-3169-                &BeadId::from("nd-exit2"),
src/dispatch/mod.rs-3170-                &test_prompt("t"),
src/dispatch/mod.rs-3171-                &adapter,
src/dispatch/mod.rs-3172-                Path::new("/tmp"),
src/dispatch/mod.rs-3173-            )
src/dispatch/mod.rs-3174-            .await
src/dispatch/mod.rs-3175-            .unwrap();
src/dispatch/mod.rs-3176-
--
src/dispatch/mod.rs-3180-
src/dispatch/mod.rs-3181-    #[tokio::test]
src/dispatch/mod.rs-3182-    async fn e2e_exit_code_137_is_crash() {
src/dispatch/mod.rs-3183-        use crate::types::Outcome;
src/dispatch/mod.rs-3184-
src/dispatch/mod.rs-3185-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3186-        adapters.insert("crash".to_string(), test_adapter("crash", "exit 137"));
src/dispatch/mod.rs-3187-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3188:        let adapter = dispatcher.adapter("crash").unwrap().clone();
src/dispatch/mod.rs-3189-
src/dispatch/mod.rs-3190-        let result = dispatcher
src/dispatch/mod.rs:3191:            .dispatch(
src/dispatch/mod.rs-3192-                &BeadId::from("nd-exit137"),
src/dispatch/mod.rs-3193-                &test_prompt("t"),
src/dispatch/mod.rs-3194-                &adapter,
src/dispatch/mod.rs-3195-                Path::new("/tmp"),
src/dispatch/mod.rs-3196-            )
src/dispatch/mod.rs-3197-            .await
src/dispatch/mod.rs-3198-            .unwrap();
src/dispatch/mod.rs-3199-
--
src/dispatch/mod.rs-3207-    #[tokio::test]
src/dispatch/mod.rs-3208-    async fn e2e_timeout_kills_agent_returns_124() {
src/dispatch/mod.rs-3209-        let mut adapter = test_adapter("sleeper", "sleep 100");
src/dispatch/mod.rs-3210-        adapter.timeout_secs = 1;
src/dispatch/mod.rs-3211-
src/dispatch/mod.rs-3212-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3213-        adapters.insert("sleeper".to_string(), adapter);
src/dispatch/mod.rs-3214-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3215:        let adapter = dispatcher.adapter("sleeper").unwrap().clone();
src/dispatch/mod.rs-3216-
src/dispatch/mod.rs-3217-        let start = Instant::now();
src/dispatch/mod.rs-3218-        let result = dispatcher
src/dispatch/mod.rs:3219:            .dispatch(
src/dispatch/mod.rs-3220-                &BeadId::from("nd-timeout"),
src/dispatch/mod.rs-3221-                &test_prompt("t"),
src/dispatch/mod.rs-3222-                &adapter,
src/dispatch/mod.rs-3223-                Path::new("/tmp"),
src/dispatch/mod.rs-3224-            )
src/dispatch/mod.rs-3225-            .await
src/dispatch/mod.rs-3226-            .unwrap();
src/dispatch/mod.rs-3227-        let wall = start.elapsed();
--
src/dispatch/mod.rs-3252-        let cmd = format!("sleep 1000 & echo $! > {pid_file_str}; sleep 1000");
src/dispatch/mod.rs-3253-
src/dispatch/mod.rs-3254-        let mut adapter = test_adapter("pgkill", &cmd);
src/dispatch/mod.rs-3255-        adapter.timeout_secs = 2;
src/dispatch/mod.rs-3256-
src/dispatch/mod.rs-3257-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3258-        adapters.insert("pgkill".to_string(), adapter);
src/dispatch/mod.rs-3259-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3260:        let adapter = dispatcher.adapter("pgkill").unwrap().clone();
src/dispatch/mod.rs-3261-
src/dispatch/mod.rs-3262-        let result = dispatcher
src/dispatch/mod.rs:3263:            .dispatch(
src/dispatch/mod.rs-3264-                &BeadId::from("nd-pgkill"),
src/dispatch/mod.rs-3265-                &test_prompt("t"),
src/dispatch/mod.rs-3266-                &adapter,
src/dispatch/mod.rs-3267-                Path::new("/tmp"),
src/dispatch/mod.rs-3268-            )
src/dispatch/mod.rs-3269-            .await
src/dispatch/mod.rs-3270-            .unwrap();
src/dispatch/mod.rs-3271-
--
src/dispatch/mod.rs-3300-
src/dispatch/mod.rs-3301-        let _ = std::fs::remove_file(&pid_file);
src/dispatch/mod.rs-3302-    }
src/dispatch/mod.rs-3303-
src/dispatch/mod.rs-3304-    #[tokio::test]
src/dispatch/mod.rs-3305-    async fn e2e_outer_cancellation_still_kills_process_group() {
src/dispatch/mod.rs-3306-        // Regression test for bf-653n7 (the mitosis-evaluation-timeout leak).
src/dispatch/mod.rs-3307-        //
src/dispatch/mod.rs:3308:        // Worker's mitosis-evaluation step wraps the *entire* dispatch() call
src/dispatch/mod.rs-3309-        // in its own, much shorter, `tokio::time::timeout` — separate from
src/dispatch/mod.rs-3310-        // and unrelated to the agent's own configured timeout exercised by
src/dispatch/mod.rs-3311-        // `e2e_timeout_kills_entire_process_group` above. Before
src/dispatch/mod.rs-3312-        // ProcessGroupKillGuard, that outer timeout firing dropped the
src/dispatch/mod.rs:3313:        // in-flight dispatch() future before its *internal* timeout-kill
src/dispatch/mod.rs-3314-        // match ever ran, silently orphaning the agent process and any
src/dispatch/mod.rs-3315-        // process-group children it had spawned — indefinitely, since
src/dispatch/mod.rs-3316-        // nothing ever reaped them.
src/dispatch/mod.rs-3317-        //
src/dispatch/mod.rs-3318-        // Here the adapter's own timeout is set effectively unreachable
src/dispatch/mod.rs-3319-        // within the test's window, so the only thing that can kill the
src/dispatch/mod.rs-3320-        // process is the guard reacting to the *outer* future being dropped.
src/dispatch/mod.rs-3321-        let pid_file =
--
src/dispatch/mod.rs-3324-
src/dispatch/mod.rs-3325-        let cmd = format!("sleep 1000 & echo $! > {pid_file_str}; sleep 1000");
src/dispatch/mod.rs-3326-        let mut adapter = test_adapter("outercancel", &cmd);
src/dispatch/mod.rs-3327-        adapter.timeout_secs = 3600;
src/dispatch/mod.rs-3328-
src/dispatch/mod.rs-3329-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3330-        adapters.insert("outercancel".to_string(), adapter);
src/dispatch/mod.rs-3331-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3332:        let adapter = dispatcher.adapter("outercancel").unwrap().clone();
src/dispatch/mod.rs-3333-
src/dispatch/mod.rs-3334-        // Mimic Worker's mitosis-evaluation wrapper: an outer timeout, far
src/dispatch/mod.rs-3335-        // shorter than the agent's own, wrapping the whole dispatch call.
src/dispatch/mod.rs-3336-        let outer = tokio::time::timeout(
src/dispatch/mod.rs-3337-            Duration::from_millis(500),
src/dispatch/mod.rs:3338:            dispatcher.dispatch(
src/dispatch/mod.rs-3339-                &BeadId::from("nd-outercancel"),
src/dispatch/mod.rs-3340-                &test_prompt("t"),
src/dispatch/mod.rs-3341-                &adapter,
src/dispatch/mod.rs-3342-                Path::new("/tmp"),
src/dispatch/mod.rs-3343-            ),
src/dispatch/mod.rs-3344-        )
src/dispatch/mod.rs-3345-        .await;
src/dispatch/mod.rs-3346-        assert!(
--
src/dispatch/mod.rs-3365-            if std::time::Instant::now() >= deadline {
src/dispatch/mod.rs-3366-                break false;
src/dispatch/mod.rs-3367-            }
src/dispatch/mod.rs-3368-            tokio::time::sleep(Duration::from_millis(100)).await;
src/dispatch/mod.rs-3369-        };
src/dispatch/mod.rs-3370-        assert!(
src/dispatch/mod.rs-3371-            dead,
src/dispatch/mod.rs-3372-            "grandchild sleep (pid {grandchild_pid}) should be dead within 3s of the *outer* \
src/dispatch/mod.rs:3373:             future being dropped, even though dispatch()'s own internal timeout never fired \
src/dispatch/mod.rs-3374-             — this is what ProcessGroupKillGuard exists to guarantee"
src/dispatch/mod.rs-3375-        );
src/dispatch/mod.rs-3376-
src/dispatch/mod.rs-3377-        let _ = std::fs::remove_file(&pid_file);
src/dispatch/mod.rs-3378-    }
src/dispatch/mod.rs-3379-
src/dispatch/mod.rs-3380-    #[tokio::test]
src/dispatch/mod.rs-3381-    async fn e2e_json_output_capture_and_token_extraction() {
--
src/dispatch/mod.rs-3388-        adapter.token_extraction = TokenExtraction::JsonField {
src/dispatch/mod.rs-3389-            input_path: "usage.input_tokens".to_string(),
src/dispatch/mod.rs-3390-            output_path: "usage.output_tokens".to_string(),
src/dispatch/mod.rs-3391-        };
src/dispatch/mod.rs-3392-
src/dispatch/mod.rs-3393-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3394-        adapters.insert("json-agent".to_string(), adapter);
src/dispatch/mod.rs-3395-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3396:        let adapter = dispatcher.adapter("json-agent").unwrap().clone();
src/dispatch/mod.rs-3397-
src/dispatch/mod.rs-3398-        let result = dispatcher
src/dispatch/mod.rs:3399:            .dispatch(
src/dispatch/mod.rs-3400-                &BeadId::from("nd-json"),
src/dispatch/mod.rs-3401-                &test_prompt("t"),
src/dispatch/mod.rs-3402-                &adapter,
src/dispatch/mod.rs-3403-                Path::new("/tmp"),
src/dispatch/mod.rs-3404-            )
src/dispatch/mod.rs-3405-            .await
src/dispatch/mod.rs-3406-            .unwrap();
src/dispatch/mod.rs-3407-
--
src/dispatch/mod.rs-3427-        );
src/dispatch/mod.rs-3428-        adapter
src/dispatch/mod.rs-3429-            .environment
src/dispatch/mod.rs-3430-            .insert("CUSTOM_FLAG".to_string(), "enabled".to_string());
src/dispatch/mod.rs-3431-
src/dispatch/mod.rs-3432-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3433-        adapters.insert("custom-env".to_string(), adapter);
src/dispatch/mod.rs-3434-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3435:        let adapter = dispatcher.adapter("custom-env").unwrap().clone();
src/dispatch/mod.rs-3436-
src/dispatch/mod.rs-3437-        let result = dispatcher
src/dispatch/mod.rs:3438:            .dispatch(
src/dispatch/mod.rs-3439-                &BeadId::from("nd-baseurl"),
src/dispatch/mod.rs-3440-                &test_prompt("t"),
src/dispatch/mod.rs-3441-                &adapter,
src/dispatch/mod.rs-3442-                Path::new("/tmp"),
src/dispatch/mod.rs-3443-            )
src/dispatch/mod.rs-3444-            .await
src/dispatch/mod.rs-3445-            .unwrap();
src/dispatch/mod.rs-3446-
--
src/dispatch/mod.rs-3464-        let _ = std::fs::create_dir_all(&workspace);
src/dispatch/mod.rs-3465-
src/dispatch/mod.rs-3466-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3467-        adapters.insert(
src/dispatch/mod.rs-3468-            "pwd".to_string(),
src/dispatch/mod.rs-3469-            test_adapter("pwd", "cd {workspace} && pwd"),
src/dispatch/mod.rs-3470-        );
src/dispatch/mod.rs-3471-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3472:        let adapter = dispatcher.adapter("pwd").unwrap().clone();
src/dispatch/mod.rs-3473-
src/dispatch/mod.rs-3474-        let result = dispatcher
src/dispatch/mod.rs:3475:            .dispatch(
src/dispatch/mod.rs-3476-                &BeadId::from("nd-wsdir"),
src/dispatch/mod.rs-3477-                &test_prompt("t"),
src/dispatch/mod.rs-3478-                &adapter,
src/dispatch/mod.rs-3479-                &workspace,
src/dispatch/mod.rs-3480-            )
src/dispatch/mod.rs-3481-            .await
src/dispatch/mod.rs-3482-            .unwrap();
src/dispatch/mod.rs-3483-
--
src/dispatch/mod.rs-3699-            "for i in $(seq 1 10); do echo -n .; sleep 0.2; done; sleep 0.1",
src/dispatch/mod.rs-3700-        );
src/dispatch/mod.rs-3701-        adapter.idle_timeout_secs = 1; // 1 second idle timeout
src/dispatch/mod.rs-3702-        adapter.hard_timeout_secs = 10; // 10 second hard timeout (should not fire)
src/dispatch/mod.rs-3703-
src/dispatch/mod.rs-3704-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3705-        adapters.insert("chatty-stdout".to_string(), adapter);
src/dispatch/mod.rs-3706-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3707:        let adapter = dispatcher.adapter("chatty-stdout").unwrap().clone();
src/dispatch/mod.rs-3708-
src/dispatch/mod.rs-3709-        let result = dispatcher
src/dispatch/mod.rs:3710:            .dispatch(
src/dispatch/mod.rs-3711-                &BeadId::from("nd-activity-stdout"),
src/dispatch/mod.rs-3712-                &test_prompt("t"),
src/dispatch/mod.rs-3713-                &adapter,
src/dispatch/mod.rs-3714-                Path::new("/tmp"),
src/dispatch/mod.rs-3715-            )
src/dispatch/mod.rs-3716-            .await
src/dispatch/mod.rs-3717-            .unwrap();
src/dispatch/mod.rs-3718-
--
src/dispatch/mod.rs-3740-            "for i in $(seq 1 10); do echo -n . >&2; sleep 0.2; done; sleep 0.1",
src/dispatch/mod.rs-3741-        );
src/dispatch/mod.rs-3742-        adapter.idle_timeout_secs = 1;
src/dispatch/mod.rs-3743-        adapter.hard_timeout_secs = 10;
src/dispatch/mod.rs-3744-
src/dispatch/mod.rs-3745-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3746-        adapters.insert("chatty-stderr".to_string(), adapter);
src/dispatch/mod.rs-3747-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3748:        let adapter = dispatcher.adapter("chatty-stderr").unwrap().clone();
src/dispatch/mod.rs-3749-
src/dispatch/mod.rs-3750-        let result = dispatcher
src/dispatch/mod.rs:3751:            .dispatch(
src/dispatch/mod.rs-3752-                &BeadId::from("nd-activity-stderr"),
src/dispatch/mod.rs-3753-                &test_prompt("t"),
src/dispatch/mod.rs-3754-                &adapter,
src/dispatch/mod.rs-3755-                Path::new("/tmp"),
src/dispatch/mod.rs-3756-            )
src/dispatch/mod.rs-3757-            .await
src/dispatch/mod.rs-3758-            .unwrap();
src/dispatch/mod.rs-3759-
--
src/dispatch/mod.rs-3778-            "for i in $(seq 1 20); do printf '\\x00\\x01\\x02\\x03\\x04\\x05\\x06\\x07\\x08\\x09\\n'; sleep 0.15; done",
src/dispatch/mod.rs-3779-        );
src/dispatch/mod.rs-3780-        adapter.idle_timeout_secs = 1;
src/dispatch/mod.rs-3781-        adapter.hard_timeout_secs = 10;
src/dispatch/mod.rs-3782-
src/dispatch/mod.rs-3783-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3784-        adapters.insert("binary-output".to_string(), adapter);
src/dispatch/mod.rs-3785-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3786:        let adapter = dispatcher.adapter("binary-output").unwrap().clone();
src/dispatch/mod.rs-3787-
src/dispatch/mod.rs-3788-        let result = dispatcher
src/dispatch/mod.rs:3789:            .dispatch(
src/dispatch/mod.rs-3790-                &BeadId::from("nd-activity-binary"),
src/dispatch/mod.rs-3791-                &test_prompt("t"),
src/dispatch/mod.rs-3792-                &adapter,
src/dispatch/mod.rs-3793-                Path::new("/tmp"),
src/dispatch/mod.rs-3794-            )
src/dispatch/mod.rs-3795-            .await
src/dispatch/mod.rs-3796-            .unwrap();
src/dispatch/mod.rs-3797-
--
src/dispatch/mod.rs-3816-            "printf '%0.s#' {1..1000}; sleep 0.2; echo done",
src/dispatch/mod.rs-3817-        );
src/dispatch/mod.rs-3818-        adapter.idle_timeout_secs = 1;
src/dispatch/mod.rs-3819-        adapter.hard_timeout_secs = 10;
src/dispatch/mod.rs-3820-
src/dispatch/mod.rs-3821-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3822-        adapters.insert("no-newlines".to_string(), adapter);
src/dispatch/mod.rs-3823-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3824:        let adapter = dispatcher.adapter("no-newlines").unwrap().clone();
src/dispatch/mod.rs-3825-
src/dispatch/mod.rs-3826-        let result = dispatcher
src/dispatch/mod.rs:3827:            .dispatch(
src/dispatch/mod.rs-3828-                &BeadId::from("nd-activity-nonewline"),
src/dispatch/mod.rs-3829-                &test_prompt("t"),
src/dispatch/mod.rs-3830-                &adapter,
src/dispatch/mod.rs-3831-                Path::new("/tmp"),
src/dispatch/mod.rs-3832-            )
src/dispatch/mod.rs-3833-            .await
src/dispatch/mod.rs-3834-            .unwrap();
src/dispatch/mod.rs-3835-
--
src/dispatch/mod.rs-3850-            "for i in $(seq 1 8); do echo -n out >&1; echo -n err >&2; sleep 0.18; done; echo done",
src/dispatch/mod.rs-3851-        );
src/dispatch/mod.rs-3852-        adapter.idle_timeout_secs = 1;
src/dispatch/mod.rs-3853-        adapter.hard_timeout_secs = 10;
src/dispatch/mod.rs-3854-
src/dispatch/mod.rs-3855-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3856-        adapters.insert("mixed-streams".to_string(), adapter);
src/dispatch/mod.rs-3857-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3858:        let adapter = dispatcher.adapter("mixed-streams").unwrap().clone();
src/dispatch/mod.rs-3859-
src/dispatch/mod.rs-3860-        let result = dispatcher
src/dispatch/mod.rs:3861:            .dispatch(
src/dispatch/mod.rs-3862-                &BeadId::from("nd-activity-mixed"),
src/dispatch/mod.rs-3863-                &test_prompt("t"),
src/dispatch/mod.rs-3864-                &adapter,
src/dispatch/mod.rs-3865-                Path::new("/tmp"),
src/dispatch/mod.rs-3866-            )
src/dispatch/mod.rs-3867-            .await
src/dispatch/mod.rs-3868-            .unwrap();
src/dispatch/mod.rs-3869-
--
src/dispatch/mod.rs-3887-            "sleep 5",
src/dispatch/mod.rs-3888-        );
src/dispatch/mod.rs-3889-        adapter.timeout_secs = 0; // Use new timeout mode, not legacy
src/dispatch/mod.rs-3890-        adapter.idle_timeout_secs = 1; // Should fire after 1s of silence
src/dispatch/mod.rs-3891-
src/dispatch/mod.rs-3892-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3893-        adapters.insert("silent-process".to_string(), adapter);
src/dispatch/mod.rs-3894-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3895:        let adapter = dispatcher.adapter("silent-process").unwrap().clone();
src/dispatch/mod.rs-3896-
src/dispatch/mod.rs-3897-        let start = Instant::now();
src/dispatch/mod.rs-3898-        let result = dispatcher
src/dispatch/mod.rs:3899:            .dispatch(
src/dispatch/mod.rs-3900-                &BeadId::from("nd-idle-timeout"),
src/dispatch/mod.rs-3901-                &test_prompt("t"),
src/dispatch/mod.rs-3902-                &adapter,
src/dispatch/mod.rs-3903-                Path::new("/tmp"),
src/dispatch/mod.rs-3904-            )
src/dispatch/mod.rs-3905-            .await
src/dispatch/mod.rs-3906-            .unwrap();
src/dispatch/mod.rs-3907-        let elapsed = start.elapsed();
--
src/dispatch/mod.rs-3938-            "for i in $(seq 1 15); do echo -n x; sleep 0.12; done; echo",
src/dispatch/mod.rs-3939-        );
src/dispatch/mod.rs-3940-        adapter.idle_timeout_secs = 1;
src/dispatch/mod.rs-3941-        adapter.hard_timeout_secs = 10;
src/dispatch/mod.rs-3942-
src/dispatch/mod.rs-3943-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3944-        adapters.insert("small-chunks".to_string(), adapter);
src/dispatch/mod.rs-3945-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3946:        let adapter = dispatcher.adapter("small-chunks").unwrap().clone();
src/dispatch/mod.rs-3947-
src/dispatch/mod.rs-3948-        let result = dispatcher
src/dispatch/mod.rs:3949:            .dispatch(
src/dispatch/mod.rs-3950-                &BeadId::from("nd-activity-chunks"),
src/dispatch/mod.rs-3951-                &test_prompt("t"),
src/dispatch/mod.rs-3952-                &adapter,
src/dispatch/mod.rs-3953-                Path::new("/tmp"),
src/dispatch/mod.rs-3954-            )
src/dispatch/mod.rs-3955-            .await
src/dispatch/mod.rs-3956-            .unwrap();
src/dispatch/mod.rs-3957-
--
src/dispatch/mod.rs-3972-            "dd if=/dev/zero bs=1024 count=50 2>/dev/null; sleep 0.5; echo done",
src/dispatch/mod.rs-3973-        );
src/dispatch/mod.rs-3974-        adapter.idle_timeout_secs = 1;
src/dispatch/mod.rs-3975-        adapter.hard_timeout_secs = 10;
src/dispatch/mod.rs-3976-
src/dispatch/mod.rs-3977-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-3978-        adapters.insert("large-burst".to_string(), adapter);
src/dispatch/mod.rs-3979-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs:3980:        let adapter = dispatcher.adapter("large-burst").unwrap().clone();
src/dispatch/mod.rs-3981-
src/dispatch/mod.rs-3982-        let result = dispatcher
src/dispatch/mod.rs:3983:            .dispatch(
src/dispatch/mod.rs-3984-                &BeadId::from("nd-activity-burst"),
src/dispatch/mod.rs-3985-                &test_prompt("t"),
src/dispatch/mod.rs-3986-                &adapter,
src/dispatch/mod.rs-3987-                Path::new("/tmp"),
src/dispatch/mod.rs-3988-            )
src/dispatch/mod.rs-3989-            .await
src/dispatch/mod.rs-3990-            .unwrap();
src/dispatch/mod.rs-3991-
--
src/dispatch/mod.rs-4005-        let mut adapter = test_adapter("timestamped", "echo output-$(date +%s%N)");
src/dispatch/mod.rs-4006-        adapter.idle_timeout_secs = 1;
src/dispatch/mod.rs-4007-
src/dispatch/mod.rs-4008-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-4009-        adapters.insert("timestamped".to_string(), adapter);
src/dispatch/mod.rs-4010-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs-4011-
src/dispatch/mod.rs-4012-        // First dispatch
src/dispatch/mod.rs:4013:        let adapter1 = dispatcher.adapter("timestamped").unwrap().clone();
src/dispatch/mod.rs-4014-        let result1 = dispatcher
src/dispatch/mod.rs:4015:            .dispatch(
src/dispatch/mod.rs-4016-                &BeadId::from("nd-timestamp-1"),
src/dispatch/mod.rs-4017-                &test_prompt("t"),
src/dispatch/mod.rs-4018-                &adapter1,
src/dispatch/mod.rs-4019-                Path::new("/tmp"),
src/dispatch/mod.rs-4020-            )
src/dispatch/mod.rs-4021-            .await
src/dispatch/mod.rs-4022-            .unwrap();
src/dispatch/mod.rs-4023-
src/dispatch/mod.rs-4024-        assert_eq!(result1.exit_code, 0);
src/dispatch/mod.rs-4025-
src/dispatch/mod.rs-4026-        // Small delay to ensure different timestamp
src/dispatch/mod.rs-4027-        tokio::time::sleep(Duration::from_millis(100)).await;
src/dispatch/mod.rs-4028-
src/dispatch/mod.rs-4029-        // Second dispatch
src/dispatch/mod.rs:4030:        let adapter2 = dispatcher.adapter("timestamped").unwrap().clone();
src/dispatch/mod.rs-4031-        let result2 = dispatcher
src/dispatch/mod.rs:4032:            .dispatch(
src/dispatch/mod.rs-4033-                &BeadId::from("nd-timestamp-2"),
src/dispatch/mod.rs-4034-                &test_prompt("t"),
src/dispatch/mod.rs-4035-                &adapter2,
src/dispatch/mod.rs-4036-                Path::new("/tmp"),
src/dispatch/mod.rs-4037-            )
src/dispatch/mod.rs-4038-            .await
src/dispatch/mod.rs-4039-            .unwrap();
src/dispatch/mod.rs-4040-
--
src/dispatch/mod.rs-4049-    #[tokio::test]
src/dispatch/mod.rs-4050-    async fn activity_detection_on_normal_stdout_output() {
src/dispatch/mod.rs-4051-        // Test that activity detection works for normal stdout output
src/dispatch/mod.rs-4052-        let adapter = test_adapter("echo-test", "echo 'hello world'");
src/dispatch/mod.rs-4053-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-4054-        adapters.insert("echo-test".to_string(), adapter);
src/dispatch/mod.rs-4055-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs-4056-
src/dispatch/mod.rs:4057:        let adapter_ref = dispatcher.adapter("echo-test").unwrap().clone();
src/dispatch/mod.rs-4058-        let result = dispatcher
src/dispatch/mod.rs:4059:            .dispatch(
src/dispatch/mod.rs-4060-                &BeadId::from("nd-echo-test"),
src/dispatch/mod.rs-4061-                &test_prompt("test"),
src/dispatch/mod.rs-4062-                &adapter_ref,
src/dispatch/mod.rs-4063-                Path::new("/tmp"),
src/dispatch/mod.rs-4064-            )
src/dispatch/mod.rs-4065-            .await
src/dispatch/mod.rs-4066-            .unwrap();
src/dispatch/mod.rs-4067-
--
src/dispatch/mod.rs-4075-    #[tokio::test]
src/dispatch/mod.rs-4076-    async fn activity_detection_on_stderr_output() {
src/dispatch/mod.rs-4077-        // Test that activity detection works for stderr output
src/dispatch/mod.rs-4078-        let adapter = test_adapter("stderr-test", "echo 'error message' >&2");
src/dispatch/mod.rs-4079-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-4080-        adapters.insert("stderr-test".to_string(), adapter);
src/dispatch/mod.rs-4081-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs-4082-
src/dispatch/mod.rs:4083:        let adapter_ref = dispatcher.adapter("stderr-test").unwrap().clone();
src/dispatch/mod.rs-4084-        let result = dispatcher
src/dispatch/mod.rs:4085:            .dispatch(
src/dispatch/mod.rs-4086-                &BeadId::from("nd-stderr-test"),
src/dispatch/mod.rs-4087-                &test_prompt("test"),
src/dispatch/mod.rs-4088-                &adapter_ref,
src/dispatch/mod.rs-4089-                Path::new("/tmp"),
src/dispatch/mod.rs-4090-            )
src/dispatch/mod.rs-4091-            .await
src/dispatch/mod.rs-4092-            .unwrap();
src/dispatch/mod.rs-4093-
--
src/dispatch/mod.rs-4102-    async fn activity_detection_during_transforms() {
src/dispatch/mod.rs-4103-        // Test that activity detection works when output_transform is configured
src/dispatch/mod.rs-4104-        let mut adapter = test_adapter("transform-test", "echo 'test output'");
src/dispatch/mod.rs-4105-        adapter.output_transform = Some("cat".to_string()); // Use cat as simple transform
src/dispatch/mod.rs-4106-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-4107-        adapters.insert("transform-test".to_string(), adapter);
src/dispatch/mod.rs-4108-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs-4109-
src/dispatch/mod.rs:4110:        let adapter_ref = dispatcher.adapter("transform-test").unwrap().clone();
src/dispatch/mod.rs-4111-        let result = dispatcher
src/dispatch/mod.rs:4112:            .dispatch(
src/dispatch/mod.rs-4113-                &BeadId::from("nd-transform-test"),
src/dispatch/mod.rs-4114-                &test_prompt("test"),
src/dispatch/mod.rs-4115-                &adapter_ref,
src/dispatch/mod.rs-4116-                Path::new("/tmp"),
src/dispatch/mod.rs-4117-            )
src/dispatch/mod.rs-4118-            .await
src/dispatch/mod.rs-4119-            .unwrap();
src/dispatch/mod.rs-4120-
--
src/dispatch/mod.rs-4131-        let adapter = test_adapter(
src/dispatch/mod.rs-4132-            "multiline-test",
src/dispatch/mod.rs-4133-            "for i in 1 2 3 4 5; do echo \"line $i\"; sleep 0.1; done",
src/dispatch/mod.rs-4134-        );
src/dispatch/mod.rs-4135-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-4136-        adapters.insert("multiline-test".to_string(), adapter);
src/dispatch/mod.rs-4137-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs-4138-
src/dispatch/mod.rs:4139:        let adapter_ref = dispatcher.adapter("multiline-test").unwrap().clone();
src/dispatch/mod.rs-4140-        let result = dispatcher
src/dispatch/mod.rs:4141:            .dispatch(
src/dispatch/mod.rs-4142-                &BeadId::from("nd-multiline-test"),
src/dispatch/mod.rs-4143-                &test_prompt("test"),
src/dispatch/mod.rs-4144-                &adapter_ref,
src/dispatch/mod.rs-4145-                Path::new("/tmp"),
src/dispatch/mod.rs-4146-            )
src/dispatch/mod.rs-4147-            .await
src/dispatch/mod.rs-4148-            .unwrap();
src/dispatch/mod.rs-4149-
--
src/dispatch/mod.rs-4162-        let adapter = test_adapter(
src/dispatch/mod.rs-4163-            "chunked-test",
src/dispatch/mod.rs-4164-            "echo 'chunk1'; sleep 0.2; echo 'chunk2'; sleep 0.2; echo 'chunk3'",
src/dispatch/mod.rs-4165-        );
src/dispatch/mod.rs-4166-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-4167-        adapters.insert("chunked-test".to_string(), adapter);
src/dispatch/mod.rs-4168-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs-4169-
src/dispatch/mod.rs:4170:        let adapter_ref = dispatcher.adapter("chunked-test").unwrap().clone();
src/dispatch/mod.rs-4171-        let result = dispatcher
src/dispatch/mod.rs:4172:            .dispatch(
src/dispatch/mod.rs-4173-                &BeadId::from("nd-chunked-test"),
src/dispatch/mod.rs-4174-                &test_prompt("test"),
src/dispatch/mod.rs-4175-                &adapter_ref,
src/dispatch/mod.rs-4176-                Path::new("/tmp"),
src/dispatch/mod.rs-4177-            )
src/dispatch/mod.rs-4178-            .await
src/dispatch/mod.rs-4179-            .unwrap();
src/dispatch/mod.rs-4180-
--
src/dispatch/mod.rs-4191-    async fn activity_detection_timestamps_before_parsing() {
src/dispatch/mod.rs-4192-        // Test that activity timestamps are recorded before newline parsing
src/dispatch/mod.rs-4193-        // This test verifies the structural requirement from the acceptance criteria
src/dispatch/mod.rs-4194-        let adapter = test_adapter("timestamp-order-test", "printf 'line1\\nline2\\nline3'");
src/dispatch/mod.rs-4195-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-4196-        adapters.insert("timestamp-order-test".to_string(), adapter);
src/dispatch/mod.rs-4197-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs-4198-
src/dispatch/mod.rs:4199:        let adapter_ref = dispatcher.adapter("timestamp-order-test").unwrap().clone();
src/dispatch/mod.rs-4200-        let result = dispatcher
src/dispatch/mod.rs:4201:            .dispatch(
src/dispatch/mod.rs-4202-                &BeadId::from("nd-timestamp-order-test"),
src/dispatch/mod.rs-4203-                &test_prompt("test"),
src/dispatch/mod.rs-4204-                &adapter_ref,
src/dispatch/mod.rs-4205-                Path::new("/tmp"),
src/dispatch/mod.rs-4206-            )
src/dispatch/mod.rs-4207-            .await
src/dispatch/mod.rs-4208-            .unwrap();
src/dispatch/mod.rs-4209-
--
src/dispatch/mod.rs-4223-        let adapter = test_adapter(
src/dispatch/mod.rs-4224-            "rapid-test",
src/dispatch/mod.rs-4225-            "for i in $(seq 1 100); do echo \"rapid $i\"; done",
src/dispatch/mod.rs-4226-        );
src/dispatch/mod.rs-4227-        let mut adapters = HashMap::new();
src/dispatch/mod.rs-4228-        adapters.insert("rapid-test".to_string(), adapter);
src/dispatch/mod.rs-4229-        let dispatcher = test_dispatcher(adapters);
src/dispatch/mod.rs-4230-
src/dispatch/mod.rs:4231:        let adapter_ref = dispatcher.adapter("rapid-test").unwrap().clone();
src/dispatch/mod.rs-4232-        let result = dispatcher
src/dispatch/mod.rs:4233:            .dispatch(
src/dispatch/mod.rs-4234-                &BeadId::from("nd-rapid-test"),
src/dispatch/mod.rs-4235-                &test_prompt("test"),
src/dispatch/mod.rs-4236-                &adapter_ref,
src/dispatch/mod.rs-4237-                Path::new("/tmp"),
src/dispatch/mod.rs-4238-            )
src/dispatch/mod.rs-4239-            .await
src/dispatch/mod.rs-4240-            .unwrap();
src/dispatch/mod.rs-4241-
--
src/mitosis/mod.rs-291-                        "not at trigger point (count={}, first_failure_only={}, repeat_interval={})",
src/mitosis/mod.rs-292-                        failure_count, self.config.first_failure_only, self.config.repeat_interval
src/mitosis/mod.rs-293-                    ),
src/mitosis/mod.rs-294-                });
src/mitosis/mod.rs-295-            }
src/mitosis/mod.rs-296-        }
src/mitosis/mod.rs-297-
src/mitosis/mod.rs-298-        // Resolve the agent adapter.
src/mitosis/mod.rs:299:        let adapter = match dispatcher.adapter(agent_name) {
src/mitosis/mod.rs-300-            Some(a) => a,
src/mitosis/mod.rs-301-            None => {
src/mitosis/mod.rs-302-                tracing::warn!(
src/mitosis/mod.rs-303-                    bead_id = %bead.id,
src/mitosis/mod.rs-304-                    agent = agent_name,
src/mitosis/mod.rs-305-                    "mitosis skipped: agent adapter not found"
src/mitosis/mod.rs-306-                );
src/mitosis/mod.rs-307-                return Ok(MitosisResult::Skipped {
--
src/mitosis/mod.rs-337-        let _lock = acquire_flock(&lock_path)
src/mitosis/mod.rs-338-            .await
src/mitosis/mod.rs-339-            .context("failed to acquire mitosis flock")?;
src/mitosis/mod.rs-340-
src/mitosis/mod.rs-341-        tracing::info!(bead_id = %bead.id, "dispatching agent for mitosis analysis");
src/mitosis/mod.rs-342-
src/mitosis/mod.rs-343-        // Dispatch agent with mitosis prompt.
src/mitosis/mod.rs-344-        let exec_result = dispatcher
src/mitosis/mod.rs:345:            .dispatch(&bead.id, &prompt, adapter, workspace)
src/mitosis/mod.rs-346-            .await
src/mitosis/mod.rs-347-            .context("mitosis agent dispatch failed")?;
src/mitosis/mod.rs-348-
src/mitosis/mod.rs-349-        // Parse the agent's response.
src/mitosis/mod.rs-350-        let response = parse_mitosis_response(&exec_result.stdout);
src/mitosis/mod.rs-351-
src/mitosis/mod.rs-352-        match response {
src/mitosis/mod.rs-353-            Some(resp) if resp.splittable && !resp.children.is_empty() => {
--
src/mitosis/mod.rs-509-                reason: format!(
src/mitosis/mod.rs-510-                    "depth {} exceeds maximum depth {}",
src/mitosis/mod.rs-511-                    current_depth, self.config.max_depth
src/mitosis/mod.rs-512-                ),
src/mitosis/mod.rs-513-            });
src/mitosis/mod.rs-514-        }
src/mitosis/mod.rs-515-
src/mitosis/mod.rs-516-        // Step 5: Resolve the agent adapter
src/mitosis/mod.rs:517:        let adapter = match dispatcher.adapter(agent_name) {
src/mitosis/mod.rs-518-            Some(a) => a,
src/mitosis/mod.rs-519-            None => {
src/mitosis/mod.rs-520-                tracing::warn!(
src/mitosis/mod.rs-521-                    bead_id = %bead.id,
src/mitosis/mod.rs-522-                    agent = agent_name,
src/mitosis/mod.rs-523-                    "timeout mitosis skipped: agent adapter not found"
src/mitosis/mod.rs-524-                );
src/mitosis/mod.rs-525-                return Ok(MitosisResult::Skipped {
--
src/mitosis/mod.rs-581-        let _lock = acquire_flock(&lock_path)
src/mitosis/mod.rs-582-            .await
src/mitosis/mod.rs-583-            .context("failed to acquire timeout mitosis flock")?;
src/mitosis/mod.rs-584-
src/mitosis/mod.rs-585-        tracing::info!(bead_id = %bead.id, "dispatching agent for timeout mitosis analysis");
src/mitosis/mod.rs-586-
src/mitosis/mod.rs-587-        // Step 10: Dispatch agent with timeout-specific prompt
src/mitosis/mod.rs-588-        let exec_result = dispatcher
src/mitosis/mod.rs:589:            .dispatch(&bead.id, &prompt, adapter, workspace)
src/mitosis/mod.rs-590-            .await
src/mitosis/mod.rs-591-            .context("timeout mitosis agent dispatch failed")?;
src/mitosis/mod.rs-592-
src/mitosis/mod.rs-593-        // Step 11: Parse the agent's response
src/mitosis/mod.rs-594-        let response = parse_mitosis_response(&exec_result.stdout);
src/mitosis/mod.rs-595-
src/mitosis/mod.rs-596-        match response {
src/mitosis/mod.rs-597-            Some(resp) if resp.splittable && !resp.children.is_empty() => {
--
src/worker/mod.rs-938-                .unwrap_or_else(tracing::Span::none);
src/worker/mod.rs-939-
src/worker/mod.rs-940-            match self.state {
src/worker/mod.rs-941-                WorkerState::Selecting => self.do_select().await?,
src/worker/mod.rs-942-                WorkerState::Claiming => self.do_claim().await?,
src/worker/mod.rs-943-                WorkerState::Retrying => self.do_retry().await?,
src/worker/mod.rs-944-                WorkerState::Building => self.do_build().instrument(lifecycle_span.clone()).await?,
src/worker/mod.rs-945-                WorkerState::Dispatching => {
src/worker/mod.rs:946:                    self.do_dispatch()
src/worker/mod.rs-947-                        .instrument(lifecycle_span.clone())
src/worker/mod.rs-948-                        .await?
src/worker/mod.rs-949-                }
src/worker/mod.rs-950-                WorkerState::Executing => {
src/worker/mod.rs-951-                    self.do_execute().instrument(lifecycle_span.clone()).await?
src/worker/mod.rs-952-                }
src/worker/mod.rs-953-                WorkerState::Handling => {
src/worker/mod.rs-954-                    self.do_handle().instrument(lifecycle_span.clone()).await?
--
src/worker/mod.rs-2082-        // Store the prompt for the dispatch phase. We use a transient field pattern:
src/worker/mod.rs-2083-        // the prompt is passed via self.built_prompt.
src/worker/mod.rs-2084-        self.built_prompt = Some(prompt);
src/worker/mod.rs-2085-        self.set_state(WorkerState::Dispatching)?;
src/worker/mod.rs-2086-        Ok(())
src/worker/mod.rs-2087-    }
src/worker/mod.rs-2088-
src/worker/mod.rs-2089-    /// DISPATCHING: check rate limits, resolve adapter, and prepare for execution.
src/worker/mod.rs:2090:    async fn do_dispatch(&mut self) -> Result<()> {
src/worker/mod.rs-2091-        if self.current_bead.is_none() {
src/worker/mod.rs-2092-            bail!("DISPATCHING state without current_bead — invariant violated");
src/worker/mod.rs-2093-        }
src/worker/mod.rs-2094-
src/worker/mod.rs-2095-        // Check rate limits before dispatching.
src/worker/mod.rs-2096-        let adapter = self.resolve_adapter()?;
src/worker/mod.rs-2097-        let provider = adapter.provider.as_deref();
src/worker/mod.rs-2098-        let model = adapter.model.as_deref();
--
src/worker/mod.rs-2245-                        error = %e,
src/worker/mod.rs-2246-                        "failed to record pre-dispatch snapshot — shipped-work gate will fall back"
src/worker/mod.rs-2247-                    );
src/worker/mod.rs-2248-                }
src/worker/mod.rs-2249-
src/worker/mod.rs-2250-                self.exec_started_at = Some(Instant::now());
src/worker/mod.rs-2251-                let result = self
src/worker/mod.rs-2252-                    .dispatcher
src/worker/mod.rs:2253:                    .dispatch(&bead.id, &prompt, &adapter, dispatch_ws)
src/worker/mod.rs-2254-                    .await?;
src/worker/mod.rs-2255-
src/worker/mod.rs-2256-                // Set span status based on exit code: 0 = Ok, non-zero = Error
src/worker/mod.rs-2257-                if result.exit_code != 0 {
src/worker/mod.rs-2258-                    tracing::Span::current().record("otel.status_code", 2u64);
src/worker/mod.rs-2259-                    tracing::Span::current().record(
src/worker/mod.rs-2260-                        "otel.status_description",
src/worker/mod.rs-2261-                        format!("exit_code: {}", result.exit_code),
--
src/worker/mod.rs-3865-                bead_id: id,
src/worker/mod.rs-3866-                model,
src/worker/mod.rs-3867-                matched_rule: matched_rule.clone(),
src/worker/mod.rs-3868-                chosen_adapter: chosen_adapter_name.clone(),
src/worker/mod.rs-3869-            })?;
src/worker/mod.rs-3870-        }
src/worker/mod.rs-3871-
src/worker/mod.rs-3872-        // Resolve the chosen adapter.
src/worker/mod.rs:3873:        let adapter = self.dispatcher.adapter(&chosen_adapter_name)
src/worker/mod.rs-3874-            .cloned()
src/worker/mod.rs-3875-            .ok_or_else(|| anyhow::anyhow!(
src/worker/mod.rs-3876-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
src/worker/mod.rs-3877-                chosen_adapter_name,
src/worker/mod.rs-3878-                default_adapter.model.as_deref().unwrap_or("unknown"),
src/worker/mod.rs-3879-                matched_rule
src/worker/mod.rs-3880-            ))?;
src/worker/mod.rs-3881-
--
src/worker/mod.rs-5227-    #[tokio::test]
src/worker/mod.rs-5228-    async fn do_dispatch_without_bead_is_invariant_error() {
src/worker/mod.rs-5229-        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
src/worker/mod.rs-5230-        let mut worker = make_worker(store);
src/worker/mod.rs-5231-        worker.boot().unwrap();
src/worker/mod.rs-5232-        worker.state = WorkerState::Dispatching;
src/worker/mod.rs-5233-        worker.current_bead = None;
src/worker/mod.rs-5234-
src/worker/mod.rs:5235:        let result = worker.do_dispatch().await;
src/worker/mod.rs-5236-        assert!(result.is_err());
src/worker/mod.rs-5237-        assert!(result.unwrap_err().to_string().contains("invariant"));
src/worker/mod.rs-5238-    }
src/worker/mod.rs-5239-
src/worker/mod.rs-5240-    #[tokio::test]
src/worker/mod.rs-5241-    async fn do_execute_without_bead_is_invariant_error() {
src/worker/mod.rs-5242-        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
src/worker/mod.rs-5243-        let mut worker = make_worker(store);
--
src/telemetry/mod.rs-2745-    pub fn is_empty(&self) -> bool {
src/telemetry/mod.rs-2746-        self.hooks.is_empty()
src/telemetry/mod.rs-2747-    }
src/telemetry/mod.rs-2748-
src/telemetry/mod.rs-2749-    /// Dispatch an event to all matching hooks (fire-and-forget).
src/telemetry/mod.rs-2750-    ///
src/telemetry/mod.rs-2751-    /// Returns a list of `SinkError` events for hooks that failed, so the
src/telemetry/mod.rs-2752-    /// caller can write them to the file sink without recursion.
src/telemetry/mod.rs:2753:    pub fn dispatch(&self, event: &TelemetryEvent) -> Vec<TelemetryEvent> {
src/telemetry/mod.rs-2754-        // Never dispatch SinkError events to hooks — prevents recursion.
src/telemetry/mod.rs-2755-        if event.event_type == "telemetry.sink_error" {
src/telemetry/mod.rs-2756-            return Vec::new();
src/telemetry/mod.rs-2757-        }
src/telemetry/mod.rs-2758-
src/telemetry/mod.rs-2759-        let json = match serde_json::to_string(event) {
src/telemetry/mod.rs-2760-            Ok(j) => j,
src/telemetry/mod.rs-2761-            Err(_) => return Vec::new(),
--
src/telemetry/mod.rs-2875-            .send_string(json)
src/telemetry/mod.rs-2876-            .map_err(|e| anyhow::anyhow!("webhook POST to {url} failed: {e}"))?;
src/telemetry/mod.rs-2877-        Ok(())
src/telemetry/mod.rs-2878-    }
src/telemetry/mod.rs-2879-}
src/telemetry/mod.rs-2880-
src/telemetry/mod.rs-2881-impl Sink for HookSink {
src/telemetry/mod.rs-2882-    fn accept(&self, event: &TelemetryEvent) -> Result<()> {
src/telemetry/mod.rs:2883:        let failures = self.dispatch(event);
src/telemetry/mod.rs-2884-        for fail in &failures {
src/telemetry/mod.rs-2885-            tracing::warn!(
src/telemetry/mod.rs-2886-                hook_error = %fail.data,
src/telemetry/mod.rs-2887-                original_event = %event.event_type,
src/telemetry/mod.rs-2888-                "telemetry hook dispatch failed"
src/telemetry/mod.rs-2889-            );
src/telemetry/mod.rs-2890-        }
src/telemetry/mod.rs-2891-        Ok(())
--
src/telemetry/mod.rs-5798-            event_filter: "outcome.*".to_string(),
src/telemetry/mod.rs-5799-            command: "true".to_string(), // always succeeds
src/telemetry/mod.rs-5800-            url: None,
src/telemetry/mod.rs-5801-        }];
src/telemetry/mod.rs-5802-        let sink = HookSink::new(&configs).unwrap();
src/telemetry/mod.rs-5803-
src/telemetry/mod.rs-5804-        // Matching event — should dispatch (no failures expected)
src/telemetry/mod.rs-5805-        let event = make_test_event("outcome.handled");
src/telemetry/mod.rs:5806:        let failures = sink.dispatch(&event);
src/telemetry/mod.rs-5807-        assert!(failures.is_empty());
src/telemetry/mod.rs-5808-    }
src/telemetry/mod.rs-5809-
src/telemetry/mod.rs-5810-    #[test]
src/telemetry/mod.rs-5811-    fn hook_sink_dispatch_skips_non_matching() {
src/telemetry/mod.rs-5812-        let configs = vec![HookConfig {
src/telemetry/mod.rs-5813-            event_filter: "outcome.*".to_string(),
src/telemetry/mod.rs-5814-            command: "false".to_string(), // would fail if dispatched
src/telemetry/mod.rs-5815-            url: None,
src/telemetry/mod.rs-5816-        }];
src/telemetry/mod.rs-5817-        let sink = HookSink::new(&configs).unwrap();
src/telemetry/mod.rs-5818-
src/telemetry/mod.rs-5819-        // Non-matching event — should NOT dispatch
src/telemetry/mod.rs-5820-        let event = make_test_event("worker.started");
src/telemetry/mod.rs:5821:        let failures = sink.dispatch(&event);
src/telemetry/mod.rs-5822-        // No failures because the hook was never called
src/telemetry/mod.rs-5823-        assert!(failures.is_empty());
src/telemetry/mod.rs-5824-    }
src/telemetry/mod.rs-5825-
src/telemetry/mod.rs-5826-    #[test]
src/telemetry/mod.rs-5827-    fn hook_sink_dispatch_prevents_recursion_on_sink_error() {
src/telemetry/mod.rs-5828-        let configs = vec![HookConfig {
src/telemetry/mod.rs-5829-            event_filter: "telemetry.*".to_string(),
src/telemetry/mod.rs-5830-            command: "true".to_string(),
src/telemetry/mod.rs-5831-            url: None,
src/telemetry/mod.rs-5832-        }];
src/telemetry/mod.rs-5833-        let sink = HookSink::new(&configs).unwrap();
src/telemetry/mod.rs-5834-
src/telemetry/mod.rs-5835-        // SinkError events must never be dispatched to hooks
src/telemetry/mod.rs-5836-        let event = make_test_event("telemetry.sink_error");
src/telemetry/mod.rs:5837:        let failures = sink.dispatch(&event);
src/telemetry/mod.rs-5838-        assert!(failures.is_empty());
src/telemetry/mod.rs-5839-    }
src/telemetry/mod.rs-5840-
src/telemetry/mod.rs-5841-    #[test]
src/telemetry/mod.rs-5842-    fn hook_sink_dispatch_captures_failure() {
src/telemetry/mod.rs-5843-        let configs = vec![HookConfig {
src/telemetry/mod.rs-5844-            event_filter: "bead.*".to_string(),
src/telemetry/mod.rs-5845-            command: "/nonexistent/command/that/does/not/exist".to_string(),
src/telemetry/mod.rs-5846-            url: None,
src/telemetry/mod.rs-5847-        }];
src/telemetry/mod.rs-5848-        let sink = HookSink::new(&configs).unwrap();
src/telemetry/mod.rs-5849-
src/telemetry/mod.rs-5850-        let event = make_test_event("bead.completed");
src/telemetry/mod.rs:5851:        let failures = sink.dispatch(&event);
src/telemetry/mod.rs-5852-        assert_eq!(failures.len(), 1);
src/telemetry/mod.rs-5853-        assert_eq!(failures[0].event_type, "telemetry.sink_error");
src/telemetry/mod.rs-5854-        assert!(failures[0].data["hook_command"]
src/telemetry/mod.rs-5855-            .as_str()
src/telemetry/mod.rs-5856-            .unwrap()
src/telemetry/mod.rs-5857-            .contains("nonexistent"));
src/telemetry/mod.rs-5858-    }
src/telemetry/mod.rs-5859-
--
src/telemetry/mod.rs-5869-                event_filter: "outcome.handled".to_string(),
src/telemetry/mod.rs-5870-                command: "true".to_string(),
src/telemetry/mod.rs-5871-                url: None,
src/telemetry/mod.rs-5872-            },
src/telemetry/mod.rs-5873-        ];
src/telemetry/mod.rs-5874-        let sink = HookSink::new(&configs).unwrap();
src/telemetry/mod.rs-5875-
src/telemetry/mod.rs-5876-        let event = make_test_event("outcome.handled");
src/telemetry/mod.rs:5877:        let failures = sink.dispatch(&event);
src/telemetry/mod.rs-5878-        // Both hooks match, both succeed — no failures
src/telemetry/mod.rs-5879-        assert!(failures.is_empty());
src/telemetry/mod.rs-5880-    }
src/telemetry/mod.rs-5881-
src/telemetry/mod.rs-5882-    #[test]
src/telemetry/mod.rs-5883-    fn hook_sink_dispatches_json_to_stdin() {
src/telemetry/mod.rs-5884-        let tmp = std::env::temp_dir().join("needle-hook-test-stdin");
src/telemetry/mod.rs-5885-        let _ = std::fs::remove_file(&tmp);
--
src/telemetry/mod.rs-5888-        let configs = vec![HookConfig {
src/telemetry/mod.rs-5889-            event_filter: "worker.*".to_string(),
src/telemetry/mod.rs-5890-            command: cmd,
src/telemetry/mod.rs-5891-            url: None,
src/telemetry/mod.rs-5892-        }];
src/telemetry/mod.rs-5893-        let sink = HookSink::new(&configs).unwrap();
src/telemetry/mod.rs-5894-
src/telemetry/mod.rs-5895-        let event = make_test_event("worker.started");
src/telemetry/mod.rs:5896:        let failures = sink.dispatch(&event);
src/telemetry/mod.rs-5897-        assert!(failures.is_empty());
src/telemetry/mod.rs-5898-
src/telemetry/mod.rs-5899-        // Give the child process a moment to write
src/telemetry/mod.rs-5900-        std::thread::sleep(std::time::Duration::from_millis(200));
src/telemetry/mod.rs-5901-
src/telemetry/mod.rs-5902-        let content = std::fs::read_to_string(&tmp).unwrap_or_default();
src/telemetry/mod.rs-5903-        assert!(
src/telemetry/mod.rs-5904-            !content.is_empty(),

exec
/bin/bash -lc "rg -n \"RoutingFailed|routing\" src/types.rs src/telemetry src/worker.rs src/strand src --glob '*.rs' | sed -n '1,280p'" in /home/coding/NEEDLE
 succeeded in 0ms:
rg: src/types.rs: No such file or directory (os error 2)
rg: src/worker.rs: No such file or directory (os error 2)
src/dispatch/mod.rs:759:    /// Resolve an adapter name from a model name using routing rules.
src/dispatch/mod.rs:761:    /// If routing is configured in the config, returns the adapter name that
src/dispatch/mod.rs:767:    /// * `config` - Full config containing routing rules
src/dispatch/mod.rs:772:        if let Some(ref routing) = config.agent.routing {
src/dispatch/mod.rs:773:            // Check each routing rule in order (first match wins)
src/dispatch/mod.rs:774:            for rule in &routing.rules {
src/dispatch/mod.rs:782:                                "routing rule matched"
src/dispatch/mod.rs:792:                            "invalid routing regex pattern (should have been caught by config validation)"
src/dispatch/mod.rs:800:            if let Some(ref default_adapter) = routing.default_adapter {
src/dispatch/mod.rs:804:                    "no routing rules matched, using default_adapter"
src/dispatch/mod.rs:810:        // No routing configured or no default_adapter; fall back to agent.default
src/dispatch/mod.rs:814:            "no routing configured, using agent.default"
src/cli/mod.rs:2576:        "bound {} to {} (routing only; no bead data was migrated)",
src/telemetry/mod.rs:271:        /// The routing rule pattern that matched (e.g., "sonnet"), or "default" if no rules matched.
src/telemetry/mod.rs:276:    RoutingFailed {
src/telemetry/mod.rs:851:            EventKind::RoutingDecision { .. } => "agent.routing_decision",
src/telemetry/mod.rs:852:            EventKind::RoutingFailed { .. } => "agent.routing_failed",
src/telemetry/mod.rs:974:            | EventKind::RoutingFailed { bead_id, .. }
src/telemetry/mod.rs:1321:            EventKind::RoutingFailed {
src/telemetry/mod.rs:2358:            | EventKind::RoutingFailed { .. }
src/telemetry/mod.rs:4457:/// log_debug!(telemetry, "routing", {"model": "sonnet-4", "confidence": 0.95});
src/routing.rs:3://! Provides pattern matching logic for routing rules: regex/glob patterns
src/routing.rs:9:/// Compiled routing rule with cached glob pattern for efficient matching.
src/routing.rs:24:    /// Compile a routing rule into an efficient matcher.
src/routing.rs:130:/// use needle::routing::is_glob_pattern;
src/routing.rs:191:/// Match a model name against routing rules, returning the adapter to use.
src/routing.rs:199:/// * `rules` - Ordered list of routing rules (first match wins).
src/routing.rs:210:/// use needle::routing::match_adapter;
src/routing.rs:251:                    "invalid routing pattern — skipping rule"
src/routing.rs:265:/// Match a model name against routing rules, returning adapter and pattern.
src/routing.rs:294:                    "invalid routing pattern — skipping rule"
src/routing.rs:340:/// use needle::routing::match_adapter_with_glob;
src/routing.rs:403:/// use needle::routing::match_glob;
src/routing.rs:815:    fn real_world_anthropic_routing() {
src/routing.rs:1119:        // Real-world model routing patterns
src/routing.rs:1387:        // Real-world model routing patterns
src/config/mod.rs:29:/// A single routing rule mapping model patterns to adapters.
src/config/mod.rs:44:/// Agent routing configuration.
src/config/mod.rs:47:/// the routing rules are evaluated in order to determine which adapter to use.
src/config/mod.rs:50:    /// Ordered list of routing rules (first match wins).
src/config/mod.rs:58:    /// When true, fail if no routing rule matches instead of falling back.
src/config/mod.rs:62:    /// - A `RoutingFailed` telemetry event is emitted
src/config/mod.rs:123:    /// Model-to-adapter routing rules (optional).
src/config/mod.rs:125:    pub routing: Option<RoutingConfig>,
src/config/mod.rs:135:            routing: Self::default_routing(),
src/config/mod.rs:151:    /// Default routing rules for Anthropic subscription models.
src/config/mod.rs:156:    fn default_routing() -> Option<RoutingConfig> {
src/config/mod.rs:4595:    pub routing: Option<RoutingConfig>,
src/config/mod.rs:4910:            if let Some(ref routing) = agent.routing {
src/config/mod.rs:4911:                config.agent.routing = Some(routing.clone());
src/config/mod.rs:4912:                sources.insert("agent.routing".to_string(), source.clone());
src/config/mod.rs:4997:                    "agent.routing.default_adapter" => {
src/config/mod.rs:5000:                            .routing
src/config/mod.rs:5375:        // Validate routing regex patterns.
src/config/mod.rs:5376:        if let Some(ref routing) = config.agent.routing {
src/config/mod.rs:5377:            for (idx, rule) in routing.rules.iter().enumerate() {
src/config/mod.rs:5381:                        field: format!("agent.routing.rules[{}].match_model", idx),
src/config/mod.rs:5388:                        field: format!("agent.routing.rules[{}].adapter", idx),
src/config/mod.rs:7344:    fn default_routing_config_matches_anthropic_models() {
src/config/mod.rs:7346:        let routing = config
src/config/mod.rs:7348:            .routing
src/config/mod.rs:7349:            .expect("default routing should be Some");
src/config/mod.rs:7352:        assert_eq!(routing.rules.len(), 1);
src/config/mod.rs:7354:        let rule = &routing.rules[0];
src/config/mod.rs:7360:            routing.default_adapter.as_deref(),
src/config/mod.rs:7366:    fn routing_config_with_rules_parses() {
src/config/mod.rs:7370:  routing:
src/config/mod.rs:7383:        assert!(config.agent.routing.is_some());
src/config/mod.rs:7384:        let routing = config.agent.routing.as_ref().unwrap();
src/config/mod.rs:7385:        assert_eq!(routing.rules.len(), 2);
src/config/mod.rs:7386:        assert_eq!(routing.rules[0].match_model, "(claude-)?(sonnet|opus).*");
src/config/mod.rs:7387:        assert_eq!(routing.rules[0].adapter, "claude-print");
src/config/mod.rs:7388:        assert_eq!(routing.rules[1].match_model, "haiku.*");
src/config/mod.rs:7389:        assert_eq!(routing.rules[1].adapter, "claude-code-glm-4.7");
src/config/mod.rs:7391:            routing.default_adapter.as_deref(),
src/config/mod.rs:7397:    fn routing_config_empty_rules_list_is_valid() {
src/config/mod.rs:7400:  routing:
src/config/mod.rs:7409:        assert!(config.agent.routing.is_some());
src/config/mod.rs:7410:        let routing = config.agent.routing.as_ref().unwrap();
src/config/mod.rs:7411:        assert!(routing.rules.is_empty());
src/config/mod.rs:7412:        assert_eq!(routing.default_adapter.as_deref(), Some("fallback-adapter"));
src/config/mod.rs:7416:    fn invalid_regex_in_routing_rule_fails_validation() {
src/config/mod.rs:7418:        config.agent.routing = Some(RoutingConfig {
src/config/mod.rs:7431:                .any(|e| e.field == "agent.routing.rules[0].match_model"),
src/config/mod.rs:7438:    fn empty_adapter_in_routing_rule_fails_validation() {
src/config/mod.rs:7440:        config.agent.routing = Some(RoutingConfig {
src/config/mod.rs:7453:                .any(|e| e.field == "agent.routing.rules[0].adapter"),
src/config/mod.rs:7460:    fn valid_regex_in_routing_rule_passes_validation() {
src/config/mod.rs:7462:        config.agent.routing = Some(RoutingConfig {
src/config/mod.rs:7479:            !errors.iter().any(|e| e.field.starts_with("agent.routing")),
src/config/mod.rs:7480:            "routing config should be valid, but got errors: {:?}",
src/config/mod.rs:7486:    fn workspace_config_routing_override() {
src/config/mod.rs:7492:  routing:
src/config/mod.rs:7506:        assert!(config.agent.routing.is_some());
src/config/mod.rs:7507:        let routing = config.agent.routing.as_ref().unwrap();
src/config/mod.rs:7508:        assert_eq!(routing.rules.len(), 1);
src/config/mod.rs:7509:        assert_eq!(routing.rules[0].match_model, "fable.*");
src/config/mod.rs:7510:        assert_eq!(routing.rules[0].adapter, "fable-adapter");
src/config/mod.rs:7512:            routing.default_adapter.as_deref(),
src/config/mod.rs:7515:        assert!(sources.contains_key("agent.routing"));
src/config/mod.rs:7519:    fn env_var_routing_default_adapter_override() {
src/config/mod.rs:7528:        assert!(config.agent.routing.is_some());
src/config/mod.rs:7532:                .routing
src/config/mod.rs:7539:        assert!(sources.contains_key("agent.routing.default_adapter"));
src/config/mod.rs:7543:    fn env_var_routing_override_beats_workspace() {
src/config/mod.rs:7549:  routing:
src/config/mod.rs:7564:                .routing
src/config/mod.rs:7581:                .routing
src/config/mod.rs:7589:            sources.get("agent.routing.default_adapter"),
src/config/mod.rs:7595:    fn multiple_validation_errors_in_routing() {
src/config/mod.rs:7597:        config.agent.routing = Some(RoutingConfig {
src/config/mod.rs:7617:        let routing_errors: Vec<_> = errors
src/config/mod.rs:7619:            .filter(|e| e.field.starts_with("agent.routing"))
src/config/mod.rs:7623:            routing_errors.len() >= 3,
src/config/mod.rs:7624:            "expected at least 3 routing errors, got {}: {:?}",
src/config/mod.rs:7625:            routing_errors.len(),
src/config/mod.rs:7626:            routing_errors
src/config/mod.rs:7631:    fn routing_config_yaml_roundtrip() {
src/config/mod.rs:7632:        let routing = RoutingConfig {
src/config/mod.rs:7647:        let yaml = serde_yaml::to_string(&routing).unwrap();
src/config/mod.rs:7650:        assert_eq!(decoded.rules.len(), routing.rules.len());
src/config/mod.rs:7651:        assert_eq!(decoded.rules[0].match_model, routing.rules[0].match_model);
src/config/mod.rs:7652:        assert_eq!(decoded.rules[0].adapter, routing.rules[0].adapter);
src/config/mod.rs:7653:        assert_eq!(decoded.default_adapter, routing.default_adapter);
src/config/mod.rs:7657:    fn agent_config_with_routing_yaml_roundtrip() {
src/config/mod.rs:7659:            routing: Some(RoutingConfig {
src/config/mod.rs:7673:        assert!(decoded.routing.is_some());
src/config/mod.rs:7674:        let routing = decoded.routing.as_ref().unwrap();
src/config/mod.rs:7675:        assert_eq!(routing.rules.len(), 1);
src/config/mod.rs:7676:        assert_eq!(routing.rules[0].match_model, "haiku");
src/config/mod.rs:7677:        assert_eq!(routing.default_adapter.as_deref(), Some("fallback"));
src/config/mod.rs:7681:    fn routing_patterns_match_bare_aliases() {
src/config/mod.rs:7684:        config.agent.routing = Some(RoutingConfig {
src/config/mod.rs:7709:            !errors.iter().any(|e| e.field.starts_with("agent.routing")),
src/config/mod.rs:7716:    fn routing_patterns_match_full_model_ids() {
src/config/mod.rs:7719:        config.agent.routing = Some(RoutingConfig {
src/config/mod.rs:7740:            !errors.iter().any(|e| e.field.starts_with("agent.routing")),
src/config/mod.rs:7747:    fn routing_patterns_match_with_wildcards() {
src/config/mod.rs:7750:        config.agent.routing = Some(RoutingConfig {
src/config/mod.rs:7767:            !errors.iter().any(|e| e.field.starts_with("agent.routing")),
src/config/mod.rs:7774:    fn routing_config_from_global_config_file() {
src/config/mod.rs:7780:  routing:
src/config/mod.rs:7791:        assert!(config.agent.routing.is_some());
src/config/mod.rs:7792:        let routing = config.agent.routing.as_ref().unwrap();
src/config/mod.rs:7793:        assert_eq!(routing.rules.len(), 2);
src/config/mod.rs:7794:        assert_eq!(routing.rules[0].adapter, "claude-print");
src/config/mod.rs:7795:        assert_eq!(routing.rules[1].adapter, "claude-code-glm-4.7");
src/config/mod.rs:7797:            routing.default_adapter.as_deref(),
src/config/mod.rs:7803:    fn routing_config_backward_compatibility() {
src/config/mod.rs:7804:        // Config without routing section should behave like before (pure agent.default)
src/config/mod.rs:7816:            config.agent.routing.is_none(),
src/config/mod.rs:7817:            "routing should be None when not specified"
src/config/mod.rs:7825:    fn routing_first_match_wins() {
src/config/mod.rs:7827:        let routing = RoutingConfig {
src/config/mod.rs:7843:        let first_rule = &routing.rules[0];
src/config/mod.rs:7851:        let second_rule = &routing.rules[1];
src/config/mod.rs:7857:        // The routing logic in apply_routing_rules iterates in order and returns first match
src/config/mod.rs:7861:    fn routing_workspace_override() {
src/config/mod.rs:7868:  routing:
src/config/mod.rs:7883:        assert!(config.agent.routing.is_some());
src/config/mod.rs:7884:        let routing = config.agent.routing.as_ref().unwrap();
src/config/mod.rs:7885:        assert_eq!(routing.rules.len(), 1);
src/config/mod.rs:7886:        assert_eq!(routing.rules[0].adapter, "workspace-sonnet-adapter");
src/config/mod.rs:7888:            routing.default_adapter.as_deref(),
src/config/mod.rs:7894:    fn routing_default_fallback_no_match() {
src/config/mod.rs:7896:        let routing = RoutingConfig {
src/config/mod.rs:7906:        let re = regex::Regex::new(&routing.rules[0].match_model).unwrap();
src/config/mod.rs:7913:        assert_eq!(routing.default_adapter.as_deref(), Some("fallback-adapter"));
src/config/mod.rs:7917:    fn routing_strict_mode_failure() {
src/config/mod.rs:7919:        let routing = RoutingConfig {
src/config/mod.rs:7929:        assert!(routing.strict, "strict mode should be enabled");
src/config/mod.rs:7932:        let re = regex::Regex::new(&routing.rules[0].match_model).unwrap();
src/config/mod.rs:7939:        // (This is tested in integration tests via apply_routing_rules)
src/config/mod.rs:7943:    fn routing_default_anthropic_models_to_claude_print() {
src/config/mod.rs:7944:        // Test that default routing rules route Anthropic models to claude-print
src/config/mod.rs:7945:        let default_routing = AgentConfig::default_routing();
src/config/mod.rs:7946:        assert!(default_routing.is_some());
src/config/mod.rs:7948:        let routing = default_routing.unwrap();
src/config/mod.rs:7949:        assert!(!routing.rules.is_empty(), "should have default rules");
src/config/mod.rs:7952:        let rule = &routing.rules[0];
src/config/mod.rs:7971:            routing.default_adapter.as_deref(),
src/config/mod.rs:7977:    fn routing_glm_47_defaults_to_claude_code_glm_47() {
src/config/mod.rs:7979:        let default_routing = AgentConfig::default_routing();
src/config/mod.rs:7980:        let routing = default_routing.unwrap();
src/config/mod.rs:7983:        let rule = &routing.rules[0];
src/config/mod.rs:7994:            routing.default_adapter.as_deref(),
src/lib.rs:52:pub mod routing;
src/worker/logging.rs:203:/// log_event!(&telemetry, "routing", "model_count" => 5);
src/worker/logging.rs:307:/// log_debug(&telemetry, "routing", &[("model", "sonnet-4")]).ok();
src/worker/logging.rs:426:/// log_debug_with_bead(&telemetry, "routing", &[("model", "sonnet-4")], &bead_id).ok();
src/worker/logging.rs:622:        log_event!(&telemetry, "routing",
src/telemetry/mod.rs:271:        /// The routing rule pattern that matched (e.g., "sonnet"), or "default" if no rules matched.
src/telemetry/mod.rs:276:    RoutingFailed {
src/telemetry/mod.rs:851:            EventKind::RoutingDecision { .. } => "agent.routing_decision",
src/telemetry/mod.rs:852:            EventKind::RoutingFailed { .. } => "agent.routing_failed",
src/telemetry/mod.rs:974:            | EventKind::RoutingFailed { bead_id, .. }
src/telemetry/mod.rs:1321:            EventKind::RoutingFailed {
src/telemetry/mod.rs:2358:            | EventKind::RoutingFailed { .. }
src/telemetry/mod.rs:4457:/// log_debug!(telemetry, "routing", {"model": "sonnet-4", "confidence": 0.95});
src/worker/mod.rs:43:use crate::routing;
src/worker/mod.rs:1431:        // model is the default adapter's model — routing overrides apply
src/worker/mod.rs:3836:    /// Resolve the agent adapter from config with model-based routing.
src/worker/mod.rs:3855:        // Apply routing rules if configured.
src/worker/mod.rs:3856:        let (chosen_adapter_name, matched_rule) = self.apply_routing_rules(&default_adapter)?;
src/worker/mod.rs:3858:        // Emit routing decision telemetry on every routing decision.
src/worker/mod.rs:3876:                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
src/worker/mod.rs:3885:    /// Apply routing rules to determine the final adapter.
src/worker/mod.rs:3888:    /// If no routing rules match, returns (default_adapter_name, "default").
src/worker/mod.rs:3890:    fn apply_routing_rules(
src/worker/mod.rs:3894:        let routing_config = match &self.config.agent.routing {
src/worker/mod.rs:3902:        let default_adapter_name = routing_config
src/worker/mod.rs:3907:        // Use the routing module's match_adapter_with_pattern function to get both adapter and pattern.
src/worker/mod.rs:3908:        match routing::match_adapter_with_pattern(
src/worker/mod.rs:3910:            &routing_config.rules,
src/worker/mod.rs:3919:                    if routing_config.strict {
src/worker/mod.rs:3922:                            // Emit RoutingFailed telemetry event.
src/worker/mod.rs:3923:                            let _ = self.telemetry.emit(EventKind::RoutingFailed {
src/worker/mod.rs:3926:                                rules_tried: routing_config.rules.len() as u32,
src/worker/mod.rs:3930:                            "no routing rule matched model '{}' — add a rule to agent.routing.rules or set routing.strict: false to fall back to the default adapter",
src/worker/mod.rs:3935:                    // Strict mode disabled - distinguish between routing default and original default.
src/worker/mod.rs:3936:                    if routing_config.default_adapter.is_some() {
src/worker/mod.rs:3937:                        "routing-default"
src/worker/mod.rs:3950:                    "routing decision completed"
src/worker/mod.rs:3957:                if routing_config.strict {
src/worker/mod.rs:3960:                        // Emit RoutingFailed telemetry event.
src/worker/mod.rs:3961:                        let _ = self.telemetry.emit(EventKind::RoutingFailed {
src/worker/mod.rs:3964:                            rules_tried: routing_config.rules.len() as u32,
src/worker/mod.rs:3968:                        "no routing rule matched model '{}' and no default adapter available — add a rule to agent.routing.rules or set routing.strict: false to fall back to the default adapter",
src/worker/mod.rs:3976:                    "no routing rule matched and no default adapter available, but strict mode is disabled"
src/worker/mod.rs:4345:        // Drop the default routing rules. They rewrite any sonnet/opus/fable/
src/worker/mod.rs:4351:        // adapter routing actually selected. Tests that exercise routing build
src/worker/mod.rs:4353:        config.agent.routing = None;
src/worker/mod.rs:4414:        // routing, so resolution must succeed without consulting any adapter
src/worker/mod.rs:4416:        // error, which only held because the default routing rules rewrote the
src/worker/mod.rs:5587:        config.agent.routing = None; // test dispatcher only has echo-test; disable model-based routing
src/worker/mod.rs:5691:    // ── apply_routing_rules tests ──
src/worker/mod.rs:5694:    async fn apply_routing_rules_strict_true_no_match_returns_err() {
src/worker/mod.rs:5700:        config.agent.routing = Some(RoutingConfig {
src/worker/mod.rs:5709:        let worker = Worker::new(config, "test-routing-strict".to_string(), store);
src/worker/mod.rs:5729:        let result = worker.apply_routing_rules(&adapter);
src/worker/mod.rs:5733:            err_msg.contains("no routing rule matched model 'gpt-4o'"),
src/worker/mod.rs:5738:            err_msg.contains("set routing.strict: false"),
src/worker/mod.rs:5745:    async fn apply_routing_rules_strict_false_no_match_returns_default() {
src/worker/mod.rs:5751:        config.agent.routing = Some(RoutingConfig {
src/worker/mod.rs:5759:        let worker = Worker::new(config, "test-routing-non-strict".to_string(), store);
src/worker/mod.rs:5779:        let result = worker.apply_routing_rules(&adapter);
src/worker/mod.rs:5788:    async fn apply_routing_rules_strict_true_match_returns_ok() {
src/worker/mod.rs:5794:        config.agent.routing = Some(RoutingConfig {
src/worker/mod.rs:5802:        let worker = Worker::new(config, "test-routing-match".to_string(), store);
src/worker/mod.rs:5822:        let result = worker.apply_routing_rules(&adapter);
src/worker/mod.rs:5831:    async fn apply_routing_rules_strict_false_uses_default_adapter() {
src/worker/mod.rs:5837:        config.agent.routing = Some(RoutingConfig {
src/worker/mod.rs:5845:        let worker = Worker::new(config, "test-routing-default-adapter".to_string(), store);
src/worker/mod.rs:5865:        let result = worker.apply_routing_rules(&adapter);
src/worker/mod.rs:5868:        // Should use the routing default adapter when no rule matches.
src/worker/mod.rs:5870:        assert_eq!(matched_rule, "routing-default");
src/worker/mod.rs:5874:    async fn default_routing_rules_anthropic_subscription_models() {
src/worker/mod.rs:5875:        // Verify that default routing rules route Anthropic subscription models to claude-print.
src/worker/mod.rs:5880:        // Use the default routing rules from AgentConfig::default_routing().
src/worker/mod.rs:5881:        let default_routing = config
src/worker/mod.rs:5883:            .routing
src/worker/mod.rs:5885:            .expect("default routing should be set");
src/worker/mod.rs:5887:        // Verify the default routing rules are configured correctly.
src/worker/mod.rs:5889:            !default_routing.rules.is_empty(),
src/worker/mod.rs:5890:            "should have at least one routing rule"
src/worker/mod.rs:5893:            default_routing.rules[0].match_model, "(claude-)?(sonnet|opus|fable|haiku).*",
src/worker/mod.rs:5897:            default_routing.rules[0].adapter, "claude-print",
src/worker/mod.rs:5901:            default_routing.default_adapter,
src/worker/mod.rs:5906:            !default_routing.strict,
src/worker/mod.rs:5910:        // Test routing with actual Anthropic subscription model names.
src/worker/mod.rs:5925:                format!("test-routing-{}", model_name),
src/worker/mod.rs:5947:            let result = worker.apply_routing_rules(&adapter);
src/worker/mod.rs:5950:                "routing should succeed for model {}: {:?}",
src/worker/mod.rs:5962:                "model {} should match a routing rule, not fall back to default",
src/worker/mod.rs:5978:                format!("test-routing-{}", model_name),
src/worker/mod.rs:6000:            let result = worker.apply_routing_rules(&adapter);
src/worker/mod.rs:6003:                "routing should succeed for model {}: {:?}",
src/worker/mod.rs:6014:                matched_rule, "routing-default",
src/worker/mod.rs:6015:                "non-matching model {} should use routing-default adapter",
src/worker/mod.rs:6022:    async fn apply_routing_rules_first_match_wins() {

exec
/bin/bash -lc 'rg -n "apply_routing_rules|routing_rule|matched_rule|resolve_adapter_name|Dispatcher" src/worker/mod.rs' in /home/coding/NEEDLE
 succeeded in 0ms:
36:use crate::dispatch::{self, Dispatcher};
324:    dispatcher: Dispatcher,
494:        // Phase: Dispatcher setup (adapter loading)
499:        let dispatcher = match Dispatcher::new(&config, telemetry.clone()) {
507:                Dispatcher::with_adapters(builtins, telemetry.clone(), config.agent.timeout)
3856:        let (chosen_adapter_name, matched_rule) = self.apply_routing_rules(&default_adapter)?;
3867:                matched_rule: matched_rule.clone(),
3879:                matched_rule
3887:    /// Returns (chosen_adapter_name, matched_rule_pattern).
3890:    fn apply_routing_rules(
4106:    pub fn set_dispatcher(&mut self, dispatcher: Dispatcher) {
5623:            Dispatcher::with_adapters(adapters, Telemetry::new("test-worker".to_string()), 5);
5691:    // ── apply_routing_rules tests ──
5694:    async fn apply_routing_rules_strict_true_no_match_returns_err() {
5729:        let result = worker.apply_routing_rules(&adapter);
5745:    async fn apply_routing_rules_strict_false_no_match_returns_default() {
5779:        let result = worker.apply_routing_rules(&adapter);
5781:        let (chosen_adapter, matched_rule) = result.unwrap();
5784:        assert_eq!(matched_rule, "default");
5788:    async fn apply_routing_rules_strict_true_match_returns_ok() {
5822:        let result = worker.apply_routing_rules(&adapter);
5824:        let (chosen_adapter, matched_rule) = result.unwrap();
5827:        assert_eq!(matched_rule, "claude-.*");
5831:    async fn apply_routing_rules_strict_false_uses_default_adapter() {
5865:        let result = worker.apply_routing_rules(&adapter);
5867:        let (chosen_adapter, matched_rule) = result.unwrap();
5870:        assert_eq!(matched_rule, "routing-default");
5874:    async fn default_routing_rules_anthropic_subscription_models() {
5947:            let result = worker.apply_routing_rules(&adapter);
5954:            let (chosen_adapter, matched_rule) = result.unwrap();
5961:                matched_rule, "default",
6000:            let result = worker.apply_routing_rules(&adapter);
6007:            let (chosen_adapter, matched_rule) = result.unwrap();
6014:                matched_rule, "routing-default",
6022:    async fn apply_routing_rules_first_match_wins() {
6071:        let result = worker.apply_routing_rules(&adapter);
6073:        let (chosen_adapter, matched_rule) = result.unwrap();
6081:            matched_rule, "claude-sonnet-.*",
6087:    async fn apply_routing_rules_order_matters() {
6137:        let result = worker.apply_routing_rules(&adapter);
6139:        let (chosen_adapter, matched_rule) = result.unwrap();
6148:            matched_rule, "claude-.*",
6156:    async fn apply_routing_rules_baseline_multiple_rules_same_adapter() {
6212:        let result = worker.apply_routing_rules(&adapter);
6215:        let (chosen_adapter, matched_rule) = result.unwrap();
6224:            matched_rule, "claude-.*",
6230:    async fn apply_routing_rules_baseline_three_rules_all_match() {
6290:        let result = worker.apply_routing_rules(&adapter);
6293:        let (chosen_adapter, matched_rule) = result.unwrap();
6301:            matched_rule, "claude-.*",
6307:             chosen_adapter={}, matched_rule={}",
6308:            chosen_adapter, matched_rule
6313:    async fn apply_routing_rules_baseline_invalid_then_valid() {
6372:        let result = worker.apply_routing_rules(&adapter);
6378:        let (chosen_adapter, matched_rule) = result.unwrap();
6386:            matched_rule, "claude-.*",
6392:             chosen_adapter={}, matched_rule={}",
6393:            chosen_adapter, matched_rule
6398:    async fn apply_routing_rules_baseline_first_match_stops_evaluation() {
6457:        let result = worker.apply_routing_rules(&adapter);
6460:        let (chosen_adapter, matched_rule) = result.unwrap();
6468:            matched_rule, "^claude-sonnet-4-6$",
6474:             chosen_adapter={}, matched_rule={}",
6475:            chosen_adapter, matched_rule

exec
/bin/bash -lc "sed -n '1110,1430p' src/worker/mod.rs; sed -n '5360,5900p' src/worker/mod.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
            step: "idle_action_validation".to_string(),
            duration_ms: step_start.elapsed().as_millis() as u64,
        })?;

        // Check boot timeout before each step
        self.check_boot_timeout(BOOT_TIMEOUT_SECS)?;

        // Step: Adapter preflight
        self.telemetry.emit(EventKind::InitStepStarted {
            step: "adapter_preflight".to_string(),
        })?;
        let step_start = Instant::now();
        // Validate that the configured adapter exists before entering the main claim loop.
        // This ensures adapter configuration errors are caught early during startup rather
        // than after a bead has been claimed, preventing orphaned in_progress beads.
        if let Err(e) = self.resolve_adapter() {
            // Provide clear, actionable error message for missing adapter
            let adapter_name = &self.config.agent.default;
            eprintln!("ERROR: Configured agent adapter '{adapter_name}' was not found.");
            eprintln!("Startup is aborting to prevent claiming beads with an invalid adapter configuration.");
            eprintln!();
            eprintln!("To fix this, check one of these locations for your adapter configuration:");
            eprintln!("  - ~/.needle/agents/{adapter_name}.yaml");
            eprintln!("  - ~/.local/bin/claude-config/agents/{adapter_name}/config.json");
            eprintln!("  - ~/.config/needle/adapters/{adapter_name}.yaml");
            eprintln!();
            eprintln!("Common causes:");
            eprintln!("  - Adapter file does not exist or is in a different location");
            eprintln!("  - Incorrect adapter name specified in agent.default config");
            eprintln!(
                "  - Adapter file exists but is missing required fields (provider, model, etc.)"
            );
            eprintln!();
            eprintln!("Underlying error: {e}");
            bail!("adapter '{adapter_name}' not found — startup aborted");
        }
        self.telemetry.emit(EventKind::InitStepCompleted {
            step: "adapter_preflight".to_string(),
            duration_ms: step_start.elapsed().as_millis() as u64,
        })?;

        // Check boot timeout before each step
        self.check_boot_timeout(BOOT_TIMEOUT_SECS)?;

        // Step: Heartbeat emitter start
        self.telemetry.emit(EventKind::InitStepStarted {
            step: "heartbeat_emitter".to_string(),
        })?;
        let step_start = Instant::now();
        if let Err(e) = self.health.start_emitter() {
            tracing::warn!(error = %e, "failed to start heartbeat emitter");
        }
        self.telemetry.emit(EventKind::InitStepCompleted {
            step: "heartbeat_emitter".to_string(),
            duration_ms: step_start.elapsed().as_millis() as u64,
        })?;

        // Emit worker started event — boot complete
        self.telemetry.emit(EventKind::WorkerStarted {
            worker_name: self.worker_name.clone(),
            version: env!("CARGO_PKG_VERSION").to_string(),
        })?;

        self.set_state(WorkerState::Selecting)?;

        tracing::info!(
            worker = %self.worker_name,
            strands = ?self.strands.strand_names(),
            "worker booted"
        );

        Ok(())
    }

    /// Check if boot has exceeded the timeout and abort if so.
    ///
    /// Emits `worker.boot.timeout` and exits the process with a non-zero
    /// code. This is a last-resort measure when an init step hangs.
    fn check_boot_timeout(&self, timeout_secs: u64) -> Result<()> {
        if let Some(boot_start) = self.boot_time {
            let elapsed = boot_start.elapsed();
            if elapsed.as_secs() > timeout_secs {
                let elapsed_ms = elapsed.as_millis() as u64;
                // Emit the timeout event before aborting
                let _ = self
                    .telemetry
                    .emit(EventKind::WorkerBootTimeout { elapsed_ms });
                tracing::error!(
                    elapsed_ms,
                    "boot timeout exceeded {}s — aborting",
                    timeout_secs
                );
                // Flush telemetry before exit
                std::mem::forget(self.telemetry.clone());
                // Exit with a distinct code to indicate boot timeout
                std::process::exit(71); // EX_OSERR + custom offset
            }
        }
        Ok(())
    }

    // ── Signal handling ─────────────────────────────────────────────────────

    /// Install SIGINT, SIGTERM, and SIGHUP handlers that set the shutdown flag.
    ///
    /// SIGHUP is handled because when the parent bash dies (e.g., tmux session
    /// killed, external reaper), the child process receives SIGHUP by default.
    /// Without a handler, the process terminates immediately without emitting
    /// worker.stopped telemetry or flushing the telemetry buffer.
    ///
    /// Uses synchronous signal handlers via libc/signal-hook to ensure signals
    /// are caught immediately, even if the tokio runtime hasn't polled async
    /// signal tasks yet. This prevents silent process termination when signals
    /// arrive early (e.g., SIGHUP from parent bash death during worker startup).
    fn install_signal_handlers(&self) {
        // Store a global reference to the shutdown flag for signal handlers.
        // We use a leak to ensure the reference lives for the entire program duration.
        let shutdown_ptr = Arc::into_raw(self.shutdown.clone()) as usize;
        set_global_shutdown_flag(shutdown_ptr);

        // Register atexit handler to emit worker.stopped telemetry on unexpected termination.
        // This provides diagnostic information when the worker is killed by an external
        // process (e.g., capacity governor, OOM killer, SIGKILL).
        let start_time = self.boot_time.unwrap_or_else(Instant::now);
        let heartbeat_path = self.health.heartbeat_path().to_str().map(String::from);
        register_atexit_handler(
            self.worker_name.clone(),
            self.beads_processed,
            start_time,
            format!("{:?}", self.state),
            None, // log_file_path - not available during boot
            heartbeat_path,
        );

        #[cfg(unix)]
        {
            // Install synchronous signal handlers using libc.
            // These handlers are called immediately when the signal is received,
            // before the tokio runtime has a chance to process any async tasks.
            unsafe {
                install_unix_signal_handlers();
            }
        }

        #[cfg(not(unix))]
        {
            // On non-Unix platforms, use tokio's ctrl_c handler.
            let shutdown_int = self.shutdown.clone();
            tokio::spawn(async move {
                if tokio::signal::ctrl_c().await.is_ok() {
                    tracing::info!("received SIGINT, initiating graceful shutdown");
                    shutdown_int.store(true, Ordering::SeqCst);
                }
            });
        }
    }

    // ── State handlers ──────────────────────────────────────────────────────

    /// SELECTING: run strand waterfall to find a candidate bead.
    async fn do_select(&mut self) -> Result<()> {
        // Clear per-cycle state.
        // Preserve race-lost exclusions with TTL and beads that lost a race in the current cycle.
        // NOTE: Do NOT reset retry_count or consecutive_race_lost here — they must
        // accumulate across cycles to prevent infinite race-lost loops (see needle-aad8).
        self.race_lost_this_cycle.clear();
        self.current_bead = None;
        self.current_strand = None;
        self.health.update_strand(None);

        // Restore home store if it was swapped for a remote workspace.
        self.restore_home_store();

        // Update heartbeat with home workspace (not current_workspace which
        // might be stale). This ensures heartbeat reports correctly even if
        // restore_home_store() was a no-op (stores already equal).
        self.health.update_state(
            &WorkerState::Selecting,
            None,
            Some(self.config.workspace.default.as_path()),
        );

        // Try atomic claim_auto first (server-selected bead in one transaction).
        // This eliminates the race condition where two workers both see the same
        // bead in ready() and race to claim it.
        let strand = "auto";
        let claim = self.claimer.claim_auto(&self.qualified_id(), strand).await;

        match claim {
            Ok(ClaimResult::Claimed(bead)) => {
                tracing::info!(bead_id = %bead.id, "atomically claimed bead via claim_auto");
                let workspace = if is_workspace_unset(&bead.workspace) {
                    self.current_workspace.clone()
                } else {
                    bead.workspace.clone()
                };
                if !is_workspace_unset(&workspace) {
                    self.telemetry.set_workspace(workspace);
                }
                crate::hoop_hooks::emit_needle_event(
                    &self.current_workspace,
                    &self.worker_name,
                    Some(bead.id.as_ref()),
                    Some(strand),
                    "claim",
                    serde_json::json!({}),
                );
                self.current_bead = Some(bead);
                self.current_strand = Some(strand.to_string());
                self.health.update_strand(Some(strand));
                self.consecutive_race_lost = 0;
                self.set_state(WorkerState::Building)?;
                return Ok(());
            }
            Ok(ClaimResult::NotClaimable { reason }) => {
                tracing::debug!(
                    reason,
                    "claim_auto returned no beads, falling back to strand waterfall"
                );
                // Fall through to strand waterfall
            }
            Err(e) => {
                tracing::warn!(error = %e, "claim_auto failed, falling back to strand waterfall");
                // Fall through to strand waterfall
            }
            Ok(other) => {
                tracing::warn!(
                    ?other,
                    "claim_auto returned unexpected result, falling back to strand waterfall"
                );
                // Fall through to strand waterfall
            }
        }

        // Fallback: run strand waterfall to find a candidate bead.
        let exclusions = self.current_exclusions();
        let candidate = self
            .strands
            .select(self.store.as_ref(), &exclusions)
            .await?;
        self.last_waterfall_restarts = candidate.waterfall_restarts;
        self.last_restart_triggers = candidate.restart_triggers.clone();
        self.last_strand_evaluations = candidate
            .strand_evaluations
            .iter()
            .map(|e| (e.strand_name.clone(), e.result.clone(), e.duration_ms))
            .collect();

        match candidate.bead {
            Some((bead, strand_name)) => {
                tracing::debug!(bead_id = %bead.id, strand = %strand_name, "candidate found");

                // If the bead is from a remote workspace (found by Explore),
                // swap the active store so claim/show/release operate on the
                // correct workspace. Only switch if the workspace has a real
                // .beads/ directory — avoids false triggers from mock/stub beads.
                let bead_ws = bead.workspace.clone();
                if !is_workspace_unset(&bead_ws)
                    && bead_ws != self.config.workspace.default
                    && bead_ws.join(".beads").is_dir()
                {
                    tracing::info!(
                        bead_id = %bead.id,
                        remote_workspace = %bead_ws.display(),
                        "bead is from remote workspace, switching store"
                    );
                    self.switch_store_to(&bead_ws)?;
                }

                // Always update current_workspace to reflect the bead's workspace.
                // For local beads, this keeps heartbeat consistent with home workspace.
                // For cross-workspace beads, this ensures heartbeat reports where
                // the work is actually happening.
                if !is_workspace_unset(&bead_ws) {
                    self.current_workspace = bead_ws.clone();
                }

                crate::hoop_hooks::emit_needle_event(
                    &self.current_workspace,
                    &self.worker_name,
                    Some(bead.id.as_ref()),
                    Some(strand_name.as_str()),
                    "claim",
                    serde_json::json!({}),
                );
                self.health.update_strand(Some(strand_name.as_str()));
                self.current_bead = Some(bead);
                self.current_strand = Some(strand_name);

                // Update heartbeat immediately with the bead's workspace so that
                // observers see the correct workspace even before transitioning to
                // CLAIMING. This ensures heartbeats are accurate for cross-workspace
                // work (see bead needle-c63c).
                self.health.update_state(
                    &WorkerState::Selecting,
                    Some(&self.current_bead.as_ref().unwrap().id),
                    Some(&bead_ws),
                );

                self.set_state(WorkerState::Claiming)?;
            }
            None => {
                // Set found_but_excluded flag if candidates were found but all excluded
                // This enables short retry backoff instead of full idle backoff
                self.found_but_excluded = self.found_but_all_excluded();
                self.set_state(WorkerState::Exhausted)?;
            }
        }

        Ok(())
    }

    /// Swap the active bead store to a remote workspace.
    ///
    /// Loads the target workspace's explicit backend binding, creates that
    /// store, and rebuilds the Claimer to use it. The home store is restored
    /// at the start of the next select cycle.
    fn switch_store_to(&mut self, workspace: &std::path::Path) -> Result<()> {
        // Resolve this worker's default adapter model so remote-workspace
        // claims carry the same velocity-scoring metadata as home-workspace
        // claims (bf-backed stores only — bead-rs has no such input). The
        worker.current_bead = Some(bead);
        worker.set_state(WorkerState::Executing).unwrap();

        // Verify that current_workspace was updated with the remote workspace
        assert_eq!(worker.current_workspace, remote_ws);
    }

    #[test]
    fn set_state_uses_home_workspace_when_bead_workspace_is_unset() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let dir = tempfile::tempdir().unwrap();
        let home_ws = dir.path().join("home");
        let mut config = valid_test_config();
        config.workspace.home = home_ws.clone();
        config.workspace.default = home_ws.clone();
        let mut worker = Worker::new(config, "test-unset-ws".to_string(), store);
        worker.boot().unwrap();

        // Set up a bead with an unset workspace (".")
        let bead = Bead {
            id: BeadId::from("needle-unset"),
            title: "Unset workspace bead".to_string(),
            body: None,
            priority: 1,
            status: BeadStatus::InProgress,
            assignee: Some(worker.qualified_id()),
            labels: vec![],
            workspace: std::path::PathBuf::from("."),
            dependencies: vec![],
            dependents: vec![],
            comments: vec![],
            created_at: chrono::Utc::now(),
            updated_at: chrono::Utc::now(),
        };

        worker.current_bead = Some(bead);
        worker.set_state(WorkerState::Executing).unwrap();

        // Verify that current_workspace was updated with the home workspace
        assert_eq!(worker.current_workspace, home_ws);
    }

    #[test]
    fn set_state_uses_home_workspace_when_no_current_bead() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let dir = tempfile::tempdir().unwrap();
        let home_ws = dir.path().join("home");
        let mut config = valid_test_config();
        config.workspace.home = home_ws.clone();
        config.workspace.default = home_ws.clone();
        let mut worker = Worker::new(config, "test-no-bead".to_string(), store);
        worker.boot().unwrap();

        // No current bead, current_workspace is unset
        worker.current_bead = None;
        worker.current_workspace = std::path::PathBuf::from("");
        worker.set_state(WorkerState::Exhausted).unwrap();

        // Verify that current_workspace was updated with the home workspace
        assert_eq!(worker.current_workspace, home_ws);
    }

    // ── do_log tests ──

    #[tokio::test]
    async fn do_log_increments_beads_processed() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        // Isolate workspace home to avoid registry pollution from other tests.
        let dir = tempfile::tempdir().unwrap();
        let mut config = valid_test_config();
        config.self_modification.hot_reload = false;
        config.workspace.home = dir.path().to_path_buf();
        let mut worker = Worker::new(config, "test-log-inc".to_string(), store);
        worker.boot().unwrap();
        worker.state = WorkerState::Logging;
        worker.current_bead = Some(make_test_bead("needle-log-1"));

        assert_eq!(worker.beads_processed(), 0);
        worker.do_log().unwrap();
        assert_eq!(worker.beads_processed(), 1);
        assert_eq!(*worker.state(), WorkerState::Selecting);
    }

    #[tokio::test]
    async fn do_log_clears_current_bead_and_effort() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut worker = make_worker(store);
        worker.boot().unwrap();
        worker.state = WorkerState::Logging;
        worker.current_bead = Some(make_test_bead("needle-log-2"));
        worker.last_effort = Some(EffortData {
            cycle_start: Instant::now(),
            agent_name: "test".to_string(),
            model: None,
            tokens: dispatch::TokenUsage::default(),
            estimated_cost_usd: None,
        });

        worker.do_log().unwrap();

        assert!(worker.current_bead.is_none());
        assert!(worker.last_effort.is_none());
    }

    #[tokio::test]
    async fn do_log_transitions_to_selecting() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut worker = make_worker(store);
        worker.boot().unwrap();
        worker.state = WorkerState::Logging;
        worker.current_bead = Some(make_test_bead("needle-log-3"));

        worker.do_log().unwrap();
        assert_eq!(*worker.state(), WorkerState::Selecting);
    }

    // ── handle_exhausted tests ──

    #[tokio::test]
    async fn handle_exhausted_with_exit_returns_stopped() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut config = valid_test_config();
        config.worker.idle_action = IdleAction::Exit;
        config.self_modification.hot_reload = false;
        let mut worker = Worker::new(config, "test-exhaust-exit".to_string(), store);
        worker.boot().unwrap();
        worker.state = WorkerState::Exhausted;

        let result = worker.handle_exhausted().await.unwrap();
        assert_eq!(result, WorkerState::Stopped);
    }

    #[tokio::test]
    async fn handle_exhausted_with_wait_returns_selecting() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut config = valid_test_config();
        config.worker.idle_action = IdleAction::Wait;
        // Use a very short timeout so the test doesn't block.
        config.worker.idle_timeout = 0;
        config.self_modification.hot_reload = false;
        let mut worker = Worker::new(config, "test-exhaust-wait".to_string(), store);
        worker.boot().unwrap();
        worker.state = WorkerState::Exhausted;

        let result = worker.handle_exhausted().await.unwrap();
        assert_eq!(result, WorkerState::Selecting);
    }

    // ── stop tests ──

    #[tokio::test]
    async fn stop_returns_stopped_state() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut worker = make_worker(store);
        worker.boot().unwrap();

        let result = worker.stop("test shutdown").await.unwrap();
        assert_eq!(result, WorkerState::Stopped);
    }

    // ── resolve_provider tests ──

    #[tokio::test]
    async fn resolve_provider_returns_none_for_missing_adapter() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut config = Config::default();
        config.agent.default = "nonexistent-adapter".to_string();
        config.self_modification.hot_reload = false;
        let worker = Worker::new(config, "test-provider".to_string(), store);

        // Default adapter not found → provider is None.
        assert!(worker.resolve_provider().is_none());
    }

    // ── restore_home_store tests ──

    #[tokio::test]
    async fn restore_home_store_is_noop_when_stores_match() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut worker = make_worker(store);
        worker.boot().unwrap();

        // home_store and store should be the same Arc initially.
        assert!(Arc::ptr_eq(&worker.store, &worker.home_store));
        worker.restore_home_store();
        assert!(Arc::ptr_eq(&worker.store, &worker.home_store));
    }

    // ── do_select with exclusion set ──

    #[tokio::test]
    async fn do_select_clears_race_lost_this_cycle_and_retry_count() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut config = valid_test_config();
        config.self_modification.hot_reload = false;
        // Disable Explore strand so it doesn't find beads from the filesystem
        config.strands.explore.enabled = false;
        let mut worker = Worker::new(config, "test-worker".to_string(), store);
        worker.boot().unwrap();
        worker.race_lost_this_cycle.insert(BeadId::from("old-bead"));
        worker.retry_count = 3;

        worker.do_select().await.unwrap();

        assert!(worker.race_lost_this_cycle.is_empty());
        // retry_count is NOT cleared by do_select() anymore — it must accumulate
        // across cycles to prevent infinite race-lost loops (see needle-aad8).
        assert_eq!(worker.retry_count, 3);
        // Note: exclusion_set is NOT cleared by do_select() anymore - it persists
        // for race-lost beads until they expire or the worker transitions to Exhausted
    }

    // ── full cycle test ──

    #[tokio::test]
    async fn full_cycle_with_echo_agent() {
        use std::collections::HashMap;

        // Test a full cycle: select → claim → build → dispatch → execute → handle → log
        let bead = make_test_bead("needle-echo");
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::new(vec![bead]));
        let mut config = Config::default();
        config.worker.idle_action = IdleAction::Exit;
        // Disable hot-reload in tests — it would re-exec into a different binary.
        config.self_modification.hot_reload = false;
        // Use a simple echo adapter so the test finishes quickly.
        config.agent.default = "echo-test".to_string();
        config.agent.routing = None; // test dispatcher only has echo-test; disable model-based routing
        config.agent.timeout = 5;
        // Set workspace.default to match the bead's workspace so the remote
        // store switch logic doesn't fire.
        config.workspace.default = std::path::PathBuf::from("/tmp/test-workspace");
        // Disable Explore: once the MockStore's one bead is processed and
        // `run()` loops again, Pluck/Mend return NoWork and the waterfall
        // previously fell through to a real Explore scan of $HOME, claiming
        // and mutating real beads in unrelated repos on this server via the
        // real bf/br CLI. See bf-2unnq's contamination addendum.
        config.strands.explore.enabled = false;

        let mut worker = Worker::new(config, "test-worker".to_string(), store);

        // Replace the dispatcher with one that has a simple echo adapter.
        let echo_adapter = crate::dispatch::AgentAdapter {
            name: "echo-test".to_string(),
            description: None,
            agent_cli: "echo".to_string(),
            version_command: None,
            input_method: crate::types::InputMethod::Stdin,
            invoke_template: "echo done".to_string(),
            environment: HashMap::new(),
            timeout_secs: 5,
            idle_timeout_secs: 0,
            hard_timeout_secs: 0,
            provider: None,
            model: None,
            token_extraction: crate::dispatch::TokenExtraction::None,
            output_transform: None,
            harness: None,
            harness_version: None,
        };
        let mut adapters = HashMap::new();
        adapters.insert("echo-test".to_string(), echo_adapter);
        worker.dispatcher =
            Dispatcher::with_adapters(adapters, Telemetry::new("test-worker".to_string()), 5);

        let result = worker.run().await.unwrap();
        assert!(
            result == WorkerState::Stopped || result == WorkerState::Exhausted,
            "expected terminal state, got {:?}",
            result
        );
        // At least one bead was processed through the pipeline.
        assert!(
            worker.beads_processed() >= 1,
            "expected at least 1 bead processed, got {}",
            worker.beads_processed()
        );
    }

    // ── check_auto_canary tests ──

    #[tokio::test]
    async fn check_auto_canary_no_op_when_self_modification_disabled() {
        let dir = tempfile::tempdir().unwrap();
        // Create bin/ so the path exists but needle-testing is absent.
        std::fs::create_dir_all(dir.path().join("bin")).unwrap();
        let store = Arc::new(MockStore::empty());
        let mut config = valid_test_config();
        config.self_modification.enabled = false;
        config.self_modification.auto_promote = true;
        config.self_modification.hot_reload = false;
        config.workspace.home = dir.path().to_path_buf();
        let mut worker = Worker::new(config, "test-canary-disabled".to_string(), store);
        worker.boot().unwrap();
        // Must not fail even though canary workspace and binary are absent.
        assert!(worker.check_auto_canary().is_ok());
    }

    #[tokio::test]
    async fn check_auto_canary_no_op_when_auto_promote_false() {
        let dir = tempfile::tempdir().unwrap();
        std::fs::create_dir_all(dir.path().join("bin")).unwrap();
        let store = Arc::new(MockStore::empty());
        let mut config = valid_test_config();
        config.self_modification.enabled = true;
        config.self_modification.auto_promote = false;
        config.self_modification.hot_reload = false;
        config.workspace.home = dir.path().to_path_buf();
        let mut worker = Worker::new(config, "test-canary-no-promote".to_string(), store);
        worker.boot().unwrap();
        assert!(worker.check_auto_canary().is_ok());
    }

    #[tokio::test]
    async fn check_auto_canary_no_op_when_no_testing_binary() {
        let dir = tempfile::tempdir().unwrap();
        // bin/ exists but needle-testing does not.
        std::fs::create_dir_all(dir.path().join("bin")).unwrap();
        let store = Arc::new(MockStore::empty());
        let mut config = valid_test_config();
        config.self_modification.enabled = true;
        config.self_modification.auto_promote = true;
        config.self_modification.hot_reload = false;
        config.workspace.home = dir.path().to_path_buf();
        config.self_modification.canary_workspace = dir.path().join("canary");
        let mut worker = Worker::new(config, "test-canary-no-binary".to_string(), store);
        worker.boot().unwrap();
        // No :testing binary → returns Ok without touching canary workspace.
        assert!(worker.check_auto_canary().is_ok());
    }

    // ── apply_routing_rules tests ──

    #[tokio::test]
    async fn apply_routing_rules_strict_true_no_match_returns_err() {
        use crate::config::{RoutingConfig, RoutingRule};
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut config = Config::default();
        config.self_modification.hot_reload = false;
        // Enable strict mode with a rule that won't match.
        config.agent.routing = Some(RoutingConfig {
            rules: vec![RoutingRule {
                match_model: "sonnet".to_string(),
                adapter: "claude-print".to_string(),
            }],
            default_adapter: None,
            strict: true,
        });
        // Set up a model that won't match the rule.
        let worker = Worker::new(config, "test-routing-strict".to_string(), store);
        let adapter = crate::dispatch::AgentAdapter {
            name: "claude".to_string(),
            description: None,
            agent_cli: "claude".to_string(),
            version_command: None,
            input_method: crate::types::InputMethod::Stdin,
            invoke_template: "claude {prompt}".to_string(),
            environment: std::collections::HashMap::new(),
            timeout_secs: 3600,
            idle_timeout_secs: 0,
            hard_timeout_secs: 0,
            provider: Some("anthropic".to_string()),
            model: Some("gpt-4o".to_string()),
            token_extraction: crate::dispatch::TokenExtraction::None,
            output_transform: None,
            harness: None,
            harness_version: None,
        };

        let result = worker.apply_routing_rules(&adapter);
        assert!(result.is_err());
        let err_msg = result.unwrap_err().to_string();
        assert!(
            err_msg.contains("no routing rule matched model 'gpt-4o'"),
            "error message should mention the model: {}",
            err_msg
        );
        assert!(
            err_msg.contains("set routing.strict: false"),
            "error message should suggest disabling strict mode: {}",
            err_msg
        );
    }

    #[tokio::test]
    async fn apply_routing_rules_strict_false_no_match_returns_default() {
        use crate::config::{RoutingConfig, RoutingRule};
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut config = Config::default();
        config.self_modification.hot_reload = false;
        // Disable strict mode with a rule that won't match.
        config.agent.routing = Some(RoutingConfig {
            rules: vec![RoutingRule {
                match_model: "sonnet".to_string(),
                adapter: "claude-print".to_string(),
            }],
            default_adapter: None,
            strict: false,
        });
        let worker = Worker::new(config, "test-routing-non-strict".to_string(), store);
        let adapter = crate::dispatch::AgentAdapter {
            name: "claude".to_string(),
            description: None,
            agent_cli: "claude".to_string(),
            version_command: None,
            input_method: crate::types::InputMethod::Stdin,
            invoke_template: "claude {prompt}".to_string(),
            environment: std::collections::HashMap::new(),
            timeout_secs: 3600,
            idle_timeout_secs: 0,
            hard_timeout_secs: 0,
            provider: Some("anthropic".to_string()),
            model: Some("gpt-4o".to_string()),
            token_extraction: crate::dispatch::TokenExtraction::None,
            output_transform: None,
            harness: None,
            harness_version: None,
        };

        let result = worker.apply_routing_rules(&adapter);
        assert!(result.is_ok());
        let (chosen_adapter, matched_rule) = result.unwrap();
        // Should fall back to default adapter when no rule matches and strict is false.
        assert_eq!(chosen_adapter, "claude");
        assert_eq!(matched_rule, "default");
    }

    #[tokio::test]
    async fn apply_routing_rules_strict_true_match_returns_ok() {
        use crate::config::{RoutingConfig, RoutingRule};
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut config = Config::default();
        config.self_modification.hot_reload = false;
        // Enable strict mode with a rule that will match.
        config.agent.routing = Some(RoutingConfig {
            rules: vec![RoutingRule {
                match_model: "claude-.*".to_string(),
                adapter: "claude-print".to_string(),
            }],
            default_adapter: None,
            strict: true,
        });
        let worker = Worker::new(config, "test-routing-match".to_string(), store);
        let adapter = crate::dispatch::AgentAdapter {
            name: "claude".to_string(),
            description: None,
            agent_cli: "claude".to_string(),
            version_command: None,
            input_method: crate::types::InputMethod::Stdin,
            invoke_template: "claude {prompt}".to_string(),
            environment: std::collections::HashMap::new(),
            timeout_secs: 3600,
            idle_timeout_secs: 0,
            hard_timeout_secs: 0,
            provider: Some("anthropic".to_string()),
            model: Some("claude-sonnet-4-6".to_string()),
            token_extraction: crate::dispatch::TokenExtraction::None,
            output_transform: None,
            harness: None,
            harness_version: None,
        };

        let result = worker.apply_routing_rules(&adapter);
        assert!(result.is_ok());
        let (chosen_adapter, matched_rule) = result.unwrap();
        // Should use the matched adapter when rule matches.
        assert_eq!(chosen_adapter, "claude-print");
        assert_eq!(matched_rule, "claude-.*");
    }

    #[tokio::test]
    async fn apply_routing_rules_strict_false_uses_default_adapter() {
        use crate::config::{RoutingConfig, RoutingRule};
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut config = Config::default();
        config.self_modification.hot_reload = false;
        // Disable strict mode with default_adapter set.
        config.agent.routing = Some(RoutingConfig {
            rules: vec![RoutingRule {
                match_model: "sonnet".to_string(),
                adapter: "claude-print".to_string(),
            }],
            default_adapter: Some("claude-code-glm-4.7".to_string()),
            strict: false,
        });
        let worker = Worker::new(config, "test-routing-default-adapter".to_string(), store);
        let adapter = crate::dispatch::AgentAdapter {
            name: "claude".to_string(),
            description: None,
            agent_cli: "claude".to_string(),
            version_command: None,
            input_method: crate::types::InputMethod::Stdin,
            invoke_template: "claude {prompt}".to_string(),
            environment: std::collections::HashMap::new(),
            timeout_secs: 3600,
            idle_timeout_secs: 0,
            hard_timeout_secs: 0,
            provider: Some("anthropic".to_string()),
            model: Some("gpt-4o".to_string()),
            token_extraction: crate::dispatch::TokenExtraction::None,
            output_transform: None,
            harness: None,
            harness_version: None,
        };

        let result = worker.apply_routing_rules(&adapter);
        assert!(result.is_ok());
        let (chosen_adapter, matched_rule) = result.unwrap();
        // Should use the routing default adapter when no rule matches.
        assert_eq!(chosen_adapter, "claude-code-glm-4.7");
        assert_eq!(matched_rule, "routing-default");
    }

    #[tokio::test]
    async fn default_routing_rules_anthropic_subscription_models() {
        // Verify that default routing rules route Anthropic subscription models to claude-print.
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut config = Config::default();
        config.self_modification.hot_reload = false;

        // Use the default routing rules from AgentConfig::default_routing().
        let default_routing = config
            .agent
            .routing
            .as_ref()
            .expect("default routing should be set");

        // Verify the default routing rules are configured correctly.
        assert!(
            !default_routing.rules.is_empty(),
            "should have at least one routing rule"
        );
        assert_eq!(
            default_routing.rules[0].match_model, "(claude-)?(sonnet|opus|fable|haiku).*",
            "first rule should match Anthropic subscription models"
        );
        assert_eq!(
            default_routing.rules[0].adapter, "claude-print",
            "first rule should route to claude-print"
        );
        assert_eq!(

exec
/bin/bash -lc "rg -n \"pub struct Adapter|adapter\" src/types.rs src/worker/mod.rs src/config/mod.rs | sed -n '1,220p'; rg -n \"model\" src/worker/mod.rs | sed -n '1,180p'" in /home/coding/NEEDLE
 succeeded in 0ms:
rg: src/types.rs: No such file or directory (os error 2)
src/config/mod.rs:29:/// A single routing rule mapping model patterns to adapters.
src/config/mod.rs:31:/// Rules are evaluated in order; the first matching rule determines the adapter.
src/config/mod.rs:41:    pub adapter: String,
src/config/mod.rs:46:/// Maps model name patterns to adapters. When a bead specifies a model,
src/config/mod.rs:47:/// the routing rules are evaluated in order to determine which adapter to use.
src/config/mod.rs:54:    /// Fallback adapter when no rules match (defaults to `agent.default`).
src/config/mod.rs:56:    pub default_adapter: Option<String>,
src/config/mod.rs:63:    /// - No fallback to `agent.default` or `default_adapter` occurs
src/config/mod.rs:107:    /// Name or path of the default agent adapter (e.g., `claude`).
src/config/mod.rs:119:    /// Directory containing adapter TOML files.
src/config/mod.rs:120:    #[serde(default = "AgentConfig::default_adapters_dir")]
src/config/mod.rs:121:    pub adapters_dir: PathBuf,
src/config/mod.rs:123:    /// Model-to-adapter routing rules (optional).
src/config/mod.rs:134:            adapters_dir: Self::default_adapters_dir(),
src/config/mod.rs:147:    fn default_adapters_dir() -> PathBuf {
src/config/mod.rs:148:        dirs_or_home(".config/needle/adapters")
src/config/mod.rs:162:                adapter: "claude-print".to_string(),
src/config/mod.rs:164:            default_adapter: Some("claude-code-glm-4.7".to_string()),
src/config/mod.rs:4694:        self.agent.adapters_dir = expand_tilde(&self.agent.adapters_dir);
src/config/mod.rs:4997:                    "agent.routing.default_adapter" => {
src/config/mod.rs:5002:                            .default_adapter = Some(value);
src/config/mod.rs:5385:                // Validate that adapter is not empty.
src/config/mod.rs:5386:                if rule.adapter.is_empty() {
src/config/mod.rs:5388:                        field: format!("agent.routing.rules[{}].adapter", idx),
src/config/mod.rs:7356:        assert_eq!(rule.adapter, "claude-print");
src/config/mod.rs:7360:            routing.default_adapter.as_deref(),
src/config/mod.rs:7373:        adapter: claude-print
src/config/mod.rs:7375:        adapter: claude-code-glm-4.7
src/config/mod.rs:7376:    default_adapter: claude-code-glm-4.7
src/config/mod.rs:7387:        assert_eq!(routing.rules[0].adapter, "claude-print");
src/config/mod.rs:7389:        assert_eq!(routing.rules[1].adapter, "claude-code-glm-4.7");
src/config/mod.rs:7391:            routing.default_adapter.as_deref(),
src/config/mod.rs:7402:    default_adapter: fallback-adapter
src/config/mod.rs:7412:        assert_eq!(routing.default_adapter.as_deref(), Some("fallback-adapter"));
src/config/mod.rs:7421:                adapter: "some-adapter".to_string(),
src/config/mod.rs:7423:            default_adapter: None,
src/config/mod.rs:7438:    fn empty_adapter_in_routing_rule_fails_validation() {
src/config/mod.rs:7443:                adapter: String::new(), // Empty adapter
src/config/mod.rs:7445:            default_adapter: None,
src/config/mod.rs:7453:                .any(|e| e.field == "agent.routing.rules[0].adapter"),
src/config/mod.rs:7454:            "expected empty adapter error, got: {:?}",
src/config/mod.rs:7466:                    adapter: "claude-print".to_string(),
src/config/mod.rs:7470:                    adapter: "claude-opus".to_string(),
src/config/mod.rs:7473:            default_adapter: Some("claude-fallback".to_string()),
src/config/mod.rs:7495:        adapter: fable-adapter
src/config/mod.rs:7496:    default_adapter: workspace-default
src/config/mod.rs:7510:        assert_eq!(routing.rules[0].adapter, "fable-adapter");
src/config/mod.rs:7512:            routing.default_adapter.as_deref(),
src/config/mod.rs:7519:    fn env_var_routing_default_adapter_override() {
src/config/mod.rs:7535:                .default_adapter
src/config/mod.rs:7539:        assert!(sources.contains_key("agent.routing.default_adapter"));
src/config/mod.rs:7550:    default_adapter: workspace-fallback
src/config/mod.rs:7567:                .default_adapter
src/config/mod.rs:7584:                .default_adapter
src/config/mod.rs:7589:            sources.get("agent.routing.default_adapter"),
src/config/mod.rs:7601:                    adapter: "adapter1".to_string(),
src/config/mod.rs:7605:                    adapter: String::new(), // Empty adapter
src/config/mod.rs:7609:                    adapter: "adapter3".to_string(),
src/config/mod.rs:7612:            default_adapter: None,
src/config/mod.rs:7636:                    adapter: "adapter-sonnet".to_string(),
src/config/mod.rs:7640:                    adapter: "adapter-opus".to_string(),
src/config/mod.rs:7643:            default_adapter: Some("default-adapter".to_string()),
src/config/mod.rs:7652:        assert_eq!(decoded.rules[0].adapter, routing.rules[0].adapter);
src/config/mod.rs:7653:        assert_eq!(decoded.default_adapter, routing.default_adapter);
src/config/mod.rs:7662:                    adapter: "haiku-adapter".to_string(),
src/config/mod.rs:7664:                default_adapter: Some("fallback".to_string()),
src/config/mod.rs:7677:        assert_eq!(routing.default_adapter.as_deref(), Some("fallback"));
src/config/mod.rs:7688:                    adapter: "sonnet-adapter".to_string(),
src/config/mod.rs:7692:                    adapter: "opus-adapter".to_string(),
src/config/mod.rs:7696:                    adapter: "fable-adapter".to_string(),
src/config/mod.rs:7700:                    adapter: "haiku-adapter".to_string(),
src/config/mod.rs:7703:            default_adapter: Some("default-adapter".to_string()),
src/config/mod.rs:7723:                    adapter: "sonnet-46-adapter".to_string(),
src/config/mod.rs:7727:                    adapter: "opus-48-adapter".to_string(),
src/config/mod.rs:7731:                    adapter: "fable-5-adapter".to_string(),
src/config/mod.rs:7734:            default_adapter: Some("default-adapter".to_string()),
src/config/mod.rs:7754:                    adapter: "haiku-adapter".to_string(),
src/config/mod.rs:7758:                    adapter: "sonnet-adapter".to_string(),
src/config/mod.rs:7761:            default_adapter: Some("claude-code-glm-4.7".to_string()),
src/config/mod.rs:7783:        adapter: claude-print
src/config/mod.rs:7785:        adapter: claude-code-glm-4.7
src/config/mod.rs:7786:    default_adapter: claude-code-glm-4.7
src/config/mod.rs:7794:        assert_eq!(routing.rules[0].adapter, "claude-print");
src/config/mod.rs:7795:        assert_eq!(routing.rules[1].adapter, "claude-code-glm-4.7");
src/config/mod.rs:7797:            routing.default_adapter.as_deref(),
src/config/mod.rs:7831:                    adapter: "first-adapter".to_string(),
src/config/mod.rs:7835:                    adapter: "second-adapter".to_string(),
src/config/mod.rs:7838:            default_adapter: Some("fallback-adapter".to_string()),
src/config/mod.rs:7871:        adapter: workspace-sonnet-adapter
src/config/mod.rs:7872:    default_adapter: workspace-fallback
src/config/mod.rs:7886:        assert_eq!(routing.rules[0].adapter, "workspace-sonnet-adapter");
src/config/mod.rs:7888:            routing.default_adapter.as_deref(),
src/config/mod.rs:7895:        // Test that when no rules match, we fall back to default_adapter
src/config/mod.rs:7899:                adapter: "sonnet-adapter".to_string(),
src/config/mod.rs:7901:            default_adapter: Some("fallback-adapter".to_string()),
src/config/mod.rs:7912:        // Should fall back to default_adapter
src/config/mod.rs:7913:        assert_eq!(routing.default_adapter.as_deref(), Some("fallback-adapter"));
src/config/mod.rs:7922:                adapter: "sonnet-adapter".to_string(),
src/config/mod.rs:7924:            default_adapter: None,
src/config/mod.rs:7966:        // Verify the adapter is claude-print
src/config/mod.rs:7967:        assert_eq!(rule.adapter, "claude-print");
src/config/mod.rs:7971:            routing.default_adapter.as_deref(),
src/config/mod.rs:7978:        // Test that glm-4.7 models use claude-code-glm-4.7 adapter
src/config/mod.rs:7989:            "should not match adapter names"
src/config/mod.rs:7992:        // Should fall back to default_adapter
src/config/mod.rs:7994:            routing.default_adapter.as_deref(),
src/config/mod.rs:8771:        config.agent.adapters_dir = PathBuf::from("~/adapters");
src/config/mod.rs:8785:            config.agent.adapters_dir,
src/config/mod.rs:8786:            PathBuf::from("/home/testuser/adapters")
src/config/mod.rs:8863:        config.agent.adapters_dir = PathBuf::from("relative/adapters");
src/config/mod.rs:8874:            config.agent.adapters_dir,
src/config/mod.rs:8875:            PathBuf::from("relative/adapters")
src/config/mod.rs:9050:        config.agent.adapters_dir = PathBuf::from("~/adapters");
src/config/mod.rs:9068:            config.agent.adapters_dir,
src/config/mod.rs:9069:            PathBuf::from("/home/testuser/adapters")
src/config/mod.rs:9092:        config.agent.adapters_dir = PathBuf::from("~/.needle.d/adapters");
src/config/mod.rs:9103:            config.agent.adapters_dir,
src/config/mod.rs:9104:            PathBuf::from("/home/testuser/.needle.d/adapters")
src/worker/mod.rs:494:        // Phase: Dispatcher setup (adapter loading)
src/worker/mod.rs:502:                tracing::warn!(error = %e, "failed to load adapters, using built-in defaults");
src/worker/mod.rs:503:                let builtins = crate::dispatch::builtin_adapters()
src/worker/mod.rs:507:                Dispatcher::with_adapters(builtins, telemetry.clone(), config.agent.timeout)
src/worker/mod.rs:583:        // Match by qualified identity ({adapter}-{worker_id}).
src/worker/mod.rs:828:            needle.model = %self.config.agent.default, // Will be updated when adapter is resolved
src/worker/mod.rs:1119:            step: "adapter_preflight".to_string(),
src/worker/mod.rs:1122:        // Validate that the configured adapter exists before entering the main claim loop.
src/worker/mod.rs:1123:        // This ensures adapter configuration errors are caught early during startup rather
src/worker/mod.rs:1125:        if let Err(e) = self.resolve_adapter() {
src/worker/mod.rs:1126:            // Provide clear, actionable error message for missing adapter
src/worker/mod.rs:1127:            let adapter_name = &self.config.agent.default;
src/worker/mod.rs:1128:            eprintln!("ERROR: Configured agent adapter '{adapter_name}' was not found.");
src/worker/mod.rs:1129:            eprintln!("Startup is aborting to prevent claiming beads with an invalid adapter configuration.");
src/worker/mod.rs:1131:            eprintln!("To fix this, check one of these locations for your adapter configuration:");
src/worker/mod.rs:1132:            eprintln!("  - ~/.needle/agents/{adapter_name}.yaml");
src/worker/mod.rs:1133:            eprintln!("  - ~/.local/bin/claude-config/agents/{adapter_name}/config.json");
src/worker/mod.rs:1134:            eprintln!("  - ~/.config/needle/adapters/{adapter_name}.yaml");
src/worker/mod.rs:1138:            eprintln!("  - Incorrect adapter name specified in agent.default config");
src/worker/mod.rs:1144:            bail!("adapter '{adapter_name}' not found — startup aborted");
src/worker/mod.rs:1147:            step: "adapter_preflight".to_string(),
src/worker/mod.rs:1428:        // Resolve this worker's default adapter model so remote-workspace
src/worker/mod.rs:1431:        // model is the default adapter's model — routing overrides apply
src/worker/mod.rs:1436:            .adapter(&self.config.agent.default)
src/worker/mod.rs:2089:    /// DISPATCHING: check rate limits, resolve adapter, and prepare for execution.
src/worker/mod.rs:2096:        let adapter = self.resolve_adapter()?;
src/worker/mod.rs:2097:        let provider = adapter.provider.as_deref();
src/worker/mod.rs:2098:        let model = adapter.model.as_deref();
src/worker/mod.rs:2099:        self.health.update_adapter(Some(&adapter.name));
src/worker/mod.rs:2109:            serde_json::json!({"adapter": adapter.name, "model": model}),
src/worker/mod.rs:2119:        self.do_dispatch_inner(adapter)
src/worker/mod.rs:2124:    async fn do_dispatch_inner(&mut self, adapter: crate::dispatch::AgentAdapter) -> Result<()> {
src/worker/mod.rs:2125:        let provider = adapter.provider.as_deref();
src/worker/mod.rs:2126:        let model = adapter.model.as_deref();
src/worker/mod.rs:2181:        let adapter = self.resolve_adapter()?;
src/worker/mod.rs:2253:                    .dispatch(&bead.id, &prompt, &adapter, dispatch_ws)
src/worker/mod.rs:2267:                    &adapter.token_extraction,
src/worker/mod.rs:2314:            dispatch::extract_tokens(&adapter.token_extraction, &output.stdout, &output.stderr);
src/worker/mod.rs:2315:        let model_name = adapter.model.as_deref().unwrap_or("");
src/worker/mod.rs:2319:            effort.agent_name = adapter.name.clone();
src/worker/mod.rs:2320:            effort.model = adapter.model.clone();
src/worker/mod.rs:3716:    /// Fully-qualified worker identity (`{adapter}-{worker_id}`).
src/worker/mod.rs:3719:    /// collisions when workers from different adapter pools share a NATO name.
src/worker/mod.rs:3836:    /// Resolve the agent adapter from config with model-based routing.
src/worker/mod.rs:3837:    fn resolve_adapter(&self) -> Result<crate::dispatch::AgentAdapter> {
src/worker/mod.rs:3840:        // Start with the configured default adapter.
src/worker/mod.rs:3841:        let default_adapter_name = &self.config.agent.default;
src/worker/mod.rs:3843:        // Resolve the default adapter to get its model (if any).
src/worker/mod.rs:3844:        let default_adapter = self
src/worker/mod.rs:3846:            .adapter(default_adapter_name)
src/worker/mod.rs:3850:                    "adapter '{}' not found in any of the expected configuration directories",
src/worker/mod.rs:3851:                    default_adapter_name
src/worker/mod.rs:3856:        let (chosen_adapter_name, matched_rule) = self.apply_routing_rules(&default_adapter)?;
src/worker/mod.rs:3860:            let model = default_adapter
src/worker/mod.rs:3868:                chosen_adapter: chosen_adapter_name.clone(),
src/worker/mod.rs:3872:        // Resolve the chosen adapter.
src/worker/mod.rs:3873:        let adapter = self.dispatcher.adapter(&chosen_adapter_name)
src/worker/mod.rs:3876:                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
src/worker/mod.rs:3877:                chosen_adapter_name,
src/worker/mod.rs:3878:                default_adapter.model.as_deref().unwrap_or("unknown"),
src/worker/mod.rs:3882:        Ok(adapter)
src/worker/mod.rs:3885:    /// Apply routing rules to determine the final adapter.
src/worker/mod.rs:3887:    /// Returns (chosen_adapter_name, matched_rule_pattern).
src/worker/mod.rs:3888:    /// If no routing rules match, returns (default_adapter_name, "default").
src/worker/mod.rs:3892:        default_adapter: &crate::dispatch::AgentAdapter,
src/worker/mod.rs:3896:            _ => return Ok((default_adapter.name.clone(), "default".to_string())),
src/worker/mod.rs:3899:        let model_name = default_adapter.model.as_deref().unwrap_or("");
src/worker/mod.rs:3901:        // Determine the default adapter name to use.
src/worker/mod.rs:3902:        let default_adapter_name = routing_config
src/worker/mod.rs:3903:            .default_adapter
src/worker/mod.rs:3905:            .unwrap_or(&default_adapter.name);
src/worker/mod.rs:3907:        // Use the routing module's match_adapter_with_pattern function to get both adapter and pattern.
src/worker/mod.rs:3908:        match routing::match_adapter_with_pattern(
src/worker/mod.rs:3911:            default_adapter_name,
src/worker/mod.rs:3913:            Some((adapter_name, matched_pattern)) => {
src/worker/mod.rs:3917:                    // No rule matched - using default adapter.
src/worker/mod.rs:3930:                            "no routing rule matched model '{}' — add a rule to agent.routing.rules or set routing.strict: false to fall back to the default adapter",
src/worker/mod.rs:3936:                    if routing_config.default_adapter.is_some() {
src/worker/mod.rs:3949:                    adapter = %adapter_name,
src/worker/mod.rs:3952:                Ok((adapter_name, final_pattern.to_string()))
src/worker/mod.rs:3955:                // No rule matched and no default adapter available.
src/worker/mod.rs:3968:                        "no routing rule matched model '{}' and no default adapter available — add a rule to agent.routing.rules or set routing.strict: false to fall back to the default adapter",
src/worker/mod.rs:3973:                // Strict mode disabled but no default adapter - this shouldn't happen with valid config.
src/worker/mod.rs:3976:                    "no routing rule matched and no default adapter available, but strict mode is disabled"
src/worker/mod.rs:3978:                Ok((default_adapter_name.to_string(), "default".to_string()))
src/worker/mod.rs:4036:    /// Resolve the provider name from the configured adapter.
src/worker/mod.rs:4038:        let adapter_name = &self.config.agent.default;
src/worker/mod.rs:4040:            .adapter(adapter_name)
src/worker/mod.rs:4105:    /// Replace the dispatcher (for testing with custom adapters).
src/worker/mod.rs:4330:    /// Return a configuration whose selected adapter is one of NEEDLE's
src/worker/mod.rs:4332:    /// operator-provided `claude` adapter, which is absent in isolated CI.
src/worker/mod.rs:4339:        // the fleet and the other tests sharing this worker id. `adapters_dir`
src/worker/mod.rs:4340:        // is pinned alongside it so adapter resolution sees only built-ins and
src/worker/mod.rs:4343:        config.agent.adapters_dir = home.join("adapters");
src/worker/mod.rs:4347:        // `claude-code-glm-4.7` — both operator-provided adapters that exist
src/worker/mod.rs:4348:        // only in the real `~/.config/needle/adapters`. Leaving them on makes
src/worker/mod.rs:4351:        // adapter routing actually selected. Tests that exercise routing build
src/worker/mod.rs:4409:    async fn resolve_adapter_returns_builtin() {
src/worker/mod.rs:4412:        let result = worker.resolve_adapter();
src/worker/mod.rs:4414:        // routing, so resolution must succeed without consulting any adapter
src/worker/mod.rs:4417:        // request to `claude-print` — an adapter that is not built in.
src/worker/mod.rs:4418:        let adapter = result.expect("built-in adapter should resolve");
828:            needle.model = %self.config.agent.default, // Will be updated when adapter is resolved
1050:            model: None,
1140:                "  - Adapter file exists but is missing required fields (provider, model, etc.)"
1428:        // Resolve this worker's default adapter model so remote-workspace
1431:        // model is the default adapter's model — routing overrides apply
1434:        let model = self
1437:            .and_then(|a| a.model.clone());
1455:            model,
1699:                    model: None,
2098:        let model = adapter.model.as_deref();
2109:            serde_json::json!({"adapter": adapter.name, "model": model}),
2114:            gen_ai.request.model = %model.unwrap_or("unknown"),
2126:        let model = adapter.model.as_deref();
2128:        let decision = self.rate_limiter.check(provider, model, &self.registry)?;
2138:                model: model.map(|s| s.to_string()),
2151:            model: model.map(|s| s.to_string()),
2315:        let model_name = adapter.model.as_deref().unwrap_or("");
2316:        let estimated_cost = cost::estimate_cost(&tokens, model_name, &self.config.pricing);
2320:            effort.model = adapter.model.clone();
2968:                model: effort.model.clone(),
2979:                    model = ?effort.model,
3836:    /// Resolve the agent adapter from config with model-based routing.
3843:        // Resolve the default adapter to get its model (if any).
3860:            let model = default_adapter
3861:                .model
3866:                model,
3876:                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
3878:                default_adapter.model.as_deref().unwrap_or("unknown"),
3899:        let model_name = default_adapter.model.as_deref().unwrap_or("");
3909:            model_name,
3925:                                model: model_name.to_string(),
3930:                            "no routing rule matched model '{}' — add a rule to agent.routing.rules or set routing.strict: false to fall back to the default adapter",
3931:                            model_name
3947:                    model = %model_name,
3963:                            model: model_name.to_string(),
3968:                        "no routing rule matched model '{}' and no default adapter available — add a rule to agent.routing.rules or set routing.strict: false to fall back to the default adapter",
3969:                        model_name
3975:                    model = %model_name,
4346:        // haiku model to `claude-print` and otherwise fall back to
5453:            model: None,
5587:        config.agent.routing = None; // test dispatcher only has echo-test; disable model-based routing
5614:            model: None,
5702:                match_model: "sonnet".to_string(),
5708:        // Set up a model that won't match the rule.
5722:            model: Some("gpt-4o".to_string()),
5733:            err_msg.contains("no routing rule matched model 'gpt-4o'"),
5734:            "error message should mention the model: {}",
5753:                match_model: "sonnet".to_string(),
5772:            model: Some("gpt-4o".to_string()),
5796:                match_model: "claude-.*".to_string(),
5815:            model: Some("claude-sonnet-4-6".to_string()),
5839:                match_model: "sonnet".to_string(),
5858:            model: Some("gpt-4o".to_string()),
5874:    async fn default_routing_rules_anthropic_subscription_models() {
5875:        // Verify that default routing rules route Anthropic subscription models to claude-print.
5893:            default_routing.rules[0].match_model, "(claude-)?(sonnet|opus|fable|haiku).*",
5894:            "first rule should match Anthropic subscription models"
5910:        // Test routing with actual Anthropic subscription model names.
5911:        let anthropic_models = vec![
5922:        for model_name in anthropic_models {
5925:                format!("test-routing-{}", model_name),
5940:                model: Some(model_name.to_string()),
5950:                "routing should succeed for model {}: {:?}",
5951:                model_name,
5957:                "Anthropic subscription model {} should route to claude-print, got {}",
5958:                model_name, chosen_adapter
5962:                "model {} should match a routing rule, not fall back to default",
5963:                model_name
5967:        // Test that non-Anthropic models route to claude-code-glm-4.7.
5968:        let non_anthropic_models = vec![
5975:        for model_name in non_anthropic_models {
5978:                format!("test-routing-{}", model_name),
5993:                model: Some(model_name.to_string()),
6003:                "routing should succeed for model {}: {:?}",
6004:                model_name,
6010:                "Non-subscription model {} should route to claude-code-glm-4.7, got {}",
6011:                model_name, chosen_adapter
6015:                "non-matching model {} should use routing-default adapter",
6016:                model_name
6023:        // Test that when multiple rules match the same model, the first rule wins.
6035:                    match_model: "claude-sonnet-.*".to_string(),
6039:                    match_model: "claude-.*".to_string(),
6064:            model: Some("claude-sonnet-4-6".to_string()),
6101:                    match_model: "claude-.*".to_string(),
6105:                    match_model: "claude-sonnet-.*".to_string(),
6130:            model: Some("claude-sonnet-4-6".to_string()),
6158:        /// both match the same model AND route to the same adapter.
6175:                    match_model: "claude-.*".to_string(),
6179:                    match_model: "claude-sonnet-.*".to_string(),
6205:            model: Some("claude-sonnet-4-6".to_string()),
6232:        /// all match the same model.
6249:                    match_model: "claude-.*".to_string(),
6253:                    match_model: "claude-sonnet-.*".to_string(),
6257:                    match_model: "claude-sonnet-4-.*".to_string(),
6283:            model: Some("claude-sonnet-4-6".to_string()),
6331:                    match_model: "claude-.*".to_string(),
6335:                    match_model: "[invalid(regex".to_string(),
6339:                    match_model: "claude-sonnet-.*".to_string(),
6365:            model: Some("claude-sonnet-4-6".to_string()),
6416:                    match_model: "^claude-sonnet-4-6$".to_string(),
6420:                    match_model: "claude-.*".to_string(),
6424:                    match_model: "*".to_string(),
6450:            model: Some("claude-sonnet-4-6".to_string()),

exec
/bin/bash -lc "sed -n '450,525p' src/worker/mod.rs; sed -n '2080,2345p' src/worker/mod.rs; sed -n '3810,4005p' src/worker/mod.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
            step: "strand_setup".to_string(),
            duration_ms: strand_start.elapsed().as_millis() as u64,
        });

        // Phase: Claimer creation
        let _ = telemetry.emit(EventKind::InitStepStarted {
            step: "claimer_creation".to_string(),
        });
        let claimer_start = Instant::now();
        let claimer = Claimer::new(
            store.clone(),
            std::path::PathBuf::from("/tmp"),
            config.worker.max_claim_retries,
            100,
            telemetry.clone(),
        );
        let _ = telemetry.emit(EventKind::InitStepCompleted {
            step: "claimer_creation".to_string(),
            duration_ms: claimer_start.elapsed().as_millis() as u64,
        });

        // Phase: PromptBuilder setup
        let _ = telemetry.emit(EventKind::InitStepStarted {
            step: "prompt_builder_setup".to_string(),
        });
        let prompt_start = Instant::now();
        let prompt_builder = PromptBuilder::with_workspace(
            &config.prompt,
            &config.workspace.default,
        )
        .unwrap_or_else(|e| {
            tracing::warn!(error = %e, "failed to load workspace learnings, using default prompt builder");
            PromptBuilder::new(&config.prompt)
        })
        .with_cross_workspace_skills(
            &config.strands.explore.workspaces,
            &config.workspace.labels,
        )
        .with_global_learnings(&config.strands.learning.global_learnings_file);
        let _ = telemetry.emit(EventKind::InitStepCompleted {
            step: "prompt_builder_setup".to_string(),
            duration_ms: prompt_start.elapsed().as_millis() as u64,
        });

        // Phase: Dispatcher setup (adapter loading)
        let _ = telemetry.emit(EventKind::InitStepStarted {
            step: "dispatcher_setup".to_string(),
        });
        let dispatcher_start = Instant::now();
        let dispatcher = match Dispatcher::new(&config, telemetry.clone()) {
            Ok(d) => d,
            Err(e) => {
                tracing::warn!(error = %e, "failed to load adapters, using built-in defaults");
                let builtins = crate::dispatch::builtin_adapters()
                    .into_iter()
                    .map(|a| (a.name.clone(), a))
                    .collect();
                Dispatcher::with_adapters(builtins, telemetry.clone(), config.agent.timeout)
            }
        };
        let _ = telemetry.emit(EventKind::InitStepCompleted {
            step: "dispatcher_setup".to_string(),
            duration_ms: dispatcher_start.elapsed().as_millis() as u64,
        });

        // Phase: OutcomeHandler creation
        let _ = telemetry.emit(EventKind::InitStepStarted {
            step: "outcome_handler_creation".to_string(),
        });
        let outcome_start = Instant::now();
        let outcome_handler = OutcomeHandler::new(config.clone(), telemetry.clone());
        let _ = telemetry.emit(EventKind::InitStepCompleted {
            step: "outcome_handler_creation".to_string(),
            duration_ms: outcome_start.elapsed().as_millis() as u64,
        });

        );

        // Store the prompt for the dispatch phase. We use a transient field pattern:
        // the prompt is passed via self.built_prompt.
        self.built_prompt = Some(prompt);
        self.set_state(WorkerState::Dispatching)?;
        Ok(())
    }

    /// DISPATCHING: check rate limits, resolve adapter, and prepare for execution.
    async fn do_dispatch(&mut self) -> Result<()> {
        if self.current_bead.is_none() {
            bail!("DISPATCHING state without current_bead — invariant violated");
        }

        // Check rate limits before dispatching.
        let adapter = self.resolve_adapter()?;
        let provider = adapter.provider.as_deref();
        let model = adapter.model.as_deref();
        self.health.update_adapter(Some(&adapter.name));

        // Enter the agent.dispatch span for the dispatching phase.
        let _bead_id = self.current_bead.as_ref().map(|b| b.id.clone());
        crate::hoop_hooks::emit_needle_event(
            &self.current_workspace,
            &self.worker_name,
            _bead_id.as_ref().map(|id| id.as_ref()),
            self.current_strand.as_deref(),
            "dispatch",
            serde_json::json!({"adapter": adapter.name, "model": model}),
        );
        let dispatch_span = tracing::info_span!(
            "agent.dispatch",
            gen_ai.system = %provider.unwrap_or("unknown"),
            gen_ai.request.model = %model.unwrap_or("unknown"),
            needle.agent.pid = tracing::field::Empty, // Will be set when process starts
            needle.agent.exit_code = tracing::field::Empty, // Will be set after execution
        );

        self.do_dispatch_inner(adapter)
            .instrument(dispatch_span)
            .await
    }

    async fn do_dispatch_inner(&mut self, adapter: crate::dispatch::AgentAdapter) -> Result<()> {
        let provider = adapter.provider.as_deref();
        let model = adapter.model.as_deref();

        let decision = self.rate_limiter.check(provider, model, &self.registry)?;

        if !decision.is_allowed() {
            let reason = format!("{decision}");
            tracing::info!(
                reason = %reason,
                "rate limited, waiting before retry"
            );
            self.telemetry.emit(EventKind::RateLimitWait {
                provider: provider.unwrap_or("unknown").to_string(),
                model: model.map(|s| s.to_string()),
                reason: reason.clone(),
            })?;

            // Wait before retrying (5 seconds).
            tokio::time::sleep(std::time::Duration::from_secs(5)).await;

            // Stay in Dispatching state to retry the rate limit check.
            return Ok(());
        }

        self.telemetry.emit(EventKind::RateLimitAllowed {
            provider: provider.unwrap_or("unknown").to_string(),
            model: model.map(|s| s.to_string()),
        })?;

        // Check system resources (CPU and memory warnings).
        crate::rate_limit::RateLimiter::check_system_resources(
            self.config.worker.cpu_load_warn,
            self.config.worker.memory_free_warn_mb,
            &self.telemetry,
        );

        self.set_state(WorkerState::Executing)?;
        Ok(())
    }

    /// EXECUTING: run the agent process and capture output.
    async fn do_execute(&mut self) -> Result<()> {
        let bead = match self.current_bead {
            Some(ref b) => b.clone(),
            None => {
                bail!("EXECUTING state without current_bead — invariant violated");
            }
        };

        let prompt = match self.built_prompt.take() {
            Some(p) => p,
            None => {
                bail!("EXECUTING state without built_prompt — invariant violated");
            }
        };

        let adapter = self.resolve_adapter()?;

        // Use the bead's workspace if set (remote bead from Explore),
        // otherwise fall back to the config's default workspace.
        let dispatch_ws = if is_workspace_unset(&bead.workspace) {
            &self.config.workspace.default
        } else {
            &bead.workspace
        };

        // Race the dispatch against the shutdown signal.
        let was_interrupted;
        let exec_result = if self.shutdown.load(Ordering::SeqCst) {
            // Already shutting down — don't start the agent.
            was_interrupted = true;
            None
        } else {
            // Capture HEAD so do_handle can tag new commits with Bead-Id on success.
            // Wrap in timeout to prevent indefinite hang if git subprocess hangs.
            match tokio::time::timeout(
                std::time::Duration::from_secs(10),
                commit_hook::git_head(dispatch_ws.to_str().unwrap_or(".")),
            )
            .await
            {
                Ok(Ok(head)) => {
                    self.pre_dispatch_head = Some(head);
                }
                Ok(Err(e)) => {
                    tracing::debug!(
                        workspace = %dispatch_ws.display(),
                        error = %e,
                        "git_head failed (not a git repo or git error), skipping Bead-Id trailer"
                    );
                }
                Err(_) => {
                    tracing::warn!(
                        workspace = %dispatch_ws.display(),
                        "git_head timed out after 10s, skipping Bead-Id trailer"
                    );
                }
            }
            // Enter the agent.execution span for the actual agent process execution.
            // This is a child of agent.dispatch.
            // We need to record attributes on the parent agent.dispatch span after execution,
            // so we use a scope to drop the execution_span guard first.
            let execution_span = tracing::info_span!(
                "agent.execution",
                needle.bead.id = %bead.id,
            );
            let (result, exec_tokens) = async {
                // Snapshot workspace HEAD + the bead's notes before the agent
                // runs, so the shipped-work gate has a baseline to judge the
                // closure against. Best-effort: a missing snapshot degrades the
                // gate to its conservative path, it never blocks dispatch.
                if let Err(e) = crate::validation::predispatch::record(
                    dispatch_ws,
                    &bead.id,
                    self.store.as_ref(),
                )
                .await
                {
                    tracing::warn!(
                        bead_id = %bead.id,
                        error = %e,
                        "failed to record pre-dispatch snapshot — shipped-work gate will fall back"
                    );
                }

                self.exec_started_at = Some(Instant::now());
                let result = self
                    .dispatcher
                    .dispatch(&bead.id, &prompt, &adapter, dispatch_ws)
                    .await?;

                // Set span status based on exit code: 0 = Ok, non-zero = Error
                if result.exit_code != 0 {
                    tracing::Span::current().record("otel.status_code", 2u64);
                    tracing::Span::current().record(
                        "otel.status_description",
                        format!("exit_code: {}", result.exit_code),
                    );
                }

                // Extract tokens from the result while still in the execution span.
                let exec_tokens = dispatch::extract_tokens(
                    &adapter.token_extraction,
                    &result.stdout,
                    &result.stderr,
                );
                Ok::<_, anyhow::Error>((result, exec_tokens))
            }
            .instrument(execution_span)
            .await?;

            // Now we're back in the agent.dispatch span. Record the execution results.
            tracing::Span::current().record("needle.agent.pid", result.pid);
            tracing::Span::current().record("needle.agent.exit_code", result.exit_code);
            if let Some(input_tokens) = exec_tokens.input_tokens {
                tracing::Span::current().record("gen_ai.usage.input_tokens", input_tokens);
            }
            if let Some(output_tokens) = exec_tokens.output_tokens {
                tracing::Span::current().record("gen_ai.usage.output_tokens", output_tokens);
            }

            // Set agent.dispatch span status based on exit code: 0 = Ok, non-zero = Error
            if result.exit_code != 0 {
                tracing::Span::current().record("otel.status_code", 2u64);
                tracing::Span::current().record(
                    "otel.status_description",
                    format!("exit_code: {}", result.exit_code),
                );
            }

            was_interrupted = self.shutdown.load(Ordering::SeqCst);
            Some(result)
        };

        let output = match exec_result {
            Some(ref result) => AgentOutcome {
                exit_code: result.exit_code,
                stdout: result.stdout.clone(),
                stderr: result.stderr.clone(),
            },
            None => AgentOutcome {
                exit_code: 130, // Simulated SIGINT
                stdout: String::new(),
                stderr: "interrupted before execution".to_string(),
            },
        };

        // Extract tokens and compute cost for effort tracking.
        let tokens =
            dispatch::extract_tokens(&adapter.token_extraction, &output.stdout, &output.stderr);
        let model_name = adapter.model.as_deref().unwrap_or("");
        let estimated_cost = cost::estimate_cost(&tokens, model_name, &self.config.pricing);

        if let Some(ref mut effort) = self.last_effort {
            effort.agent_name = adapter.name.clone();
            effort.model = adapter.model.clone();
            effort.tokens = tokens;
            effort.estimated_cost_usd = estimated_cost;
        }

        // Write trace files for stdout and stderr.
        // Errors are logged but don't fail the bead cycle — the output is
        // still available in exec_output for the outcome handler.
        if let Err(e) =
            self.write_trace_files(&bead.id, dispatch_ws, &output.stdout, &output.stderr)
        {
            tracing::warn!(
                bead_id = %bead.id,
                error = %e,
                "failed to write trace files, continuing with normal flow"
            );
        }

        self.exec_output = Some((output, was_interrupted));
        self.set_state(WorkerState::Handling)?;
        Ok(())
    }

    /// Write captured stdout and stderr to trace files.
    ///
    /// Creates `.beads/traces/<bead-id>/` directory and writes:
                        Some(self.config.workspace.default.as_path())
                    } else {
                        Some(self.current_workspace.as_path())
                    }
                }
            }
            _ => {
                // For other non-bead-processing states, use tracked workspace or home
                if is_workspace_unset(&self.current_workspace) {
                    Some(self.config.workspace.default.as_path())
                } else {
                    Some(self.current_workspace.as_path())
                }
            }
        };
        self.health
            .update_state(&to, current_bead_id, current_workspace);
        // Sync current_workspace from the shared state so subsequent heartbeats
        // use the correct workspace during cross-workspace work.
        if let Some(ws) = current_workspace {
            self.current_workspace = ws.to_path_buf();
        }
        self.state = to;
        Ok(())
    }

    /// Resolve the agent adapter from config with model-based routing.
    fn resolve_adapter(&self) -> Result<crate::dispatch::AgentAdapter> {
        let bead_id = self.current_bead.as_ref().map(|b| b.id.clone());

        // Start with the configured default adapter.
        let default_adapter_name = &self.config.agent.default;

        // Resolve the default adapter to get its model (if any).
        let default_adapter = self
            .dispatcher
            .adapter(default_adapter_name)
            .cloned()
            .ok_or_else(|| {
                anyhow::anyhow!(
                    "adapter '{}' not found in any of the expected configuration directories",
                    default_adapter_name
                )
            })?;

        // Apply routing rules if configured.
        let (chosen_adapter_name, matched_rule) = self.apply_routing_rules(&default_adapter)?;

        // Emit routing decision telemetry on every routing decision.
        if let Some(id) = bead_id {
            let model = default_adapter
                .model
                .clone()
                .unwrap_or_else(|| "unknown".to_string());
            self.telemetry.emit(EventKind::RoutingDecision {
                bead_id: id,
                model,
                matched_rule: matched_rule.clone(),
                chosen_adapter: chosen_adapter_name.clone(),
            })?;
        }

        // Resolve the chosen adapter.
        let adapter = self.dispatcher.adapter(&chosen_adapter_name)
            .cloned()
            .ok_or_else(|| anyhow::anyhow!(
                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
                chosen_adapter_name,
                default_adapter.model.as_deref().unwrap_or("unknown"),
                matched_rule
            ))?;

        Ok(adapter)
    }

    /// Apply routing rules to determine the final adapter.
    ///
    /// Returns (chosen_adapter_name, matched_rule_pattern).
    /// If no routing rules match, returns (default_adapter_name, "default").
    /// When strict mode is enabled and no rule matches, returns an error.
    fn apply_routing_rules(
        &self,
        default_adapter: &crate::dispatch::AgentAdapter,
    ) -> Result<(String, String)> {
        let routing_config = match &self.config.agent.routing {
            Some(r) if !r.rules.is_empty() => r,
            _ => return Ok((default_adapter.name.clone(), "default".to_string())),
        };

        let model_name = default_adapter.model.as_deref().unwrap_or("");

        // Determine the default adapter name to use.
        let default_adapter_name = routing_config
            .default_adapter
            .as_ref()
            .unwrap_or(&default_adapter.name);

        // Use the routing module's match_adapter_with_pattern function to get both adapter and pattern.
        match routing::match_adapter_with_pattern(
            model_name,
            &routing_config.rules,
            default_adapter_name,
        ) {
            Some((adapter_name, matched_pattern)) => {
                // Determine if this was a rule match or default fallback.
                // If matched_pattern is "default", it means no rule matched.
                let final_pattern = if matched_pattern == "default" {
                    // No rule matched - using default adapter.
                    // Check if strict mode is enabled - if so, fail.
                    if routing_config.strict {
                        let bead_id = self.current_bead.as_ref().map(|b| b.id.clone());
                        if let Some(id) = bead_id {
                            // Emit RoutingFailed telemetry event.
                            let _ = self.telemetry.emit(EventKind::RoutingFailed {
                                bead_id: id,
                                model: model_name.to_string(),
                                rules_tried: routing_config.rules.len() as u32,
                            });
                        }
                        bail!(
                            "no routing rule matched model '{}' — add a rule to agent.routing.rules or set routing.strict: false to fall back to the default adapter",
                            model_name
                        );
                    }

                    // Strict mode disabled - distinguish between routing default and original default.
                    if routing_config.default_adapter.is_some() {
                        "routing-default"
                    } else {
                        "default"
                    }
                } else {
                    // A rule actually matched - use the pattern from the rule.
                    &matched_pattern
                };

                tracing::debug!(
                    model = %model_name,
                    pattern = %final_pattern,
                    adapter = %adapter_name,
                    "routing decision completed"
                );
                Ok((adapter_name, final_pattern.to_string()))
            }
            None => {
                // No rule matched and no default adapter available.
                // If strict mode is enabled, fail with a clear error.
                if routing_config.strict {
                    let bead_id = self.current_bead.as_ref().map(|b| b.id.clone());
                    if let Some(id) = bead_id {
                        // Emit RoutingFailed telemetry event.
                        let _ = self.telemetry.emit(EventKind::RoutingFailed {
                            bead_id: id,
                            model: model_name.to_string(),
                            rules_tried: routing_config.rules.len() as u32,
                        });
                    }
                    bail!(
                        "no routing rule matched model '{}' and no default adapter available — add a rule to agent.routing.rules or set routing.strict: false to fall back to the default adapter",
                        model_name
                    );
                }

                // Strict mode disabled but no default adapter - this shouldn't happen with valid config.
                tracing::warn!(
                    model = %model_name,
                    "no routing rule matched and no default adapter available, but strict mode is disabled"
                );
                Ok((default_adapter_name.to_string(), "default".to_string()))
            }
        }
    }

    /// Check daily budget and emit appropriate telemetry / trigger shutdown.
    fn check_budget(&mut self) -> Result<()> {
        // Skip if no budget configured.
        if self.config.budget.warn_usd <= 0.0 && self.config.budget.stop_usd <= 0.0 {
            return Ok(());
        }

        // Resolve log directory for scanning.
        let log_dir = self
            .config
            .telemetry
            .file_sink
            .log_dir
            .clone()
            .unwrap_or_else(|| self.config.workspace.home.join("logs"));
        let daily_cost = cost::scan_daily_cost(&log_dir);

        match cost::check_budget(daily_cost, &self.config.budget) {
            BudgetCheck::Ok => {}
            BudgetCheck::Warn {
                daily_cost,
                threshold,
            } => {

exec
/bin/bash -lc "sed -n '1,180p' src/worker/mod.rs; rg -n \"fn valid_test_config|fn config_for_tests|adapters_dir|set_dispatcher|test_adapter\" src/worker/mod.rs | sed -n '1,180p'; sed -n '4280,4445p' src/worker/mod.rs; sed -n '5400,5665p' src/worker/mod.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
//! Worker loop: the core NEEDLE state machine.
//!
//! The Worker executes the strand waterfall, claims beads, dispatches the
//! agent, handles outcomes, and emits telemetry for every transition.
//!
//! State transitions are explicit — there is no implicit fallthrough and no
//! state that does not have a defined handler. The worker emits telemetry for
//! every transition.
//!
//! Depends on: `strand`, `claim`, `prompt`, `dispatch`, `outcome`,
//!             `bead_store`, `telemetry`, `health`, `config`, `types`.

use std::collections::HashSet;
use std::fs;
use std::hash::{Hash, Hasher};
use std::path::PathBuf;
use std::sync::atomic::{AtomicBool, Ordering};
use std::sync::Arc;
use std::sync::Mutex;
use std::time::{Duration, Instant};

use anyhow::{bail, Context, Result};
// Needed for `.instrument()` — attaches a span to a future instead of holding an
// `Entered` guard across `.await`, which is unsound and leaked spans (bf-3uj6i).
use tracing::Instrument;

#[cfg(unix)]
use std::sync::atomic::{AtomicU32, AtomicUsize, Ordering as AtomicOrdering};

use crate::bead_store::BeadStore;
use crate::canary::CanaryRunner;
use crate::claim::Claimer;
use crate::commit_hook;
use crate::config::{Config, ConfigLoader};
use crate::cost::{self, BudgetCheck, EffortData};
use crate::dispatch::{self, Dispatcher};
use crate::health::HealthMonitor;
use crate::mitosis::{detects_needle_internal_config, MitosisEvaluator};
use crate::outcome::OutcomeHandler;
use crate::prompt::{BuiltPrompt, PromptBuilder};
use crate::rate_limit::RateLimiter;
use crate::registry::{Registry, WorkerEntry};
use crate::routing;
use crate::strand::StrandRunner;
use crate::telemetry::{EventKind, Telemetry};
use crate::types::{AgentOutcome, Bead, BeadId, ClaimResult, IdleAction, Outcome, WorkerState};
use crate::upgrade::{self, HotReloadCheck};

// ──────────────────────────────────────────────────────────────────────────────
// Global shutdown flag for signal handlers
// ──────────────────────────────────────────────────────────────────────────────

/// Global pointer to the shutdown flag, used by synchronous signal handlers.
/// This is necessary because signal handlers run in a separate context and
/// cannot easily access the Worker's shutdown flag directly.
#[cfg(unix)]
static GLOBAL_SHUTDOWN_FLAG: AtomicUsize = AtomicUsize::new(0);

/// Set the global shutdown flag pointer.
/// Called by `install_signal_handlers` to register the shutdown flag.
#[cfg(unix)]
fn set_global_shutdown_flag(ptr: usize) {
    GLOBAL_SHUTDOWN_FLAG.store(ptr, AtomicOrdering::SeqCst);
}

/// Clear the global shutdown flag pointer.
/// Called when the worker is dropped to avoid dangling pointers.
#[cfg(unix)]
fn clear_global_shutdown_flag() {
    GLOBAL_SHUTDOWN_FLAG.store(0, AtomicOrdering::SeqCst);
    LAST_SIGNAL.store(0, AtomicOrdering::SeqCst);
}

/// Track the last received signal for diagnostic logging.
/// AtomicU32 allows lock-free reads/writes from the signal handler.
#[cfg(unix)]
static LAST_SIGNAL: AtomicU32 = AtomicU32::new(0);

/// Synchronous signal handler for SIGTERM, SIGINT, and SIGHUP.
///
/// This function is called directly by the OS when a signal is received.
/// It must be async-signal-safe: no allocation, no locking, no I/O.
/// We set the atomic shutdown flag, record the signal number, and return immediately.
#[cfg(unix)]
extern "C" fn signal_handler(sig: i32) {
    // SAFETY: The signal handler is only installed after set_global_shutdown_flag
    // has been called with a valid pointer. The pointer remains valid for the
    // entire lifetime of the worker process.
    let ptr = GLOBAL_SHUTDOWN_FLAG.load(AtomicOrdering::SeqCst) as *const AtomicBool;
    if !ptr.is_null() {
        // SAFETY: The pointer is valid and points to an AtomicBool that lives
        // for the entire program duration.
        unsafe {
            (*ptr).store(true, AtomicOrdering::SeqCst);
        }
        // Record the signal number so the main loop can log it.
        LAST_SIGNAL.store(sig as u32, AtomicOrdering::SeqCst);
    }
}

/// Install synchronous signal handlers for SIGTERM, SIGINT, and SIGHUP.
///
/// Uses libc::sigaction to register handlers that set the shutdown flag
/// immediately when a signal is received. This ensures that signals are
/// caught even if the tokio runtime hasn't polled async signal tasks yet.
#[cfg(unix)]
unsafe fn install_unix_signal_handlers() {
    use libc::{sigaction, sigemptyset, SA_RESTART, SIGHUP, SIGINT, SIGTERM};

    // Set up the sigaction structure.
    let mut act: libc::sigaction = std::mem::zeroed();
    act.sa_sigaction = signal_handler as *const () as usize;
    // Block all signals during handler execution to prevent re-entrancy issues.
    sigemptyset(&mut act.sa_mask as *mut libc::sigset_t);
    // Use SA_RESTART to automatically restart system calls interrupted by signals.
    act.sa_flags = SA_RESTART;

    // Install handlers for SIGTERM, SIGINT, and SIGHUP.
    // We ignore errors here - if a handler can't be installed, we'll log a
    // warning but continue. The async handlers (below) provide a fallback.
    for &sig in &[SIGTERM, SIGINT, SIGHUP] {
        let mut old: libc::sigaction = std::mem::zeroed();
        if sigaction(sig, &act, &mut old) == 0 {
            tracing::debug!(signal = sig, "installed synchronous signal handler");
        } else {
            // Log the error but don't fail - the async handlers provide a fallback.
            #[cfg(target_os = "linux")]
            let errno = unsafe { *libc::__errno_location() };
            #[cfg(target_os = "macos")]
            let errno = unsafe { *libc::__error() };
            tracing::warn!(
                signal = sig,
                errno = errno,
                "failed to install synchronous signal handler"
            );
        }
    }
}

/// Global state for atexit handler to emit worker.stopped on unexpected termination.
///
/// When a worker is killed externally (e.g., SIGKILL from capacity governor),
/// the normal signal handlers don't run. The atexit handler provides a last-resort
/// mechanism to emit worker.stopped telemetry with diagnostic information.
static ATEXIT_WORKER_STATE: Mutex<Option<AtexitWorkerState>> = Mutex::new(None);

/// State captured for the atexit handler.
struct AtexitWorkerState {
    worker_name: String,
    beads_processed: u64,
    start_time: Instant,
    last_state: String,
    log_file_path: Option<String>,
    /// Path to this worker's heartbeat file for cleanup on unexpected termination.
    heartbeat_path: Option<String>,
}

/// Register the atexit handler with worker state.
///
/// Called by `install_signal_handlers` to ensure the atexit handler can
/// emit meaningful telemetry if the process terminates unexpectedly.
fn register_atexit_handler(
    worker_name: String,
    beads_processed: u64,
    start_time: Instant,
    last_state: String,
    log_file_path: Option<String>,
    heartbeat_path: Option<String>,
) {
    let state = AtexitWorkerState {
        worker_name,
        beads_processed,
        start_time,
        last_state,
        log_file_path,
        heartbeat_path,
    };
    *ATEXIT_WORKER_STATE.lock().unwrap() = Some(state);

    // Register the atexit handler.
4106:    pub fn set_dispatcher(&mut self, dispatcher: Dispatcher) {
4333:    fn valid_test_config() -> Config {
4339:        // the fleet and the other tests sharing this worker id. `adapters_dir`
4343:        config.agent.adapters_dir = home.join("adapters");
        async fn clear_assignee(&self, _id: &BeadId) -> Result<()> {
            Ok(())
        }

        fn has_valid_store(&self) -> bool {
            true // Mock store always has a valid store
        }
    }

    fn make_test_bead(id: &str) -> Bead {
        Bead {
            id: BeadId::from(id),
            title: format!("Test bead {id}"),
            body: Some("Do the thing".to_string()),
            priority: 1,
            status: BeadStatus::Open,
            assignee: None,
            labels: vec![],
            workspace: std::path::PathBuf::from("/tmp/test-workspace"),
            dependencies: vec![],
            dependents: vec![],
            comments: vec![],
            created_at: chrono::Utc::now(),
            updated_at: chrono::Utc::now(),
        }
    }

    fn make_worker(store: Arc<dyn BeadStore>) -> Worker {
        let mut config = valid_test_config();
        // Disable hot-reload in tests — it would re-exec into a different binary.
        config.self_modification.hot_reload = false;
        // Disable Explore: it defaults to scanning the real $HOME for any
        // `.beads/` directory and operating on whatever it finds via the real
        // `bf`/`br` CLI. This shared helper is used by unit tests that never
        // intend to exercise real filesystem discovery (that's what
        // strand::explore::tests own, with its own tempdir isolation) — a
        // multi-cycle test using this helper (e.g. via `.run()`) previously
        // reached a real Explore scan once its MockStore emptied out,
        // claiming/mutating real beads across unrelated repos on this
        // server. See bf-2unnq's contamination addendum.
        config.strands.explore.enabled = false;
        // Pin Explore scan root to a tempdir so the Explore strand cannot
        // scan the real home directory if it ever runs. Even though Explore
        // is disabled above, defense-in-depth ensures tests are isolated.
        let temp_dir = tempfile::tempdir().unwrap();
        config.strands.explore.workspace_root = temp_dir.path().to_path_buf();
        config.strands.explore.workspaces = Vec::new();
        Worker::new(config, "test-worker".to_string(), store)
    }

    /// Return a configuration whose selected adapter is one of NEEDLE's
    /// built-ins. `Config::default()` intentionally names the legacy
    /// operator-provided `claude` adapter, which is absent in isolated CI.
    fn valid_test_config() -> Config {
        let mut config = Config::default();
        config.agent.default = "claude-sonnet".to_string();
        // Isolate operator state. `workspace.home` defaults to the real
        // `~/.needle`, whose `state/workers.json` is the live fleet's worker
        // registry — booting a test worker registered into it and raced both
        // the fleet and the other tests sharing this worker id. `adapters_dir`
        // is pinned alongside it so adapter resolution sees only built-ins and
        // never depends on what the operator happens to have installed.
        let home = crate::util::test_env::isolated_home();
        config.agent.adapters_dir = home.join("adapters");
        config.workspace.home = home;
        // Drop the default routing rules. They rewrite any sonnet/opus/fable/
        // haiku model to `claude-print` and otherwise fall back to
        // `claude-code-glm-4.7` — both operator-provided adapters that exist
        // only in the real `~/.config/needle/adapters`. Leaving them on makes
        // these tests pass or fail based on what the machine happens to have
        // installed, and the boot error blames `agent.default` rather than the
        // adapter routing actually selected. Tests that exercise routing build
        // their own `RoutingConfig`.
        config.agent.routing = None;
        config
    }

    #[tokio::test]
    async fn worker_starts_in_booting_state() {
        let store = Arc::new(MockStore::empty());
        let worker = make_worker(store);
        assert_eq!(*worker.state(), WorkerState::Booting);
    }

    #[tokio::test]
    async fn boot_validates_config() {
        let store = Arc::new(MockStore::empty());
        let mut config = Config::default();
        config.agent.default = String::new(); // Invalid
                                              // Pin workspace_root to prevent Explore strand from scanning real home
        let temp_dir = tempfile::tempdir().unwrap();
        config.strands.explore.workspace_root = temp_dir.path().to_path_buf();
        config.strands.explore.workspaces = Vec::new();
        let mut worker = Worker::new(config, "test-worker".to_string(), store);
        let result = worker.boot();
        assert!(result.is_err());
        assert!(result.unwrap_err().to_string().contains("agent.default"));
    }

    #[tokio::test]
    async fn boot_transitions_to_selecting() {
        let store = Arc::new(MockStore::empty());
        let mut worker = make_worker(store);
        worker.boot().unwrap();
        assert_eq!(*worker.state(), WorkerState::Selecting);
    }

    #[tokio::test]
    async fn run_with_empty_store_returns_exhausted_or_stopped() {
        let store = Arc::new(MockStore::empty());
        let mut config = valid_test_config();
        config.worker.idle_action = IdleAction::Exit;
        config.self_modification.hot_reload = false;
        config.strands.explore.enabled = false;
        // Pin workspace_root to prevent Explore strand from scanning real home
        let temp_dir = tempfile::tempdir().unwrap();
        config.strands.explore.workspace_root = temp_dir.path().to_path_buf();
        config.strands.explore.workspaces = Vec::new();
        let mut worker = Worker::new(config, "test-worker".to_string(), store);

        let result = worker.run().await.unwrap();
        assert!(
            result == WorkerState::Stopped || result == WorkerState::Exhausted,
            "expected Stopped or Exhausted, got {:?}",
            result
        );
    }

    #[tokio::test]
    async fn resolve_adapter_returns_builtin() {
        let store = Arc::new(MockStore::empty());
        let worker = make_worker(store);
        let result = worker.resolve_adapter();
        // `valid_test_config` selects the `claude-sonnet` built-in and disables
        // routing, so resolution must succeed without consulting any adapter
        // the operator happens to have installed. This previously asserted an
        // error, which only held because the default routing rules rewrote the
        // request to `claude-print` — an adapter that is not built in.
        let adapter = result.expect("built-in adapter should resolve");
        assert_eq!(adapter.name, "claude-sonnet");
    }

    #[tokio::test]
    async fn beads_processed_starts_at_zero() {
        let store = Arc::new(MockStore::empty());
        // Use an isolated workspace home so the registry doesn't pick up
        // entries left by other tests (e.g., full_cycle_with_echo_agent).
        let dir = tempfile::tempdir().unwrap();
        let mut config = Config::default();
        config.self_modification.hot_reload = false;
        config.workspace.home = dir.path().to_path_buf();
        // Pin workspace_root to prevent Explore strand from scanning real home
        config.strands.explore.workspace_root = dir.path().to_path_buf();
        config.strands.explore.workspaces = Vec::new();
        let worker = Worker::new(config, "test-worker".to_string(), store);
        assert_eq!(worker.beads_processed(), 0);
    }

    #[tokio::test]
    async fn do_select_with_no_beads_transitions_to_exhausted() {
        let store = Arc::new(MockStore::empty());
        let mut config = valid_test_config();
        config.self_modification.hot_reload = false;
        // Disable Explore strand so it doesn't find beads from the filesystem
        config.strands.explore.enabled = false;
        // Pin workspace_root to prevent Explore strand from scanning real home
    }

    #[test]
    fn set_state_uses_home_workspace_when_no_current_bead() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let dir = tempfile::tempdir().unwrap();
        let home_ws = dir.path().join("home");
        let mut config = valid_test_config();
        config.workspace.home = home_ws.clone();
        config.workspace.default = home_ws.clone();
        let mut worker = Worker::new(config, "test-no-bead".to_string(), store);
        worker.boot().unwrap();

        // No current bead, current_workspace is unset
        worker.current_bead = None;
        worker.current_workspace = std::path::PathBuf::from("");
        worker.set_state(WorkerState::Exhausted).unwrap();

        // Verify that current_workspace was updated with the home workspace
        assert_eq!(worker.current_workspace, home_ws);
    }

    // ── do_log tests ──

    #[tokio::test]
    async fn do_log_increments_beads_processed() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        // Isolate workspace home to avoid registry pollution from other tests.
        let dir = tempfile::tempdir().unwrap();
        let mut config = valid_test_config();
        config.self_modification.hot_reload = false;
        config.workspace.home = dir.path().to_path_buf();
        let mut worker = Worker::new(config, "test-log-inc".to_string(), store);
        worker.boot().unwrap();
        worker.state = WorkerState::Logging;
        worker.current_bead = Some(make_test_bead("needle-log-1"));

        assert_eq!(worker.beads_processed(), 0);
        worker.do_log().unwrap();
        assert_eq!(worker.beads_processed(), 1);
        assert_eq!(*worker.state(), WorkerState::Selecting);
    }

    #[tokio::test]
    async fn do_log_clears_current_bead_and_effort() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut worker = make_worker(store);
        worker.boot().unwrap();
        worker.state = WorkerState::Logging;
        worker.current_bead = Some(make_test_bead("needle-log-2"));
        worker.last_effort = Some(EffortData {
            cycle_start: Instant::now(),
            agent_name: "test".to_string(),
            model: None,
            tokens: dispatch::TokenUsage::default(),
            estimated_cost_usd: None,
        });

        worker.do_log().unwrap();

        assert!(worker.current_bead.is_none());
        assert!(worker.last_effort.is_none());
    }

    #[tokio::test]
    async fn do_log_transitions_to_selecting() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut worker = make_worker(store);
        worker.boot().unwrap();
        worker.state = WorkerState::Logging;
        worker.current_bead = Some(make_test_bead("needle-log-3"));

        worker.do_log().unwrap();
        assert_eq!(*worker.state(), WorkerState::Selecting);
    }

    // ── handle_exhausted tests ──

    #[tokio::test]
    async fn handle_exhausted_with_exit_returns_stopped() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut config = valid_test_config();
        config.worker.idle_action = IdleAction::Exit;
        config.self_modification.hot_reload = false;
        let mut worker = Worker::new(config, "test-exhaust-exit".to_string(), store);
        worker.boot().unwrap();
        worker.state = WorkerState::Exhausted;

        let result = worker.handle_exhausted().await.unwrap();
        assert_eq!(result, WorkerState::Stopped);
    }

    #[tokio::test]
    async fn handle_exhausted_with_wait_returns_selecting() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut config = valid_test_config();
        config.worker.idle_action = IdleAction::Wait;
        // Use a very short timeout so the test doesn't block.
        config.worker.idle_timeout = 0;
        config.self_modification.hot_reload = false;
        let mut worker = Worker::new(config, "test-exhaust-wait".to_string(), store);
        worker.boot().unwrap();
        worker.state = WorkerState::Exhausted;

        let result = worker.handle_exhausted().await.unwrap();
        assert_eq!(result, WorkerState::Selecting);
    }

    // ── stop tests ──

    #[tokio::test]
    async fn stop_returns_stopped_state() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut worker = make_worker(store);
        worker.boot().unwrap();

        let result = worker.stop("test shutdown").await.unwrap();
        assert_eq!(result, WorkerState::Stopped);
    }

    // ── resolve_provider tests ──

    #[tokio::test]
    async fn resolve_provider_returns_none_for_missing_adapter() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut config = Config::default();
        config.agent.default = "nonexistent-adapter".to_string();
        config.self_modification.hot_reload = false;
        let worker = Worker::new(config, "test-provider".to_string(), store);

        // Default adapter not found → provider is None.
        assert!(worker.resolve_provider().is_none());
    }

    // ── restore_home_store tests ──

    #[tokio::test]
    async fn restore_home_store_is_noop_when_stores_match() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut worker = make_worker(store);
        worker.boot().unwrap();

        // home_store and store should be the same Arc initially.
        assert!(Arc::ptr_eq(&worker.store, &worker.home_store));
        worker.restore_home_store();
        assert!(Arc::ptr_eq(&worker.store, &worker.home_store));
    }

    // ── do_select with exclusion set ──

    #[tokio::test]
    async fn do_select_clears_race_lost_this_cycle_and_retry_count() {
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut config = valid_test_config();
        config.self_modification.hot_reload = false;
        // Disable Explore strand so it doesn't find beads from the filesystem
        config.strands.explore.enabled = false;
        let mut worker = Worker::new(config, "test-worker".to_string(), store);
        worker.boot().unwrap();
        worker.race_lost_this_cycle.insert(BeadId::from("old-bead"));
        worker.retry_count = 3;

        worker.do_select().await.unwrap();

        assert!(worker.race_lost_this_cycle.is_empty());
        // retry_count is NOT cleared by do_select() anymore — it must accumulate
        // across cycles to prevent infinite race-lost loops (see needle-aad8).
        assert_eq!(worker.retry_count, 3);
        // Note: exclusion_set is NOT cleared by do_select() anymore - it persists
        // for race-lost beads until they expire or the worker transitions to Exhausted
    }

    // ── full cycle test ──

    #[tokio::test]
    async fn full_cycle_with_echo_agent() {
        use std::collections::HashMap;

        // Test a full cycle: select → claim → build → dispatch → execute → handle → log
        let bead = make_test_bead("needle-echo");
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::new(vec![bead]));
        let mut config = Config::default();
        config.worker.idle_action = IdleAction::Exit;
        // Disable hot-reload in tests — it would re-exec into a different binary.
        config.self_modification.hot_reload = false;
        // Use a simple echo adapter so the test finishes quickly.
        config.agent.default = "echo-test".to_string();
        config.agent.routing = None; // test dispatcher only has echo-test; disable model-based routing
        config.agent.timeout = 5;
        // Set workspace.default to match the bead's workspace so the remote
        // store switch logic doesn't fire.
        config.workspace.default = std::path::PathBuf::from("/tmp/test-workspace");
        // Disable Explore: once the MockStore's one bead is processed and
        // `run()` loops again, Pluck/Mend return NoWork and the waterfall
        // previously fell through to a real Explore scan of $HOME, claiming
        // and mutating real beads in unrelated repos on this server via the
        // real bf/br CLI. See bf-2unnq's contamination addendum.
        config.strands.explore.enabled = false;

        let mut worker = Worker::new(config, "test-worker".to_string(), store);

        // Replace the dispatcher with one that has a simple echo adapter.
        let echo_adapter = crate::dispatch::AgentAdapter {
            name: "echo-test".to_string(),
            description: None,
            agent_cli: "echo".to_string(),
            version_command: None,
            input_method: crate::types::InputMethod::Stdin,
            invoke_template: "echo done".to_string(),
            environment: HashMap::new(),
            timeout_secs: 5,
            idle_timeout_secs: 0,
            hard_timeout_secs: 0,
            provider: None,
            model: None,
            token_extraction: crate::dispatch::TokenExtraction::None,
            output_transform: None,
            harness: None,
            harness_version: None,
        };
        let mut adapters = HashMap::new();
        adapters.insert("echo-test".to_string(), echo_adapter);
        worker.dispatcher =
            Dispatcher::with_adapters(adapters, Telemetry::new("test-worker".to_string()), 5);

        let result = worker.run().await.unwrap();
        assert!(
            result == WorkerState::Stopped || result == WorkerState::Exhausted,
            "expected terminal state, got {:?}",
            result
        );
        // At least one bead was processed through the pipeline.
        assert!(
            worker.beads_processed() >= 1,
            "expected at least 1 bead processed, got {}",
            worker.beads_processed()
        );
    }

    // ── check_auto_canary tests ──

    #[tokio::test]
    async fn check_auto_canary_no_op_when_self_modification_disabled() {
        let dir = tempfile::tempdir().unwrap();
        // Create bin/ so the path exists but needle-testing is absent.
        std::fs::create_dir_all(dir.path().join("bin")).unwrap();
        let store = Arc::new(MockStore::empty());
        let mut config = valid_test_config();
        config.self_modification.enabled = false;
        config.self_modification.auto_promote = true;
        config.self_modification.hot_reload = false;
        config.workspace.home = dir.path().to_path_buf();
        let mut worker = Worker::new(config, "test-canary-disabled".to_string(), store);
        worker.boot().unwrap();
        // Must not fail even though canary workspace and binary are absent.
        assert!(worker.check_auto_canary().is_ok());
    }

    #[tokio::test]
    async fn check_auto_canary_no_op_when_auto_promote_false() {
        let dir = tempfile::tempdir().unwrap();
        std::fs::create_dir_all(dir.path().join("bin")).unwrap();
        let store = Arc::new(MockStore::empty());
        let mut config = valid_test_config();
        config.self_modification.enabled = true;
        config.self_modification.auto_promote = false;

exec
/bin/bash -lc "rg -n \"adapters_dir\" README.md docs src tests | sed -n '1,180p'; rg -n \"claude-print|claude-code-glm-4.7\" ~/.config/needle 2>/dev/null | sed -n '1,80p' || true" in /home/coding/NEEDLE
 succeeded in 0ms:
src/dispatch/mod.rs:712:        let adapters = load_adapters(&config.agent.adapters_dir, &builtin_adapters())?;
src/dispatch/mod.rs:1871:    let adapters = load_adapters(&config.agent.adapters_dir, &builtin_adapters())?;
src/cli/mod.rs:850:    let model = dispatch::load_adapters(&config.agent.adapters_dir, &dispatch::builtin_adapters())
src/cli/mod.rs:2149:  adapters_dir: {adapters_dir}
src/cli/mod.rs:2393:        adapters_dir = dirs_or_home(".config/needle/adapters").display()
src/cli/mod.rs:3959:    match dispatch::load_adapters(&config.agent.adapters_dir, &dispatch::builtin_adapters()) {
src/cli/mod.rs:5319:        config.agent.adapters_dir = PathBuf::from("/definitely/missing/needle-adapters");
docs/plan/plan.md:2470:  adapters_dir: ~/.needle/agents
docs/configuration.md:87:  adapters_dir: ~/.config/needle/adapters
docs/configuration.md:400:  adapters_dir: ~/.config/needle/adapters
src/config/mod.rs:120:    #[serde(default = "AgentConfig::default_adapters_dir")]
src/config/mod.rs:121:    pub adapters_dir: PathBuf,
src/config/mod.rs:134:            adapters_dir: Self::default_adapters_dir(),
src/config/mod.rs:147:    fn default_adapters_dir() -> PathBuf {
src/config/mod.rs:4694:        self.agent.adapters_dir = expand_tilde(&self.agent.adapters_dir);
src/config/mod.rs:8771:        config.agent.adapters_dir = PathBuf::from("~/adapters");
src/config/mod.rs:8785:            config.agent.adapters_dir,
src/config/mod.rs:8863:        config.agent.adapters_dir = PathBuf::from("relative/adapters");
src/config/mod.rs:8874:            config.agent.adapters_dir,
src/config/mod.rs:9050:        config.agent.adapters_dir = PathBuf::from("~/adapters");
src/config/mod.rs:9068:            config.agent.adapters_dir,
src/config/mod.rs:9092:        config.agent.adapters_dir = PathBuf::from("~/.needle.d/adapters");
src/config/mod.rs:9103:            config.agent.adapters_dir,
tests/routing_integration.rs:38:            adapters_dir: PathBuf::from("/nonexistent"),
tests/timeout_config_integration.rs:114:  adapters_dir: ~/.config/needle/adapters
tests/bead_rs_lifecycle.rs:120:            "agent:\n  default: e2e-agent\n  timeout: 30\n  adapters_dir: {}\n  routing: null\nworker:\n  idle_action: exit\n  enforce_shipped_work: false\n  cpu_load_warn: 1.0\n  memory_free_warn_mb: 1\nworkspace:\n  home: {}\nstrands:\n  explore:\n    enabled: false\n    workspace_root: {}\n    workspaces: []\n  splice:\n    enabled: false\n  reflect:\n    enabled: false\ntelemetry:\n  file_sink:\n    enabled: true\n    log_dir: {}\n",
src/worker/mod.rs:4339:        // the fleet and the other tests sharing this worker id. `adapters_dir`
src/worker/mod.rs:4343:        config.agent.adapters_dir = home.join("adapters");
/home/coding/.config/needle/adapters/claude-code-glm-5-turbo.yaml.bak-pre-timeout3600-20260817:2:description: "Claude Code via ZAI proxy (GLM-5-Turbo, all roles — standalone primary adapter; previously only reachable as a subagent role inside other adapters. NOT a cheap tier: its old off-peak 1x promo expired end of April 2026, presumed ~3x like the rest of the GLM-5 family now, confirm on the z.ai dashboard; default to claude-code-glm-4.7 for routine dispatch)"
/home/coding/.config/needle/adapters/claude-print-fable.yaml:1:name: claude-print-fable
/home/coding/.config/needle/adapters/claude-print-fable.yaml:2:description: Claude Code (fable) via claude-print — subscription billing (cc_entrypoint=cli)
/home/coding/.config/needle/adapters/claude-print-fable.yaml:3:agent_cli: claude-print
/home/coding/.config/needle/adapters/claude-print-fable.yaml:4:version_command: "claude-print --version"
/home/coding/.config/needle/adapters/claude-print-fable.yaml:7:invoke_template: "cd {workspace} && unset CLAUDECODE CLAUDE_CODE_SSE_PORT VSCODE_IPC_HOOK_CLI VSCODE_GIT_IPC_HANDLE VSCODE_GIT_ASKPASS_NODE VSCODE_GIT_ASKPASS_MAIN VSCODE_GIT_ASKPASS_EXTRA_ARGS VSCODE_INJECTION VSCODE_NONCE VSCODE_PID VSCODE_CWD && /home/coding/.local/bin/claude-print --model {model} --max-turns 60 --output-format stream-json --dangerously-skip-permissions --no-inherit-hooks < {prompt_file}"
/home/coding/.config/needle/adapters/glm.yaml:2:description: "Claude Code via ZAI proxy (GLM-5-Turbo primary, GLM-5.3 opus-role fallback, GLM-4.7 haiku/subagent — NOT a cheap tier: GLM-5-Turbo's old off-peak 1x promo expired end of April 2026, it's presumed ~3x like the rest of the GLM-5 family now, confirm on the z.ai dashboard; default to claude-code-glm-4.7 for routine dispatch)"
/home/coding/.config/needle/adapters/claude-print-opus.yaml:1:name: claude-print-opus
/home/coding/.config/needle/adapters/claude-print-opus.yaml:2:description: Claude Code (opus) via claude-print — subscription billing (cc_entrypoint=cli)
/home/coding/.config/needle/adapters/claude-print-opus.yaml:3:agent_cli: claude-print
/home/coding/.config/needle/adapters/claude-print-opus.yaml:4:version_command: "claude-print --version"
/home/coding/.config/needle/adapters/claude-print-opus.yaml:7:invoke_template: "cd {workspace} && unset CLAUDECODE CLAUDE_CODE_SSE_PORT VSCODE_IPC_HOOK_CLI VSCODE_GIT_IPC_HANDLE VSCODE_GIT_ASKPASS_NODE VSCODE_GIT_ASKPASS_MAIN VSCODE_GIT_ASKPASS_EXTRA_ARGS VSCODE_INJECTION VSCODE_NONCE VSCODE_PID VSCODE_CWD && /home/coding/.local/bin/claude-print --model {model} --max-turns 100 --output-format stream-json --dangerously-skip-permissions --no-inherit-hooks < {prompt_file}"
/home/coding/.config/needle/adapters/claude-code-glm-4.7.yaml:1:name: claude-code-glm-4.7
/home/coding/.config/needle/adapters/claude-code-glm-5.3.yaml.bak-pre-timeout3600-20260817:2:description: "Claude Code via ZAI proxy (GLM-5.3 — the GLM-5 family is billed at ~3x GLM-4.7's z.ai Max Plan quota cost, confirm current multiplier on the z.ai dashboard before high-volume use; default to claude-code-glm-4.7 for routine dispatch, reserve this for tasks that need the stronger reasoning)"
/home/coding/.config/needle/adapters/claude-code-glm-4.7.yaml.bak-pre-timeout3600-20260817:1:name: claude-code-glm-4.7
/home/coding/.config/needle/adapters/claude-print.yaml:1:name: claude-print
/home/coding/.config/needle/adapters/claude-print.yaml:3:agent_cli: claude-print
/home/coding/.config/needle/adapters/claude-print.yaml:4:version_command: "claude-print --version"
/home/coding/.config/needle/adapters/claude-print.yaml:7:invoke_template: "cd {workspace} && claude-print --model {model} --max-turns 30 --output-format json --dangerously-skip-permissions --no-inherit-hooks"
/home/coding/.config/needle/config.yaml:2:  default: claude-code-glm-4.7
/home/coding/.config/needle/adapters/claude-code-glm-5.yaml.bak-pre-timeout3600-20260817:2:description: "Claude Code via ZAI proxy (GLM-5 was retired by z.ai and is silently served as GLM-5.3 as of 2026-08 — this adapter now requests GLM-5.3 directly for the primary role, GLM-5-Turbo subagent. The GLM-5 family is billed at ~3x GLM-4.7's z.ai Max Plan quota cost, confirm current multiplier on the z.ai dashboard before high-volume use; default to claude-code-glm-4.7 for routine dispatch)"
/home/coding/.config/needle/adapters/claude-code-glm-5.yaml:2:description: "Claude Code via ZAI proxy (GLM-5 was retired by z.ai and is silently served as GLM-5.3 as of 2026-08 — this adapter now requests GLM-5.3 directly for the primary role, GLM-5-Turbo subagent. The GLM-5 family is billed at ~3x GLM-4.7's z.ai Max Plan quota cost, confirm current multiplier on the z.ai dashboard before high-volume use; default to claude-code-glm-4.7 for routine dispatch)"
/home/coding/.config/needle/adapters/claude-code-glm-5.3.yaml:2:description: "Claude Code via ZAI proxy (GLM-5.3 — the GLM-5 family is billed at ~3x GLM-4.7's z.ai Max Plan quota cost, confirm current multiplier on the z.ai dashboard before high-volume use; default to claude-code-glm-4.7 for routine dispatch, reserve this for tasks that need the stronger reasoning)"
/home/coding/.config/needle/adapters/claude-code-glm-5-turbo.yaml:2:description: "Claude Code via ZAI proxy (GLM-5-Turbo, all roles — standalone primary adapter; previously only reachable as a subagent role inside other adapters. NOT a cheap tier: its old off-peak 1x promo expired end of April 2026, presumed ~3x like the rest of the GLM-5 family now, confirm on the z.ai dashboard; default to claude-code-glm-4.7 for routine dispatch)"
/home/coding/.config/needle/adapters/glm.yaml.bak-pre-timeout3600-20260817:2:description: "Claude Code via ZAI proxy (GLM-5-Turbo primary, GLM-5.3 opus-role fallback, GLM-4.7 haiku/subagent — NOT a cheap tier: GLM-5-Turbo's old off-peak 1x promo expired end of April 2026, it's presumed ~3x like the rest of the GLM-5 family now, confirm on the z.ai dashboard; default to claude-code-glm-4.7 for routine dispatch)"
/home/coding/.config/needle/workers/glm-hoop.env:2:NEEDLE_AGENT=claude-code-glm-4.7
/home/coding/.config/needle/workers/glm-bead-forge.env:2:NEEDLE_AGENT=claude-code-glm-4.7
/home/coding/.config/needle/workers/glm-spaxel.env:2:NEEDLE_AGENT=claude-code-glm-4.7
/home/coding/.config/needle/workers/glm-seam.env:2:NEEDLE_AGENT=claude-code-glm-4.7
/home/coding/.config/needle/workers/glm-needle.env:2:NEEDLE_AGENT=claude-code-glm-4.7
/home/coding/.config/needle/config.yaml.bak-before-otlp-enable-20260815-174107:2:  default: claude-code-glm-4.7
/home/coding/.config/needle/workers/glm-armor.env:2:NEEDLE_AGENT=claude-code-glm-4.7

exec
/bin/bash -lc 'git log --all --oneline -S"routed adapter" -- src/worker/mod.rs; git log --all --oneline -S"resolve_adapter_name" -- src/dispatch/mod.rs; git log --all --oneline -S"missing adapter path" -- .' in /home/coding/NEEDLE
 succeeded in 8047ms:
82404c5 beads: close needle-5359b52a
1ddc533 beads: file needle-3653fee9 (CI red for 6+ runs on cargo fmt --check)
06ad956 chore(beads): file needle-85488dd4 — premature closure is systemic
617c824 chore(beads): reopen needle-ea87796e — verify_index skips deletion-only commits
4a1e941 fix(needle-ea87796e): publish checkpoint roots dynamically
9a67c53 chore(beads): checkpoint needle-ea87796e outcome, with active roots
382f2e4 chore(beads): prune superseded generation objects, keeping only the live roots
ce9a153 fix(beads): commit the generation objects the checkpoint pointers reference
45982a8 migrate: rehydrate the bead workspace from bead-forge to bead-rs
8da5a2b chore(git): stop tracking .beads/.bf_history rotating churn
78d20c1 chore(beads): refile the BeadBackend descriptor scope as bf-1x9qu
dce4420 chore(beads): 9 more ADR-013 beads, incl. the root cause of the bf-46m05 miss
7c391e9 chore(beads): remove old checkpoint files
d4f4b35 chore(beads): update bead store state after worker session
9441016 chore(beads): pin descriptors to verified binary versions; close ready-queue leak
9889359 fix(needle-bf-2lhg6): remove async call from sync do_log function
192a2ab fix(needle-bf-3s2b0): add runtime guard to prevent OTLP startup crash
3a28c5f feat(needle-bf-vi8ng): implement timeout eligibility decision for mitosis
dc26282 Fence bf-1o1sr (starvation-alert bead tight-looping agent-sandbox worker)

codex
The routed resolution already returns both the selected adapter name and the matched pattern, but it currently looks up the result in a map that includes built-ins. That means a route can silently succeed using a built-in adapter when its configured YAML is absent, and the current error omits the expected path. I’m checking the adjacent bead intent and adapter filename conventions before adding the source-file validation at the post-routing lookup.
exec
/bin/bash -lc 'bead show bf-14w 2>&1; bead show bf-2wi 2>&1; bead show needle-1bd77af2 2>&1' in /home/coding/NEEDLE
 succeeded in 0ms:
bead: Workspace error: Issue not found: bf-14w
bead: Workspace error: Issue not found: bf-2wi
ID: needle-1bd77af2
Title: Add loud failure for missing routed adapters
Status: InProgress
Priority: P2
Revision: 5
Created: 2026-08-14T16:10:50.156676261Z
Updated: 2026-08-17T05:18:04.398083715Z
Description: ## Scope
Error handling when the routing logic selects an adapter that does not exist.

## Implement
- After routing selects an adapter, verify the adapter YAML exists
- If missing, fail dispatch loudly with clear error message (mentions model, matched rule, missing adapter)
- Do NOT fall back to builtin adapters (complements bf-14w and bf-2wi)

## Acceptance criteria
- [ ] Missing adapter YAML = dispatch failure, not silent fallback
- [ ] Error message includes: model name, matched pattern, missing adapter path
- [ ] Unit test: routing to non-existent adapter fails with appropriate error

## Depends on
Child 3 (routing must be wired first)
Assignee: codex-gpt-5.6-luna-xhigh-icg-2
Type: task

exec
/bin/bash -lc "sed -n '3880,3990p' src/worker/mod.rs; sed -n '5680,5745p' src/worker/mod.rs; sed -n '4320,4430p' src/worker/mod.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
            ))?;

        Ok(adapter)
    }

    /// Apply routing rules to determine the final adapter.
    ///
    /// Returns (chosen_adapter_name, matched_rule_pattern).
    /// If no routing rules match, returns (default_adapter_name, "default").
    /// When strict mode is enabled and no rule matches, returns an error.
    fn apply_routing_rules(
        &self,
        default_adapter: &crate::dispatch::AgentAdapter,
    ) -> Result<(String, String)> {
        let routing_config = match &self.config.agent.routing {
            Some(r) if !r.rules.is_empty() => r,
            _ => return Ok((default_adapter.name.clone(), "default".to_string())),
        };

        let model_name = default_adapter.model.as_deref().unwrap_or("");

        // Determine the default adapter name to use.
        let default_adapter_name = routing_config
            .default_adapter
            .as_ref()
            .unwrap_or(&default_adapter.name);

        // Use the routing module's match_adapter_with_pattern function to get both adapter and pattern.
        match routing::match_adapter_with_pattern(
            model_name,
            &routing_config.rules,
            default_adapter_name,
        ) {
            Some((adapter_name, matched_pattern)) => {
                // Determine if this was a rule match or default fallback.
                // If matched_pattern is "default", it means no rule matched.
                let final_pattern = if matched_pattern == "default" {
                    // No rule matched - using default adapter.
                    // Check if strict mode is enabled - if so, fail.
                    if routing_config.strict {
                        let bead_id = self.current_bead.as_ref().map(|b| b.id.clone());
                        if let Some(id) = bead_id {
                            // Emit RoutingFailed telemetry event.
                            let _ = self.telemetry.emit(EventKind::RoutingFailed {
                                bead_id: id,
                                model: model_name.to_string(),
                                rules_tried: routing_config.rules.len() as u32,
                            });
                        }
                        bail!(
                            "no routing rule matched model '{}' — add a rule to agent.routing.rules or set routing.strict: false to fall back to the default adapter",
                            model_name
                        );
                    }

                    // Strict mode disabled - distinguish between routing default and original default.
                    if routing_config.default_adapter.is_some() {
                        "routing-default"
                    } else {
                        "default"
                    }
                } else {
                    // A rule actually matched - use the pattern from the rule.
                    &matched_pattern
                };

                tracing::debug!(
                    model = %model_name,
                    pattern = %final_pattern,
                    adapter = %adapter_name,
                    "routing decision completed"
                );
                Ok((adapter_name, final_pattern.to_string()))
            }
            None => {
                // No rule matched and no default adapter available.
                // If strict mode is enabled, fail with a clear error.
                if routing_config.strict {
                    let bead_id = self.current_bead.as_ref().map(|b| b.id.clone());
                    if let Some(id) = bead_id {
                        // Emit RoutingFailed telemetry event.
                        let _ = self.telemetry.emit(EventKind::RoutingFailed {
                            bead_id: id,
                            model: model_name.to_string(),
                            rules_tried: routing_config.rules.len() as u32,
                        });
                    }
                    bail!(
                        "no routing rule matched model '{}' and no default adapter available — add a rule to agent.routing.rules or set routing.strict: false to fall back to the default adapter",
                        model_name
                    );
                }

                // Strict mode disabled but no default adapter - this shouldn't happen with valid config.
                tracing::warn!(
                    model = %model_name,
                    "no routing rule matched and no default adapter available, but strict mode is disabled"
                );
                Ok((default_adapter_name.to_string(), "default".to_string()))
            }
        }
    }

    /// Check daily budget and emit appropriate telemetry / trigger shutdown.
    fn check_budget(&mut self) -> Result<()> {
        // Skip if no budget configured.
        if self.config.budget.warn_usd <= 0.0 && self.config.budget.stop_usd <= 0.0 {
            return Ok(());
        }

        // Resolve log directory for scanning.
        config.self_modification.enabled = true;
        config.self_modification.auto_promote = true;
        config.self_modification.hot_reload = false;
        config.workspace.home = dir.path().to_path_buf();
        config.self_modification.canary_workspace = dir.path().join("canary");
        let mut worker = Worker::new(config, "test-canary-no-binary".to_string(), store);
        worker.boot().unwrap();
        // No :testing binary → returns Ok without touching canary workspace.
        assert!(worker.check_auto_canary().is_ok());
    }

    // ── apply_routing_rules tests ──

    #[tokio::test]
    async fn apply_routing_rules_strict_true_no_match_returns_err() {
        use crate::config::{RoutingConfig, RoutingRule};
        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
        let mut config = Config::default();
        config.self_modification.hot_reload = false;
        // Enable strict mode with a rule that won't match.
        config.agent.routing = Some(RoutingConfig {
            rules: vec![RoutingRule {
                match_model: "sonnet".to_string(),
                adapter: "claude-print".to_string(),
            }],
            default_adapter: None,
            strict: true,
        });
        // Set up a model that won't match the rule.
        let worker = Worker::new(config, "test-routing-strict".to_string(), store);
        let adapter = crate::dispatch::AgentAdapter {
            name: "claude".to_string(),
            description: None,
            agent_cli: "claude".to_string(),
            version_command: None,
            input_method: crate::types::InputMethod::Stdin,
            invoke_template: "claude {prompt}".to_string(),
            environment: std::collections::HashMap::new(),
            timeout_secs: 3600,
            idle_timeout_secs: 0,
            hard_timeout_secs: 0,
            provider: Some("anthropic".to_string()),
            model: Some("gpt-4o".to_string()),
            token_extraction: crate::dispatch::TokenExtraction::None,
            output_transform: None,
            harness: None,
            harness_version: None,
        };

        let result = worker.apply_routing_rules(&adapter);
        assert!(result.is_err());
        let err_msg = result.unwrap_err().to_string();
        assert!(
            err_msg.contains("no routing rule matched model 'gpt-4o'"),
            "error message should mention the model: {}",
            err_msg
        );
        assert!(
            err_msg.contains("set routing.strict: false"),
            "error message should suggest disabling strict mode: {}",
            err_msg
        );
    }

    #[tokio::test]
    async fn apply_routing_rules_strict_false_no_match_returns_default() {
        config.strands.explore.enabled = false;
        // Pin Explore scan root to a tempdir so the Explore strand cannot
        // scan the real home directory if it ever runs. Even though Explore
        // is disabled above, defense-in-depth ensures tests are isolated.
        let temp_dir = tempfile::tempdir().unwrap();
        config.strands.explore.workspace_root = temp_dir.path().to_path_buf();
        config.strands.explore.workspaces = Vec::new();
        Worker::new(config, "test-worker".to_string(), store)
    }

    /// Return a configuration whose selected adapter is one of NEEDLE's
    /// built-ins. `Config::default()` intentionally names the legacy
    /// operator-provided `claude` adapter, which is absent in isolated CI.
    fn valid_test_config() -> Config {
        let mut config = Config::default();
        config.agent.default = "claude-sonnet".to_string();
        // Isolate operator state. `workspace.home` defaults to the real
        // `~/.needle`, whose `state/workers.json` is the live fleet's worker
        // registry — booting a test worker registered into it and raced both
        // the fleet and the other tests sharing this worker id. `adapters_dir`
        // is pinned alongside it so adapter resolution sees only built-ins and
        // never depends on what the operator happens to have installed.
        let home = crate::util::test_env::isolated_home();
        config.agent.adapters_dir = home.join("adapters");
        config.workspace.home = home;
        // Drop the default routing rules. They rewrite any sonnet/opus/fable/
        // haiku model to `claude-print` and otherwise fall back to
        // `claude-code-glm-4.7` — both operator-provided adapters that exist
        // only in the real `~/.config/needle/adapters`. Leaving them on makes
        // these tests pass or fail based on what the machine happens to have
        // installed, and the boot error blames `agent.default` rather than the
        // adapter routing actually selected. Tests that exercise routing build
        // their own `RoutingConfig`.
        config.agent.routing = None;
        config
    }

    #[tokio::test]
    async fn worker_starts_in_booting_state() {
        let store = Arc::new(MockStore::empty());
        let worker = make_worker(store);
        assert_eq!(*worker.state(), WorkerState::Booting);
    }

    #[tokio::test]
    async fn boot_validates_config() {
        let store = Arc::new(MockStore::empty());
        let mut config = Config::default();
        config.agent.default = String::new(); // Invalid
                                              // Pin workspace_root to prevent Explore strand from scanning real home
        let temp_dir = tempfile::tempdir().unwrap();
        config.strands.explore.workspace_root = temp_dir.path().to_path_buf();
        config.strands.explore.workspaces = Vec::new();
        let mut worker = Worker::new(config, "test-worker".to_string(), store);
        let result = worker.boot();
        assert!(result.is_err());
        assert!(result.unwrap_err().to_string().contains("agent.default"));
    }

    #[tokio::test]
    async fn boot_transitions_to_selecting() {
        let store = Arc::new(MockStore::empty());
        let mut worker = make_worker(store);
        worker.boot().unwrap();
        assert_eq!(*worker.state(), WorkerState::Selecting);
    }

    #[tokio::test]
    async fn run_with_empty_store_returns_exhausted_or_stopped() {
        let store = Arc::new(MockStore::empty());
        let mut config = valid_test_config();
        config.worker.idle_action = IdleAction::Exit;
        config.self_modification.hot_reload = false;
        config.strands.explore.enabled = false;
        // Pin workspace_root to prevent Explore strand from scanning real home
        let temp_dir = tempfile::tempdir().unwrap();
        config.strands.explore.workspace_root = temp_dir.path().to_path_buf();
        config.strands.explore.workspaces = Vec::new();
        let mut worker = Worker::new(config, "test-worker".to_string(), store);

        let result = worker.run().await.unwrap();
        assert!(
            result == WorkerState::Stopped || result == WorkerState::Exhausted,
            "expected Stopped or Exhausted, got {:?}",
            result
        );
    }

    #[tokio::test]
    async fn resolve_adapter_returns_builtin() {
        let store = Arc::new(MockStore::empty());
        let worker = make_worker(store);
        let result = worker.resolve_adapter();
        // `valid_test_config` selects the `claude-sonnet` built-in and disables
        // routing, so resolution must succeed without consulting any adapter
        // the operator happens to have installed. This previously asserted an
        // error, which only held because the default routing rules rewrote the
        // request to `claude-print` — an adapter that is not built in.
        let adapter = result.expect("built-in adapter should resolve");
        assert_eq!(adapter.name, "claude-sonnet");
    }

    #[tokio::test]
    async fn beads_processed_starts_at_zero() {
        let store = Arc::new(MockStore::empty());
        // Use an isolated workspace home so the registry doesn't pick up
        // entries left by other tests (e.g., full_cycle_with_echo_agent).
        let dir = tempfile::tempdir().unwrap();
        let mut config = Config::default();
        config.self_modification.hot_reload = false;
        config.workspace.home = dir.path().to_path_buf();

exec
/bin/bash -lc "git show --stat --oneline 8e85024 && git show 8e85024 -- src/worker/mod.rs | sed -n '1,260p'; git show --stat --oneline bf-14w 2>/dev/null || true" in /home/coding/NEEDLE
 succeeded in 0ms:
8e85024 test(bf-3p3u1): validate adapters before worker claims
 src/worker/mod.rs          |  87 +++++++++++-------
 tests/integration_tests.rs | 218 +++++++++++++++++++++++++++++++++++++++++++++
 2 files changed, 272 insertions(+), 33 deletions(-)
commit 8e85024e5dbaa06e0059116fce2a4686fdad7294
Author: jedarden <github@jedarden.com>
Date:   Thu Aug 13 18:46:31 2026 -0400

    test(bf-3p3u1): validate adapters before worker claims

diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index c71627d..58be85d 100644
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1100,16 +1100,25 @@ impl Worker {
         // This ensures adapter configuration errors are caught early during startup rather
         // than after a bead has been claimed, preventing orphaned in_progress beads.
         if let Err(e) = self.resolve_adapter() {
-            // Provide clear context that this is a startup preflight failure
-            bail!(
-                "startup adapter preflight failed: {e}\n\n\
-                 The configured agent adapter could not be resolved. This check runs during \
-                 worker initialization to ensure adapter configuration is valid before \
-                 processing any beads. Common causes:\n\
-                 - Adapter YAML file missing from ~/.needle/agents/ or ~/.local/bin/claude-config/agents/\n\
-                 - Incorrect adapter name in agent.default config\n\
-                 - Adapter defined but missing required fields (provider, model, etc.)",
+            // Provide clear, actionable error message for missing adapter
+            let adapter_name = &self.config.agent.default;
+            eprintln!("ERROR: Configured agent adapter '{adapter_name}' was not found.");
+            eprintln!("Startup is aborting to prevent claiming beads with an invalid adapter configuration.");
+            eprintln!();
+            eprintln!("To fix this, check one of these locations for your adapter configuration:");
+            eprintln!("  - ~/.needle/agents/{adapter_name}.yaml");
+            eprintln!("  - ~/.local/bin/claude-config/agents/{adapter_name}/config.json");
+            eprintln!("  - ~/.config/needle/adapters/{adapter_name}.yaml");
+            eprintln!();
+            eprintln!("Common causes:");
+            eprintln!("  - Adapter file does not exist or is in a different location");
+            eprintln!("  - Incorrect adapter name specified in agent.default config");
+            eprintln!(
+                "  - Adapter file exists but is missing required fields (provider, model, etc.)"
             );
+            eprintln!();
+            eprintln!("Underlying error: {e}");
+            bail!("adapter '{adapter_name}' not found — startup aborted");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3793,12 +3802,16 @@ impl Worker {
         let default_adapter_name = &self.config.agent.default;
 
         // Resolve the default adapter to get its model (if any).
-        let default_adapter = self.dispatcher.adapter(default_adapter_name)
+        let default_adapter = self
+            .dispatcher
+            .adapter(default_adapter_name)
             .cloned()
-            .ok_or_else(|| anyhow::anyhow!(
-                "configured agent adapter '{}' not found — check ~/.needle/agents/{}.yaml or ~/.local/bin/claude-config/agents/{}/config.json exists",
-                default_adapter_name, default_adapter_name, default_adapter_name
-            ))?;
+            .ok_or_else(|| {
+                anyhow::anyhow!(
+                    "adapter '{}' not found in any of the expected configuration directories",
+                    default_adapter_name
+                )
+            })?;
 
         // Apply routing rules if configured.
         let (chosen_adapter_name, matched_rule) = self.apply_routing_rules(&default_adapter)?;
@@ -3821,11 +3834,10 @@ impl Worker {
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed agent adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter is missing from ~/.config/needle/adapters/{}.yaml",
+                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
-                matched_rule,
-                chosen_adapter_name
+                matched_rule
             ))?;
 
         Ok(adapter)
@@ -4254,7 +4266,7 @@ mod tests {
     }
 
     fn make_worker(store: Arc<dyn BeadStore>) -> Worker {
-        let mut config = Config::default();
+        let mut config = valid_test_config();
         // Disable hot-reload in tests — it would re-exec into a different binary.
         config.self_modification.hot_reload = false;
         // Disable Explore: it defaults to scanning the real $HOME for any
@@ -4276,6 +4288,15 @@ mod tests {
         Worker::new(config, "test-worker".to_string(), store)
     }
 
+    /// Return a configuration whose selected adapter is one of NEEDLE's
+    /// built-ins. `Config::default()` intentionally names the legacy
+    /// operator-provided `claude` adapter, which is absent in isolated CI.
+    fn valid_test_config() -> Config {
+        let mut config = Config::default();
+        config.agent.default = "claude-sonnet".to_string();
+        config
+    }
+
     #[tokio::test]
     async fn worker_starts_in_booting_state() {
         let store = Arc::new(MockStore::empty());
@@ -4309,7 +4330,7 @@ mod tests {
     #[tokio::test]
     async fn run_with_empty_store_returns_exhausted_or_stopped() {
         let store = Arc::new(MockStore::empty());
-        let mut config = Config::default();
+        let mut config = valid_test_config();
         config.worker.idle_action = IdleAction::Exit;
         config.self_modification.hot_reload = false;
         config.strands.explore.enabled = false;
@@ -4386,7 +4407,7 @@ mod tests {
     #[tokio::test]
     async fn shutdown_flag_causes_stop() {
         let store = Arc::new(MockStore::empty());
-        let mut config = Config::default();
+        let mut config = valid_test_config();
         config.worker.idle_action = IdleAction::Exit;
         config.self_modification.hot_reload = false;
         // Disable Explore strand so it doesn't scan the real filesystem —
@@ -4724,7 +4745,7 @@ mod tests {
                 let mut bead = make_test_bead("needle-span-depth");
                 bead.workspace = home.path().to_path_buf();
                 let store = Arc::new(MockStore::new(vec![bead.clone()]));
-                let mut config = Config::default();
+                let mut config = valid_test_config();
                 config.workspace.home = home.path().to_path_buf();
                 config.workspace.default = home.path().to_path_buf();
                 config.self_modification.hot_reload = false;
@@ -5080,7 +5101,7 @@ mod tests {
         std::fs::write(log_dir.join("worker.jsonl"), &log_content).unwrap();
 
         let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
-        let mut config = Config::default();
+        let mut config = valid_test_config();
         config.self_modification.hot_reload = false;
         config.workspace.home = dir.path().to_path_buf();
         config.telemetry.file_sink.log_dir = Some(log_dir);
@@ -5111,7 +5132,7 @@ mod tests {
         std::fs::write(log_dir.join("worker.jsonl"), &log_content).unwrap();
 
         let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
-        let mut config = Config::default();
+        let mut config = valid_test_config();
         config.self_modification.hot_reload = false;
         config.workspace.home = dir.path().to_path_buf();
         config.telemetry.file_sink.log_dir = Some(log_dir);
@@ -5240,7 +5261,7 @@ mod tests {
     fn set_state_uses_bead_workspace_for_cross_workspace_bead() {
         let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
         let dir = tempfile::tempdir().unwrap();
-        let mut config = Config::default();
+        let mut config = valid_test_config();
         config.workspace.home = dir.path().join("home");
         config.workspace.default = dir.path().join("home");
         let mut worker = Worker::new(config, "test-cross-ws".to_string(), store);
@@ -5276,7 +5297,7 @@ mod tests {
         let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
         let dir = tempfile::tempdir().unwrap();
         let home_ws = dir.path().join("home");
-        let mut config = Config::default();
+        let mut config = valid_test_config();
         config.workspace.home = home_ws.clone();
         config.workspace.default = home_ws.clone();
         let mut worker = Worker::new(config, "test-unset-ws".to_string(), store);
@@ -5311,7 +5332,7 @@ mod tests {
         let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
         let dir = tempfile::tempdir().unwrap();
         let home_ws = dir.path().join("home");
-        let mut config = Config::default();
+        let mut config = valid_test_config();
         config.workspace.home = home_ws.clone();
         config.workspace.default = home_ws.clone();
         let mut worker = Worker::new(config, "test-no-bead".to_string(), store);
@@ -5333,7 +5354,7 @@ mod tests {
         let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
         // Isolate workspace home to avoid registry pollution from other tests.
         let dir = tempfile::tempdir().unwrap();
-        let mut config = Config::default();
+        let mut config = valid_test_config();
         config.self_modification.hot_reload = false;
         config.workspace.home = dir.path().to_path_buf();
         let mut worker = Worker::new(config, "test-log-inc".to_string(), store);
@@ -5385,7 +5406,7 @@ mod tests {
     #[tokio::test]
     async fn handle_exhausted_with_exit_returns_stopped() {
         let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
-        let mut config = Config::default();
+        let mut config = valid_test_config();
         config.worker.idle_action = IdleAction::Exit;
         config.self_modification.hot_reload = false;
         let mut worker = Worker::new(config, "test-exhaust-exit".to_string(), store);
@@ -5399,7 +5420,7 @@ mod tests {
     #[tokio::test]
     async fn handle_exhausted_with_wait_returns_selecting() {
         let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
-        let mut config = Config::default();
+        let mut config = valid_test_config();
         config.worker.idle_action = IdleAction::Wait;
         // Use a very short timeout so the test doesn't block.
         config.worker.idle_timeout = 0;
@@ -5457,7 +5478,7 @@ mod tests {
     #[tokio::test]
     async fn do_select_clears_race_lost_this_cycle_and_retry_count() {
         let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
-        let mut config = Config::default();
+        let mut config = valid_test_config();
         config.self_modification.hot_reload = false;
         // Disable Explore strand so it doesn't find beads from the filesystem
         config.strands.explore.enabled = false;
@@ -5551,7 +5572,7 @@ mod tests {
         // Create bin/ so the path exists but needle-testing is absent.
         std::fs::create_dir_all(dir.path().join("bin")).unwrap();
         let store = Arc::new(MockStore::empty());
-        let mut config = Config::default();
+        let mut config = valid_test_config();
         config.self_modification.enabled = false;
         config.self_modification.auto_promote = true;
         config.self_modification.hot_reload = false;
@@ -5567,7 +5588,7 @@ mod tests {
         let dir = tempfile::tempdir().unwrap();
         std::fs::create_dir_all(dir.path().join("bin")).unwrap();
         let store = Arc::new(MockStore::empty());
-        let mut config = Config::default();
+        let mut config = valid_test_config();
         config.self_modification.enabled = true;
         config.self_modification.auto_promote = false;
         config.self_modification.hot_reload = false;
@@ -5583,7 +5604,7 @@ mod tests {
         // bin/ exists but needle-testing does not.
         std::fs::create_dir_all(dir.path().join("bin")).unwrap();
         let store = Arc::new(MockStore::empty());
-        let mut config = Config::default();
+        let mut config = valid_test_config();
         config.self_modification.enabled = true;
         config.self_modification.auto_promote = true;
         config.self_modification.hot_reload = false;

exec
/bin/bash -lc "rg -n -C 10 \"match_adapter_with_pattern\" src/routing* src --glob '*.rs' | sed -n '1,240p'; ls src | sed -n '1,120p'; sed -n '1,260p' src/routing.rs 2>/dev/null || true" in /home/coding/NEEDLE
 succeeded in 0ms:
src/routing.rs-265-/// Match a model name against routing rules, returning adapter and pattern.
src/routing.rs-266-///
src/routing.rs-267-/// Similar to `match_adapter`, but returns both the adapter name and the
src/routing.rs-268-/// matched pattern, so callers can distinguish between rule matches and
src/routing.rs-269-/// default fallbacks.
src/routing.rs-270-///
src/routing.rs-271-/// # Returns
src/routing.rs-272-///
src/routing.rs-273-/// * `Some((adapter, pattern))` - Adapter name and the pattern that matched.
src/routing.rs-274-/// * `None` - No rule matched and default is empty/invalid.
src/routing.rs:275:pub fn match_adapter_with_pattern(
src/routing.rs-276-    model: &str,
src/routing.rs-277-    rules: &[RoutingRule],
src/routing.rs-278-    default: &str,
src/routing.rs-279-) -> Option<(String, String)> {
src/routing.rs-280-    // Compile rules and test in order (first match wins).
src/routing.rs-281-    for rule in rules {
src/routing.rs-282-        match CompiledRule::from_rule(rule) {
src/routing.rs-283-            Ok(compiled) => {
src/routing.rs-284-                if compiled.matches(model) {
src/routing.rs-285-                    return Some((rule.adapter.clone(), rule.match_model.clone()));
--
src/routing.rs-265-/// Match a model name against routing rules, returning adapter and pattern.
src/routing.rs-266-///
src/routing.rs-267-/// Similar to `match_adapter`, but returns both the adapter name and the
src/routing.rs-268-/// matched pattern, so callers can distinguish between rule matches and
src/routing.rs-269-/// default fallbacks.
src/routing.rs-270-///
src/routing.rs-271-/// # Returns
src/routing.rs-272-///
src/routing.rs-273-/// * `Some((adapter, pattern))` - Adapter name and the pattern that matched.
src/routing.rs-274-/// * `None` - No rule matched and default is empty/invalid.
src/routing.rs:275:pub fn match_adapter_with_pattern(
src/routing.rs-276-    model: &str,
src/routing.rs-277-    rules: &[RoutingRule],
src/routing.rs-278-    default: &str,
src/routing.rs-279-) -> Option<(String, String)> {
src/routing.rs-280-    // Compile rules and test in order (first match wins).
src/routing.rs-281-    for rule in rules {
src/routing.rs-282-        match CompiledRule::from_rule(rule) {
src/routing.rs-283-            Ok(compiled) => {
src/routing.rs-284-                if compiled.matches(model) {
src/routing.rs-285-                    return Some((rule.adapter.clone(), rule.match_model.clone()));
--
src/worker/mod.rs-3897-        };
src/worker/mod.rs-3898-
src/worker/mod.rs-3899-        let model_name = default_adapter.model.as_deref().unwrap_or("");
src/worker/mod.rs-3900-
src/worker/mod.rs-3901-        // Determine the default adapter name to use.
src/worker/mod.rs-3902-        let default_adapter_name = routing_config
src/worker/mod.rs-3903-            .default_adapter
src/worker/mod.rs-3904-            .as_ref()
src/worker/mod.rs-3905-            .unwrap_or(&default_adapter.name);
src/worker/mod.rs-3906-
src/worker/mod.rs:3907:        // Use the routing module's match_adapter_with_pattern function to get both adapter and pattern.
src/worker/mod.rs:3908:        match routing::match_adapter_with_pattern(
src/worker/mod.rs-3909-            model_name,
src/worker/mod.rs-3910-            &routing_config.rules,
src/worker/mod.rs-3911-            default_adapter_name,
src/worker/mod.rs-3912-        ) {
src/worker/mod.rs-3913-            Some((adapter_name, matched_pattern)) => {
src/worker/mod.rs-3914-                // Determine if this was a rule match or default fallback.
src/worker/mod.rs-3915-                // If matched_pattern is "default", it means no rule matched.
src/worker/mod.rs-3916-                let final_pattern = if matched_pattern == "default" {
src/worker/mod.rs-3917-                    // No rule matched - using default adapter.
src/worker/mod.rs-3918-                    // Check if strict mode is enabled - if so, fail.
agent_event.rs
bead_store
bin
canary
cargo_test.rs
checkpoint_utils.rs
claim
claude_md_placement.rs
cli
commit_hook.rs
config
cost
decision
dispatch
drift
health
hoop_hooks.rs
integration_t
kubectl.rs
learning
lib.rs
log_writer.rs
main.rs
mitosis
outcome
peer
process_guard.rs
prompt
rate_limit
registry
resolve
routing.rs
sanitize
skill
span
spawn_path
stats
strand
supervisor
telemetry
test_output.rs
test_runner.rs
tmux_socket.rs
trace
transcript
tsnet.rs
types
upgrade
util.rs
validation
worker
workspace_equality.rs
//! Routing rule matcher for model-to-adapter dispatch.
//!
//! Provides pattern matching logic for routing rules: regex/glob patterns
//! against model names, first-match-wins semantics, and default fallback.

use crate::config::RoutingRule;
use std::sync::Arc;

/// Compiled routing rule with cached glob pattern for efficient matching.
#[derive(Debug, Clone)]
struct CompiledRule {
    /// Compiled glob pattern matcher.
    glob_matcher: Option<Arc<glob::Pattern>>,
    /// Raw glob source, needed at match time to special-case a bare `*`/`**`
    /// (see `matches()`).
    glob_source: Option<String>,
    /// Fallback regex matcher for patterns that aren't valid globs.
    regex_matcher: Option<Arc<regex::Regex>>,
    /// Adapter to use on match.
    adapter: String,
}

impl CompiledRule {
    /// Compile a routing rule into an efficient matcher.
    ///
    /// Supports both regex patterns (any valid regex) and glob-style patterns:
    /// - `*` matches any sequence of non-separator characters
    /// - `**` matches any sequence of characters, including slashes
    /// - `?` matches any single character
    /// - `[a-z]` matches any character in the bracket
    /// - `[!a-z]` matches any character not in the bracket
    ///
    /// Detects whether a pattern should be treated as regex or glob based on
    /// the presence of regex metacharacters. If the pattern contains regex
    /// features (like `.*`, `^`, `$`, etc.), it's compiled as regex. Otherwise,
    /// if it contains glob wildcards (`*`, `?`, etc.), it's compiled as glob
    /// using the glob crate for efficient pattern matching.
    fn from_rule(rule: &RoutingRule) -> Result<Self, String> {
        let pattern = &rule.match_model;
        let adapter = rule.adapter.clone();

        // First, check if the pattern should be treated as regex
        // If it has clear regex features, compile as regex
        if !needs_glob_conversion(pattern) {
            // Try regex compilation
            match regex::Regex::new(pattern) {
                Ok(regex) => {
                    return Ok(CompiledRule {
                        glob_matcher: None,
                        glob_source: None,
                        regex_matcher: Some(Arc::new(regex)),
                        adapter,
                    })
                }
                Err(_) => {
                    // Regex failed, try glob as fallback
                }
            }
        }

        // Try glob pattern compilation using the glob crate
        match glob::Pattern::new(pattern) {
            Ok(glob_pattern) => Ok(CompiledRule {
                glob_matcher: Some(Arc::new(glob_pattern)),
                glob_source: Some(pattern.clone()),
                regex_matcher: None,
                adapter,
            }),
            Err(e) => {
                // Glob compilation failed, return error
                Err(format!(
                    "Failed to compile glob pattern '{}': {}",
                    pattern, e
                ))
            }
        }
    }

    /// Test if a model name matches this rule.
    fn matches(&self, model: &str) -> bool {
        // Prefer glob matching if available. require_literal_separator ensures
        // a single `*` doesn't cross `/` (only `**` should span path segments) —
        // the glob crate's default matches() allows `*` to cross separators,
        // which would let e.g. "provider/*" wrongly match "provider/foo/model".
        //
        // Exception: a *bare* "*" or "**" (the whole pattern, no literal
        // segments) is a full catch-all by convention — it's meant to match
        // any model name at all, slashes included, the same way a default
        // fallback rule would.
        if let Some(ref glob) = self.glob_matcher {
            let is_bare_wildcard = matches!(self.glob_source.as_deref(), Some("*") | Some("**"));
            return glob.matches_with(
                model,
                glob::MatchOptions {
                    case_sensitive: true,
                    require_literal_separator: !is_bare_wildcard,
                    require_literal_leading_dot: false,
                },
            );
        }

        // Fall back to regex matching
        if let Some(ref regex) = self.regex_matcher {
            return regex.is_match(model);
        }

        false
    }
}

/// Check if a pattern is a glob pattern.
///
/// A simple heuristic that returns true if the pattern contains glob-style
/// wildcards (`*` or `**`). This is a basic check that doesn't attempt to
/// distinguish between glob and regex patterns - it just reports whether
/// the pattern contains wildcard characters.
///
/// # Arguments
///
/// * `pattern` - The pattern string to check.
///
/// # Returns
///
/// * `true` if the pattern contains `*` or `**`.
/// * `false` otherwise.
///
/// # Examples
///
/// ```
/// use needle::routing::is_glob_pattern;
///
/// // Glob patterns with wildcards
/// assert!(is_glob_pattern("*"));
/// assert!(is_glob_pattern("**"));
/// assert!(is_glob_pattern("gpt-*"));
/// assert!(is_glob_pattern("test/**"));
///
/// // Non-glob patterns (no wildcards)
/// assert!(!is_glob_pattern("exact-match"));
/// assert!(!is_glob_pattern("gpt-4"));
/// assert!(!is_glob_pattern("claude-sonnet"));
/// ```
pub fn is_glob_pattern(pattern: &str) -> bool {
    pattern.contains('*')
}

/// Check if a pattern contains glob-style wildcards that need conversion.
///
/// Returns true if the pattern appears to be a glob pattern rather than
/// a well-formed regex. The heuristic checks for:
/// 1. Regex features: anchors (^$), character classes [], parentheses (),
///    quantifiers +?{}, alternation |, or the regex wildcard sequence .*
/// 2. If present, it's a regex pattern - no conversion needed
/// 3. If absent, but * is present, it's a glob pattern - convert it
fn needs_glob_conversion(pattern: &str) -> bool {
    // First, check if it looks like a regex by looking for regex metacharacters.
    // The key indicator is .* which is "any character" in regex but not in glob.
    let has_regex_features = pattern.contains(|c| {
        matches!(
            c,
            '^' | '$' | '(' | ')' | '[' | ']' | '{' | '}' | '+' | '?' | '|' | '\\'
        )
    }) || pattern.contains(".*");

    if has_regex_features {
        // Has clear regex features - treat as regex
        return false;
    }

    // At this point, we have a simple pattern that might be glob or regex.
    // If it contains unescaped * (and we already ruled out other regex chars),
    // treat it as a glob pattern.
    let chars: Vec<char> = pattern.chars().collect();
    let mut i = 0;

    while i < chars.len() {
        if chars[i] == '\\' && i + 1 < chars.len() {
            // Skip escaped character.
            i += 2;
            continue;
        }
        if chars[i] == '*' {
            return true;
        }
        i += 1;
    }

    false
}

/// Match a model name against routing rules, returning the adapter to use.
///
/// Evaluates rules in order; the first matching rule determines the adapter.
/// If no rule matches, returns the default adapter.
///
/// # Arguments
///
/// * `model` - The model name to match (e.g., "claude-sonnet-4-6").
/// * `rules` - Ordered list of routing rules (first match wins).
/// * `default` - Fallback adapter when no rules match.
///
/// # Returns
///
/// * `Some(adapter)` - The adapter name to use.
/// * `None` - No rule matched and default is empty/invalid (caller should handle).
///
/// # Examples
///
/// ```
/// use needle::routing::match_adapter;
/// use needle::config::RoutingRule;
///
/// let rules = vec![
///     RoutingRule {
///         match_model: "sonnet.*".to_string(),
///         adapter: "claude-print".to_string(),
///     },
///     RoutingRule {
///         match_model: "*".to_string(),
///         adapter: "default-adapter".to_string(),
///     },
/// ];
///
/// // First rule matches.
/// assert_eq!(
///     match_adapter("sonnet-4-6", &rules, "fallback"),
///     Some("claude-print".to_string())
/// );
///
/// // Second rule (catch-all) matches.
/// assert_eq!(
///     match_adapter("other-model", &rules, "fallback"),
///     Some("default-adapter".to_string())
/// );
/// ```
pub fn match_adapter(model: &str, rules: &[RoutingRule], default: &str) -> Option<String> {
    // Compile rules and test in order (first match wins).
    for rule in rules {
        match CompiledRule::from_rule(rule) {
            Ok(compiled) => {
                if compiled.matches(model) {
                    return Some(compiled.adapter.clone());
                }
            }
            Err(e) => {
                // Log the error but continue with other rules.
                // Invalid patterns are skipped rather than failing the entire dispatch.
                tracing::warn!(
                    pattern = %rule.match_model,
                    error = %e,
                    "invalid routing pattern — skipping rule"
                );
            }
        }
    }

    // No rule matched — use default if provided.
    if default.is_empty() {
        None
    } else {

exec
/bin/bash -lc "sed -n '4650,4720p' src/config/mod.rs; rg -n -C 5 \"RoutingRule|routing.*adapter|adapter.*routing\" src/config/mod.rs | sed -n '1,240p'" in /home/coding/NEEDLE
 succeeded in 0ms:
    #[serde(default)]
    pub budget: BudgetConfig,
    /// Verification commands run after agent success, before accepting closure.
    /// Legacy format — prefer `gates` for new configurations.
    #[serde(default)]
    pub verification: Vec<String>,
    /// Pluggable validation gates.
    #[serde(default)]
    pub gates: Vec<GateConfig>,
    /// Self-modification (hot-reload) configuration.
    #[serde(default)]
    pub self_modification: SelfModificationConfig,
    /// FABRIC live dashboard forwarding.
    #[serde(default)]
    pub fabric: FabricConfig,
    /// Supervisor detection configuration.
    #[serde(default)]
    pub supervisor: SupervisorConfig,
    /// Outcome handling configuration (failure circuit-breaker).
    #[serde(default)]
    pub outcome: OutcomeConfig,
    /// Tsnet identity provisioning configuration.
    #[serde(default)]
    pub tsnet: crate::tsnet::TsnetConfig,
    /// Validation gate execution configuration (timeout, stderr cap).
    #[serde(default)]
    pub validation: ValidationConfig,
}

impl Config {
    /// Expand all tilde (~) paths to absolute paths using $HOME.
    ///
    /// This must be called after deserialization to ensure all paths
    /// like `~/.needle` are expanded to `/home/user/.needle`.
    /// Paths without tildes are left unchanged.
    pub fn expand_tildes(&mut self) {
        // workspace section
        self.workspace.default = expand_tilde(&self.workspace.default);
        self.workspace.home = expand_tilde(&self.workspace.home);

        // worker section
        self.worker.worker_binary_path = expand_tilde_option(&self.worker.worker_binary_path);

        // agent section
        self.agent.adapters_dir = expand_tilde(&self.agent.adapters_dir);

        // bead_cli section
        self.bead_cli.path = expand_tilde_option(&self.bead_cli.path);

        // strands.explore section
        self.strands.explore.workspace_root = expand_tilde(&self.strands.explore.workspace_root);
        self.strands.explore.workspaces = expand_tilde_vec(&self.strands.explore.workspaces);

        // strands.weave section
        self.strands.weave.exclude_workspaces =
            expand_tilde_vec(&self.strands.weave.exclude_workspaces);

        // strands.splice section
        self.strands.splice.report_workspace =
            expand_tilde_option(&self.strands.splice.report_workspace);

        // strands.pulse.scanners[].command paths are strings, not PathBuf, so skip

        // strands.reflect section - no PathBuf fields

        // strands.learning section
        self.strands.learning.global_learnings_file =
            expand_tilde(&self.strands.learning.global_learnings_file);

        // telemetry section
        if let Some(ref mut log_dir) = self.telemetry.file_sink.log_dir {
24-
25-// ──────────────────────────────────────────────────────────────────────────────
26-// Sub-structs
27-// ──────────────────────────────────────────────────────────────────────────────
28-
29:/// A single routing rule mapping model patterns to adapters.
30-///
31-/// Rules are evaluated in order; the first matching rule determines the adapter.
32-#[derive(Debug, Clone, Serialize, Deserialize)]
33:pub struct RoutingRule {
34-    /// Regex or glob pattern to match against model names.
35-    ///
36-    /// Patterns are matched against the model name only (not provider prefixes).
37-    /// Examples: `"sonnet"`, `"opus"`, `"claude-sonnet-4-6"`, `"(claude-)?(sonnet|opus).*"`.
38-    pub match_model: String,
--
42-}
43-
44-/// Agent routing configuration.
45-///
46-/// Maps model name patterns to adapters. When a bead specifies a model,
47:/// the routing rules are evaluated in order to determine which adapter to use.
48-#[derive(Debug, Clone, Default, Serialize, Deserialize)]
49-pub struct RoutingConfig {
50-    /// Ordered list of routing rules (first match wins).
51-    #[serde(default)]
52:    pub rules: Vec<RoutingRule>,
53-
54-    /// Fallback adapter when no rules match (defaults to `agent.default`).
55-    #[serde(default)]
56-    pub default_adapter: Option<String>,
57-
--
118-
119-    /// Directory containing adapter TOML files.
120-    #[serde(default = "AgentConfig::default_adapters_dir")]
121-    pub adapters_dir: PathBuf,
122-
123:    /// Model-to-adapter routing rules (optional).
124-    #[serde(default)]
125-    pub routing: Option<RoutingConfig>,
126-}
127-
128-impl Default for AgentConfig {
--
153-    /// Routers Anthropic Claude models (sonnet, opus, fable, haiku) to claude-print
154-    /// to use subscription billing before the June 15, 2026 API credit transition.
155-    /// All other models fall back to claude-code-glm-4.7.
156-    fn default_routing() -> Option<RoutingConfig> {
157-        Some(RoutingConfig {
158:            rules: vec![RoutingRule {
159-                // Match Anthropic Claude models on subscription billing
160-                // Patterns: claude-sonnet-4-6, claude-opus-4-6, claude-fable-5, claude-haiku-4-5-20251001
161-                match_model: "(claude-)?(sonnet|opus|fable|haiku).*".to_string(),
162-                adapter: "claude-print".to_string(),
163-            }],
--
4992-                                value = %value,
4993-                                "invalid value for agent.timeout — expected integer"
4994-                            );
4995-                        }
4996-                    }
4997:                    "agent.routing.default_adapter" => {
4998-                        config
4999-                            .agent
5000-                            .routing
5001-                            .get_or_insert_with(RoutingConfig::default)
5002-                            .default_adapter = Some(value);
--
5383-                    });
5384-                }
5385-                // Validate that adapter is not empty.
5386-                if rule.adapter.is_empty() {
5387-                    errors.push(ConfigError {
5388:                        field: format!("agent.routing.rules[{}].adapter", idx),
5389-                        message: "must not be empty".to_string(),
5390-                    });
5391-                }
5392-            }
5393-        }
--
7355-        assert_eq!(rule.match_model, "(claude-)?(sonnet|opus|fable|haiku).*");
7356-        assert_eq!(rule.adapter, "claude-print");
7357-
7358-        // Default fallback should be claude-code-glm-4.7
7359-        assert_eq!(
7360:            routing.default_adapter.as_deref(),
7361-            Some("claude-code-glm-4.7")
7362-        );
7363-    }
7364-
7365-    #[test]
--
7382-
7383-        assert!(config.agent.routing.is_some());
7384-        let routing = config.agent.routing.as_ref().unwrap();
7385-        assert_eq!(routing.rules.len(), 2);
7386-        assert_eq!(routing.rules[0].match_model, "(claude-)?(sonnet|opus).*");
7387:        assert_eq!(routing.rules[0].adapter, "claude-print");
7388-        assert_eq!(routing.rules[1].match_model, "haiku.*");
7389:        assert_eq!(routing.rules[1].adapter, "claude-code-glm-4.7");
7390-        assert_eq!(
7391:            routing.default_adapter.as_deref(),
7392-            Some("claude-code-glm-4.7")
7393-        );
7394-    }
7395-
7396-    #[test]
--
7407-        let config = ConfigLoader::load_from_path(&path).unwrap();
7408-
7409-        assert!(config.agent.routing.is_some());
7410-        let routing = config.agent.routing.as_ref().unwrap();
7411-        assert!(routing.rules.is_empty());
7412:        assert_eq!(routing.default_adapter.as_deref(), Some("fallback-adapter"));
7413-    }
7414-
7415-    #[test]
7416-    fn invalid_regex_in_routing_rule_fails_validation() {
7417-        let mut config = Config::default();
7418-        config.agent.routing = Some(RoutingConfig {
7419:            rules: vec![RoutingRule {
7420-                match_model: "[invalid(regex".to_string(), // Unclosed bracket
7421-                adapter: "some-adapter".to_string(),
7422-            }],
7423-            default_adapter: None,
7424-            strict: false,
--
7433-            errors
7434-        );
7435-    }
7436-
7437-    #[test]
7438:    fn empty_adapter_in_routing_rule_fails_validation() {
7439-        let mut config = Config::default();
7440-        config.agent.routing = Some(RoutingConfig {
7441:            rules: vec![RoutingRule {
7442-                match_model: "sonnet".to_string(),
7443-                adapter: String::new(), // Empty adapter
7444-            }],
7445-            default_adapter: None,
7446-            strict: false,
--
7448-
7449-        let errors = ConfigLoader::validate(&config);
7450-        assert!(
7451-            errors
7452-                .iter()
7453:                .any(|e| e.field == "agent.routing.rules[0].adapter"),
7454-            "expected empty adapter error, got: {:?}",
7455-            errors
7456-        );
7457-    }
7458-
7459-    #[test]
7460-    fn valid_regex_in_routing_rule_passes_validation() {
7461-        let mut config = Config::default();
7462-        config.agent.routing = Some(RoutingConfig {
7463-            rules: vec![
7464:                RoutingRule {
7465-                    match_model: "(claude-)?sonnet.*".to_string(),
7466-                    adapter: "claude-print".to_string(),
7467-                },
7468:                RoutingRule {
7469-                    match_model: "opus".to_string(),
7470-                    adapter: "claude-opus".to_string(),
7471-                },
7472-            ],
7473-            default_adapter: Some("claude-fallback".to_string()),
--
7505-
7506-        assert!(config.agent.routing.is_some());
7507-        let routing = config.agent.routing.as_ref().unwrap();
7508-        assert_eq!(routing.rules.len(), 1);
7509-        assert_eq!(routing.rules[0].match_model, "fable.*");
7510:        assert_eq!(routing.rules[0].adapter, "fable-adapter");
7511-        assert_eq!(
7512:            routing.default_adapter.as_deref(),
7513-            Some("workspace-default")
7514-        );
7515-        assert!(sources.contains_key("agent.routing"));
7516-    }
7517-
7518-    #[test]
7519:    fn env_var_routing_default_adapter_override() {
7520-        let mut config = Config::default();
7521-        let mut sources = SourceMap::new();
7522-
7523-        let key = "NEEDLE_AGENT__ROUTING__DEFAULT_ADAPTER";
7524-        std::env::set_var(key, "env-fallback");
--
7534-                .unwrap()
7535-                .default_adapter
7536-                .as_deref(),
7537-            Some("env-fallback")
7538-        );
7539:        assert!(sources.contains_key("agent.routing.default_adapter"));
7540-    }
7541-
7542-    #[test]
7543-    fn env_var_routing_override_beats_workspace() {
7544-        let dir = tempfile::tempdir().unwrap();
--
7584-                .default_adapter
7585-                .as_deref(),
7586-            Some("env-fallback")
7587-        );
7588-        assert!(matches!(
7589:            sources.get("agent.routing.default_adapter"),
7590-            Some(ConfigSource::EnvVar(_))
7591-        ));
7592-    }
7593-
7594-    #[test]
7595-    fn multiple_validation_errors_in_routing() {
7596-        let mut config = Config::default();
7597-        config.agent.routing = Some(RoutingConfig {
7598-            rules: vec![
7599:                RoutingRule {
7600-                    match_model: "[invalid".to_string(),
7601-                    adapter: "adapter1".to_string(),
7602-                },
7603:                RoutingRule {
7604-                    match_model: "valid".to_string(),
7605-                    adapter: String::new(), // Empty adapter
7606-                },
7607:                RoutingRule {
7608-                    match_model: "(unclosed".to_string(),
7609-                    adapter: "adapter3".to_string(),
7610-                },
7611-            ],
7612-            default_adapter: None,
--
7629-
7630-    #[test]
7631-    fn routing_config_yaml_roundtrip() {
7632-        let routing = RoutingConfig {
7633-            rules: vec![
7634:                RoutingRule {
7635-                    match_model: "sonnet".to_string(),

exec
/bin/bash -lc "sed -n '270,550p' src/worker/mod.rs; rg -n \"failed to load adapters|using built-in defaults|Dispatcher::new|invalid.*adapter|load_adapters\" src/worker/mod.rs src/cli/mod.rs tests --glob '*.rs'" in /home/coding/NEEDLE
 succeeded in 0ms:
    // No-op on non-Unix platforms
}

#[cfg(not(unix))]
fn clear_global_shutdown_flag() {
    // No-op on non-Unix platforms
}

#[cfg(not(unix))]
fn register_atexit_handler(
    _worker_name: String,
    _beads_processed: u64,
    _start_time: Instant,
    _last_state: String,
    _log_file_path: Option<String>,
    _heartbeat_path: Option<String>,
) {
    // No-op on non-Unix platforms
}

#[cfg(not(unix))]
fn update_atexit_state(_last_state: String) {
    // No-op on non-Unix platforms
}

#[cfg(not(unix))]
fn clear_atexit_state() {
    // No-op on non-Unix platforms
}

/// TTL for race-lost bead exclusions.
///
/// After losing a claim race, a bead is excluded from selection for this duration
/// to prevent infinite loops where the selector returns the same bead repeatedly.
const RACE_LOST_EXCLUSION_TTL: Duration = Duration::from_secs(30);

/// Timeout for HANDLING state watchdog.
///
/// If the worker remains in HANDLING state for longer than this duration,
/// the watchdog thread will force a recovery. This is longer than the
/// inner timeouts (50s, 60s, 90s) to allow normal recovery to work first.
const HANDLING_WATCHDOG_TIMEOUT_SECS: u64 = 120;

/// The NEEDLE worker — owns and drives the full state machine.
pub struct Worker {
    config: Config,
    worker_name: String,
    store: Arc<dyn BeadStore>,
    /// Home workspace store — kept for restore after processing a remote bead.
    home_store: Arc<dyn BeadStore>,
    telemetry: Telemetry,
    strands: StrandRunner,
    claimer: Claimer,
    prompt_builder: PromptBuilder,
    dispatcher: Dispatcher,
    outcome_handler: OutcomeHandler,
    health: HealthMonitor,
    registry: Registry,
    rate_limiter: RateLimiter,
    mitosis_evaluator: MitosisEvaluator,

    // State machine fields
    state: WorkerState,
    current_bead: Option<Bead>,
    exclusion_set: HashSet<BeadId>,
    /// Race-lost exclusions with TTL — prevents re-selecting beads that just lost a claim race.
    /// Each entry is (bead_id, expiration_time). Entries are pruned on access.
    race_lost_exclusions: Vec<(BeadId, Instant)>,
    /// Beads that lost a claim race in the current selection cycle.
    /// These are added to exclusion_set to prevent immediate re-selection.
    /// Cleared at the start of the next SELECTING cycle.
    race_lost_this_cycle: HashSet<BeadId>,
    retry_count: u32,
    consecutive_race_lost: u32,
    beads_processed: u64,
    shutdown: Arc<AtomicBool>,
    last_error: Option<anyhow::Error>,
    boot_time: Option<Instant>,

    // Transient fields — pass data between state handlers within a single cycle.
    built_prompt: Option<BuiltPrompt>,
    current_strand: Option<String>,
    exec_output: Option<(AgentOutcome, bool)>,
    /// When agent execution began — used only to compute `duration_ms` for
    /// the HOOP event tap (Hook 2). Set in `do_execute`, consumed in
    /// `do_handle`.
    exec_started_at: Option<Instant>,
    /// Effort tracking data for the current bead cycle.
    last_effort: Option<EffortData>,
    /// HEAD SHA captured just before agent dispatch; used to detect new commits.
    pre_dispatch_head: Option<String>,
    /// The workspace of the current bead store — updated when switching to remote.
    /// Used to ensure heartbeat reports the actual workspace where work is happening.
    current_workspace: PathBuf,
    /// Whether `worker.booting` was already emitted externally (e.g., from CLI layer).
    /// When true, `run()` skips emitting the booting event to avoid duplicates.
    booting_emitted: bool,
    /// Waterfall restart count from the most recent select cycle (for exhausted telemetry).
    last_waterfall_restarts: u32,
    /// Names of strands that triggered waterfall restarts in the most recent cycle.
    last_restart_triggers: Vec<String>,
    /// Strand evaluations from the most recent select cycle (for exhausted telemetry).
    last_strand_evaluations: Vec<(String, String, u64)>,
    /// Timestamp when the worker entered HANDLING state.
    /// Used by the watchdog to detect stuck HANDLING state.
    handling_state_entered_at: Option<Instant>,
    /// Flag set by the watchdog thread when HANDLING state timeout is detected.
    /// The main worker loop checks this flag and forces recovery if set.
    watchdog_triggered: Arc<AtomicBool>,
    /// Handle to the watchdog thread for cleanup on worker drop.
    #[allow(dead_code)]
    watchdog_handle: Option<std::thread::JoinHandle<()>>,
    /// The current bead lifecycle span. Created when a bead is claimed and
    /// instrumented onto each state-handler future until the lifecycle ends.
    ///
    /// This must remain a `Span`, not an `EnteredSpan`: entered guards mutate a
    /// thread-local stack and cannot safely be stored across `.await` points.
    bead_lifecycle_span: Option<tracing::Span>,
    /// The last outcome for the current bead (used to record on bead.lifecycle span).
    last_outcome: Option<String>,
    /// Last observed mtime across all workspace .beads/issues.jsonl files.
    /// Used for event-driven wakeups from idle state.
    last_workspace_mtime: Option<std::time::SystemTime>,
    /// Whether the most recent select cycle found candidates but they were all excluded.
    /// Used to trigger short retry instead of full idle backoff.
    found_but_excluded: bool,
    /// Spawn-path binary metadata recorded at boot time.
    /// Used to detect in-place binary modifications during worker lifecycle.
    spawn_path_metadata: Option<crate::spawn_path::BinaryMetadata>,
}

impl Worker {
    /// Construct a worker using a pre-existing telemetry instance.
    ///
    /// Use this when telemetry has already been started (e.g. after emitting
    /// `worker.booting` from the CLI layer) so that early init steps are
    /// visible in the JSONL log.
    pub fn new_with_telemetry(
        config: Config,
        worker_name: String,
        store: Arc<dyn BeadStore>,
        telemetry: Telemetry,
    ) -> Self {
        Self::build(config, worker_name, store, telemetry, true)
    }

    /// Construct a worker from config, a worker name, and a bead store implementation.
    ///
    /// Creates its own telemetry instance. Prefer [`new_with_telemetry`] when
    /// the caller has already created and started telemetry for early boot
    /// diagnostics.
    pub fn new(config: Config, worker_name: String, store: Arc<dyn BeadStore>) -> Self {
        let qualified_id = format!("{}-{}", config.agent.default, worker_name);
        let telemetry = Telemetry::from_config(qualified_id.clone(), &config.telemetry)
            .unwrap_or_else(|e| {
                tracing::warn!(error = %e, "failed to create hook-enabled telemetry, falling back");
                Telemetry::new(qualified_id.clone())
            });
        Self::build(config, worker_name, store, telemetry, false)
    }

    /// Shared construction logic used by both [`new`] and [`new_with_telemetry`].
    fn build(
        config: Config,
        worker_name: String,
        store: Arc<dyn BeadStore>,
        telemetry: Telemetry,
        booting_emitted: bool,
    ) -> Self {
        let qualified_id = format!("{}-{}", config.agent.default, worker_name);

        // Phase: Strand setup
        let _ = telemetry.emit(EventKind::InitStepStarted {
            step: "strand_setup".to_string(),
        });
        let strand_start = Instant::now();
        let strand_registry = Registry::default_location(&config.workspace.home);
        let strands =
            StrandRunner::from_config(&config, &qualified_id, strand_registry, telemetry.clone());
        let _ = telemetry.emit(EventKind::InitStepCompleted {
            step: "strand_setup".to_string(),
            duration_ms: strand_start.elapsed().as_millis() as u64,
        });

        // Phase: Claimer creation
        let _ = telemetry.emit(EventKind::InitStepStarted {
            step: "claimer_creation".to_string(),
        });
        let claimer_start = Instant::now();
        let claimer = Claimer::new(
            store.clone(),
            std::path::PathBuf::from("/tmp"),
            config.worker.max_claim_retries,
            100,
            telemetry.clone(),
        );
        let _ = telemetry.emit(EventKind::InitStepCompleted {
            step: "claimer_creation".to_string(),
            duration_ms: claimer_start.elapsed().as_millis() as u64,
        });

        // Phase: PromptBuilder setup
        let _ = telemetry.emit(EventKind::InitStepStarted {
            step: "prompt_builder_setup".to_string(),
        });
        let prompt_start = Instant::now();
        let prompt_builder = PromptBuilder::with_workspace(
            &config.prompt,
            &config.workspace.default,
        )
        .unwrap_or_else(|e| {
            tracing::warn!(error = %e, "failed to load workspace learnings, using default prompt builder");
            PromptBuilder::new(&config.prompt)
        })
        .with_cross_workspace_skills(
            &config.strands.explore.workspaces,
            &config.workspace.labels,
        )
        .with_global_learnings(&config.strands.learning.global_learnings_file);
        let _ = telemetry.emit(EventKind::InitStepCompleted {
            step: "prompt_builder_setup".to_string(),
            duration_ms: prompt_start.elapsed().as_millis() as u64,
        });

        // Phase: Dispatcher setup (adapter loading)
        let _ = telemetry.emit(EventKind::InitStepStarted {
            step: "dispatcher_setup".to_string(),
        });
        let dispatcher_start = Instant::now();
        let dispatcher = match Dispatcher::new(&config, telemetry.clone()) {
            Ok(d) => d,
            Err(e) => {
                tracing::warn!(error = %e, "failed to load adapters, using built-in defaults");
                let builtins = crate::dispatch::builtin_adapters()
                    .into_iter()
                    .map(|a| (a.name.clone(), a))
                    .collect();
                Dispatcher::with_adapters(builtins, telemetry.clone(), config.agent.timeout)
            }
        };
        let _ = telemetry.emit(EventKind::InitStepCompleted {
            step: "dispatcher_setup".to_string(),
            duration_ms: dispatcher_start.elapsed().as_millis() as u64,
        });

        // Phase: OutcomeHandler creation
        let _ = telemetry.emit(EventKind::InitStepStarted {
            step: "outcome_handler_creation".to_string(),
        });
        let outcome_start = Instant::now();
        let outcome_handler = OutcomeHandler::new(config.clone(), telemetry.clone());
        let _ = telemetry.emit(EventKind::InitStepCompleted {
            step: "outcome_handler_creation".to_string(),
            duration_ms: outcome_start.elapsed().as_millis() as u64,
        });

        // Create the shutdown flag BEFORE creating HealthMonitor so we can share it.
        // This ensures that when the heartbeat emitter's circuit breaker fires,
        // it sets the worker's shutdown flag (not its own private flag), allowing
        // the main worker loop to gracefully stop with worker.stopped telemetry.
        let shutdown = Arc::new(AtomicBool::new(false));

        // Phase: HealthMonitor setup
        let _ = telemetry.emit(EventKind::InitStepStarted {
            step: "health_monitor_setup".to_string(),
        });
        let health_start = Instant::now();
        let health = HealthMonitor::new(
            config.clone(),
            worker_name.clone(),
            telemetry.clone(),
            Some(shutdown.clone()),
        );
        let _ = telemetry.emit(EventKind::InitStepCompleted {
            step: "health_monitor_setup".to_string(),
            duration_ms: health_start.elapsed().as_millis() as u64,
        });

        // Phase: RateLimiter setup
        let _ = telemetry.emit(EventKind::InitStepStarted {
            step: "rate_limiter_setup".to_string(),
src/cli/mod.rs:850:    let model = dispatch::load_adapters(&config.agent.adapters_dir, &dispatch::builtin_adapters())
src/cli/mod.rs:3959:    match dispatch::load_adapters(&config.agent.adapters_dir, &dispatch::builtin_adapters()) {
tests/routing_integration.rs:1662:            make_rule("[invalid(unclosed", "bad-pattern-adapter"), // Invalid regex
tests/routing_integration.rs:1694:    // Test that when all routing patterns are invalid, the default adapter is used.
tests/routing_integration.rs:1701:            make_rule("[invalid1", "adapter1"), // Invalid
tests/routing_integration.rs:1726:        "All invalid patterns should fall back to default adapter"
tests/routing_matcher_baseline.rs:195:        make_rule("[invalid(regex", "invalid-adapter"),
src/worker/mod.rs:499:        let dispatcher = match Dispatcher::new(&config, telemetry.clone()) {
src/worker/mod.rs:502:                tracing::warn!(error = %e, "failed to load adapters, using built-in defaults");
src/worker/mod.rs:1129:            eprintln!("Startup is aborting to prevent claiming beads with an invalid adapter configuration.");
src/worker/mod.rs:6336:                    adapter: "invalid-adapter".to_string(),
tests/integration_tests.rs:1506:        stderr_output.contains("invalid adapter") ||
tests/integration_tests.rs:1865:        stderr_output.contains("invalid adapter") ||

codex
The adjacent routing code confirms the correct fix point: `Worker::resolve_adapter`, after `apply_routing_rules` returns the adapter and pattern but before the dispatcher map lookup. I’ll require a matching `<adapters_dir>/<adapter>.yaml` (or `.yml`) for actual routing selections, so even a same-named built-in cannot satisfy the route; ordinary non-routed built-in defaults remain compatible.
apply patch
patch: completed
/home/coding/NEEDLE/src/worker/mod.rs
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..c4c73aad7001718a0e5770326849ab06bb2ac94b
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..c4c73aad7001718a0e5770326849ab06bb2ac94b
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

codex
The implementation now checks both supported YAML extensions immediately after routing. The focused test deliberately routes to `claude-sonnet`, a built-in name with no YAML, proving the dispatcher cannot silently satisfy a routed selection from its built-in map. I’m reviewing the precise diff and running formatting plus the targeted worker test next.
exec
/bin/bash -lc 'git diff -- src/worker/mod.rs && cargo fmt --check' in /home/coding/NEEDLE
 exited 1 in 638ms:
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:182:
         let dependencies = if bead.dependencies.is_empty() {
             "None".to_string()
         } else {
[31m-            bead
[m[31m-                .dependencies
[m[32m+            bead.dependencies
[m                 .iter()
                 .map(|d| format!("- {} ({})", d.id, d.dependency_type))
                 .collect::<Vec<_>>()
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:194:
         let dependents = if bead.dependents.is_empty() {
             "None".to_string()
         } else {
[31m-            bead
[m[31m-                .dependents
[m[32m+            bead.dependents
[m                 .iter()
                 .map(|d| format!("- {} ({})", d.id, d.dependency_type))
                 .collect::<Vec<_>>()
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:213:
         let comments = if bead.comments.is_empty() {
             "None".to_string()
         } else {
[31m-            bead
[m[31m-                .comments
[m[32m+            bead.comments
[m                 .iter()
                 .take(3) // Only show most recent 3
[31m-                .map(|c| {
[m[31m-                    format!(
[m[31m-                        "- [{}] {}",
[m[31m-                        c.created_at.format("%Y-%m-%d %H:%M"),
[m[31m-                        c.text
[m[31m-                    )
[m[31m-                })
[m[32m+                .map(|c| format!("- [{}] {}", c.created_at.format("%Y-%m-%d %H:%M"), c.text))
[m                 .collect::<Vec<_>>()
                 .join("\n")
         };
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:234:
         Ok(template
             .replace("{bead_id}", bead.id.as_ref())
             .replace("{bead_title}", &bead.title)
[31m-            .replace("{bead_body}", bead.body.as_deref().unwrap_or("(no description)"))
[m[32m+            .replace(
[m[32m+                "{bead_body}",
[m[32m+                bead.body.as_deref().unwrap_or("(no description)"),
[m[32m+            )
[m             .replace("{priority}", &bead.priority.to_string())
             .replace("{status}", &format!("{:?}", bead.status))
             .replace("{assignee}", assignee)
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:250:
     /// Parse and validate the LLM response.
     fn parse_response(&self, response_text: &str) -> ValidationResult {
         // Strip markdown code fencing if present
[31m-        let cleaned = response_text.trim().trim_start_matches("```json").trim_end_matches("```").trim();
[m[32m+        let cleaned = response_text
[m[32m+            .trim()
[m[32m+            .trim_start_matches("```json")
[m[32m+            .trim_end_matches("```")
[m[32m+            .trim();
[m 
         // Parse JSON
         let parsed: ResolveResponse = match serde_json::from_str(cleaned) {
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:336:
         "resolve"
     }
 
[31m-    async fn evaluate(&self, _store: &dyn crate::bead_store::BeadStore, _exclusions: &HashSet<BeadId>) -> StrandResult {
[m[32m+    async fn evaluate(
[m[32m+        &self,
[m[32m+        _store: &dyn crate::bead_store::BeadStore,
[m[32m+        _exclusions: &HashSet<BeadId>,
[m[32m+    ) -> StrandResult {
[m         // Resolve doesn't query the store — it receives beads from the caller
         // This is evaluated in the worker loop, not in the strand waterfall
         StrandResult::NoWork
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:409:
                 );
 
                 // Emit telemetry
[31m-                let _ = self.telemetry.emit(crate::telemetry::EventKind::ResolveEvaluated {
[m[31m-                    bead_id: bead.id.clone(),
[m[31m-                    decision: format!("{:?}", decision),
[m[31m-                    evidence: outcome.evidence().to_string(),
[m[31m-                });
[m[32m+                let _ = self
[m[32m+                    .telemetry
[m[32m+                    .emit(crate::telemetry::EventKind::ResolveEvaluated {
[m[32m+                        bead_id: bead.id.clone(),
[m[32m+                        decision: format!("{:?}", decision),
[m[32m+                        evidence: outcome.evidence().to_string(),
[m[32m+                    });
[m 
                 Ok(outcome)
             }
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:438:
 #[cfg(test)]
 mod tests {
     use super::*;
[31m-    use crate::types::{BeadId, BeadStatus, ClaimResult, BrDependency};
[m     use crate::bead_store::Filters;
     use crate::telemetry::test_utils::TestHelper;
[31m-    use chrono::{Utc, TimeZone};
[m[32m+    use crate::types::{BeadId, BeadStatus, BrDependency, ClaimResult};
[m[32m+    use chrono::{TimeZone, Utc};
[m     use std::path::PathBuf;
 
     /// A mock store for tests.
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:545:
 
     #[test]
     fn parse_valid_complete_response() {
[31m-        let strand = ResolveStrand::new(
[m[31m-            ResolveConfig::default(),
[m[31m-            Telemetry::new("test".to_string()),
[m[31m-        );
[m[32m+        let strand =
[m[32m+            ResolveStrand::new(ResolveConfig::default(), Telemetry::new("test".to_string()));
[m         let response = r#"{"decision": "complete", "evidence": "All dependencies satisfied, ready to implement"}"#;
         let result = strand.parse_response(response);
 
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:555:
         match result {
             ValidationResult::Valid { decision, outcome } => {
                 assert_eq!(decision, ResolveDecision::Complete);
[31m-                assert_eq!(outcome.evidence(), "All dependencies satisfied, ready to implement");
[m[32m+                assert_eq!(
[m[32m+                    outcome.evidence(),
[m[32m+                    "All dependencies satisfied, ready to implement"
[m[32m+                );
[m             }
             ValidationResult::Invalid { .. } => panic!("Expected valid response"),
         }
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:563:
 
     #[test]
     fn parse_valid_retry_response() {
[31m-        let strand = ResolveStrand::new(
[m[31m-            ResolveConfig::default(),
[m[31m-            Telemetry::new("test".to_string()),
[m[31m-        );
[m[32m+        let strand =
[m[32m+            ResolveStrand::new(ResolveConfig::default(), Telemetry::new("test".to_string()));
[m         let response = r#"{"decision": "retry", "evidence": "External service temporarily unavailable", "retry_after_seconds": 300}"#;
         let result = strand.parse_response(response);
 
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:573:
         match result {
             ValidationResult::Valid { decision, outcome } => {
                 assert_eq!(decision, ResolveDecision::Retry);
[31m-                assert_eq!(outcome.evidence(), "External service temporarily unavailable");
[m[31m-                if let ResolveOutcome::Retry { retry_after_seconds, .. } = outcome {
[m[32m+                assert_eq!(
[m[32m+                    outcome.evidence(),
[m[32m+                    "External service temporarily unavailable"
[m[32m+                );
[m[32m+                if let ResolveOutcome::Retry {
[m[32m+                    retry_after_seconds,
[m[32m+                    ..
[m[32m+                } = outcome
[m[32m+                {
[m                     assert_eq!(retry_after_seconds, 300);
                 } else {
                     panic!("Expected Retry outcome");
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:586:
 
     #[test]
     fn parse_valid_blocked_response() {
[31m-        let strand = ResolveStrand::new(
[m[31m-            ResolveConfig::default(),
[m[31m-            Telemetry::new("test".to_string()),
[m[31m-        );
[m[32m+        let strand =
[m[32m+            ResolveStrand::new(ResolveConfig::default(), Telemetry::new("test".to_string()));
[m         let response = r#"{"decision": "blocked", "evidence": "Depends on unimplemented feature", "blocker_id": "needle-dep-001"}"#;
         let result = strand.parse_response(response);
 
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:609:
 
     #[test]
     fn parse_valid_split_response() {
[31m-        let strand = ResolveStrand::new(
[m[31m-            ResolveConfig::default(),
[m[31m-            Telemetry::new("test".to_string()),
[m[31m-        );
[m[32m+        let strand =
[m[32m+            ResolveStrand::new(ResolveConfig::default(), Telemetry::new("test".to_string()));
[m         let response = r#"{"decision": "split", "evidence": "Task is too large", "split_reason": "Should be decomposed into 3 phases"}"#;
         let result = strand.parse_response(response);
 
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:621:
                 assert_eq!(decision, ResolveDecision::Split);
                 assert_eq!(outcome.evidence(), "Task is too large");
                 if let ResolveOutcome::Split { split_reason, .. } = outcome {
[31m-                    assert_eq!(split_reason, Some("Should be decomposed into 3 phases".to_string()));
[m[32m+                    assert_eq!(
[m[32m+                        split_reason,
[m[32m+                        Some("Should be decomposed into 3 phases".to_string())
[m[32m+                    );
[m                 } else {
                     panic!("Expected Split outcome");
                 }
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:632:
 
     #[test]
     fn parse_invalid_json() {
[31m-        let strand = ResolveStrand::new(
[m[31m-            ResolveConfig::default(),
[m[31m-            Telemetry::new("test".to_string()),
[m[31m-        );
[m[32m+        let strand =
[m[32m+            ResolveStrand::new(ResolveConfig::default(), Telemetry::new("test".to_string()));
[m         let response = "not valid json";
         let result = strand.parse_response(response);
 
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:649:
 
     #[test]
     fn parse_invalid_decision_value() {
[31m-        let strand = ResolveStrand::new(
[m[31m-            ResolveConfig::default(),
[m[31m-            Telemetry::new("test".to_string()),
[m[31m-        );
[m[32m+        let strand =
[m[32m+            ResolveStrand::new(ResolveConfig::default(), Telemetry::new("test".to_string()));
[m         let response = r#"{"decision": "invalid", "evidence": "test"}"#;
         let result = strand.parse_response(response);
 
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:666:
 
     #[test]
     fn parse_blocked_without_blocker_id() {
[31m-        let strand = ResolveStrand::new(
[m[31m-            ResolveConfig::default(),
[m[31m-            Telemetry::new("test".to_string()),
[m[31m-        );
[m[32m+        let strand =
[m[32m+            ResolveStrand::new(ResolveConfig::default(), Telemetry::new("test".to_string()));
[m         let response = r#"{"decision": "blocked", "evidence": "test"}"#;
         let result = strand.parse_response(response);
 
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:683:
 
     #[test]
     fn parse_response_with_markdown_fencing() {
[31m-        let strand = ResolveStrand::new(
[m[31m-            ResolveConfig::default(),
[m[31m-            Telemetry::new("test".to_string()),
[m[31m-        );
[m[32m+        let strand =
[m[32m+            ResolveStrand::new(ResolveConfig::default(), Telemetry::new("test".to_string()));
[m         let response = r#"```json
 {"decision": "complete", "evidence": "test"}
 ```"#;
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:702:
 
     #[test]
     fn parse_retry_with_default_timeout() {
[31m-        let strand = ResolveStrand::new(
[m[31m-            ResolveConfig::default(),
[m[31m-            Telemetry::new("test".to_string()),
[m[31m-        );
[m[32m+        let strand =
[m[32m+            ResolveStrand::new(ResolveConfig::default(), Telemetry::new("test".to_string()));
[m         let response = r#"{"decision": "retry", "evidence": "test"}"#;
         let result = strand.parse_response(response);
 
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:712:
         match result {
             ValidationResult::Valid { decision, outcome } => {
                 assert_eq!(decision, ResolveDecision::Retry);
[31m-                if let ResolveOutcome::Retry { retry_after_seconds, .. } = outcome {
[m[32m+                if let ResolveOutcome::Retry {
[m[32m+                    retry_after_seconds,
[m[32m+                    ..
[m[32m+                } = outcome
[m[32m+                {
[m                     assert_eq!(retry_after_seconds, 600); // default
                 } else {
                     panic!("Expected Retry outcome");
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:724:
 
     #[test]
     fn build_prompt_includes_all_variables() {
[31m-        let strand = ResolveStrand::new(
[m[31m-            ResolveConfig::default(),
[m[31m-            Telemetry::new("test".to_string()),
[m[31m-        );
[m[32m+        let strand =
[m[32m+            ResolveStrand::new(ResolveConfig::default(), Telemetry::new("test".to_string()));
[m         let bead = make_test_bead();
         let workspace = PathBuf::from("/tmp/test");
         let prompt = strand.build_prompt(&bead, &workspace).unwrap();
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:740:
 
     #[test]
     fn strand_name_is_resolve() {
[31m-        let strand = ResolveStrand::new(
[m[31m-            ResolveConfig::default(),
[m[31m-            Telemetry::new("test".to_string()),
[m[31m-        );
[m[32m+        let strand =
[m[32m+            ResolveStrand::new(ResolveConfig::default(), Telemetry::new("test".to_string()));
[m         assert_eq!(strand.name(), "resolve");
     }
 
Diff in /home/coding/NEEDLE/src/strand/resolve.rs:750:
     #[tokio::test]
     async fn evaluate_returns_no_work() {
[31m-        let strand = ResolveStrand::new(
[m[31m-            ResolveConfig::default(),
[m[31m-            Telemetry::new("test".to_string()),
[m[31m-        );
[m[32m+        let strand =
[m[32m+            ResolveStrand::new(ResolveConfig::default(), Telemetry::new("test".to_string()));
[m         let store = MockStore;
         let exclusions = HashSet::new();
         let result = strand.evaluate(&store, &exclusions).await;

diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..c4c73aad7001718a0e5770326849ab06bb2ac94b
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

exec
/bin/bash -lc 'cargo test worker::tests::resolve_adapter_fails_when_routed_yaml_is_missing --lib' in /home/coding/NEEDLE
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..c4c73aad7001718a0e5770326849ab06bb2ac94b
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..c4c73aad7001718a0e5770326849ab06bb2ac94b
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

 exited 101 in 11264ms:
   Compiling needle v0.3.1 (/home/coding/NEEDLE)
error[E0603]: struct import `ResolveConfig` is private
    --> src/strand/mod.rs:68:19
     |
  68 | pub use resolve::{ResolveConfig, ResolveStrand};
     |                   ^^^^^^^^^^^^^ private struct import
     |
note: the struct import `ResolveConfig` is defined here...
    --> src/strand/resolve.rs:11:5
     |
  11 | use crate::config::ResolveConfig;
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
note: ...and refers to the struct `ResolveConfig` which is defined here
    --> src/config/mod.rs:3452:1
     |
3452 | pub struct ResolveConfig {
     | ^^^^^^^^^^^^^^^^^^^^^^^^ you could import this directly
help: import `ResolveConfig` directly
     |
  68 | pub use resolve::{config::ResolveConfig, ResolveStrand};
     |                   ++++++++

warning: unused import: `crate::prompt::PromptBuilder`
  --> src/strand/resolve.rs:12:5
   |
12 | use crate::prompt::PromptBuilder;
   |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   |
   = note: `#[warn(unused_imports)]` (part of `#[warn(unused)]`) on by default

warning: unused import: `StrandError`
  --> src/strand/resolve.rs:14:67
   |
14 | use crate::types::{Bead, BeadId, ResolveDecision, ResolveOutcome, StrandError, StrandResult};
   |                                                                   ^^^^^^^^^^^

warning: unused import: `PathBuf`
  --> src/strand/resolve.rs:18:23
   |
18 | use std::path::{Path, PathBuf};
   |                       ^^^^^^^

warning: unused import: `BrDependency`
   --> src/strand/resolve.rs:441:57
    |
441 |     use crate::types::{BeadId, BeadStatus, ClaimResult, BrDependency};
    |                                                         ^^^^^^^^^^^^

warning: unused import: `crate::telemetry::test_utils::TestHelper`
   --> src/strand/resolve.rs:443:9
    |
443 |     use crate::telemetry::test_utils::TestHelper;
    |         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

warning: unused import: `TimeZone`
   --> src/strand/resolve.rs:444:23
    |
444 |     use chrono::{Utc, TimeZone};
    |                       ^^^^^^^^

error[E0046]: not all trait items implemented, missing: `close`
    --> src/bead_store/cli_store.rs:292:1
     |
 292 | impl BeadStore for CliBeadStore {
     | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/claim/mod.rs:659:5
     |
 659 |     impl BeadStore for MockBeadStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/claim/mod.rs:1320:9
     |
1320 |         impl BeadStore for FailingShowStore {
     |         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/mitosis/mod.rs:1366:5
     |
1366 |     impl BeadStore for MockStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/outcome/mod.rs:1299:5
     |
1299 |     impl BeadStore for MockBeadStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/outcome/mod.rs:1989:9
     |
1989 |         impl BeadStore for SlowFlushStore {
     |         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/outcome/mod.rs:2097:9
     |
2097 |         impl BeadStore for SlowReleaseStore {
     |         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/outcome/mod.rs:2265:9
     |
2265 |         impl BeadStore for SlowShowStore {
     |         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/peer/mod.rs:312:5
     |
 312 |     impl BeadStore for MockBeadStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/explore.rs:990:5
     |
 990 |     impl BeadStore for DummyStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/explore.rs:1086:5
     |
1086 |     impl BeadStore for AdaptiveScanStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/explore.rs:2448:5
     |
2448 |     impl BeadStore for ExcludedCandidatesStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/explore.rs:2580:5
     |
2580 |     impl BeadStore for AssignedBeadsStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/explore.rs:2705:5
     |
2705 |     impl BeadStore for BlockedBeadsStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/explore.rs:2820:5
     |
2820 |     impl BeadStore for ExcludedAndAssignedStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/explore.rs:2957:5
     |
2957 |     impl BeadStore for ValidBeadStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/knot.rs:313:5
     |
 313 |     impl BeadStore for KnotTestStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/knot.rs:393:5
     |
 393 |     impl BeadStore for FailingStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/knot.rs:843:5
     |
 843 |     impl BeadStore for NoHomeStoreMock {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/mend.rs:1637:5
     |
1637 |     impl BeadStore for MockBeadStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/mend.rs:1744:5
     |
1744 |     impl BeadStore for FailingStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/mend.rs:4457:5
     |
4457 |     impl BeadStore for DepTrackingMockStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/pluck.rs:628:5
     |
 628 |     impl BeadStore for MemoryStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/pluck.rs:739:5
     |
 739 |     impl BeadStore for UnfilteredStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/pluck.rs:832:5
     |
 832 |     impl BeadStore for FailingStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/pulse.rs:534:5
     |
 534 |     impl BeadStore for MockStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/reflect.rs:1501:9
     |
1501 |         impl crate::bead_store::BeadStore for NoOpStore {
     |         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/resolve.rs:451:5
     |
 451 |     impl crate::bead_store::BeadStore for MockStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/splice.rs:1155:5
     |
1155 |     impl BeadStore for NoOpStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/unravel.rs:650:5
     |
 650 |     impl BeadStore for MockStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/weave.rs:734:5
     |
 734 |     impl BeadStore for MockStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/strand/mod.rs:659:5
     |
 659 |     impl BeadStore for EmptyStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/worker/mod.rs:4226:5
     |
4226 |     impl BeadStore for MockStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/worker/mod.rs:4571:5
     |
4571 |     impl BeadStore for RaceLostStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/worker/mod.rs:4659:5
     |
4659 |     impl BeadStore for NotClaimableStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0046]: not all trait items implemented, missing: `close`
    --> src/worker/mod.rs:4751:5
     |
4751 |     impl BeadStore for SuspectStore {
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ missing `close` in implementation
     |
    ::: src/bead_store/mod.rs:1214:5
     |
1214 |     async fn close(&self, id: &BeadId) -> Result<()>;
     |     ------------------------------------------------- `close` from trait

error[E0308]: mismatched types
   --> src/outcome/mod.rs:444:74
    |
444 |                     match verify_shipped_work(&current, &bead.workspace, store).await {
    |                           -------------------                            ^^^^^ expected `&Arc<dyn BeadStore>`, found `&dyn BeadStore`
    |                           |
    |                           arguments to this function are incorrect
    |
    = note: expected reference `&std::sync::Arc<(dyn bead_store::BeadStore + 'static)>`
               found reference `&dyn bead_store::BeadStore`
note: function defined here
   --> src/validation/shipped_work.rs:63:14
    |
 63 | pub async fn verify_shipped_work(
    |              ^^^^^^^^^^^^^^^^^^^
...
 66 |     store: &Arc<dyn BeadStore>,
    |     --------------------------

error[E0277]: `std::result::Result<std::process::Output, std::io::Error>` is not a future
   --> src/strand/resolve.rs:323:51
    |
323 |             let output = child.wait_with_output().await?;
    |                                                   ^^^^^ `std::result::Result<std::process::Output, std::io::Error>` is not a future
    |
    = help: the trait `futures::Future` is not implemented for `std::result::Result<std::process::Output, std::io::Error>`
    = note: std::result::Result<std::process::Output, std::io::Error> must be a future or must implement `IntoFuture` to be awaited
    = note: required for `std::result::Result<std::process::Output, std::io::Error>` to implement `std::future::IntoFuture`
help: remove the `.await`
    |
323 -             let output = child.wait_with_output().await?;
323 +             let output = child.wait_with_output()?;
    |

error[E0599]: no method named `instrument` found for opaque type `impl futures::Future<Output = std::result::Result<std::process::Output, anyhow::Error>>` in the current scope
   --> src/strand/resolve.rs:378:58
    |
378 |         let output = match self.invoke_resolver(&prompt).instrument(span).await {
    |                                                          ^^^^^^^^^^ method not found in `impl futures::Future<Output = std::result::Result<std::process::Output, anyhow::Error>>`
    |
   ::: /home/coding/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/tracing-0.1.44/src/instrument.rs:86:8
    |
 86 |     fn instrument(self, span: Span) -> Instrumented<Self> {
    |        ---------- the method is available for `impl futures::Future<Output = std::result::Result<std::process::Output, anyhow::Error>>` here
    |
    = help: items from traits can only be used if the trait is in scope
help: trait `Instrument` which provides `instrument` is implemented but not in scope; perhaps you want to import it
    |
 11 + use tracing::Instrument;
    |

error[E0599]: no variant named `ResolveEvaluated` found for enum `telemetry::EventKind`
   --> src/strand/resolve.rs:412:74
    |
412 |                 let _ = self.telemetry.emit(crate::telemetry::EventKind::ResolveEvaluated {
    |                                                                          ^^^^^^^^^^^^^^^^ variant not found in `telemetry::EventKind`
    |
   ::: src/telemetry/mod.rs:93:1
    |
 93 | pub enum EventKind {
    | ------------------ variant `ResolveEvaluated` not found here

error[E0599]: no method named `name` found for struct `resolve::ResolveStrand` in the current scope
   --> src/strand/resolve.rs:747:27
    |
143 | pub struct ResolveStrand {
    | ------------------------ method `name` not found for this struct
...
747 |         assert_eq!(strand.name(), "resolve");
    |                           ^^^^ method not found in `resolve::ResolveStrand`
    |
   ::: src/strand/mod.rs:77:8
    |
 77 |     fn name(&self) -> &str;
    |        ---- the method is available for `resolve::ResolveStrand` here
    |
    = help: items from traits can only be used if the trait is in scope
help: trait `Strand` which provides `name` is implemented but not in scope; perhaps you want to import it
    |
440 +     use crate::strand::Strand;
    |

error[E0599]: no method named `evaluate` found for struct `resolve::ResolveStrand` in the current scope
   --> src/strand/resolve.rs:758:29
    |
143 | pub struct ResolveStrand {
    | ------------------------ method `evaluate` not found for this struct
...
758 |         let result = strand.evaluate(&store, &exclusions).await;
    |                             ^^^^^^^^ method not found in `resolve::ResolveStrand`
    |
   ::: src/strand/mod.rs:80:14
    |
 80 |     async fn evaluate(&self, store: &dyn BeadStore, exclusions: &HashSet<BeadId>) -> StrandResult;
    |              -------- the method is available for `resolve::ResolveStrand` here
    |
    = help: items from traits can only be used if the trait is in scope
help: trait `Strand` which provides `evaluate` is implemented but not in scope; perhaps you want to import it
    |
440 +     use crate::strand::Strand;
    |

error[E0004]: non-exhaustive patterns: `&telemetry::EventKind::UpgradeCheckStarted { .. }`, `&telemetry::EventKind::UpgradeCheckCompleted { .. }` and `&telemetry::EventKind::UpgradeCheckFailed { .. }` not covered
    --> src/telemetry/mod.rs:2233:15
     |
2233 |         match self {
     |               ^^^^ patterns `&telemetry::EventKind::UpgradeCheckStarted { .. }`, `&telemetry::EventKind::UpgradeCheckCompleted { .. }` and `&telemetry::EventKind::UpgradeCheckFailed { .. }` not covered
     |
note: `telemetry::EventKind` defined here
    --> src/telemetry/mod.rs:93:10
     |
  93 | pub enum EventKind {
     |          ^^^^^^^^^
...
 625 |     UpgradeCheckStarted {
     |     ------------------- not covered
...
 630 |     UpgradeCheckCompleted {
     |     --------------------- not covered
...
 643 |     UpgradeCheckFailed {
     |     ------------------ not covered
     = note: the matched value is of type `&telemetry::EventKind`
help: ensure that all possible cases are being handled by adding a match arm with a wildcard pattern, a match arm with multiple or-patterns as shown, or multiple match arms
     |
2372 ~             | EventKind::Log { .. } => None,
2373 ~             &telemetry::EventKind::UpgradeCheckStarted { .. } | &telemetry::EventKind::UpgradeCheckCompleted { .. } | &telemetry::EventKind::UpgradeCheckFailed { .. } => todo!(),
     |

Some errors have detailed explanations: E0004, E0046, E0277, E0308, E0599, E0603.
For more information about an error, try `rustc --explain E0004`.
warning: `needle` (lib test) generated 6 warnings
error: could not compile `needle` (lib test) due to 44 previous errors; 6 warnings emitted

codex
The targeted test could not compile the shared checkout because concurrent work currently has unrelated repository-wide errors: the new `ResolveStrand` export is incomplete, many `BeadStore` mocks lack a newly required `close`, and `OutcomeHandler` has an incompatible store type. `cargo fmt --check` also reports formatting only in that concurrent `src/strand/resolve.rs`; our `src/worker/mod.rs` hunk is formatted. I’ll still validate this change against a clean `HEAD` snapshot, then stage only our hunks so no concurrent edits are committed.
exec
/bin/bash -lc 'tmpdir=$(mktemp -d /home/coding/scratch/needle-missing-route.XXXXXX)
printf '"'%s\\n' \""'$tmpdir"
git archive HEAD | tar -x -C "$tmpdir"' in /home/coding/NEEDLE
 succeeded in 0ms:
/home/coding/scratch/needle-missing-route.WaGL5D

diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..c4c73aad7001718a0e5770326849ab06bb2ac94b
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

apply patch
patch: completed
/home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..c4c73aad7001718a0e5770326849ab06bb2ac94b
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..c4c73aad7001718a0e5770326849ab06bb2ac94b
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

exec
/bin/bash -lc 'cargo fmt --check && cargo test worker::tests::resolve_adapter_fails_when_routed_yaml_is_missing --lib' in /home/coding/scratch/needle-missing-route.WaGL5D
diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..c4c73aad7001718a0e5770326849ab06bb2ac94b
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..c4c73aad7001718a0e5770326849ab06bb2ac94b
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

 exited 101 in 12842ms:
    Blocking waiting for file lock on package cache
    Blocking waiting for file lock on package cache
    Blocking waiting for file lock on package cache
    Blocking waiting for file lock on artifact directory
   Compiling needle v0.3.1 (/home/coding/scratch/needle-missing-route.WaGL5D)
error[E0308]: mismatched types
   --> src/outcome/mod.rs:444:74
    |
444 |                     match verify_shipped_work(&current, &bead.workspace, store).await {
    |                           -------------------                            ^^^^^ expected `&Arc<dyn BeadStore>`, found `&dyn BeadStore`
    |                           |
    |                           arguments to this function are incorrect
    |
    = note: expected reference `&std::sync::Arc<(dyn bead_store::BeadStore + 'static)>`
               found reference `&dyn bead_store::BeadStore`
note: function defined here
   --> src/validation/shipped_work.rs:63:14
    |
 63 | pub async fn verify_shipped_work(
    |              ^^^^^^^^^^^^^^^^^^^
...
 66 |     store: &Arc<dyn BeadStore>,
    |     --------------------------

error[E0004]: non-exhaustive patterns: `&telemetry::EventKind::UpgradeCheckStarted { .. }`, `&telemetry::EventKind::UpgradeCheckCompleted { .. }` and `&telemetry::EventKind::UpgradeCheckFailed { .. }` not covered
    --> src/telemetry/mod.rs:961:15
     |
 961 |         match self {
     |               ^^^^ patterns `&telemetry::EventKind::UpgradeCheckStarted { .. }`, `&telemetry::EventKind::UpgradeCheckCompleted { .. }` and `&telemetry::EventKind::UpgradeCheckFailed { .. }` not covered
     |
note: `telemetry::EventKind` defined here
    --> src/telemetry/mod.rs:93:10
     |
  93 | pub enum EventKind {
     |          ^^^^^^^^^
...
 625 |     UpgradeCheckStarted {
     |     ------------------- not covered
...
 630 |     UpgradeCheckCompleted {
     |     --------------------- not covered
...
 643 |     UpgradeCheckFailed {
     |     ------------------ not covered
     = note: the matched value is of type `&telemetry::EventKind`
help: ensure that all possible cases are being handled by adding a match arm with a wildcard pattern, a match arm with multiple or-patterns as shown, or multiple match arms
     |
1097 ~             EventKind::Log { bead_id, .. } => bead_id.clone(),
1098 ~             &telemetry::EventKind::UpgradeCheckStarted { .. } | &telemetry::EventKind::UpgradeCheckCompleted { .. } | &telemetry::EventKind::UpgradeCheckFailed { .. } => todo!(),
     |

error[E0004]: non-exhaustive patterns: `&telemetry::EventKind::UpgradeCheckStarted { .. }`, `&telemetry::EventKind::UpgradeCheckCompleted { .. }` and `&telemetry::EventKind::UpgradeCheckFailed { .. }` not covered
    --> src/telemetry/mod.rs:2230:15
     |
2230 |         match self {
     |               ^^^^ patterns `&telemetry::EventKind::UpgradeCheckStarted { .. }`, `&telemetry::EventKind::UpgradeCheckCompleted { .. }` and `&telemetry::EventKind::UpgradeCheckFailed { .. }` not covered
     |
note: `telemetry::EventKind` defined here
    --> src/telemetry/mod.rs:93:10
     |
  93 | pub enum EventKind {
     |          ^^^^^^^^^
...
 625 |     UpgradeCheckStarted {
     |     ------------------- not covered
...
 630 |     UpgradeCheckCompleted {
     |     --------------------- not covered
...
 643 |     UpgradeCheckFailed {
     |     ------------------ not covered
     = note: the matched value is of type `&telemetry::EventKind`
help: ensure that all possible cases are being handled by adding a match arm with a wildcard pattern, a match arm with multiple or-patterns as shown, or multiple match arms
     |
2369 ~             | EventKind::Log { .. } => None,
2370 ~             &telemetry::EventKind::UpgradeCheckStarted { .. } | &telemetry::EventKind::UpgradeCheckCompleted { .. } | &telemetry::EventKind::UpgradeCheckFailed { .. } => todo!(),
     |

Some errors have detailed explanations: E0004, E0308.
For more information about an error, try `rustc --explain E0004`.
error: could not compile `needle` (lib test) due to 3 previous errors

exec
/bin/bash -lc "rg -n \"startup aborted|Configured agent adapter|adapter preflight failed|Underlying error|not found in any of the expected\" src tests --glob '*.rs'" in /home/coding/NEEDLE
 succeeded in 0ms:
tests/integration_tests.rs:1890:            || final_line.contains("startup aborted")
src/worker/mod.rs:1128:            eprintln!("ERROR: Configured agent adapter '{adapter_name}' was not found.");
src/worker/mod.rs:1143:            eprintln!("Underlying error: {e}");
src/worker/mod.rs:1144:            bail!("adapter '{adapter_name}' not found — startup aborted");
src/worker/mod.rs:3850:                    "adapter '{}' not found in any of the expected configuration directories",

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..c4c73aad7001718a0e5770326849ab06bb2ac94b
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

exec
/bin/bash -lc "sed -n '1840,1910p' tests/integration_tests.rs; sed -n '1098,1150p' src/worker/mod.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
        stderr_output.contains("1.") ||  // Numbered lists
        stderr_output.contains("\n\n"), // At minimum, multi-paragraph structure
        "error message should be structured with bullets or multiple paragraphs for readability. \
         Got stderr:\n{}",
        stderr_output
    );

    // ASSERTION 8: Error message must indicate the problem occurred at startup
    // This prevents users from thinking it's a runtime issue
    assert!(
        stderr_output.contains("startup")
            || stderr_output.contains("Startup")
            || stderr_output.contains("boot")
            || stderr_output.contains("initialization")
            || stderr_output.contains("aborting"),
        "error message should indicate the problem occurred at startup/boot time. \
         Got stderr:\n{}",
        stderr_output
    );

    // ASSERTION 9: Error message must prevent bead claiming with invalid config
    // This is critical - the error should explain WHY startup is aborting
    assert!(
        stderr_output.contains("claiming") ||
        stderr_output.contains("prevent") ||
        stderr_output.contains("invalid adapter") ||
        stderr_output.contains("invalid configuration") ||
        stderr_output.contains("aborting to prevent"),
        "error message should explain why startup is aborting (to prevent claiming beads with bad config). \
         Got stderr:\n{}",
        stderr_output
    );

    // ASSERTION 10: Verify the error message is substantial and multi-line
    // A good error message should be detailed, not a single line
    let line_count = stderr_output.lines().count();
    assert!(
        line_count >= 5,
        "error message should be substantial (at least 5 lines), but got {} lines. \
         Got stderr:\n{}",
        line_count,
        stderr_output
    );

    // ASSERTION 11: Verify the error message ends with a clear error statement
    // The final line should summarize the problem
    let final_line = stderr_output.lines().last().unwrap_or("");
    assert!(
        final_line.contains("Error:")
            || final_line.contains("error:")
            || final_line.contains("startup aborted")
            || final_line.contains("not found"),
        "error message should end with a clear error summary. Final line: '{}'. \
         Got stderr:\n{}",
        final_line,
        stderr_output
    );

    // ASSERTION 12: Error message must include actionable configuration directory guidance
    // This is distinct from path checking - it validates the presence of remediation language
    // that explicitly directs users to check their configuration directory
    assert!(
        stderr_output.contains("check your configuration directory") ||
        stderr_output.contains("check your config") ||
        stderr_output.contains("check the config") ||
        stderr_output.contains("configuration directory") ||
        (stderr_output.contains("check") && stderr_output.contains("config")),
        "error message should include actionable guidance about checking the configuration directory. \
         Got stderr:\n{}",
        stderr_output
    );
                    }
                }
                Err(e) => {
                    tracing::debug!(
                        error = %e,
                        "failed to detect supervisor presence, skipping idle_action validation"
                    );
                }
            }
        }

        self.telemetry.emit(EventKind::InitStepCompleted {
            step: "idle_action_validation".to_string(),
            duration_ms: step_start.elapsed().as_millis() as u64,
        })?;

        // Check boot timeout before each step
        self.check_boot_timeout(BOOT_TIMEOUT_SECS)?;

        // Step: Adapter preflight
        self.telemetry.emit(EventKind::InitStepStarted {
            step: "adapter_preflight".to_string(),
        })?;
        let step_start = Instant::now();
        // Validate that the configured adapter exists before entering the main claim loop.
        // This ensures adapter configuration errors are caught early during startup rather
        // than after a bead has been claimed, preventing orphaned in_progress beads.
        if let Err(e) = self.resolve_adapter() {
            // Provide clear, actionable error message for missing adapter
            let adapter_name = &self.config.agent.default;
            eprintln!("ERROR: Configured agent adapter '{adapter_name}' was not found.");
            eprintln!("Startup is aborting to prevent claiming beads with an invalid adapter configuration.");
            eprintln!();
            eprintln!("To fix this, check one of these locations for your adapter configuration:");
            eprintln!("  - ~/.needle/agents/{adapter_name}.yaml");
            eprintln!("  - ~/.local/bin/claude-config/agents/{adapter_name}/config.json");
            eprintln!("  - ~/.config/needle/adapters/{adapter_name}.yaml");
            eprintln!();
            eprintln!("Common causes:");
            eprintln!("  - Adapter file does not exist or is in a different location");
            eprintln!("  - Incorrect adapter name specified in agent.default config");
            eprintln!(
                "  - Adapter file exists but is missing required fields (provider, model, etc.)"
            );
            eprintln!();
            eprintln!("Underlying error: {e}");
            bail!("adapter '{adapter_name}' not found — startup aborted");
        }
        self.telemetry.emit(EventKind::InitStepCompleted {
            step: "adapter_preflight".to_string(),
            duration_ms: step_start.elapsed().as_millis() as u64,
        })?;


diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..c4c73aad7001718a0e5770326849ab06bb2ac94b
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

codex
One additional boundary issue is that boot preflight currently prints the detailed routing error but returns a generic “default adapter not found” error to callers. I’m preserving the existing startup wording while returning the original routed model/pattern/path context too, so both CLI stderr and the propagated failure are loud and actionable.
apply patch
patch: completed
/home/coding/NEEDLE/src/worker/mod.rs
diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..ef7da38d3e914b951334dddcdd7524794573d6f7
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..ef7da38d3e914b951334dddcdd7524794573d6f7
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),

exec
/bin/bash -lc "rustfmt --check --edition 2021 src/worker/mod.rs; git status --short; git diff --stat; git diff -- src/worker/mod.rs | sed -n '1,220p'" in /home/coding/NEEDLE
 succeeded in 0ms:
 M .beads/checkpoint/current.json
 M .beads/checkpoint/forensic.jsonl
 M .beads/checkpoint/previous.json
 M .needle-predispatch-sha
 M src/strand/mod.rs
 M src/telemetry/mod.rs
 M src/upgrade/mod.rs
 M src/worker/mod.rs
 M tests/integration_tests.rs
?? --help
?? .beads/checkpoint/objects/gen-0c5b2aa96fd30c9c372032e85b1f0a8e.jsonl
?? .beads/checkpoint/objects/gen-12ae6dad60bacdcab79be5949e96297b.jsonl
?? .beads/checkpoint/objects/gen-13ec9756a8aeae3f82a04cb619367129.jsonl
?? .beads/checkpoint/objects/gen-1829989699bd2680c2327b6115596b39.jsonl
?? .beads/checkpoint/objects/gen-2863b71f269d1415bb231bc00ed7cecf.jsonl
?? .beads/checkpoint/objects/gen-3513ca656738071a17843102b1ea7f58.jsonl
?? .beads/checkpoint/objects/gen-374da8575d2392f9d352de501f49d768.jsonl
?? .beads/checkpoint/objects/gen-3921b7a3c493a345bb0bfef34ee7e1d5.jsonl
?? .beads/checkpoint/objects/gen-40e94b5b41861e8139afbc8b9e4fb5d0.jsonl
?? .beads/checkpoint/objects/gen-48b926f4bc6b1214b763ee3aef3b435a.jsonl
?? .beads/checkpoint/objects/gen-5c6a26aaf97a0fe8ade3b9cfd8101796.jsonl
?? .beads/checkpoint/objects/gen-d4b55a2ebb6446b39956a9704b5fe6df.jsonl
?? .beads/checkpoint/objects/gen-fcc39a31ae90434c93dcfbd5deb37f36.jsonl
?? .beads/decisions/needle-ci-b5r9w-failure-logs.txt
?? .beads/events.jsonl
?? .beads/heartbeats.jsonl
?? .beads/traces/
?? .crates.toml
?? .crates2.json
?? docs/checkpoint-commit-workflow.md
?? docs/design/timeout-decomposition-types.md
?? docs/process-spawning-test-catalog.md
?? docs/research-mitosis-implementation-and-failure-semantics.md
?? docs/research/mitosis-implementation-and-failure-semantics.md
?? docs/test-isolation-cross-reference-checklist.md
?? failing_tests_list.txt
?? full_benchmark.txt
?? needle-ci-podgc-override.yml
?? needle-ci-zpx54-build-logs.txt
?? src/strand/resolve.rs
?? test-exit-code.txt
?? test-failure-analysis-report.md
?? test-failures-bf-3ksm3.md
?? test-results.txt
?? test_full_output.log
?? test_p95_stderr.txt
?? test_p95_stdout.txt
?? test_template_rendering.rs
?? test_trace_full.txt
?? tests/github_release_upgrade_regression.rs
?? tests/hard_timeout_tests.rs
?? tests/process_guard.rs
?? tests/test_mend_stale_assignee.rs
 .beads/checkpoint/current.json   |   20 +-
 .beads/checkpoint/forensic.jsonl |   83 ++-
 .beads/checkpoint/previous.json  |   20 +-
 .needle-predispatch-sha          |    2 +-
 src/strand/mod.rs                |    1 +
 src/telemetry/mod.rs             |    3 +
 src/upgrade/mod.rs               |   44 +-
 src/worker/mod.rs                |    2 +-
 tests/integration_tests.rs       | 1028 ++++++++++++++++++++++++++++++++++++++
 9 files changed, 1164 insertions(+), 39 deletions(-)
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 73d5120..ef7da38 100644
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1141,7 +1141,7 @@ impl Worker {
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..ef7da38d3e914b951334dddcdd7524794573d6f7
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),

exec
/bin/bash -lc "sed -n '3830,3915p' src/worker/mod.rs; git diff -- src/worker/mod.rs; git show HEAD:src/worker/mod.rs | sed -n '3830,3915p'" in /home/coding/NEEDLE
 succeeded in 0ms:
            self.current_workspace = ws.to_path_buf();
        }
        self.state = to;
        Ok(())
    }

    /// Resolve the agent adapter from config with model-based routing.
    fn resolve_adapter(&self) -> Result<crate::dispatch::AgentAdapter> {
        let bead_id = self.current_bead.as_ref().map(|b| b.id.clone());

        // Start with the configured default adapter.
        let default_adapter_name = &self.config.agent.default;

        // Resolve the default adapter to get its model (if any).
        let default_adapter = self
            .dispatcher
            .adapter(default_adapter_name)
            .cloned()
            .ok_or_else(|| {
                anyhow::anyhow!(
                    "adapter '{}' not found in any of the expected configuration directories",
                    default_adapter_name
                )
            })?;

        // Apply routing rules if configured.
        let (chosen_adapter_name, matched_rule) = self.apply_routing_rules(&default_adapter)?;

        // Emit routing decision telemetry on every routing decision.
        if let Some(id) = bead_id {
            let model = default_adapter
                .model
                .clone()
                .unwrap_or_else(|| "unknown".to_string());
            self.telemetry.emit(EventKind::RoutingDecision {
                bead_id: id,
                model,
                matched_rule: matched_rule.clone(),
                chosen_adapter: chosen_adapter_name.clone(),
            })?;
        }

        // Resolve the chosen adapter.
        let adapter = self.dispatcher.adapter(&chosen_adapter_name)
            .cloned()
            .ok_or_else(|| anyhow::anyhow!(
                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
                chosen_adapter_name,
                default_adapter.model.as_deref().unwrap_or("unknown"),
                matched_rule
            ))?;

        Ok(adapter)
    }

    /// Apply routing rules to determine the final adapter.
    ///
    /// Returns (chosen_adapter_name, matched_rule_pattern).
    /// If no routing rules match, returns (default_adapter_name, "default").
    /// When strict mode is enabled and no rule matches, returns an error.
    fn apply_routing_rules(
        &self,
        default_adapter: &crate::dispatch::AgentAdapter,
    ) -> Result<(String, String)> {
        let routing_config = match &self.config.agent.routing {
            Some(r) if !r.rules.is_empty() => r,
            _ => return Ok((default_adapter.name.clone(), "default".to_string())),
        };

        let model_name = default_adapter.model.as_deref().unwrap_or("");

        // Determine the default adapter name to use.
        let default_adapter_name = routing_config
            .default_adapter
            .as_ref()
            .unwrap_or(&default_adapter.name);

        // Use the routing module's match_adapter_with_pattern function to get both adapter and pattern.
        match routing::match_adapter_with_pattern(
            model_name,
            &routing_config.rules,
            default_adapter_name,
        ) {
            Some((adapter_name, matched_pattern)) => {
                // Determine if this was a rule match or default fallback.
                // If matched_pattern is "default", it means no rule matched.
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 73d5120..ef7da38 100644
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1141,7 +1141,7 @@ impl Worker {
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
            self.current_workspace = ws.to_path_buf();
        }
        self.state = to;
        Ok(())
    }

    /// Resolve the agent adapter from config with model-based routing.
    fn resolve_adapter(&self) -> Result<crate::dispatch::AgentAdapter> {
        let bead_id = self.current_bead.as_ref().map(|b| b.id.clone());

        // Start with the configured default adapter.
        let default_adapter_name = &self.config.agent.default;

        // Resolve the default adapter to get its model (if any).
        let default_adapter = self
            .dispatcher
            .adapter(default_adapter_name)
            .cloned()
            .ok_or_else(|| {
                anyhow::anyhow!(
                    "adapter '{}' not found in any of the expected configuration directories",
                    default_adapter_name
                )
            })?;

        // Apply routing rules if configured.
        let (chosen_adapter_name, matched_rule) = self.apply_routing_rules(&default_adapter)?;

        // Emit routing decision telemetry on every routing decision.
        if let Some(id) = bead_id {
            let model = default_adapter
                .model
                .clone()
                .unwrap_or_else(|| "unknown".to_string());
            self.telemetry.emit(EventKind::RoutingDecision {
                bead_id: id,
                model,
                matched_rule: matched_rule.clone(),
                chosen_adapter: chosen_adapter_name.clone(),
            })?;
        }

        // Resolve the chosen adapter.
        let adapter = self.dispatcher.adapter(&chosen_adapter_name)
            .cloned()
            .ok_or_else(|| anyhow::anyhow!(
                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
                chosen_adapter_name,
                default_adapter.model.as_deref().unwrap_or("unknown"),
                matched_rule
            ))?;

        Ok(adapter)
    }

    /// Apply routing rules to determine the final adapter.
    ///
    /// Returns (chosen_adapter_name, matched_rule_pattern).
    /// If no routing rules match, returns (default_adapter_name, "default").
    /// When strict mode is enabled and no rule matches, returns an error.
    fn apply_routing_rules(
        &self,
        default_adapter: &crate::dispatch::AgentAdapter,
    ) -> Result<(String, String)> {
        let routing_config = match &self.config.agent.routing {
            Some(r) if !r.rules.is_empty() => r,
            _ => return Ok((default_adapter.name.clone(), "default".to_string())),
        };

        let model_name = default_adapter.model.as_deref().unwrap_or("");

        // Determine the default adapter name to use.
        let default_adapter_name = routing_config
            .default_adapter
            .as_ref()
            .unwrap_or(&default_adapter.name);

        // Use the routing module's match_adapter_with_pattern function to get both adapter and pattern.
        match routing::match_adapter_with_pattern(
            model_name,
            &routing_config.rules,
            default_adapter_name,
        ) {
            Some((adapter_name, matched_pattern)) => {
                // Determine if this was a rule match or default fallback.
                // If matched_pattern is "default", it means no rule matched.

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..ef7da38d3e914b951334dddcdd7524794573d6f7
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),

exec
/bin/bash -lc "rg -n \"routed_yaml|expected_yaml|missing its YAML|matched pattern\" src/worker/mod.rs || true; git diff --numstat -- src/worker/mod.rs; git diff -- src/worker/mod.rs | sed -n '1,100p'" in /home/coding/NEEDLE
 succeeded in 0ms:
1	1	src/worker/mod.rs
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 73d5120..ef7da38 100644
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1141,7 +1141,7 @@ impl Worker {
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..ef7da38d3e914b951334dddcdd7524794573d6f7
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),

codex
A concurrent worker rewrote `src/worker/mod.rs` after the first patch, removing the routing check and test while leaving the boot-message edit. I’m reapplying the task patch now and will immediately isolate/stage its exact hunks; I won’t overwrite the other worker’s changes.
apply patch
patch: completed
/home/coding/NEEDLE/src/worker/mod.rs
diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

exec
/bin/bash -lc "git diff -- src/worker/mod.rs | sed -n '1,260p'; rg -n \"routed_yaml|missing its YAML|preflight failed\" src/worker/mod.rs" in /home/coding/NEEDLE
 succeeded in 0ms:
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 73d5120..4ac00ba 100644
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1141,7 +1141,7 @@ impl Worker {
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@ impl Worker {
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4419,6 +4446,38 @@ mod tests {
         assert_eq!(adapter.name, "claude-sonnet");
     }
 
+    #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
     #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
1144:            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
3889:                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
4450:    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

exec
/bin/bash -lc "git add src/worker/mod.rs && git diff --cached --check && git diff --cached --stat && git diff --cached -- src/worker/mod.rs | sed -n '1,260p'" in /home/coding/NEEDLE
 succeeded in 0ms:
 src/worker/mod.rs | 63 +++++++++++++++++++++++++++++++++++++++++++++++++++++--
 1 file changed, 61 insertions(+), 2 deletions(-)
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 73d5120..4ac00ba 100644
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1141,7 +1141,7 @@ impl Worker {
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@ impl Worker {
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4419,6 +4446,38 @@ mod tests {
         assert_eq!(adapter.name, "claude-sonnet");
     }
 
+    #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
     #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

codex
The staged diff is now isolated to `src/worker/mod.rs` and contains only the routed YAML validation, detailed propagated error, and focused unit test; unrelated dirty files remain unstaged. The clean-snapshot verification was also blocked by an existing `OutcomeHandler` trait-type error in `HEAD`, so I’m recording that limitation and proceeding with the scoped commit.
exec
/bin/bash -lc 'git status --short && git commit -m "fix(needle-1bd77af2): fail on missing routed adapter YAML"' in /home/coding/NEEDLE
 succeeded in 0ms:
 M .beads/checkpoint/current.json
 M .beads/checkpoint/forensic.jsonl
 M .beads/checkpoint/previous.json
 M .needle-predispatch-sha
 M src/outcome/mod.rs
 M src/strand/mod.rs
 M src/upgrade/mod.rs
M  src/worker/mod.rs
 M tests/integration_tests.rs
?? --help
?? .beads/checkpoint/objects/gen-0c5b2aa96fd30c9c372032e85b1f0a8e.jsonl
?? .beads/checkpoint/objects/gen-12ae6dad60bacdcab79be5949e96297b.jsonl
?? .beads/checkpoint/objects/gen-13ec9756a8aeae3f82a04cb619367129.jsonl
?? .beads/checkpoint/objects/gen-1829989699bd2680c2327b6115596b39.jsonl
?? .beads/checkpoint/objects/gen-2863b71f269d1415bb231bc00ed7cecf.jsonl
?? .beads/checkpoint/objects/gen-3513ca656738071a17843102b1ea7f58.jsonl
?? .beads/checkpoint/objects/gen-374da8575d2392f9d352de501f49d768.jsonl
?? .beads/checkpoint/objects/gen-3921b7a3c493a345bb0bfef34ee7e1d5.jsonl
?? .beads/checkpoint/objects/gen-40e94b5b41861e8139afbc8b9e4fb5d0.jsonl
?? .beads/checkpoint/objects/gen-48b926f4bc6b1214b763ee3aef3b435a.jsonl
?? .beads/checkpoint/objects/gen-5c6a26aaf97a0fe8ade3b9cfd8101796.jsonl
?? .beads/checkpoint/objects/gen-d4b55a2ebb6446b39956a9704b5fe6df.jsonl
?? .beads/checkpoint/objects/gen-fcc39a31ae90434c93dcfbd5deb37f36.jsonl
?? .beads/decisions/needle-ci-b5r9w-failure-logs.txt
?? .beads/events.jsonl
?? .beads/heartbeats.jsonl
?? .beads/traces/
?? .crates.toml
?? .crates2.json
?? docs/checkpoint-commit-workflow.md
?? docs/design/timeout-decomposition-types.md
?? docs/process-spawning-test-catalog.md
?? docs/research-mitosis-implementation-and-failure-semantics.md
?? docs/research/mitosis-implementation-and-failure-semantics.md
?? docs/test-isolation-cross-reference-checklist.md
?? failing_tests_list.txt
?? full_benchmark.txt
?? needle-ci-podgc-override.yml
?? needle-ci-zpx54-build-logs.txt
?? src/strand/resolve.rs
?? test-exit-code.txt
?? test-failure-analysis-report.md
?? test-failures-bf-3ksm3.md
?? test-results.txt
?? test_full_output.log
?? test_p95_stderr.txt
?? test_p95_stdout.txt
?? test_template_rendering.rs
?? test_trace_full.txt
?? tests/github_release_upgrade_regression.rs
?? tests/hard_timeout_tests.rs
?? tests/process_guard.rs
?? tests/test_mend_stale_assignee.rs
[main 620c709] fix(needle-1bd77af2): fail on missing routed adapter YAML
 1 file changed, 61 insertions(+), 2 deletions(-)

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

exec
/bin/bash -lc 'git push origin main' in /home/coding/NEEDLE
 exited 1 in 457ms:
To https://git.ardenone.com/jedarden/NEEDLE.git
 ! [rejected]        main -> main (fetch first)
error: failed to push some refs to 'https://git.ardenone.com/jedarden/NEEDLE.git'
hint: Updates were rejected because the remote contains work that you do not
hint: have locally. This is usually caused by another repository pushing to
hint: the same ref. If you want to integrate the remote changes, use
hint: 'git pull' before pushing again.
hint: See the 'Note about fast-forwards' in 'git push --help' for details.

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

codex
The scoped commit succeeded as `620c709`, but the push was rejected because another worker advanced `origin/main`. I’ll fetch the remote, inspect the divergence, then integrate it without force-pushing and while preserving the shared worktree’s unrelated edits.
exec
/bin/bash -lc "git fetch origin main && printf '%s\\n' '--- divergence ---' && git rev-list --left-right --count HEAD...origin/main && printf '%s\\n' '--- remote commits ---' && git log --oneline --decorate -8 origin/main && printf '%s\\n' '--- local task commit ---' && git show --stat --oneline HEAD" in /home/coding/NEEDLE
 succeeded in 861ms:
From https://git.ardenone.com/jedarden/NEEDLE
 * branch            main       -> FETCH_HEAD
   48e167a..9e4ba8e  main       -> origin/main
--- divergence ---
3	2
--- remote commits ---
9e4ba8e (origin/main) fix(needle-2cb3c619): capture cargo test launch timestamp
48e167a fix(needle-ea87796e): include active root objects in checkpoint commits
f45a8c3 fix(needle-ea87796e): ensure checkpoint commits include active root objects
82404c5 beads: close needle-5359b52a
f05a37d Merge origin/main: preserve concurrent work
627f3d2 test(needle-4b602eed): strengthen file sink coverage
216f611 fix(needle-5359b52a): track current workspace in telemetry
ff05fd3 fix(needle-5359b52a): track current workspace in telemetry
--- local task commit ---
620c709 fix(needle-1bd77af2): fail on missing routed adapter YAML
 src/worker/mod.rs | 63 +++++++++++++++++++++++++++++++++++++++++++++++++++++--
 1 file changed, 61 insertions(+), 2 deletions(-)

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

exec
/bin/bash -lc "git log --oneline --decorate --left-right HEAD...origin/main | sed -n '1,20p'; git merge-base HEAD origin/main; git status --short | sed -n '1,80p'" in /home/coding/NEEDLE
 succeeded in 0ms:
< 620c709 (HEAD -> main) fix(needle-1bd77af2): fail on missing routed adapter YAML
> 9e4ba8e (origin/main) fix(needle-2cb3c619): capture cargo test launch timestamp
< 9f7d549 fix(needle-2cb3c619): capture cargo test launch timestamp
< 8edda30 fix(needle-ea87796e): include active root objects in checkpoint commits
> 48e167a fix(needle-ea87796e): include active root objects in checkpoint commits
f45a8c3f6f8f2183fad6eb8281f036657292ce96
 M .beads/checkpoint/current.json
 M .beads/checkpoint/forensic.jsonl
 M .beads/checkpoint/previous.json
 M .needle-predispatch-sha
 M src/outcome/mod.rs
 M src/strand/mod.rs
 M src/upgrade/mod.rs
 M tests/integration_tests.rs
?? --help
?? .beads/checkpoint/objects/gen-0c5b2aa96fd30c9c372032e85b1f0a8e.jsonl
?? .beads/checkpoint/objects/gen-12ae6dad60bacdcab79be5949e96297b.jsonl
?? .beads/checkpoint/objects/gen-13ec9756a8aeae3f82a04cb619367129.jsonl
?? .beads/checkpoint/objects/gen-1829989699bd2680c2327b6115596b39.jsonl
?? .beads/checkpoint/objects/gen-2863b71f269d1415bb231bc00ed7cecf.jsonl
?? .beads/checkpoint/objects/gen-3513ca656738071a17843102b1ea7f58.jsonl
?? .beads/checkpoint/objects/gen-374da8575d2392f9d352de501f49d768.jsonl
?? .beads/checkpoint/objects/gen-3921b7a3c493a345bb0bfef34ee7e1d5.jsonl
?? .beads/checkpoint/objects/gen-40e94b5b41861e8139afbc8b9e4fb5d0.jsonl
?? .beads/checkpoint/objects/gen-48b926f4bc6b1214b763ee3aef3b435a.jsonl
?? .beads/checkpoint/objects/gen-5c6a26aaf97a0fe8ade3b9cfd8101796.jsonl
?? .beads/checkpoint/objects/gen-d4b55a2ebb6446b39956a9704b5fe6df.jsonl
?? .beads/checkpoint/objects/gen-fcc39a31ae90434c93dcfbd5deb37f36.jsonl
?? .beads/decisions/needle-ci-b5r9w-failure-logs.txt
?? .beads/events.jsonl
?? .beads/heartbeats.jsonl
?? .beads/traces/
?? .crates.toml
?? .crates2.json
?? docs/checkpoint-commit-workflow.md
?? docs/design/timeout-decomposition-types.md
?? docs/process-spawning-test-catalog.md
?? docs/research-mitosis-implementation-and-failure-semantics.md
?? docs/research/mitosis-implementation-and-failure-semantics.md
?? docs/test-isolation-cross-reference-checklist.md
?? failing_tests_list.txt
?? full_benchmark.txt
?? needle-ci-podgc-override.yml
?? needle-ci-zpx54-build-logs.txt
?? src/strand/resolve.rs
?? test-exit-code.txt
?? test-failure-analysis-report.md
?? test-failures-bf-3ksm3.md
?? test-results.txt
?? test_full_output.log
?? test_p95_stderr.txt
?? test_p95_stdout.txt
?? test_template_rendering.rs
?? test_trace_full.txt
?? tests/github_release_upgrade_regression.rs
?? tests/hard_timeout_tests.rs
?? tests/process_guard.rs
?? tests/test_mend_stale_assignee.rs

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

exec
/bin/bash -lc 'git rebase --autostash origin/main' in /home/coding/NEEDLE
 succeeded in 50ms:
Rebasing (1/1)Applied autostash.
Successfully rebased and updated refs/heads/main.

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

exec
/bin/bash -lc "git status --short | sed -n '1,100p'; printf '%s\\n' '--- log ---'; git log --oneline --decorate -6; printf '%s\\n' '--- task diff ---'; git show --stat --oneline HEAD" in /home/coding/NEEDLE
 succeeded in 0ms:
 M .beads/checkpoint/current.json
 M .beads/checkpoint/forensic.jsonl
 M .beads/checkpoint/previous.json
 M .needle-predispatch-sha
 M src/outcome/mod.rs
 M src/strand/mod.rs
 M src/telemetry/mod.rs
 M src/upgrade/mod.rs
 M tests/integration_tests.rs
?? --help
?? .beads/checkpoint/objects/gen-0c5b2aa96fd30c9c372032e85b1f0a8e.jsonl
?? .beads/checkpoint/objects/gen-12ae6dad60bacdcab79be5949e96297b.jsonl
?? .beads/checkpoint/objects/gen-13ec9756a8aeae3f82a04cb619367129.jsonl
?? .beads/checkpoint/objects/gen-1829989699bd2680c2327b6115596b39.jsonl
?? .beads/checkpoint/objects/gen-2863b71f269d1415bb231bc00ed7cecf.jsonl
?? .beads/checkpoint/objects/gen-3513ca656738071a17843102b1ea7f58.jsonl
?? .beads/checkpoint/objects/gen-374da8575d2392f9d352de501f49d768.jsonl
?? .beads/checkpoint/objects/gen-3921b7a3c493a345bb0bfef34ee7e1d5.jsonl
?? .beads/checkpoint/objects/gen-40e94b5b41861e8139afbc8b9e4fb5d0.jsonl
?? .beads/checkpoint/objects/gen-48b926f4bc6b1214b763ee3aef3b435a.jsonl
?? .beads/checkpoint/objects/gen-5c6a26aaf97a0fe8ade3b9cfd8101796.jsonl
?? .beads/checkpoint/objects/gen-d4b55a2ebb6446b39956a9704b5fe6df.jsonl
?? .beads/checkpoint/objects/gen-fcc39a31ae90434c93dcfbd5deb37f36.jsonl
?? .beads/decisions/needle-ci-b5r9w-failure-logs.txt
?? .beads/events.jsonl
?? .beads/heartbeats.jsonl
?? .beads/traces/
?? .crates.toml
?? .crates2.json
?? docs/checkpoint-commit-workflow.md
?? docs/design/timeout-decomposition-types.md
?? docs/process-spawning-test-catalog.md
?? docs/research-mitosis-implementation-and-failure-semantics.md
?? docs/research/mitosis-implementation-and-failure-semantics.md
?? docs/test-isolation-cross-reference-checklist.md
?? failing_tests_list.txt
?? full_benchmark.txt
?? needle-ci-podgc-override.yml
?? needle-ci-zpx54-build-logs.txt
?? src/strand/resolve.rs
?? test-exit-code.txt
?? test-failure-analysis-report.md
?? test-failures-bf-3ksm3.md
?? test-results.txt
?? test_full_output.log
?? test_p95_stderr.txt
?? test_p95_stdout.txt
?? test_template_rendering.rs
?? test_trace_full.txt
?? tests/github_release_upgrade_regression.rs
?? tests/hard_timeout_tests.rs
?? tests/process_guard.rs
?? tests/test_mend_stale_assignee.rs
--- log ---
2c3eae3 (HEAD -> main) fix(needle-1bd77af2): fail on missing routed adapter YAML
9e4ba8e (origin/main) fix(needle-2cb3c619): capture cargo test launch timestamp
48e167a fix(needle-ea87796e): include active root objects in checkpoint commits
f45a8c3 fix(needle-ea87796e): ensure checkpoint commits include active root objects
82404c5 beads: close needle-5359b52a
f05a37d Merge origin/main: preserve concurrent work
--- task diff ---
2c3eae3 fix(needle-1bd77af2): fail on missing routed adapter YAML
 src/worker/mod.rs | 63 +++++++++++++++++++++++++++++++++++++++++++++++++++++--
 1 file changed, 61 insertions(+), 2 deletions(-)

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

exec
/bin/bash -lc 'git push origin main' in /home/coding/NEEDLE
diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

 succeeded in 1201ms:
To https://git.ardenone.com/jedarden/NEEDLE.git
   9e4ba8e..2c3eae3  main -> main

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

codex
The remote advanced commits were integrated via a non-destructive autostash rebase, and the task commit is now pushed successfully as `2c3eae3` on `origin/main`. I’m closing the bead with the implementation and verification limitation recorded; the shared worktree’s unrelated changes remain untouched.
exec
/bin/bash -lc 'bead close needle-1bd77af2 --reason "Added post-routing YAML existence checks for both .yaml and .yml, preventing built-in fallback and returning model/pattern/path context; added a unit test covering a routed built-in name with missing YAML. rustfmt passed for src/worker/mod.rs; cargo test was blocked by pre-existing compile errors outside this task."' in /home/coding/NEEDLE
 succeeded in 0ms:
needle-1bd77af2

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

exec
/bin/bash -lc "bead show needle-1bd77af2 2>&1 | sed -n '1,40p'" in /home/coding/NEEDLE
 succeeded in 0ms:
ID: needle-1bd77af2
Title: Add loud failure for missing routed adapters
Status: Closed
Priority: P2
Revision: 6
Created: 2026-08-14T16:10:50.156676261Z
Updated: 2026-08-17T05:24:57.305485796Z
Description: ## Scope
Error handling when the routing logic selects an adapter that does not exist.

## Implement
- After routing selects an adapter, verify the adapter YAML exists
- If missing, fail dispatch loudly with clear error message (mentions model, matched rule, missing adapter)
- Do NOT fall back to builtin adapters (complements bf-14w and bf-2wi)

## Acceptance criteria
- [ ] Missing adapter YAML = dispatch failure, not silent fallback
- [ ] Error message includes: model name, matched pattern, missing adapter path
- [ ] Unit test: routing to non-existent adapter fails with appropriate error

## Depends on
Child 3 (routing must be wired first)
Assignee: codex-gpt-5.6-luna-xhigh-icg-2
Type: task

exec
/bin/bash -lc "git status --short | sed -n '1,100p'; printf '%s\\n' '--- pushed commit ---'; git log -1 --oneline --decorate; git diff HEAD -- src/worker/mod.rs; git show --check --oneline HEAD" in /home/coding/NEEDLE
 succeeded in 0ms:
 M .beads/checkpoint/current.json
 M .beads/checkpoint/forensic.jsonl
 M .beads/checkpoint/previous.json
 M .needle-predispatch-sha
 M src/outcome/mod.rs
 M src/strand/mod.rs
 M src/telemetry/mod.rs
 M src/upgrade/mod.rs
 M tests/integration_tests.rs
?? --help
?? .beads/checkpoint/objects/gen-0c5b2aa96fd30c9c372032e85b1f0a8e.jsonl
?? .beads/checkpoint/objects/gen-12ae6dad60bacdcab79be5949e96297b.jsonl
?? .beads/checkpoint/objects/gen-13ec9756a8aeae3f82a04cb619367129.jsonl
?? .beads/checkpoint/objects/gen-1829989699bd2680c2327b6115596b39.jsonl
?? .beads/checkpoint/objects/gen-1a5cd8e306acc9d161d24fdac62b6cce.jsonl
?? .beads/checkpoint/objects/gen-2863b71f269d1415bb231bc00ed7cecf.jsonl
?? .beads/checkpoint/objects/gen-3513ca656738071a17843102b1ea7f58.jsonl
?? .beads/checkpoint/objects/gen-374da8575d2392f9d352de501f49d768.jsonl
?? .beads/checkpoint/objects/gen-3921b7a3c493a345bb0bfef34ee7e1d5.jsonl
?? .beads/checkpoint/objects/gen-40e94b5b41861e8139afbc8b9e4fb5d0.jsonl
?? .beads/checkpoint/objects/gen-48b926f4bc6b1214b763ee3aef3b435a.jsonl
?? .beads/checkpoint/objects/gen-5c6a26aaf97a0fe8ade3b9cfd8101796.jsonl
?? .beads/checkpoint/objects/gen-d4b55a2ebb6446b39956a9704b5fe6df.jsonl
?? .beads/checkpoint/objects/gen-fcc39a31ae90434c93dcfbd5deb37f36.jsonl
?? .beads/decisions/needle-ci-b5r9w-failure-logs.txt
?? .beads/events.jsonl
?? .beads/heartbeats.jsonl
?? .beads/traces/
?? .crates.toml
?? .crates2.json
?? docs/checkpoint-commit-workflow.md
?? docs/design/timeout-decomposition-types.md
?? docs/process-spawning-test-catalog.md
?? docs/research-mitosis-implementation-and-failure-semantics.md
?? docs/research/mitosis-implementation-and-failure-semantics.md
?? docs/test-isolation-cross-reference-checklist.md
?? failing_tests_list.txt
?? full_benchmark.txt
?? needle-ci-podgc-override.yml
?? needle-ci-zpx54-build-logs.txt
?? src/strand/resolve.rs
?? test-exit-code.txt
?? test-failure-analysis-report.md
?? test-failures-bf-3ksm3.md
?? test-results.txt
?? test_full_output.log
?? test_p95_stderr.txt
?? test_p95_stdout.txt
?? test_template_rendering.rs
?? test_trace_full.txt
?? tests/github_release_upgrade_regression.rs
?? tests/hard_timeout_tests.rs
?? tests/process_guard.rs
?? tests/test_mend_stale_assignee.rs
--- pushed commit ---
2c3eae3 (HEAD -> main, origin/main) fix(needle-1bd77af2): fail on missing routed adapter YAML
2c3eae3 fix(needle-1bd77af2): fail on missing routed adapter YAML

diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

2026-08-17T05:25:32.700198Z ERROR codex_core::tools::router: error=exec_command failed for `/bin/bash -lc 'rm -rf /home/coding/scratch/needle-missing-route.WaGL5D && test ! -e /home/coding/scratch/needle-missing-route.WaGL5D'`: CreateProcess { message: "Rejected(\"`/bin/bash -lc 'rm -rf /home/coding/scratch/needle-missing-route.WaGL5D && test ! -e /home/coding/scratch/needle-missing-route.WaGL5D'` rejected: rm -f style commands are not permitted. Use a safer approach\")" }
diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

codex
Implemented and pushed as `2c3eae3`.

- Routed adapters now require `<adapters_dir>/<adapter>.yaml` or `.yml`; built-in fallback is prevented.
- Errors include model, matched pattern, and missing path.
- Added focused unit test.
- Bead closed.

`rustfmt` passed for the changed file. Targeted tests were blocked by pre-existing repository compile errors.
diff --git a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
index 73d51204c6c3ac3d59a89fdef35686be4024ca76..399598562496651f943ecfa46a920dbf9893d456
--- a//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
+++ b//home/coding/scratch/needle-missing-route.WaGL5D/src/worker/mod.rs
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up
diff --git a/src/worker/mod.rs b/src/worker/mod.rs
index 8837e3d321038daebe28823844d6de87d40df53a..4ac00ba282f543e15554b27b0d64e6c91457d5bd
--- a/src/worker/mod.rs
+++ b/src/worker/mod.rs
@@ -1080,7 +1080,7 @@
 
         // Warn if idle_action=exit without supervisor supervision
         if self.config.worker.idle_action == crate::types::IdleAction::Exit {
-            match self.health.detect_supervisor_direct() {
+            match self.health.detect_supervisor() {
                 Ok(supervisor_present) => {
                     if !supervisor_present {
                         tracing::warn!(
@@ -1141,7 +1141,7 @@
             );
             eprintln!();
             eprintln!("Underlying error: {e}");
-            bail!("adapter '{adapter_name}' not found — startup aborted");
+            bail!("adapter '{adapter_name}' preflight failed — startup aborted: {e}");
         }
         self.telemetry.emit(EventKind::InitStepCompleted {
             step: "adapter_preflight".to_string(),
@@ -3869,11 +3869,38 @@
             })?;
         }
 
+        // A routed adapter must come from an operator-provided YAML file.
+        // Dispatcher also keeps built-in adapters available for the ordinary
+        // (non-routed) default path, so checking only its adapter map here
+        // would silently turn a missing routed file into a built-in fallback.
+        if matched_rule != "default" {
+            let expected_yaml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yaml"));
+            let expected_yml = self
+                .config
+                .agent
+                .adapters_dir
+                .join(format!("{chosen_adapter_name}.yml"));
+            if !expected_yaml.is_file() && !expected_yml.is_file() {
+                bail!(
+                    "routed adapter '{}' is missing its YAML configuration: model '{}' matched pattern '{}', but no adapter YAML was found at '{}' (also checked '{}')",
+                    chosen_adapter_name,
+                    default_adapter.model.as_deref().unwrap_or("unknown"),
+                    matched_rule,
+                    expected_yaml.display(),
+                    expected_yml.display()
+                );
+            }
+        }
+
         // Resolve the chosen adapter.
         let adapter = self.dispatcher.adapter(&chosen_adapter_name)
             .cloned()
             .ok_or_else(|| anyhow::anyhow!(
-                "routed adapter '{}' not found — routing matched model '{}' with rule '{}', but the adapter does not exist",
+                "routed adapter '{}' could not be loaded — routing matched model '{}' with pattern '{}', but the adapter YAML could not be loaded",
                 chosen_adapter_name,
                 default_adapter.model.as_deref().unwrap_or("unknown"),
                 matched_rule
@@ -4420,6 +4447,38 @@
     }
 
     #[tokio::test]
+    async fn resolve_adapter_fails_when_routed_yaml_is_missing() {
+        use crate::config::{RoutingConfig, RoutingRule};
+
+        let store: Arc<dyn BeadStore> = Arc::new(MockStore::empty());
+        let mut config = valid_test_config();
+        config.agent.routing = Some(RoutingConfig {
+            rules: vec![RoutingRule {
+                match_model: "claude-sonnet-4-6".to_string(),
+                // This is intentionally the name of a built-in adapter. A
+                // routed selection must not use it when its YAML is absent.
+                adapter: "claude-sonnet".to_string(),
+            }],
+            default_adapter: None,
+            strict: false,
+        });
+        let expected_yaml = config.agent.adapters_dir.join("claude-sonnet.yaml");
+        let worker = Worker::new(config, "test-routing-missing-yaml".to_string(), store);
+
+        let error = worker
+            .resolve_adapter()
+            .expect_err("missing routed adapter YAML must fail resolution")
+            .to_string();
+
+        assert!(error.contains("claude-sonnet-4-6"), "error: {error}");
+        assert!(error.contains("matched pattern"), "error: {error}");
+        assert!(
+            error.contains(expected_yaml.to_string_lossy().as_ref()),
+            "error: {error}"
+        );
+    }
+
+    #[tokio::test]
     async fn beads_processed_starts_at_zero() {
         let store = Arc::new(MockStore::empty());
         // Use an isolated workspace home so the registry doesn't pick up

tokens used
158,358
