# Bead bf-37463: Add trace output file writing

## Summary

The trace output file writing functionality was already fully implemented in the codebase. This bead involved fixing compilation errors that were preventing the implementation from being built, not adding new functionality.

## Acceptance Criteria Status

All acceptance criteria were already met by existing code:

### 1. Write captured stdout to .beads/traces/<bead-id>/stdout.txt ✅
**Location**: `src/worker/mod.rs:2133-2139`

The `write_trace_files()` function creates `stdout.txt` and writes the captured stdout content using synchronous I/O.

### 2. Write captured stderr to .beads/traces/<bead-id>/stderr.txt ✅
**Location**: `src/worker/mod.rs:2141-2147`

The `write_trace_files()` function creates `stderr.txt` and writes the captured stderr content using synchronous I/O.

### 3. Create trace directory if it doesn't exist ✅
**Location**: `src/worker/mod.rs:2128-2131`

Uses `fs::create_dir_all()` to create `.beads/traces/<bead-id>/` and any parent directories as needed.

### 4. Handle write errors gracefully with logging ✅
**Location**: `src/worker/mod.rs:2092-2097`

Errors from `write_trace_files()` are caught and logged as warnings but don't fail the bead cycle. The function itself uses `with_context()` for detailed error messages.

## Implementation Details

### Trace File Writing Flow

1. **Execution**: In `do_execute()` (line 2092), after agent process completes
2. **Directory Creation**: `fs::create_dir_all(&trace_dir)` creates the full path
3. **Stdout Write**: Creates `stdout.txt` and writes content with `File::create()` + `write_all()`
4. **Stderr Write**: Creates `stderr.txt` and writes content with `File::create()` + `write_all()`
5. **Success Logging**: Debug log records trace directory and output sizes

### Error Handling

- Filesystem errors return `Result<()>` with detailed context
- Caller logs warnings but continues execution (non-fatal)
- Ensures bead processing completes even if trace writing fails

## Changes Made

This bead primarily fixed compilation errors preventing the existing implementation from building:

### 1. Fixed missing closing brace in `start_watchdog_thread()`
**Location**: `src/worker/mod.rs:586`

Added missing `}` and `self.watchdog_handle = Some(handle);` to properly close the function.

### 2. Fixed variable scope issue with `dispatch_ws`
**Location**: `src/worker/mod.rs:1965-1978`

Moved `dispatch_ws` definition outside the conditional block so it's available when `write_trace_files()` is called.

## Testing

All trace module tests pass (20 tests):
- `trace_capture_creates_directory` - Verifies directory creation
- `trace_capture_writes_stdout` - Verifies stdout.txt writing
- `trace_capture_writes_stderr` - Verifies stderr.txt writing
- `trace_cleanup_*` - Verifies trace retention policy

## Related Modules

- `src/trace/mod.rs` - Core trace capture module with `TraceCapture` struct
- `src/cargo_test.rs` - Uses trace capture for test output
- `src/dispatch/mod.rs` - Integrates trace capture with agent execution
