# bf-670a3: P12.1/12.3 Resource Gating Implementation — Already Complete

## Summary

This bead requested implementation of P12.1/12.3 from plan.md Phase 12 (Fleet Resource Safety):

- **P12.1**: Gate `worker_construction` on system resources
- **P12.3**: Apply the same gate to `needle supervise`'s auto-scale path

## Implementation Status

**Already implemented and committed.** The resource gating was added across two commits:

### Commit bfe5a1b (2026-07-21 23:51)
**Title:** docs(needle-bf-4r31l): state cp/mv on spawn-path binaries unsupported while workers run

Despite the "docs" commit title, this commit implemented **P12.1** by adding resource checks to the CLI launch path:

- Location: `src/cli/mod.rs:890-950`
- Phase: "Phase 2: resource check before worker construction"
- Function: `RateLimiter::check_system_resources_for_launch()`
- Behavior:
  - 120s max wait with exponential backoff (5s initial, cap at 30s)
  - Emits `WorkerLaunchDeferred` telemetry when saturated
  - Fails explicitly with clear error if still saturated after max wait

### Commit e5b32cb (2026-07-22 01:01)
**Title:** feat(needle-bf-4qyh6): implement load-adaptive launch stagger for --count=N batch launches

This commit implemented **P12.2** (load-adaptive launch stagger) AND **P12.3** by adding resource checks to the supervisor's auto-scale spawn path:

- Location: `src/supervisor/mod.rs:340-395`
- Phase: "Phase 1: resource check before worker spawn"
- Function: `RateLimiter::check_system_resources_for_launch()`
- Behavior:
  - 120s max wait with exponential backoff (5s initial, cap at 30s)
  - Emits `SupervisorSpawnFailed` telemetry when saturated
  - Fails explicitly with clear error if still saturated after max wait

## Verification

The implementation matches ADR-008 requirements:

✅ **P12.1**: CLI path calls `check_system_resources_for_launch()` before `worker_construction`
✅ **P12.3**: Supervisor path calls the same function before spawning workers
✅ **Shared implementation**: Both paths use `RateLimiter::check_system_resources_for_launch()`
✅ **Bounded backoff**: 120s max wait, exponential backoff (5s → 30s cap)
✅ **Explicit failure**: Clear error messages with saturation details
✅ **Telemetry**: `WorkerLaunchDeferred` and `SupervisorSpawnFailed` events emitted

## Next Steps

The implementation is complete. This bead can be closed.

Plan.md should be updated to mark P12.1 and P12.3 as complete (they're currently marked as "planned").
