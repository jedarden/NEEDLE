# Bead bf-5397r: Test for Stale-Assignee Starvation

## Status: Already Completed

The test for stale-assignee starvation was already implemented in commit `e788ab0` on 2026-08-01.

## Implementation Summary

The following tests were added to `tests/starvation_tests.rs`:

### 1. Main Test: `pluck_starvation_when_all_beads_have_stale_assignees` (lines 603-700)

This test verifies:
- ✅ Starvation is detected when all open beads have stale assignees
- ✅ `PluckStarvationDetected` telemetry event is emitted with:
  - Correct workspace path
  - `open_count` = 3
  - `excluded_count` = 3 (all beads excluded)
  - `candidate_exclusion_reasons` containing assignee-based exclusions
- ✅ No workspace modifications occur:
  - No claim events
  - No release events
- ✅ All exclusion reasons are assignee-based (format: "assignee:<worker-id>")

### 2. Scenario Builder Extension: `with_stale_assignee_beads()` (lines 144-149)

Extended `StarvationScenarioBuilder` with:
- `stale_assignee_count` field
- `stale_assignees` vector for custom assignee IDs
- `with_stale_assignee_beads()` builder method
- Logic to create beads with assignees set (lines 195-208)

### 3. Scenario Builder Test: `scenario_builder_creates_stale_assignee_beads` (lines 817-849)

Verifies:
- Correct number of stale-assigned beads created
- Assignee IDs match provided list
- Regular open beads still created in correct quantities

## Test Execution

The test requires the `integration` feature:
```bash
cargo test --test starvation_tests --features integration
```

## Verification

All acceptance criteria met:
1. ✅ Unit test exists for stale-assignee starvation
2. ✅ Test verifies `PluckStarvationDetected` telemetry event emission
3. ✅ Test confirms no workspace modifications (no claim/release events)

The implementation is complete and committed.
