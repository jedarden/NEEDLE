# Core Worker Module Unit Test Results

## Task: Test core worker modules (strand, claim, prompt, dispatch, outcome)

### Execution Summary

Ran `cargo test --lib` for each of the 5 core worker modules on 2026-07-14.

### Results

| Module   | Tests Passed | Tests Failed | Duration |
|----------|--------------|--------------|----------|
| strand   | 274          | 0            | 0.35s    |
| claim    | 26           | 0            | 51.61s   |
| prompt   | 43           | 0            | 11.34s   |
| dispatch | 72           | 0            | 13.92s   |
| outcome  | 54           | 0            | 30.01s   |
| **Total**| **469**      | **0**        | **~107s** |

### Modules Tested

1. **strand** (depends on bead_store, types)
   - Tests for explore, knot, mend, pluck, pulse, reflect, splice, unravel, weave strands
   - Waterfall logic and strand orchestration
   - All 274 tests passing

2. **claim** (depends on bead_store, telemetry, types)
   - Bead claiming logic, race conditions, exclusion sets
   - Flock-based workspace locking
   - All 26 tests passing

3. **prompt** (depends on config, types)
   - Template building and variable substitution
   - Template validation and defaults
   - All 43 tests passing

4. **dispatch** (depends on config, telemetry, types)
   - Adapter loading and configuration
   - Prompt rendering and timeout handling
   - Token extraction and dispatch execution
   - All 72 tests passing

5. **outcome** (depends on bead_store, config, telemetry, types)
   - Outcome classification (success, failure, crash, timeout)
   - Bead state transitions and verification gates
   - Canary outcome matching and cargo test metrics
   - All 54 tests passing

### Conclusion

All core worker module unit tests pass without failures. The intermediate dependency chain is functioning correctly.
