# Bead bf-63o2: Heartbeat File Cleanup Function

## Status: Already Implemented

The `cleanup_heartbeat_file` function is already fully implemented in `src/health/mod.rs` at line 895.

## Implementation Details

The function meets all acceptance criteria:

1. **Function signature**: `pub fn cleanup_heartbeat_file(path: &Path) -> Result<()>`
2. **Uses `std::fs::remove_file`**: Core deletion logic at line 896
3. **Returns `Ok(())` if file is removed**: Line 897
4. **Returns `Ok(())` if file doesn't exist**: Lines 898-904 (handles `NotFound` as success for idempotent cleanup)
5. **Returns `Err` if removal fails for unexpected reasons**: Lines 906-914 (propagates errors with context)

## Additional Features

The implementation includes:
- Proper documentation with doc comments and example usage
- Debug logging for idempotent cleanup (file not found)
- Error context with path information
- Comprehensive test coverage (lines 2105-2243):
  - Removing existing files
  - Handling non-existent files
  - Error propagation on failure
  - Permission denied errors
  - Real heartbeat path format

## Integration

The function is:
- Exported from the `health` module
- Used by `HealthMonitor::cleanup_heartbeat_file()` method
- Called in the `stop()` method for cleanup on shutdown
- Called in the `Drop` implementation for cleanup on worker drop

No changes needed - the implementation is complete and production-ready.

## Test Verification (2026-07-29)

All 7 tests passing:
```
test health::tests::cleanup_heartbeat_file_errs_on_other_io_errors ... ok
test health::tests::cleanup_heartbeat_file_errs_on_permission_denied ... ok
test health::tests::cleanup_heartbeat_file_errs_on_removal_failure ... ok
test health::tests::cleanup_heartbeat_file_ok_when_file_missing ... ok
test health::tests::cleanup_heartbeat_file_removes_existing_file ... ok
test health::tests::cleanup_heartbeat_file_removes_symlink ... ok
test health::tests::cleanup_heartbeat_file_with_heartbeat_path ... ok

test result: ok. 7 passed; 0 failed; 0 ignored; 0 measured; 1615 filtered out
```

The function correctly:
- Removes existing heartbeat files
- Returns Ok(()) for non-existent files (idempotent)
- Propagates errors for unexpected failures
- Handles permission denied errors
- Works with symlink files
- Integrates with actual heartbeat path format
