# Bead bf-5617s: Implement core p95 calculation function

## Finding: Function Already Implemented

The `calculate_p95` function was already fully implemented in `src/stats/mod.rs` (lines 399-430) prior to this bead.

## Verification Results

### ✅ Acceptance Criteria Met

1. **Function compiles without errors**
   - `cargo build --lib` succeeds
   - No compilation errors or warnings

2. **Algorithm matches documented approach**
   - Uses linear interpolation method (like Criterion.rs)
   - Implementation follows the approach decided in bead bf-5zrpl
   - Formula: `rank = 0.95 * (n - 1)` with linear interpolation between floor and ceiling values

3. **Basic smoke test passes**
   - All 5 unit tests pass (stats::tests::calculate_p95_*)
   - All 7 integration tests pass (tests/p95_correctness.rs)
   - Known values verified (e.g., `[10, 20, ..., 100]` → p95 = 96)

### Implementation Details

The function includes:
- Edge case handling (empty slice → 0, single element → element)
- Clear code comments explaining the linear interpolation algorithm
- Proper rounding with epsilon for floating-point precision
- Comprehensive documentation with examples
- Works with unsorted input (sorts internally)

### Test Coverage

- Empty/single element edge cases
- Sorted and unsorted input
- Duplicate values
- Large datasets (1000 elements)
- Realistic latency data
- Outlier handling

All tests pass successfully.
